part of '../tg.dart';

/// Predicate Error.
abstract class ErrorBase extends TlConstructor {
  /// Predicate Error constructor.
  const ErrorBase._() : super._();
}

/// Error.
///
/// ID: `c4b9f9bb`.
class Error extends ErrorBase {
  /// Error constructor.
  const Error({
    required this.code,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory Error.deserialize(Uint8List buffer) {
    // final code = _readint(buffer);
    // final text = _readstring(buffer);
    // final result = Error(code: code, text: text,);

    // return result;

    throw Exception();
  }

  /// Code.
  final int code;

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc4b9f9bb);
    buffer.writeInt(code);
    buffer.writeString(text);
  }
}

/// Predicate Input Peer.
abstract class InputPeerBase extends TlConstructor {
  /// Predicate Input Peer constructor.
  const InputPeerBase._() : super._();
}

/// Input Peer Empty.
///
/// ID: `7f3b18ea`.
class InputPeerEmpty extends InputPeerBase {
  /// Input Peer Empty constructor.
  const InputPeerEmpty() : super._();

  /// Deserialize.
  factory InputPeerEmpty.deserialize(Uint8List buffer) {
    // final result = InputPeerEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7f3b18ea);
  }
}

/// Input Peer Self.
///
/// ID: `7da07ec9`.
class InputPeerSelf extends InputPeerBase {
  /// Input Peer Self constructor.
  const InputPeerSelf() : super._();

  /// Deserialize.
  factory InputPeerSelf.deserialize(Uint8List buffer) {
    // final result = InputPeerSelf();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7da07ec9);
  }
}

/// Input Peer Chat.
///
/// ID: `35a95cb9`.
class InputPeerChat extends InputPeerBase {
  /// Input Peer Chat constructor.
  const InputPeerChat({
    required this.chatId,
  }) : super._();

  /// Deserialize.
  factory InputPeerChat.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final result = InputPeerChat(chatId: chatId,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x35a95cb9);
    buffer.writeLong(chatId);
  }
}

/// Input Peer User.
///
/// ID: `dde8a54c`.
class InputPeerUser extends InputPeerBase {
  /// Input Peer User constructor.
  const InputPeerUser({
    required this.userId,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputPeerUser.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputPeerUser(userId: userId, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdde8a54c);
    buffer.writeLong(userId);
    buffer.writeLong(accessHash);
  }
}

/// Input Peer Channel.
///
/// ID: `27bcbbfc`.
class InputPeerChannel extends InputPeerBase {
  /// Input Peer Channel constructor.
  const InputPeerChannel({
    required this.channelId,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputPeerChannel.deserialize(Uint8List buffer) {
    // final channelId = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputPeerChannel(channelId: channelId, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Channel Id.
  final int channelId;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x27bcbbfc);
    buffer.writeLong(channelId);
    buffer.writeLong(accessHash);
  }
}

/// Input Peer User From Message.
///
/// ID: `a87b0a1c`.
class InputPeerUserFromMessage extends InputPeerBase {
  /// Input Peer User From Message constructor.
  const InputPeerUserFromMessage({
    required this.peer,
    required this.msgId,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory InputPeerUserFromMessage.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final userId = _readlong(buffer);
    // final result = InputPeerUserFromMessage(peer: peer, msgId: msgId, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// User Id.
  final int userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa87b0a1c);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeLong(userId);
  }
}

/// Input Peer Channel From Message.
///
/// ID: `bd2a0840`.
class InputPeerChannelFromMessage extends InputPeerBase {
  /// Input Peer Channel From Message constructor.
  const InputPeerChannelFromMessage({
    required this.peer,
    required this.msgId,
    required this.channelId,
  }) : super._();

  /// Deserialize.
  factory InputPeerChannelFromMessage.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final channelId = _readlong(buffer);
    // final result = InputPeerChannelFromMessage(peer: peer, msgId: msgId, channelId: channelId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Channel Id.
  final int channelId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbd2a0840);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeLong(channelId);
  }
}

/// Predicate Input User.
abstract class InputUserBase extends TlConstructor {
  /// Predicate Input User constructor.
  const InputUserBase._() : super._();
}

/// Input User Empty.
///
/// ID: `b98886cf`.
class InputUserEmpty extends InputUserBase {
  /// Input User Empty constructor.
  const InputUserEmpty() : super._();

  /// Deserialize.
  factory InputUserEmpty.deserialize(Uint8List buffer) {
    // final result = InputUserEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb98886cf);
  }
}

/// Input User Self.
///
/// ID: `f7c1b13f`.
class InputUserSelf extends InputUserBase {
  /// Input User Self constructor.
  const InputUserSelf() : super._();

  /// Deserialize.
  factory InputUserSelf.deserialize(Uint8List buffer) {
    // final result = InputUserSelf();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf7c1b13f);
  }
}

/// Input User.
///
/// ID: `f21158c6`.
class InputUser extends InputUserBase {
  /// Input User constructor.
  const InputUser({
    required this.userId,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputUser.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputUser(userId: userId, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf21158c6);
    buffer.writeLong(userId);
    buffer.writeLong(accessHash);
  }
}

/// Input User From Message.
///
/// ID: `1da448e2`.
class InputUserFromMessage extends InputUserBase {
  /// Input User From Message constructor.
  const InputUserFromMessage({
    required this.peer,
    required this.msgId,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory InputUserFromMessage.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final userId = _readlong(buffer);
    // final result = InputUserFromMessage(peer: peer, msgId: msgId, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// User Id.
  final int userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1da448e2);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeLong(userId);
  }
}

/// Predicate Input Contact.
abstract class InputContactBase extends TlConstructor {
  /// Predicate Input Contact constructor.
  const InputContactBase._() : super._();
}

/// Input Phone Contact.
///
/// ID: `f392b7f4`.
class InputPhoneContact extends InputContactBase {
  /// Input Phone Contact constructor.
  const InputPhoneContact({
    required this.clientId,
    required this.phone,
    required this.firstName,
    required this.lastName,
  }) : super._();

  /// Deserialize.
  factory InputPhoneContact.deserialize(Uint8List buffer) {
    // final clientId = _readlong(buffer);
    // final phone = _readstring(buffer);
    // final firstName = _readstring(buffer);
    // final lastName = _readstring(buffer);
    // final result = InputPhoneContact(clientId: clientId, phone: phone, firstName: firstName, lastName: lastName,);

    // return result;

    throw Exception();
  }

  /// Client Id.
  final int clientId;

  /// Phone.
  final String phone;

  /// First Name.
  final String firstName;

  /// Last Name.
  final String lastName;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf392b7f4);
    buffer.writeLong(clientId);
    buffer.writeString(phone);
    buffer.writeString(firstName);
    buffer.writeString(lastName);
  }
}

/// Predicate Input File.
abstract class InputFileBase extends TlConstructor {
  /// Predicate Input File constructor.
  const InputFileBase._() : super._();
}

/// Input File.
///
/// ID: `f52ff27f`.
class InputFile extends InputFileBase {
  /// Input File constructor.
  const InputFile({
    required this.id,
    required this.parts,
    required this.name,
    required this.md5Checksum,
  }) : super._();

  /// Deserialize.
  factory InputFile.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final parts = _readint(buffer);
    // final name = _readstring(buffer);
    // final md5Checksum = _readstring(buffer);
    // final result = InputFile(id: id, parts: parts, name: name, md5Checksum: md5Checksum,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Parts.
  final int parts;

  /// Name.
  final String name;

  /// Md5 Checksum.
  final String md5Checksum;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf52ff27f);
    buffer.writeLong(id);
    buffer.writeInt(parts);
    buffer.writeString(name);
    buffer.writeString(md5Checksum);
  }
}

/// Input File Big.
///
/// ID: `fa4f0bb5`.
class InputFileBig extends InputFileBase {
  /// Input File Big constructor.
  const InputFileBig({
    required this.id,
    required this.parts,
    required this.name,
  }) : super._();

  /// Deserialize.
  factory InputFileBig.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final parts = _readint(buffer);
    // final name = _readstring(buffer);
    // final result = InputFileBig(id: id, parts: parts, name: name,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Parts.
  final int parts;

  /// Name.
  final String name;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfa4f0bb5);
    buffer.writeLong(id);
    buffer.writeInt(parts);
    buffer.writeString(name);
  }
}

/// Predicate Input Media.
abstract class InputMediaBase extends TlConstructor {
  /// Predicate Input Media constructor.
  const InputMediaBase._() : super._();
}

/// Input Media Empty.
///
/// ID: `9664f57f`.
class InputMediaEmpty extends InputMediaBase {
  /// Input Media Empty constructor.
  const InputMediaEmpty() : super._();

  /// Deserialize.
  factory InputMediaEmpty.deserialize(Uint8List buffer) {
    // final result = InputMediaEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9664f57f);
  }
}

/// Input Media Uploaded Photo.
///
/// ID: `1e287d04`.
class InputMediaUploadedPhoto extends InputMediaBase {
  /// Input Media Uploaded Photo constructor.
  const InputMediaUploadedPhoto({
    required this.spoiler,
    required this.file,
    this.stickers,
    this.ttlSeconds,
  }) : super._();

  /// Deserialize.
  factory InputMediaUploadedPhoto.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final spoiler = _readflags.2?true(buffer);
    // final file = _readInputFile(buffer);
    // final stickers = _readflags.0?Vector<InputDocument>(buffer);
    // final ttlSeconds = _readflags.1?int(buffer);
    // final result = InputMediaUploadedPhoto(flags: flags, spoiler: spoiler, file: file, stickers: stickers, ttlSeconds: ttlSeconds,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: spoiler,
      b00: stickers != null,
      b01: ttlSeconds != null,
    );

    return v;
  }

  /// spoiler: bit 2 of flags.2?true
  final bool spoiler;

  /// File.
  final InputFileBase file;

  /// Stickers.
  final List<InputDocumentBase>? stickers;

  /// Ttl Seconds.
  final int? ttlSeconds;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1e287d04);
    buffer.writeInt(flags);
    buffer.writeObject(file);
    final localStickersCopy = stickers;
    if (localStickersCopy != null) {
      buffer.writeVectorObject(localStickersCopy);
    }
    final localTtlSecondsCopy = ttlSeconds;
    if (localTtlSecondsCopy != null) {
      buffer.writeInt(localTtlSecondsCopy);
    }
  }
}

/// Input Media Photo.
///
/// ID: `b3ba0635`.
class InputMediaPhoto extends InputMediaBase {
  /// Input Media Photo constructor.
  const InputMediaPhoto({
    required this.spoiler,
    required this.id,
    this.ttlSeconds,
  }) : super._();

  /// Deserialize.
  factory InputMediaPhoto.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final spoiler = _readflags.1?true(buffer);
    // final id = _readInputPhoto(buffer);
    // final ttlSeconds = _readflags.0?int(buffer);
    // final result = InputMediaPhoto(flags: flags, spoiler: spoiler, id: id, ttlSeconds: ttlSeconds,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: spoiler,
      b00: ttlSeconds != null,
    );

    return v;
  }

  /// spoiler: bit 1 of flags.1?true
  final bool spoiler;

  /// Id.
  final InputPhotoBase id;

  /// Ttl Seconds.
  final int? ttlSeconds;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb3ba0635);
    buffer.writeInt(flags);
    buffer.writeObject(id);
    final localTtlSecondsCopy = ttlSeconds;
    if (localTtlSecondsCopy != null) {
      buffer.writeInt(localTtlSecondsCopy);
    }
  }
}

/// Input Media Geo Point.
///
/// ID: `f9c44144`.
class InputMediaGeoPoint extends InputMediaBase {
  /// Input Media Geo Point constructor.
  const InputMediaGeoPoint({
    required this.geoPoint,
  }) : super._();

  /// Deserialize.
  factory InputMediaGeoPoint.deserialize(Uint8List buffer) {
    // final geoPoint = _readInputGeoPoint(buffer);
    // final result = InputMediaGeoPoint(geoPoint: geoPoint,);

    // return result;

    throw Exception();
  }

  /// Geo Point.
  final InputGeoPointBase geoPoint;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf9c44144);
    buffer.writeObject(geoPoint);
  }
}

/// Input Media Contact.
///
/// ID: `f8ab7dfb`.
class InputMediaContact extends InputMediaBase {
  /// Input Media Contact constructor.
  const InputMediaContact({
    required this.phoneNumber,
    required this.firstName,
    required this.lastName,
    required this.vcard,
  }) : super._();

  /// Deserialize.
  factory InputMediaContact.deserialize(Uint8List buffer) {
    // final phoneNumber = _readstring(buffer);
    // final firstName = _readstring(buffer);
    // final lastName = _readstring(buffer);
    // final vcard = _readstring(buffer);
    // final result = InputMediaContact(phoneNumber: phoneNumber, firstName: firstName, lastName: lastName, vcard: vcard,);

    // return result;

    throw Exception();
  }

  /// Phone Number.
  final String phoneNumber;

  /// First Name.
  final String firstName;

  /// Last Name.
  final String lastName;

  /// Vcard.
  final String vcard;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf8ab7dfb);
    buffer.writeString(phoneNumber);
    buffer.writeString(firstName);
    buffer.writeString(lastName);
    buffer.writeString(vcard);
  }
}

/// Input Media Uploaded Document.
///
/// ID: `5b38c6c1`.
class InputMediaUploadedDocument extends InputMediaBase {
  /// Input Media Uploaded Document constructor.
  const InputMediaUploadedDocument({
    required this.nosoundVideo,
    required this.forceFile,
    required this.spoiler,
    required this.file,
    this.thumb,
    required this.mimeType,
    required this.attributes,
    this.stickers,
    this.ttlSeconds,
  }) : super._();

  /// Deserialize.
  factory InputMediaUploadedDocument.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final nosoundVideo = _readflags.3?true(buffer);
    // final forceFile = _readflags.4?true(buffer);
    // final spoiler = _readflags.5?true(buffer);
    // final file = _readInputFile(buffer);
    // final thumb = _readflags.2?InputFile(buffer);
    // final mimeType = _readstring(buffer);
    // final attributes = _readVector<DocumentAttribute>(buffer);
    // final stickers = _readflags.0?Vector<InputDocument>(buffer);
    // final ttlSeconds = _readflags.1?int(buffer);
    // final result = InputMediaUploadedDocument(flags: flags, nosoundVideo: nosoundVideo, forceFile: forceFile, spoiler: spoiler, file: file, thumb: thumb, mimeType: mimeType, attributes: attributes, stickers: stickers, ttlSeconds: ttlSeconds,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: nosoundVideo,
      b04: forceFile,
      b05: spoiler,
      b02: thumb != null,
      b00: stickers != null,
      b01: ttlSeconds != null,
    );

    return v;
  }

  /// nosound_video: bit 3 of flags.3?true
  final bool nosoundVideo;

  /// force_file: bit 4 of flags.4?true
  final bool forceFile;

  /// spoiler: bit 5 of flags.5?true
  final bool spoiler;

  /// File.
  final InputFileBase file;

  /// Thumb.
  final InputFileBase? thumb;

  /// Mime Type.
  final String mimeType;

  /// Attributes.
  final List<DocumentAttributeBase> attributes;

  /// Stickers.
  final List<InputDocumentBase>? stickers;

  /// Ttl Seconds.
  final int? ttlSeconds;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5b38c6c1);
    buffer.writeInt(flags);
    buffer.writeObject(file);
    final localThumbCopy = thumb;
    if (localThumbCopy != null) {
      buffer.writeObject(localThumbCopy);
    }
    buffer.writeString(mimeType);
    buffer.writeVectorObject(attributes);
    final localStickersCopy = stickers;
    if (localStickersCopy != null) {
      buffer.writeVectorObject(localStickersCopy);
    }
    final localTtlSecondsCopy = ttlSeconds;
    if (localTtlSecondsCopy != null) {
      buffer.writeInt(localTtlSecondsCopy);
    }
  }
}

/// Input Media Document.
///
/// ID: `33473058`.
class InputMediaDocument extends InputMediaBase {
  /// Input Media Document constructor.
  const InputMediaDocument({
    required this.spoiler,
    required this.id,
    this.ttlSeconds,
    this.query,
  }) : super._();

  /// Deserialize.
  factory InputMediaDocument.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final spoiler = _readflags.2?true(buffer);
    // final id = _readInputDocument(buffer);
    // final ttlSeconds = _readflags.0?int(buffer);
    // final query = _readflags.1?string(buffer);
    // final result = InputMediaDocument(flags: flags, spoiler: spoiler, id: id, ttlSeconds: ttlSeconds, query: query,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: spoiler,
      b00: ttlSeconds != null,
      b01: query != null,
    );

    return v;
  }

  /// spoiler: bit 2 of flags.2?true
  final bool spoiler;

  /// Id.
  final InputDocumentBase id;

  /// Ttl Seconds.
  final int? ttlSeconds;

  /// Query.
  final String? query;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x33473058);
    buffer.writeInt(flags);
    buffer.writeObject(id);
    final localTtlSecondsCopy = ttlSeconds;
    if (localTtlSecondsCopy != null) {
      buffer.writeInt(localTtlSecondsCopy);
    }
    final localQueryCopy = query;
    if (localQueryCopy != null) {
      buffer.writeString(localQueryCopy);
    }
  }
}

/// Input Media Venue.
///
/// ID: `c13d1c11`.
class InputMediaVenue extends InputMediaBase {
  /// Input Media Venue constructor.
  const InputMediaVenue({
    required this.geoPoint,
    required this.title,
    required this.address,
    required this.provider,
    required this.venueId,
    required this.venueType,
  }) : super._();

  /// Deserialize.
  factory InputMediaVenue.deserialize(Uint8List buffer) {
    // final geoPoint = _readInputGeoPoint(buffer);
    // final title = _readstring(buffer);
    // final address = _readstring(buffer);
    // final provider = _readstring(buffer);
    // final venueId = _readstring(buffer);
    // final venueType = _readstring(buffer);
    // final result = InputMediaVenue(geoPoint: geoPoint, title: title, address: address, provider: provider, venueId: venueId, venueType: venueType,);

    // return result;

    throw Exception();
  }

  /// Geo Point.
  final InputGeoPointBase geoPoint;

  /// Title.
  final String title;

  /// Address.
  final String address;

  /// Provider.
  final String provider;

  /// Venue Id.
  final String venueId;

  /// Venue Type.
  final String venueType;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc13d1c11);
    buffer.writeObject(geoPoint);
    buffer.writeString(title);
    buffer.writeString(address);
    buffer.writeString(provider);
    buffer.writeString(venueId);
    buffer.writeString(venueType);
  }
}

/// Input Media Photo External.
///
/// ID: `e5bbfe1a`.
class InputMediaPhotoExternal extends InputMediaBase {
  /// Input Media Photo External constructor.
  const InputMediaPhotoExternal({
    required this.spoiler,
    required this.url,
    this.ttlSeconds,
  }) : super._();

  /// Deserialize.
  factory InputMediaPhotoExternal.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final spoiler = _readflags.1?true(buffer);
    // final url = _readstring(buffer);
    // final ttlSeconds = _readflags.0?int(buffer);
    // final result = InputMediaPhotoExternal(flags: flags, spoiler: spoiler, url: url, ttlSeconds: ttlSeconds,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: spoiler,
      b00: ttlSeconds != null,
    );

    return v;
  }

  /// spoiler: bit 1 of flags.1?true
  final bool spoiler;

  /// Url.
  final String url;

  /// Ttl Seconds.
  final int? ttlSeconds;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe5bbfe1a);
    buffer.writeInt(flags);
    buffer.writeString(url);
    final localTtlSecondsCopy = ttlSeconds;
    if (localTtlSecondsCopy != null) {
      buffer.writeInt(localTtlSecondsCopy);
    }
  }
}

/// Input Media Document External.
///
/// ID: `fb52dc99`.
class InputMediaDocumentExternal extends InputMediaBase {
  /// Input Media Document External constructor.
  const InputMediaDocumentExternal({
    required this.spoiler,
    required this.url,
    this.ttlSeconds,
  }) : super._();

  /// Deserialize.
  factory InputMediaDocumentExternal.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final spoiler = _readflags.1?true(buffer);
    // final url = _readstring(buffer);
    // final ttlSeconds = _readflags.0?int(buffer);
    // final result = InputMediaDocumentExternal(flags: flags, spoiler: spoiler, url: url, ttlSeconds: ttlSeconds,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: spoiler,
      b00: ttlSeconds != null,
    );

    return v;
  }

  /// spoiler: bit 1 of flags.1?true
  final bool spoiler;

  /// Url.
  final String url;

  /// Ttl Seconds.
  final int? ttlSeconds;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfb52dc99);
    buffer.writeInt(flags);
    buffer.writeString(url);
    final localTtlSecondsCopy = ttlSeconds;
    if (localTtlSecondsCopy != null) {
      buffer.writeInt(localTtlSecondsCopy);
    }
  }
}

/// Input Media Game.
///
/// ID: `d33f43f3`.
class InputMediaGame extends InputMediaBase {
  /// Input Media Game constructor.
  const InputMediaGame({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory InputMediaGame.deserialize(Uint8List buffer) {
    // final id = _readInputGame(buffer);
    // final result = InputMediaGame(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final InputGameBase id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd33f43f3);
    buffer.writeObject(id);
  }
}

/// Input Media Invoice.
///
/// ID: `8eb5a6d5`.
class InputMediaInvoice extends InputMediaBase {
  /// Input Media Invoice constructor.
  const InputMediaInvoice({
    required this.title,
    required this.description,
    this.photo,
    required this.invoice,
    required this.payload,
    required this.provider,
    required this.providerData,
    this.startParam,
    this.extendedMedia,
  }) : super._();

  /// Deserialize.
  factory InputMediaInvoice.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final title = _readstring(buffer);
    // final description = _readstring(buffer);
    // final photo = _readflags.0?InputWebDocument(buffer);
    // final invoice = _readInvoice(buffer);
    // final payload = _readbytes(buffer);
    // final provider = _readstring(buffer);
    // final providerData = _readDataJSON(buffer);
    // final startParam = _readflags.1?string(buffer);
    // final extendedMedia = _readflags.2?InputMedia(buffer);
    // final result = InputMediaInvoice(flags: flags, title: title, description: description, photo: photo, invoice: invoice, payload: payload, provider: provider, providerData: providerData, startParam: startParam, extendedMedia: extendedMedia,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: photo != null,
      b01: startParam != null,
      b02: extendedMedia != null,
    );

    return v;
  }

  /// Title.
  final String title;

  /// Description.
  final String description;

  /// Photo.
  final InputWebDocumentBase? photo;

  /// Invoice.
  final InvoiceBase invoice;

  /// Payload.
  final Uint8List payload;

  /// Provider.
  final String provider;

  /// Provider Data.
  final DataJSONBase providerData;

  /// Start Param.
  final String? startParam;

  /// Extended Media.
  final InputMediaBase? extendedMedia;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8eb5a6d5);
    buffer.writeInt(flags);
    buffer.writeString(title);
    buffer.writeString(description);
    final localPhotoCopy = photo;
    if (localPhotoCopy != null) {
      buffer.writeObject(localPhotoCopy);
    }
    buffer.writeObject(invoice);
    buffer.writeBytes(payload);
    buffer.writeString(provider);
    buffer.writeObject(providerData);
    final localStartParamCopy = startParam;
    if (localStartParamCopy != null) {
      buffer.writeString(localStartParamCopy);
    }
    final localExtendedMediaCopy = extendedMedia;
    if (localExtendedMediaCopy != null) {
      buffer.writeObject(localExtendedMediaCopy);
    }
  }
}

/// Input Media Geo Live.
///
/// ID: `971fa843`.
class InputMediaGeoLive extends InputMediaBase {
  /// Input Media Geo Live constructor.
  const InputMediaGeoLive({
    required this.stopped,
    required this.geoPoint,
    this.heading,
    this.period,
    this.proximityNotificationRadius,
  }) : super._();

  /// Deserialize.
  factory InputMediaGeoLive.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final stopped = _readflags.0?true(buffer);
    // final geoPoint = _readInputGeoPoint(buffer);
    // final heading = _readflags.2?int(buffer);
    // final period = _readflags.1?int(buffer);
    // final proximityNotificationRadius = _readflags.3?int(buffer);
    // final result = InputMediaGeoLive(flags: flags, stopped: stopped, geoPoint: geoPoint, heading: heading, period: period, proximityNotificationRadius: proximityNotificationRadius,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: stopped,
      b02: heading != null,
      b01: period != null,
      b03: proximityNotificationRadius != null,
    );

    return v;
  }

  /// stopped: bit 0 of flags.0?true
  final bool stopped;

  /// Geo Point.
  final InputGeoPointBase geoPoint;

  /// Heading.
  final int? heading;

  /// Period.
  final int? period;

  /// Proximity Notification Radius.
  final int? proximityNotificationRadius;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x971fa843);
    buffer.writeInt(flags);
    buffer.writeObject(geoPoint);
    final localHeadingCopy = heading;
    if (localHeadingCopy != null) {
      buffer.writeInt(localHeadingCopy);
    }
    final localPeriodCopy = period;
    if (localPeriodCopy != null) {
      buffer.writeInt(localPeriodCopy);
    }
    final localProximityNotificationRadiusCopy = proximityNotificationRadius;
    if (localProximityNotificationRadiusCopy != null) {
      buffer.writeInt(localProximityNotificationRadiusCopy);
    }
  }
}

/// Input Media Poll.
///
/// ID: `0f94e5f1`.
class InputMediaPoll extends InputMediaBase {
  /// Input Media Poll constructor.
  const InputMediaPoll({
    required this.poll,
    this.correctAnswers,
    this.solution,
    this.solutionEntities,
  }) : super._();

  /// Deserialize.
  factory InputMediaPoll.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final poll = _readPoll(buffer);
    // final correctAnswers = _readflags.0?Vector<bytes>(buffer);
    // final solution = _readflags.1?string(buffer);
    // final solutionEntities = _readflags.1?Vector<MessageEntity>(buffer);
    // final result = InputMediaPoll(flags: flags, poll: poll, correctAnswers: correctAnswers, solution: solution, solutionEntities: solutionEntities,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: correctAnswers != null,
      b01: solution != null || solutionEntities != null,
    );

    return v;
  }

  /// Poll.
  final PollBase poll;

  /// Correct Answers.
  final List<Uint8List>? correctAnswers;

  /// Solution.
  final String? solution;

  /// Solution Entities.
  final List<MessageEntityBase>? solutionEntities;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0f94e5f1);
    buffer.writeInt(flags);
    buffer.writeObject(poll);
    final localCorrectAnswersCopy = correctAnswers;
    if (localCorrectAnswersCopy != null) {
      buffer.writeVectorBytes(localCorrectAnswersCopy);
    }
    final localSolutionCopy = solution;
    if (localSolutionCopy != null) {
      buffer.writeString(localSolutionCopy);
    }
    final localSolutionEntitiesCopy = solutionEntities;
    if (localSolutionEntitiesCopy != null) {
      buffer.writeVectorObject(localSolutionEntitiesCopy);
    }
  }
}

/// Input Media Dice.
///
/// ID: `e66fbf7b`.
class InputMediaDice extends InputMediaBase {
  /// Input Media Dice constructor.
  const InputMediaDice({
    required this.emoticon,
  }) : super._();

  /// Deserialize.
  factory InputMediaDice.deserialize(Uint8List buffer) {
    // final emoticon = _readstring(buffer);
    // final result = InputMediaDice(emoticon: emoticon,);

    // return result;

    throw Exception();
  }

  /// Emoticon.
  final String emoticon;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe66fbf7b);
    buffer.writeString(emoticon);
  }
}

/// Input Media Story.
///
/// ID: `89fdd778`.
class InputMediaStory extends InputMediaBase {
  /// Input Media Story constructor.
  const InputMediaStory({
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory InputMediaStory.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final result = InputMediaStory(peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x89fdd778);
    buffer.writeObject(peer);
    buffer.writeInt(id);
  }
}

/// Input Media Web Page.
///
/// ID: `c21b8849`.
class InputMediaWebPage extends InputMediaBase {
  /// Input Media Web Page constructor.
  const InputMediaWebPage({
    required this.forceLargeMedia,
    required this.forceSmallMedia,
    required this.optional,
    required this.url,
  }) : super._();

  /// Deserialize.
  factory InputMediaWebPage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final forceLargeMedia = _readflags.0?true(buffer);
    // final forceSmallMedia = _readflags.1?true(buffer);
    // final optional = _readflags.2?true(buffer);
    // final url = _readstring(buffer);
    // final result = InputMediaWebPage(flags: flags, forceLargeMedia: forceLargeMedia, forceSmallMedia: forceSmallMedia, optional: optional, url: url,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: forceLargeMedia,
      b01: forceSmallMedia,
      b02: optional,
    );

    return v;
  }

  /// force_large_media: bit 0 of flags.0?true
  final bool forceLargeMedia;

  /// force_small_media: bit 1 of flags.1?true
  final bool forceSmallMedia;

  /// optional: bit 2 of flags.2?true
  final bool optional;

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc21b8849);
    buffer.writeInt(flags);
    buffer.writeString(url);
  }
}

/// Predicate Input Chat Photo.
abstract class InputChatPhotoBase extends TlConstructor {
  /// Predicate Input Chat Photo constructor.
  const InputChatPhotoBase._() : super._();
}

/// Input Chat Photo Empty.
///
/// ID: `1ca48f57`.
class InputChatPhotoEmpty extends InputChatPhotoBase {
  /// Input Chat Photo Empty constructor.
  const InputChatPhotoEmpty() : super._();

  /// Deserialize.
  factory InputChatPhotoEmpty.deserialize(Uint8List buffer) {
    // final result = InputChatPhotoEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1ca48f57);
  }
}

/// Input Chat Uploaded Photo.
///
/// ID: `bdcdaec0`.
class InputChatUploadedPhoto extends InputChatPhotoBase {
  /// Input Chat Uploaded Photo constructor.
  const InputChatUploadedPhoto({
    this.file,
    this.video,
    this.videoStartTs,
    this.videoEmojiMarkup,
  }) : super._();

  /// Deserialize.
  factory InputChatUploadedPhoto.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final file = _readflags.0?InputFile(buffer);
    // final video = _readflags.1?InputFile(buffer);
    // final videoStartTs = _readflags.2?double(buffer);
    // final videoEmojiMarkup = _readflags.3?VideoSize(buffer);
    // final result = InputChatUploadedPhoto(flags: flags, file: file, video: video, videoStartTs: videoStartTs, videoEmojiMarkup: videoEmojiMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: file != null,
      b01: video != null,
      b02: videoStartTs != null,
      b03: videoEmojiMarkup != null,
    );

    return v;
  }

  /// File.
  final InputFileBase? file;

  /// Video.
  final InputFileBase? video;

  /// Video Start Ts.
  final double? videoStartTs;

  /// Video Emoji Markup.
  final VideoSizeBase? videoEmojiMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbdcdaec0);
    buffer.writeInt(flags);
    final localFileCopy = file;
    if (localFileCopy != null) {
      buffer.writeObject(localFileCopy);
    }
    final localVideoCopy = video;
    if (localVideoCopy != null) {
      buffer.writeObject(localVideoCopy);
    }
    final localVideoStartTsCopy = videoStartTs;
    if (localVideoStartTsCopy != null) {
      buffer.writeDouble(localVideoStartTsCopy);
    }
    final localVideoEmojiMarkupCopy = videoEmojiMarkup;
    if (localVideoEmojiMarkupCopy != null) {
      buffer.writeObject(localVideoEmojiMarkupCopy);
    }
  }
}

/// Input Chat Photo.
///
/// ID: `8953ad37`.
class InputChatPhoto extends InputChatPhotoBase {
  /// Input Chat Photo constructor.
  const InputChatPhoto({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory InputChatPhoto.deserialize(Uint8List buffer) {
    // final id = _readInputPhoto(buffer);
    // final result = InputChatPhoto(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final InputPhotoBase id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8953ad37);
    buffer.writeObject(id);
  }
}

/// Predicate Input Geo Point.
abstract class InputGeoPointBase extends TlConstructor {
  /// Predicate Input Geo Point constructor.
  const InputGeoPointBase._() : super._();
}

/// Input Geo Point Empty.
///
/// ID: `e4c123d6`.
class InputGeoPointEmpty extends InputGeoPointBase {
  /// Input Geo Point Empty constructor.
  const InputGeoPointEmpty() : super._();

  /// Deserialize.
  factory InputGeoPointEmpty.deserialize(Uint8List buffer) {
    // final result = InputGeoPointEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe4c123d6);
  }
}

/// Input Geo Point.
///
/// ID: `48222faf`.
class InputGeoPoint extends InputGeoPointBase {
  /// Input Geo Point constructor.
  const InputGeoPoint({
    required this.lat,
    required this.long,
    this.accuracyRadius,
  }) : super._();

  /// Deserialize.
  factory InputGeoPoint.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final lat = _readdouble(buffer);
    // final long = _readdouble(buffer);
    // final accuracyRadius = _readflags.0?int(buffer);
    // final result = InputGeoPoint(flags: flags, lat: lat, long: long, accuracyRadius: accuracyRadius,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: accuracyRadius != null,
    );

    return v;
  }

  /// Lat.
  final double lat;

  /// Long.
  final double long;

  /// Accuracy Radius.
  final int? accuracyRadius;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x48222faf);
    buffer.writeInt(flags);
    buffer.writeDouble(lat);
    buffer.writeDouble(long);
    final localAccuracyRadiusCopy = accuracyRadius;
    if (localAccuracyRadiusCopy != null) {
      buffer.writeInt(localAccuracyRadiusCopy);
    }
  }
}

/// Predicate Input Photo.
abstract class InputPhotoBase extends TlConstructor {
  /// Predicate Input Photo constructor.
  const InputPhotoBase._() : super._();
}

/// Input Photo Empty.
///
/// ID: `1cd7bf0d`.
class InputPhotoEmpty extends InputPhotoBase {
  /// Input Photo Empty constructor.
  const InputPhotoEmpty() : super._();

  /// Deserialize.
  factory InputPhotoEmpty.deserialize(Uint8List buffer) {
    // final result = InputPhotoEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1cd7bf0d);
  }
}

/// Input Photo.
///
/// ID: `3bb3b94a`.
class InputPhoto extends InputPhotoBase {
  /// Input Photo constructor.
  const InputPhoto({
    required this.id,
    required this.accessHash,
    required this.fileReference,
  }) : super._();

  /// Deserialize.
  factory InputPhoto.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final fileReference = _readbytes(buffer);
    // final result = InputPhoto(id: id, accessHash: accessHash, fileReference: fileReference,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// File Reference.
  final Uint8List fileReference;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3bb3b94a);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeBytes(fileReference);
  }
}

/// Predicate Input File Location.
abstract class InputFileLocationBase extends TlConstructor {
  /// Predicate Input File Location constructor.
  const InputFileLocationBase._() : super._();
}

/// Input File Location.
///
/// ID: `dfdaabe1`.
class InputFileLocation extends InputFileLocationBase {
  /// Input File Location constructor.
  const InputFileLocation({
    required this.volumeId,
    required this.localId,
    required this.secret,
    required this.fileReference,
  }) : super._();

  /// Deserialize.
  factory InputFileLocation.deserialize(Uint8List buffer) {
    // final volumeId = _readlong(buffer);
    // final localId = _readint(buffer);
    // final secret = _readlong(buffer);
    // final fileReference = _readbytes(buffer);
    // final result = InputFileLocation(volumeId: volumeId, localId: localId, secret: secret, fileReference: fileReference,);

    // return result;

    throw Exception();
  }

  /// Volume Id.
  final int volumeId;

  /// Local Id.
  final int localId;

  /// Secret.
  final int secret;

  /// File Reference.
  final Uint8List fileReference;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdfdaabe1);
    buffer.writeLong(volumeId);
    buffer.writeInt(localId);
    buffer.writeLong(secret);
    buffer.writeBytes(fileReference);
  }
}

/// Input Encrypted File Location.
///
/// ID: `f5235d55`.
class InputEncryptedFileLocation extends InputFileLocationBase {
  /// Input Encrypted File Location constructor.
  const InputEncryptedFileLocation({
    required this.id,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputEncryptedFileLocation.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputEncryptedFileLocation(id: id, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf5235d55);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
  }
}

/// Input Document File Location.
///
/// ID: `bad07584`.
class InputDocumentFileLocation extends InputFileLocationBase {
  /// Input Document File Location constructor.
  const InputDocumentFileLocation({
    required this.id,
    required this.accessHash,
    required this.fileReference,
    required this.thumbSize,
  }) : super._();

  /// Deserialize.
  factory InputDocumentFileLocation.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final fileReference = _readbytes(buffer);
    // final thumbSize = _readstring(buffer);
    // final result = InputDocumentFileLocation(id: id, accessHash: accessHash, fileReference: fileReference, thumbSize: thumbSize,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// File Reference.
  final Uint8List fileReference;

  /// Thumb Size.
  final String thumbSize;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbad07584);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeBytes(fileReference);
    buffer.writeString(thumbSize);
  }
}

/// Input Secure File Location.
///
/// ID: `cbc7ee28`.
class InputSecureFileLocation extends InputFileLocationBase {
  /// Input Secure File Location constructor.
  const InputSecureFileLocation({
    required this.id,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputSecureFileLocation.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputSecureFileLocation(id: id, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcbc7ee28);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
  }
}

/// Input Takeout File Location.
///
/// ID: `29be5899`.
class InputTakeoutFileLocation extends InputFileLocationBase {
  /// Input Takeout File Location constructor.
  const InputTakeoutFileLocation() : super._();

  /// Deserialize.
  factory InputTakeoutFileLocation.deserialize(Uint8List buffer) {
    // final result = InputTakeoutFileLocation();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x29be5899);
  }
}

/// Input Photo File Location.
///
/// ID: `40181ffe`.
class InputPhotoFileLocation extends InputFileLocationBase {
  /// Input Photo File Location constructor.
  const InputPhotoFileLocation({
    required this.id,
    required this.accessHash,
    required this.fileReference,
    required this.thumbSize,
  }) : super._();

  /// Deserialize.
  factory InputPhotoFileLocation.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final fileReference = _readbytes(buffer);
    // final thumbSize = _readstring(buffer);
    // final result = InputPhotoFileLocation(id: id, accessHash: accessHash, fileReference: fileReference, thumbSize: thumbSize,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// File Reference.
  final Uint8List fileReference;

  /// Thumb Size.
  final String thumbSize;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x40181ffe);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeBytes(fileReference);
    buffer.writeString(thumbSize);
  }
}

/// Input Photo Legacy File Location.
///
/// ID: `d83466f3`.
class InputPhotoLegacyFileLocation extends InputFileLocationBase {
  /// Input Photo Legacy File Location constructor.
  const InputPhotoLegacyFileLocation({
    required this.id,
    required this.accessHash,
    required this.fileReference,
    required this.volumeId,
    required this.localId,
    required this.secret,
  }) : super._();

  /// Deserialize.
  factory InputPhotoLegacyFileLocation.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final fileReference = _readbytes(buffer);
    // final volumeId = _readlong(buffer);
    // final localId = _readint(buffer);
    // final secret = _readlong(buffer);
    // final result = InputPhotoLegacyFileLocation(id: id, accessHash: accessHash, fileReference: fileReference, volumeId: volumeId, localId: localId, secret: secret,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// File Reference.
  final Uint8List fileReference;

  /// Volume Id.
  final int volumeId;

  /// Local Id.
  final int localId;

  /// Secret.
  final int secret;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd83466f3);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeBytes(fileReference);
    buffer.writeLong(volumeId);
    buffer.writeInt(localId);
    buffer.writeLong(secret);
  }
}

/// Input Peer Photo File Location.
///
/// ID: `37257e99`.
class InputPeerPhotoFileLocation extends InputFileLocationBase {
  /// Input Peer Photo File Location constructor.
  const InputPeerPhotoFileLocation({
    required this.big,
    required this.peer,
    required this.photoId,
  }) : super._();

  /// Deserialize.
  factory InputPeerPhotoFileLocation.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final big = _readflags.0?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final photoId = _readlong(buffer);
    // final result = InputPeerPhotoFileLocation(flags: flags, big: big, peer: peer, photoId: photoId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: big,
    );

    return v;
  }

  /// big: bit 0 of flags.0?true
  final bool big;

  /// Peer.
  final InputPeerBase peer;

  /// Photo Id.
  final int photoId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x37257e99);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeLong(photoId);
  }
}

/// Input Sticker Set Thumb.
///
/// ID: `9d84f3db`.
class InputStickerSetThumb extends InputFileLocationBase {
  /// Input Sticker Set Thumb constructor.
  const InputStickerSetThumb({
    required this.stickerset,
    required this.thumbVersion,
  }) : super._();

  /// Deserialize.
  factory InputStickerSetThumb.deserialize(Uint8List buffer) {
    // final stickerset = _readInputStickerSet(buffer);
    // final thumbVersion = _readint(buffer);
    // final result = InputStickerSetThumb(stickerset: stickerset, thumbVersion: thumbVersion,);

    // return result;

    throw Exception();
  }

  /// Stickerset.
  final InputStickerSetBase stickerset;

  /// Thumb Version.
  final int thumbVersion;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9d84f3db);
    buffer.writeObject(stickerset);
    buffer.writeInt(thumbVersion);
  }
}

/// Input Group Call Stream.
///
/// ID: `0598a92a`.
class InputGroupCallStream extends InputFileLocationBase {
  /// Input Group Call Stream constructor.
  const InputGroupCallStream({
    required this.call,
    required this.timeMs,
    required this.scale,
    this.videoChannel,
    this.videoQuality,
  }) : super._();

  /// Deserialize.
  factory InputGroupCallStream.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final call = _readInputGroupCall(buffer);
    // final timeMs = _readlong(buffer);
    // final scale = _readint(buffer);
    // final videoChannel = _readflags.0?int(buffer);
    // final videoQuality = _readflags.0?int(buffer);
    // final result = InputGroupCallStream(flags: flags, call: call, timeMs: timeMs, scale: scale, videoChannel: videoChannel, videoQuality: videoQuality,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: videoChannel != null || videoQuality != null,
    );

    return v;
  }

  /// Call.
  final InputGroupCallBase call;

  /// Time Ms.
  final int timeMs;

  /// Scale.
  final int scale;

  /// Video Channel.
  final int? videoChannel;

  /// Video Quality.
  final int? videoQuality;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0598a92a);
    buffer.writeInt(flags);
    buffer.writeObject(call);
    buffer.writeLong(timeMs);
    buffer.writeInt(scale);
    final localVideoChannelCopy = videoChannel;
    if (localVideoChannelCopy != null) {
      buffer.writeInt(localVideoChannelCopy);
    }
    final localVideoQualityCopy = videoQuality;
    if (localVideoQualityCopy != null) {
      buffer.writeInt(localVideoQualityCopy);
    }
  }
}

/// Predicate Peer.
abstract class PeerBase extends TlConstructor {
  /// Predicate Peer constructor.
  const PeerBase._() : super._();
}

/// Peer User.
///
/// ID: `59511722`.
class PeerUser extends PeerBase {
  /// Peer User constructor.
  const PeerUser({
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory PeerUser.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final result = PeerUser(userId: userId,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x59511722);
    buffer.writeLong(userId);
  }
}

/// Peer Chat.
///
/// ID: `36c6019a`.
class PeerChat extends PeerBase {
  /// Peer Chat constructor.
  const PeerChat({
    required this.chatId,
  }) : super._();

  /// Deserialize.
  factory PeerChat.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final result = PeerChat(chatId: chatId,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x36c6019a);
    buffer.writeLong(chatId);
  }
}

/// Peer Channel.
///
/// ID: `a2a5371e`.
class PeerChannel extends PeerBase {
  /// Peer Channel constructor.
  const PeerChannel({
    required this.channelId,
  }) : super._();

  /// Deserialize.
  factory PeerChannel.deserialize(Uint8List buffer) {
    // final channelId = _readlong(buffer);
    // final result = PeerChannel(channelId: channelId,);

    // return result;

    throw Exception();
  }

  /// Channel Id.
  final int channelId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa2a5371e);
    buffer.writeLong(channelId);
  }
}

/// Predicate Storage File Type.
abstract class StorageFileTypeBase extends TlConstructor {
  /// Predicate Storage File Type constructor.
  const StorageFileTypeBase._() : super._();
}

/// Storage File Unknown.
///
/// ID: `aa963b05`.
class StorageFileUnknown extends StorageFileTypeBase {
  /// Storage File Unknown constructor.
  const StorageFileUnknown() : super._();

  /// Deserialize.
  factory StorageFileUnknown.deserialize(Uint8List buffer) {
    // final result = StorageFileUnknown();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xaa963b05);
  }
}

/// Storage File Partial.
///
/// ID: `40bc6f52`.
class StorageFilePartial extends StorageFileTypeBase {
  /// Storage File Partial constructor.
  const StorageFilePartial() : super._();

  /// Deserialize.
  factory StorageFilePartial.deserialize(Uint8List buffer) {
    // final result = StorageFilePartial();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x40bc6f52);
  }
}

/// Storage File Jpeg.
///
/// ID: `007efe0e`.
class StorageFileJpeg extends StorageFileTypeBase {
  /// Storage File Jpeg constructor.
  const StorageFileJpeg() : super._();

  /// Deserialize.
  factory StorageFileJpeg.deserialize(Uint8List buffer) {
    // final result = StorageFileJpeg();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x007efe0e);
  }
}

/// Storage File Gif.
///
/// ID: `cae1aadf`.
class StorageFileGif extends StorageFileTypeBase {
  /// Storage File Gif constructor.
  const StorageFileGif() : super._();

  /// Deserialize.
  factory StorageFileGif.deserialize(Uint8List buffer) {
    // final result = StorageFileGif();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcae1aadf);
  }
}

/// Storage File Png.
///
/// ID: `0a4f63c0`.
class StorageFilePng extends StorageFileTypeBase {
  /// Storage File Png constructor.
  const StorageFilePng() : super._();

  /// Deserialize.
  factory StorageFilePng.deserialize(Uint8List buffer) {
    // final result = StorageFilePng();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0a4f63c0);
  }
}

/// Storage File Pdf.
///
/// ID: `ae1e508d`.
class StorageFilePdf extends StorageFileTypeBase {
  /// Storage File Pdf constructor.
  const StorageFilePdf() : super._();

  /// Deserialize.
  factory StorageFilePdf.deserialize(Uint8List buffer) {
    // final result = StorageFilePdf();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xae1e508d);
  }
}

/// Storage File Mp3.
///
/// ID: `528a0677`.
class StorageFileMp3 extends StorageFileTypeBase {
  /// Storage File Mp3 constructor.
  const StorageFileMp3() : super._();

  /// Deserialize.
  factory StorageFileMp3.deserialize(Uint8List buffer) {
    // final result = StorageFileMp3();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x528a0677);
  }
}

/// Storage File Mov.
///
/// ID: `4b09ebbc`.
class StorageFileMov extends StorageFileTypeBase {
  /// Storage File Mov constructor.
  const StorageFileMov() : super._();

  /// Deserialize.
  factory StorageFileMov.deserialize(Uint8List buffer) {
    // final result = StorageFileMov();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4b09ebbc);
  }
}

/// Storage File Mp4.
///
/// ID: `b3cea0e4`.
class StorageFileMp4 extends StorageFileTypeBase {
  /// Storage File Mp4 constructor.
  const StorageFileMp4() : super._();

  /// Deserialize.
  factory StorageFileMp4.deserialize(Uint8List buffer) {
    // final result = StorageFileMp4();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb3cea0e4);
  }
}

/// Storage File Webp.
///
/// ID: `1081464c`.
class StorageFileWebp extends StorageFileTypeBase {
  /// Storage File Webp constructor.
  const StorageFileWebp() : super._();

  /// Deserialize.
  factory StorageFileWebp.deserialize(Uint8List buffer) {
    // final result = StorageFileWebp();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1081464c);
  }
}

/// Predicate User.
abstract class UserBase extends TlConstructor {
  /// Predicate User constructor.
  const UserBase._() : super._();
}

/// User Empty.
///
/// ID: `d3bc4b7a`.
class UserEmpty extends UserBase {
  /// User Empty constructor.
  const UserEmpty({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory UserEmpty.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final result = UserEmpty(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd3bc4b7a);
    buffer.writeLong(id);
  }
}

/// User.
///
/// ID: `215c4438`.
class User extends UserBase {
  /// User constructor.
  const User({
    required this.self,
    required this.contact,
    required this.mutualContact,
    required this.deleted,
    required this.bot,
    required this.botChatHistory,
    required this.botNochats,
    required this.verified,
    required this.restricted,
    required this.min,
    required this.botInlineGeo,
    required this.support,
    required this.scam,
    required this.applyMinPhoto,
    required this.fake,
    required this.botAttachMenu,
    required this.premium,
    required this.attachMenuEnabled,
    required this.botCanEdit,
    required this.closeFriend,
    required this.storiesHidden,
    required this.storiesUnavailable,
    required this.id,
    this.accessHash,
    this.firstName,
    this.lastName,
    this.username,
    this.phone,
    this.photo,
    this.status,
    this.botInfoVersion,
    this.restrictionReason,
    this.botInlinePlaceholder,
    this.langCode,
    this.emojiStatus,
    this.usernames,
    this.storiesMaxId,
    this.color,
    this.profileColor,
  }) : super._();

  /// Deserialize.
  factory User.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final self = _readflags.10?true(buffer);
    // final contact = _readflags.11?true(buffer);
    // final mutualContact = _readflags.12?true(buffer);
    // final deleted = _readflags.13?true(buffer);
    // final bot = _readflags.14?true(buffer);
    // final botChatHistory = _readflags.15?true(buffer);
    // final botNochats = _readflags.16?true(buffer);
    // final verified = _readflags.17?true(buffer);
    // final restricted = _readflags.18?true(buffer);
    // final min = _readflags.20?true(buffer);
    // final botInlineGeo = _readflags.21?true(buffer);
    // final support = _readflags.23?true(buffer);
    // final scam = _readflags.24?true(buffer);
    // final applyMinPhoto = _readflags.25?true(buffer);
    // final fake = _readflags.26?true(buffer);
    // final botAttachMenu = _readflags.27?true(buffer);
    // final premium = _readflags.28?true(buffer);
    // final attachMenuEnabled = _readflags.29?true(buffer);
    // final flags2 = _read#(buffer);
    // final botCanEdit = _readflags2.1?true(buffer);
    // final closeFriend = _readflags2.2?true(buffer);
    // final storiesHidden = _readflags2.3?true(buffer);
    // final storiesUnavailable = _readflags2.4?true(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readflags.0?long(buffer);
    // final firstName = _readflags.1?string(buffer);
    // final lastName = _readflags.2?string(buffer);
    // final username = _readflags.3?string(buffer);
    // final phone = _readflags.4?string(buffer);
    // final photo = _readflags.5?UserProfilePhoto(buffer);
    // final status = _readflags.6?UserStatus(buffer);
    // final botInfoVersion = _readflags.14?int(buffer);
    // final restrictionReason = _readflags.18?Vector<RestrictionReason>(buffer);
    // final botInlinePlaceholder = _readflags.19?string(buffer);
    // final langCode = _readflags.22?string(buffer);
    // final emojiStatus = _readflags.30?EmojiStatus(buffer);
    // final usernames = _readflags2.0?Vector<Username>(buffer);
    // final storiesMaxId = _readflags2.5?int(buffer);
    // final color = _readflags2.8?PeerColor(buffer);
    // final profileColor = _readflags2.9?PeerColor(buffer);
    // final result = User(flags: flags, self: self, contact: contact, mutualContact: mutualContact, deleted: deleted, bot: bot, botChatHistory: botChatHistory, botNochats: botNochats, verified: verified, restricted: restricted, min: min, botInlineGeo: botInlineGeo, support: support, scam: scam, applyMinPhoto: applyMinPhoto, fake: fake, botAttachMenu: botAttachMenu, premium: premium, attachMenuEnabled: attachMenuEnabled, flags2: flags2, botCanEdit: botCanEdit, closeFriend: closeFriend, storiesHidden: storiesHidden, storiesUnavailable: storiesUnavailable, id: id, accessHash: accessHash, firstName: firstName, lastName: lastName, username: username, phone: phone, photo: photo, status: status, botInfoVersion: botInfoVersion, restrictionReason: restrictionReason, botInlinePlaceholder: botInlinePlaceholder, langCode: langCode, emojiStatus: emojiStatus, usernames: usernames, storiesMaxId: storiesMaxId, color: color, profileColor: profileColor,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b10: self,
      b11: contact,
      b12: mutualContact,
      b13: deleted,
      b14: bot || botInfoVersion != null,
      b15: botChatHistory,
      b16: botNochats,
      b17: verified,
      b18: restricted || restrictionReason != null,
      b20: min,
      b21: botInlineGeo,
      b23: support,
      b24: scam,
      b25: applyMinPhoto,
      b26: fake,
      b27: botAttachMenu,
      b28: premium,
      b29: attachMenuEnabled,
      b00: accessHash != null,
      b01: firstName != null,
      b02: lastName != null,
      b03: username != null,
      b04: phone != null,
      b05: photo != null,
      b06: status != null,
      b19: botInlinePlaceholder != null,
      b22: langCode != null,
      b30: emojiStatus != null,
    );

    return v;
  }

  /// Flags2.
  int get flags2 {
    final v = _flag(
      b01: botCanEdit,
      b02: closeFriend,
      b03: storiesHidden,
      b04: storiesUnavailable,
      b00: usernames != null,
      b05: storiesMaxId != null,
      b08: color != null,
      b09: profileColor != null,
    );

    return v;
  }

  /// self: bit 10 of flags.10?true
  final bool self;

  /// contact: bit 11 of flags.11?true
  final bool contact;

  /// mutual_contact: bit 12 of flags.12?true
  final bool mutualContact;

  /// deleted: bit 13 of flags.13?true
  final bool deleted;

  /// bot: bit 14 of flags.14?true
  final bool bot;

  /// bot_chat_history: bit 15 of flags.15?true
  final bool botChatHistory;

  /// bot_nochats: bit 16 of flags.16?true
  final bool botNochats;

  /// verified: bit 17 of flags.17?true
  final bool verified;

  /// restricted: bit 18 of flags.18?true
  final bool restricted;

  /// min: bit 20 of flags.20?true
  final bool min;

  /// bot_inline_geo: bit 21 of flags.21?true
  final bool botInlineGeo;

  /// support: bit 23 of flags.23?true
  final bool support;

  /// scam: bit 24 of flags.24?true
  final bool scam;

  /// apply_min_photo: bit 25 of flags.25?true
  final bool applyMinPhoto;

  /// fake: bit 26 of flags.26?true
  final bool fake;

  /// bot_attach_menu: bit 27 of flags.27?true
  final bool botAttachMenu;

  /// premium: bit 28 of flags.28?true
  final bool premium;

  /// attach_menu_enabled: bit 29 of flags.29?true
  final bool attachMenuEnabled;

  /// bot_can_edit: bit 1 of flags2.1?true
  final bool botCanEdit;

  /// close_friend: bit 2 of flags2.2?true
  final bool closeFriend;

  /// stories_hidden: bit 3 of flags2.3?true
  final bool storiesHidden;

  /// stories_unavailable: bit 4 of flags2.4?true
  final bool storiesUnavailable;

  /// Id.
  final int id;

  /// Access Hash.
  final int? accessHash;

  /// First Name.
  final String? firstName;

  /// Last Name.
  final String? lastName;

  /// Username.
  final String? username;

  /// Phone.
  final String? phone;

  /// Photo.
  final UserProfilePhotoBase? photo;

  /// Status.
  final UserStatusBase? status;

  /// Bot Info Version.
  final int? botInfoVersion;

  /// Restriction Reason.
  final List<RestrictionReasonBase>? restrictionReason;

  /// Bot Inline Placeholder.
  final String? botInlinePlaceholder;

  /// Lang Code.
  final String? langCode;

  /// Emoji Status.
  final EmojiStatusBase? emojiStatus;

  /// Usernames.
  final List<UsernameBase>? usernames;

  /// Stories Max Id.
  final int? storiesMaxId;

  /// Color.
  final PeerColorBase? color;

  /// Profile Color.
  final PeerColorBase? profileColor;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x215c4438);
    buffer.writeInt(flags);
    buffer.writeInt(flags2);
    buffer.writeLong(id);
    final localAccessHashCopy = accessHash;
    if (localAccessHashCopy != null) {
      buffer.writeLong(localAccessHashCopy);
    }
    final localFirstNameCopy = firstName;
    if (localFirstNameCopy != null) {
      buffer.writeString(localFirstNameCopy);
    }
    final localLastNameCopy = lastName;
    if (localLastNameCopy != null) {
      buffer.writeString(localLastNameCopy);
    }
    final localUsernameCopy = username;
    if (localUsernameCopy != null) {
      buffer.writeString(localUsernameCopy);
    }
    final localPhoneCopy = phone;
    if (localPhoneCopy != null) {
      buffer.writeString(localPhoneCopy);
    }
    final localPhotoCopy = photo;
    if (localPhotoCopy != null) {
      buffer.writeObject(localPhotoCopy);
    }
    final localStatusCopy = status;
    if (localStatusCopy != null) {
      buffer.writeObject(localStatusCopy);
    }
    final localBotInfoVersionCopy = botInfoVersion;
    if (localBotInfoVersionCopy != null) {
      buffer.writeInt(localBotInfoVersionCopy);
    }
    final localRestrictionReasonCopy = restrictionReason;
    if (localRestrictionReasonCopy != null) {
      buffer.writeVectorObject(localRestrictionReasonCopy);
    }
    final localBotInlinePlaceholderCopy = botInlinePlaceholder;
    if (localBotInlinePlaceholderCopy != null) {
      buffer.writeString(localBotInlinePlaceholderCopy);
    }
    final localLangCodeCopy = langCode;
    if (localLangCodeCopy != null) {
      buffer.writeString(localLangCodeCopy);
    }
    final localEmojiStatusCopy = emojiStatus;
    if (localEmojiStatusCopy != null) {
      buffer.writeObject(localEmojiStatusCopy);
    }
    final localUsernamesCopy = usernames;
    if (localUsernamesCopy != null) {
      buffer.writeVectorObject(localUsernamesCopy);
    }
    final localStoriesMaxIdCopy = storiesMaxId;
    if (localStoriesMaxIdCopy != null) {
      buffer.writeInt(localStoriesMaxIdCopy);
    }
    final localColorCopy = color;
    if (localColorCopy != null) {
      buffer.writeObject(localColorCopy);
    }
    final localProfileColorCopy = profileColor;
    if (localProfileColorCopy != null) {
      buffer.writeObject(localProfileColorCopy);
    }
  }
}

/// Predicate User Profile Photo.
abstract class UserProfilePhotoBase extends TlConstructor {
  /// Predicate User Profile Photo constructor.
  const UserProfilePhotoBase._() : super._();
}

/// User Profile Photo Empty.
///
/// ID: `4f11bae1`.
class UserProfilePhotoEmpty extends UserProfilePhotoBase {
  /// User Profile Photo Empty constructor.
  const UserProfilePhotoEmpty() : super._();

  /// Deserialize.
  factory UserProfilePhotoEmpty.deserialize(Uint8List buffer) {
    // final result = UserProfilePhotoEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4f11bae1);
  }
}

/// User Profile Photo.
///
/// ID: `82d1f706`.
class UserProfilePhoto extends UserProfilePhotoBase {
  /// User Profile Photo constructor.
  const UserProfilePhoto({
    required this.hasVideo,
    required this.personal,
    required this.photoId,
    this.strippedThumb,
    required this.dcId,
  }) : super._();

  /// Deserialize.
  factory UserProfilePhoto.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final hasVideo = _readflags.0?true(buffer);
    // final personal = _readflags.2?true(buffer);
    // final photoId = _readlong(buffer);
    // final strippedThumb = _readflags.1?bytes(buffer);
    // final dcId = _readint(buffer);
    // final result = UserProfilePhoto(flags: flags, hasVideo: hasVideo, personal: personal, photoId: photoId, strippedThumb: strippedThumb, dcId: dcId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: hasVideo,
      b02: personal,
      b01: strippedThumb != null,
    );

    return v;
  }

  /// has_video: bit 0 of flags.0?true
  final bool hasVideo;

  /// personal: bit 2 of flags.2?true
  final bool personal;

  /// Photo Id.
  final int photoId;

  /// Stripped Thumb.
  final Uint8List? strippedThumb;

  /// Dc Id.
  final int dcId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x82d1f706);
    buffer.writeInt(flags);
    buffer.writeLong(photoId);
    final localStrippedThumbCopy = strippedThumb;
    if (localStrippedThumbCopy != null) {
      buffer.writeBytes(localStrippedThumbCopy);
    }
    buffer.writeInt(dcId);
  }
}

/// Predicate User Status.
abstract class UserStatusBase extends TlConstructor {
  /// Predicate User Status constructor.
  const UserStatusBase._() : super._();
}

/// User Status Empty.
///
/// ID: `09d05049`.
class UserStatusEmpty extends UserStatusBase {
  /// User Status Empty constructor.
  const UserStatusEmpty() : super._();

  /// Deserialize.
  factory UserStatusEmpty.deserialize(Uint8List buffer) {
    // final result = UserStatusEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x09d05049);
  }
}

/// User Status Online.
///
/// ID: `edb93949`.
class UserStatusOnline extends UserStatusBase {
  /// User Status Online constructor.
  const UserStatusOnline({
    required this.expires,
  }) : super._();

  /// Deserialize.
  factory UserStatusOnline.deserialize(Uint8List buffer) {
    // final expires = _readint(buffer);
    // final result = UserStatusOnline(expires: expires,);

    // return result;

    throw Exception();
  }

  /// Expires.
  final int expires;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xedb93949);
    buffer.writeInt(expires);
  }
}

/// User Status Offline.
///
/// ID: `008c703f`.
class UserStatusOffline extends UserStatusBase {
  /// User Status Offline constructor.
  const UserStatusOffline({
    required this.wasOnline,
  }) : super._();

  /// Deserialize.
  factory UserStatusOffline.deserialize(Uint8List buffer) {
    // final wasOnline = _readint(buffer);
    // final result = UserStatusOffline(wasOnline: wasOnline,);

    // return result;

    throw Exception();
  }

  /// Was Online.
  final int wasOnline;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x008c703f);
    buffer.writeInt(wasOnline);
  }
}

/// User Status Recently.
///
/// ID: `e26f42f1`.
class UserStatusRecently extends UserStatusBase {
  /// User Status Recently constructor.
  const UserStatusRecently() : super._();

  /// Deserialize.
  factory UserStatusRecently.deserialize(Uint8List buffer) {
    // final result = UserStatusRecently();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe26f42f1);
  }
}

/// User Status Last Week.
///
/// ID: `07bf09fc`.
class UserStatusLastWeek extends UserStatusBase {
  /// User Status Last Week constructor.
  const UserStatusLastWeek() : super._();

  /// Deserialize.
  factory UserStatusLastWeek.deserialize(Uint8List buffer) {
    // final result = UserStatusLastWeek();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x07bf09fc);
  }
}

/// User Status Last Month.
///
/// ID: `77ebc742`.
class UserStatusLastMonth extends UserStatusBase {
  /// User Status Last Month constructor.
  const UserStatusLastMonth() : super._();

  /// Deserialize.
  factory UserStatusLastMonth.deserialize(Uint8List buffer) {
    // final result = UserStatusLastMonth();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x77ebc742);
  }
}

/// Predicate Chat.
abstract class ChatBase extends TlConstructor {
  /// Predicate Chat constructor.
  const ChatBase._() : super._();
}

/// Chat Empty.
///
/// ID: `29562865`.
class ChatEmpty extends ChatBase {
  /// Chat Empty constructor.
  const ChatEmpty({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory ChatEmpty.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final result = ChatEmpty(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x29562865);
    buffer.writeLong(id);
  }
}

/// Chat.
///
/// ID: `41cbf256`.
class Chat extends ChatBase {
  /// Chat constructor.
  const Chat({
    required this.creator,
    required this.left,
    required this.deactivated,
    required this.callActive,
    required this.callNotEmpty,
    required this.noforwards,
    required this.id,
    required this.title,
    required this.photo,
    required this.participantsCount,
    required this.date,
    required this.version,
    this.migratedTo,
    this.adminRights,
    this.defaultBannedRights,
  }) : super._();

  /// Deserialize.
  factory Chat.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final creator = _readflags.0?true(buffer);
    // final left = _readflags.2?true(buffer);
    // final deactivated = _readflags.5?true(buffer);
    // final callActive = _readflags.23?true(buffer);
    // final callNotEmpty = _readflags.24?true(buffer);
    // final noforwards = _readflags.25?true(buffer);
    // final id = _readlong(buffer);
    // final title = _readstring(buffer);
    // final photo = _readChatPhoto(buffer);
    // final participantsCount = _readint(buffer);
    // final date = _readint(buffer);
    // final version = _readint(buffer);
    // final migratedTo = _readflags.6?InputChannel(buffer);
    // final adminRights = _readflags.14?ChatAdminRights(buffer);
    // final defaultBannedRights = _readflags.18?ChatBannedRights(buffer);
    // final result = Chat(flags: flags, creator: creator, left: left, deactivated: deactivated, callActive: callActive, callNotEmpty: callNotEmpty, noforwards: noforwards, id: id, title: title, photo: photo, participantsCount: participantsCount, date: date, version: version, migratedTo: migratedTo, adminRights: adminRights, defaultBannedRights: defaultBannedRights,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: creator,
      b02: left,
      b05: deactivated,
      b23: callActive,
      b24: callNotEmpty,
      b25: noforwards,
      b06: migratedTo != null,
      b14: adminRights != null,
      b18: defaultBannedRights != null,
    );

    return v;
  }

  /// creator: bit 0 of flags.0?true
  final bool creator;

  /// left: bit 2 of flags.2?true
  final bool left;

  /// deactivated: bit 5 of flags.5?true
  final bool deactivated;

  /// call_active: bit 23 of flags.23?true
  final bool callActive;

  /// call_not_empty: bit 24 of flags.24?true
  final bool callNotEmpty;

  /// noforwards: bit 25 of flags.25?true
  final bool noforwards;

  /// Id.
  final int id;

  /// Title.
  final String title;

  /// Photo.
  final ChatPhotoBase photo;

  /// Participants Count.
  final int participantsCount;

  /// Date.
  final DateTime date;

  /// Version.
  final int version;

  /// Migrated To.
  final InputChannelBase? migratedTo;

  /// Admin Rights.
  final ChatAdminRightsBase? adminRights;

  /// Default Banned Rights.
  final ChatBannedRightsBase? defaultBannedRights;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x41cbf256);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeString(title);
    buffer.writeObject(photo);
    buffer.writeInt(participantsCount);
    buffer.writeDateTime(date);
    buffer.writeInt(version);
    final localMigratedToCopy = migratedTo;
    if (localMigratedToCopy != null) {
      buffer.writeObject(localMigratedToCopy);
    }
    final localAdminRightsCopy = adminRights;
    if (localAdminRightsCopy != null) {
      buffer.writeObject(localAdminRightsCopy);
    }
    final localDefaultBannedRightsCopy = defaultBannedRights;
    if (localDefaultBannedRightsCopy != null) {
      buffer.writeObject(localDefaultBannedRightsCopy);
    }
  }
}

/// Chat Forbidden.
///
/// ID: `6592a1a7`.
class ChatForbidden extends ChatBase {
  /// Chat Forbidden constructor.
  const ChatForbidden({
    required this.id,
    required this.title,
  }) : super._();

  /// Deserialize.
  factory ChatForbidden.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final title = _readstring(buffer);
    // final result = ChatForbidden(id: id, title: title,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Title.
  final String title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6592a1a7);
    buffer.writeLong(id);
    buffer.writeString(title);
  }
}

/// Channel.
///
/// ID: `0aadfc8f`.
class Channel extends ChatBase {
  /// Channel constructor.
  const Channel({
    required this.creator,
    required this.left,
    required this.broadcast,
    required this.verified,
    required this.megagroup,
    required this.restricted,
    required this.signatures,
    required this.min,
    required this.scam,
    required this.hasLink,
    required this.hasGeo,
    required this.slowmodeEnabled,
    required this.callActive,
    required this.callNotEmpty,
    required this.fake,
    required this.gigagroup,
    required this.noforwards,
    required this.joinToSend,
    required this.joinRequest,
    required this.forum,
    required this.storiesHidden,
    required this.storiesHiddenMin,
    required this.storiesUnavailable,
    required this.id,
    this.accessHash,
    required this.title,
    this.username,
    required this.photo,
    required this.date,
    this.restrictionReason,
    this.adminRights,
    this.bannedRights,
    this.defaultBannedRights,
    this.participantsCount,
    this.usernames,
    this.storiesMaxId,
    this.color,
    this.profileColor,
    this.emojiStatus,
    this.level,
  }) : super._();

  /// Deserialize.
  factory Channel.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final creator = _readflags.0?true(buffer);
    // final left = _readflags.2?true(buffer);
    // final broadcast = _readflags.5?true(buffer);
    // final verified = _readflags.7?true(buffer);
    // final megagroup = _readflags.8?true(buffer);
    // final restricted = _readflags.9?true(buffer);
    // final signatures = _readflags.11?true(buffer);
    // final min = _readflags.12?true(buffer);
    // final scam = _readflags.19?true(buffer);
    // final hasLink = _readflags.20?true(buffer);
    // final hasGeo = _readflags.21?true(buffer);
    // final slowmodeEnabled = _readflags.22?true(buffer);
    // final callActive = _readflags.23?true(buffer);
    // final callNotEmpty = _readflags.24?true(buffer);
    // final fake = _readflags.25?true(buffer);
    // final gigagroup = _readflags.26?true(buffer);
    // final noforwards = _readflags.27?true(buffer);
    // final joinToSend = _readflags.28?true(buffer);
    // final joinRequest = _readflags.29?true(buffer);
    // final forum = _readflags.30?true(buffer);
    // final flags2 = _read#(buffer);
    // final storiesHidden = _readflags2.1?true(buffer);
    // final storiesHiddenMin = _readflags2.2?true(buffer);
    // final storiesUnavailable = _readflags2.3?true(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readflags.13?long(buffer);
    // final title = _readstring(buffer);
    // final username = _readflags.6?string(buffer);
    // final photo = _readChatPhoto(buffer);
    // final date = _readint(buffer);
    // final restrictionReason = _readflags.9?Vector<RestrictionReason>(buffer);
    // final adminRights = _readflags.14?ChatAdminRights(buffer);
    // final bannedRights = _readflags.15?ChatBannedRights(buffer);
    // final defaultBannedRights = _readflags.18?ChatBannedRights(buffer);
    // final participantsCount = _readflags.17?int(buffer);
    // final usernames = _readflags2.0?Vector<Username>(buffer);
    // final storiesMaxId = _readflags2.4?int(buffer);
    // final color = _readflags2.7?PeerColor(buffer);
    // final profileColor = _readflags2.8?PeerColor(buffer);
    // final emojiStatus = _readflags2.9?EmojiStatus(buffer);
    // final level = _readflags2.10?int(buffer);
    // final result = Channel(flags: flags, creator: creator, left: left, broadcast: broadcast, verified: verified, megagroup: megagroup, restricted: restricted, signatures: signatures, min: min, scam: scam, hasLink: hasLink, hasGeo: hasGeo, slowmodeEnabled: slowmodeEnabled, callActive: callActive, callNotEmpty: callNotEmpty, fake: fake, gigagroup: gigagroup, noforwards: noforwards, joinToSend: joinToSend, joinRequest: joinRequest, forum: forum, flags2: flags2, storiesHidden: storiesHidden, storiesHiddenMin: storiesHiddenMin, storiesUnavailable: storiesUnavailable, id: id, accessHash: accessHash, title: title, username: username, photo: photo, date: date, restrictionReason: restrictionReason, adminRights: adminRights, bannedRights: bannedRights, defaultBannedRights: defaultBannedRights, participantsCount: participantsCount, usernames: usernames, storiesMaxId: storiesMaxId, color: color, profileColor: profileColor, emojiStatus: emojiStatus, level: level,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: creator,
      b02: left,
      b05: broadcast,
      b07: verified,
      b08: megagroup,
      b09: restricted || restrictionReason != null,
      b11: signatures,
      b12: min,
      b19: scam,
      b20: hasLink,
      b21: hasGeo,
      b22: slowmodeEnabled,
      b23: callActive,
      b24: callNotEmpty,
      b25: fake,
      b26: gigagroup,
      b27: noforwards,
      b28: joinToSend,
      b29: joinRequest,
      b30: forum,
      b13: accessHash != null,
      b06: username != null,
      b14: adminRights != null,
      b15: bannedRights != null,
      b18: defaultBannedRights != null,
      b17: participantsCount != null,
    );

    return v;
  }

  /// Flags2.
  int get flags2 {
    final v = _flag(
      b01: storiesHidden,
      b02: storiesHiddenMin,
      b03: storiesUnavailable,
      b00: usernames != null,
      b04: storiesMaxId != null,
      b07: color != null,
      b08: profileColor != null,
      b09: emojiStatus != null,
      b10: level != null,
    );

    return v;
  }

  /// creator: bit 0 of flags.0?true
  final bool creator;

  /// left: bit 2 of flags.2?true
  final bool left;

  /// broadcast: bit 5 of flags.5?true
  final bool broadcast;

  /// verified: bit 7 of flags.7?true
  final bool verified;

  /// megagroup: bit 8 of flags.8?true
  final bool megagroup;

  /// restricted: bit 9 of flags.9?true
  final bool restricted;

  /// signatures: bit 11 of flags.11?true
  final bool signatures;

  /// min: bit 12 of flags.12?true
  final bool min;

  /// scam: bit 19 of flags.19?true
  final bool scam;

  /// has_link: bit 20 of flags.20?true
  final bool hasLink;

  /// has_geo: bit 21 of flags.21?true
  final bool hasGeo;

  /// slowmode_enabled: bit 22 of flags.22?true
  final bool slowmodeEnabled;

  /// call_active: bit 23 of flags.23?true
  final bool callActive;

  /// call_not_empty: bit 24 of flags.24?true
  final bool callNotEmpty;

  /// fake: bit 25 of flags.25?true
  final bool fake;

  /// gigagroup: bit 26 of flags.26?true
  final bool gigagroup;

  /// noforwards: bit 27 of flags.27?true
  final bool noforwards;

  /// join_to_send: bit 28 of flags.28?true
  final bool joinToSend;

  /// join_request: bit 29 of flags.29?true
  final bool joinRequest;

  /// forum: bit 30 of flags.30?true
  final bool forum;

  /// stories_hidden: bit 1 of flags2.1?true
  final bool storiesHidden;

  /// stories_hidden_min: bit 2 of flags2.2?true
  final bool storiesHiddenMin;

  /// stories_unavailable: bit 3 of flags2.3?true
  final bool storiesUnavailable;

  /// Id.
  final int id;

  /// Access Hash.
  final int? accessHash;

  /// Title.
  final String title;

  /// Username.
  final String? username;

  /// Photo.
  final ChatPhotoBase photo;

  /// Date.
  final DateTime date;

  /// Restriction Reason.
  final List<RestrictionReasonBase>? restrictionReason;

  /// Admin Rights.
  final ChatAdminRightsBase? adminRights;

  /// Banned Rights.
  final ChatBannedRightsBase? bannedRights;

  /// Default Banned Rights.
  final ChatBannedRightsBase? defaultBannedRights;

  /// Participants Count.
  final int? participantsCount;

  /// Usernames.
  final List<UsernameBase>? usernames;

  /// Stories Max Id.
  final int? storiesMaxId;

  /// Color.
  final PeerColorBase? color;

  /// Profile Color.
  final PeerColorBase? profileColor;

  /// Emoji Status.
  final EmojiStatusBase? emojiStatus;

  /// Level.
  final int? level;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0aadfc8f);
    buffer.writeInt(flags);
    buffer.writeInt(flags2);
    buffer.writeLong(id);
    final localAccessHashCopy = accessHash;
    if (localAccessHashCopy != null) {
      buffer.writeLong(localAccessHashCopy);
    }
    buffer.writeString(title);
    final localUsernameCopy = username;
    if (localUsernameCopy != null) {
      buffer.writeString(localUsernameCopy);
    }
    buffer.writeObject(photo);
    buffer.writeDateTime(date);
    final localRestrictionReasonCopy = restrictionReason;
    if (localRestrictionReasonCopy != null) {
      buffer.writeVectorObject(localRestrictionReasonCopy);
    }
    final localAdminRightsCopy = adminRights;
    if (localAdminRightsCopy != null) {
      buffer.writeObject(localAdminRightsCopy);
    }
    final localBannedRightsCopy = bannedRights;
    if (localBannedRightsCopy != null) {
      buffer.writeObject(localBannedRightsCopy);
    }
    final localDefaultBannedRightsCopy = defaultBannedRights;
    if (localDefaultBannedRightsCopy != null) {
      buffer.writeObject(localDefaultBannedRightsCopy);
    }
    final localParticipantsCountCopy = participantsCount;
    if (localParticipantsCountCopy != null) {
      buffer.writeInt(localParticipantsCountCopy);
    }
    final localUsernamesCopy = usernames;
    if (localUsernamesCopy != null) {
      buffer.writeVectorObject(localUsernamesCopy);
    }
    final localStoriesMaxIdCopy = storiesMaxId;
    if (localStoriesMaxIdCopy != null) {
      buffer.writeInt(localStoriesMaxIdCopy);
    }
    final localColorCopy = color;
    if (localColorCopy != null) {
      buffer.writeObject(localColorCopy);
    }
    final localProfileColorCopy = profileColor;
    if (localProfileColorCopy != null) {
      buffer.writeObject(localProfileColorCopy);
    }
    final localEmojiStatusCopy = emojiStatus;
    if (localEmojiStatusCopy != null) {
      buffer.writeObject(localEmojiStatusCopy);
    }
    final localLevelCopy = level;
    if (localLevelCopy != null) {
      buffer.writeInt(localLevelCopy);
    }
  }
}

/// Channel Forbidden.
///
/// ID: `17d493d5`.
class ChannelForbidden extends ChatBase {
  /// Channel Forbidden constructor.
  const ChannelForbidden({
    required this.broadcast,
    required this.megagroup,
    required this.id,
    required this.accessHash,
    required this.title,
    this.untilDate,
  }) : super._();

  /// Deserialize.
  factory ChannelForbidden.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final broadcast = _readflags.5?true(buffer);
    // final megagroup = _readflags.8?true(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final title = _readstring(buffer);
    // final untilDate = _readflags.16?int(buffer);
    // final result = ChannelForbidden(flags: flags, broadcast: broadcast, megagroup: megagroup, id: id, accessHash: accessHash, title: title, untilDate: untilDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b05: broadcast,
      b08: megagroup,
      b16: untilDate != null,
    );

    return v;
  }

  /// broadcast: bit 5 of flags.5?true
  final bool broadcast;

  /// megagroup: bit 8 of flags.8?true
  final bool megagroup;

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Title.
  final String title;

  /// Until Date.
  final DateTime? untilDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x17d493d5);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeString(title);
    final localUntilDateCopy = untilDate;
    if (localUntilDateCopy != null) {
      buffer.writeDateTime(localUntilDateCopy);
    }
  }
}

/// Predicate Chat Full.
abstract class ChatFullBase extends TlConstructor {
  /// Predicate Chat Full constructor.
  const ChatFullBase._() : super._();
}

/// Chat Full.
///
/// ID: `c9d31138`.
class ChatFull extends ChatFullBase {
  /// Chat Full constructor.
  const ChatFull({
    required this.canSetUsername,
    required this.hasScheduled,
    required this.translationsDisabled,
    required this.id,
    required this.about,
    required this.participants,
    this.chatPhoto,
    required this.notifySettings,
    this.exportedInvite,
    this.botInfo,
    this.pinnedMsgId,
    this.folderId,
    this.call,
    this.ttlPeriod,
    this.groupcallDefaultJoinAs,
    this.themeEmoticon,
    this.requestsPending,
    this.recentRequesters,
    this.availableReactions,
  }) : super._();

  /// Deserialize.
  factory ChatFull.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final canSetUsername = _readflags.7?true(buffer);
    // final hasScheduled = _readflags.8?true(buffer);
    // final translationsDisabled = _readflags.19?true(buffer);
    // final id = _readlong(buffer);
    // final about = _readstring(buffer);
    // final participants = _readChatParticipants(buffer);
    // final chatPhoto = _readflags.2?Photo(buffer);
    // final notifySettings = _readPeerNotifySettings(buffer);
    // final exportedInvite = _readflags.13?ExportedChatInvite(buffer);
    // final botInfo = _readflags.3?Vector<BotInfo>(buffer);
    // final pinnedMsgId = _readflags.6?int(buffer);
    // final folderId = _readflags.11?int(buffer);
    // final call = _readflags.12?InputGroupCall(buffer);
    // final ttlPeriod = _readflags.14?int(buffer);
    // final groupcallDefaultJoinAs = _readflags.15?Peer(buffer);
    // final themeEmoticon = _readflags.16?string(buffer);
    // final requestsPending = _readflags.17?int(buffer);
    // final recentRequesters = _readflags.17?Vector<long>(buffer);
    // final availableReactions = _readflags.18?ChatReactions(buffer);
    // final result = ChatFull(flags: flags, canSetUsername: canSetUsername, hasScheduled: hasScheduled, translationsDisabled: translationsDisabled, id: id, about: about, participants: participants, chatPhoto: chatPhoto, notifySettings: notifySettings, exportedInvite: exportedInvite, botInfo: botInfo, pinnedMsgId: pinnedMsgId, folderId: folderId, call: call, ttlPeriod: ttlPeriod, groupcallDefaultJoinAs: groupcallDefaultJoinAs, themeEmoticon: themeEmoticon, requestsPending: requestsPending, recentRequesters: recentRequesters, availableReactions: availableReactions,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b07: canSetUsername,
      b08: hasScheduled,
      b19: translationsDisabled,
      b02: chatPhoto != null,
      b13: exportedInvite != null,
      b03: botInfo != null,
      b06: pinnedMsgId != null,
      b11: folderId != null,
      b12: call != null,
      b14: ttlPeriod != null,
      b15: groupcallDefaultJoinAs != null,
      b16: themeEmoticon != null,
      b17: requestsPending != null || recentRequesters != null,
      b18: availableReactions != null,
    );

    return v;
  }

  /// can_set_username: bit 7 of flags.7?true
  final bool canSetUsername;

  /// has_scheduled: bit 8 of flags.8?true
  final bool hasScheduled;

  /// translations_disabled: bit 19 of flags.19?true
  final bool translationsDisabled;

  /// Id.
  final int id;

  /// About.
  final String about;

  /// Participants.
  final ChatParticipantsBase participants;

  /// Chat Photo.
  final PhotoBase? chatPhoto;

  /// Notify Settings.
  final PeerNotifySettingsBase notifySettings;

  /// Exported Invite.
  final ExportedChatInviteBase? exportedInvite;

  /// Bot Info.
  final List<BotInfoBase>? botInfo;

  /// Pinned Msg Id.
  final int? pinnedMsgId;

  /// Folder Id.
  final int? folderId;

  /// Call.
  final InputGroupCallBase? call;

  /// Ttl Period.
  final int? ttlPeriod;

  /// Groupcall Default Join As.
  final PeerBase? groupcallDefaultJoinAs;

  /// Theme Emoticon.
  final String? themeEmoticon;

  /// Requests Pending.
  final int? requestsPending;

  /// Recent Requesters.
  final List<int>? recentRequesters;

  /// Available Reactions.
  final ChatReactionsBase? availableReactions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc9d31138);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeString(about);
    buffer.writeObject(participants);
    final localChatPhotoCopy = chatPhoto;
    if (localChatPhotoCopy != null) {
      buffer.writeObject(localChatPhotoCopy);
    }
    buffer.writeObject(notifySettings);
    final localExportedInviteCopy = exportedInvite;
    if (localExportedInviteCopy != null) {
      buffer.writeObject(localExportedInviteCopy);
    }
    final localBotInfoCopy = botInfo;
    if (localBotInfoCopy != null) {
      buffer.writeVectorObject(localBotInfoCopy);
    }
    final localPinnedMsgIdCopy = pinnedMsgId;
    if (localPinnedMsgIdCopy != null) {
      buffer.writeInt(localPinnedMsgIdCopy);
    }
    final localFolderIdCopy = folderId;
    if (localFolderIdCopy != null) {
      buffer.writeInt(localFolderIdCopy);
    }
    final localCallCopy = call;
    if (localCallCopy != null) {
      buffer.writeObject(localCallCopy);
    }
    final localTtlPeriodCopy = ttlPeriod;
    if (localTtlPeriodCopy != null) {
      buffer.writeInt(localTtlPeriodCopy);
    }
    final localGroupcallDefaultJoinAsCopy = groupcallDefaultJoinAs;
    if (localGroupcallDefaultJoinAsCopy != null) {
      buffer.writeObject(localGroupcallDefaultJoinAsCopy);
    }
    final localThemeEmoticonCopy = themeEmoticon;
    if (localThemeEmoticonCopy != null) {
      buffer.writeString(localThemeEmoticonCopy);
    }
    final localRequestsPendingCopy = requestsPending;
    if (localRequestsPendingCopy != null) {
      buffer.writeInt(localRequestsPendingCopy);
    }
    final localRecentRequestersCopy = recentRequesters;
    if (localRecentRequestersCopy != null) {
      buffer.writeVectorLong(localRecentRequestersCopy);
    }
    final localAvailableReactionsCopy = availableReactions;
    if (localAvailableReactionsCopy != null) {
      buffer.writeObject(localAvailableReactionsCopy);
    }
  }
}

/// Channel Full.
///
/// ID: `0f2bcb6f`.
class ChannelFull extends ChatFullBase {
  /// Channel Full constructor.
  const ChannelFull({
    required this.canViewParticipants,
    required this.canSetUsername,
    required this.canSetStickers,
    required this.hiddenPrehistory,
    required this.canSetLocation,
    required this.hasScheduled,
    required this.canViewStats,
    required this.blocked,
    required this.canDeleteChannel,
    required this.antispam,
    required this.participantsHidden,
    required this.translationsDisabled,
    required this.storiesPinnedAvailable,
    required this.viewForumAsMessages,
    required this.id,
    required this.about,
    this.participantsCount,
    this.adminsCount,
    this.kickedCount,
    this.bannedCount,
    this.onlineCount,
    required this.readInboxMaxId,
    required this.readOutboxMaxId,
    required this.unreadCount,
    required this.chatPhoto,
    required this.notifySettings,
    this.exportedInvite,
    required this.botInfo,
    this.migratedFromChatId,
    this.migratedFromMaxId,
    this.pinnedMsgId,
    this.stickerset,
    this.availableMinId,
    this.folderId,
    this.linkedChatId,
    this.location,
    this.slowmodeSeconds,
    this.slowmodeNextSendDate,
    this.statsDc,
    required this.pts,
    this.call,
    this.ttlPeriod,
    this.pendingSuggestions,
    this.groupcallDefaultJoinAs,
    this.themeEmoticon,
    this.requestsPending,
    this.recentRequesters,
    this.defaultSendAs,
    this.availableReactions,
    this.stories,
    this.wallpaper,
  }) : super._();

  /// Deserialize.
  factory ChannelFull.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final canViewParticipants = _readflags.3?true(buffer);
    // final canSetUsername = _readflags.6?true(buffer);
    // final canSetStickers = _readflags.7?true(buffer);
    // final hiddenPrehistory = _readflags.10?true(buffer);
    // final canSetLocation = _readflags.16?true(buffer);
    // final hasScheduled = _readflags.19?true(buffer);
    // final canViewStats = _readflags.20?true(buffer);
    // final blocked = _readflags.22?true(buffer);
    // final flags2 = _read#(buffer);
    // final canDeleteChannel = _readflags2.0?true(buffer);
    // final antispam = _readflags2.1?true(buffer);
    // final participantsHidden = _readflags2.2?true(buffer);
    // final translationsDisabled = _readflags2.3?true(buffer);
    // final storiesPinnedAvailable = _readflags2.5?true(buffer);
    // final viewForumAsMessages = _readflags2.6?true(buffer);
    // final id = _readlong(buffer);
    // final about = _readstring(buffer);
    // final participantsCount = _readflags.0?int(buffer);
    // final adminsCount = _readflags.1?int(buffer);
    // final kickedCount = _readflags.2?int(buffer);
    // final bannedCount = _readflags.2?int(buffer);
    // final onlineCount = _readflags.13?int(buffer);
    // final readInboxMaxId = _readint(buffer);
    // final readOutboxMaxId = _readint(buffer);
    // final unreadCount = _readint(buffer);
    // final chatPhoto = _readPhoto(buffer);
    // final notifySettings = _readPeerNotifySettings(buffer);
    // final exportedInvite = _readflags.23?ExportedChatInvite(buffer);
    // final botInfo = _readVector<BotInfo>(buffer);
    // final migratedFromChatId = _readflags.4?long(buffer);
    // final migratedFromMaxId = _readflags.4?int(buffer);
    // final pinnedMsgId = _readflags.5?int(buffer);
    // final stickerset = _readflags.8?StickerSet(buffer);
    // final availableMinId = _readflags.9?int(buffer);
    // final folderId = _readflags.11?int(buffer);
    // final linkedChatId = _readflags.14?long(buffer);
    // final location = _readflags.15?ChannelLocation(buffer);
    // final slowmodeSeconds = _readflags.17?int(buffer);
    // final slowmodeNextSendDate = _readflags.18?int(buffer);
    // final statsDc = _readflags.12?int(buffer);
    // final pts = _readint(buffer);
    // final call = _readflags.21?InputGroupCall(buffer);
    // final ttlPeriod = _readflags.24?int(buffer);
    // final pendingSuggestions = _readflags.25?Vector<string>(buffer);
    // final groupcallDefaultJoinAs = _readflags.26?Peer(buffer);
    // final themeEmoticon = _readflags.27?string(buffer);
    // final requestsPending = _readflags.28?int(buffer);
    // final recentRequesters = _readflags.28?Vector<long>(buffer);
    // final defaultSendAs = _readflags.29?Peer(buffer);
    // final availableReactions = _readflags.30?ChatReactions(buffer);
    // final stories = _readflags2.4?PeerStories(buffer);
    // final wallpaper = _readflags2.7?WallPaper(buffer);
    // final result = ChannelFull(flags: flags, canViewParticipants: canViewParticipants, canSetUsername: canSetUsername, canSetStickers: canSetStickers, hiddenPrehistory: hiddenPrehistory, canSetLocation: canSetLocation, hasScheduled: hasScheduled, canViewStats: canViewStats, blocked: blocked, flags2: flags2, canDeleteChannel: canDeleteChannel, antispam: antispam, participantsHidden: participantsHidden, translationsDisabled: translationsDisabled, storiesPinnedAvailable: storiesPinnedAvailable, viewForumAsMessages: viewForumAsMessages, id: id, about: about, participantsCount: participantsCount, adminsCount: adminsCount, kickedCount: kickedCount, bannedCount: bannedCount, onlineCount: onlineCount, readInboxMaxId: readInboxMaxId, readOutboxMaxId: readOutboxMaxId, unreadCount: unreadCount, chatPhoto: chatPhoto, notifySettings: notifySettings, exportedInvite: exportedInvite, botInfo: botInfo, migratedFromChatId: migratedFromChatId, migratedFromMaxId: migratedFromMaxId, pinnedMsgId: pinnedMsgId, stickerset: stickerset, availableMinId: availableMinId, folderId: folderId, linkedChatId: linkedChatId, location: location, slowmodeSeconds: slowmodeSeconds, slowmodeNextSendDate: slowmodeNextSendDate, statsDc: statsDc, pts: pts, call: call, ttlPeriod: ttlPeriod, pendingSuggestions: pendingSuggestions, groupcallDefaultJoinAs: groupcallDefaultJoinAs, themeEmoticon: themeEmoticon, requestsPending: requestsPending, recentRequesters: recentRequesters, defaultSendAs: defaultSendAs, availableReactions: availableReactions, stories: stories, wallpaper: wallpaper,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: canViewParticipants,
      b06: canSetUsername,
      b07: canSetStickers,
      b10: hiddenPrehistory,
      b16: canSetLocation,
      b19: hasScheduled,
      b20: canViewStats,
      b22: blocked,
      b00: participantsCount != null,
      b01: adminsCount != null,
      b02: kickedCount != null || bannedCount != null,
      b13: onlineCount != null,
      b23: exportedInvite != null,
      b04: migratedFromChatId != null || migratedFromMaxId != null,
      b05: pinnedMsgId != null,
      b08: stickerset != null,
      b09: availableMinId != null,
      b11: folderId != null,
      b14: linkedChatId != null,
      b15: location != null,
      b17: slowmodeSeconds != null,
      b18: slowmodeNextSendDate != null,
      b12: statsDc != null,
      b21: call != null,
      b24: ttlPeriod != null,
      b25: pendingSuggestions != null,
      b26: groupcallDefaultJoinAs != null,
      b27: themeEmoticon != null,
      b28: requestsPending != null || recentRequesters != null,
      b29: defaultSendAs != null,
      b30: availableReactions != null,
    );

    return v;
  }

  /// Flags2.
  int get flags2 {
    final v = _flag(
      b00: canDeleteChannel,
      b01: antispam,
      b02: participantsHidden,
      b03: translationsDisabled,
      b05: storiesPinnedAvailable,
      b06: viewForumAsMessages,
      b04: stories != null,
      b07: wallpaper != null,
    );

    return v;
  }

  /// can_view_participants: bit 3 of flags.3?true
  final bool canViewParticipants;

  /// can_set_username: bit 6 of flags.6?true
  final bool canSetUsername;

  /// can_set_stickers: bit 7 of flags.7?true
  final bool canSetStickers;

  /// hidden_prehistory: bit 10 of flags.10?true
  final bool hiddenPrehistory;

  /// can_set_location: bit 16 of flags.16?true
  final bool canSetLocation;

  /// has_scheduled: bit 19 of flags.19?true
  final bool hasScheduled;

  /// can_view_stats: bit 20 of flags.20?true
  final bool canViewStats;

  /// blocked: bit 22 of flags.22?true
  final bool blocked;

  /// can_delete_channel: bit 0 of flags2.0?true
  final bool canDeleteChannel;

  /// antispam: bit 1 of flags2.1?true
  final bool antispam;

  /// participants_hidden: bit 2 of flags2.2?true
  final bool participantsHidden;

  /// translations_disabled: bit 3 of flags2.3?true
  final bool translationsDisabled;

  /// stories_pinned_available: bit 5 of flags2.5?true
  final bool storiesPinnedAvailable;

  /// view_forum_as_messages: bit 6 of flags2.6?true
  final bool viewForumAsMessages;

  /// Id.
  final int id;

  /// About.
  final String about;

  /// Participants Count.
  final int? participantsCount;

  /// Admins Count.
  final int? adminsCount;

  /// Kicked Count.
  final int? kickedCount;

  /// Banned Count.
  final int? bannedCount;

  /// Online Count.
  final int? onlineCount;

  /// Read Inbox Max Id.
  final int readInboxMaxId;

  /// Read Outbox Max Id.
  final int readOutboxMaxId;

  /// Unread Count.
  final int unreadCount;

  /// Chat Photo.
  final PhotoBase chatPhoto;

  /// Notify Settings.
  final PeerNotifySettingsBase notifySettings;

  /// Exported Invite.
  final ExportedChatInviteBase? exportedInvite;

  /// Bot Info.
  final List<BotInfoBase> botInfo;

  /// Migrated From Chat Id.
  final int? migratedFromChatId;

  /// Migrated From Max Id.
  final int? migratedFromMaxId;

  /// Pinned Msg Id.
  final int? pinnedMsgId;

  /// Stickerset.
  final StickerSetBase? stickerset;

  /// Available Min Id.
  final int? availableMinId;

  /// Folder Id.
  final int? folderId;

  /// Linked Chat Id.
  final int? linkedChatId;

  /// Location.
  final ChannelLocationBase? location;

  /// Slowmode Seconds.
  final int? slowmodeSeconds;

  /// Slowmode Next Send Date.
  final DateTime? slowmodeNextSendDate;

  /// Stats Dc.
  final int? statsDc;

  /// Pts.
  final int pts;

  /// Call.
  final InputGroupCallBase? call;

  /// Ttl Period.
  final int? ttlPeriod;

  /// Pending Suggestions.
  final List<String>? pendingSuggestions;

  /// Groupcall Default Join As.
  final PeerBase? groupcallDefaultJoinAs;

  /// Theme Emoticon.
  final String? themeEmoticon;

  /// Requests Pending.
  final int? requestsPending;

  /// Recent Requesters.
  final List<int>? recentRequesters;

  /// Default Send As.
  final PeerBase? defaultSendAs;

  /// Available Reactions.
  final ChatReactionsBase? availableReactions;

  /// Stories.
  final PeerStoriesBase? stories;

  /// Wallpaper.
  final WallPaperBase? wallpaper;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0f2bcb6f);
    buffer.writeInt(flags);
    buffer.writeInt(flags2);
    buffer.writeLong(id);
    buffer.writeString(about);
    final localParticipantsCountCopy = participantsCount;
    if (localParticipantsCountCopy != null) {
      buffer.writeInt(localParticipantsCountCopy);
    }
    final localAdminsCountCopy = adminsCount;
    if (localAdminsCountCopy != null) {
      buffer.writeInt(localAdminsCountCopy);
    }
    final localKickedCountCopy = kickedCount;
    if (localKickedCountCopy != null) {
      buffer.writeInt(localKickedCountCopy);
    }
    final localBannedCountCopy = bannedCount;
    if (localBannedCountCopy != null) {
      buffer.writeInt(localBannedCountCopy);
    }
    final localOnlineCountCopy = onlineCount;
    if (localOnlineCountCopy != null) {
      buffer.writeInt(localOnlineCountCopy);
    }
    buffer.writeInt(readInboxMaxId);
    buffer.writeInt(readOutboxMaxId);
    buffer.writeInt(unreadCount);
    buffer.writeObject(chatPhoto);
    buffer.writeObject(notifySettings);
    final localExportedInviteCopy = exportedInvite;
    if (localExportedInviteCopy != null) {
      buffer.writeObject(localExportedInviteCopy);
    }
    buffer.writeVectorObject(botInfo);
    final localMigratedFromChatIdCopy = migratedFromChatId;
    if (localMigratedFromChatIdCopy != null) {
      buffer.writeLong(localMigratedFromChatIdCopy);
    }
    final localMigratedFromMaxIdCopy = migratedFromMaxId;
    if (localMigratedFromMaxIdCopy != null) {
      buffer.writeInt(localMigratedFromMaxIdCopy);
    }
    final localPinnedMsgIdCopy = pinnedMsgId;
    if (localPinnedMsgIdCopy != null) {
      buffer.writeInt(localPinnedMsgIdCopy);
    }
    final localStickersetCopy = stickerset;
    if (localStickersetCopy != null) {
      buffer.writeObject(localStickersetCopy);
    }
    final localAvailableMinIdCopy = availableMinId;
    if (localAvailableMinIdCopy != null) {
      buffer.writeInt(localAvailableMinIdCopy);
    }
    final localFolderIdCopy = folderId;
    if (localFolderIdCopy != null) {
      buffer.writeInt(localFolderIdCopy);
    }
    final localLinkedChatIdCopy = linkedChatId;
    if (localLinkedChatIdCopy != null) {
      buffer.writeLong(localLinkedChatIdCopy);
    }
    final localLocationCopy = location;
    if (localLocationCopy != null) {
      buffer.writeObject(localLocationCopy);
    }
    final localSlowmodeSecondsCopy = slowmodeSeconds;
    if (localSlowmodeSecondsCopy != null) {
      buffer.writeInt(localSlowmodeSecondsCopy);
    }
    final localSlowmodeNextSendDateCopy = slowmodeNextSendDate;
    if (localSlowmodeNextSendDateCopy != null) {
      buffer.writeDateTime(localSlowmodeNextSendDateCopy);
    }
    final localStatsDcCopy = statsDc;
    if (localStatsDcCopy != null) {
      buffer.writeInt(localStatsDcCopy);
    }
    buffer.writeInt(pts);
    final localCallCopy = call;
    if (localCallCopy != null) {
      buffer.writeObject(localCallCopy);
    }
    final localTtlPeriodCopy = ttlPeriod;
    if (localTtlPeriodCopy != null) {
      buffer.writeInt(localTtlPeriodCopy);
    }
    final localPendingSuggestionsCopy = pendingSuggestions;
    if (localPendingSuggestionsCopy != null) {
      buffer.writeVectorString(localPendingSuggestionsCopy);
    }
    final localGroupcallDefaultJoinAsCopy = groupcallDefaultJoinAs;
    if (localGroupcallDefaultJoinAsCopy != null) {
      buffer.writeObject(localGroupcallDefaultJoinAsCopy);
    }
    final localThemeEmoticonCopy = themeEmoticon;
    if (localThemeEmoticonCopy != null) {
      buffer.writeString(localThemeEmoticonCopy);
    }
    final localRequestsPendingCopy = requestsPending;
    if (localRequestsPendingCopy != null) {
      buffer.writeInt(localRequestsPendingCopy);
    }
    final localRecentRequestersCopy = recentRequesters;
    if (localRecentRequestersCopy != null) {
      buffer.writeVectorLong(localRecentRequestersCopy);
    }
    final localDefaultSendAsCopy = defaultSendAs;
    if (localDefaultSendAsCopy != null) {
      buffer.writeObject(localDefaultSendAsCopy);
    }
    final localAvailableReactionsCopy = availableReactions;
    if (localAvailableReactionsCopy != null) {
      buffer.writeObject(localAvailableReactionsCopy);
    }
    final localStoriesCopy = stories;
    if (localStoriesCopy != null) {
      buffer.writeObject(localStoriesCopy);
    }
    final localWallpaperCopy = wallpaper;
    if (localWallpaperCopy != null) {
      buffer.writeObject(localWallpaperCopy);
    }
  }
}

/// Predicate Chat Participant.
abstract class ChatParticipantBase extends TlConstructor {
  /// Predicate Chat Participant constructor.
  const ChatParticipantBase._() : super._();
}

/// Chat Participant.
///
/// ID: `c02d4007`.
class ChatParticipant extends ChatParticipantBase {
  /// Chat Participant constructor.
  const ChatParticipant({
    required this.userId,
    required this.inviterId,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory ChatParticipant.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final inviterId = _readlong(buffer);
    // final date = _readint(buffer);
    // final result = ChatParticipant(userId: userId, inviterId: inviterId, date: date,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Inviter Id.
  final int inviterId;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc02d4007);
    buffer.writeLong(userId);
    buffer.writeLong(inviterId);
    buffer.writeDateTime(date);
  }
}

/// Chat Participant Creator.
///
/// ID: `e46bcee4`.
class ChatParticipantCreator extends ChatParticipantBase {
  /// Chat Participant Creator constructor.
  const ChatParticipantCreator({
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory ChatParticipantCreator.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final result = ChatParticipantCreator(userId: userId,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe46bcee4);
    buffer.writeLong(userId);
  }
}

/// Chat Participant Admin.
///
/// ID: `a0933f5b`.
class ChatParticipantAdmin extends ChatParticipantBase {
  /// Chat Participant Admin constructor.
  const ChatParticipantAdmin({
    required this.userId,
    required this.inviterId,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory ChatParticipantAdmin.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final inviterId = _readlong(buffer);
    // final date = _readint(buffer);
    // final result = ChatParticipantAdmin(userId: userId, inviterId: inviterId, date: date,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Inviter Id.
  final int inviterId;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa0933f5b);
    buffer.writeLong(userId);
    buffer.writeLong(inviterId);
    buffer.writeDateTime(date);
  }
}

/// Predicate Chat Participants.
abstract class ChatParticipantsBase extends TlConstructor {
  /// Predicate Chat Participants constructor.
  const ChatParticipantsBase._() : super._();
}

/// Chat Participants Forbidden.
///
/// ID: `8763d3e1`.
class ChatParticipantsForbidden extends ChatParticipantsBase {
  /// Chat Participants Forbidden constructor.
  const ChatParticipantsForbidden({
    required this.chatId,
    this.selfParticipant,
  }) : super._();

  /// Deserialize.
  factory ChatParticipantsForbidden.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final chatId = _readlong(buffer);
    // final selfParticipant = _readflags.0?ChatParticipant(buffer);
    // final result = ChatParticipantsForbidden(flags: flags, chatId: chatId, selfParticipant: selfParticipant,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: selfParticipant != null,
    );

    return v;
  }

  /// Chat Id.
  final int chatId;

  /// Self Participant.
  final ChatParticipantBase? selfParticipant;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8763d3e1);
    buffer.writeInt(flags);
    buffer.writeLong(chatId);
    final localSelfParticipantCopy = selfParticipant;
    if (localSelfParticipantCopy != null) {
      buffer.writeObject(localSelfParticipantCopy);
    }
  }
}

/// Chat Participants.
///
/// ID: `3cbc93f8`.
class ChatParticipants extends ChatParticipantsBase {
  /// Chat Participants constructor.
  const ChatParticipants({
    required this.chatId,
    required this.participants,
    required this.version,
  }) : super._();

  /// Deserialize.
  factory ChatParticipants.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final participants = _readVector<ChatParticipant>(buffer);
    // final version = _readint(buffer);
    // final result = ChatParticipants(chatId: chatId, participants: participants, version: version,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// Participants.
  final List<ChatParticipantBase> participants;

  /// Version.
  final int version;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3cbc93f8);
    buffer.writeLong(chatId);
    buffer.writeVectorObject(participants);
    buffer.writeInt(version);
  }
}

/// Predicate Chat Photo.
abstract class ChatPhotoBase extends TlConstructor {
  /// Predicate Chat Photo constructor.
  const ChatPhotoBase._() : super._();
}

/// Chat Photo Empty.
///
/// ID: `37c1011c`.
class ChatPhotoEmpty extends ChatPhotoBase {
  /// Chat Photo Empty constructor.
  const ChatPhotoEmpty() : super._();

  /// Deserialize.
  factory ChatPhotoEmpty.deserialize(Uint8List buffer) {
    // final result = ChatPhotoEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x37c1011c);
  }
}

/// Chat Photo.
///
/// ID: `1c6e1c11`.
class ChatPhoto extends ChatPhotoBase {
  /// Chat Photo constructor.
  const ChatPhoto({
    required this.hasVideo,
    required this.photoId,
    this.strippedThumb,
    required this.dcId,
  }) : super._();

  /// Deserialize.
  factory ChatPhoto.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final hasVideo = _readflags.0?true(buffer);
    // final photoId = _readlong(buffer);
    // final strippedThumb = _readflags.1?bytes(buffer);
    // final dcId = _readint(buffer);
    // final result = ChatPhoto(flags: flags, hasVideo: hasVideo, photoId: photoId, strippedThumb: strippedThumb, dcId: dcId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: hasVideo,
      b01: strippedThumb != null,
    );

    return v;
  }

  /// has_video: bit 0 of flags.0?true
  final bool hasVideo;

  /// Photo Id.
  final int photoId;

  /// Stripped Thumb.
  final Uint8List? strippedThumb;

  /// Dc Id.
  final int dcId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1c6e1c11);
    buffer.writeInt(flags);
    buffer.writeLong(photoId);
    final localStrippedThumbCopy = strippedThumb;
    if (localStrippedThumbCopy != null) {
      buffer.writeBytes(localStrippedThumbCopy);
    }
    buffer.writeInt(dcId);
  }
}

/// Predicate Message.
abstract class MessageBase extends TlConstructor {
  /// Predicate Message constructor.
  const MessageBase._() : super._();
}

/// Message Empty.
///
/// ID: `90a6ca84`.
class MessageEmpty extends MessageBase {
  /// Message Empty constructor.
  const MessageEmpty({
    required this.id,
    this.peerId,
  }) : super._();

  /// Deserialize.
  factory MessageEmpty.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final id = _readint(buffer);
    // final peerId = _readflags.0?Peer(buffer);
    // final result = MessageEmpty(flags: flags, id: id, peerId: peerId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: peerId != null,
    );

    return v;
  }

  /// Id.
  final int id;

  /// Peer Id.
  final PeerBase? peerId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x90a6ca84);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    final localPeerIdCopy = peerId;
    if (localPeerIdCopy != null) {
      buffer.writeObject(localPeerIdCopy);
    }
  }
}

/// Message.
///
/// ID: `76bec211`.
class Message extends MessageBase {
  /// Message constructor.
  const Message({
    required this.out,
    required this.mentioned,
    required this.mediaUnread,
    required this.silent,
    required this.post,
    required this.fromScheduled,
    required this.legacy,
    required this.editHide,
    required this.pinned,
    required this.noforwards,
    required this.invertMedia,
    required this.id,
    this.fromId,
    required this.peerId,
    this.savedPeerId,
    this.fwdFrom,
    this.viaBotId,
    this.replyTo,
    required this.date,
    required this.message,
    this.media,
    this.replyMarkup,
    this.entities,
    this.views,
    this.forwards,
    this.replies,
    this.editDate,
    this.postAuthor,
    this.groupedId,
    this.reactions,
    this.restrictionReason,
    this.ttlPeriod,
  }) : super._();

  /// Deserialize.
  factory Message.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final out = _readflags.1?true(buffer);
    // final mentioned = _readflags.4?true(buffer);
    // final mediaUnread = _readflags.5?true(buffer);
    // final silent = _readflags.13?true(buffer);
    // final post = _readflags.14?true(buffer);
    // final fromScheduled = _readflags.18?true(buffer);
    // final legacy = _readflags.19?true(buffer);
    // final editHide = _readflags.21?true(buffer);
    // final pinned = _readflags.24?true(buffer);
    // final noforwards = _readflags.26?true(buffer);
    // final invertMedia = _readflags.27?true(buffer);
    // final id = _readint(buffer);
    // final fromId = _readflags.8?Peer(buffer);
    // final peerId = _readPeer(buffer);
    // final savedPeerId = _readflags.28?Peer(buffer);
    // final fwdFrom = _readflags.2?MessageFwdHeader(buffer);
    // final viaBotId = _readflags.11?long(buffer);
    // final replyTo = _readflags.3?MessageReplyHeader(buffer);
    // final date = _readint(buffer);
    // final message = _readstring(buffer);
    // final media = _readflags.9?MessageMedia(buffer);
    // final replyMarkup = _readflags.6?ReplyMarkup(buffer);
    // final entities = _readflags.7?Vector<MessageEntity>(buffer);
    // final views = _readflags.10?int(buffer);
    // final forwards = _readflags.10?int(buffer);
    // final replies = _readflags.23?MessageReplies(buffer);
    // final editDate = _readflags.15?int(buffer);
    // final postAuthor = _readflags.16?string(buffer);
    // final groupedId = _readflags.17?long(buffer);
    // final reactions = _readflags.20?MessageReactions(buffer);
    // final restrictionReason = _readflags.22?Vector<RestrictionReason>(buffer);
    // final ttlPeriod = _readflags.25?int(buffer);
    // final result = Message(flags: flags, out: out, mentioned: mentioned, mediaUnread: mediaUnread, silent: silent, post: post, fromScheduled: fromScheduled, legacy: legacy, editHide: editHide, pinned: pinned, noforwards: noforwards, invertMedia: invertMedia, id: id, fromId: fromId, peerId: peerId, savedPeerId: savedPeerId, fwdFrom: fwdFrom, viaBotId: viaBotId, replyTo: replyTo, date: date, message: message, media: media, replyMarkup: replyMarkup, entities: entities, views: views, forwards: forwards, replies: replies, editDate: editDate, postAuthor: postAuthor, groupedId: groupedId, reactions: reactions, restrictionReason: restrictionReason, ttlPeriod: ttlPeriod,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: out,
      b04: mentioned,
      b05: mediaUnread,
      b13: silent,
      b14: post,
      b18: fromScheduled,
      b19: legacy,
      b21: editHide,
      b24: pinned,
      b26: noforwards,
      b27: invertMedia,
      b08: fromId != null,
      b28: savedPeerId != null,
      b02: fwdFrom != null,
      b11: viaBotId != null,
      b03: replyTo != null,
      b09: media != null,
      b06: replyMarkup != null,
      b07: entities != null,
      b10: views != null || forwards != null,
      b23: replies != null,
      b15: editDate != null,
      b16: postAuthor != null,
      b17: groupedId != null,
      b20: reactions != null,
      b22: restrictionReason != null,
      b25: ttlPeriod != null,
    );

    return v;
  }

  /// out: bit 1 of flags.1?true
  final bool out;

  /// mentioned: bit 4 of flags.4?true
  final bool mentioned;

  /// media_unread: bit 5 of flags.5?true
  final bool mediaUnread;

  /// silent: bit 13 of flags.13?true
  final bool silent;

  /// post: bit 14 of flags.14?true
  final bool post;

  /// from_scheduled: bit 18 of flags.18?true
  final bool fromScheduled;

  /// legacy: bit 19 of flags.19?true
  final bool legacy;

  /// edit_hide: bit 21 of flags.21?true
  final bool editHide;

  /// pinned: bit 24 of flags.24?true
  final bool pinned;

  /// noforwards: bit 26 of flags.26?true
  final bool noforwards;

  /// invert_media: bit 27 of flags.27?true
  final bool invertMedia;

  /// Id.
  final int id;

  /// From Id.
  final PeerBase? fromId;

  /// Peer Id.
  final PeerBase peerId;

  /// Saved Peer Id.
  final PeerBase? savedPeerId;

  /// Fwd From.
  final MessageFwdHeaderBase? fwdFrom;

  /// Via Bot Id.
  final int? viaBotId;

  /// Reply To.
  final MessageReplyHeaderBase? replyTo;

  /// Date.
  final DateTime date;

  /// Message.
  final String message;

  /// Media.
  final MessageMediaBase? media;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Views.
  final int? views;

  /// Forwards.
  final int? forwards;

  /// Replies.
  final MessageRepliesBase? replies;

  /// Edit Date.
  final DateTime? editDate;

  /// Post Author.
  final String? postAuthor;

  /// Grouped Id.
  final int? groupedId;

  /// Reactions.
  final MessageReactionsBase? reactions;

  /// Restriction Reason.
  final List<RestrictionReasonBase>? restrictionReason;

  /// Ttl Period.
  final int? ttlPeriod;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x76bec211);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    final localFromIdCopy = fromId;
    if (localFromIdCopy != null) {
      buffer.writeObject(localFromIdCopy);
    }
    buffer.writeObject(peerId);
    final localSavedPeerIdCopy = savedPeerId;
    if (localSavedPeerIdCopy != null) {
      buffer.writeObject(localSavedPeerIdCopy);
    }
    final localFwdFromCopy = fwdFrom;
    if (localFwdFromCopy != null) {
      buffer.writeObject(localFwdFromCopy);
    }
    final localViaBotIdCopy = viaBotId;
    if (localViaBotIdCopy != null) {
      buffer.writeLong(localViaBotIdCopy);
    }
    final localReplyToCopy = replyTo;
    if (localReplyToCopy != null) {
      buffer.writeObject(localReplyToCopy);
    }
    buffer.writeDateTime(date);
    buffer.writeString(message);
    final localMediaCopy = media;
    if (localMediaCopy != null) {
      buffer.writeObject(localMediaCopy);
    }
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localViewsCopy = views;
    if (localViewsCopy != null) {
      buffer.writeInt(localViewsCopy);
    }
    final localForwardsCopy = forwards;
    if (localForwardsCopy != null) {
      buffer.writeInt(localForwardsCopy);
    }
    final localRepliesCopy = replies;
    if (localRepliesCopy != null) {
      buffer.writeObject(localRepliesCopy);
    }
    final localEditDateCopy = editDate;
    if (localEditDateCopy != null) {
      buffer.writeDateTime(localEditDateCopy);
    }
    final localPostAuthorCopy = postAuthor;
    if (localPostAuthorCopy != null) {
      buffer.writeString(localPostAuthorCopy);
    }
    final localGroupedIdCopy = groupedId;
    if (localGroupedIdCopy != null) {
      buffer.writeLong(localGroupedIdCopy);
    }
    final localReactionsCopy = reactions;
    if (localReactionsCopy != null) {
      buffer.writeObject(localReactionsCopy);
    }
    final localRestrictionReasonCopy = restrictionReason;
    if (localRestrictionReasonCopy != null) {
      buffer.writeVectorObject(localRestrictionReasonCopy);
    }
    final localTtlPeriodCopy = ttlPeriod;
    if (localTtlPeriodCopy != null) {
      buffer.writeInt(localTtlPeriodCopy);
    }
  }
}

/// Message Service.
///
/// ID: `2b085862`.
class MessageService extends MessageBase {
  /// Message Service constructor.
  const MessageService({
    required this.out,
    required this.mentioned,
    required this.mediaUnread,
    required this.silent,
    required this.post,
    required this.legacy,
    required this.id,
    this.fromId,
    required this.peerId,
    this.replyTo,
    required this.date,
    required this.action,
    this.ttlPeriod,
  }) : super._();

  /// Deserialize.
  factory MessageService.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final out = _readflags.1?true(buffer);
    // final mentioned = _readflags.4?true(buffer);
    // final mediaUnread = _readflags.5?true(buffer);
    // final silent = _readflags.13?true(buffer);
    // final post = _readflags.14?true(buffer);
    // final legacy = _readflags.19?true(buffer);
    // final id = _readint(buffer);
    // final fromId = _readflags.8?Peer(buffer);
    // final peerId = _readPeer(buffer);
    // final replyTo = _readflags.3?MessageReplyHeader(buffer);
    // final date = _readint(buffer);
    // final action = _readMessageAction(buffer);
    // final ttlPeriod = _readflags.25?int(buffer);
    // final result = MessageService(flags: flags, out: out, mentioned: mentioned, mediaUnread: mediaUnread, silent: silent, post: post, legacy: legacy, id: id, fromId: fromId, peerId: peerId, replyTo: replyTo, date: date, action: action, ttlPeriod: ttlPeriod,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: out,
      b04: mentioned,
      b05: mediaUnread,
      b13: silent,
      b14: post,
      b19: legacy,
      b08: fromId != null,
      b03: replyTo != null,
      b25: ttlPeriod != null,
    );

    return v;
  }

  /// out: bit 1 of flags.1?true
  final bool out;

  /// mentioned: bit 4 of flags.4?true
  final bool mentioned;

  /// media_unread: bit 5 of flags.5?true
  final bool mediaUnread;

  /// silent: bit 13 of flags.13?true
  final bool silent;

  /// post: bit 14 of flags.14?true
  final bool post;

  /// legacy: bit 19 of flags.19?true
  final bool legacy;

  /// Id.
  final int id;

  /// From Id.
  final PeerBase? fromId;

  /// Peer Id.
  final PeerBase peerId;

  /// Reply To.
  final MessageReplyHeaderBase? replyTo;

  /// Date.
  final DateTime date;

  /// Action.
  final MessageActionBase action;

  /// Ttl Period.
  final int? ttlPeriod;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2b085862);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    final localFromIdCopy = fromId;
    if (localFromIdCopy != null) {
      buffer.writeObject(localFromIdCopy);
    }
    buffer.writeObject(peerId);
    final localReplyToCopy = replyTo;
    if (localReplyToCopy != null) {
      buffer.writeObject(localReplyToCopy);
    }
    buffer.writeDateTime(date);
    buffer.writeObject(action);
    final localTtlPeriodCopy = ttlPeriod;
    if (localTtlPeriodCopy != null) {
      buffer.writeInt(localTtlPeriodCopy);
    }
  }
}

/// Predicate Message Media.
abstract class MessageMediaBase extends TlConstructor {
  /// Predicate Message Media constructor.
  const MessageMediaBase._() : super._();
}

/// Message Media Empty.
///
/// ID: `3ded6320`.
class MessageMediaEmpty extends MessageMediaBase {
  /// Message Media Empty constructor.
  const MessageMediaEmpty() : super._();

  /// Deserialize.
  factory MessageMediaEmpty.deserialize(Uint8List buffer) {
    // final result = MessageMediaEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3ded6320);
  }
}

/// Message Media Photo.
///
/// ID: `695150d7`.
class MessageMediaPhoto extends MessageMediaBase {
  /// Message Media Photo constructor.
  const MessageMediaPhoto({
    required this.spoiler,
    this.photo,
    this.ttlSeconds,
  }) : super._();

  /// Deserialize.
  factory MessageMediaPhoto.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final spoiler = _readflags.3?true(buffer);
    // final photo = _readflags.0?Photo(buffer);
    // final ttlSeconds = _readflags.2?int(buffer);
    // final result = MessageMediaPhoto(flags: flags, spoiler: spoiler, photo: photo, ttlSeconds: ttlSeconds,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: spoiler,
      b00: photo != null,
      b02: ttlSeconds != null,
    );

    return v;
  }

  /// spoiler: bit 3 of flags.3?true
  final bool spoiler;

  /// Photo.
  final PhotoBase? photo;

  /// Ttl Seconds.
  final int? ttlSeconds;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x695150d7);
    buffer.writeInt(flags);
    final localPhotoCopy = photo;
    if (localPhotoCopy != null) {
      buffer.writeObject(localPhotoCopy);
    }
    final localTtlSecondsCopy = ttlSeconds;
    if (localTtlSecondsCopy != null) {
      buffer.writeInt(localTtlSecondsCopy);
    }
  }
}

/// Message Media Geo.
///
/// ID: `56e0d474`.
class MessageMediaGeo extends MessageMediaBase {
  /// Message Media Geo constructor.
  const MessageMediaGeo({
    required this.geo,
  }) : super._();

  /// Deserialize.
  factory MessageMediaGeo.deserialize(Uint8List buffer) {
    // final geo = _readGeoPoint(buffer);
    // final result = MessageMediaGeo(geo: geo,);

    // return result;

    throw Exception();
  }

  /// Geo.
  final GeoPointBase geo;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x56e0d474);
    buffer.writeObject(geo);
  }
}

/// Message Media Contact.
///
/// ID: `70322949`.
class MessageMediaContact extends MessageMediaBase {
  /// Message Media Contact constructor.
  const MessageMediaContact({
    required this.phoneNumber,
    required this.firstName,
    required this.lastName,
    required this.vcard,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory MessageMediaContact.deserialize(Uint8List buffer) {
    // final phoneNumber = _readstring(buffer);
    // final firstName = _readstring(buffer);
    // final lastName = _readstring(buffer);
    // final vcard = _readstring(buffer);
    // final userId = _readlong(buffer);
    // final result = MessageMediaContact(phoneNumber: phoneNumber, firstName: firstName, lastName: lastName, vcard: vcard, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Phone Number.
  final String phoneNumber;

  /// First Name.
  final String firstName;

  /// Last Name.
  final String lastName;

  /// Vcard.
  final String vcard;

  /// User Id.
  final int userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x70322949);
    buffer.writeString(phoneNumber);
    buffer.writeString(firstName);
    buffer.writeString(lastName);
    buffer.writeString(vcard);
    buffer.writeLong(userId);
  }
}

/// Message Media Unsupported.
///
/// ID: `9f84f49e`.
class MessageMediaUnsupported extends MessageMediaBase {
  /// Message Media Unsupported constructor.
  const MessageMediaUnsupported() : super._();

  /// Deserialize.
  factory MessageMediaUnsupported.deserialize(Uint8List buffer) {
    // final result = MessageMediaUnsupported();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9f84f49e);
  }
}

/// Message Media Document.
///
/// ID: `4cf4d72d`.
class MessageMediaDocument extends MessageMediaBase {
  /// Message Media Document constructor.
  const MessageMediaDocument({
    required this.nopremium,
    required this.spoiler,
    required this.video,
    required this.round,
    required this.voice,
    this.document,
    this.altDocument,
    this.ttlSeconds,
  }) : super._();

  /// Deserialize.
  factory MessageMediaDocument.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final nopremium = _readflags.3?true(buffer);
    // final spoiler = _readflags.4?true(buffer);
    // final video = _readflags.6?true(buffer);
    // final round = _readflags.7?true(buffer);
    // final voice = _readflags.8?true(buffer);
    // final document = _readflags.0?Document(buffer);
    // final altDocument = _readflags.5?Document(buffer);
    // final ttlSeconds = _readflags.2?int(buffer);
    // final result = MessageMediaDocument(flags: flags, nopremium: nopremium, spoiler: spoiler, video: video, round: round, voice: voice, document: document, altDocument: altDocument, ttlSeconds: ttlSeconds,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: nopremium,
      b04: spoiler,
      b06: video,
      b07: round,
      b08: voice,
      b00: document != null,
      b05: altDocument != null,
      b02: ttlSeconds != null,
    );

    return v;
  }

  /// nopremium: bit 3 of flags.3?true
  final bool nopremium;

  /// spoiler: bit 4 of flags.4?true
  final bool spoiler;

  /// video: bit 6 of flags.6?true
  final bool video;

  /// round: bit 7 of flags.7?true
  final bool round;

  /// voice: bit 8 of flags.8?true
  final bool voice;

  /// Document.
  final DocumentBase? document;

  /// Alt Document.
  final DocumentBase? altDocument;

  /// Ttl Seconds.
  final int? ttlSeconds;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4cf4d72d);
    buffer.writeInt(flags);
    final localDocumentCopy = document;
    if (localDocumentCopy != null) {
      buffer.writeObject(localDocumentCopy);
    }
    final localAltDocumentCopy = altDocument;
    if (localAltDocumentCopy != null) {
      buffer.writeObject(localAltDocumentCopy);
    }
    final localTtlSecondsCopy = ttlSeconds;
    if (localTtlSecondsCopy != null) {
      buffer.writeInt(localTtlSecondsCopy);
    }
  }
}

/// Message Media Web Page.
///
/// ID: `ddf10c3b`.
class MessageMediaWebPage extends MessageMediaBase {
  /// Message Media Web Page constructor.
  const MessageMediaWebPage({
    required this.forceLargeMedia,
    required this.forceSmallMedia,
    required this.manual,
    required this.safe,
    required this.webpage,
  }) : super._();

  /// Deserialize.
  factory MessageMediaWebPage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final forceLargeMedia = _readflags.0?true(buffer);
    // final forceSmallMedia = _readflags.1?true(buffer);
    // final manual = _readflags.3?true(buffer);
    // final safe = _readflags.4?true(buffer);
    // final webpage = _readWebPage(buffer);
    // final result = MessageMediaWebPage(flags: flags, forceLargeMedia: forceLargeMedia, forceSmallMedia: forceSmallMedia, manual: manual, safe: safe, webpage: webpage,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: forceLargeMedia,
      b01: forceSmallMedia,
      b03: manual,
      b04: safe,
    );

    return v;
  }

  /// force_large_media: bit 0 of flags.0?true
  final bool forceLargeMedia;

  /// force_small_media: bit 1 of flags.1?true
  final bool forceSmallMedia;

  /// manual: bit 3 of flags.3?true
  final bool manual;

  /// safe: bit 4 of flags.4?true
  final bool safe;

  /// Webpage.
  final WebPageBase webpage;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xddf10c3b);
    buffer.writeInt(flags);
    buffer.writeObject(webpage);
  }
}

/// Message Media Venue.
///
/// ID: `2ec0533f`.
class MessageMediaVenue extends MessageMediaBase {
  /// Message Media Venue constructor.
  const MessageMediaVenue({
    required this.geo,
    required this.title,
    required this.address,
    required this.provider,
    required this.venueId,
    required this.venueType,
  }) : super._();

  /// Deserialize.
  factory MessageMediaVenue.deserialize(Uint8List buffer) {
    // final geo = _readGeoPoint(buffer);
    // final title = _readstring(buffer);
    // final address = _readstring(buffer);
    // final provider = _readstring(buffer);
    // final venueId = _readstring(buffer);
    // final venueType = _readstring(buffer);
    // final result = MessageMediaVenue(geo: geo, title: title, address: address, provider: provider, venueId: venueId, venueType: venueType,);

    // return result;

    throw Exception();
  }

  /// Geo.
  final GeoPointBase geo;

  /// Title.
  final String title;

  /// Address.
  final String address;

  /// Provider.
  final String provider;

  /// Venue Id.
  final String venueId;

  /// Venue Type.
  final String venueType;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2ec0533f);
    buffer.writeObject(geo);
    buffer.writeString(title);
    buffer.writeString(address);
    buffer.writeString(provider);
    buffer.writeString(venueId);
    buffer.writeString(venueType);
  }
}

/// Message Media Game.
///
/// ID: `fdb19008`.
class MessageMediaGame extends MessageMediaBase {
  /// Message Media Game constructor.
  const MessageMediaGame({
    required this.game,
  }) : super._();

  /// Deserialize.
  factory MessageMediaGame.deserialize(Uint8List buffer) {
    // final game = _readGame(buffer);
    // final result = MessageMediaGame(game: game,);

    // return result;

    throw Exception();
  }

  /// Game.
  final GameBase game;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfdb19008);
    buffer.writeObject(game);
  }
}

/// Message Media Invoice.
///
/// ID: `f6a548d3`.
class MessageMediaInvoice extends MessageMediaBase {
  /// Message Media Invoice constructor.
  const MessageMediaInvoice({
    required this.shippingAddressRequested,
    required this.test,
    required this.title,
    required this.description,
    this.photo,
    this.receiptMsgId,
    required this.currency,
    required this.totalAmount,
    required this.startParam,
    this.extendedMedia,
  }) : super._();

  /// Deserialize.
  factory MessageMediaInvoice.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final shippingAddressRequested = _readflags.1?true(buffer);
    // final test = _readflags.3?true(buffer);
    // final title = _readstring(buffer);
    // final description = _readstring(buffer);
    // final photo = _readflags.0?WebDocument(buffer);
    // final receiptMsgId = _readflags.2?int(buffer);
    // final currency = _readstring(buffer);
    // final totalAmount = _readlong(buffer);
    // final startParam = _readstring(buffer);
    // final extendedMedia = _readflags.4?MessageExtendedMedia(buffer);
    // final result = MessageMediaInvoice(flags: flags, shippingAddressRequested: shippingAddressRequested, test: test, title: title, description: description, photo: photo, receiptMsgId: receiptMsgId, currency: currency, totalAmount: totalAmount, startParam: startParam, extendedMedia: extendedMedia,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: shippingAddressRequested,
      b03: test,
      b00: photo != null,
      b02: receiptMsgId != null,
      b04: extendedMedia != null,
    );

    return v;
  }

  /// shipping_address_requested: bit 1 of flags.1?true
  final bool shippingAddressRequested;

  /// test: bit 3 of flags.3?true
  final bool test;

  /// Title.
  final String title;

  /// Description.
  final String description;

  /// Photo.
  final WebDocumentBase? photo;

  /// Receipt Msg Id.
  final int? receiptMsgId;

  /// Currency.
  final String currency;

  /// Total Amount.
  final int totalAmount;

  /// Start Param.
  final String startParam;

  /// Extended Media.
  final MessageExtendedMediaBase? extendedMedia;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf6a548d3);
    buffer.writeInt(flags);
    buffer.writeString(title);
    buffer.writeString(description);
    final localPhotoCopy = photo;
    if (localPhotoCopy != null) {
      buffer.writeObject(localPhotoCopy);
    }
    final localReceiptMsgIdCopy = receiptMsgId;
    if (localReceiptMsgIdCopy != null) {
      buffer.writeInt(localReceiptMsgIdCopy);
    }
    buffer.writeString(currency);
    buffer.writeLong(totalAmount);
    buffer.writeString(startParam);
    final localExtendedMediaCopy = extendedMedia;
    if (localExtendedMediaCopy != null) {
      buffer.writeObject(localExtendedMediaCopy);
    }
  }
}

/// Message Media Geo Live.
///
/// ID: `b940c666`.
class MessageMediaGeoLive extends MessageMediaBase {
  /// Message Media Geo Live constructor.
  const MessageMediaGeoLive({
    required this.geo,
    this.heading,
    required this.period,
    this.proximityNotificationRadius,
  }) : super._();

  /// Deserialize.
  factory MessageMediaGeoLive.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final geo = _readGeoPoint(buffer);
    // final heading = _readflags.0?int(buffer);
    // final period = _readint(buffer);
    // final proximityNotificationRadius = _readflags.1?int(buffer);
    // final result = MessageMediaGeoLive(flags: flags, geo: geo, heading: heading, period: period, proximityNotificationRadius: proximityNotificationRadius,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: heading != null,
      b01: proximityNotificationRadius != null,
    );

    return v;
  }

  /// Geo.
  final GeoPointBase geo;

  /// Heading.
  final int? heading;

  /// Period.
  final int period;

  /// Proximity Notification Radius.
  final int? proximityNotificationRadius;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb940c666);
    buffer.writeInt(flags);
    buffer.writeObject(geo);
    final localHeadingCopy = heading;
    if (localHeadingCopy != null) {
      buffer.writeInt(localHeadingCopy);
    }
    buffer.writeInt(period);
    final localProximityNotificationRadiusCopy = proximityNotificationRadius;
    if (localProximityNotificationRadiusCopy != null) {
      buffer.writeInt(localProximityNotificationRadiusCopy);
    }
  }
}

/// Message Media Poll.
///
/// ID: `4bd6e798`.
class MessageMediaPoll extends MessageMediaBase {
  /// Message Media Poll constructor.
  const MessageMediaPoll({
    required this.poll,
    required this.results,
  }) : super._();

  /// Deserialize.
  factory MessageMediaPoll.deserialize(Uint8List buffer) {
    // final poll = _readPoll(buffer);
    // final results = _readPollResults(buffer);
    // final result = MessageMediaPoll(poll: poll, results: results,);

    // return result;

    throw Exception();
  }

  /// Poll.
  final PollBase poll;

  /// Results.
  final PollResultsBase results;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4bd6e798);
    buffer.writeObject(poll);
    buffer.writeObject(results);
  }
}

/// Message Media Dice.
///
/// ID: `3f7ee58b`.
class MessageMediaDice extends MessageMediaBase {
  /// Message Media Dice constructor.
  const MessageMediaDice({
    required this.value,
    required this.emoticon,
  }) : super._();

  /// Deserialize.
  factory MessageMediaDice.deserialize(Uint8List buffer) {
    // final value = _readint(buffer);
    // final emoticon = _readstring(buffer);
    // final result = MessageMediaDice(value: value, emoticon: emoticon,);

    // return result;

    throw Exception();
  }

  /// Value.
  final int value;

  /// Emoticon.
  final String emoticon;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3f7ee58b);
    buffer.writeInt(value);
    buffer.writeString(emoticon);
  }
}

/// Message Media Story.
///
/// ID: `68cb6283`.
class MessageMediaStory extends MessageMediaBase {
  /// Message Media Story constructor.
  const MessageMediaStory({
    required this.viaMention,
    required this.peer,
    required this.id,
    this.story,
  }) : super._();

  /// Deserialize.
  factory MessageMediaStory.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final viaMention = _readflags.1?true(buffer);
    // final peer = _readPeer(buffer);
    // final id = _readint(buffer);
    // final story = _readflags.0?StoryItem(buffer);
    // final result = MessageMediaStory(flags: flags, viaMention: viaMention, peer: peer, id: id, story: story,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: viaMention,
      b00: story != null,
    );

    return v;
  }

  /// via_mention: bit 1 of flags.1?true
  final bool viaMention;

  /// Peer.
  final PeerBase peer;

  /// Id.
  final int id;

  /// Story.
  final StoryItemBase? story;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x68cb6283);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(id);
    final localStoryCopy = story;
    if (localStoryCopy != null) {
      buffer.writeObject(localStoryCopy);
    }
  }
}

/// Message Media Giveaway.
///
/// ID: `daad85b0`.
class MessageMediaGiveaway extends MessageMediaBase {
  /// Message Media Giveaway constructor.
  const MessageMediaGiveaway({
    required this.onlyNewSubscribers,
    required this.winnersAreVisible,
    required this.channels,
    this.countriesIso2,
    this.prizeDescription,
    required this.quantity,
    required this.months,
    required this.untilDate,
  }) : super._();

  /// Deserialize.
  factory MessageMediaGiveaway.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final onlyNewSubscribers = _readflags.0?true(buffer);
    // final winnersAreVisible = _readflags.2?true(buffer);
    // final channels = _readVector<long>(buffer);
    // final countriesIso2 = _readflags.1?Vector<string>(buffer);
    // final prizeDescription = _readflags.3?string(buffer);
    // final quantity = _readint(buffer);
    // final months = _readint(buffer);
    // final untilDate = _readint(buffer);
    // final result = MessageMediaGiveaway(flags: flags, onlyNewSubscribers: onlyNewSubscribers, winnersAreVisible: winnersAreVisible, channels: channels, countriesIso2: countriesIso2, prizeDescription: prizeDescription, quantity: quantity, months: months, untilDate: untilDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: onlyNewSubscribers,
      b02: winnersAreVisible,
      b01: countriesIso2 != null,
      b03: prizeDescription != null,
    );

    return v;
  }

  /// only_new_subscribers: bit 0 of flags.0?true
  final bool onlyNewSubscribers;

  /// winners_are_visible: bit 2 of flags.2?true
  final bool winnersAreVisible;

  /// Channels.
  final List<int> channels;

  /// Countries Iso2.
  final List<String>? countriesIso2;

  /// Prize Description.
  final String? prizeDescription;

  /// Quantity.
  final int quantity;

  /// Months.
  final int months;

  /// Until Date.
  final DateTime untilDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdaad85b0);
    buffer.writeInt(flags);
    buffer.writeVectorLong(channels);
    final localCountriesIso2Copy = countriesIso2;
    if (localCountriesIso2Copy != null) {
      buffer.writeVectorString(localCountriesIso2Copy);
    }
    final localPrizeDescriptionCopy = prizeDescription;
    if (localPrizeDescriptionCopy != null) {
      buffer.writeString(localPrizeDescriptionCopy);
    }
    buffer.writeInt(quantity);
    buffer.writeInt(months);
    buffer.writeDateTime(untilDate);
  }
}

/// Message Media Giveaway Results.
///
/// ID: `c6991068`.
class MessageMediaGiveawayResults extends MessageMediaBase {
  /// Message Media Giveaway Results constructor.
  const MessageMediaGiveawayResults({
    required this.onlyNewSubscribers,
    required this.refunded,
    required this.channelId,
    this.additionalPeersCount,
    required this.launchMsgId,
    required this.winnersCount,
    required this.unclaimedCount,
    required this.winners,
    required this.months,
    this.prizeDescription,
    required this.untilDate,
  }) : super._();

  /// Deserialize.
  factory MessageMediaGiveawayResults.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final onlyNewSubscribers = _readflags.0?true(buffer);
    // final refunded = _readflags.2?true(buffer);
    // final channelId = _readlong(buffer);
    // final additionalPeersCount = _readflags.3?int(buffer);
    // final launchMsgId = _readint(buffer);
    // final winnersCount = _readint(buffer);
    // final unclaimedCount = _readint(buffer);
    // final winners = _readVector<long>(buffer);
    // final months = _readint(buffer);
    // final prizeDescription = _readflags.1?string(buffer);
    // final untilDate = _readint(buffer);
    // final result = MessageMediaGiveawayResults(flags: flags, onlyNewSubscribers: onlyNewSubscribers, refunded: refunded, channelId: channelId, additionalPeersCount: additionalPeersCount, launchMsgId: launchMsgId, winnersCount: winnersCount, unclaimedCount: unclaimedCount, winners: winners, months: months, prizeDescription: prizeDescription, untilDate: untilDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: onlyNewSubscribers,
      b02: refunded,
      b03: additionalPeersCount != null,
      b01: prizeDescription != null,
    );

    return v;
  }

  /// only_new_subscribers: bit 0 of flags.0?true
  final bool onlyNewSubscribers;

  /// refunded: bit 2 of flags.2?true
  final bool refunded;

  /// Channel Id.
  final int channelId;

  /// Additional Peers Count.
  final int? additionalPeersCount;

  /// Launch Msg Id.
  final int launchMsgId;

  /// Winners Count.
  final int winnersCount;

  /// Unclaimed Count.
  final int unclaimedCount;

  /// Winners.
  final List<int> winners;

  /// Months.
  final int months;

  /// Prize Description.
  final String? prizeDescription;

  /// Until Date.
  final DateTime untilDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc6991068);
    buffer.writeInt(flags);
    buffer.writeLong(channelId);
    final localAdditionalPeersCountCopy = additionalPeersCount;
    if (localAdditionalPeersCountCopy != null) {
      buffer.writeInt(localAdditionalPeersCountCopy);
    }
    buffer.writeInt(launchMsgId);
    buffer.writeInt(winnersCount);
    buffer.writeInt(unclaimedCount);
    buffer.writeVectorLong(winners);
    buffer.writeInt(months);
    final localPrizeDescriptionCopy = prizeDescription;
    if (localPrizeDescriptionCopy != null) {
      buffer.writeString(localPrizeDescriptionCopy);
    }
    buffer.writeDateTime(untilDate);
  }
}

/// Predicate Message Action.
abstract class MessageActionBase extends TlConstructor {
  /// Predicate Message Action constructor.
  const MessageActionBase._() : super._();
}

/// Message Action Empty.
///
/// ID: `b6aef7b0`.
class MessageActionEmpty extends MessageActionBase {
  /// Message Action Empty constructor.
  const MessageActionEmpty() : super._();

  /// Deserialize.
  factory MessageActionEmpty.deserialize(Uint8List buffer) {
    // final result = MessageActionEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb6aef7b0);
  }
}

/// Message Action Chat Create.
///
/// ID: `bd47cbad`.
class MessageActionChatCreate extends MessageActionBase {
  /// Message Action Chat Create constructor.
  const MessageActionChatCreate({
    required this.title,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessageActionChatCreate.deserialize(Uint8List buffer) {
    // final title = _readstring(buffer);
    // final users = _readVector<long>(buffer);
    // final result = MessageActionChatCreate(title: title, users: users,);

    // return result;

    throw Exception();
  }

  /// Title.
  final String title;

  /// Users.
  final List<int> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbd47cbad);
    buffer.writeString(title);
    buffer.writeVectorLong(users);
  }
}

/// Message Action Chat Edit Title.
///
/// ID: `b5a1ce5a`.
class MessageActionChatEditTitle extends MessageActionBase {
  /// Message Action Chat Edit Title constructor.
  const MessageActionChatEditTitle({
    required this.title,
  }) : super._();

  /// Deserialize.
  factory MessageActionChatEditTitle.deserialize(Uint8List buffer) {
    // final title = _readstring(buffer);
    // final result = MessageActionChatEditTitle(title: title,);

    // return result;

    throw Exception();
  }

  /// Title.
  final String title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb5a1ce5a);
    buffer.writeString(title);
  }
}

/// Message Action Chat Edit Photo.
///
/// ID: `7fcb13a8`.
class MessageActionChatEditPhoto extends MessageActionBase {
  /// Message Action Chat Edit Photo constructor.
  const MessageActionChatEditPhoto({
    required this.photo,
  }) : super._();

  /// Deserialize.
  factory MessageActionChatEditPhoto.deserialize(Uint8List buffer) {
    // final photo = _readPhoto(buffer);
    // final result = MessageActionChatEditPhoto(photo: photo,);

    // return result;

    throw Exception();
  }

  /// Photo.
  final PhotoBase photo;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7fcb13a8);
    buffer.writeObject(photo);
  }
}

/// Message Action Chat Delete Photo.
///
/// ID: `95e3fbef`.
class MessageActionChatDeletePhoto extends MessageActionBase {
  /// Message Action Chat Delete Photo constructor.
  const MessageActionChatDeletePhoto() : super._();

  /// Deserialize.
  factory MessageActionChatDeletePhoto.deserialize(Uint8List buffer) {
    // final result = MessageActionChatDeletePhoto();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x95e3fbef);
  }
}

/// Message Action Chat Add User.
///
/// ID: `15cefd00`.
class MessageActionChatAddUser extends MessageActionBase {
  /// Message Action Chat Add User constructor.
  const MessageActionChatAddUser({
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessageActionChatAddUser.deserialize(Uint8List buffer) {
    // final users = _readVector<long>(buffer);
    // final result = MessageActionChatAddUser(users: users,);

    // return result;

    throw Exception();
  }

  /// Users.
  final List<int> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x15cefd00);
    buffer.writeVectorLong(users);
  }
}

/// Message Action Chat Delete User.
///
/// ID: `a43f30cc`.
class MessageActionChatDeleteUser extends MessageActionBase {
  /// Message Action Chat Delete User constructor.
  const MessageActionChatDeleteUser({
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory MessageActionChatDeleteUser.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final result = MessageActionChatDeleteUser(userId: userId,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa43f30cc);
    buffer.writeLong(userId);
  }
}

/// Message Action Chat Joined By Link.
///
/// ID: `031224c3`.
class MessageActionChatJoinedByLink extends MessageActionBase {
  /// Message Action Chat Joined By Link constructor.
  const MessageActionChatJoinedByLink({
    required this.inviterId,
  }) : super._();

  /// Deserialize.
  factory MessageActionChatJoinedByLink.deserialize(Uint8List buffer) {
    // final inviterId = _readlong(buffer);
    // final result = MessageActionChatJoinedByLink(inviterId: inviterId,);

    // return result;

    throw Exception();
  }

  /// Inviter Id.
  final int inviterId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x031224c3);
    buffer.writeLong(inviterId);
  }
}

/// Message Action Channel Create.
///
/// ID: `95d2ac92`.
class MessageActionChannelCreate extends MessageActionBase {
  /// Message Action Channel Create constructor.
  const MessageActionChannelCreate({
    required this.title,
  }) : super._();

  /// Deserialize.
  factory MessageActionChannelCreate.deserialize(Uint8List buffer) {
    // final title = _readstring(buffer);
    // final result = MessageActionChannelCreate(title: title,);

    // return result;

    throw Exception();
  }

  /// Title.
  final String title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x95d2ac92);
    buffer.writeString(title);
  }
}

/// Message Action Chat Migrate To.
///
/// ID: `e1037f92`.
class MessageActionChatMigrateTo extends MessageActionBase {
  /// Message Action Chat Migrate To constructor.
  const MessageActionChatMigrateTo({
    required this.channelId,
  }) : super._();

  /// Deserialize.
  factory MessageActionChatMigrateTo.deserialize(Uint8List buffer) {
    // final channelId = _readlong(buffer);
    // final result = MessageActionChatMigrateTo(channelId: channelId,);

    // return result;

    throw Exception();
  }

  /// Channel Id.
  final int channelId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe1037f92);
    buffer.writeLong(channelId);
  }
}

/// Message Action Channel Migrate From.
///
/// ID: `ea3948e9`.
class MessageActionChannelMigrateFrom extends MessageActionBase {
  /// Message Action Channel Migrate From constructor.
  const MessageActionChannelMigrateFrom({
    required this.title,
    required this.chatId,
  }) : super._();

  /// Deserialize.
  factory MessageActionChannelMigrateFrom.deserialize(Uint8List buffer) {
    // final title = _readstring(buffer);
    // final chatId = _readlong(buffer);
    // final result = MessageActionChannelMigrateFrom(title: title, chatId: chatId,);

    // return result;

    throw Exception();
  }

  /// Title.
  final String title;

  /// Chat Id.
  final int chatId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xea3948e9);
    buffer.writeString(title);
    buffer.writeLong(chatId);
  }
}

/// Message Action Pin Message.
///
/// ID: `94bd38ed`.
class MessageActionPinMessage extends MessageActionBase {
  /// Message Action Pin Message constructor.
  const MessageActionPinMessage() : super._();

  /// Deserialize.
  factory MessageActionPinMessage.deserialize(Uint8List buffer) {
    // final result = MessageActionPinMessage();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x94bd38ed);
  }
}

/// Message Action History Clear.
///
/// ID: `9fbab604`.
class MessageActionHistoryClear extends MessageActionBase {
  /// Message Action History Clear constructor.
  const MessageActionHistoryClear() : super._();

  /// Deserialize.
  factory MessageActionHistoryClear.deserialize(Uint8List buffer) {
    // final result = MessageActionHistoryClear();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9fbab604);
  }
}

/// Message Action Game Score.
///
/// ID: `92a72876`.
class MessageActionGameScore extends MessageActionBase {
  /// Message Action Game Score constructor.
  const MessageActionGameScore({
    required this.gameId,
    required this.score,
  }) : super._();

  /// Deserialize.
  factory MessageActionGameScore.deserialize(Uint8List buffer) {
    // final gameId = _readlong(buffer);
    // final score = _readint(buffer);
    // final result = MessageActionGameScore(gameId: gameId, score: score,);

    // return result;

    throw Exception();
  }

  /// Game Id.
  final int gameId;

  /// Score.
  final int score;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x92a72876);
    buffer.writeLong(gameId);
    buffer.writeInt(score);
  }
}

/// Message Action Payment Sent Me.
///
/// ID: `8f31b327`.
class MessageActionPaymentSentMe extends MessageActionBase {
  /// Message Action Payment Sent Me constructor.
  const MessageActionPaymentSentMe({
    required this.recurringInit,
    required this.recurringUsed,
    required this.currency,
    required this.totalAmount,
    required this.payload,
    this.info,
    this.shippingOptionId,
    required this.charge,
  }) : super._();

  /// Deserialize.
  factory MessageActionPaymentSentMe.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final recurringInit = _readflags.2?true(buffer);
    // final recurringUsed = _readflags.3?true(buffer);
    // final currency = _readstring(buffer);
    // final totalAmount = _readlong(buffer);
    // final payload = _readbytes(buffer);
    // final info = _readflags.0?PaymentRequestedInfo(buffer);
    // final shippingOptionId = _readflags.1?string(buffer);
    // final charge = _readPaymentCharge(buffer);
    // final result = MessageActionPaymentSentMe(flags: flags, recurringInit: recurringInit, recurringUsed: recurringUsed, currency: currency, totalAmount: totalAmount, payload: payload, info: info, shippingOptionId: shippingOptionId, charge: charge,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: recurringInit,
      b03: recurringUsed,
      b00: info != null,
      b01: shippingOptionId != null,
    );

    return v;
  }

  /// recurring_init: bit 2 of flags.2?true
  final bool recurringInit;

  /// recurring_used: bit 3 of flags.3?true
  final bool recurringUsed;

  /// Currency.
  final String currency;

  /// Total Amount.
  final int totalAmount;

  /// Payload.
  final Uint8List payload;

  /// Info.
  final PaymentRequestedInfoBase? info;

  /// Shipping Option Id.
  final String? shippingOptionId;

  /// Charge.
  final PaymentChargeBase charge;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8f31b327);
    buffer.writeInt(flags);
    buffer.writeString(currency);
    buffer.writeLong(totalAmount);
    buffer.writeBytes(payload);
    final localInfoCopy = info;
    if (localInfoCopy != null) {
      buffer.writeObject(localInfoCopy);
    }
    final localShippingOptionIdCopy = shippingOptionId;
    if (localShippingOptionIdCopy != null) {
      buffer.writeString(localShippingOptionIdCopy);
    }
    buffer.writeObject(charge);
  }
}

/// Message Action Payment Sent.
///
/// ID: `96163f56`.
class MessageActionPaymentSent extends MessageActionBase {
  /// Message Action Payment Sent constructor.
  const MessageActionPaymentSent({
    required this.recurringInit,
    required this.recurringUsed,
    required this.currency,
    required this.totalAmount,
    this.invoiceSlug,
  }) : super._();

  /// Deserialize.
  factory MessageActionPaymentSent.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final recurringInit = _readflags.2?true(buffer);
    // final recurringUsed = _readflags.3?true(buffer);
    // final currency = _readstring(buffer);
    // final totalAmount = _readlong(buffer);
    // final invoiceSlug = _readflags.0?string(buffer);
    // final result = MessageActionPaymentSent(flags: flags, recurringInit: recurringInit, recurringUsed: recurringUsed, currency: currency, totalAmount: totalAmount, invoiceSlug: invoiceSlug,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: recurringInit,
      b03: recurringUsed,
      b00: invoiceSlug != null,
    );

    return v;
  }

  /// recurring_init: bit 2 of flags.2?true
  final bool recurringInit;

  /// recurring_used: bit 3 of flags.3?true
  final bool recurringUsed;

  /// Currency.
  final String currency;

  /// Total Amount.
  final int totalAmount;

  /// Invoice Slug.
  final String? invoiceSlug;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x96163f56);
    buffer.writeInt(flags);
    buffer.writeString(currency);
    buffer.writeLong(totalAmount);
    final localInvoiceSlugCopy = invoiceSlug;
    if (localInvoiceSlugCopy != null) {
      buffer.writeString(localInvoiceSlugCopy);
    }
  }
}

/// Message Action Phone Call.
///
/// ID: `80e11a7f`.
class MessageActionPhoneCall extends MessageActionBase {
  /// Message Action Phone Call constructor.
  const MessageActionPhoneCall({
    required this.video,
    required this.callId,
    this.reason,
    this.duration,
  }) : super._();

  /// Deserialize.
  factory MessageActionPhoneCall.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final video = _readflags.2?true(buffer);
    // final callId = _readlong(buffer);
    // final reason = _readflags.0?PhoneCallDiscardReason(buffer);
    // final duration = _readflags.1?int(buffer);
    // final result = MessageActionPhoneCall(flags: flags, video: video, callId: callId, reason: reason, duration: duration,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: video,
      b00: reason != null,
      b01: duration != null,
    );

    return v;
  }

  /// video: bit 2 of flags.2?true
  final bool video;

  /// Call Id.
  final int callId;

  /// Reason.
  final PhoneCallDiscardReasonBase? reason;

  /// Duration.
  final int? duration;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x80e11a7f);
    buffer.writeInt(flags);
    buffer.writeLong(callId);
    final localReasonCopy = reason;
    if (localReasonCopy != null) {
      buffer.writeObject(localReasonCopy);
    }
    final localDurationCopy = duration;
    if (localDurationCopy != null) {
      buffer.writeInt(localDurationCopy);
    }
  }
}

/// Message Action Screenshot Taken.
///
/// ID: `4792929b`.
class MessageActionScreenshotTaken extends MessageActionBase {
  /// Message Action Screenshot Taken constructor.
  const MessageActionScreenshotTaken() : super._();

  /// Deserialize.
  factory MessageActionScreenshotTaken.deserialize(Uint8List buffer) {
    // final result = MessageActionScreenshotTaken();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4792929b);
  }
}

/// Message Action Custom Action.
///
/// ID: `fae69f56`.
class MessageActionCustomAction extends MessageActionBase {
  /// Message Action Custom Action constructor.
  const MessageActionCustomAction({
    required this.message,
  }) : super._();

  /// Deserialize.
  factory MessageActionCustomAction.deserialize(Uint8List buffer) {
    // final message = _readstring(buffer);
    // final result = MessageActionCustomAction(message: message,);

    // return result;

    throw Exception();
  }

  /// Message.
  final String message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfae69f56);
    buffer.writeString(message);
  }
}

/// Message Action Bot Allowed.
///
/// ID: `c516d679`.
class MessageActionBotAllowed extends MessageActionBase {
  /// Message Action Bot Allowed constructor.
  const MessageActionBotAllowed({
    required this.attachMenu,
    required this.fromRequest,
    this.domain,
    this.app,
  }) : super._();

  /// Deserialize.
  factory MessageActionBotAllowed.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final attachMenu = _readflags.1?true(buffer);
    // final fromRequest = _readflags.3?true(buffer);
    // final domain = _readflags.0?string(buffer);
    // final app = _readflags.2?BotApp(buffer);
    // final result = MessageActionBotAllowed(flags: flags, attachMenu: attachMenu, fromRequest: fromRequest, domain: domain, app: app,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: attachMenu,
      b03: fromRequest,
      b00: domain != null,
      b02: app != null,
    );

    return v;
  }

  /// attach_menu: bit 1 of flags.1?true
  final bool attachMenu;

  /// from_request: bit 3 of flags.3?true
  final bool fromRequest;

  /// Domain.
  final String? domain;

  /// App.
  final BotAppBase? app;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc516d679);
    buffer.writeInt(flags);
    final localDomainCopy = domain;
    if (localDomainCopy != null) {
      buffer.writeString(localDomainCopy);
    }
    final localAppCopy = app;
    if (localAppCopy != null) {
      buffer.writeObject(localAppCopy);
    }
  }
}

/// Message Action Secure Values Sent Me.
///
/// ID: `1b287353`.
class MessageActionSecureValuesSentMe extends MessageActionBase {
  /// Message Action Secure Values Sent Me constructor.
  const MessageActionSecureValuesSentMe({
    required this.values,
    required this.credentials,
  }) : super._();

  /// Deserialize.
  factory MessageActionSecureValuesSentMe.deserialize(Uint8List buffer) {
    // final values = _readVector<SecureValue>(buffer);
    // final credentials = _readSecureCredentialsEncrypted(buffer);
    // final result = MessageActionSecureValuesSentMe(values: values, credentials: credentials,);

    // return result;

    throw Exception();
  }

  /// Values.
  final List<SecureValueBase> values;

  /// Credentials.
  final SecureCredentialsEncryptedBase credentials;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1b287353);
    buffer.writeVectorObject(values);
    buffer.writeObject(credentials);
  }
}

/// Message Action Secure Values Sent.
///
/// ID: `d95c6154`.
class MessageActionSecureValuesSent extends MessageActionBase {
  /// Message Action Secure Values Sent constructor.
  const MessageActionSecureValuesSent({
    required this.types,
  }) : super._();

  /// Deserialize.
  factory MessageActionSecureValuesSent.deserialize(Uint8List buffer) {
    // final types = _readVector<SecureValueType>(buffer);
    // final result = MessageActionSecureValuesSent(types: types,);

    // return result;

    throw Exception();
  }

  /// Types.
  final List<SecureValueTypeBase> types;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd95c6154);
    buffer.writeVectorObject(types);
  }
}

/// Message Action Contact Sign Up.
///
/// ID: `f3f25f76`.
class MessageActionContactSignUp extends MessageActionBase {
  /// Message Action Contact Sign Up constructor.
  const MessageActionContactSignUp() : super._();

  /// Deserialize.
  factory MessageActionContactSignUp.deserialize(Uint8List buffer) {
    // final result = MessageActionContactSignUp();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf3f25f76);
  }
}

/// Message Action Geo Proximity Reached.
///
/// ID: `98e0d697`.
class MessageActionGeoProximityReached extends MessageActionBase {
  /// Message Action Geo Proximity Reached constructor.
  const MessageActionGeoProximityReached({
    required this.fromId,
    required this.toId,
    required this.distance,
  }) : super._();

  /// Deserialize.
  factory MessageActionGeoProximityReached.deserialize(Uint8List buffer) {
    // final fromId = _readPeer(buffer);
    // final toId = _readPeer(buffer);
    // final distance = _readint(buffer);
    // final result = MessageActionGeoProximityReached(fromId: fromId, toId: toId, distance: distance,);

    // return result;

    throw Exception();
  }

  /// From Id.
  final PeerBase fromId;

  /// To Id.
  final PeerBase toId;

  /// Distance.
  final int distance;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x98e0d697);
    buffer.writeObject(fromId);
    buffer.writeObject(toId);
    buffer.writeInt(distance);
  }
}

/// Message Action Group Call.
///
/// ID: `7a0d7f42`.
class MessageActionGroupCall extends MessageActionBase {
  /// Message Action Group Call constructor.
  const MessageActionGroupCall({
    required this.call,
    this.duration,
  }) : super._();

  /// Deserialize.
  factory MessageActionGroupCall.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final call = _readInputGroupCall(buffer);
    // final duration = _readflags.0?int(buffer);
    // final result = MessageActionGroupCall(flags: flags, call: call, duration: duration,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: duration != null,
    );

    return v;
  }

  /// Call.
  final InputGroupCallBase call;

  /// Duration.
  final int? duration;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7a0d7f42);
    buffer.writeInt(flags);
    buffer.writeObject(call);
    final localDurationCopy = duration;
    if (localDurationCopy != null) {
      buffer.writeInt(localDurationCopy);
    }
  }
}

/// Message Action Invite To Group Call.
///
/// ID: `502f92f7`.
class MessageActionInviteToGroupCall extends MessageActionBase {
  /// Message Action Invite To Group Call constructor.
  const MessageActionInviteToGroupCall({
    required this.call,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessageActionInviteToGroupCall.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final users = _readVector<long>(buffer);
    // final result = MessageActionInviteToGroupCall(call: call, users: users,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Users.
  final List<int> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x502f92f7);
    buffer.writeObject(call);
    buffer.writeVectorLong(users);
  }
}

/// Message Action Set Messages T T L.
///
/// ID: `3c134d7b`.
class MessageActionSetMessagesTTL extends MessageActionBase {
  /// Message Action Set Messages T T L constructor.
  const MessageActionSetMessagesTTL({
    required this.period,
    this.autoSettingFrom,
  }) : super._();

  /// Deserialize.
  factory MessageActionSetMessagesTTL.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final period = _readint(buffer);
    // final autoSettingFrom = _readflags.0?long(buffer);
    // final result = MessageActionSetMessagesTTL(flags: flags, period: period, autoSettingFrom: autoSettingFrom,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: autoSettingFrom != null,
    );

    return v;
  }

  /// Period.
  final int period;

  /// Auto Setting From.
  final int? autoSettingFrom;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3c134d7b);
    buffer.writeInt(flags);
    buffer.writeInt(period);
    final localAutoSettingFromCopy = autoSettingFrom;
    if (localAutoSettingFromCopy != null) {
      buffer.writeLong(localAutoSettingFromCopy);
    }
  }
}

/// Message Action Group Call Scheduled.
///
/// ID: `b3a07661`.
class MessageActionGroupCallScheduled extends MessageActionBase {
  /// Message Action Group Call Scheduled constructor.
  const MessageActionGroupCallScheduled({
    required this.call,
    required this.scheduleDate,
  }) : super._();

  /// Deserialize.
  factory MessageActionGroupCallScheduled.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final scheduleDate = _readint(buffer);
    // final result = MessageActionGroupCallScheduled(call: call, scheduleDate: scheduleDate,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Schedule Date.
  final DateTime scheduleDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb3a07661);
    buffer.writeObject(call);
    buffer.writeDateTime(scheduleDate);
  }
}

/// Message Action Set Chat Theme.
///
/// ID: `aa786345`.
class MessageActionSetChatTheme extends MessageActionBase {
  /// Message Action Set Chat Theme constructor.
  const MessageActionSetChatTheme({
    required this.emoticon,
  }) : super._();

  /// Deserialize.
  factory MessageActionSetChatTheme.deserialize(Uint8List buffer) {
    // final emoticon = _readstring(buffer);
    // final result = MessageActionSetChatTheme(emoticon: emoticon,);

    // return result;

    throw Exception();
  }

  /// Emoticon.
  final String emoticon;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xaa786345);
    buffer.writeString(emoticon);
  }
}

/// Message Action Chat Joined By Request.
///
/// ID: `ebbca3cb`.
class MessageActionChatJoinedByRequest extends MessageActionBase {
  /// Message Action Chat Joined By Request constructor.
  const MessageActionChatJoinedByRequest() : super._();

  /// Deserialize.
  factory MessageActionChatJoinedByRequest.deserialize(Uint8List buffer) {
    // final result = MessageActionChatJoinedByRequest();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xebbca3cb);
  }
}

/// Message Action Web View Data Sent Me.
///
/// ID: `47dd8079`.
class MessageActionWebViewDataSentMe extends MessageActionBase {
  /// Message Action Web View Data Sent Me constructor.
  const MessageActionWebViewDataSentMe({
    required this.text,
    required this.data,
  }) : super._();

  /// Deserialize.
  factory MessageActionWebViewDataSentMe.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final data = _readstring(buffer);
    // final result = MessageActionWebViewDataSentMe(text: text, data: data,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Data.
  final String data;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x47dd8079);
    buffer.writeString(text);
    buffer.writeString(data);
  }
}

/// Message Action Web View Data Sent.
///
/// ID: `b4c38cb5`.
class MessageActionWebViewDataSent extends MessageActionBase {
  /// Message Action Web View Data Sent constructor.
  const MessageActionWebViewDataSent({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory MessageActionWebViewDataSent.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final result = MessageActionWebViewDataSent(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb4c38cb5);
    buffer.writeString(text);
  }
}

/// Message Action Gift Premium.
///
/// ID: `c83d6aec`.
class MessageActionGiftPremium extends MessageActionBase {
  /// Message Action Gift Premium constructor.
  const MessageActionGiftPremium({
    required this.currency,
    required this.amount,
    required this.months,
    this.cryptoCurrency,
    this.cryptoAmount,
  }) : super._();

  /// Deserialize.
  factory MessageActionGiftPremium.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final currency = _readstring(buffer);
    // final amount = _readlong(buffer);
    // final months = _readint(buffer);
    // final cryptoCurrency = _readflags.0?string(buffer);
    // final cryptoAmount = _readflags.0?long(buffer);
    // final result = MessageActionGiftPremium(flags: flags, currency: currency, amount: amount, months: months, cryptoCurrency: cryptoCurrency, cryptoAmount: cryptoAmount,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: cryptoCurrency != null || cryptoAmount != null,
    );

    return v;
  }

  /// Currency.
  final String currency;

  /// Amount.
  final int amount;

  /// Months.
  final int months;

  /// Crypto Currency.
  final String? cryptoCurrency;

  /// Crypto Amount.
  final int? cryptoAmount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc83d6aec);
    buffer.writeInt(flags);
    buffer.writeString(currency);
    buffer.writeLong(amount);
    buffer.writeInt(months);
    final localCryptoCurrencyCopy = cryptoCurrency;
    if (localCryptoCurrencyCopy != null) {
      buffer.writeString(localCryptoCurrencyCopy);
    }
    final localCryptoAmountCopy = cryptoAmount;
    if (localCryptoAmountCopy != null) {
      buffer.writeLong(localCryptoAmountCopy);
    }
  }
}

/// Message Action Topic Create.
///
/// ID: `0d999256`.
class MessageActionTopicCreate extends MessageActionBase {
  /// Message Action Topic Create constructor.
  const MessageActionTopicCreate({
    required this.title,
    required this.iconColor,
    this.iconEmojiId,
  }) : super._();

  /// Deserialize.
  factory MessageActionTopicCreate.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final title = _readstring(buffer);
    // final iconColor = _readint(buffer);
    // final iconEmojiId = _readflags.0?long(buffer);
    // final result = MessageActionTopicCreate(flags: flags, title: title, iconColor: iconColor, iconEmojiId: iconEmojiId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: iconEmojiId != null,
    );

    return v;
  }

  /// Title.
  final String title;

  /// Icon Color.
  final int iconColor;

  /// Icon Emoji Id.
  final int? iconEmojiId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0d999256);
    buffer.writeInt(flags);
    buffer.writeString(title);
    buffer.writeInt(iconColor);
    final localIconEmojiIdCopy = iconEmojiId;
    if (localIconEmojiIdCopy != null) {
      buffer.writeLong(localIconEmojiIdCopy);
    }
  }
}

/// Message Action Topic Edit.
///
/// ID: `c0944820`.
class MessageActionTopicEdit extends MessageActionBase {
  /// Message Action Topic Edit constructor.
  const MessageActionTopicEdit({
    this.title,
    this.iconEmojiId,
    this.closed,
    this.hidden,
  }) : super._();

  /// Deserialize.
  factory MessageActionTopicEdit.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final title = _readflags.0?string(buffer);
    // final iconEmojiId = _readflags.1?long(buffer);
    // final closed = _readflags.2?Bool(buffer);
    // final hidden = _readflags.3?Bool(buffer);
    // final result = MessageActionTopicEdit(flags: flags, title: title, iconEmojiId: iconEmojiId, closed: closed, hidden: hidden,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: title != null,
      b01: iconEmojiId != null,
      b02: closed != null,
      b03: hidden != null,
    );

    return v;
  }

  /// Title.
  final String? title;

  /// Icon Emoji Id.
  final int? iconEmojiId;

  /// Closed.
  final bool? closed;

  /// Hidden.
  final bool? hidden;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc0944820);
    buffer.writeInt(flags);
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localIconEmojiIdCopy = iconEmojiId;
    if (localIconEmojiIdCopy != null) {
      buffer.writeLong(localIconEmojiIdCopy);
    }
    final localClosedCopy = closed;
    if (localClosedCopy != null) {
      buffer.writeBool(localClosedCopy);
    }
    final localHiddenCopy = hidden;
    if (localHiddenCopy != null) {
      buffer.writeBool(localHiddenCopy);
    }
  }
}

/// Message Action Suggest Profile Photo.
///
/// ID: `57de635e`.
class MessageActionSuggestProfilePhoto extends MessageActionBase {
  /// Message Action Suggest Profile Photo constructor.
  const MessageActionSuggestProfilePhoto({
    required this.photo,
  }) : super._();

  /// Deserialize.
  factory MessageActionSuggestProfilePhoto.deserialize(Uint8List buffer) {
    // final photo = _readPhoto(buffer);
    // final result = MessageActionSuggestProfilePhoto(photo: photo,);

    // return result;

    throw Exception();
  }

  /// Photo.
  final PhotoBase photo;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x57de635e);
    buffer.writeObject(photo);
  }
}

/// Message Action Requested Peer.
///
/// ID: `31518e9b`.
class MessageActionRequestedPeer extends MessageActionBase {
  /// Message Action Requested Peer constructor.
  const MessageActionRequestedPeer({
    required this.buttonId,
    required this.peers,
  }) : super._();

  /// Deserialize.
  factory MessageActionRequestedPeer.deserialize(Uint8List buffer) {
    // final buttonId = _readint(buffer);
    // final peers = _readVector<Peer>(buffer);
    // final result = MessageActionRequestedPeer(buttonId: buttonId, peers: peers,);

    // return result;

    throw Exception();
  }

  /// Button Id.
  final int buttonId;

  /// Peers.
  final List<PeerBase> peers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x31518e9b);
    buffer.writeInt(buttonId);
    buffer.writeVectorObject(peers);
  }
}

/// Message Action Set Chat Wall Paper.
///
/// ID: `5060a3f4`.
class MessageActionSetChatWallPaper extends MessageActionBase {
  /// Message Action Set Chat Wall Paper constructor.
  const MessageActionSetChatWallPaper({
    required this.same,
    required this.forBoth,
    required this.wallpaper,
  }) : super._();

  /// Deserialize.
  factory MessageActionSetChatWallPaper.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final same = _readflags.0?true(buffer);
    // final forBoth = _readflags.1?true(buffer);
    // final wallpaper = _readWallPaper(buffer);
    // final result = MessageActionSetChatWallPaper(flags: flags, same: same, forBoth: forBoth, wallpaper: wallpaper,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: same,
      b01: forBoth,
    );

    return v;
  }

  /// same: bit 0 of flags.0?true
  final bool same;

  /// for_both: bit 1 of flags.1?true
  final bool forBoth;

  /// Wallpaper.
  final WallPaperBase wallpaper;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5060a3f4);
    buffer.writeInt(flags);
    buffer.writeObject(wallpaper);
  }
}

/// Message Action Gift Code.
///
/// ID: `678c2e09`.
class MessageActionGiftCode extends MessageActionBase {
  /// Message Action Gift Code constructor.
  const MessageActionGiftCode({
    required this.viaGiveaway,
    required this.unclaimed,
    this.boostPeer,
    required this.months,
    required this.slug,
    this.currency,
    this.amount,
    this.cryptoCurrency,
    this.cryptoAmount,
  }) : super._();

  /// Deserialize.
  factory MessageActionGiftCode.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final viaGiveaway = _readflags.0?true(buffer);
    // final unclaimed = _readflags.2?true(buffer);
    // final boostPeer = _readflags.1?Peer(buffer);
    // final months = _readint(buffer);
    // final slug = _readstring(buffer);
    // final currency = _readflags.2?string(buffer);
    // final amount = _readflags.2?long(buffer);
    // final cryptoCurrency = _readflags.3?string(buffer);
    // final cryptoAmount = _readflags.3?long(buffer);
    // final result = MessageActionGiftCode(flags: flags, viaGiveaway: viaGiveaway, unclaimed: unclaimed, boostPeer: boostPeer, months: months, slug: slug, currency: currency, amount: amount, cryptoCurrency: cryptoCurrency, cryptoAmount: cryptoAmount,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: viaGiveaway,
      b02: unclaimed || currency != null || amount != null,
      b01: boostPeer != null,
      b03: cryptoCurrency != null || cryptoAmount != null,
    );

    return v;
  }

  /// via_giveaway: bit 0 of flags.0?true
  final bool viaGiveaway;

  /// unclaimed: bit 2 of flags.2?true
  final bool unclaimed;

  /// Boost Peer.
  final PeerBase? boostPeer;

  /// Months.
  final int months;

  /// Slug.
  final String slug;

  /// Currency.
  final String? currency;

  /// Amount.
  final int? amount;

  /// Crypto Currency.
  final String? cryptoCurrency;

  /// Crypto Amount.
  final int? cryptoAmount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x678c2e09);
    buffer.writeInt(flags);
    final localBoostPeerCopy = boostPeer;
    if (localBoostPeerCopy != null) {
      buffer.writeObject(localBoostPeerCopy);
    }
    buffer.writeInt(months);
    buffer.writeString(slug);
    final localCurrencyCopy = currency;
    if (localCurrencyCopy != null) {
      buffer.writeString(localCurrencyCopy);
    }
    final localAmountCopy = amount;
    if (localAmountCopy != null) {
      buffer.writeLong(localAmountCopy);
    }
    final localCryptoCurrencyCopy = cryptoCurrency;
    if (localCryptoCurrencyCopy != null) {
      buffer.writeString(localCryptoCurrencyCopy);
    }
    final localCryptoAmountCopy = cryptoAmount;
    if (localCryptoAmountCopy != null) {
      buffer.writeLong(localCryptoAmountCopy);
    }
  }
}

/// Message Action Giveaway Launch.
///
/// ID: `332ba9ed`.
class MessageActionGiveawayLaunch extends MessageActionBase {
  /// Message Action Giveaway Launch constructor.
  const MessageActionGiveawayLaunch() : super._();

  /// Deserialize.
  factory MessageActionGiveawayLaunch.deserialize(Uint8List buffer) {
    // final result = MessageActionGiveawayLaunch();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x332ba9ed);
  }
}

/// Message Action Giveaway Results.
///
/// ID: `2a9fadc5`.
class MessageActionGiveawayResults extends MessageActionBase {
  /// Message Action Giveaway Results constructor.
  const MessageActionGiveawayResults({
    required this.winnersCount,
    required this.unclaimedCount,
  }) : super._();

  /// Deserialize.
  factory MessageActionGiveawayResults.deserialize(Uint8List buffer) {
    // final winnersCount = _readint(buffer);
    // final unclaimedCount = _readint(buffer);
    // final result = MessageActionGiveawayResults(winnersCount: winnersCount, unclaimedCount: unclaimedCount,);

    // return result;

    throw Exception();
  }

  /// Winners Count.
  final int winnersCount;

  /// Unclaimed Count.
  final int unclaimedCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2a9fadc5);
    buffer.writeInt(winnersCount);
    buffer.writeInt(unclaimedCount);
  }
}

/// Predicate Dialog.
abstract class DialogBase extends TlConstructor {
  /// Predicate Dialog constructor.
  const DialogBase._() : super._();
}

/// Dialog.
///
/// ID: `d58a08c6`.
class Dialog extends DialogBase {
  /// Dialog constructor.
  const Dialog({
    required this.pinned,
    required this.unreadMark,
    required this.viewForumAsMessages,
    required this.peer,
    required this.topMessage,
    required this.readInboxMaxId,
    required this.readOutboxMaxId,
    required this.unreadCount,
    required this.unreadMentionsCount,
    required this.unreadReactionsCount,
    required this.notifySettings,
    this.pts,
    this.draft,
    this.folderId,
    this.ttlPeriod,
  }) : super._();

  /// Deserialize.
  factory Dialog.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pinned = _readflags.2?true(buffer);
    // final unreadMark = _readflags.3?true(buffer);
    // final viewForumAsMessages = _readflags.6?true(buffer);
    // final peer = _readPeer(buffer);
    // final topMessage = _readint(buffer);
    // final readInboxMaxId = _readint(buffer);
    // final readOutboxMaxId = _readint(buffer);
    // final unreadCount = _readint(buffer);
    // final unreadMentionsCount = _readint(buffer);
    // final unreadReactionsCount = _readint(buffer);
    // final notifySettings = _readPeerNotifySettings(buffer);
    // final pts = _readflags.0?int(buffer);
    // final draft = _readflags.1?DraftMessage(buffer);
    // final folderId = _readflags.4?int(buffer);
    // final ttlPeriod = _readflags.5?int(buffer);
    // final result = Dialog(flags: flags, pinned: pinned, unreadMark: unreadMark, viewForumAsMessages: viewForumAsMessages, peer: peer, topMessage: topMessage, readInboxMaxId: readInboxMaxId, readOutboxMaxId: readOutboxMaxId, unreadCount: unreadCount, unreadMentionsCount: unreadMentionsCount, unreadReactionsCount: unreadReactionsCount, notifySettings: notifySettings, pts: pts, draft: draft, folderId: folderId, ttlPeriod: ttlPeriod,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: pinned,
      b03: unreadMark,
      b06: viewForumAsMessages,
      b00: pts != null,
      b01: draft != null,
      b04: folderId != null,
      b05: ttlPeriod != null,
    );

    return v;
  }

  /// pinned: bit 2 of flags.2?true
  final bool pinned;

  /// unread_mark: bit 3 of flags.3?true
  final bool unreadMark;

  /// view_forum_as_messages: bit 6 of flags.6?true
  final bool viewForumAsMessages;

  /// Peer.
  final PeerBase peer;

  /// Top Message.
  final int topMessage;

  /// Read Inbox Max Id.
  final int readInboxMaxId;

  /// Read Outbox Max Id.
  final int readOutboxMaxId;

  /// Unread Count.
  final int unreadCount;

  /// Unread Mentions Count.
  final int unreadMentionsCount;

  /// Unread Reactions Count.
  final int unreadReactionsCount;

  /// Notify Settings.
  final PeerNotifySettingsBase notifySettings;

  /// Pts.
  final int? pts;

  /// Draft.
  final DraftMessageBase? draft;

  /// Folder Id.
  final int? folderId;

  /// Ttl Period.
  final int? ttlPeriod;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd58a08c6);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(topMessage);
    buffer.writeInt(readInboxMaxId);
    buffer.writeInt(readOutboxMaxId);
    buffer.writeInt(unreadCount);
    buffer.writeInt(unreadMentionsCount);
    buffer.writeInt(unreadReactionsCount);
    buffer.writeObject(notifySettings);
    final localPtsCopy = pts;
    if (localPtsCopy != null) {
      buffer.writeInt(localPtsCopy);
    }
    final localDraftCopy = draft;
    if (localDraftCopy != null) {
      buffer.writeObject(localDraftCopy);
    }
    final localFolderIdCopy = folderId;
    if (localFolderIdCopy != null) {
      buffer.writeInt(localFolderIdCopy);
    }
    final localTtlPeriodCopy = ttlPeriod;
    if (localTtlPeriodCopy != null) {
      buffer.writeInt(localTtlPeriodCopy);
    }
  }
}

/// Dialog Folder.
///
/// ID: `71bd134c`.
class DialogFolder extends DialogBase {
  /// Dialog Folder constructor.
  const DialogFolder({
    required this.pinned,
    required this.folder,
    required this.peer,
    required this.topMessage,
    required this.unreadMutedPeersCount,
    required this.unreadUnmutedPeersCount,
    required this.unreadMutedMessagesCount,
    required this.unreadUnmutedMessagesCount,
  }) : super._();

  /// Deserialize.
  factory DialogFolder.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pinned = _readflags.2?true(buffer);
    // final folder = _readFolder(buffer);
    // final peer = _readPeer(buffer);
    // final topMessage = _readint(buffer);
    // final unreadMutedPeersCount = _readint(buffer);
    // final unreadUnmutedPeersCount = _readint(buffer);
    // final unreadMutedMessagesCount = _readint(buffer);
    // final unreadUnmutedMessagesCount = _readint(buffer);
    // final result = DialogFolder(flags: flags, pinned: pinned, folder: folder, peer: peer, topMessage: topMessage, unreadMutedPeersCount: unreadMutedPeersCount, unreadUnmutedPeersCount: unreadUnmutedPeersCount, unreadMutedMessagesCount: unreadMutedMessagesCount, unreadUnmutedMessagesCount: unreadUnmutedMessagesCount,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: pinned,
    );

    return v;
  }

  /// pinned: bit 2 of flags.2?true
  final bool pinned;

  /// Folder.
  final FolderBase folder;

  /// Peer.
  final PeerBase peer;

  /// Top Message.
  final int topMessage;

  /// Unread Muted Peers Count.
  final int unreadMutedPeersCount;

  /// Unread Unmuted Peers Count.
  final int unreadUnmutedPeersCount;

  /// Unread Muted Messages Count.
  final int unreadMutedMessagesCount;

  /// Unread Unmuted Messages Count.
  final int unreadUnmutedMessagesCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x71bd134c);
    buffer.writeInt(flags);
    buffer.writeObject(folder);
    buffer.writeObject(peer);
    buffer.writeInt(topMessage);
    buffer.writeInt(unreadMutedPeersCount);
    buffer.writeInt(unreadUnmutedPeersCount);
    buffer.writeInt(unreadMutedMessagesCount);
    buffer.writeInt(unreadUnmutedMessagesCount);
  }
}

/// Predicate Photo.
abstract class PhotoBase extends TlConstructor {
  /// Predicate Photo constructor.
  const PhotoBase._() : super._();
}

/// Photo Empty.
///
/// ID: `2331b22d`.
class PhotoEmpty extends PhotoBase {
  /// Photo Empty constructor.
  const PhotoEmpty({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory PhotoEmpty.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final result = PhotoEmpty(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2331b22d);
    buffer.writeLong(id);
  }
}

/// Photo.
///
/// ID: `fb197a65`.
class Photo extends PhotoBase {
  /// Photo constructor.
  const Photo({
    required this.hasStickers,
    required this.id,
    required this.accessHash,
    required this.fileReference,
    required this.date,
    required this.sizes,
    this.videoSizes,
    required this.dcId,
  }) : super._();

  /// Deserialize.
  factory Photo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final hasStickers = _readflags.0?true(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final fileReference = _readbytes(buffer);
    // final date = _readint(buffer);
    // final sizes = _readVector<PhotoSize>(buffer);
    // final videoSizes = _readflags.1?Vector<VideoSize>(buffer);
    // final dcId = _readint(buffer);
    // final result = Photo(flags: flags, hasStickers: hasStickers, id: id, accessHash: accessHash, fileReference: fileReference, date: date, sizes: sizes, videoSizes: videoSizes, dcId: dcId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: hasStickers,
      b01: videoSizes != null,
    );

    return v;
  }

  /// has_stickers: bit 0 of flags.0?true
  final bool hasStickers;

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// File Reference.
  final Uint8List fileReference;

  /// Date.
  final DateTime date;

  /// Sizes.
  final List<PhotoSizeBase> sizes;

  /// Video Sizes.
  final List<VideoSizeBase>? videoSizes;

  /// Dc Id.
  final int dcId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfb197a65);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeBytes(fileReference);
    buffer.writeDateTime(date);
    buffer.writeVectorObject(sizes);
    final localVideoSizesCopy = videoSizes;
    if (localVideoSizesCopy != null) {
      buffer.writeVectorObject(localVideoSizesCopy);
    }
    buffer.writeInt(dcId);
  }
}

/// Predicate Photo Size.
abstract class PhotoSizeBase extends TlConstructor {
  /// Predicate Photo Size constructor.
  const PhotoSizeBase._() : super._();
}

/// Photo Size Empty.
///
/// ID: `0e17e23c`.
class PhotoSizeEmpty extends PhotoSizeBase {
  /// Photo Size Empty constructor.
  const PhotoSizeEmpty({
    required this.type,
  }) : super._();

  /// Deserialize.
  factory PhotoSizeEmpty.deserialize(Uint8List buffer) {
    // final type = _readstring(buffer);
    // final result = PhotoSizeEmpty(type: type,);

    // return result;

    throw Exception();
  }

  /// Type.
  final String type;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0e17e23c);
    buffer.writeString(type);
  }
}

/// Photo Size.
///
/// ID: `75c78e60`.
class PhotoSize extends PhotoSizeBase {
  /// Photo Size constructor.
  const PhotoSize({
    required this.type,
    required this.w,
    required this.h,
    required this.size,
  }) : super._();

  /// Deserialize.
  factory PhotoSize.deserialize(Uint8List buffer) {
    // final type = _readstring(buffer);
    // final w = _readint(buffer);
    // final h = _readint(buffer);
    // final size = _readint(buffer);
    // final result = PhotoSize(type: type, w: w, h: h, size: size,);

    // return result;

    throw Exception();
  }

  /// Type.
  final String type;

  /// W.
  final int w;

  /// H.
  final int h;

  /// Size.
  final int size;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x75c78e60);
    buffer.writeString(type);
    buffer.writeInt(w);
    buffer.writeInt(h);
    buffer.writeInt(size);
  }
}

/// Photo Cached Size.
///
/// ID: `021e1ad6`.
class PhotoCachedSize extends PhotoSizeBase {
  /// Photo Cached Size constructor.
  const PhotoCachedSize({
    required this.type,
    required this.w,
    required this.h,
    required this.bytes,
  }) : super._();

  /// Deserialize.
  factory PhotoCachedSize.deserialize(Uint8List buffer) {
    // final type = _readstring(buffer);
    // final w = _readint(buffer);
    // final h = _readint(buffer);
    // final bytes = _readbytes(buffer);
    // final result = PhotoCachedSize(type: type, w: w, h: h, bytes: bytes,);

    // return result;

    throw Exception();
  }

  /// Type.
  final String type;

  /// W.
  final int w;

  /// H.
  final int h;

  /// Bytes.
  final Uint8List bytes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x021e1ad6);
    buffer.writeString(type);
    buffer.writeInt(w);
    buffer.writeInt(h);
    buffer.writeBytes(bytes);
  }
}

/// Photo Stripped Size.
///
/// ID: `e0b0bc2e`.
class PhotoStrippedSize extends PhotoSizeBase {
  /// Photo Stripped Size constructor.
  const PhotoStrippedSize({
    required this.type,
    required this.bytes,
  }) : super._();

  /// Deserialize.
  factory PhotoStrippedSize.deserialize(Uint8List buffer) {
    // final type = _readstring(buffer);
    // final bytes = _readbytes(buffer);
    // final result = PhotoStrippedSize(type: type, bytes: bytes,);

    // return result;

    throw Exception();
  }

  /// Type.
  final String type;

  /// Bytes.
  final Uint8List bytes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe0b0bc2e);
    buffer.writeString(type);
    buffer.writeBytes(bytes);
  }
}

/// Photo Size Progressive.
///
/// ID: `fa3efb95`.
class PhotoSizeProgressive extends PhotoSizeBase {
  /// Photo Size Progressive constructor.
  const PhotoSizeProgressive({
    required this.type,
    required this.w,
    required this.h,
    required this.sizes,
  }) : super._();

  /// Deserialize.
  factory PhotoSizeProgressive.deserialize(Uint8List buffer) {
    // final type = _readstring(buffer);
    // final w = _readint(buffer);
    // final h = _readint(buffer);
    // final sizes = _readVector<int>(buffer);
    // final result = PhotoSizeProgressive(type: type, w: w, h: h, sizes: sizes,);

    // return result;

    throw Exception();
  }

  /// Type.
  final String type;

  /// W.
  final int w;

  /// H.
  final int h;

  /// Sizes.
  final List<int> sizes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfa3efb95);
    buffer.writeString(type);
    buffer.writeInt(w);
    buffer.writeInt(h);
    buffer.writeVectorInt(sizes);
  }
}

/// Photo Path Size.
///
/// ID: `d8214d41`.
class PhotoPathSize extends PhotoSizeBase {
  /// Photo Path Size constructor.
  const PhotoPathSize({
    required this.type,
    required this.bytes,
  }) : super._();

  /// Deserialize.
  factory PhotoPathSize.deserialize(Uint8List buffer) {
    // final type = _readstring(buffer);
    // final bytes = _readbytes(buffer);
    // final result = PhotoPathSize(type: type, bytes: bytes,);

    // return result;

    throw Exception();
  }

  /// Type.
  final String type;

  /// Bytes.
  final Uint8List bytes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd8214d41);
    buffer.writeString(type);
    buffer.writeBytes(bytes);
  }
}

/// Predicate Geo Point.
abstract class GeoPointBase extends TlConstructor {
  /// Predicate Geo Point constructor.
  const GeoPointBase._() : super._();
}

/// Geo Point Empty.
///
/// ID: `1117dd5f`.
class GeoPointEmpty extends GeoPointBase {
  /// Geo Point Empty constructor.
  const GeoPointEmpty() : super._();

  /// Deserialize.
  factory GeoPointEmpty.deserialize(Uint8List buffer) {
    // final result = GeoPointEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1117dd5f);
  }
}

/// Geo Point.
///
/// ID: `b2a2f663`.
class GeoPoint extends GeoPointBase {
  /// Geo Point constructor.
  const GeoPoint({
    required this.long,
    required this.lat,
    required this.accessHash,
    this.accuracyRadius,
  }) : super._();

  /// Deserialize.
  factory GeoPoint.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final long = _readdouble(buffer);
    // final lat = _readdouble(buffer);
    // final accessHash = _readlong(buffer);
    // final accuracyRadius = _readflags.0?int(buffer);
    // final result = GeoPoint(flags: flags, long: long, lat: lat, accessHash: accessHash, accuracyRadius: accuracyRadius,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: accuracyRadius != null,
    );

    return v;
  }

  /// Long.
  final double long;

  /// Lat.
  final double lat;

  /// Access Hash.
  final int accessHash;

  /// Accuracy Radius.
  final int? accuracyRadius;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb2a2f663);
    buffer.writeInt(flags);
    buffer.writeDouble(long);
    buffer.writeDouble(lat);
    buffer.writeLong(accessHash);
    final localAccuracyRadiusCopy = accuracyRadius;
    if (localAccuracyRadiusCopy != null) {
      buffer.writeInt(localAccuracyRadiusCopy);
    }
  }
}

/// Predicate Auth Sent Code.
abstract class AuthSentCodeBase extends TlConstructor {
  /// Predicate Auth Sent Code constructor.
  const AuthSentCodeBase._() : super._();
}

/// Auth Sent Code.
///
/// ID: `5e002502`.
class AuthSentCode extends AuthSentCodeBase {
  /// Auth Sent Code constructor.
  const AuthSentCode({
    required this.type,
    required this.phoneCodeHash,
    this.nextType,
    this.timeout,
  }) : super._();

  /// Deserialize.
  factory AuthSentCode.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final type = _readauth.SentCodeType(buffer);
    // final phoneCodeHash = _readstring(buffer);
    // final nextType = _readflags.1?auth.CodeType(buffer);
    // final timeout = _readflags.2?int(buffer);
    // final result = AuthSentCode(flags: flags, type: type, phoneCodeHash: phoneCodeHash, nextType: nextType, timeout: timeout,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: nextType != null,
      b02: timeout != null,
    );

    return v;
  }

  /// Type.
  final AuthSentCodeTypeBase type;

  /// Phone Code Hash.
  final String phoneCodeHash;

  /// Next Type.
  final AuthCodeTypeBase? nextType;

  /// Timeout.
  final int? timeout;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5e002502);
    buffer.writeInt(flags);
    buffer.writeObject(type);
    buffer.writeString(phoneCodeHash);
    final localNextTypeCopy = nextType;
    if (localNextTypeCopy != null) {
      buffer.writeObject(localNextTypeCopy);
    }
    final localTimeoutCopy = timeout;
    if (localTimeoutCopy != null) {
      buffer.writeInt(localTimeoutCopy);
    }
  }
}

/// Auth Sent Code Success.
///
/// ID: `2390fe44`.
class AuthSentCodeSuccess extends AuthSentCodeBase {
  /// Auth Sent Code Success constructor.
  const AuthSentCodeSuccess({
    required this.authorization,
  }) : super._();

  /// Deserialize.
  factory AuthSentCodeSuccess.deserialize(Uint8List buffer) {
    // final authorization = _readauth.Authorization(buffer);
    // final result = AuthSentCodeSuccess(authorization: authorization,);

    // return result;

    throw Exception();
  }

  /// Authorization.
  final AuthAuthorizationBase authorization;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2390fe44);
    buffer.writeObject(authorization);
  }
}

/// Predicate Auth Authorization.
abstract class AuthAuthorizationBase extends TlConstructor {
  /// Predicate Auth Authorization constructor.
  const AuthAuthorizationBase._() : super._();
}

/// Auth Authorization.
///
/// ID: `2ea2c0d4`.
class AuthAuthorization extends AuthAuthorizationBase {
  /// Auth Authorization constructor.
  const AuthAuthorization({
    required this.setupPasswordRequired,
    this.otherwiseReloginDays,
    this.tmpSessions,
    this.futureAuthToken,
    required this.user,
  }) : super._();

  /// Deserialize.
  factory AuthAuthorization.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final setupPasswordRequired = _readflags.1?true(buffer);
    // final otherwiseReloginDays = _readflags.1?int(buffer);
    // final tmpSessions = _readflags.0?int(buffer);
    // final futureAuthToken = _readflags.2?bytes(buffer);
    // final user = _readUser(buffer);
    // final result = AuthAuthorization(flags: flags, setupPasswordRequired: setupPasswordRequired, otherwiseReloginDays: otherwiseReloginDays, tmpSessions: tmpSessions, futureAuthToken: futureAuthToken, user: user,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: setupPasswordRequired || otherwiseReloginDays != null,
      b00: tmpSessions != null,
      b02: futureAuthToken != null,
    );

    return v;
  }

  /// setup_password_required: bit 1 of flags.1?true
  final bool setupPasswordRequired;

  /// Otherwise Relogin Days.
  final int? otherwiseReloginDays;

  /// Tmp Sessions.
  final int? tmpSessions;

  /// Future Auth Token.
  final Uint8List? futureAuthToken;

  /// User.
  final UserBase user;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2ea2c0d4);
    buffer.writeInt(flags);
    final localOtherwiseReloginDaysCopy = otherwiseReloginDays;
    if (localOtherwiseReloginDaysCopy != null) {
      buffer.writeInt(localOtherwiseReloginDaysCopy);
    }
    final localTmpSessionsCopy = tmpSessions;
    if (localTmpSessionsCopy != null) {
      buffer.writeInt(localTmpSessionsCopy);
    }
    final localFutureAuthTokenCopy = futureAuthToken;
    if (localFutureAuthTokenCopy != null) {
      buffer.writeBytes(localFutureAuthTokenCopy);
    }
    buffer.writeObject(user);
  }
}

/// Auth Authorization Sign Up Required.
///
/// ID: `44747e9a`.
class AuthAuthorizationSignUpRequired extends AuthAuthorizationBase {
  /// Auth Authorization Sign Up Required constructor.
  const AuthAuthorizationSignUpRequired({
    this.termsOfService,
  }) : super._();

  /// Deserialize.
  factory AuthAuthorizationSignUpRequired.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final termsOfService = _readflags.0?help.TermsOfService(buffer);
    // final result = AuthAuthorizationSignUpRequired(flags: flags, termsOfService: termsOfService,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: termsOfService != null,
    );

    return v;
  }

  /// Terms Of Service.
  final HelpTermsOfServiceBase? termsOfService;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x44747e9a);
    buffer.writeInt(flags);
    final localTermsOfServiceCopy = termsOfService;
    if (localTermsOfServiceCopy != null) {
      buffer.writeObject(localTermsOfServiceCopy);
    }
  }
}

/// Predicate Auth Exported Authorization.
abstract class AuthExportedAuthorizationBase extends TlConstructor {
  /// Predicate Auth Exported Authorization constructor.
  const AuthExportedAuthorizationBase._() : super._();
}

/// Auth Exported Authorization.
///
/// ID: `b434e2b8`.
class AuthExportedAuthorization extends AuthExportedAuthorizationBase {
  /// Auth Exported Authorization constructor.
  const AuthExportedAuthorization({
    required this.id,
    required this.bytes,
  }) : super._();

  /// Deserialize.
  factory AuthExportedAuthorization.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final bytes = _readbytes(buffer);
    // final result = AuthExportedAuthorization(id: id, bytes: bytes,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Bytes.
  final Uint8List bytes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb434e2b8);
    buffer.writeLong(id);
    buffer.writeBytes(bytes);
  }
}

/// Predicate Input Notify Peer.
abstract class InputNotifyPeerBase extends TlConstructor {
  /// Predicate Input Notify Peer constructor.
  const InputNotifyPeerBase._() : super._();
}

/// Input Notify Peer.
///
/// ID: `b8bc5b0c`.
class InputNotifyPeer extends InputNotifyPeerBase {
  /// Input Notify Peer constructor.
  const InputNotifyPeer({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory InputNotifyPeer.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = InputNotifyPeer(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb8bc5b0c);
    buffer.writeObject(peer);
  }
}

/// Input Notify Users.
///
/// ID: `193b4417`.
class InputNotifyUsers extends InputNotifyPeerBase {
  /// Input Notify Users constructor.
  const InputNotifyUsers() : super._();

  /// Deserialize.
  factory InputNotifyUsers.deserialize(Uint8List buffer) {
    // final result = InputNotifyUsers();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x193b4417);
  }
}

/// Input Notify Chats.
///
/// ID: `4a95e84e`.
class InputNotifyChats extends InputNotifyPeerBase {
  /// Input Notify Chats constructor.
  const InputNotifyChats() : super._();

  /// Deserialize.
  factory InputNotifyChats.deserialize(Uint8List buffer) {
    // final result = InputNotifyChats();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4a95e84e);
  }
}

/// Input Notify Broadcasts.
///
/// ID: `b1db7c7e`.
class InputNotifyBroadcasts extends InputNotifyPeerBase {
  /// Input Notify Broadcasts constructor.
  const InputNotifyBroadcasts() : super._();

  /// Deserialize.
  factory InputNotifyBroadcasts.deserialize(Uint8List buffer) {
    // final result = InputNotifyBroadcasts();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb1db7c7e);
  }
}

/// Input Notify Forum Topic.
///
/// ID: `5c467992`.
class InputNotifyForumTopic extends InputNotifyPeerBase {
  /// Input Notify Forum Topic constructor.
  const InputNotifyForumTopic({
    required this.peer,
    required this.topMsgId,
  }) : super._();

  /// Deserialize.
  factory InputNotifyForumTopic.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final topMsgId = _readint(buffer);
    // final result = InputNotifyForumTopic(peer: peer, topMsgId: topMsgId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Top Msg Id.
  final int topMsgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5c467992);
    buffer.writeObject(peer);
    buffer.writeInt(topMsgId);
  }
}

/// Predicate Input Peer Notify Settings.
abstract class InputPeerNotifySettingsBase extends TlConstructor {
  /// Predicate Input Peer Notify Settings constructor.
  const InputPeerNotifySettingsBase._() : super._();
}

/// Input Peer Notify Settings.
///
/// ID: `cacb6ae2`.
class InputPeerNotifySettings extends InputPeerNotifySettingsBase {
  /// Input Peer Notify Settings constructor.
  const InputPeerNotifySettings({
    this.showPreviews,
    this.silent,
    this.muteUntil,
    this.sound,
    this.storiesMuted,
    this.storiesHideSender,
    this.storiesSound,
  }) : super._();

  /// Deserialize.
  factory InputPeerNotifySettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final showPreviews = _readflags.0?Bool(buffer);
    // final silent = _readflags.1?Bool(buffer);
    // final muteUntil = _readflags.2?int(buffer);
    // final sound = _readflags.3?NotificationSound(buffer);
    // final storiesMuted = _readflags.6?Bool(buffer);
    // final storiesHideSender = _readflags.7?Bool(buffer);
    // final storiesSound = _readflags.8?NotificationSound(buffer);
    // final result = InputPeerNotifySettings(flags: flags, showPreviews: showPreviews, silent: silent, muteUntil: muteUntil, sound: sound, storiesMuted: storiesMuted, storiesHideSender: storiesHideSender, storiesSound: storiesSound,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: showPreviews != null,
      b01: silent != null,
      b02: muteUntil != null,
      b03: sound != null,
      b06: storiesMuted != null,
      b07: storiesHideSender != null,
      b08: storiesSound != null,
    );

    return v;
  }

  /// Show Previews.
  final bool? showPreviews;

  /// Silent.
  final bool? silent;

  /// Mute Until.
  final int? muteUntil;

  /// Sound.
  final NotificationSoundBase? sound;

  /// Stories Muted.
  final bool? storiesMuted;

  /// Stories Hide Sender.
  final bool? storiesHideSender;

  /// Stories Sound.
  final NotificationSoundBase? storiesSound;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcacb6ae2);
    buffer.writeInt(flags);
    final localShowPreviewsCopy = showPreviews;
    if (localShowPreviewsCopy != null) {
      buffer.writeBool(localShowPreviewsCopy);
    }
    final localSilentCopy = silent;
    if (localSilentCopy != null) {
      buffer.writeBool(localSilentCopy);
    }
    final localMuteUntilCopy = muteUntil;
    if (localMuteUntilCopy != null) {
      buffer.writeInt(localMuteUntilCopy);
    }
    final localSoundCopy = sound;
    if (localSoundCopy != null) {
      buffer.writeObject(localSoundCopy);
    }
    final localStoriesMutedCopy = storiesMuted;
    if (localStoriesMutedCopy != null) {
      buffer.writeBool(localStoriesMutedCopy);
    }
    final localStoriesHideSenderCopy = storiesHideSender;
    if (localStoriesHideSenderCopy != null) {
      buffer.writeBool(localStoriesHideSenderCopy);
    }
    final localStoriesSoundCopy = storiesSound;
    if (localStoriesSoundCopy != null) {
      buffer.writeObject(localStoriesSoundCopy);
    }
  }
}

/// Predicate Peer Notify Settings.
abstract class PeerNotifySettingsBase extends TlConstructor {
  /// Predicate Peer Notify Settings constructor.
  const PeerNotifySettingsBase._() : super._();
}

/// Peer Notify Settings.
///
/// ID: `99622c0c`.
class PeerNotifySettings extends PeerNotifySettingsBase {
  /// Peer Notify Settings constructor.
  const PeerNotifySettings({
    this.showPreviews,
    this.silent,
    this.muteUntil,
    this.iosSound,
    this.androidSound,
    this.otherSound,
    this.storiesMuted,
    this.storiesHideSender,
    this.storiesIosSound,
    this.storiesAndroidSound,
    this.storiesOtherSound,
  }) : super._();

  /// Deserialize.
  factory PeerNotifySettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final showPreviews = _readflags.0?Bool(buffer);
    // final silent = _readflags.1?Bool(buffer);
    // final muteUntil = _readflags.2?int(buffer);
    // final iosSound = _readflags.3?NotificationSound(buffer);
    // final androidSound = _readflags.4?NotificationSound(buffer);
    // final otherSound = _readflags.5?NotificationSound(buffer);
    // final storiesMuted = _readflags.6?Bool(buffer);
    // final storiesHideSender = _readflags.7?Bool(buffer);
    // final storiesIosSound = _readflags.8?NotificationSound(buffer);
    // final storiesAndroidSound = _readflags.9?NotificationSound(buffer);
    // final storiesOtherSound = _readflags.10?NotificationSound(buffer);
    // final result = PeerNotifySettings(flags: flags, showPreviews: showPreviews, silent: silent, muteUntil: muteUntil, iosSound: iosSound, androidSound: androidSound, otherSound: otherSound, storiesMuted: storiesMuted, storiesHideSender: storiesHideSender, storiesIosSound: storiesIosSound, storiesAndroidSound: storiesAndroidSound, storiesOtherSound: storiesOtherSound,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: showPreviews != null,
      b01: silent != null,
      b02: muteUntil != null,
      b03: iosSound != null,
      b04: androidSound != null,
      b05: otherSound != null,
      b06: storiesMuted != null,
      b07: storiesHideSender != null,
      b08: storiesIosSound != null,
      b09: storiesAndroidSound != null,
      b10: storiesOtherSound != null,
    );

    return v;
  }

  /// Show Previews.
  final bool? showPreviews;

  /// Silent.
  final bool? silent;

  /// Mute Until.
  final int? muteUntil;

  /// Ios Sound.
  final NotificationSoundBase? iosSound;

  /// Android Sound.
  final NotificationSoundBase? androidSound;

  /// Other Sound.
  final NotificationSoundBase? otherSound;

  /// Stories Muted.
  final bool? storiesMuted;

  /// Stories Hide Sender.
  final bool? storiesHideSender;

  /// Stories Ios Sound.
  final NotificationSoundBase? storiesIosSound;

  /// Stories Android Sound.
  final NotificationSoundBase? storiesAndroidSound;

  /// Stories Other Sound.
  final NotificationSoundBase? storiesOtherSound;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x99622c0c);
    buffer.writeInt(flags);
    final localShowPreviewsCopy = showPreviews;
    if (localShowPreviewsCopy != null) {
      buffer.writeBool(localShowPreviewsCopy);
    }
    final localSilentCopy = silent;
    if (localSilentCopy != null) {
      buffer.writeBool(localSilentCopy);
    }
    final localMuteUntilCopy = muteUntil;
    if (localMuteUntilCopy != null) {
      buffer.writeInt(localMuteUntilCopy);
    }
    final localIosSoundCopy = iosSound;
    if (localIosSoundCopy != null) {
      buffer.writeObject(localIosSoundCopy);
    }
    final localAndroidSoundCopy = androidSound;
    if (localAndroidSoundCopy != null) {
      buffer.writeObject(localAndroidSoundCopy);
    }
    final localOtherSoundCopy = otherSound;
    if (localOtherSoundCopy != null) {
      buffer.writeObject(localOtherSoundCopy);
    }
    final localStoriesMutedCopy = storiesMuted;
    if (localStoriesMutedCopy != null) {
      buffer.writeBool(localStoriesMutedCopy);
    }
    final localStoriesHideSenderCopy = storiesHideSender;
    if (localStoriesHideSenderCopy != null) {
      buffer.writeBool(localStoriesHideSenderCopy);
    }
    final localStoriesIosSoundCopy = storiesIosSound;
    if (localStoriesIosSoundCopy != null) {
      buffer.writeObject(localStoriesIosSoundCopy);
    }
    final localStoriesAndroidSoundCopy = storiesAndroidSound;
    if (localStoriesAndroidSoundCopy != null) {
      buffer.writeObject(localStoriesAndroidSoundCopy);
    }
    final localStoriesOtherSoundCopy = storiesOtherSound;
    if (localStoriesOtherSoundCopy != null) {
      buffer.writeObject(localStoriesOtherSoundCopy);
    }
  }
}

/// Predicate Peer Settings.
abstract class PeerSettingsBase extends TlConstructor {
  /// Predicate Peer Settings constructor.
  const PeerSettingsBase._() : super._();
}

/// Peer Settings.
///
/// ID: `a518110d`.
class PeerSettings extends PeerSettingsBase {
  /// Peer Settings constructor.
  const PeerSettings({
    required this.reportSpam,
    required this.addContact,
    required this.blockContact,
    required this.shareContact,
    required this.needContactsException,
    required this.reportGeo,
    required this.autoarchived,
    required this.inviteMembers,
    required this.requestChatBroadcast,
    this.geoDistance,
    this.requestChatTitle,
    this.requestChatDate,
  }) : super._();

  /// Deserialize.
  factory PeerSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final reportSpam = _readflags.0?true(buffer);
    // final addContact = _readflags.1?true(buffer);
    // final blockContact = _readflags.2?true(buffer);
    // final shareContact = _readflags.3?true(buffer);
    // final needContactsException = _readflags.4?true(buffer);
    // final reportGeo = _readflags.5?true(buffer);
    // final autoarchived = _readflags.7?true(buffer);
    // final inviteMembers = _readflags.8?true(buffer);
    // final requestChatBroadcast = _readflags.10?true(buffer);
    // final geoDistance = _readflags.6?int(buffer);
    // final requestChatTitle = _readflags.9?string(buffer);
    // final requestChatDate = _readflags.9?int(buffer);
    // final result = PeerSettings(flags: flags, reportSpam: reportSpam, addContact: addContact, blockContact: blockContact, shareContact: shareContact, needContactsException: needContactsException, reportGeo: reportGeo, autoarchived: autoarchived, inviteMembers: inviteMembers, requestChatBroadcast: requestChatBroadcast, geoDistance: geoDistance, requestChatTitle: requestChatTitle, requestChatDate: requestChatDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: reportSpam,
      b01: addContact,
      b02: blockContact,
      b03: shareContact,
      b04: needContactsException,
      b05: reportGeo,
      b07: autoarchived,
      b08: inviteMembers,
      b10: requestChatBroadcast,
      b06: geoDistance != null,
      b09: requestChatTitle != null || requestChatDate != null,
    );

    return v;
  }

  /// report_spam: bit 0 of flags.0?true
  final bool reportSpam;

  /// add_contact: bit 1 of flags.1?true
  final bool addContact;

  /// block_contact: bit 2 of flags.2?true
  final bool blockContact;

  /// share_contact: bit 3 of flags.3?true
  final bool shareContact;

  /// need_contacts_exception: bit 4 of flags.4?true
  final bool needContactsException;

  /// report_geo: bit 5 of flags.5?true
  final bool reportGeo;

  /// autoarchived: bit 7 of flags.7?true
  final bool autoarchived;

  /// invite_members: bit 8 of flags.8?true
  final bool inviteMembers;

  /// request_chat_broadcast: bit 10 of flags.10?true
  final bool requestChatBroadcast;

  /// Geo Distance.
  final int? geoDistance;

  /// Request Chat Title.
  final String? requestChatTitle;

  /// Request Chat Date.
  final DateTime? requestChatDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa518110d);
    buffer.writeInt(flags);
    final localGeoDistanceCopy = geoDistance;
    if (localGeoDistanceCopy != null) {
      buffer.writeInt(localGeoDistanceCopy);
    }
    final localRequestChatTitleCopy = requestChatTitle;
    if (localRequestChatTitleCopy != null) {
      buffer.writeString(localRequestChatTitleCopy);
    }
    final localRequestChatDateCopy = requestChatDate;
    if (localRequestChatDateCopy != null) {
      buffer.writeDateTime(localRequestChatDateCopy);
    }
  }
}

/// Predicate Wall Paper.
abstract class WallPaperBase extends TlConstructor {
  /// Predicate Wall Paper constructor.
  const WallPaperBase._() : super._();
}

/// Wall Paper.
///
/// ID: `a437c3ed`.
class WallPaper extends WallPaperBase {
  /// Wall Paper constructor.
  const WallPaper({
    required this.id,
    required this.creator,
    required this.ddefault,
    required this.pattern,
    required this.dark,
    required this.accessHash,
    required this.slug,
    required this.document,
    this.settings,
  }) : super._();

  /// Deserialize.
  factory WallPaper.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final flags = _read#(buffer);
    // final creator = _readflags.0?true(buffer);
    // final ddefault = _readflags.1?true(buffer);
    // final pattern = _readflags.3?true(buffer);
    // final dark = _readflags.4?true(buffer);
    // final accessHash = _readlong(buffer);
    // final slug = _readstring(buffer);
    // final document = _readDocument(buffer);
    // final settings = _readflags.2?WallPaperSettings(buffer);
    // final result = WallPaper(id: id, flags: flags, creator: creator, ddefault: ddefault, pattern: pattern, dark: dark, accessHash: accessHash, slug: slug, document: document, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: creator,
      b01: ddefault,
      b03: pattern,
      b04: dark,
      b02: settings != null,
    );

    return v;
  }

  /// Id.
  final int id;

  /// creator: bit 0 of flags.0?true
  final bool creator;

  /// default: bit 1 of flags.1?true
  final bool ddefault;

  /// pattern: bit 3 of flags.3?true
  final bool pattern;

  /// dark: bit 4 of flags.4?true
  final bool dark;

  /// Access Hash.
  final int accessHash;

  /// Slug.
  final String slug;

  /// Document.
  final DocumentBase document;

  /// Settings.
  final WallPaperSettingsBase? settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa437c3ed);
    buffer.writeLong(id);
    buffer.writeInt(flags);
    buffer.writeLong(accessHash);
    buffer.writeString(slug);
    buffer.writeObject(document);
    final localSettingsCopy = settings;
    if (localSettingsCopy != null) {
      buffer.writeObject(localSettingsCopy);
    }
  }
}

/// Wall Paper No File.
///
/// ID: `e0804116`.
class WallPaperNoFile extends WallPaperBase {
  /// Wall Paper No File constructor.
  const WallPaperNoFile({
    required this.id,
    required this.ddefault,
    required this.dark,
    this.settings,
  }) : super._();

  /// Deserialize.
  factory WallPaperNoFile.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final flags = _read#(buffer);
    // final ddefault = _readflags.1?true(buffer);
    // final dark = _readflags.4?true(buffer);
    // final settings = _readflags.2?WallPaperSettings(buffer);
    // final result = WallPaperNoFile(id: id, flags: flags, ddefault: ddefault, dark: dark, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: ddefault,
      b04: dark,
      b02: settings != null,
    );

    return v;
  }

  /// Id.
  final int id;

  /// default: bit 1 of flags.1?true
  final bool ddefault;

  /// dark: bit 4 of flags.4?true
  final bool dark;

  /// Settings.
  final WallPaperSettingsBase? settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe0804116);
    buffer.writeLong(id);
    buffer.writeInt(flags);
    final localSettingsCopy = settings;
    if (localSettingsCopy != null) {
      buffer.writeObject(localSettingsCopy);
    }
  }
}

/// Predicate Report Reason.
abstract class ReportReasonBase extends TlConstructor {
  /// Predicate Report Reason constructor.
  const ReportReasonBase._() : super._();
}

/// Input Report Reason Spam.
///
/// ID: `58dbcab8`.
class InputReportReasonSpam extends ReportReasonBase {
  /// Input Report Reason Spam constructor.
  const InputReportReasonSpam() : super._();

  /// Deserialize.
  factory InputReportReasonSpam.deserialize(Uint8List buffer) {
    // final result = InputReportReasonSpam();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x58dbcab8);
  }
}

/// Input Report Reason Violence.
///
/// ID: `1e22c78d`.
class InputReportReasonViolence extends ReportReasonBase {
  /// Input Report Reason Violence constructor.
  const InputReportReasonViolence() : super._();

  /// Deserialize.
  factory InputReportReasonViolence.deserialize(Uint8List buffer) {
    // final result = InputReportReasonViolence();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1e22c78d);
  }
}

/// Input Report Reason Pornography.
///
/// ID: `2e59d922`.
class InputReportReasonPornography extends ReportReasonBase {
  /// Input Report Reason Pornography constructor.
  const InputReportReasonPornography() : super._();

  /// Deserialize.
  factory InputReportReasonPornography.deserialize(Uint8List buffer) {
    // final result = InputReportReasonPornography();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2e59d922);
  }
}

/// Input Report Reason Child Abuse.
///
/// ID: `adf44ee3`.
class InputReportReasonChildAbuse extends ReportReasonBase {
  /// Input Report Reason Child Abuse constructor.
  const InputReportReasonChildAbuse() : super._();

  /// Deserialize.
  factory InputReportReasonChildAbuse.deserialize(Uint8List buffer) {
    // final result = InputReportReasonChildAbuse();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xadf44ee3);
  }
}

/// Input Report Reason Other.
///
/// ID: `c1e4a2b1`.
class InputReportReasonOther extends ReportReasonBase {
  /// Input Report Reason Other constructor.
  const InputReportReasonOther() : super._();

  /// Deserialize.
  factory InputReportReasonOther.deserialize(Uint8List buffer) {
    // final result = InputReportReasonOther();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc1e4a2b1);
  }
}

/// Input Report Reason Copyright.
///
/// ID: `9b89f93a`.
class InputReportReasonCopyright extends ReportReasonBase {
  /// Input Report Reason Copyright constructor.
  const InputReportReasonCopyright() : super._();

  /// Deserialize.
  factory InputReportReasonCopyright.deserialize(Uint8List buffer) {
    // final result = InputReportReasonCopyright();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9b89f93a);
  }
}

/// Input Report Reason Geo Irrelevant.
///
/// ID: `dbd4feed`.
class InputReportReasonGeoIrrelevant extends ReportReasonBase {
  /// Input Report Reason Geo Irrelevant constructor.
  const InputReportReasonGeoIrrelevant() : super._();

  /// Deserialize.
  factory InputReportReasonGeoIrrelevant.deserialize(Uint8List buffer) {
    // final result = InputReportReasonGeoIrrelevant();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdbd4feed);
  }
}

/// Input Report Reason Fake.
///
/// ID: `f5ddd6e7`.
class InputReportReasonFake extends ReportReasonBase {
  /// Input Report Reason Fake constructor.
  const InputReportReasonFake() : super._();

  /// Deserialize.
  factory InputReportReasonFake.deserialize(Uint8List buffer) {
    // final result = InputReportReasonFake();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf5ddd6e7);
  }
}

/// Input Report Reason Illegal Drugs.
///
/// ID: `0a8eb2be`.
class InputReportReasonIllegalDrugs extends ReportReasonBase {
  /// Input Report Reason Illegal Drugs constructor.
  const InputReportReasonIllegalDrugs() : super._();

  /// Deserialize.
  factory InputReportReasonIllegalDrugs.deserialize(Uint8List buffer) {
    // final result = InputReportReasonIllegalDrugs();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0a8eb2be);
  }
}

/// Input Report Reason Personal Details.
///
/// ID: `9ec7863d`.
class InputReportReasonPersonalDetails extends ReportReasonBase {
  /// Input Report Reason Personal Details constructor.
  const InputReportReasonPersonalDetails() : super._();

  /// Deserialize.
  factory InputReportReasonPersonalDetails.deserialize(Uint8List buffer) {
    // final result = InputReportReasonPersonalDetails();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9ec7863d);
  }
}

/// Predicate User Full.
abstract class UserFullBase extends TlConstructor {
  /// Predicate User Full constructor.
  const UserFullBase._() : super._();
}

/// User Full.
///
/// ID: `b9b12c6c`.
class UserFull extends UserFullBase {
  /// User Full constructor.
  const UserFull({
    required this.blocked,
    required this.phoneCallsAvailable,
    required this.phoneCallsPrivate,
    required this.canPinMessage,
    required this.hasScheduled,
    required this.videoCallsAvailable,
    required this.voiceMessagesForbidden,
    required this.translationsDisabled,
    required this.storiesPinnedAvailable,
    required this.blockedMyStoriesFrom,
    required this.wallpaperOverridden,
    required this.id,
    this.about,
    required this.settings,
    this.personalPhoto,
    this.profilePhoto,
    this.fallbackPhoto,
    required this.notifySettings,
    this.botInfo,
    this.pinnedMsgId,
    required this.commonChatsCount,
    this.folderId,
    this.ttlPeriod,
    this.themeEmoticon,
    this.privateForwardName,
    this.botGroupAdminRights,
    this.botBroadcastAdminRights,
    this.premiumGifts,
    this.wallpaper,
    this.stories,
  }) : super._();

  /// Deserialize.
  factory UserFull.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final blocked = _readflags.0?true(buffer);
    // final phoneCallsAvailable = _readflags.4?true(buffer);
    // final phoneCallsPrivate = _readflags.5?true(buffer);
    // final canPinMessage = _readflags.7?true(buffer);
    // final hasScheduled = _readflags.12?true(buffer);
    // final videoCallsAvailable = _readflags.13?true(buffer);
    // final voiceMessagesForbidden = _readflags.20?true(buffer);
    // final translationsDisabled = _readflags.23?true(buffer);
    // final storiesPinnedAvailable = _readflags.26?true(buffer);
    // final blockedMyStoriesFrom = _readflags.27?true(buffer);
    // final wallpaperOverridden = _readflags.28?true(buffer);
    // final id = _readlong(buffer);
    // final about = _readflags.1?string(buffer);
    // final settings = _readPeerSettings(buffer);
    // final personalPhoto = _readflags.21?Photo(buffer);
    // final profilePhoto = _readflags.2?Photo(buffer);
    // final fallbackPhoto = _readflags.22?Photo(buffer);
    // final notifySettings = _readPeerNotifySettings(buffer);
    // final botInfo = _readflags.3?BotInfo(buffer);
    // final pinnedMsgId = _readflags.6?int(buffer);
    // final commonChatsCount = _readint(buffer);
    // final folderId = _readflags.11?int(buffer);
    // final ttlPeriod = _readflags.14?int(buffer);
    // final themeEmoticon = _readflags.15?string(buffer);
    // final privateForwardName = _readflags.16?string(buffer);
    // final botGroupAdminRights = _readflags.17?ChatAdminRights(buffer);
    // final botBroadcastAdminRights = _readflags.18?ChatAdminRights(buffer);
    // final premiumGifts = _readflags.19?Vector<PremiumGiftOption>(buffer);
    // final wallpaper = _readflags.24?WallPaper(buffer);
    // final stories = _readflags.25?PeerStories(buffer);
    // final result = UserFull(flags: flags, blocked: blocked, phoneCallsAvailable: phoneCallsAvailable, phoneCallsPrivate: phoneCallsPrivate, canPinMessage: canPinMessage, hasScheduled: hasScheduled, videoCallsAvailable: videoCallsAvailable, voiceMessagesForbidden: voiceMessagesForbidden, translationsDisabled: translationsDisabled, storiesPinnedAvailable: storiesPinnedAvailable, blockedMyStoriesFrom: blockedMyStoriesFrom, wallpaperOverridden: wallpaperOverridden, id: id, about: about, settings: settings, personalPhoto: personalPhoto, profilePhoto: profilePhoto, fallbackPhoto: fallbackPhoto, notifySettings: notifySettings, botInfo: botInfo, pinnedMsgId: pinnedMsgId, commonChatsCount: commonChatsCount, folderId: folderId, ttlPeriod: ttlPeriod, themeEmoticon: themeEmoticon, privateForwardName: privateForwardName, botGroupAdminRights: botGroupAdminRights, botBroadcastAdminRights: botBroadcastAdminRights, premiumGifts: premiumGifts, wallpaper: wallpaper, stories: stories,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: blocked,
      b04: phoneCallsAvailable,
      b05: phoneCallsPrivate,
      b07: canPinMessage,
      b12: hasScheduled,
      b13: videoCallsAvailable,
      b20: voiceMessagesForbidden,
      b23: translationsDisabled,
      b26: storiesPinnedAvailable,
      b27: blockedMyStoriesFrom,
      b28: wallpaperOverridden,
      b01: about != null,
      b21: personalPhoto != null,
      b02: profilePhoto != null,
      b22: fallbackPhoto != null,
      b03: botInfo != null,
      b06: pinnedMsgId != null,
      b11: folderId != null,
      b14: ttlPeriod != null,
      b15: themeEmoticon != null,
      b16: privateForwardName != null,
      b17: botGroupAdminRights != null,
      b18: botBroadcastAdminRights != null,
      b19: premiumGifts != null,
      b24: wallpaper != null,
      b25: stories != null,
    );

    return v;
  }

  /// blocked: bit 0 of flags.0?true
  final bool blocked;

  /// phone_calls_available: bit 4 of flags.4?true
  final bool phoneCallsAvailable;

  /// phone_calls_private: bit 5 of flags.5?true
  final bool phoneCallsPrivate;

  /// can_pin_message: bit 7 of flags.7?true
  final bool canPinMessage;

  /// has_scheduled: bit 12 of flags.12?true
  final bool hasScheduled;

  /// video_calls_available: bit 13 of flags.13?true
  final bool videoCallsAvailable;

  /// voice_messages_forbidden: bit 20 of flags.20?true
  final bool voiceMessagesForbidden;

  /// translations_disabled: bit 23 of flags.23?true
  final bool translationsDisabled;

  /// stories_pinned_available: bit 26 of flags.26?true
  final bool storiesPinnedAvailable;

  /// blocked_my_stories_from: bit 27 of flags.27?true
  final bool blockedMyStoriesFrom;

  /// wallpaper_overridden: bit 28 of flags.28?true
  final bool wallpaperOverridden;

  /// Id.
  final int id;

  /// About.
  final String? about;

  /// Settings.
  final PeerSettingsBase settings;

  /// Personal Photo.
  final PhotoBase? personalPhoto;

  /// Profile Photo.
  final PhotoBase? profilePhoto;

  /// Fallback Photo.
  final PhotoBase? fallbackPhoto;

  /// Notify Settings.
  final PeerNotifySettingsBase notifySettings;

  /// Bot Info.
  final BotInfoBase? botInfo;

  /// Pinned Msg Id.
  final int? pinnedMsgId;

  /// Common Chats Count.
  final int commonChatsCount;

  /// Folder Id.
  final int? folderId;

  /// Ttl Period.
  final int? ttlPeriod;

  /// Theme Emoticon.
  final String? themeEmoticon;

  /// Private Forward Name.
  final String? privateForwardName;

  /// Bot Group Admin Rights.
  final ChatAdminRightsBase? botGroupAdminRights;

  /// Bot Broadcast Admin Rights.
  final ChatAdminRightsBase? botBroadcastAdminRights;

  /// Premium Gifts.
  final List<PremiumGiftOptionBase>? premiumGifts;

  /// Wallpaper.
  final WallPaperBase? wallpaper;

  /// Stories.
  final PeerStoriesBase? stories;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb9b12c6c);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    final localAboutCopy = about;
    if (localAboutCopy != null) {
      buffer.writeString(localAboutCopy);
    }
    buffer.writeObject(settings);
    final localPersonalPhotoCopy = personalPhoto;
    if (localPersonalPhotoCopy != null) {
      buffer.writeObject(localPersonalPhotoCopy);
    }
    final localProfilePhotoCopy = profilePhoto;
    if (localProfilePhotoCopy != null) {
      buffer.writeObject(localProfilePhotoCopy);
    }
    final localFallbackPhotoCopy = fallbackPhoto;
    if (localFallbackPhotoCopy != null) {
      buffer.writeObject(localFallbackPhotoCopy);
    }
    buffer.writeObject(notifySettings);
    final localBotInfoCopy = botInfo;
    if (localBotInfoCopy != null) {
      buffer.writeObject(localBotInfoCopy);
    }
    final localPinnedMsgIdCopy = pinnedMsgId;
    if (localPinnedMsgIdCopy != null) {
      buffer.writeInt(localPinnedMsgIdCopy);
    }
    buffer.writeInt(commonChatsCount);
    final localFolderIdCopy = folderId;
    if (localFolderIdCopy != null) {
      buffer.writeInt(localFolderIdCopy);
    }
    final localTtlPeriodCopy = ttlPeriod;
    if (localTtlPeriodCopy != null) {
      buffer.writeInt(localTtlPeriodCopy);
    }
    final localThemeEmoticonCopy = themeEmoticon;
    if (localThemeEmoticonCopy != null) {
      buffer.writeString(localThemeEmoticonCopy);
    }
    final localPrivateForwardNameCopy = privateForwardName;
    if (localPrivateForwardNameCopy != null) {
      buffer.writeString(localPrivateForwardNameCopy);
    }
    final localBotGroupAdminRightsCopy = botGroupAdminRights;
    if (localBotGroupAdminRightsCopy != null) {
      buffer.writeObject(localBotGroupAdminRightsCopy);
    }
    final localBotBroadcastAdminRightsCopy = botBroadcastAdminRights;
    if (localBotBroadcastAdminRightsCopy != null) {
      buffer.writeObject(localBotBroadcastAdminRightsCopy);
    }
    final localPremiumGiftsCopy = premiumGifts;
    if (localPremiumGiftsCopy != null) {
      buffer.writeVectorObject(localPremiumGiftsCopy);
    }
    final localWallpaperCopy = wallpaper;
    if (localWallpaperCopy != null) {
      buffer.writeObject(localWallpaperCopy);
    }
    final localStoriesCopy = stories;
    if (localStoriesCopy != null) {
      buffer.writeObject(localStoriesCopy);
    }
  }
}

/// Predicate Contact.
abstract class ContactBase extends TlConstructor {
  /// Predicate Contact constructor.
  const ContactBase._() : super._();
}

/// Contact.
///
/// ID: `145ade0b`.
class Contact extends ContactBase {
  /// Contact constructor.
  const Contact({
    required this.userId,
    required this.mutual,
  }) : super._();

  /// Deserialize.
  factory Contact.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final mutual = _readBool(buffer);
    // final result = Contact(userId: userId, mutual: mutual,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Mutual.
  final bool mutual;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x145ade0b);
    buffer.writeLong(userId);
    buffer.writeBool(mutual);
  }
}

/// Predicate Imported Contact.
abstract class ImportedContactBase extends TlConstructor {
  /// Predicate Imported Contact constructor.
  const ImportedContactBase._() : super._();
}

/// Imported Contact.
///
/// ID: `c13e3c50`.
class ImportedContact extends ImportedContactBase {
  /// Imported Contact constructor.
  const ImportedContact({
    required this.userId,
    required this.clientId,
  }) : super._();

  /// Deserialize.
  factory ImportedContact.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final clientId = _readlong(buffer);
    // final result = ImportedContact(userId: userId, clientId: clientId,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Client Id.
  final int clientId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc13e3c50);
    buffer.writeLong(userId);
    buffer.writeLong(clientId);
  }
}

/// Predicate Contact Status.
abstract class ContactStatusBase extends TlConstructor {
  /// Predicate Contact Status constructor.
  const ContactStatusBase._() : super._();
}

/// Contact Status.
///
/// ID: `16d9703b`.
class ContactStatus extends ContactStatusBase {
  /// Contact Status constructor.
  const ContactStatus({
    required this.userId,
    required this.status,
  }) : super._();

  /// Deserialize.
  factory ContactStatus.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final status = _readUserStatus(buffer);
    // final result = ContactStatus(userId: userId, status: status,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Status.
  final UserStatusBase status;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x16d9703b);
    buffer.writeLong(userId);
    buffer.writeObject(status);
  }
}

/// Predicate Contacts Contacts.
abstract class ContactsContactsBase extends TlConstructor {
  /// Predicate Contacts Contacts constructor.
  const ContactsContactsBase._() : super._();
}

/// Contacts Contacts Not Modified.
///
/// ID: `b74ba9d2`.
class ContactsContactsNotModified extends ContactsContactsBase {
  /// Contacts Contacts Not Modified constructor.
  const ContactsContactsNotModified() : super._();

  /// Deserialize.
  factory ContactsContactsNotModified.deserialize(Uint8List buffer) {
    // final result = ContactsContactsNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb74ba9d2);
  }
}

/// Contacts Contacts.
///
/// ID: `eae87e42`.
class ContactsContacts extends ContactsContactsBase {
  /// Contacts Contacts constructor.
  const ContactsContacts({
    required this.contacts,
    required this.savedCount,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ContactsContacts.deserialize(Uint8List buffer) {
    // final contacts = _readVector<Contact>(buffer);
    // final savedCount = _readint(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ContactsContacts(contacts: contacts, savedCount: savedCount, users: users,);

    // return result;

    throw Exception();
  }

  /// Contacts.
  final List<ContactBase> contacts;

  /// Saved Count.
  final int savedCount;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xeae87e42);
    buffer.writeVectorObject(contacts);
    buffer.writeInt(savedCount);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Contacts Imported Contacts.
abstract class ContactsImportedContactsBase extends TlConstructor {
  /// Predicate Contacts Imported Contacts constructor.
  const ContactsImportedContactsBase._() : super._();
}

/// Contacts Imported Contacts.
///
/// ID: `77d01c3b`.
class ContactsImportedContacts extends ContactsImportedContactsBase {
  /// Contacts Imported Contacts constructor.
  const ContactsImportedContacts({
    required this.imported,
    required this.popularInvites,
    required this.retryContacts,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ContactsImportedContacts.deserialize(Uint8List buffer) {
    // final imported = _readVector<ImportedContact>(buffer);
    // final popularInvites = _readVector<PopularContact>(buffer);
    // final retryContacts = _readVector<long>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ContactsImportedContacts(imported: imported, popularInvites: popularInvites, retryContacts: retryContacts, users: users,);

    // return result;

    throw Exception();
  }

  /// Imported.
  final List<ImportedContactBase> imported;

  /// Popular Invites.
  final List<PopularContactBase> popularInvites;

  /// Retry Contacts.
  final List<int> retryContacts;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x77d01c3b);
    buffer.writeVectorObject(imported);
    buffer.writeVectorObject(popularInvites);
    buffer.writeVectorLong(retryContacts);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Contacts Blocked.
abstract class ContactsBlockedBase extends TlConstructor {
  /// Predicate Contacts Blocked constructor.
  const ContactsBlockedBase._() : super._();
}

/// Contacts Blocked.
///
/// ID: `0ade1591`.
class ContactsBlocked extends ContactsBlockedBase {
  /// Contacts Blocked constructor.
  const ContactsBlocked({
    required this.blocked,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ContactsBlocked.deserialize(Uint8List buffer) {
    // final blocked = _readVector<PeerBlocked>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ContactsBlocked(blocked: blocked, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Blocked.
  final List<PeerBlockedBase> blocked;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0ade1591);
    buffer.writeVectorObject(blocked);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Contacts Blocked Slice.
///
/// ID: `e1664194`.
class ContactsBlockedSlice extends ContactsBlockedBase {
  /// Contacts Blocked Slice constructor.
  const ContactsBlockedSlice({
    required this.count,
    required this.blocked,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ContactsBlockedSlice.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final blocked = _readVector<PeerBlocked>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ContactsBlockedSlice(count: count, blocked: blocked, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Blocked.
  final List<PeerBlockedBase> blocked;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe1664194);
    buffer.writeInt(count);
    buffer.writeVectorObject(blocked);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Messages Dialogs.
abstract class MessagesDialogsBase extends TlConstructor {
  /// Predicate Messages Dialogs constructor.
  const MessagesDialogsBase._() : super._();
}

/// Messages Dialogs.
///
/// ID: `15ba6c40`.
class MessagesDialogs extends MessagesDialogsBase {
  /// Messages Dialogs constructor.
  const MessagesDialogs({
    required this.dialogs,
    required this.messages,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesDialogs.deserialize(Uint8List buffer) {
    // final dialogs = _readVector<Dialog>(buffer);
    // final messages = _readVector<Message>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesDialogs(dialogs: dialogs, messages: messages, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Dialogs.
  final List<DialogBase> dialogs;

  /// Messages.
  final List<MessageBase> messages;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x15ba6c40);
    buffer.writeVectorObject(dialogs);
    buffer.writeVectorObject(messages);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Messages Dialogs Slice.
///
/// ID: `71e094f3`.
class MessagesDialogsSlice extends MessagesDialogsBase {
  /// Messages Dialogs Slice constructor.
  const MessagesDialogsSlice({
    required this.count,
    required this.dialogs,
    required this.messages,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesDialogsSlice.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final dialogs = _readVector<Dialog>(buffer);
    // final messages = _readVector<Message>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesDialogsSlice(count: count, dialogs: dialogs, messages: messages, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Dialogs.
  final List<DialogBase> dialogs;

  /// Messages.
  final List<MessageBase> messages;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x71e094f3);
    buffer.writeInt(count);
    buffer.writeVectorObject(dialogs);
    buffer.writeVectorObject(messages);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Messages Dialogs Not Modified.
///
/// ID: `f0e3e596`.
class MessagesDialogsNotModified extends MessagesDialogsBase {
  /// Messages Dialogs Not Modified constructor.
  const MessagesDialogsNotModified({
    required this.count,
  }) : super._();

  /// Deserialize.
  factory MessagesDialogsNotModified.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final result = MessagesDialogsNotModified(count: count,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf0e3e596);
    buffer.writeInt(count);
  }
}

/// Predicate Messages Messages.
abstract class MessagesMessagesBase extends TlConstructor {
  /// Predicate Messages Messages constructor.
  const MessagesMessagesBase._() : super._();
}

/// Messages Messages.
///
/// ID: `8c718e87`.
class MessagesMessages extends MessagesMessagesBase {
  /// Messages Messages constructor.
  const MessagesMessages({
    required this.messages,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesMessages.deserialize(Uint8List buffer) {
    // final messages = _readVector<Message>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesMessages(messages: messages, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Messages.
  final List<MessageBase> messages;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8c718e87);
    buffer.writeVectorObject(messages);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Messages Messages Slice.
///
/// ID: `3a54685e`.
class MessagesMessagesSlice extends MessagesMessagesBase {
  /// Messages Messages Slice constructor.
  const MessagesMessagesSlice({
    required this.inexact,
    required this.count,
    this.nextRate,
    this.offsetIdOffset,
    required this.messages,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesMessagesSlice.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final inexact = _readflags.1?true(buffer);
    // final count = _readint(buffer);
    // final nextRate = _readflags.0?int(buffer);
    // final offsetIdOffset = _readflags.2?int(buffer);
    // final messages = _readVector<Message>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesMessagesSlice(flags: flags, inexact: inexact, count: count, nextRate: nextRate, offsetIdOffset: offsetIdOffset, messages: messages, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: inexact,
      b00: nextRate != null,
      b02: offsetIdOffset != null,
    );

    return v;
  }

  /// inexact: bit 1 of flags.1?true
  final bool inexact;

  /// Count.
  final int count;

  /// Next Rate.
  final int? nextRate;

  /// Offset Id Offset.
  final int? offsetIdOffset;

  /// Messages.
  final List<MessageBase> messages;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3a54685e);
    buffer.writeInt(flags);
    buffer.writeInt(count);
    final localNextRateCopy = nextRate;
    if (localNextRateCopy != null) {
      buffer.writeInt(localNextRateCopy);
    }
    final localOffsetIdOffsetCopy = offsetIdOffset;
    if (localOffsetIdOffsetCopy != null) {
      buffer.writeInt(localOffsetIdOffsetCopy);
    }
    buffer.writeVectorObject(messages);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Messages Channel Messages.
///
/// ID: `c776ba4e`.
class MessagesChannelMessages extends MessagesMessagesBase {
  /// Messages Channel Messages constructor.
  const MessagesChannelMessages({
    required this.inexact,
    required this.pts,
    required this.count,
    this.offsetIdOffset,
    required this.messages,
    required this.topics,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesChannelMessages.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final inexact = _readflags.1?true(buffer);
    // final pts = _readint(buffer);
    // final count = _readint(buffer);
    // final offsetIdOffset = _readflags.2?int(buffer);
    // final messages = _readVector<Message>(buffer);
    // final topics = _readVector<ForumTopic>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesChannelMessages(flags: flags, inexact: inexact, pts: pts, count: count, offsetIdOffset: offsetIdOffset, messages: messages, topics: topics, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: inexact,
      b02: offsetIdOffset != null,
    );

    return v;
  }

  /// inexact: bit 1 of flags.1?true
  final bool inexact;

  /// Pts.
  final int pts;

  /// Count.
  final int count;

  /// Offset Id Offset.
  final int? offsetIdOffset;

  /// Messages.
  final List<MessageBase> messages;

  /// Topics.
  final List<ForumTopicBase> topics;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc776ba4e);
    buffer.writeInt(flags);
    buffer.writeInt(pts);
    buffer.writeInt(count);
    final localOffsetIdOffsetCopy = offsetIdOffset;
    if (localOffsetIdOffsetCopy != null) {
      buffer.writeInt(localOffsetIdOffsetCopy);
    }
    buffer.writeVectorObject(messages);
    buffer.writeVectorObject(topics);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Messages Messages Not Modified.
///
/// ID: `74535f21`.
class MessagesMessagesNotModified extends MessagesMessagesBase {
  /// Messages Messages Not Modified constructor.
  const MessagesMessagesNotModified({
    required this.count,
  }) : super._();

  /// Deserialize.
  factory MessagesMessagesNotModified.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final result = MessagesMessagesNotModified(count: count,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x74535f21);
    buffer.writeInt(count);
  }
}

/// Predicate Messages Chats.
abstract class MessagesChatsBase extends TlConstructor {
  /// Predicate Messages Chats constructor.
  const MessagesChatsBase._() : super._();
}

/// Messages Chats.
///
/// ID: `64ff9fd5`.
class MessagesChats extends MessagesChatsBase {
  /// Messages Chats constructor.
  const MessagesChats({
    required this.chats,
  }) : super._();

  /// Deserialize.
  factory MessagesChats.deserialize(Uint8List buffer) {
    // final chats = _readVector<Chat>(buffer);
    // final result = MessagesChats(chats: chats,);

    // return result;

    throw Exception();
  }

  /// Chats.
  final List<ChatBase> chats;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x64ff9fd5);
    buffer.writeVectorObject(chats);
  }
}

/// Messages Chats Slice.
///
/// ID: `9cd81144`.
class MessagesChatsSlice extends MessagesChatsBase {
  /// Messages Chats Slice constructor.
  const MessagesChatsSlice({
    required this.count,
    required this.chats,
  }) : super._();

  /// Deserialize.
  factory MessagesChatsSlice.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final result = MessagesChatsSlice(count: count, chats: chats,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Chats.
  final List<ChatBase> chats;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9cd81144);
    buffer.writeInt(count);
    buffer.writeVectorObject(chats);
  }
}

/// Predicate Messages Chat Full.
abstract class MessagesChatFullBase extends TlConstructor {
  /// Predicate Messages Chat Full constructor.
  const MessagesChatFullBase._() : super._();
}

/// Messages Chat Full.
///
/// ID: `e5d7d19c`.
class MessagesChatFull extends MessagesChatFullBase {
  /// Messages Chat Full constructor.
  const MessagesChatFull({
    required this.fullChat,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesChatFull.deserialize(Uint8List buffer) {
    // final fullChat = _readChatFull(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesChatFull(fullChat: fullChat, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Full Chat.
  final ChatFullBase fullChat;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe5d7d19c);
    buffer.writeObject(fullChat);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Messages Affected History.
abstract class MessagesAffectedHistoryBase extends TlConstructor {
  /// Predicate Messages Affected History constructor.
  const MessagesAffectedHistoryBase._() : super._();
}

/// Messages Affected History.
///
/// ID: `b45c69d1`.
class MessagesAffectedHistory extends MessagesAffectedHistoryBase {
  /// Messages Affected History constructor.
  const MessagesAffectedHistory({
    required this.pts,
    required this.ptsCount,
    required this.offset,
  }) : super._();

  /// Deserialize.
  factory MessagesAffectedHistory.deserialize(Uint8List buffer) {
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final offset = _readint(buffer);
    // final result = MessagesAffectedHistory(pts: pts, ptsCount: ptsCount, offset: offset,);

    // return result;

    throw Exception();
  }

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Offset.
  final int offset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb45c69d1);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
    buffer.writeInt(offset);
  }
}

/// Predicate Messages Filter.
abstract class MessagesFilterBase extends TlConstructor {
  /// Predicate Messages Filter constructor.
  const MessagesFilterBase._() : super._();
}

/// Input Messages Filter Empty.
///
/// ID: `57e2f66c`.
class InputMessagesFilterEmpty extends MessagesFilterBase {
  /// Input Messages Filter Empty constructor.
  const InputMessagesFilterEmpty() : super._();

  /// Deserialize.
  factory InputMessagesFilterEmpty.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x57e2f66c);
  }
}

/// Input Messages Filter Photos.
///
/// ID: `9609a51c`.
class InputMessagesFilterPhotos extends MessagesFilterBase {
  /// Input Messages Filter Photos constructor.
  const InputMessagesFilterPhotos() : super._();

  /// Deserialize.
  factory InputMessagesFilterPhotos.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterPhotos();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9609a51c);
  }
}

/// Input Messages Filter Video.
///
/// ID: `9fc00e65`.
class InputMessagesFilterVideo extends MessagesFilterBase {
  /// Input Messages Filter Video constructor.
  const InputMessagesFilterVideo() : super._();

  /// Deserialize.
  factory InputMessagesFilterVideo.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterVideo();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9fc00e65);
  }
}

/// Input Messages Filter Photo Video.
///
/// ID: `56e9f0e4`.
class InputMessagesFilterPhotoVideo extends MessagesFilterBase {
  /// Input Messages Filter Photo Video constructor.
  const InputMessagesFilterPhotoVideo() : super._();

  /// Deserialize.
  factory InputMessagesFilterPhotoVideo.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterPhotoVideo();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x56e9f0e4);
  }
}

/// Input Messages Filter Document.
///
/// ID: `9eddf188`.
class InputMessagesFilterDocument extends MessagesFilterBase {
  /// Input Messages Filter Document constructor.
  const InputMessagesFilterDocument() : super._();

  /// Deserialize.
  factory InputMessagesFilterDocument.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterDocument();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9eddf188);
  }
}

/// Input Messages Filter Url.
///
/// ID: `7ef0dd87`.
class InputMessagesFilterUrl extends MessagesFilterBase {
  /// Input Messages Filter Url constructor.
  const InputMessagesFilterUrl() : super._();

  /// Deserialize.
  factory InputMessagesFilterUrl.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterUrl();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7ef0dd87);
  }
}

/// Input Messages Filter Gif.
///
/// ID: `ffc86587`.
class InputMessagesFilterGif extends MessagesFilterBase {
  /// Input Messages Filter Gif constructor.
  const InputMessagesFilterGif() : super._();

  /// Deserialize.
  factory InputMessagesFilterGif.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterGif();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xffc86587);
  }
}

/// Input Messages Filter Voice.
///
/// ID: `50f5c392`.
class InputMessagesFilterVoice extends MessagesFilterBase {
  /// Input Messages Filter Voice constructor.
  const InputMessagesFilterVoice() : super._();

  /// Deserialize.
  factory InputMessagesFilterVoice.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterVoice();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x50f5c392);
  }
}

/// Input Messages Filter Music.
///
/// ID: `3751b49e`.
class InputMessagesFilterMusic extends MessagesFilterBase {
  /// Input Messages Filter Music constructor.
  const InputMessagesFilterMusic() : super._();

  /// Deserialize.
  factory InputMessagesFilterMusic.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterMusic();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3751b49e);
  }
}

/// Input Messages Filter Chat Photos.
///
/// ID: `3a20ecb8`.
class InputMessagesFilterChatPhotos extends MessagesFilterBase {
  /// Input Messages Filter Chat Photos constructor.
  const InputMessagesFilterChatPhotos() : super._();

  /// Deserialize.
  factory InputMessagesFilterChatPhotos.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterChatPhotos();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3a20ecb8);
  }
}

/// Input Messages Filter Phone Calls.
///
/// ID: `80c99768`.
class InputMessagesFilterPhoneCalls extends MessagesFilterBase {
  /// Input Messages Filter Phone Calls constructor.
  const InputMessagesFilterPhoneCalls({
    required this.missed,
  }) : super._();

  /// Deserialize.
  factory InputMessagesFilterPhoneCalls.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final missed = _readflags.0?true(buffer);
    // final result = InputMessagesFilterPhoneCalls(flags: flags, missed: missed,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: missed,
    );

    return v;
  }

  /// missed: bit 0 of flags.0?true
  final bool missed;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x80c99768);
    buffer.writeInt(flags);
  }
}

/// Input Messages Filter Round Voice.
///
/// ID: `7a7c17a4`.
class InputMessagesFilterRoundVoice extends MessagesFilterBase {
  /// Input Messages Filter Round Voice constructor.
  const InputMessagesFilterRoundVoice() : super._();

  /// Deserialize.
  factory InputMessagesFilterRoundVoice.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterRoundVoice();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7a7c17a4);
  }
}

/// Input Messages Filter Round Video.
///
/// ID: `b549da53`.
class InputMessagesFilterRoundVideo extends MessagesFilterBase {
  /// Input Messages Filter Round Video constructor.
  const InputMessagesFilterRoundVideo() : super._();

  /// Deserialize.
  factory InputMessagesFilterRoundVideo.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterRoundVideo();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb549da53);
  }
}

/// Input Messages Filter My Mentions.
///
/// ID: `c1f8e69a`.
class InputMessagesFilterMyMentions extends MessagesFilterBase {
  /// Input Messages Filter My Mentions constructor.
  const InputMessagesFilterMyMentions() : super._();

  /// Deserialize.
  factory InputMessagesFilterMyMentions.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterMyMentions();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc1f8e69a);
  }
}

/// Input Messages Filter Geo.
///
/// ID: `e7026d0d`.
class InputMessagesFilterGeo extends MessagesFilterBase {
  /// Input Messages Filter Geo constructor.
  const InputMessagesFilterGeo() : super._();

  /// Deserialize.
  factory InputMessagesFilterGeo.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterGeo();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe7026d0d);
  }
}

/// Input Messages Filter Contacts.
///
/// ID: `e062db83`.
class InputMessagesFilterContacts extends MessagesFilterBase {
  /// Input Messages Filter Contacts constructor.
  const InputMessagesFilterContacts() : super._();

  /// Deserialize.
  factory InputMessagesFilterContacts.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterContacts();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe062db83);
  }
}

/// Input Messages Filter Pinned.
///
/// ID: `1bb00451`.
class InputMessagesFilterPinned extends MessagesFilterBase {
  /// Input Messages Filter Pinned constructor.
  const InputMessagesFilterPinned() : super._();

  /// Deserialize.
  factory InputMessagesFilterPinned.deserialize(Uint8List buffer) {
    // final result = InputMessagesFilterPinned();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1bb00451);
  }
}

/// Predicate Update.
abstract class UpdateBase extends TlConstructor {
  /// Predicate Update constructor.
  const UpdateBase._() : super._();
}

/// Update New Message.
///
/// ID: `1f2b0afd`.
class UpdateNewMessage extends UpdateBase {
  /// Update New Message constructor.
  const UpdateNewMessage({
    required this.message,
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory UpdateNewMessage.deserialize(Uint8List buffer) {
    // final message = _readMessage(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = UpdateNewMessage(message: message, pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Message.
  final MessageBase message;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1f2b0afd);
    buffer.writeObject(message);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Update Message I D.
///
/// ID: `4e90bfd6`.
class UpdateMessageID extends UpdateBase {
  /// Update Message I D constructor.
  const UpdateMessageID({
    required this.id,
    required this.randomId,
  }) : super._();

  /// Deserialize.
  factory UpdateMessageID.deserialize(Uint8List buffer) {
    // final id = _readint(buffer);
    // final randomId = _readlong(buffer);
    // final result = UpdateMessageID(id: id, randomId: randomId,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Random Id.
  final int randomId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4e90bfd6);
    buffer.writeInt(id);
    buffer.writeLong(randomId);
  }
}

/// Update Delete Messages.
///
/// ID: `a20db0e5`.
class UpdateDeleteMessages extends UpdateBase {
  /// Update Delete Messages constructor.
  const UpdateDeleteMessages({
    required this.messages,
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory UpdateDeleteMessages.deserialize(Uint8List buffer) {
    // final messages = _readVector<int>(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = UpdateDeleteMessages(messages: messages, pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Messages.
  final List<int> messages;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa20db0e5);
    buffer.writeVectorInt(messages);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Update User Typing.
///
/// ID: `c01e857f`.
class UpdateUserTyping extends UpdateBase {
  /// Update User Typing constructor.
  const UpdateUserTyping({
    required this.userId,
    required this.action,
  }) : super._();

  /// Deserialize.
  factory UpdateUserTyping.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final action = _readSendMessageAction(buffer);
    // final result = UpdateUserTyping(userId: userId, action: action,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Action.
  final SendMessageActionBase action;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc01e857f);
    buffer.writeLong(userId);
    buffer.writeObject(action);
  }
}

/// Update Chat User Typing.
///
/// ID: `83487af0`.
class UpdateChatUserTyping extends UpdateBase {
  /// Update Chat User Typing constructor.
  const UpdateChatUserTyping({
    required this.chatId,
    required this.fromId,
    required this.action,
  }) : super._();

  /// Deserialize.
  factory UpdateChatUserTyping.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final fromId = _readPeer(buffer);
    // final action = _readSendMessageAction(buffer);
    // final result = UpdateChatUserTyping(chatId: chatId, fromId: fromId, action: action,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// From Id.
  final PeerBase fromId;

  /// Action.
  final SendMessageActionBase action;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x83487af0);
    buffer.writeLong(chatId);
    buffer.writeObject(fromId);
    buffer.writeObject(action);
  }
}

/// Update Chat Participants.
///
/// ID: `07761198`.
class UpdateChatParticipants extends UpdateBase {
  /// Update Chat Participants constructor.
  const UpdateChatParticipants({
    required this.participants,
  }) : super._();

  /// Deserialize.
  factory UpdateChatParticipants.deserialize(Uint8List buffer) {
    // final participants = _readChatParticipants(buffer);
    // final result = UpdateChatParticipants(participants: participants,);

    // return result;

    throw Exception();
  }

  /// Participants.
  final ChatParticipantsBase participants;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x07761198);
    buffer.writeObject(participants);
  }
}

/// Update User Status.
///
/// ID: `e5bdf8de`.
class UpdateUserStatus extends UpdateBase {
  /// Update User Status constructor.
  const UpdateUserStatus({
    required this.userId,
    required this.status,
  }) : super._();

  /// Deserialize.
  factory UpdateUserStatus.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final status = _readUserStatus(buffer);
    // final result = UpdateUserStatus(userId: userId, status: status,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Status.
  final UserStatusBase status;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe5bdf8de);
    buffer.writeLong(userId);
    buffer.writeObject(status);
  }
}

/// Update User Name.
///
/// ID: `a7848924`.
class UpdateUserName extends UpdateBase {
  /// Update User Name constructor.
  const UpdateUserName({
    required this.userId,
    required this.firstName,
    required this.lastName,
    required this.usernames,
  }) : super._();

  /// Deserialize.
  factory UpdateUserName.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final firstName = _readstring(buffer);
    // final lastName = _readstring(buffer);
    // final usernames = _readVector<Username>(buffer);
    // final result = UpdateUserName(userId: userId, firstName: firstName, lastName: lastName, usernames: usernames,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// First Name.
  final String firstName;

  /// Last Name.
  final String lastName;

  /// Usernames.
  final List<UsernameBase> usernames;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa7848924);
    buffer.writeLong(userId);
    buffer.writeString(firstName);
    buffer.writeString(lastName);
    buffer.writeVectorObject(usernames);
  }
}

/// Update New Authorization.
///
/// ID: `8951abef`.
class UpdateNewAuthorization extends UpdateBase {
  /// Update New Authorization constructor.
  const UpdateNewAuthorization({
    required this.unconfirmed,
    required this.hash,
    this.date,
    this.device,
    this.location,
  }) : super._();

  /// Deserialize.
  factory UpdateNewAuthorization.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final unconfirmed = _readflags.0?true(buffer);
    // final hash = _readlong(buffer);
    // final date = _readflags.0?int(buffer);
    // final device = _readflags.0?string(buffer);
    // final location = _readflags.0?string(buffer);
    // final result = UpdateNewAuthorization(flags: flags, unconfirmed: unconfirmed, hash: hash, date: date, device: device, location: location,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: unconfirmed || date != null || device != null || location != null,
    );

    return v;
  }

  /// unconfirmed: bit 0 of flags.0?true
  final bool unconfirmed;

  /// Hash.
  final int hash;

  /// Date.
  final DateTime? date;

  /// Device.
  final String? device;

  /// Location.
  final String? location;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8951abef);
    buffer.writeInt(flags);
    buffer.writeLong(hash);
    final localDateCopy = date;
    if (localDateCopy != null) {
      buffer.writeDateTime(localDateCopy);
    }
    final localDeviceCopy = device;
    if (localDeviceCopy != null) {
      buffer.writeString(localDeviceCopy);
    }
    final localLocationCopy = location;
    if (localLocationCopy != null) {
      buffer.writeString(localLocationCopy);
    }
  }
}

/// Update New Encrypted Message.
///
/// ID: `12bcbd9a`.
class UpdateNewEncryptedMessage extends UpdateBase {
  /// Update New Encrypted Message constructor.
  const UpdateNewEncryptedMessage({
    required this.message,
    required this.qts,
  }) : super._();

  /// Deserialize.
  factory UpdateNewEncryptedMessage.deserialize(Uint8List buffer) {
    // final message = _readEncryptedMessage(buffer);
    // final qts = _readint(buffer);
    // final result = UpdateNewEncryptedMessage(message: message, qts: qts,);

    // return result;

    throw Exception();
  }

  /// Message.
  final EncryptedMessageBase message;

  /// Qts.
  final int qts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x12bcbd9a);
    buffer.writeObject(message);
    buffer.writeInt(qts);
  }
}

/// Update Encrypted Chat Typing.
///
/// ID: `1710f156`.
class UpdateEncryptedChatTyping extends UpdateBase {
  /// Update Encrypted Chat Typing constructor.
  const UpdateEncryptedChatTyping({
    required this.chatId,
  }) : super._();

  /// Deserialize.
  factory UpdateEncryptedChatTyping.deserialize(Uint8List buffer) {
    // final chatId = _readint(buffer);
    // final result = UpdateEncryptedChatTyping(chatId: chatId,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1710f156);
    buffer.writeInt(chatId);
  }
}

/// Update Encryption.
///
/// ID: `b4a2e88d`.
class UpdateEncryption extends UpdateBase {
  /// Update Encryption constructor.
  const UpdateEncryption({
    required this.chat,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory UpdateEncryption.deserialize(Uint8List buffer) {
    // final chat = _readEncryptedChat(buffer);
    // final date = _readint(buffer);
    // final result = UpdateEncryption(chat: chat, date: date,);

    // return result;

    throw Exception();
  }

  /// Chat.
  final EncryptedChatBase chat;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb4a2e88d);
    buffer.writeObject(chat);
    buffer.writeDateTime(date);
  }
}

/// Update Encrypted Messages Read.
///
/// ID: `38fe25b7`.
class UpdateEncryptedMessagesRead extends UpdateBase {
  /// Update Encrypted Messages Read constructor.
  const UpdateEncryptedMessagesRead({
    required this.chatId,
    required this.maxDate,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory UpdateEncryptedMessagesRead.deserialize(Uint8List buffer) {
    // final chatId = _readint(buffer);
    // final maxDate = _readint(buffer);
    // final date = _readint(buffer);
    // final result = UpdateEncryptedMessagesRead(chatId: chatId, maxDate: maxDate, date: date,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// Max Date.
  final DateTime maxDate;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x38fe25b7);
    buffer.writeInt(chatId);
    buffer.writeDateTime(maxDate);
    buffer.writeDateTime(date);
  }
}

/// Update Chat Participant Add.
///
/// ID: `3dda5451`.
class UpdateChatParticipantAdd extends UpdateBase {
  /// Update Chat Participant Add constructor.
  const UpdateChatParticipantAdd({
    required this.chatId,
    required this.userId,
    required this.inviterId,
    required this.date,
    required this.version,
  }) : super._();

  /// Deserialize.
  factory UpdateChatParticipantAdd.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final userId = _readlong(buffer);
    // final inviterId = _readlong(buffer);
    // final date = _readint(buffer);
    // final version = _readint(buffer);
    // final result = UpdateChatParticipantAdd(chatId: chatId, userId: userId, inviterId: inviterId, date: date, version: version,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// User Id.
  final int userId;

  /// Inviter Id.
  final int inviterId;

  /// Date.
  final DateTime date;

  /// Version.
  final int version;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3dda5451);
    buffer.writeLong(chatId);
    buffer.writeLong(userId);
    buffer.writeLong(inviterId);
    buffer.writeDateTime(date);
    buffer.writeInt(version);
  }
}

/// Update Chat Participant Delete.
///
/// ID: `e32f3d77`.
class UpdateChatParticipantDelete extends UpdateBase {
  /// Update Chat Participant Delete constructor.
  const UpdateChatParticipantDelete({
    required this.chatId,
    required this.userId,
    required this.version,
  }) : super._();

  /// Deserialize.
  factory UpdateChatParticipantDelete.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final userId = _readlong(buffer);
    // final version = _readint(buffer);
    // final result = UpdateChatParticipantDelete(chatId: chatId, userId: userId, version: version,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// User Id.
  final int userId;

  /// Version.
  final int version;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe32f3d77);
    buffer.writeLong(chatId);
    buffer.writeLong(userId);
    buffer.writeInt(version);
  }
}

/// Update Dc Options.
///
/// ID: `8e5e9873`.
class UpdateDcOptions extends UpdateBase {
  /// Update Dc Options constructor.
  const UpdateDcOptions({
    required this.dcOptions,
  }) : super._();

  /// Deserialize.
  factory UpdateDcOptions.deserialize(Uint8List buffer) {
    // final dcOptions = _readVector<DcOption>(buffer);
    // final result = UpdateDcOptions(dcOptions: dcOptions,);

    // return result;

    throw Exception();
  }

  /// Dc Options.
  final List<DcOptionBase> dcOptions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8e5e9873);
    buffer.writeVectorObject(dcOptions);
  }
}

/// Update Notify Settings.
///
/// ID: `bec268ef`.
class UpdateNotifySettings extends UpdateBase {
  /// Update Notify Settings constructor.
  const UpdateNotifySettings({
    required this.peer,
    required this.notifySettings,
  }) : super._();

  /// Deserialize.
  factory UpdateNotifySettings.deserialize(Uint8List buffer) {
    // final peer = _readNotifyPeer(buffer);
    // final notifySettings = _readPeerNotifySettings(buffer);
    // final result = UpdateNotifySettings(peer: peer, notifySettings: notifySettings,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final NotifyPeerBase peer;

  /// Notify Settings.
  final PeerNotifySettingsBase notifySettings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbec268ef);
    buffer.writeObject(peer);
    buffer.writeObject(notifySettings);
  }
}

/// Update Service Notification.
///
/// ID: `ebe46819`.
class UpdateServiceNotification extends UpdateBase {
  /// Update Service Notification constructor.
  const UpdateServiceNotification({
    required this.popup,
    required this.invertMedia,
    this.inboxDate,
    required this.type,
    required this.message,
    required this.media,
    required this.entities,
  }) : super._();

  /// Deserialize.
  factory UpdateServiceNotification.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final popup = _readflags.0?true(buffer);
    // final invertMedia = _readflags.2?true(buffer);
    // final inboxDate = _readflags.1?int(buffer);
    // final type = _readstring(buffer);
    // final message = _readstring(buffer);
    // final media = _readMessageMedia(buffer);
    // final entities = _readVector<MessageEntity>(buffer);
    // final result = UpdateServiceNotification(flags: flags, popup: popup, invertMedia: invertMedia, inboxDate: inboxDate, type: type, message: message, media: media, entities: entities,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: popup,
      b02: invertMedia,
      b01: inboxDate != null,
    );

    return v;
  }

  /// popup: bit 0 of flags.0?true
  final bool popup;

  /// invert_media: bit 2 of flags.2?true
  final bool invertMedia;

  /// Inbox Date.
  final DateTime? inboxDate;

  /// Type.
  final String type;

  /// Message.
  final String message;

  /// Media.
  final MessageMediaBase media;

  /// Entities.
  final List<MessageEntityBase> entities;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xebe46819);
    buffer.writeInt(flags);
    final localInboxDateCopy = inboxDate;
    if (localInboxDateCopy != null) {
      buffer.writeDateTime(localInboxDateCopy);
    }
    buffer.writeString(type);
    buffer.writeString(message);
    buffer.writeObject(media);
    buffer.writeVectorObject(entities);
  }
}

/// Update Privacy.
///
/// ID: `ee3b272a`.
class UpdatePrivacy extends UpdateBase {
  /// Update Privacy constructor.
  const UpdatePrivacy({
    required this.key,
    required this.rules,
  }) : super._();

  /// Deserialize.
  factory UpdatePrivacy.deserialize(Uint8List buffer) {
    // final key = _readPrivacyKey(buffer);
    // final rules = _readVector<PrivacyRule>(buffer);
    // final result = UpdatePrivacy(key: key, rules: rules,);

    // return result;

    throw Exception();
  }

  /// Key.
  final PrivacyKeyBase key;

  /// Rules.
  final List<PrivacyRuleBase> rules;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xee3b272a);
    buffer.writeObject(key);
    buffer.writeVectorObject(rules);
  }
}

/// Update User Phone.
///
/// ID: `05492a13`.
class UpdateUserPhone extends UpdateBase {
  /// Update User Phone constructor.
  const UpdateUserPhone({
    required this.userId,
    required this.phone,
  }) : super._();

  /// Deserialize.
  factory UpdateUserPhone.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final phone = _readstring(buffer);
    // final result = UpdateUserPhone(userId: userId, phone: phone,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Phone.
  final String phone;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x05492a13);
    buffer.writeLong(userId);
    buffer.writeString(phone);
  }
}

/// Update Read History Inbox.
///
/// ID: `9c974fdf`.
class UpdateReadHistoryInbox extends UpdateBase {
  /// Update Read History Inbox constructor.
  const UpdateReadHistoryInbox({
    this.folderId,
    required this.peer,
    required this.maxId,
    required this.stillUnreadCount,
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory UpdateReadHistoryInbox.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final folderId = _readflags.0?int(buffer);
    // final peer = _readPeer(buffer);
    // final maxId = _readint(buffer);
    // final stillUnreadCount = _readint(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = UpdateReadHistoryInbox(flags: flags, folderId: folderId, peer: peer, maxId: maxId, stillUnreadCount: stillUnreadCount, pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: folderId != null,
    );

    return v;
  }

  /// Folder Id.
  final int? folderId;

  /// Peer.
  final PeerBase peer;

  /// Max Id.
  final int maxId;

  /// Still Unread Count.
  final int stillUnreadCount;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9c974fdf);
    buffer.writeInt(flags);
    final localFolderIdCopy = folderId;
    if (localFolderIdCopy != null) {
      buffer.writeInt(localFolderIdCopy);
    }
    buffer.writeObject(peer);
    buffer.writeInt(maxId);
    buffer.writeInt(stillUnreadCount);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Update Read History Outbox.
///
/// ID: `2f2f21bf`.
class UpdateReadHistoryOutbox extends UpdateBase {
  /// Update Read History Outbox constructor.
  const UpdateReadHistoryOutbox({
    required this.peer,
    required this.maxId,
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory UpdateReadHistoryOutbox.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final maxId = _readint(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = UpdateReadHistoryOutbox(peer: peer, maxId: maxId, pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Max Id.
  final int maxId;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2f2f21bf);
    buffer.writeObject(peer);
    buffer.writeInt(maxId);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Update Web Page.
///
/// ID: `7f891213`.
class UpdateWebPage extends UpdateBase {
  /// Update Web Page constructor.
  const UpdateWebPage({
    required this.webpage,
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory UpdateWebPage.deserialize(Uint8List buffer) {
    // final webpage = _readWebPage(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = UpdateWebPage(webpage: webpage, pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Webpage.
  final WebPageBase webpage;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7f891213);
    buffer.writeObject(webpage);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Update Read Messages Contents.
///
/// ID: `f8227181`.
class UpdateReadMessagesContents extends UpdateBase {
  /// Update Read Messages Contents constructor.
  const UpdateReadMessagesContents({
    required this.messages,
    required this.pts,
    required this.ptsCount,
    this.date,
  }) : super._();

  /// Deserialize.
  factory UpdateReadMessagesContents.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final messages = _readVector<int>(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final date = _readflags.0?int(buffer);
    // final result = UpdateReadMessagesContents(flags: flags, messages: messages, pts: pts, ptsCount: ptsCount, date: date,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: date != null,
    );

    return v;
  }

  /// Messages.
  final List<int> messages;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Date.
  final DateTime? date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf8227181);
    buffer.writeInt(flags);
    buffer.writeVectorInt(messages);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
    final localDateCopy = date;
    if (localDateCopy != null) {
      buffer.writeDateTime(localDateCopy);
    }
  }
}

/// Update Channel Too Long.
///
/// ID: `108d941f`.
class UpdateChannelTooLong extends UpdateBase {
  /// Update Channel Too Long constructor.
  const UpdateChannelTooLong({
    required this.channelId,
    this.pts,
  }) : super._();

  /// Deserialize.
  factory UpdateChannelTooLong.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final channelId = _readlong(buffer);
    // final pts = _readflags.0?int(buffer);
    // final result = UpdateChannelTooLong(flags: flags, channelId: channelId, pts: pts,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: pts != null,
    );

    return v;
  }

  /// Channel Id.
  final int channelId;

  /// Pts.
  final int? pts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x108d941f);
    buffer.writeInt(flags);
    buffer.writeLong(channelId);
    final localPtsCopy = pts;
    if (localPtsCopy != null) {
      buffer.writeInt(localPtsCopy);
    }
  }
}

/// Update Channel.
///
/// ID: `635b4c09`.
class UpdateChannel extends UpdateBase {
  /// Update Channel constructor.
  const UpdateChannel({
    required this.channelId,
  }) : super._();

  /// Deserialize.
  factory UpdateChannel.deserialize(Uint8List buffer) {
    // final channelId = _readlong(buffer);
    // final result = UpdateChannel(channelId: channelId,);

    // return result;

    throw Exception();
  }

  /// Channel Id.
  final int channelId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x635b4c09);
    buffer.writeLong(channelId);
  }
}

/// Update New Channel Message.
///
/// ID: `62ba04d9`.
class UpdateNewChannelMessage extends UpdateBase {
  /// Update New Channel Message constructor.
  const UpdateNewChannelMessage({
    required this.message,
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory UpdateNewChannelMessage.deserialize(Uint8List buffer) {
    // final message = _readMessage(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = UpdateNewChannelMessage(message: message, pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Message.
  final MessageBase message;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x62ba04d9);
    buffer.writeObject(message);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Update Read Channel Inbox.
///
/// ID: `922e6e10`.
class UpdateReadChannelInbox extends UpdateBase {
  /// Update Read Channel Inbox constructor.
  const UpdateReadChannelInbox({
    this.folderId,
    required this.channelId,
    required this.maxId,
    required this.stillUnreadCount,
    required this.pts,
  }) : super._();

  /// Deserialize.
  factory UpdateReadChannelInbox.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final folderId = _readflags.0?int(buffer);
    // final channelId = _readlong(buffer);
    // final maxId = _readint(buffer);
    // final stillUnreadCount = _readint(buffer);
    // final pts = _readint(buffer);
    // final result = UpdateReadChannelInbox(flags: flags, folderId: folderId, channelId: channelId, maxId: maxId, stillUnreadCount: stillUnreadCount, pts: pts,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: folderId != null,
    );

    return v;
  }

  /// Folder Id.
  final int? folderId;

  /// Channel Id.
  final int channelId;

  /// Max Id.
  final int maxId;

  /// Still Unread Count.
  final int stillUnreadCount;

  /// Pts.
  final int pts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x922e6e10);
    buffer.writeInt(flags);
    final localFolderIdCopy = folderId;
    if (localFolderIdCopy != null) {
      buffer.writeInt(localFolderIdCopy);
    }
    buffer.writeLong(channelId);
    buffer.writeInt(maxId);
    buffer.writeInt(stillUnreadCount);
    buffer.writeInt(pts);
  }
}

/// Update Delete Channel Messages.
///
/// ID: `c32d5b12`.
class UpdateDeleteChannelMessages extends UpdateBase {
  /// Update Delete Channel Messages constructor.
  const UpdateDeleteChannelMessages({
    required this.channelId,
    required this.messages,
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory UpdateDeleteChannelMessages.deserialize(Uint8List buffer) {
    // final channelId = _readlong(buffer);
    // final messages = _readVector<int>(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = UpdateDeleteChannelMessages(channelId: channelId, messages: messages, pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Channel Id.
  final int channelId;

  /// Messages.
  final List<int> messages;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc32d5b12);
    buffer.writeLong(channelId);
    buffer.writeVectorInt(messages);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Update Channel Message Views.
///
/// ID: `f226ac08`.
class UpdateChannelMessageViews extends UpdateBase {
  /// Update Channel Message Views constructor.
  const UpdateChannelMessageViews({
    required this.channelId,
    required this.id,
    required this.views,
  }) : super._();

  /// Deserialize.
  factory UpdateChannelMessageViews.deserialize(Uint8List buffer) {
    // final channelId = _readlong(buffer);
    // final id = _readint(buffer);
    // final views = _readint(buffer);
    // final result = UpdateChannelMessageViews(channelId: channelId, id: id, views: views,);

    // return result;

    throw Exception();
  }

  /// Channel Id.
  final int channelId;

  /// Id.
  final int id;

  /// Views.
  final int views;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf226ac08);
    buffer.writeLong(channelId);
    buffer.writeInt(id);
    buffer.writeInt(views);
  }
}

/// Update Chat Participant Admin.
///
/// ID: `d7ca61a2`.
class UpdateChatParticipantAdmin extends UpdateBase {
  /// Update Chat Participant Admin constructor.
  const UpdateChatParticipantAdmin({
    required this.chatId,
    required this.userId,
    required this.isAdmin,
    required this.version,
  }) : super._();

  /// Deserialize.
  factory UpdateChatParticipantAdmin.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final userId = _readlong(buffer);
    // final isAdmin = _readBool(buffer);
    // final version = _readint(buffer);
    // final result = UpdateChatParticipantAdmin(chatId: chatId, userId: userId, isAdmin: isAdmin, version: version,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// User Id.
  final int userId;

  /// Is Admin.
  final bool isAdmin;

  /// Version.
  final int version;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd7ca61a2);
    buffer.writeLong(chatId);
    buffer.writeLong(userId);
    buffer.writeBool(isAdmin);
    buffer.writeInt(version);
  }
}

/// Update New Sticker Set.
///
/// ID: `688a30aa`.
class UpdateNewStickerSet extends UpdateBase {
  /// Update New Sticker Set constructor.
  const UpdateNewStickerSet({
    required this.stickerset,
  }) : super._();

  /// Deserialize.
  factory UpdateNewStickerSet.deserialize(Uint8List buffer) {
    // final stickerset = _readmessages.StickerSet(buffer);
    // final result = UpdateNewStickerSet(stickerset: stickerset,);

    // return result;

    throw Exception();
  }

  /// Stickerset.
  final MessagesStickerSetBase stickerset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x688a30aa);
    buffer.writeObject(stickerset);
  }
}

/// Update Sticker Sets Order.
///
/// ID: `0bb2d201`.
class UpdateStickerSetsOrder extends UpdateBase {
  /// Update Sticker Sets Order constructor.
  const UpdateStickerSetsOrder({
    required this.masks,
    required this.emojis,
    required this.order,
  }) : super._();

  /// Deserialize.
  factory UpdateStickerSetsOrder.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final masks = _readflags.0?true(buffer);
    // final emojis = _readflags.1?true(buffer);
    // final order = _readVector<long>(buffer);
    // final result = UpdateStickerSetsOrder(flags: flags, masks: masks, emojis: emojis, order: order,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: masks,
      b01: emojis,
    );

    return v;
  }

  /// masks: bit 0 of flags.0?true
  final bool masks;

  /// emojis: bit 1 of flags.1?true
  final bool emojis;

  /// Order.
  final List<int> order;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0bb2d201);
    buffer.writeInt(flags);
    buffer.writeVectorLong(order);
  }
}

/// Update Sticker Sets.
///
/// ID: `31c24808`.
class UpdateStickerSets extends UpdateBase {
  /// Update Sticker Sets constructor.
  const UpdateStickerSets({
    required this.masks,
    required this.emojis,
  }) : super._();

  /// Deserialize.
  factory UpdateStickerSets.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final masks = _readflags.0?true(buffer);
    // final emojis = _readflags.1?true(buffer);
    // final result = UpdateStickerSets(flags: flags, masks: masks, emojis: emojis,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: masks,
      b01: emojis,
    );

    return v;
  }

  /// masks: bit 0 of flags.0?true
  final bool masks;

  /// emojis: bit 1 of flags.1?true
  final bool emojis;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x31c24808);
    buffer.writeInt(flags);
  }
}

/// Update Saved Gifs.
///
/// ID: `9375341e`.
class UpdateSavedGifs extends UpdateBase {
  /// Update Saved Gifs constructor.
  const UpdateSavedGifs() : super._();

  /// Deserialize.
  factory UpdateSavedGifs.deserialize(Uint8List buffer) {
    // final result = UpdateSavedGifs();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9375341e);
  }
}

/// Update Bot Inline Query.
///
/// ID: `496f379c`.
class UpdateBotInlineQuery extends UpdateBase {
  /// Update Bot Inline Query constructor.
  const UpdateBotInlineQuery({
    required this.queryId,
    required this.userId,
    required this.query,
    this.geo,
    this.peerType,
    required this.offset,
  }) : super._();

  /// Deserialize.
  factory UpdateBotInlineQuery.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final queryId = _readlong(buffer);
    // final userId = _readlong(buffer);
    // final query = _readstring(buffer);
    // final geo = _readflags.0?GeoPoint(buffer);
    // final peerType = _readflags.1?InlineQueryPeerType(buffer);
    // final offset = _readstring(buffer);
    // final result = UpdateBotInlineQuery(flags: flags, queryId: queryId, userId: userId, query: query, geo: geo, peerType: peerType, offset: offset,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: geo != null,
      b01: peerType != null,
    );

    return v;
  }

  /// Query Id.
  final int queryId;

  /// User Id.
  final int userId;

  /// Query.
  final String query;

  /// Geo.
  final GeoPointBase? geo;

  /// Peer Type.
  final InlineQueryPeerTypeBase? peerType;

  /// Offset.
  final String offset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x496f379c);
    buffer.writeInt(flags);
    buffer.writeLong(queryId);
    buffer.writeLong(userId);
    buffer.writeString(query);
    final localGeoCopy = geo;
    if (localGeoCopy != null) {
      buffer.writeObject(localGeoCopy);
    }
    final localPeerTypeCopy = peerType;
    if (localPeerTypeCopy != null) {
      buffer.writeObject(localPeerTypeCopy);
    }
    buffer.writeString(offset);
  }
}

/// Update Bot Inline Send.
///
/// ID: `12f12a07`.
class UpdateBotInlineSend extends UpdateBase {
  /// Update Bot Inline Send constructor.
  const UpdateBotInlineSend({
    required this.userId,
    required this.query,
    this.geo,
    required this.id,
    this.msgId,
  }) : super._();

  /// Deserialize.
  factory UpdateBotInlineSend.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final userId = _readlong(buffer);
    // final query = _readstring(buffer);
    // final geo = _readflags.0?GeoPoint(buffer);
    // final id = _readstring(buffer);
    // final msgId = _readflags.1?InputBotInlineMessageID(buffer);
    // final result = UpdateBotInlineSend(flags: flags, userId: userId, query: query, geo: geo, id: id, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: geo != null,
      b01: msgId != null,
    );

    return v;
  }

  /// User Id.
  final int userId;

  /// Query.
  final String query;

  /// Geo.
  final GeoPointBase? geo;

  /// Id.
  final String id;

  /// Msg Id.
  final InputBotInlineMessageIDBase? msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x12f12a07);
    buffer.writeInt(flags);
    buffer.writeLong(userId);
    buffer.writeString(query);
    final localGeoCopy = geo;
    if (localGeoCopy != null) {
      buffer.writeObject(localGeoCopy);
    }
    buffer.writeString(id);
    final localMsgIdCopy = msgId;
    if (localMsgIdCopy != null) {
      buffer.writeObject(localMsgIdCopy);
    }
  }
}

/// Update Edit Channel Message.
///
/// ID: `1b3f4df7`.
class UpdateEditChannelMessage extends UpdateBase {
  /// Update Edit Channel Message constructor.
  const UpdateEditChannelMessage({
    required this.message,
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory UpdateEditChannelMessage.deserialize(Uint8List buffer) {
    // final message = _readMessage(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = UpdateEditChannelMessage(message: message, pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Message.
  final MessageBase message;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1b3f4df7);
    buffer.writeObject(message);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Update Bot Callback Query.
///
/// ID: `b9cfc48d`.
class UpdateBotCallbackQuery extends UpdateBase {
  /// Update Bot Callback Query constructor.
  const UpdateBotCallbackQuery({
    required this.queryId,
    required this.userId,
    required this.peer,
    required this.msgId,
    required this.chatInstance,
    this.data,
    this.gameShortName,
  }) : super._();

  /// Deserialize.
  factory UpdateBotCallbackQuery.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final queryId = _readlong(buffer);
    // final userId = _readlong(buffer);
    // final peer = _readPeer(buffer);
    // final msgId = _readint(buffer);
    // final chatInstance = _readlong(buffer);
    // final data = _readflags.0?bytes(buffer);
    // final gameShortName = _readflags.1?string(buffer);
    // final result = UpdateBotCallbackQuery(flags: flags, queryId: queryId, userId: userId, peer: peer, msgId: msgId, chatInstance: chatInstance, data: data, gameShortName: gameShortName,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: data != null,
      b01: gameShortName != null,
    );

    return v;
  }

  /// Query Id.
  final int queryId;

  /// User Id.
  final int userId;

  /// Peer.
  final PeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Chat Instance.
  final int chatInstance;

  /// Data.
  final Uint8List? data;

  /// Game Short Name.
  final String? gameShortName;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb9cfc48d);
    buffer.writeInt(flags);
    buffer.writeLong(queryId);
    buffer.writeLong(userId);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeLong(chatInstance);
    final localDataCopy = data;
    if (localDataCopy != null) {
      buffer.writeBytes(localDataCopy);
    }
    final localGameShortNameCopy = gameShortName;
    if (localGameShortNameCopy != null) {
      buffer.writeString(localGameShortNameCopy);
    }
  }
}

/// Update Edit Message.
///
/// ID: `e40370a3`.
class UpdateEditMessage extends UpdateBase {
  /// Update Edit Message constructor.
  const UpdateEditMessage({
    required this.message,
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory UpdateEditMessage.deserialize(Uint8List buffer) {
    // final message = _readMessage(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = UpdateEditMessage(message: message, pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Message.
  final MessageBase message;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe40370a3);
    buffer.writeObject(message);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Update Inline Bot Callback Query.
///
/// ID: `691e9052`.
class UpdateInlineBotCallbackQuery extends UpdateBase {
  /// Update Inline Bot Callback Query constructor.
  const UpdateInlineBotCallbackQuery({
    required this.queryId,
    required this.userId,
    required this.msgId,
    required this.chatInstance,
    this.data,
    this.gameShortName,
  }) : super._();

  /// Deserialize.
  factory UpdateInlineBotCallbackQuery.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final queryId = _readlong(buffer);
    // final userId = _readlong(buffer);
    // final msgId = _readInputBotInlineMessageID(buffer);
    // final chatInstance = _readlong(buffer);
    // final data = _readflags.0?bytes(buffer);
    // final gameShortName = _readflags.1?string(buffer);
    // final result = UpdateInlineBotCallbackQuery(flags: flags, queryId: queryId, userId: userId, msgId: msgId, chatInstance: chatInstance, data: data, gameShortName: gameShortName,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: data != null,
      b01: gameShortName != null,
    );

    return v;
  }

  /// Query Id.
  final int queryId;

  /// User Id.
  final int userId;

  /// Msg Id.
  final InputBotInlineMessageIDBase msgId;

  /// Chat Instance.
  final int chatInstance;

  /// Data.
  final Uint8List? data;

  /// Game Short Name.
  final String? gameShortName;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x691e9052);
    buffer.writeInt(flags);
    buffer.writeLong(queryId);
    buffer.writeLong(userId);
    buffer.writeObject(msgId);
    buffer.writeLong(chatInstance);
    final localDataCopy = data;
    if (localDataCopy != null) {
      buffer.writeBytes(localDataCopy);
    }
    final localGameShortNameCopy = gameShortName;
    if (localGameShortNameCopy != null) {
      buffer.writeString(localGameShortNameCopy);
    }
  }
}

/// Update Read Channel Outbox.
///
/// ID: `b75f99a9`.
class UpdateReadChannelOutbox extends UpdateBase {
  /// Update Read Channel Outbox constructor.
  const UpdateReadChannelOutbox({
    required this.channelId,
    required this.maxId,
  }) : super._();

  /// Deserialize.
  factory UpdateReadChannelOutbox.deserialize(Uint8List buffer) {
    // final channelId = _readlong(buffer);
    // final maxId = _readint(buffer);
    // final result = UpdateReadChannelOutbox(channelId: channelId, maxId: maxId,);

    // return result;

    throw Exception();
  }

  /// Channel Id.
  final int channelId;

  /// Max Id.
  final int maxId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb75f99a9);
    buffer.writeLong(channelId);
    buffer.writeInt(maxId);
  }
}

/// Update Draft Message.
///
/// ID: `1b49ec6d`.
class UpdateDraftMessage extends UpdateBase {
  /// Update Draft Message constructor.
  const UpdateDraftMessage({
    required this.peer,
    this.topMsgId,
    required this.draft,
  }) : super._();

  /// Deserialize.
  factory UpdateDraftMessage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readPeer(buffer);
    // final topMsgId = _readflags.0?int(buffer);
    // final draft = _readDraftMessage(buffer);
    // final result = UpdateDraftMessage(flags: flags, peer: peer, topMsgId: topMsgId, draft: draft,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: topMsgId != null,
    );

    return v;
  }

  /// Peer.
  final PeerBase peer;

  /// Top Msg Id.
  final int? topMsgId;

  /// Draft.
  final DraftMessageBase draft;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1b49ec6d);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
    buffer.writeObject(draft);
  }
}

/// Update Read Featured Stickers.
///
/// ID: `571d2742`.
class UpdateReadFeaturedStickers extends UpdateBase {
  /// Update Read Featured Stickers constructor.
  const UpdateReadFeaturedStickers() : super._();

  /// Deserialize.
  factory UpdateReadFeaturedStickers.deserialize(Uint8List buffer) {
    // final result = UpdateReadFeaturedStickers();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x571d2742);
  }
}

/// Update Recent Stickers.
///
/// ID: `9a422c20`.
class UpdateRecentStickers extends UpdateBase {
  /// Update Recent Stickers constructor.
  const UpdateRecentStickers() : super._();

  /// Deserialize.
  factory UpdateRecentStickers.deserialize(Uint8List buffer) {
    // final result = UpdateRecentStickers();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9a422c20);
  }
}

/// Update Config.
///
/// ID: `a229dd06`.
class UpdateConfig extends UpdateBase {
  /// Update Config constructor.
  const UpdateConfig() : super._();

  /// Deserialize.
  factory UpdateConfig.deserialize(Uint8List buffer) {
    // final result = UpdateConfig();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa229dd06);
  }
}

/// Update Pts Changed.
///
/// ID: `3354678f`.
class UpdatePtsChanged extends UpdateBase {
  /// Update Pts Changed constructor.
  const UpdatePtsChanged() : super._();

  /// Deserialize.
  factory UpdatePtsChanged.deserialize(Uint8List buffer) {
    // final result = UpdatePtsChanged();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3354678f);
  }
}

/// Update Channel Web Page.
///
/// ID: `2f2ba99f`.
class UpdateChannelWebPage extends UpdateBase {
  /// Update Channel Web Page constructor.
  const UpdateChannelWebPage({
    required this.channelId,
    required this.webpage,
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory UpdateChannelWebPage.deserialize(Uint8List buffer) {
    // final channelId = _readlong(buffer);
    // final webpage = _readWebPage(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = UpdateChannelWebPage(channelId: channelId, webpage: webpage, pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Channel Id.
  final int channelId;

  /// Webpage.
  final WebPageBase webpage;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2f2ba99f);
    buffer.writeLong(channelId);
    buffer.writeObject(webpage);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Update Dialog Pinned.
///
/// ID: `6e6fe51c`.
class UpdateDialogPinned extends UpdateBase {
  /// Update Dialog Pinned constructor.
  const UpdateDialogPinned({
    required this.pinned,
    this.folderId,
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory UpdateDialogPinned.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pinned = _readflags.0?true(buffer);
    // final folderId = _readflags.1?int(buffer);
    // final peer = _readDialogPeer(buffer);
    // final result = UpdateDialogPinned(flags: flags, pinned: pinned, folderId: folderId, peer: peer,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: pinned,
      b01: folderId != null,
    );

    return v;
  }

  /// pinned: bit 0 of flags.0?true
  final bool pinned;

  /// Folder Id.
  final int? folderId;

  /// Peer.
  final DialogPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6e6fe51c);
    buffer.writeInt(flags);
    final localFolderIdCopy = folderId;
    if (localFolderIdCopy != null) {
      buffer.writeInt(localFolderIdCopy);
    }
    buffer.writeObject(peer);
  }
}

/// Update Pinned Dialogs.
///
/// ID: `fa0f3ca2`.
class UpdatePinnedDialogs extends UpdateBase {
  /// Update Pinned Dialogs constructor.
  const UpdatePinnedDialogs({
    this.folderId,
    this.order,
  }) : super._();

  /// Deserialize.
  factory UpdatePinnedDialogs.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final folderId = _readflags.1?int(buffer);
    // final order = _readflags.0?Vector<DialogPeer>(buffer);
    // final result = UpdatePinnedDialogs(flags: flags, folderId: folderId, order: order,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: folderId != null,
      b00: order != null,
    );

    return v;
  }

  /// Folder Id.
  final int? folderId;

  /// Order.
  final List<DialogPeerBase>? order;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfa0f3ca2);
    buffer.writeInt(flags);
    final localFolderIdCopy = folderId;
    if (localFolderIdCopy != null) {
      buffer.writeInt(localFolderIdCopy);
    }
    final localOrderCopy = order;
    if (localOrderCopy != null) {
      buffer.writeVectorObject(localOrderCopy);
    }
  }
}

/// Update Bot Webhook J S O N.
///
/// ID: `8317c0c3`.
class UpdateBotWebhookJSON extends UpdateBase {
  /// Update Bot Webhook J S O N constructor.
  const UpdateBotWebhookJSON({
    required this.data,
  }) : super._();

  /// Deserialize.
  factory UpdateBotWebhookJSON.deserialize(Uint8List buffer) {
    // final data = _readDataJSON(buffer);
    // final result = UpdateBotWebhookJSON(data: data,);

    // return result;

    throw Exception();
  }

  /// Data.
  final DataJSONBase data;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8317c0c3);
    buffer.writeObject(data);
  }
}

/// Update Bot Webhook J S O N Query.
///
/// ID: `9b9240a6`.
class UpdateBotWebhookJSONQuery extends UpdateBase {
  /// Update Bot Webhook J S O N Query constructor.
  const UpdateBotWebhookJSONQuery({
    required this.queryId,
    required this.data,
    required this.timeout,
  }) : super._();

  /// Deserialize.
  factory UpdateBotWebhookJSONQuery.deserialize(Uint8List buffer) {
    // final queryId = _readlong(buffer);
    // final data = _readDataJSON(buffer);
    // final timeout = _readint(buffer);
    // final result = UpdateBotWebhookJSONQuery(queryId: queryId, data: data, timeout: timeout,);

    // return result;

    throw Exception();
  }

  /// Query Id.
  final int queryId;

  /// Data.
  final DataJSONBase data;

  /// Timeout.
  final int timeout;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9b9240a6);
    buffer.writeLong(queryId);
    buffer.writeObject(data);
    buffer.writeInt(timeout);
  }
}

/// Update Bot Shipping Query.
///
/// ID: `b5aefd7d`.
class UpdateBotShippingQuery extends UpdateBase {
  /// Update Bot Shipping Query constructor.
  const UpdateBotShippingQuery({
    required this.queryId,
    required this.userId,
    required this.payload,
    required this.shippingAddress,
  }) : super._();

  /// Deserialize.
  factory UpdateBotShippingQuery.deserialize(Uint8List buffer) {
    // final queryId = _readlong(buffer);
    // final userId = _readlong(buffer);
    // final payload = _readbytes(buffer);
    // final shippingAddress = _readPostAddress(buffer);
    // final result = UpdateBotShippingQuery(queryId: queryId, userId: userId, payload: payload, shippingAddress: shippingAddress,);

    // return result;

    throw Exception();
  }

  /// Query Id.
  final int queryId;

  /// User Id.
  final int userId;

  /// Payload.
  final Uint8List payload;

  /// Shipping Address.
  final PostAddressBase shippingAddress;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb5aefd7d);
    buffer.writeLong(queryId);
    buffer.writeLong(userId);
    buffer.writeBytes(payload);
    buffer.writeObject(shippingAddress);
  }
}

/// Update Bot Precheckout Query.
///
/// ID: `8caa9a96`.
class UpdateBotPrecheckoutQuery extends UpdateBase {
  /// Update Bot Precheckout Query constructor.
  const UpdateBotPrecheckoutQuery({
    required this.queryId,
    required this.userId,
    required this.payload,
    this.info,
    this.shippingOptionId,
    required this.currency,
    required this.totalAmount,
  }) : super._();

  /// Deserialize.
  factory UpdateBotPrecheckoutQuery.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final queryId = _readlong(buffer);
    // final userId = _readlong(buffer);
    // final payload = _readbytes(buffer);
    // final info = _readflags.0?PaymentRequestedInfo(buffer);
    // final shippingOptionId = _readflags.1?string(buffer);
    // final currency = _readstring(buffer);
    // final totalAmount = _readlong(buffer);
    // final result = UpdateBotPrecheckoutQuery(flags: flags, queryId: queryId, userId: userId, payload: payload, info: info, shippingOptionId: shippingOptionId, currency: currency, totalAmount: totalAmount,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: info != null,
      b01: shippingOptionId != null,
    );

    return v;
  }

  /// Query Id.
  final int queryId;

  /// User Id.
  final int userId;

  /// Payload.
  final Uint8List payload;

  /// Info.
  final PaymentRequestedInfoBase? info;

  /// Shipping Option Id.
  final String? shippingOptionId;

  /// Currency.
  final String currency;

  /// Total Amount.
  final int totalAmount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8caa9a96);
    buffer.writeInt(flags);
    buffer.writeLong(queryId);
    buffer.writeLong(userId);
    buffer.writeBytes(payload);
    final localInfoCopy = info;
    if (localInfoCopy != null) {
      buffer.writeObject(localInfoCopy);
    }
    final localShippingOptionIdCopy = shippingOptionId;
    if (localShippingOptionIdCopy != null) {
      buffer.writeString(localShippingOptionIdCopy);
    }
    buffer.writeString(currency);
    buffer.writeLong(totalAmount);
  }
}

/// Update Phone Call.
///
/// ID: `ab0f6b1e`.
class UpdatePhoneCall extends UpdateBase {
  /// Update Phone Call constructor.
  const UpdatePhoneCall({
    required this.phoneCall,
  }) : super._();

  /// Deserialize.
  factory UpdatePhoneCall.deserialize(Uint8List buffer) {
    // final phoneCall = _readPhoneCall(buffer);
    // final result = UpdatePhoneCall(phoneCall: phoneCall,);

    // return result;

    throw Exception();
  }

  /// Phone Call.
  final PhoneCallBase phoneCall;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xab0f6b1e);
    buffer.writeObject(phoneCall);
  }
}

/// Update Lang Pack Too Long.
///
/// ID: `46560264`.
class UpdateLangPackTooLong extends UpdateBase {
  /// Update Lang Pack Too Long constructor.
  const UpdateLangPackTooLong({
    required this.langCode,
  }) : super._();

  /// Deserialize.
  factory UpdateLangPackTooLong.deserialize(Uint8List buffer) {
    // final langCode = _readstring(buffer);
    // final result = UpdateLangPackTooLong(langCode: langCode,);

    // return result;

    throw Exception();
  }

  /// Lang Code.
  final String langCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x46560264);
    buffer.writeString(langCode);
  }
}

/// Update Lang Pack.
///
/// ID: `56022f4d`.
class UpdateLangPack extends UpdateBase {
  /// Update Lang Pack constructor.
  const UpdateLangPack({
    required this.difference,
  }) : super._();

  /// Deserialize.
  factory UpdateLangPack.deserialize(Uint8List buffer) {
    // final difference = _readLangPackDifference(buffer);
    // final result = UpdateLangPack(difference: difference,);

    // return result;

    throw Exception();
  }

  /// Difference.
  final LangPackDifferenceBase difference;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x56022f4d);
    buffer.writeObject(difference);
  }
}

/// Update Faved Stickers.
///
/// ID: `e511996d`.
class UpdateFavedStickers extends UpdateBase {
  /// Update Faved Stickers constructor.
  const UpdateFavedStickers() : super._();

  /// Deserialize.
  factory UpdateFavedStickers.deserialize(Uint8List buffer) {
    // final result = UpdateFavedStickers();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe511996d);
  }
}

/// Update Channel Read Messages Contents.
///
/// ID: `ea29055d`.
class UpdateChannelReadMessagesContents extends UpdateBase {
  /// Update Channel Read Messages Contents constructor.
  const UpdateChannelReadMessagesContents({
    required this.channelId,
    this.topMsgId,
    required this.messages,
  }) : super._();

  /// Deserialize.
  factory UpdateChannelReadMessagesContents.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final channelId = _readlong(buffer);
    // final topMsgId = _readflags.0?int(buffer);
    // final messages = _readVector<int>(buffer);
    // final result = UpdateChannelReadMessagesContents(flags: flags, channelId: channelId, topMsgId: topMsgId, messages: messages,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: topMsgId != null,
    );

    return v;
  }

  /// Channel Id.
  final int channelId;

  /// Top Msg Id.
  final int? topMsgId;

  /// Messages.
  final List<int> messages;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xea29055d);
    buffer.writeInt(flags);
    buffer.writeLong(channelId);
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
    buffer.writeVectorInt(messages);
  }
}

/// Update Contacts Reset.
///
/// ID: `7084a7be`.
class UpdateContactsReset extends UpdateBase {
  /// Update Contacts Reset constructor.
  const UpdateContactsReset() : super._();

  /// Deserialize.
  factory UpdateContactsReset.deserialize(Uint8List buffer) {
    // final result = UpdateContactsReset();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7084a7be);
  }
}

/// Update Channel Available Messages.
///
/// ID: `b23fc698`.
class UpdateChannelAvailableMessages extends UpdateBase {
  /// Update Channel Available Messages constructor.
  const UpdateChannelAvailableMessages({
    required this.channelId,
    required this.availableMinId,
  }) : super._();

  /// Deserialize.
  factory UpdateChannelAvailableMessages.deserialize(Uint8List buffer) {
    // final channelId = _readlong(buffer);
    // final availableMinId = _readint(buffer);
    // final result = UpdateChannelAvailableMessages(channelId: channelId, availableMinId: availableMinId,);

    // return result;

    throw Exception();
  }

  /// Channel Id.
  final int channelId;

  /// Available Min Id.
  final int availableMinId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb23fc698);
    buffer.writeLong(channelId);
    buffer.writeInt(availableMinId);
  }
}

/// Update Dialog Unread Mark.
///
/// ID: `e16459c3`.
class UpdateDialogUnreadMark extends UpdateBase {
  /// Update Dialog Unread Mark constructor.
  const UpdateDialogUnreadMark({
    required this.unread,
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory UpdateDialogUnreadMark.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final unread = _readflags.0?true(buffer);
    // final peer = _readDialogPeer(buffer);
    // final result = UpdateDialogUnreadMark(flags: flags, unread: unread, peer: peer,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: unread,
    );

    return v;
  }

  /// unread: bit 0 of flags.0?true
  final bool unread;

  /// Peer.
  final DialogPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe16459c3);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
  }
}

/// Update Message Poll.
///
/// ID: `aca1657b`.
class UpdateMessagePoll extends UpdateBase {
  /// Update Message Poll constructor.
  const UpdateMessagePoll({
    required this.pollId,
    this.poll,
    required this.results,
  }) : super._();

  /// Deserialize.
  factory UpdateMessagePoll.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pollId = _readlong(buffer);
    // final poll = _readflags.0?Poll(buffer);
    // final results = _readPollResults(buffer);
    // final result = UpdateMessagePoll(flags: flags, pollId: pollId, poll: poll, results: results,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: poll != null,
    );

    return v;
  }

  /// Poll Id.
  final int pollId;

  /// Poll.
  final PollBase? poll;

  /// Results.
  final PollResultsBase results;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xaca1657b);
    buffer.writeInt(flags);
    buffer.writeLong(pollId);
    final localPollCopy = poll;
    if (localPollCopy != null) {
      buffer.writeObject(localPollCopy);
    }
    buffer.writeObject(results);
  }
}

/// Update Chat Default Banned Rights.
///
/// ID: `54c01850`.
class UpdateChatDefaultBannedRights extends UpdateBase {
  /// Update Chat Default Banned Rights constructor.
  const UpdateChatDefaultBannedRights({
    required this.peer,
    required this.defaultBannedRights,
    required this.version,
  }) : super._();

  /// Deserialize.
  factory UpdateChatDefaultBannedRights.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final defaultBannedRights = _readChatBannedRights(buffer);
    // final version = _readint(buffer);
    // final result = UpdateChatDefaultBannedRights(peer: peer, defaultBannedRights: defaultBannedRights, version: version,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Default Banned Rights.
  final ChatBannedRightsBase defaultBannedRights;

  /// Version.
  final int version;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x54c01850);
    buffer.writeObject(peer);
    buffer.writeObject(defaultBannedRights);
    buffer.writeInt(version);
  }
}

/// Update Folder Peers.
///
/// ID: `19360dc0`.
class UpdateFolderPeers extends UpdateBase {
  /// Update Folder Peers constructor.
  const UpdateFolderPeers({
    required this.folderPeers,
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory UpdateFolderPeers.deserialize(Uint8List buffer) {
    // final folderPeers = _readVector<FolderPeer>(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = UpdateFolderPeers(folderPeers: folderPeers, pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Folder Peers.
  final List<FolderPeerBase> folderPeers;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x19360dc0);
    buffer.writeVectorObject(folderPeers);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Update Peer Settings.
///
/// ID: `6a7e7366`.
class UpdatePeerSettings extends UpdateBase {
  /// Update Peer Settings constructor.
  const UpdatePeerSettings({
    required this.peer,
    required this.settings,
  }) : super._();

  /// Deserialize.
  factory UpdatePeerSettings.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final settings = _readPeerSettings(buffer);
    // final result = UpdatePeerSettings(peer: peer, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Settings.
  final PeerSettingsBase settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6a7e7366);
    buffer.writeObject(peer);
    buffer.writeObject(settings);
  }
}

/// Update Peer Located.
///
/// ID: `b4afcfb0`.
class UpdatePeerLocated extends UpdateBase {
  /// Update Peer Located constructor.
  const UpdatePeerLocated({
    required this.peers,
  }) : super._();

  /// Deserialize.
  factory UpdatePeerLocated.deserialize(Uint8List buffer) {
    // final peers = _readVector<PeerLocated>(buffer);
    // final result = UpdatePeerLocated(peers: peers,);

    // return result;

    throw Exception();
  }

  /// Peers.
  final List<PeerLocatedBase> peers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb4afcfb0);
    buffer.writeVectorObject(peers);
  }
}

/// Update New Scheduled Message.
///
/// ID: `39a51dfb`.
class UpdateNewScheduledMessage extends UpdateBase {
  /// Update New Scheduled Message constructor.
  const UpdateNewScheduledMessage({
    required this.message,
  }) : super._();

  /// Deserialize.
  factory UpdateNewScheduledMessage.deserialize(Uint8List buffer) {
    // final message = _readMessage(buffer);
    // final result = UpdateNewScheduledMessage(message: message,);

    // return result;

    throw Exception();
  }

  /// Message.
  final MessageBase message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x39a51dfb);
    buffer.writeObject(message);
  }
}

/// Update Delete Scheduled Messages.
///
/// ID: `90866cee`.
class UpdateDeleteScheduledMessages extends UpdateBase {
  /// Update Delete Scheduled Messages constructor.
  const UpdateDeleteScheduledMessages({
    required this.peer,
    required this.messages,
  }) : super._();

  /// Deserialize.
  factory UpdateDeleteScheduledMessages.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final messages = _readVector<int>(buffer);
    // final result = UpdateDeleteScheduledMessages(peer: peer, messages: messages,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Messages.
  final List<int> messages;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x90866cee);
    buffer.writeObject(peer);
    buffer.writeVectorInt(messages);
  }
}

/// Update Theme.
///
/// ID: `8216fba3`.
class UpdateTheme extends UpdateBase {
  /// Update Theme constructor.
  const UpdateTheme({
    required this.theme,
  }) : super._();

  /// Deserialize.
  factory UpdateTheme.deserialize(Uint8List buffer) {
    // final theme = _readTheme(buffer);
    // final result = UpdateTheme(theme: theme,);

    // return result;

    throw Exception();
  }

  /// Theme.
  final ThemeBase theme;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8216fba3);
    buffer.writeObject(theme);
  }
}

/// Update Geo Live Viewed.
///
/// ID: `871fb939`.
class UpdateGeoLiveViewed extends UpdateBase {
  /// Update Geo Live Viewed constructor.
  const UpdateGeoLiveViewed({
    required this.peer,
    required this.msgId,
  }) : super._();

  /// Deserialize.
  factory UpdateGeoLiveViewed.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final msgId = _readint(buffer);
    // final result = UpdateGeoLiveViewed(peer: peer, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x871fb939);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
  }
}

/// Update Login Token.
///
/// ID: `564fe691`.
class UpdateLoginToken extends UpdateBase {
  /// Update Login Token constructor.
  const UpdateLoginToken() : super._();

  /// Deserialize.
  factory UpdateLoginToken.deserialize(Uint8List buffer) {
    // final result = UpdateLoginToken();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x564fe691);
  }
}

/// Update Message Poll Vote.
///
/// ID: `24f40e77`.
class UpdateMessagePollVote extends UpdateBase {
  /// Update Message Poll Vote constructor.
  const UpdateMessagePollVote({
    required this.pollId,
    required this.peer,
    required this.options,
    required this.qts,
  }) : super._();

  /// Deserialize.
  factory UpdateMessagePollVote.deserialize(Uint8List buffer) {
    // final pollId = _readlong(buffer);
    // final peer = _readPeer(buffer);
    // final options = _readVector<bytes>(buffer);
    // final qts = _readint(buffer);
    // final result = UpdateMessagePollVote(pollId: pollId, peer: peer, options: options, qts: qts,);

    // return result;

    throw Exception();
  }

  /// Poll Id.
  final int pollId;

  /// Peer.
  final PeerBase peer;

  /// Options.
  final List<Uint8List> options;

  /// Qts.
  final int qts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x24f40e77);
    buffer.writeLong(pollId);
    buffer.writeObject(peer);
    buffer.writeVectorBytes(options);
    buffer.writeInt(qts);
  }
}

/// Update Dialog Filter.
///
/// ID: `26ffde7d`.
class UpdateDialogFilter extends UpdateBase {
  /// Update Dialog Filter constructor.
  const UpdateDialogFilter({
    required this.id,
    this.filter,
  }) : super._();

  /// Deserialize.
  factory UpdateDialogFilter.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final id = _readint(buffer);
    // final filter = _readflags.0?DialogFilter(buffer);
    // final result = UpdateDialogFilter(flags: flags, id: id, filter: filter,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: filter != null,
    );

    return v;
  }

  /// Id.
  final int id;

  /// Filter.
  final DialogFilterBase? filter;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x26ffde7d);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    final localFilterCopy = filter;
    if (localFilterCopy != null) {
      buffer.writeObject(localFilterCopy);
    }
  }
}

/// Update Dialog Filter Order.
///
/// ID: `a5d72105`.
class UpdateDialogFilterOrder extends UpdateBase {
  /// Update Dialog Filter Order constructor.
  const UpdateDialogFilterOrder({
    required this.order,
  }) : super._();

  /// Deserialize.
  factory UpdateDialogFilterOrder.deserialize(Uint8List buffer) {
    // final order = _readVector<int>(buffer);
    // final result = UpdateDialogFilterOrder(order: order,);

    // return result;

    throw Exception();
  }

  /// Order.
  final List<int> order;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa5d72105);
    buffer.writeVectorInt(order);
  }
}

/// Update Dialog Filters.
///
/// ID: `3504914f`.
class UpdateDialogFilters extends UpdateBase {
  /// Update Dialog Filters constructor.
  const UpdateDialogFilters() : super._();

  /// Deserialize.
  factory UpdateDialogFilters.deserialize(Uint8List buffer) {
    // final result = UpdateDialogFilters();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3504914f);
  }
}

/// Update Phone Call Signaling Data.
///
/// ID: `2661bf09`.
class UpdatePhoneCallSignalingData extends UpdateBase {
  /// Update Phone Call Signaling Data constructor.
  const UpdatePhoneCallSignalingData({
    required this.phoneCallId,
    required this.data,
  }) : super._();

  /// Deserialize.
  factory UpdatePhoneCallSignalingData.deserialize(Uint8List buffer) {
    // final phoneCallId = _readlong(buffer);
    // final data = _readbytes(buffer);
    // final result = UpdatePhoneCallSignalingData(phoneCallId: phoneCallId, data: data,);

    // return result;

    throw Exception();
  }

  /// Phone Call Id.
  final int phoneCallId;

  /// Data.
  final Uint8List data;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2661bf09);
    buffer.writeLong(phoneCallId);
    buffer.writeBytes(data);
  }
}

/// Update Channel Message Forwards.
///
/// ID: `d29a27f4`.
class UpdateChannelMessageForwards extends UpdateBase {
  /// Update Channel Message Forwards constructor.
  const UpdateChannelMessageForwards({
    required this.channelId,
    required this.id,
    required this.forwards,
  }) : super._();

  /// Deserialize.
  factory UpdateChannelMessageForwards.deserialize(Uint8List buffer) {
    // final channelId = _readlong(buffer);
    // final id = _readint(buffer);
    // final forwards = _readint(buffer);
    // final result = UpdateChannelMessageForwards(channelId: channelId, id: id, forwards: forwards,);

    // return result;

    throw Exception();
  }

  /// Channel Id.
  final int channelId;

  /// Id.
  final int id;

  /// Forwards.
  final int forwards;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd29a27f4);
    buffer.writeLong(channelId);
    buffer.writeInt(id);
    buffer.writeInt(forwards);
  }
}

/// Update Read Channel Discussion Inbox.
///
/// ID: `d6b19546`.
class UpdateReadChannelDiscussionInbox extends UpdateBase {
  /// Update Read Channel Discussion Inbox constructor.
  const UpdateReadChannelDiscussionInbox({
    required this.channelId,
    required this.topMsgId,
    required this.readMaxId,
    this.broadcastId,
    this.broadcastPost,
  }) : super._();

  /// Deserialize.
  factory UpdateReadChannelDiscussionInbox.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final channelId = _readlong(buffer);
    // final topMsgId = _readint(buffer);
    // final readMaxId = _readint(buffer);
    // final broadcastId = _readflags.0?long(buffer);
    // final broadcastPost = _readflags.0?int(buffer);
    // final result = UpdateReadChannelDiscussionInbox(flags: flags, channelId: channelId, topMsgId: topMsgId, readMaxId: readMaxId, broadcastId: broadcastId, broadcastPost: broadcastPost,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: broadcastId != null || broadcastPost != null,
    );

    return v;
  }

  /// Channel Id.
  final int channelId;

  /// Top Msg Id.
  final int topMsgId;

  /// Read Max Id.
  final int readMaxId;

  /// Broadcast Id.
  final int? broadcastId;

  /// Broadcast Post.
  final int? broadcastPost;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd6b19546);
    buffer.writeInt(flags);
    buffer.writeLong(channelId);
    buffer.writeInt(topMsgId);
    buffer.writeInt(readMaxId);
    final localBroadcastIdCopy = broadcastId;
    if (localBroadcastIdCopy != null) {
      buffer.writeLong(localBroadcastIdCopy);
    }
    final localBroadcastPostCopy = broadcastPost;
    if (localBroadcastPostCopy != null) {
      buffer.writeInt(localBroadcastPostCopy);
    }
  }
}

/// Update Read Channel Discussion Outbox.
///
/// ID: `695c9e7c`.
class UpdateReadChannelDiscussionOutbox extends UpdateBase {
  /// Update Read Channel Discussion Outbox constructor.
  const UpdateReadChannelDiscussionOutbox({
    required this.channelId,
    required this.topMsgId,
    required this.readMaxId,
  }) : super._();

  /// Deserialize.
  factory UpdateReadChannelDiscussionOutbox.deserialize(Uint8List buffer) {
    // final channelId = _readlong(buffer);
    // final topMsgId = _readint(buffer);
    // final readMaxId = _readint(buffer);
    // final result = UpdateReadChannelDiscussionOutbox(channelId: channelId, topMsgId: topMsgId, readMaxId: readMaxId,);

    // return result;

    throw Exception();
  }

  /// Channel Id.
  final int channelId;

  /// Top Msg Id.
  final int topMsgId;

  /// Read Max Id.
  final int readMaxId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x695c9e7c);
    buffer.writeLong(channelId);
    buffer.writeInt(topMsgId);
    buffer.writeInt(readMaxId);
  }
}

/// Update Peer Blocked.
///
/// ID: `ebe07752`.
class UpdatePeerBlocked extends UpdateBase {
  /// Update Peer Blocked constructor.
  const UpdatePeerBlocked({
    required this.blocked,
    required this.blockedMyStoriesFrom,
    required this.peerId,
  }) : super._();

  /// Deserialize.
  factory UpdatePeerBlocked.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final blocked = _readflags.0?true(buffer);
    // final blockedMyStoriesFrom = _readflags.1?true(buffer);
    // final peerId = _readPeer(buffer);
    // final result = UpdatePeerBlocked(flags: flags, blocked: blocked, blockedMyStoriesFrom: blockedMyStoriesFrom, peerId: peerId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: blocked,
      b01: blockedMyStoriesFrom,
    );

    return v;
  }

  /// blocked: bit 0 of flags.0?true
  final bool blocked;

  /// blocked_my_stories_from: bit 1 of flags.1?true
  final bool blockedMyStoriesFrom;

  /// Peer Id.
  final PeerBase peerId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xebe07752);
    buffer.writeInt(flags);
    buffer.writeObject(peerId);
  }
}

/// Update Channel User Typing.
///
/// ID: `8c88c923`.
class UpdateChannelUserTyping extends UpdateBase {
  /// Update Channel User Typing constructor.
  const UpdateChannelUserTyping({
    required this.channelId,
    this.topMsgId,
    required this.fromId,
    required this.action,
  }) : super._();

  /// Deserialize.
  factory UpdateChannelUserTyping.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final channelId = _readlong(buffer);
    // final topMsgId = _readflags.0?int(buffer);
    // final fromId = _readPeer(buffer);
    // final action = _readSendMessageAction(buffer);
    // final result = UpdateChannelUserTyping(flags: flags, channelId: channelId, topMsgId: topMsgId, fromId: fromId, action: action,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: topMsgId != null,
    );

    return v;
  }

  /// Channel Id.
  final int channelId;

  /// Top Msg Id.
  final int? topMsgId;

  /// From Id.
  final PeerBase fromId;

  /// Action.
  final SendMessageActionBase action;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8c88c923);
    buffer.writeInt(flags);
    buffer.writeLong(channelId);
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
    buffer.writeObject(fromId);
    buffer.writeObject(action);
  }
}

/// Update Pinned Messages.
///
/// ID: `ed85eab5`.
class UpdatePinnedMessages extends UpdateBase {
  /// Update Pinned Messages constructor.
  const UpdatePinnedMessages({
    required this.pinned,
    required this.peer,
    required this.messages,
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory UpdatePinnedMessages.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pinned = _readflags.0?true(buffer);
    // final peer = _readPeer(buffer);
    // final messages = _readVector<int>(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = UpdatePinnedMessages(flags: flags, pinned: pinned, peer: peer, messages: messages, pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: pinned,
    );

    return v;
  }

  /// pinned: bit 0 of flags.0?true
  final bool pinned;

  /// Peer.
  final PeerBase peer;

  /// Messages.
  final List<int> messages;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xed85eab5);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeVectorInt(messages);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Update Pinned Channel Messages.
///
/// ID: `5bb98608`.
class UpdatePinnedChannelMessages extends UpdateBase {
  /// Update Pinned Channel Messages constructor.
  const UpdatePinnedChannelMessages({
    required this.pinned,
    required this.channelId,
    required this.messages,
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory UpdatePinnedChannelMessages.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pinned = _readflags.0?true(buffer);
    // final channelId = _readlong(buffer);
    // final messages = _readVector<int>(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = UpdatePinnedChannelMessages(flags: flags, pinned: pinned, channelId: channelId, messages: messages, pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: pinned,
    );

    return v;
  }

  /// pinned: bit 0 of flags.0?true
  final bool pinned;

  /// Channel Id.
  final int channelId;

  /// Messages.
  final List<int> messages;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5bb98608);
    buffer.writeInt(flags);
    buffer.writeLong(channelId);
    buffer.writeVectorInt(messages);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Update Chat.
///
/// ID: `f89a6a4e`.
class UpdateChat extends UpdateBase {
  /// Update Chat constructor.
  const UpdateChat({
    required this.chatId,
  }) : super._();

  /// Deserialize.
  factory UpdateChat.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final result = UpdateChat(chatId: chatId,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf89a6a4e);
    buffer.writeLong(chatId);
  }
}

/// Update Group Call Participants.
///
/// ID: `f2ebdb4e`.
class UpdateGroupCallParticipants extends UpdateBase {
  /// Update Group Call Participants constructor.
  const UpdateGroupCallParticipants({
    required this.call,
    required this.participants,
    required this.version,
  }) : super._();

  /// Deserialize.
  factory UpdateGroupCallParticipants.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final participants = _readVector<GroupCallParticipant>(buffer);
    // final version = _readint(buffer);
    // final result = UpdateGroupCallParticipants(call: call, participants: participants, version: version,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Participants.
  final List<GroupCallParticipantBase> participants;

  /// Version.
  final int version;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf2ebdb4e);
    buffer.writeObject(call);
    buffer.writeVectorObject(participants);
    buffer.writeInt(version);
  }
}

/// Update Group Call.
///
/// ID: `14b24500`.
class UpdateGroupCall extends UpdateBase {
  /// Update Group Call constructor.
  const UpdateGroupCall({
    required this.chatId,
    required this.call,
  }) : super._();

  /// Deserialize.
  factory UpdateGroupCall.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final call = _readGroupCall(buffer);
    // final result = UpdateGroupCall(chatId: chatId, call: call,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// Call.
  final GroupCallBase call;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x14b24500);
    buffer.writeLong(chatId);
    buffer.writeObject(call);
  }
}

/// Update Peer History T T L.
///
/// ID: `bb9bb9a5`.
class UpdatePeerHistoryTTL extends UpdateBase {
  /// Update Peer History T T L constructor.
  const UpdatePeerHistoryTTL({
    required this.peer,
    this.ttlPeriod,
  }) : super._();

  /// Deserialize.
  factory UpdatePeerHistoryTTL.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readPeer(buffer);
    // final ttlPeriod = _readflags.0?int(buffer);
    // final result = UpdatePeerHistoryTTL(flags: flags, peer: peer, ttlPeriod: ttlPeriod,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: ttlPeriod != null,
    );

    return v;
  }

  /// Peer.
  final PeerBase peer;

  /// Ttl Period.
  final int? ttlPeriod;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbb9bb9a5);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localTtlPeriodCopy = ttlPeriod;
    if (localTtlPeriodCopy != null) {
      buffer.writeInt(localTtlPeriodCopy);
    }
  }
}

/// Update Chat Participant.
///
/// ID: `d087663a`.
class UpdateChatParticipant extends UpdateBase {
  /// Update Chat Participant constructor.
  const UpdateChatParticipant({
    required this.chatId,
    required this.date,
    required this.actorId,
    required this.userId,
    this.prevParticipant,
    this.newParticipant,
    this.invite,
    required this.qts,
  }) : super._();

  /// Deserialize.
  factory UpdateChatParticipant.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final chatId = _readlong(buffer);
    // final date = _readint(buffer);
    // final actorId = _readlong(buffer);
    // final userId = _readlong(buffer);
    // final prevParticipant = _readflags.0?ChatParticipant(buffer);
    // final newParticipant = _readflags.1?ChatParticipant(buffer);
    // final invite = _readflags.2?ExportedChatInvite(buffer);
    // final qts = _readint(buffer);
    // final result = UpdateChatParticipant(flags: flags, chatId: chatId, date: date, actorId: actorId, userId: userId, prevParticipant: prevParticipant, newParticipant: newParticipant, invite: invite, qts: qts,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: prevParticipant != null,
      b01: newParticipant != null,
      b02: invite != null,
    );

    return v;
  }

  /// Chat Id.
  final int chatId;

  /// Date.
  final DateTime date;

  /// Actor Id.
  final int actorId;

  /// User Id.
  final int userId;

  /// Prev Participant.
  final ChatParticipantBase? prevParticipant;

  /// New Participant.
  final ChatParticipantBase? newParticipant;

  /// Invite.
  final ExportedChatInviteBase? invite;

  /// Qts.
  final int qts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd087663a);
    buffer.writeInt(flags);
    buffer.writeLong(chatId);
    buffer.writeDateTime(date);
    buffer.writeLong(actorId);
    buffer.writeLong(userId);
    final localPrevParticipantCopy = prevParticipant;
    if (localPrevParticipantCopy != null) {
      buffer.writeObject(localPrevParticipantCopy);
    }
    final localNewParticipantCopy = newParticipant;
    if (localNewParticipantCopy != null) {
      buffer.writeObject(localNewParticipantCopy);
    }
    final localInviteCopy = invite;
    if (localInviteCopy != null) {
      buffer.writeObject(localInviteCopy);
    }
    buffer.writeInt(qts);
  }
}

/// Update Channel Participant.
///
/// ID: `985d3abb`.
class UpdateChannelParticipant extends UpdateBase {
  /// Update Channel Participant constructor.
  const UpdateChannelParticipant({
    required this.viaChatlist,
    required this.channelId,
    required this.date,
    required this.actorId,
    required this.userId,
    this.prevParticipant,
    this.newParticipant,
    this.invite,
    required this.qts,
  }) : super._();

  /// Deserialize.
  factory UpdateChannelParticipant.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final viaChatlist = _readflags.3?true(buffer);
    // final channelId = _readlong(buffer);
    // final date = _readint(buffer);
    // final actorId = _readlong(buffer);
    // final userId = _readlong(buffer);
    // final prevParticipant = _readflags.0?ChannelParticipant(buffer);
    // final newParticipant = _readflags.1?ChannelParticipant(buffer);
    // final invite = _readflags.2?ExportedChatInvite(buffer);
    // final qts = _readint(buffer);
    // final result = UpdateChannelParticipant(flags: flags, viaChatlist: viaChatlist, channelId: channelId, date: date, actorId: actorId, userId: userId, prevParticipant: prevParticipant, newParticipant: newParticipant, invite: invite, qts: qts,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: viaChatlist,
      b00: prevParticipant != null,
      b01: newParticipant != null,
      b02: invite != null,
    );

    return v;
  }

  /// via_chatlist: bit 3 of flags.3?true
  final bool viaChatlist;

  /// Channel Id.
  final int channelId;

  /// Date.
  final DateTime date;

  /// Actor Id.
  final int actorId;

  /// User Id.
  final int userId;

  /// Prev Participant.
  final ChannelParticipantBase? prevParticipant;

  /// New Participant.
  final ChannelParticipantBase? newParticipant;

  /// Invite.
  final ExportedChatInviteBase? invite;

  /// Qts.
  final int qts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x985d3abb);
    buffer.writeInt(flags);
    buffer.writeLong(channelId);
    buffer.writeDateTime(date);
    buffer.writeLong(actorId);
    buffer.writeLong(userId);
    final localPrevParticipantCopy = prevParticipant;
    if (localPrevParticipantCopy != null) {
      buffer.writeObject(localPrevParticipantCopy);
    }
    final localNewParticipantCopy = newParticipant;
    if (localNewParticipantCopy != null) {
      buffer.writeObject(localNewParticipantCopy);
    }
    final localInviteCopy = invite;
    if (localInviteCopy != null) {
      buffer.writeObject(localInviteCopy);
    }
    buffer.writeInt(qts);
  }
}

/// Update Bot Stopped.
///
/// ID: `c4870a49`.
class UpdateBotStopped extends UpdateBase {
  /// Update Bot Stopped constructor.
  const UpdateBotStopped({
    required this.userId,
    required this.date,
    required this.stopped,
    required this.qts,
  }) : super._();

  /// Deserialize.
  factory UpdateBotStopped.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final date = _readint(buffer);
    // final stopped = _readBool(buffer);
    // final qts = _readint(buffer);
    // final result = UpdateBotStopped(userId: userId, date: date, stopped: stopped, qts: qts,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Date.
  final DateTime date;

  /// Stopped.
  final bool stopped;

  /// Qts.
  final int qts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc4870a49);
    buffer.writeLong(userId);
    buffer.writeDateTime(date);
    buffer.writeBool(stopped);
    buffer.writeInt(qts);
  }
}

/// Update Group Call Connection.
///
/// ID: `0b783982`.
class UpdateGroupCallConnection extends UpdateBase {
  /// Update Group Call Connection constructor.
  const UpdateGroupCallConnection({
    required this.presentation,
    required this.params,
  }) : super._();

  /// Deserialize.
  factory UpdateGroupCallConnection.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final presentation = _readflags.0?true(buffer);
    // final params = _readDataJSON(buffer);
    // final result = UpdateGroupCallConnection(flags: flags, presentation: presentation, params: params,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: presentation,
    );

    return v;
  }

  /// presentation: bit 0 of flags.0?true
  final bool presentation;

  /// Params.
  final DataJSONBase params;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0b783982);
    buffer.writeInt(flags);
    buffer.writeObject(params);
  }
}

/// Update Bot Commands.
///
/// ID: `4d712f2e`.
class UpdateBotCommands extends UpdateBase {
  /// Update Bot Commands constructor.
  const UpdateBotCommands({
    required this.peer,
    required this.botId,
    required this.commands,
  }) : super._();

  /// Deserialize.
  factory UpdateBotCommands.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final botId = _readlong(buffer);
    // final commands = _readVector<BotCommand>(buffer);
    // final result = UpdateBotCommands(peer: peer, botId: botId, commands: commands,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Bot Id.
  final int botId;

  /// Commands.
  final List<BotCommandBase> commands;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4d712f2e);
    buffer.writeObject(peer);
    buffer.writeLong(botId);
    buffer.writeVectorObject(commands);
  }
}

/// Update Pending Join Requests.
///
/// ID: `7063c3db`.
class UpdatePendingJoinRequests extends UpdateBase {
  /// Update Pending Join Requests constructor.
  const UpdatePendingJoinRequests({
    required this.peer,
    required this.requestsPending,
    required this.recentRequesters,
  }) : super._();

  /// Deserialize.
  factory UpdatePendingJoinRequests.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final requestsPending = _readint(buffer);
    // final recentRequesters = _readVector<long>(buffer);
    // final result = UpdatePendingJoinRequests(peer: peer, requestsPending: requestsPending, recentRequesters: recentRequesters,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Requests Pending.
  final int requestsPending;

  /// Recent Requesters.
  final List<int> recentRequesters;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7063c3db);
    buffer.writeObject(peer);
    buffer.writeInt(requestsPending);
    buffer.writeVectorLong(recentRequesters);
  }
}

/// Update Bot Chat Invite Requester.
///
/// ID: `11dfa986`.
class UpdateBotChatInviteRequester extends UpdateBase {
  /// Update Bot Chat Invite Requester constructor.
  const UpdateBotChatInviteRequester({
    required this.peer,
    required this.date,
    required this.userId,
    required this.about,
    required this.invite,
    required this.qts,
  }) : super._();

  /// Deserialize.
  factory UpdateBotChatInviteRequester.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final date = _readint(buffer);
    // final userId = _readlong(buffer);
    // final about = _readstring(buffer);
    // final invite = _readExportedChatInvite(buffer);
    // final qts = _readint(buffer);
    // final result = UpdateBotChatInviteRequester(peer: peer, date: date, userId: userId, about: about, invite: invite, qts: qts,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Date.
  final DateTime date;

  /// User Id.
  final int userId;

  /// About.
  final String about;

  /// Invite.
  final ExportedChatInviteBase invite;

  /// Qts.
  final int qts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x11dfa986);
    buffer.writeObject(peer);
    buffer.writeDateTime(date);
    buffer.writeLong(userId);
    buffer.writeString(about);
    buffer.writeObject(invite);
    buffer.writeInt(qts);
  }
}

/// Update Message Reactions.
///
/// ID: `5e1b3cb8`.
class UpdateMessageReactions extends UpdateBase {
  /// Update Message Reactions constructor.
  const UpdateMessageReactions({
    required this.peer,
    required this.msgId,
    this.topMsgId,
    required this.reactions,
  }) : super._();

  /// Deserialize.
  factory UpdateMessageReactions.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readPeer(buffer);
    // final msgId = _readint(buffer);
    // final topMsgId = _readflags.0?int(buffer);
    // final reactions = _readMessageReactions(buffer);
    // final result = UpdateMessageReactions(flags: flags, peer: peer, msgId: msgId, topMsgId: topMsgId, reactions: reactions,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: topMsgId != null,
    );

    return v;
  }

  /// Peer.
  final PeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Top Msg Id.
  final int? topMsgId;

  /// Reactions.
  final MessageReactionsBase reactions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5e1b3cb8);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
    buffer.writeObject(reactions);
  }
}

/// Update Attach Menu Bots.
///
/// ID: `17b7a20b`.
class UpdateAttachMenuBots extends UpdateBase {
  /// Update Attach Menu Bots constructor.
  const UpdateAttachMenuBots() : super._();

  /// Deserialize.
  factory UpdateAttachMenuBots.deserialize(Uint8List buffer) {
    // final result = UpdateAttachMenuBots();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x17b7a20b);
  }
}

/// Update Web View Result Sent.
///
/// ID: `1592b79d`.
class UpdateWebViewResultSent extends UpdateBase {
  /// Update Web View Result Sent constructor.
  const UpdateWebViewResultSent({
    required this.queryId,
  }) : super._();

  /// Deserialize.
  factory UpdateWebViewResultSent.deserialize(Uint8List buffer) {
    // final queryId = _readlong(buffer);
    // final result = UpdateWebViewResultSent(queryId: queryId,);

    // return result;

    throw Exception();
  }

  /// Query Id.
  final int queryId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1592b79d);
    buffer.writeLong(queryId);
  }
}

/// Update Bot Menu Button.
///
/// ID: `14b85813`.
class UpdateBotMenuButton extends UpdateBase {
  /// Update Bot Menu Button constructor.
  const UpdateBotMenuButton({
    required this.botId,
    required this.button,
  }) : super._();

  /// Deserialize.
  factory UpdateBotMenuButton.deserialize(Uint8List buffer) {
    // final botId = _readlong(buffer);
    // final button = _readBotMenuButton(buffer);
    // final result = UpdateBotMenuButton(botId: botId, button: button,);

    // return result;

    throw Exception();
  }

  /// Bot Id.
  final int botId;

  /// Button.
  final BotMenuButtonBase button;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x14b85813);
    buffer.writeLong(botId);
    buffer.writeObject(button);
  }
}

/// Update Saved Ringtones.
///
/// ID: `74d8be99`.
class UpdateSavedRingtones extends UpdateBase {
  /// Update Saved Ringtones constructor.
  const UpdateSavedRingtones() : super._();

  /// Deserialize.
  factory UpdateSavedRingtones.deserialize(Uint8List buffer) {
    // final result = UpdateSavedRingtones();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x74d8be99);
  }
}

/// Update Transcribed Audio.
///
/// ID: `0084cd5a`.
class UpdateTranscribedAudio extends UpdateBase {
  /// Update Transcribed Audio constructor.
  const UpdateTranscribedAudio({
    required this.pending,
    required this.peer,
    required this.msgId,
    required this.transcriptionId,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory UpdateTranscribedAudio.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pending = _readflags.0?true(buffer);
    // final peer = _readPeer(buffer);
    // final msgId = _readint(buffer);
    // final transcriptionId = _readlong(buffer);
    // final text = _readstring(buffer);
    // final result = UpdateTranscribedAudio(flags: flags, pending: pending, peer: peer, msgId: msgId, transcriptionId: transcriptionId, text: text,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: pending,
    );

    return v;
  }

  /// pending: bit 0 of flags.0?true
  final bool pending;

  /// Peer.
  final PeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Transcription Id.
  final int transcriptionId;

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0084cd5a);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeLong(transcriptionId);
    buffer.writeString(text);
  }
}

/// Update Read Featured Emoji Stickers.
///
/// ID: `fb4c496c`.
class UpdateReadFeaturedEmojiStickers extends UpdateBase {
  /// Update Read Featured Emoji Stickers constructor.
  const UpdateReadFeaturedEmojiStickers() : super._();

  /// Deserialize.
  factory UpdateReadFeaturedEmojiStickers.deserialize(Uint8List buffer) {
    // final result = UpdateReadFeaturedEmojiStickers();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfb4c496c);
  }
}

/// Update User Emoji Status.
///
/// ID: `28373599`.
class UpdateUserEmojiStatus extends UpdateBase {
  /// Update User Emoji Status constructor.
  const UpdateUserEmojiStatus({
    required this.userId,
    required this.emojiStatus,
  }) : super._();

  /// Deserialize.
  factory UpdateUserEmojiStatus.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final emojiStatus = _readEmojiStatus(buffer);
    // final result = UpdateUserEmojiStatus(userId: userId, emojiStatus: emojiStatus,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Emoji Status.
  final EmojiStatusBase emojiStatus;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x28373599);
    buffer.writeLong(userId);
    buffer.writeObject(emojiStatus);
  }
}

/// Update Recent Emoji Statuses.
///
/// ID: `30f443db`.
class UpdateRecentEmojiStatuses extends UpdateBase {
  /// Update Recent Emoji Statuses constructor.
  const UpdateRecentEmojiStatuses() : super._();

  /// Deserialize.
  factory UpdateRecentEmojiStatuses.deserialize(Uint8List buffer) {
    // final result = UpdateRecentEmojiStatuses();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x30f443db);
  }
}

/// Update Recent Reactions.
///
/// ID: `6f7863f4`.
class UpdateRecentReactions extends UpdateBase {
  /// Update Recent Reactions constructor.
  const UpdateRecentReactions() : super._();

  /// Deserialize.
  factory UpdateRecentReactions.deserialize(Uint8List buffer) {
    // final result = UpdateRecentReactions();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6f7863f4);
  }
}

/// Update Move Sticker Set To Top.
///
/// ID: `86fccf85`.
class UpdateMoveStickerSetToTop extends UpdateBase {
  /// Update Move Sticker Set To Top constructor.
  const UpdateMoveStickerSetToTop({
    required this.masks,
    required this.emojis,
    required this.stickerset,
  }) : super._();

  /// Deserialize.
  factory UpdateMoveStickerSetToTop.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final masks = _readflags.0?true(buffer);
    // final emojis = _readflags.1?true(buffer);
    // final stickerset = _readlong(buffer);
    // final result = UpdateMoveStickerSetToTop(flags: flags, masks: masks, emojis: emojis, stickerset: stickerset,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: masks,
      b01: emojis,
    );

    return v;
  }

  /// masks: bit 0 of flags.0?true
  final bool masks;

  /// emojis: bit 1 of flags.1?true
  final bool emojis;

  /// Stickerset.
  final int stickerset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x86fccf85);
    buffer.writeInt(flags);
    buffer.writeLong(stickerset);
  }
}

/// Update Message Extended Media.
///
/// ID: `5a73a98c`.
class UpdateMessageExtendedMedia extends UpdateBase {
  /// Update Message Extended Media constructor.
  const UpdateMessageExtendedMedia({
    required this.peer,
    required this.msgId,
    required this.extendedMedia,
  }) : super._();

  /// Deserialize.
  factory UpdateMessageExtendedMedia.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final msgId = _readint(buffer);
    // final extendedMedia = _readMessageExtendedMedia(buffer);
    // final result = UpdateMessageExtendedMedia(peer: peer, msgId: msgId, extendedMedia: extendedMedia,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Extended Media.
  final MessageExtendedMediaBase extendedMedia;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5a73a98c);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeObject(extendedMedia);
  }
}

/// Update Channel Pinned Topic.
///
/// ID: `192efbe3`.
class UpdateChannelPinnedTopic extends UpdateBase {
  /// Update Channel Pinned Topic constructor.
  const UpdateChannelPinnedTopic({
    required this.pinned,
    required this.channelId,
    required this.topicId,
  }) : super._();

  /// Deserialize.
  factory UpdateChannelPinnedTopic.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pinned = _readflags.0?true(buffer);
    // final channelId = _readlong(buffer);
    // final topicId = _readint(buffer);
    // final result = UpdateChannelPinnedTopic(flags: flags, pinned: pinned, channelId: channelId, topicId: topicId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: pinned,
    );

    return v;
  }

  /// pinned: bit 0 of flags.0?true
  final bool pinned;

  /// Channel Id.
  final int channelId;

  /// Topic Id.
  final int topicId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x192efbe3);
    buffer.writeInt(flags);
    buffer.writeLong(channelId);
    buffer.writeInt(topicId);
  }
}

/// Update Channel Pinned Topics.
///
/// ID: `fe198602`.
class UpdateChannelPinnedTopics extends UpdateBase {
  /// Update Channel Pinned Topics constructor.
  const UpdateChannelPinnedTopics({
    required this.channelId,
    this.order,
  }) : super._();

  /// Deserialize.
  factory UpdateChannelPinnedTopics.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final channelId = _readlong(buffer);
    // final order = _readflags.0?Vector<int>(buffer);
    // final result = UpdateChannelPinnedTopics(flags: flags, channelId: channelId, order: order,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: order != null,
    );

    return v;
  }

  /// Channel Id.
  final int channelId;

  /// Order.
  final List<int>? order;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfe198602);
    buffer.writeInt(flags);
    buffer.writeLong(channelId);
    final localOrderCopy = order;
    if (localOrderCopy != null) {
      buffer.writeVectorInt(localOrderCopy);
    }
  }
}

/// Update User.
///
/// ID: `20529438`.
class UpdateUser extends UpdateBase {
  /// Update User constructor.
  const UpdateUser({
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory UpdateUser.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final result = UpdateUser(userId: userId,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x20529438);
    buffer.writeLong(userId);
  }
}

/// Update Auto Save Settings.
///
/// ID: `ec05b097`.
class UpdateAutoSaveSettings extends UpdateBase {
  /// Update Auto Save Settings constructor.
  const UpdateAutoSaveSettings() : super._();

  /// Deserialize.
  factory UpdateAutoSaveSettings.deserialize(Uint8List buffer) {
    // final result = UpdateAutoSaveSettings();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xec05b097);
  }
}

/// Update Group Invite Privacy Forbidden.
///
/// ID: `ccf08ad6`.
class UpdateGroupInvitePrivacyForbidden extends UpdateBase {
  /// Update Group Invite Privacy Forbidden constructor.
  const UpdateGroupInvitePrivacyForbidden({
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory UpdateGroupInvitePrivacyForbidden.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final result = UpdateGroupInvitePrivacyForbidden(userId: userId,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xccf08ad6);
    buffer.writeLong(userId);
  }
}

/// Update Story.
///
/// ID: `75b3b798`.
class UpdateStory extends UpdateBase {
  /// Update Story constructor.
  const UpdateStory({
    required this.peer,
    required this.story,
  }) : super._();

  /// Deserialize.
  factory UpdateStory.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final story = _readStoryItem(buffer);
    // final result = UpdateStory(peer: peer, story: story,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Story.
  final StoryItemBase story;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x75b3b798);
    buffer.writeObject(peer);
    buffer.writeObject(story);
  }
}

/// Update Read Stories.
///
/// ID: `f74e932b`.
class UpdateReadStories extends UpdateBase {
  /// Update Read Stories constructor.
  const UpdateReadStories({
    required this.peer,
    required this.maxId,
  }) : super._();

  /// Deserialize.
  factory UpdateReadStories.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final maxId = _readint(buffer);
    // final result = UpdateReadStories(peer: peer, maxId: maxId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Max Id.
  final int maxId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf74e932b);
    buffer.writeObject(peer);
    buffer.writeInt(maxId);
  }
}

/// Update Story I D.
///
/// ID: `1bf335b9`.
class UpdateStoryID extends UpdateBase {
  /// Update Story I D constructor.
  const UpdateStoryID({
    required this.id,
    required this.randomId,
  }) : super._();

  /// Deserialize.
  factory UpdateStoryID.deserialize(Uint8List buffer) {
    // final id = _readint(buffer);
    // final randomId = _readlong(buffer);
    // final result = UpdateStoryID(id: id, randomId: randomId,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Random Id.
  final int randomId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1bf335b9);
    buffer.writeInt(id);
    buffer.writeLong(randomId);
  }
}

/// Update Stories Stealth Mode.
///
/// ID: `2c084dc1`.
class UpdateStoriesStealthMode extends UpdateBase {
  /// Update Stories Stealth Mode constructor.
  const UpdateStoriesStealthMode({
    required this.stealthMode,
  }) : super._();

  /// Deserialize.
  factory UpdateStoriesStealthMode.deserialize(Uint8List buffer) {
    // final stealthMode = _readStoriesStealthMode(buffer);
    // final result = UpdateStoriesStealthMode(stealthMode: stealthMode,);

    // return result;

    throw Exception();
  }

  /// Stealth Mode.
  final StoriesStealthModeBase stealthMode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2c084dc1);
    buffer.writeObject(stealthMode);
  }
}

/// Update Sent Story Reaction.
///
/// ID: `7d627683`.
class UpdateSentStoryReaction extends UpdateBase {
  /// Update Sent Story Reaction constructor.
  const UpdateSentStoryReaction({
    required this.peer,
    required this.storyId,
    required this.reaction,
  }) : super._();

  /// Deserialize.
  factory UpdateSentStoryReaction.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final storyId = _readint(buffer);
    // final reaction = _readReaction(buffer);
    // final result = UpdateSentStoryReaction(peer: peer, storyId: storyId, reaction: reaction,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Story Id.
  final int storyId;

  /// Reaction.
  final ReactionBase reaction;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7d627683);
    buffer.writeObject(peer);
    buffer.writeInt(storyId);
    buffer.writeObject(reaction);
  }
}

/// Update Bot Chat Boost.
///
/// ID: `904dd49c`.
class UpdateBotChatBoost extends UpdateBase {
  /// Update Bot Chat Boost constructor.
  const UpdateBotChatBoost({
    required this.peer,
    required this.boost,
    required this.qts,
  }) : super._();

  /// Deserialize.
  factory UpdateBotChatBoost.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final boost = _readBoost(buffer);
    // final qts = _readint(buffer);
    // final result = UpdateBotChatBoost(peer: peer, boost: boost, qts: qts,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Boost.
  final BoostBase boost;

  /// Qts.
  final int qts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x904dd49c);
    buffer.writeObject(peer);
    buffer.writeObject(boost);
    buffer.writeInt(qts);
  }
}

/// Update Channel View Forum As Messages.
///
/// ID: `07b68920`.
class UpdateChannelViewForumAsMessages extends UpdateBase {
  /// Update Channel View Forum As Messages constructor.
  const UpdateChannelViewForumAsMessages({
    required this.channelId,
    required this.enabled,
  }) : super._();

  /// Deserialize.
  factory UpdateChannelViewForumAsMessages.deserialize(Uint8List buffer) {
    // final channelId = _readlong(buffer);
    // final enabled = _readBool(buffer);
    // final result = UpdateChannelViewForumAsMessages(channelId: channelId, enabled: enabled,);

    // return result;

    throw Exception();
  }

  /// Channel Id.
  final int channelId;

  /// Enabled.
  final bool enabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x07b68920);
    buffer.writeLong(channelId);
    buffer.writeBool(enabled);
  }
}

/// Update Peer Wallpaper.
///
/// ID: `ae3f101d`.
class UpdatePeerWallpaper extends UpdateBase {
  /// Update Peer Wallpaper constructor.
  const UpdatePeerWallpaper({
    required this.wallpaperOverridden,
    required this.peer,
    this.wallpaper,
  }) : super._();

  /// Deserialize.
  factory UpdatePeerWallpaper.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final wallpaperOverridden = _readflags.1?true(buffer);
    // final peer = _readPeer(buffer);
    // final wallpaper = _readflags.0?WallPaper(buffer);
    // final result = UpdatePeerWallpaper(flags: flags, wallpaperOverridden: wallpaperOverridden, peer: peer, wallpaper: wallpaper,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: wallpaperOverridden,
      b00: wallpaper != null,
    );

    return v;
  }

  /// wallpaper_overridden: bit 1 of flags.1?true
  final bool wallpaperOverridden;

  /// Peer.
  final PeerBase peer;

  /// Wallpaper.
  final WallPaperBase? wallpaper;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xae3f101d);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localWallpaperCopy = wallpaper;
    if (localWallpaperCopy != null) {
      buffer.writeObject(localWallpaperCopy);
    }
  }
}

/// Update Bot Message Reaction.
///
/// ID: `ac21d3ce`.
class UpdateBotMessageReaction extends UpdateBase {
  /// Update Bot Message Reaction constructor.
  const UpdateBotMessageReaction({
    required this.peer,
    required this.msgId,
    required this.date,
    required this.actor,
    required this.oldReactions,
    required this.newReactions,
    required this.qts,
  }) : super._();

  /// Deserialize.
  factory UpdateBotMessageReaction.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final msgId = _readint(buffer);
    // final date = _readint(buffer);
    // final actor = _readPeer(buffer);
    // final oldReactions = _readVector<Reaction>(buffer);
    // final newReactions = _readVector<Reaction>(buffer);
    // final qts = _readint(buffer);
    // final result = UpdateBotMessageReaction(peer: peer, msgId: msgId, date: date, actor: actor, oldReactions: oldReactions, newReactions: newReactions, qts: qts,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Date.
  final DateTime date;

  /// Actor.
  final PeerBase actor;

  /// Old Reactions.
  final List<ReactionBase> oldReactions;

  /// New Reactions.
  final List<ReactionBase> newReactions;

  /// Qts.
  final int qts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xac21d3ce);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeDateTime(date);
    buffer.writeObject(actor);
    buffer.writeVectorObject(oldReactions);
    buffer.writeVectorObject(newReactions);
    buffer.writeInt(qts);
  }
}

/// Update Bot Message Reactions.
///
/// ID: `09cb7759`.
class UpdateBotMessageReactions extends UpdateBase {
  /// Update Bot Message Reactions constructor.
  const UpdateBotMessageReactions({
    required this.peer,
    required this.msgId,
    required this.date,
    required this.reactions,
    required this.qts,
  }) : super._();

  /// Deserialize.
  factory UpdateBotMessageReactions.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final msgId = _readint(buffer);
    // final date = _readint(buffer);
    // final reactions = _readVector<ReactionCount>(buffer);
    // final qts = _readint(buffer);
    // final result = UpdateBotMessageReactions(peer: peer, msgId: msgId, date: date, reactions: reactions, qts: qts,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Date.
  final DateTime date;

  /// Reactions.
  final List<ReactionCountBase> reactions;

  /// Qts.
  final int qts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x09cb7759);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeDateTime(date);
    buffer.writeVectorObject(reactions);
    buffer.writeInt(qts);
  }
}

/// Update Saved Dialog Pinned.
///
/// ID: `aeaf9e74`.
class UpdateSavedDialogPinned extends UpdateBase {
  /// Update Saved Dialog Pinned constructor.
  const UpdateSavedDialogPinned({
    required this.pinned,
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory UpdateSavedDialogPinned.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pinned = _readflags.0?true(buffer);
    // final peer = _readDialogPeer(buffer);
    // final result = UpdateSavedDialogPinned(flags: flags, pinned: pinned, peer: peer,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: pinned,
    );

    return v;
  }

  /// pinned: bit 0 of flags.0?true
  final bool pinned;

  /// Peer.
  final DialogPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xaeaf9e74);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
  }
}

/// Update Pinned Saved Dialogs.
///
/// ID: `686c85a6`.
class UpdatePinnedSavedDialogs extends UpdateBase {
  /// Update Pinned Saved Dialogs constructor.
  const UpdatePinnedSavedDialogs({
    this.order,
  }) : super._();

  /// Deserialize.
  factory UpdatePinnedSavedDialogs.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final order = _readflags.0?Vector<DialogPeer>(buffer);
    // final result = UpdatePinnedSavedDialogs(flags: flags, order: order,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: order != null,
    );

    return v;
  }

  /// Order.
  final List<DialogPeerBase>? order;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x686c85a6);
    buffer.writeInt(flags);
    final localOrderCopy = order;
    if (localOrderCopy != null) {
      buffer.writeVectorObject(localOrderCopy);
    }
  }
}

/// Predicate Updates State.
abstract class UpdatesStateBase extends TlConstructor {
  /// Predicate Updates State constructor.
  const UpdatesStateBase._() : super._();
}

/// Updates State.
///
/// ID: `a56c2a3e`.
class UpdatesState extends UpdatesStateBase {
  /// Updates State constructor.
  const UpdatesState({
    required this.pts,
    required this.qts,
    required this.date,
    required this.seq,
    required this.unreadCount,
  }) : super._();

  /// Deserialize.
  factory UpdatesState.deserialize(Uint8List buffer) {
    // final pts = _readint(buffer);
    // final qts = _readint(buffer);
    // final date = _readint(buffer);
    // final seq = _readint(buffer);
    // final unreadCount = _readint(buffer);
    // final result = UpdatesState(pts: pts, qts: qts, date: date, seq: seq, unreadCount: unreadCount,);

    // return result;

    throw Exception();
  }

  /// Pts.
  final int pts;

  /// Qts.
  final int qts;

  /// Date.
  final DateTime date;

  /// Seq.
  final int seq;

  /// Unread Count.
  final int unreadCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa56c2a3e);
    buffer.writeInt(pts);
    buffer.writeInt(qts);
    buffer.writeDateTime(date);
    buffer.writeInt(seq);
    buffer.writeInt(unreadCount);
  }
}

/// Predicate Updates Difference.
abstract class UpdatesDifferenceBase extends TlConstructor {
  /// Predicate Updates Difference constructor.
  const UpdatesDifferenceBase._() : super._();
}

/// Updates Difference Empty.
///
/// ID: `5d75a138`.
class UpdatesDifferenceEmpty extends UpdatesDifferenceBase {
  /// Updates Difference Empty constructor.
  const UpdatesDifferenceEmpty({
    required this.date,
    required this.seq,
  }) : super._();

  /// Deserialize.
  factory UpdatesDifferenceEmpty.deserialize(Uint8List buffer) {
    // final date = _readint(buffer);
    // final seq = _readint(buffer);
    // final result = UpdatesDifferenceEmpty(date: date, seq: seq,);

    // return result;

    throw Exception();
  }

  /// Date.
  final DateTime date;

  /// Seq.
  final int seq;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5d75a138);
    buffer.writeDateTime(date);
    buffer.writeInt(seq);
  }
}

/// Updates Difference.
///
/// ID: `00f49ca0`.
class UpdatesDifference extends UpdatesDifferenceBase {
  /// Updates Difference constructor.
  const UpdatesDifference({
    required this.newMessages,
    required this.newEncryptedMessages,
    required this.otherUpdates,
    required this.chats,
    required this.users,
    required this.state,
  }) : super._();

  /// Deserialize.
  factory UpdatesDifference.deserialize(Uint8List buffer) {
    // final newMessages = _readVector<Message>(buffer);
    // final newEncryptedMessages = _readVector<EncryptedMessage>(buffer);
    // final otherUpdates = _readVector<Update>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final state = _readupdates.State(buffer);
    // final result = UpdatesDifference(newMessages: newMessages, newEncryptedMessages: newEncryptedMessages, otherUpdates: otherUpdates, chats: chats, users: users, state: state,);

    // return result;

    throw Exception();
  }

  /// New Messages.
  final List<MessageBase> newMessages;

  /// New Encrypted Messages.
  final List<EncryptedMessageBase> newEncryptedMessages;

  /// Other Updates.
  final List<UpdateBase> otherUpdates;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// State.
  final UpdatesStateBase state;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x00f49ca0);
    buffer.writeVectorObject(newMessages);
    buffer.writeVectorObject(newEncryptedMessages);
    buffer.writeVectorObject(otherUpdates);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
    buffer.writeObject(state);
  }
}

/// Updates Difference Slice.
///
/// ID: `a8fb1981`.
class UpdatesDifferenceSlice extends UpdatesDifferenceBase {
  /// Updates Difference Slice constructor.
  const UpdatesDifferenceSlice({
    required this.newMessages,
    required this.newEncryptedMessages,
    required this.otherUpdates,
    required this.chats,
    required this.users,
    required this.intermediateState,
  }) : super._();

  /// Deserialize.
  factory UpdatesDifferenceSlice.deserialize(Uint8List buffer) {
    // final newMessages = _readVector<Message>(buffer);
    // final newEncryptedMessages = _readVector<EncryptedMessage>(buffer);
    // final otherUpdates = _readVector<Update>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final intermediateState = _readupdates.State(buffer);
    // final result = UpdatesDifferenceSlice(newMessages: newMessages, newEncryptedMessages: newEncryptedMessages, otherUpdates: otherUpdates, chats: chats, users: users, intermediateState: intermediateState,);

    // return result;

    throw Exception();
  }

  /// New Messages.
  final List<MessageBase> newMessages;

  /// New Encrypted Messages.
  final List<EncryptedMessageBase> newEncryptedMessages;

  /// Other Updates.
  final List<UpdateBase> otherUpdates;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Intermediate State.
  final UpdatesStateBase intermediateState;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa8fb1981);
    buffer.writeVectorObject(newMessages);
    buffer.writeVectorObject(newEncryptedMessages);
    buffer.writeVectorObject(otherUpdates);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
    buffer.writeObject(intermediateState);
  }
}

/// Updates Difference Too Long.
///
/// ID: `4afe8f6d`.
class UpdatesDifferenceTooLong extends UpdatesDifferenceBase {
  /// Updates Difference Too Long constructor.
  const UpdatesDifferenceTooLong({
    required this.pts,
  }) : super._();

  /// Deserialize.
  factory UpdatesDifferenceTooLong.deserialize(Uint8List buffer) {
    // final pts = _readint(buffer);
    // final result = UpdatesDifferenceTooLong(pts: pts,);

    // return result;

    throw Exception();
  }

  /// Pts.
  final int pts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4afe8f6d);
    buffer.writeInt(pts);
  }
}

/// Predicate Updates.
abstract class UpdatesBase extends TlConstructor {
  /// Predicate Updates constructor.
  const UpdatesBase._() : super._();
}

/// Updates Too Long.
///
/// ID: `e317af7e`.
class UpdatesTooLong extends UpdatesBase {
  /// Updates Too Long constructor.
  const UpdatesTooLong() : super._();

  /// Deserialize.
  factory UpdatesTooLong.deserialize(Uint8List buffer) {
    // final result = UpdatesTooLong();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe317af7e);
  }
}

/// Update Short Message.
///
/// ID: `313bc7f8`.
class UpdateShortMessage extends UpdatesBase {
  /// Update Short Message constructor.
  const UpdateShortMessage({
    required this.out,
    required this.mentioned,
    required this.mediaUnread,
    required this.silent,
    required this.id,
    required this.userId,
    required this.message,
    required this.pts,
    required this.ptsCount,
    required this.date,
    this.fwdFrom,
    this.viaBotId,
    this.replyTo,
    this.entities,
    this.ttlPeriod,
  }) : super._();

  /// Deserialize.
  factory UpdateShortMessage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final out = _readflags.1?true(buffer);
    // final mentioned = _readflags.4?true(buffer);
    // final mediaUnread = _readflags.5?true(buffer);
    // final silent = _readflags.13?true(buffer);
    // final id = _readint(buffer);
    // final userId = _readlong(buffer);
    // final message = _readstring(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final date = _readint(buffer);
    // final fwdFrom = _readflags.2?MessageFwdHeader(buffer);
    // final viaBotId = _readflags.11?long(buffer);
    // final replyTo = _readflags.3?MessageReplyHeader(buffer);
    // final entities = _readflags.7?Vector<MessageEntity>(buffer);
    // final ttlPeriod = _readflags.25?int(buffer);
    // final result = UpdateShortMessage(flags: flags, out: out, mentioned: mentioned, mediaUnread: mediaUnread, silent: silent, id: id, userId: userId, message: message, pts: pts, ptsCount: ptsCount, date: date, fwdFrom: fwdFrom, viaBotId: viaBotId, replyTo: replyTo, entities: entities, ttlPeriod: ttlPeriod,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: out,
      b04: mentioned,
      b05: mediaUnread,
      b13: silent,
      b02: fwdFrom != null,
      b11: viaBotId != null,
      b03: replyTo != null,
      b07: entities != null,
      b25: ttlPeriod != null,
    );

    return v;
  }

  /// out: bit 1 of flags.1?true
  final bool out;

  /// mentioned: bit 4 of flags.4?true
  final bool mentioned;

  /// media_unread: bit 5 of flags.5?true
  final bool mediaUnread;

  /// silent: bit 13 of flags.13?true
  final bool silent;

  /// Id.
  final int id;

  /// User Id.
  final int userId;

  /// Message.
  final String message;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Date.
  final DateTime date;

  /// Fwd From.
  final MessageFwdHeaderBase? fwdFrom;

  /// Via Bot Id.
  final int? viaBotId;

  /// Reply To.
  final MessageReplyHeaderBase? replyTo;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Ttl Period.
  final int? ttlPeriod;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x313bc7f8);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    buffer.writeLong(userId);
    buffer.writeString(message);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
    buffer.writeDateTime(date);
    final localFwdFromCopy = fwdFrom;
    if (localFwdFromCopy != null) {
      buffer.writeObject(localFwdFromCopy);
    }
    final localViaBotIdCopy = viaBotId;
    if (localViaBotIdCopy != null) {
      buffer.writeLong(localViaBotIdCopy);
    }
    final localReplyToCopy = replyTo;
    if (localReplyToCopy != null) {
      buffer.writeObject(localReplyToCopy);
    }
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localTtlPeriodCopy = ttlPeriod;
    if (localTtlPeriodCopy != null) {
      buffer.writeInt(localTtlPeriodCopy);
    }
  }
}

/// Update Short Chat Message.
///
/// ID: `4d6deea5`.
class UpdateShortChatMessage extends UpdatesBase {
  /// Update Short Chat Message constructor.
  const UpdateShortChatMessage({
    required this.out,
    required this.mentioned,
    required this.mediaUnread,
    required this.silent,
    required this.id,
    required this.fromId,
    required this.chatId,
    required this.message,
    required this.pts,
    required this.ptsCount,
    required this.date,
    this.fwdFrom,
    this.viaBotId,
    this.replyTo,
    this.entities,
    this.ttlPeriod,
  }) : super._();

  /// Deserialize.
  factory UpdateShortChatMessage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final out = _readflags.1?true(buffer);
    // final mentioned = _readflags.4?true(buffer);
    // final mediaUnread = _readflags.5?true(buffer);
    // final silent = _readflags.13?true(buffer);
    // final id = _readint(buffer);
    // final fromId = _readlong(buffer);
    // final chatId = _readlong(buffer);
    // final message = _readstring(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final date = _readint(buffer);
    // final fwdFrom = _readflags.2?MessageFwdHeader(buffer);
    // final viaBotId = _readflags.11?long(buffer);
    // final replyTo = _readflags.3?MessageReplyHeader(buffer);
    // final entities = _readflags.7?Vector<MessageEntity>(buffer);
    // final ttlPeriod = _readflags.25?int(buffer);
    // final result = UpdateShortChatMessage(flags: flags, out: out, mentioned: mentioned, mediaUnread: mediaUnread, silent: silent, id: id, fromId: fromId, chatId: chatId, message: message, pts: pts, ptsCount: ptsCount, date: date, fwdFrom: fwdFrom, viaBotId: viaBotId, replyTo: replyTo, entities: entities, ttlPeriod: ttlPeriod,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: out,
      b04: mentioned,
      b05: mediaUnread,
      b13: silent,
      b02: fwdFrom != null,
      b11: viaBotId != null,
      b03: replyTo != null,
      b07: entities != null,
      b25: ttlPeriod != null,
    );

    return v;
  }

  /// out: bit 1 of flags.1?true
  final bool out;

  /// mentioned: bit 4 of flags.4?true
  final bool mentioned;

  /// media_unread: bit 5 of flags.5?true
  final bool mediaUnread;

  /// silent: bit 13 of flags.13?true
  final bool silent;

  /// Id.
  final int id;

  /// From Id.
  final int fromId;

  /// Chat Id.
  final int chatId;

  /// Message.
  final String message;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Date.
  final DateTime date;

  /// Fwd From.
  final MessageFwdHeaderBase? fwdFrom;

  /// Via Bot Id.
  final int? viaBotId;

  /// Reply To.
  final MessageReplyHeaderBase? replyTo;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Ttl Period.
  final int? ttlPeriod;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4d6deea5);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    buffer.writeLong(fromId);
    buffer.writeLong(chatId);
    buffer.writeString(message);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
    buffer.writeDateTime(date);
    final localFwdFromCopy = fwdFrom;
    if (localFwdFromCopy != null) {
      buffer.writeObject(localFwdFromCopy);
    }
    final localViaBotIdCopy = viaBotId;
    if (localViaBotIdCopy != null) {
      buffer.writeLong(localViaBotIdCopy);
    }
    final localReplyToCopy = replyTo;
    if (localReplyToCopy != null) {
      buffer.writeObject(localReplyToCopy);
    }
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localTtlPeriodCopy = ttlPeriod;
    if (localTtlPeriodCopy != null) {
      buffer.writeInt(localTtlPeriodCopy);
    }
  }
}

/// Update Short.
///
/// ID: `78d4dec1`.
class UpdateShort extends UpdatesBase {
  /// Update Short constructor.
  const UpdateShort({
    required this.update,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory UpdateShort.deserialize(Uint8List buffer) {
    // final update = _readUpdate(buffer);
    // final date = _readint(buffer);
    // final result = UpdateShort(update: update, date: date,);

    // return result;

    throw Exception();
  }

  /// Update.
  final UpdateBase update;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x78d4dec1);
    buffer.writeObject(update);
    buffer.writeDateTime(date);
  }
}

/// Updates Combined.
///
/// ID: `725b04c3`.
class UpdatesCombined extends UpdatesBase {
  /// Updates Combined constructor.
  const UpdatesCombined({
    required this.updates,
    required this.users,
    required this.chats,
    required this.date,
    required this.seqStart,
    required this.seq,
  }) : super._();

  /// Deserialize.
  factory UpdatesCombined.deserialize(Uint8List buffer) {
    // final updates = _readVector<Update>(buffer);
    // final users = _readVector<User>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final date = _readint(buffer);
    // final seqStart = _readint(buffer);
    // final seq = _readint(buffer);
    // final result = UpdatesCombined(updates: updates, users: users, chats: chats, date: date, seqStart: seqStart, seq: seq,);

    // return result;

    throw Exception();
  }

  /// Updates.
  final List<UpdateBase> updates;

  /// Users.
  final List<UserBase> users;

  /// Chats.
  final List<ChatBase> chats;

  /// Date.
  final DateTime date;

  /// Seq Start.
  final int seqStart;

  /// Seq.
  final int seq;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x725b04c3);
    buffer.writeVectorObject(updates);
    buffer.writeVectorObject(users);
    buffer.writeVectorObject(chats);
    buffer.writeDateTime(date);
    buffer.writeInt(seqStart);
    buffer.writeInt(seq);
  }
}

/// Updates.
///
/// ID: `74ae4240`.
class Updates extends UpdatesBase {
  /// Updates constructor.
  const Updates({
    required this.updates,
    required this.users,
    required this.chats,
    required this.date,
    required this.seq,
  }) : super._();

  /// Deserialize.
  factory Updates.deserialize(Uint8List buffer) {
    // final updates = _readVector<Update>(buffer);
    // final users = _readVector<User>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final date = _readint(buffer);
    // final seq = _readint(buffer);
    // final result = Updates(updates: updates, users: users, chats: chats, date: date, seq: seq,);

    // return result;

    throw Exception();
  }

  /// Updates.
  final List<UpdateBase> updates;

  /// Users.
  final List<UserBase> users;

  /// Chats.
  final List<ChatBase> chats;

  /// Date.
  final DateTime date;

  /// Seq.
  final int seq;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x74ae4240);
    buffer.writeVectorObject(updates);
    buffer.writeVectorObject(users);
    buffer.writeVectorObject(chats);
    buffer.writeDateTime(date);
    buffer.writeInt(seq);
  }
}

/// Update Short Sent Message.
///
/// ID: `9015e101`.
class UpdateShortSentMessage extends UpdatesBase {
  /// Update Short Sent Message constructor.
  const UpdateShortSentMessage({
    required this.out,
    required this.id,
    required this.pts,
    required this.ptsCount,
    required this.date,
    this.media,
    this.entities,
    this.ttlPeriod,
  }) : super._();

  /// Deserialize.
  factory UpdateShortSentMessage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final out = _readflags.1?true(buffer);
    // final id = _readint(buffer);
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final date = _readint(buffer);
    // final media = _readflags.9?MessageMedia(buffer);
    // final entities = _readflags.7?Vector<MessageEntity>(buffer);
    // final ttlPeriod = _readflags.25?int(buffer);
    // final result = UpdateShortSentMessage(flags: flags, out: out, id: id, pts: pts, ptsCount: ptsCount, date: date, media: media, entities: entities, ttlPeriod: ttlPeriod,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: out,
      b09: media != null,
      b07: entities != null,
      b25: ttlPeriod != null,
    );

    return v;
  }

  /// out: bit 1 of flags.1?true
  final bool out;

  /// Id.
  final int id;

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Date.
  final DateTime date;

  /// Media.
  final MessageMediaBase? media;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Ttl Period.
  final int? ttlPeriod;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9015e101);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
    buffer.writeDateTime(date);
    final localMediaCopy = media;
    if (localMediaCopy != null) {
      buffer.writeObject(localMediaCopy);
    }
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localTtlPeriodCopy = ttlPeriod;
    if (localTtlPeriodCopy != null) {
      buffer.writeInt(localTtlPeriodCopy);
    }
  }
}

/// Predicate Photos Photos.
abstract class PhotosPhotosBase extends TlConstructor {
  /// Predicate Photos Photos constructor.
  const PhotosPhotosBase._() : super._();
}

/// Photos Photos.
///
/// ID: `8dca6aa5`.
class PhotosPhotos extends PhotosPhotosBase {
  /// Photos Photos constructor.
  const PhotosPhotos({
    required this.photos,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PhotosPhotos.deserialize(Uint8List buffer) {
    // final photos = _readVector<Photo>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = PhotosPhotos(photos: photos, users: users,);

    // return result;

    throw Exception();
  }

  /// Photos.
  final List<PhotoBase> photos;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8dca6aa5);
    buffer.writeVectorObject(photos);
    buffer.writeVectorObject(users);
  }
}

/// Photos Photos Slice.
///
/// ID: `15051f54`.
class PhotosPhotosSlice extends PhotosPhotosBase {
  /// Photos Photos Slice constructor.
  const PhotosPhotosSlice({
    required this.count,
    required this.photos,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PhotosPhotosSlice.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final photos = _readVector<Photo>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = PhotosPhotosSlice(count: count, photos: photos, users: users,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Photos.
  final List<PhotoBase> photos;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x15051f54);
    buffer.writeInt(count);
    buffer.writeVectorObject(photos);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Photos Photo.
abstract class PhotosPhotoBase extends TlConstructor {
  /// Predicate Photos Photo constructor.
  const PhotosPhotoBase._() : super._();
}

/// Photos Photo.
///
/// ID: `20212ca8`.
class PhotosPhoto extends PhotosPhotoBase {
  /// Photos Photo constructor.
  const PhotosPhoto({
    required this.photo,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PhotosPhoto.deserialize(Uint8List buffer) {
    // final photo = _readPhoto(buffer);
    // final users = _readVector<User>(buffer);
    // final result = PhotosPhoto(photo: photo, users: users,);

    // return result;

    throw Exception();
  }

  /// Photo.
  final PhotoBase photo;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x20212ca8);
    buffer.writeObject(photo);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Upload File.
abstract class UploadFileBase extends TlConstructor {
  /// Predicate Upload File constructor.
  const UploadFileBase._() : super._();
}

/// Upload File.
///
/// ID: `096a18d5`.
class UploadFile extends UploadFileBase {
  /// Upload File constructor.
  const UploadFile({
    required this.type,
    required this.mtime,
    required this.bytes,
  }) : super._();

  /// Deserialize.
  factory UploadFile.deserialize(Uint8List buffer) {
    // final type = _readstorage.FileType(buffer);
    // final mtime = _readint(buffer);
    // final bytes = _readbytes(buffer);
    // final result = UploadFile(type: type, mtime: mtime, bytes: bytes,);

    // return result;

    throw Exception();
  }

  /// Type.
  final StorageFileTypeBase type;

  /// Mtime.
  final int mtime;

  /// Bytes.
  final Uint8List bytes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x096a18d5);
    buffer.writeObject(type);
    buffer.writeInt(mtime);
    buffer.writeBytes(bytes);
  }
}

/// Upload File Cdn Redirect.
///
/// ID: `f18cda44`.
class UploadFileCdnRedirect extends UploadFileBase {
  /// Upload File Cdn Redirect constructor.
  const UploadFileCdnRedirect({
    required this.dcId,
    required this.fileToken,
    required this.encryptionKey,
    required this.encryptionIv,
    required this.fileHashes,
  }) : super._();

  /// Deserialize.
  factory UploadFileCdnRedirect.deserialize(Uint8List buffer) {
    // final dcId = _readint(buffer);
    // final fileToken = _readbytes(buffer);
    // final encryptionKey = _readbytes(buffer);
    // final encryptionIv = _readbytes(buffer);
    // final fileHashes = _readVector<FileHash>(buffer);
    // final result = UploadFileCdnRedirect(dcId: dcId, fileToken: fileToken, encryptionKey: encryptionKey, encryptionIv: encryptionIv, fileHashes: fileHashes,);

    // return result;

    throw Exception();
  }

  /// Dc Id.
  final int dcId;

  /// File Token.
  final Uint8List fileToken;

  /// Encryption Key.
  final Uint8List encryptionKey;

  /// Encryption Iv.
  final Uint8List encryptionIv;

  /// File Hashes.
  final List<FileHashBase> fileHashes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf18cda44);
    buffer.writeInt(dcId);
    buffer.writeBytes(fileToken);
    buffer.writeBytes(encryptionKey);
    buffer.writeBytes(encryptionIv);
    buffer.writeVectorObject(fileHashes);
  }
}

/// Predicate Dc Option.
abstract class DcOptionBase extends TlConstructor {
  /// Predicate Dc Option constructor.
  const DcOptionBase._() : super._();
}

/// Dc Option.
///
/// ID: `18b7a10d`.
class DcOption extends DcOptionBase {
  /// Dc Option constructor.
  const DcOption({
    required this.ipv6,
    required this.mediaOnly,
    required this.tcpoOnly,
    required this.cdn,
    required this.static,
    required this.thisPortOnly,
    required this.id,
    required this.ipAddress,
    required this.port,
    this.secret,
  }) : super._();

  /// Deserialize.
  factory DcOption.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final ipv6 = _readflags.0?true(buffer);
    // final mediaOnly = _readflags.1?true(buffer);
    // final tcpoOnly = _readflags.2?true(buffer);
    // final cdn = _readflags.3?true(buffer);
    // final static = _readflags.4?true(buffer);
    // final thisPortOnly = _readflags.5?true(buffer);
    // final id = _readint(buffer);
    // final ipAddress = _readstring(buffer);
    // final port = _readint(buffer);
    // final secret = _readflags.10?bytes(buffer);
    // final result = DcOption(flags: flags, ipv6: ipv6, mediaOnly: mediaOnly, tcpoOnly: tcpoOnly, cdn: cdn, static: static, thisPortOnly: thisPortOnly, id: id, ipAddress: ipAddress, port: port, secret: secret,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: ipv6,
      b01: mediaOnly,
      b02: tcpoOnly,
      b03: cdn,
      b04: static,
      b05: thisPortOnly,
      b10: secret != null,
    );

    return v;
  }

  /// ipv6: bit 0 of flags.0?true
  final bool ipv6;

  /// media_only: bit 1 of flags.1?true
  final bool mediaOnly;

  /// tcpo_only: bit 2 of flags.2?true
  final bool tcpoOnly;

  /// cdn: bit 3 of flags.3?true
  final bool cdn;

  /// static: bit 4 of flags.4?true
  final bool static;

  /// this_port_only: bit 5 of flags.5?true
  final bool thisPortOnly;

  /// Id.
  final int id;

  /// Ip Address.
  final String ipAddress;

  /// Port.
  final int port;

  /// Secret.
  final Uint8List? secret;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x18b7a10d);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    buffer.writeString(ipAddress);
    buffer.writeInt(port);
    final localSecretCopy = secret;
    if (localSecretCopy != null) {
      buffer.writeBytes(localSecretCopy);
    }
  }
}

/// Predicate Config.
abstract class ConfigBase extends TlConstructor {
  /// Predicate Config constructor.
  const ConfigBase._() : super._();
}

/// Config.
///
/// ID: `cc1a241e`.
class Config extends ConfigBase {
  /// Config constructor.
  const Config({
    required this.defaultP2pContacts,
    required this.preloadFeaturedStickers,
    required this.revokePmInbox,
    required this.blockedMode,
    required this.forceTryIpv6,
    required this.date,
    required this.expires,
    required this.testMode,
    required this.thisDc,
    required this.dcOptions,
    required this.dcTxtDomainName,
    required this.chatSizeMax,
    required this.megagroupSizeMax,
    required this.forwardedCountMax,
    required this.onlineUpdatePeriodMs,
    required this.offlineBlurTimeoutMs,
    required this.offlineIdleTimeoutMs,
    required this.onlineCloudTimeoutMs,
    required this.notifyCloudDelayMs,
    required this.notifyDefaultDelayMs,
    required this.pushChatPeriodMs,
    required this.pushChatLimit,
    required this.editTimeLimit,
    required this.revokeTimeLimit,
    required this.revokePmTimeLimit,
    required this.ratingEDecay,
    required this.stickersRecentLimit,
    required this.channelsReadMediaPeriod,
    this.tmpSessions,
    required this.callReceiveTimeoutMs,
    required this.callRingTimeoutMs,
    required this.callConnectTimeoutMs,
    required this.callPacketTimeoutMs,
    required this.meUrlPrefix,
    this.autoupdateUrlPrefix,
    this.gifSearchUsername,
    this.venueSearchUsername,
    this.imgSearchUsername,
    this.staticMapsProvider,
    required this.captionLengthMax,
    required this.messageLengthMax,
    required this.webfileDcId,
    this.suggestedLangCode,
    this.langPackVersion,
    this.baseLangPackVersion,
    this.reactionsDefault,
    this.autologinToken,
  }) : super._();

  /// Deserialize.
  factory Config.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final defaultP2pContacts = _readflags.3?true(buffer);
    // final preloadFeaturedStickers = _readflags.4?true(buffer);
    // final revokePmInbox = _readflags.6?true(buffer);
    // final blockedMode = _readflags.8?true(buffer);
    // final forceTryIpv6 = _readflags.14?true(buffer);
    // final date = _readint(buffer);
    // final expires = _readint(buffer);
    // final testMode = _readBool(buffer);
    // final thisDc = _readint(buffer);
    // final dcOptions = _readVector<DcOption>(buffer);
    // final dcTxtDomainName = _readstring(buffer);
    // final chatSizeMax = _readint(buffer);
    // final megagroupSizeMax = _readint(buffer);
    // final forwardedCountMax = _readint(buffer);
    // final onlineUpdatePeriodMs = _readint(buffer);
    // final offlineBlurTimeoutMs = _readint(buffer);
    // final offlineIdleTimeoutMs = _readint(buffer);
    // final onlineCloudTimeoutMs = _readint(buffer);
    // final notifyCloudDelayMs = _readint(buffer);
    // final notifyDefaultDelayMs = _readint(buffer);
    // final pushChatPeriodMs = _readint(buffer);
    // final pushChatLimit = _readint(buffer);
    // final editTimeLimit = _readint(buffer);
    // final revokeTimeLimit = _readint(buffer);
    // final revokePmTimeLimit = _readint(buffer);
    // final ratingEDecay = _readint(buffer);
    // final stickersRecentLimit = _readint(buffer);
    // final channelsReadMediaPeriod = _readint(buffer);
    // final tmpSessions = _readflags.0?int(buffer);
    // final callReceiveTimeoutMs = _readint(buffer);
    // final callRingTimeoutMs = _readint(buffer);
    // final callConnectTimeoutMs = _readint(buffer);
    // final callPacketTimeoutMs = _readint(buffer);
    // final meUrlPrefix = _readstring(buffer);
    // final autoupdateUrlPrefix = _readflags.7?string(buffer);
    // final gifSearchUsername = _readflags.9?string(buffer);
    // final venueSearchUsername = _readflags.10?string(buffer);
    // final imgSearchUsername = _readflags.11?string(buffer);
    // final staticMapsProvider = _readflags.12?string(buffer);
    // final captionLengthMax = _readint(buffer);
    // final messageLengthMax = _readint(buffer);
    // final webfileDcId = _readint(buffer);
    // final suggestedLangCode = _readflags.2?string(buffer);
    // final langPackVersion = _readflags.2?int(buffer);
    // final baseLangPackVersion = _readflags.2?int(buffer);
    // final reactionsDefault = _readflags.15?Reaction(buffer);
    // final autologinToken = _readflags.16?string(buffer);
    // final result = Config(flags: flags, defaultP2pContacts: defaultP2pContacts, preloadFeaturedStickers: preloadFeaturedStickers, revokePmInbox: revokePmInbox, blockedMode: blockedMode, forceTryIpv6: forceTryIpv6, date: date, expires: expires, testMode: testMode, thisDc: thisDc, dcOptions: dcOptions, dcTxtDomainName: dcTxtDomainName, chatSizeMax: chatSizeMax, megagroupSizeMax: megagroupSizeMax, forwardedCountMax: forwardedCountMax, onlineUpdatePeriodMs: onlineUpdatePeriodMs, offlineBlurTimeoutMs: offlineBlurTimeoutMs, offlineIdleTimeoutMs: offlineIdleTimeoutMs, onlineCloudTimeoutMs: onlineCloudTimeoutMs, notifyCloudDelayMs: notifyCloudDelayMs, notifyDefaultDelayMs: notifyDefaultDelayMs, pushChatPeriodMs: pushChatPeriodMs, pushChatLimit: pushChatLimit, editTimeLimit: editTimeLimit, revokeTimeLimit: revokeTimeLimit, revokePmTimeLimit: revokePmTimeLimit, ratingEDecay: ratingEDecay, stickersRecentLimit: stickersRecentLimit, channelsReadMediaPeriod: channelsReadMediaPeriod, tmpSessions: tmpSessions, callReceiveTimeoutMs: callReceiveTimeoutMs, callRingTimeoutMs: callRingTimeoutMs, callConnectTimeoutMs: callConnectTimeoutMs, callPacketTimeoutMs: callPacketTimeoutMs, meUrlPrefix: meUrlPrefix, autoupdateUrlPrefix: autoupdateUrlPrefix, gifSearchUsername: gifSearchUsername, venueSearchUsername: venueSearchUsername, imgSearchUsername: imgSearchUsername, staticMapsProvider: staticMapsProvider, captionLengthMax: captionLengthMax, messageLengthMax: messageLengthMax, webfileDcId: webfileDcId, suggestedLangCode: suggestedLangCode, langPackVersion: langPackVersion, baseLangPackVersion: baseLangPackVersion, reactionsDefault: reactionsDefault, autologinToken: autologinToken,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: defaultP2pContacts,
      b04: preloadFeaturedStickers,
      b06: revokePmInbox,
      b08: blockedMode,
      b14: forceTryIpv6,
      b00: tmpSessions != null,
      b07: autoupdateUrlPrefix != null,
      b09: gifSearchUsername != null,
      b10: venueSearchUsername != null,
      b11: imgSearchUsername != null,
      b12: staticMapsProvider != null,
      b02: suggestedLangCode != null ||
          langPackVersion != null ||
          baseLangPackVersion != null,
      b15: reactionsDefault != null,
      b16: autologinToken != null,
    );

    return v;
  }

  /// default_p2p_contacts: bit 3 of flags.3?true
  final bool defaultP2pContacts;

  /// preload_featured_stickers: bit 4 of flags.4?true
  final bool preloadFeaturedStickers;

  /// revoke_pm_inbox: bit 6 of flags.6?true
  final bool revokePmInbox;

  /// blocked_mode: bit 8 of flags.8?true
  final bool blockedMode;

  /// force_try_ipv6: bit 14 of flags.14?true
  final bool forceTryIpv6;

  /// Date.
  final DateTime date;

  /// Expires.
  final int expires;

  /// Test Mode.
  final bool testMode;

  /// This Dc.
  final int thisDc;

  /// Dc Options.
  final List<DcOptionBase> dcOptions;

  /// Dc Txt Domain Name.
  final String dcTxtDomainName;

  /// Chat Size Max.
  final int chatSizeMax;

  /// Megagroup Size Max.
  final int megagroupSizeMax;

  /// Forwarded Count Max.
  final int forwardedCountMax;

  /// Online Update Period Ms.
  final DateTime onlineUpdatePeriodMs;

  /// Offline Blur Timeout Ms.
  final int offlineBlurTimeoutMs;

  /// Offline Idle Timeout Ms.
  final int offlineIdleTimeoutMs;

  /// Online Cloud Timeout Ms.
  final int onlineCloudTimeoutMs;

  /// Notify Cloud Delay Ms.
  final int notifyCloudDelayMs;

  /// Notify Default Delay Ms.
  final int notifyDefaultDelayMs;

  /// Push Chat Period Ms.
  final int pushChatPeriodMs;

  /// Push Chat Limit.
  final int pushChatLimit;

  /// Edit Time Limit.
  final int editTimeLimit;

  /// Revoke Time Limit.
  final int revokeTimeLimit;

  /// Revoke Pm Time Limit.
  final int revokePmTimeLimit;

  /// Rating E Decay.
  final int ratingEDecay;

  /// Stickers Recent Limit.
  final int stickersRecentLimit;

  /// Channels Read Media Period.
  final int channelsReadMediaPeriod;

  /// Tmp Sessions.
  final int? tmpSessions;

  /// Call Receive Timeout Ms.
  final int callReceiveTimeoutMs;

  /// Call Ring Timeout Ms.
  final int callRingTimeoutMs;

  /// Call Connect Timeout Ms.
  final int callConnectTimeoutMs;

  /// Call Packet Timeout Ms.
  final int callPacketTimeoutMs;

  /// Me Url Prefix.
  final String meUrlPrefix;

  /// Autoupdate Url Prefix.
  final String? autoupdateUrlPrefix;

  /// Gif Search Username.
  final String? gifSearchUsername;

  /// Venue Search Username.
  final String? venueSearchUsername;

  /// Img Search Username.
  final String? imgSearchUsername;

  /// Static Maps Provider.
  final String? staticMapsProvider;

  /// Caption Length Max.
  final int captionLengthMax;

  /// Message Length Max.
  final int messageLengthMax;

  /// Webfile Dc Id.
  final int webfileDcId;

  /// Suggested Lang Code.
  final String? suggestedLangCode;

  /// Lang Pack Version.
  final int? langPackVersion;

  /// Base Lang Pack Version.
  final int? baseLangPackVersion;

  /// Reactions Default.
  final ReactionBase? reactionsDefault;

  /// Autologin Token.
  final String? autologinToken;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcc1a241e);
    buffer.writeInt(flags);
    buffer.writeDateTime(date);
    buffer.writeInt(expires);
    buffer.writeBool(testMode);
    buffer.writeInt(thisDc);
    buffer.writeVectorObject(dcOptions);
    buffer.writeString(dcTxtDomainName);
    buffer.writeInt(chatSizeMax);
    buffer.writeInt(megagroupSizeMax);
    buffer.writeInt(forwardedCountMax);
    buffer.writeDateTime(onlineUpdatePeriodMs);
    buffer.writeInt(offlineBlurTimeoutMs);
    buffer.writeInt(offlineIdleTimeoutMs);
    buffer.writeInt(onlineCloudTimeoutMs);
    buffer.writeInt(notifyCloudDelayMs);
    buffer.writeInt(notifyDefaultDelayMs);
    buffer.writeInt(pushChatPeriodMs);
    buffer.writeInt(pushChatLimit);
    buffer.writeInt(editTimeLimit);
    buffer.writeInt(revokeTimeLimit);
    buffer.writeInt(revokePmTimeLimit);
    buffer.writeInt(ratingEDecay);
    buffer.writeInt(stickersRecentLimit);
    buffer.writeInt(channelsReadMediaPeriod);
    final localTmpSessionsCopy = tmpSessions;
    if (localTmpSessionsCopy != null) {
      buffer.writeInt(localTmpSessionsCopy);
    }
    buffer.writeInt(callReceiveTimeoutMs);
    buffer.writeInt(callRingTimeoutMs);
    buffer.writeInt(callConnectTimeoutMs);
    buffer.writeInt(callPacketTimeoutMs);
    buffer.writeString(meUrlPrefix);
    final localAutoupdateUrlPrefixCopy = autoupdateUrlPrefix;
    if (localAutoupdateUrlPrefixCopy != null) {
      buffer.writeString(localAutoupdateUrlPrefixCopy);
    }
    final localGifSearchUsernameCopy = gifSearchUsername;
    if (localGifSearchUsernameCopy != null) {
      buffer.writeString(localGifSearchUsernameCopy);
    }
    final localVenueSearchUsernameCopy = venueSearchUsername;
    if (localVenueSearchUsernameCopy != null) {
      buffer.writeString(localVenueSearchUsernameCopy);
    }
    final localImgSearchUsernameCopy = imgSearchUsername;
    if (localImgSearchUsernameCopy != null) {
      buffer.writeString(localImgSearchUsernameCopy);
    }
    final localStaticMapsProviderCopy = staticMapsProvider;
    if (localStaticMapsProviderCopy != null) {
      buffer.writeString(localStaticMapsProviderCopy);
    }
    buffer.writeInt(captionLengthMax);
    buffer.writeInt(messageLengthMax);
    buffer.writeInt(webfileDcId);
    final localSuggestedLangCodeCopy = suggestedLangCode;
    if (localSuggestedLangCodeCopy != null) {
      buffer.writeString(localSuggestedLangCodeCopy);
    }
    final localLangPackVersionCopy = langPackVersion;
    if (localLangPackVersionCopy != null) {
      buffer.writeInt(localLangPackVersionCopy);
    }
    final localBaseLangPackVersionCopy = baseLangPackVersion;
    if (localBaseLangPackVersionCopy != null) {
      buffer.writeInt(localBaseLangPackVersionCopy);
    }
    final localReactionsDefaultCopy = reactionsDefault;
    if (localReactionsDefaultCopy != null) {
      buffer.writeObject(localReactionsDefaultCopy);
    }
    final localAutologinTokenCopy = autologinToken;
    if (localAutologinTokenCopy != null) {
      buffer.writeString(localAutologinTokenCopy);
    }
  }
}

/// Predicate Nearest Dc.
abstract class NearestDcBase extends TlConstructor {
  /// Predicate Nearest Dc constructor.
  const NearestDcBase._() : super._();
}

/// Nearest Dc.
///
/// ID: `8e1a1775`.
class NearestDc extends NearestDcBase {
  /// Nearest Dc constructor.
  const NearestDc({
    required this.country,
    required this.thisDc,
    required this.nearestDc,
  }) : super._();

  /// Deserialize.
  factory NearestDc.deserialize(Uint8List buffer) {
    // final country = _readstring(buffer);
    // final thisDc = _readint(buffer);
    // final nearestDc = _readint(buffer);
    // final result = NearestDc(country: country, thisDc: thisDc, nearestDc: nearestDc,);

    // return result;

    throw Exception();
  }

  /// Country.
  final String country;

  /// This Dc.
  final int thisDc;

  /// Nearest Dc.
  final int nearestDc;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8e1a1775);
    buffer.writeString(country);
    buffer.writeInt(thisDc);
    buffer.writeInt(nearestDc);
  }
}

/// Predicate Help App Update.
abstract class HelpAppUpdateBase extends TlConstructor {
  /// Predicate Help App Update constructor.
  const HelpAppUpdateBase._() : super._();
}

/// Help App Update.
///
/// ID: `ccbbce30`.
class HelpAppUpdate extends HelpAppUpdateBase {
  /// Help App Update constructor.
  const HelpAppUpdate({
    required this.canNotSkip,
    required this.id,
    required this.version,
    required this.text,
    required this.entities,
    this.document,
    this.url,
    this.sticker,
  }) : super._();

  /// Deserialize.
  factory HelpAppUpdate.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final canNotSkip = _readflags.0?true(buffer);
    // final id = _readint(buffer);
    // final version = _readstring(buffer);
    // final text = _readstring(buffer);
    // final entities = _readVector<MessageEntity>(buffer);
    // final document = _readflags.1?Document(buffer);
    // final url = _readflags.2?string(buffer);
    // final sticker = _readflags.3?Document(buffer);
    // final result = HelpAppUpdate(flags: flags, canNotSkip: canNotSkip, id: id, version: version, text: text, entities: entities, document: document, url: url, sticker: sticker,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: canNotSkip,
      b01: document != null,
      b02: url != null,
      b03: sticker != null,
    );

    return v;
  }

  /// can_not_skip: bit 0 of flags.0?true
  final bool canNotSkip;

  /// Id.
  final int id;

  /// Version.
  final String version;

  /// Text.
  final String text;

  /// Entities.
  final List<MessageEntityBase> entities;

  /// Document.
  final DocumentBase? document;

  /// Url.
  final String? url;

  /// Sticker.
  final DocumentBase? sticker;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xccbbce30);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    buffer.writeString(version);
    buffer.writeString(text);
    buffer.writeVectorObject(entities);
    final localDocumentCopy = document;
    if (localDocumentCopy != null) {
      buffer.writeObject(localDocumentCopy);
    }
    final localUrlCopy = url;
    if (localUrlCopy != null) {
      buffer.writeString(localUrlCopy);
    }
    final localStickerCopy = sticker;
    if (localStickerCopy != null) {
      buffer.writeObject(localStickerCopy);
    }
  }
}

/// Help No App Update.
///
/// ID: `c45a6536`.
class HelpNoAppUpdate extends HelpAppUpdateBase {
  /// Help No App Update constructor.
  const HelpNoAppUpdate() : super._();

  /// Deserialize.
  factory HelpNoAppUpdate.deserialize(Uint8List buffer) {
    // final result = HelpNoAppUpdate();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc45a6536);
  }
}

/// Predicate Help Invite Text.
abstract class HelpInviteTextBase extends TlConstructor {
  /// Predicate Help Invite Text constructor.
  const HelpInviteTextBase._() : super._();
}

/// Help Invite Text.
///
/// ID: `18cb9f78`.
class HelpInviteText extends HelpInviteTextBase {
  /// Help Invite Text constructor.
  const HelpInviteText({
    required this.message,
  }) : super._();

  /// Deserialize.
  factory HelpInviteText.deserialize(Uint8List buffer) {
    // final message = _readstring(buffer);
    // final result = HelpInviteText(message: message,);

    // return result;

    throw Exception();
  }

  /// Message.
  final String message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x18cb9f78);
    buffer.writeString(message);
  }
}

/// Predicate Encrypted Chat.
abstract class EncryptedChatBase extends TlConstructor {
  /// Predicate Encrypted Chat constructor.
  const EncryptedChatBase._() : super._();
}

/// Encrypted Chat Empty.
///
/// ID: `ab7ec0a0`.
class EncryptedChatEmpty extends EncryptedChatBase {
  /// Encrypted Chat Empty constructor.
  const EncryptedChatEmpty({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory EncryptedChatEmpty.deserialize(Uint8List buffer) {
    // final id = _readint(buffer);
    // final result = EncryptedChatEmpty(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xab7ec0a0);
    buffer.writeInt(id);
  }
}

/// Encrypted Chat Waiting.
///
/// ID: `66b25953`.
class EncryptedChatWaiting extends EncryptedChatBase {
  /// Encrypted Chat Waiting constructor.
  const EncryptedChatWaiting({
    required this.id,
    required this.accessHash,
    required this.date,
    required this.adminId,
    required this.participantId,
  }) : super._();

  /// Deserialize.
  factory EncryptedChatWaiting.deserialize(Uint8List buffer) {
    // final id = _readint(buffer);
    // final accessHash = _readlong(buffer);
    // final date = _readint(buffer);
    // final adminId = _readlong(buffer);
    // final participantId = _readlong(buffer);
    // final result = EncryptedChatWaiting(id: id, accessHash: accessHash, date: date, adminId: adminId, participantId: participantId,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Date.
  final DateTime date;

  /// Admin Id.
  final int adminId;

  /// Participant Id.
  final int participantId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x66b25953);
    buffer.writeInt(id);
    buffer.writeLong(accessHash);
    buffer.writeDateTime(date);
    buffer.writeLong(adminId);
    buffer.writeLong(participantId);
  }
}

/// Encrypted Chat Requested.
///
/// ID: `48f1d94c`.
class EncryptedChatRequested extends EncryptedChatBase {
  /// Encrypted Chat Requested constructor.
  const EncryptedChatRequested({
    this.folderId,
    required this.id,
    required this.accessHash,
    required this.date,
    required this.adminId,
    required this.participantId,
    required this.gA,
  }) : super._();

  /// Deserialize.
  factory EncryptedChatRequested.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final folderId = _readflags.0?int(buffer);
    // final id = _readint(buffer);
    // final accessHash = _readlong(buffer);
    // final date = _readint(buffer);
    // final adminId = _readlong(buffer);
    // final participantId = _readlong(buffer);
    // final gA = _readbytes(buffer);
    // final result = EncryptedChatRequested(flags: flags, folderId: folderId, id: id, accessHash: accessHash, date: date, adminId: adminId, participantId: participantId, gA: gA,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: folderId != null,
    );

    return v;
  }

  /// Folder Id.
  final int? folderId;

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Date.
  final DateTime date;

  /// Admin Id.
  final int adminId;

  /// Participant Id.
  final int participantId;

  /// G A.
  final Uint8List gA;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x48f1d94c);
    buffer.writeInt(flags);
    final localFolderIdCopy = folderId;
    if (localFolderIdCopy != null) {
      buffer.writeInt(localFolderIdCopy);
    }
    buffer.writeInt(id);
    buffer.writeLong(accessHash);
    buffer.writeDateTime(date);
    buffer.writeLong(adminId);
    buffer.writeLong(participantId);
    buffer.writeBytes(gA);
  }
}

/// Encrypted Chat.
///
/// ID: `61f0d4c7`.
class EncryptedChat extends EncryptedChatBase {
  /// Encrypted Chat constructor.
  const EncryptedChat({
    required this.id,
    required this.accessHash,
    required this.date,
    required this.adminId,
    required this.participantId,
    required this.gAOrB,
    required this.keyFingerprint,
  }) : super._();

  /// Deserialize.
  factory EncryptedChat.deserialize(Uint8List buffer) {
    // final id = _readint(buffer);
    // final accessHash = _readlong(buffer);
    // final date = _readint(buffer);
    // final adminId = _readlong(buffer);
    // final participantId = _readlong(buffer);
    // final gAOrB = _readbytes(buffer);
    // final keyFingerprint = _readlong(buffer);
    // final result = EncryptedChat(id: id, accessHash: accessHash, date: date, adminId: adminId, participantId: participantId, gAOrB: gAOrB, keyFingerprint: keyFingerprint,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Date.
  final DateTime date;

  /// Admin Id.
  final int adminId;

  /// Participant Id.
  final int participantId;

  /// G A Or B.
  final Uint8List gAOrB;

  /// Key Fingerprint.
  final int keyFingerprint;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x61f0d4c7);
    buffer.writeInt(id);
    buffer.writeLong(accessHash);
    buffer.writeDateTime(date);
    buffer.writeLong(adminId);
    buffer.writeLong(participantId);
    buffer.writeBytes(gAOrB);
    buffer.writeLong(keyFingerprint);
  }
}

/// Encrypted Chat Discarded.
///
/// ID: `1e1c7c45`.
class EncryptedChatDiscarded extends EncryptedChatBase {
  /// Encrypted Chat Discarded constructor.
  const EncryptedChatDiscarded({
    required this.historyDeleted,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory EncryptedChatDiscarded.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final historyDeleted = _readflags.0?true(buffer);
    // final id = _readint(buffer);
    // final result = EncryptedChatDiscarded(flags: flags, historyDeleted: historyDeleted, id: id,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: historyDeleted,
    );

    return v;
  }

  /// history_deleted: bit 0 of flags.0?true
  final bool historyDeleted;

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1e1c7c45);
    buffer.writeInt(flags);
    buffer.writeInt(id);
  }
}

/// Predicate Input Encrypted Chat.
abstract class InputEncryptedChatBase extends TlConstructor {
  /// Predicate Input Encrypted Chat constructor.
  const InputEncryptedChatBase._() : super._();
}

/// Input Encrypted Chat.
///
/// ID: `f141b5e1`.
class InputEncryptedChat extends InputEncryptedChatBase {
  /// Input Encrypted Chat constructor.
  const InputEncryptedChat({
    required this.chatId,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputEncryptedChat.deserialize(Uint8List buffer) {
    // final chatId = _readint(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputEncryptedChat(chatId: chatId, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf141b5e1);
    buffer.writeInt(chatId);
    buffer.writeLong(accessHash);
  }
}

/// Predicate Encrypted File.
abstract class EncryptedFileBase extends TlConstructor {
  /// Predicate Encrypted File constructor.
  const EncryptedFileBase._() : super._();
}

/// Encrypted File Empty.
///
/// ID: `c21f497e`.
class EncryptedFileEmpty extends EncryptedFileBase {
  /// Encrypted File Empty constructor.
  const EncryptedFileEmpty() : super._();

  /// Deserialize.
  factory EncryptedFileEmpty.deserialize(Uint8List buffer) {
    // final result = EncryptedFileEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc21f497e);
  }
}

/// Encrypted File.
///
/// ID: `a8008cd8`.
class EncryptedFile extends EncryptedFileBase {
  /// Encrypted File constructor.
  const EncryptedFile({
    required this.id,
    required this.accessHash,
    required this.size,
    required this.dcId,
    required this.keyFingerprint,
  }) : super._();

  /// Deserialize.
  factory EncryptedFile.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final size = _readlong(buffer);
    // final dcId = _readint(buffer);
    // final keyFingerprint = _readint(buffer);
    // final result = EncryptedFile(id: id, accessHash: accessHash, size: size, dcId: dcId, keyFingerprint: keyFingerprint,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Size.
  final int size;

  /// Dc Id.
  final int dcId;

  /// Key Fingerprint.
  final int keyFingerprint;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa8008cd8);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeLong(size);
    buffer.writeInt(dcId);
    buffer.writeInt(keyFingerprint);
  }
}

/// Predicate Input Encrypted File.
abstract class InputEncryptedFileBase extends TlConstructor {
  /// Predicate Input Encrypted File constructor.
  const InputEncryptedFileBase._() : super._();
}

/// Input Encrypted File Empty.
///
/// ID: `1837c364`.
class InputEncryptedFileEmpty extends InputEncryptedFileBase {
  /// Input Encrypted File Empty constructor.
  const InputEncryptedFileEmpty() : super._();

  /// Deserialize.
  factory InputEncryptedFileEmpty.deserialize(Uint8List buffer) {
    // final result = InputEncryptedFileEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1837c364);
  }
}

/// Input Encrypted File Uploaded.
///
/// ID: `64bd0306`.
class InputEncryptedFileUploaded extends InputEncryptedFileBase {
  /// Input Encrypted File Uploaded constructor.
  const InputEncryptedFileUploaded({
    required this.id,
    required this.parts,
    required this.md5Checksum,
    required this.keyFingerprint,
  }) : super._();

  /// Deserialize.
  factory InputEncryptedFileUploaded.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final parts = _readint(buffer);
    // final md5Checksum = _readstring(buffer);
    // final keyFingerprint = _readint(buffer);
    // final result = InputEncryptedFileUploaded(id: id, parts: parts, md5Checksum: md5Checksum, keyFingerprint: keyFingerprint,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Parts.
  final int parts;

  /// Md5 Checksum.
  final String md5Checksum;

  /// Key Fingerprint.
  final int keyFingerprint;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x64bd0306);
    buffer.writeLong(id);
    buffer.writeInt(parts);
    buffer.writeString(md5Checksum);
    buffer.writeInt(keyFingerprint);
  }
}

/// Input Encrypted File.
///
/// ID: `5a17b5e5`.
class InputEncryptedFile extends InputEncryptedFileBase {
  /// Input Encrypted File constructor.
  const InputEncryptedFile({
    required this.id,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputEncryptedFile.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputEncryptedFile(id: id, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5a17b5e5);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
  }
}

/// Input Encrypted File Big Uploaded.
///
/// ID: `2dc173c8`.
class InputEncryptedFileBigUploaded extends InputEncryptedFileBase {
  /// Input Encrypted File Big Uploaded constructor.
  const InputEncryptedFileBigUploaded({
    required this.id,
    required this.parts,
    required this.keyFingerprint,
  }) : super._();

  /// Deserialize.
  factory InputEncryptedFileBigUploaded.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final parts = _readint(buffer);
    // final keyFingerprint = _readint(buffer);
    // final result = InputEncryptedFileBigUploaded(id: id, parts: parts, keyFingerprint: keyFingerprint,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Parts.
  final int parts;

  /// Key Fingerprint.
  final int keyFingerprint;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2dc173c8);
    buffer.writeLong(id);
    buffer.writeInt(parts);
    buffer.writeInt(keyFingerprint);
  }
}

/// Predicate Encrypted Message.
abstract class EncryptedMessageBase extends TlConstructor {
  /// Predicate Encrypted Message constructor.
  const EncryptedMessageBase._() : super._();
}

/// Encrypted Message.
///
/// ID: `ed18c118`.
class EncryptedMessage extends EncryptedMessageBase {
  /// Encrypted Message constructor.
  const EncryptedMessage({
    required this.randomId,
    required this.chatId,
    required this.date,
    required this.bytes,
    required this.file,
  }) : super._();

  /// Deserialize.
  factory EncryptedMessage.deserialize(Uint8List buffer) {
    // final randomId = _readlong(buffer);
    // final chatId = _readint(buffer);
    // final date = _readint(buffer);
    // final bytes = _readbytes(buffer);
    // final file = _readEncryptedFile(buffer);
    // final result = EncryptedMessage(randomId: randomId, chatId: chatId, date: date, bytes: bytes, file: file,);

    // return result;

    throw Exception();
  }

  /// Random Id.
  final int randomId;

  /// Chat Id.
  final int chatId;

  /// Date.
  final DateTime date;

  /// Bytes.
  final Uint8List bytes;

  /// File.
  final EncryptedFileBase file;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xed18c118);
    buffer.writeLong(randomId);
    buffer.writeInt(chatId);
    buffer.writeDateTime(date);
    buffer.writeBytes(bytes);
    buffer.writeObject(file);
  }
}

/// Encrypted Message Service.
///
/// ID: `23734b06`.
class EncryptedMessageService extends EncryptedMessageBase {
  /// Encrypted Message Service constructor.
  const EncryptedMessageService({
    required this.randomId,
    required this.chatId,
    required this.date,
    required this.bytes,
  }) : super._();

  /// Deserialize.
  factory EncryptedMessageService.deserialize(Uint8List buffer) {
    // final randomId = _readlong(buffer);
    // final chatId = _readint(buffer);
    // final date = _readint(buffer);
    // final bytes = _readbytes(buffer);
    // final result = EncryptedMessageService(randomId: randomId, chatId: chatId, date: date, bytes: bytes,);

    // return result;

    throw Exception();
  }

  /// Random Id.
  final int randomId;

  /// Chat Id.
  final int chatId;

  /// Date.
  final DateTime date;

  /// Bytes.
  final Uint8List bytes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x23734b06);
    buffer.writeLong(randomId);
    buffer.writeInt(chatId);
    buffer.writeDateTime(date);
    buffer.writeBytes(bytes);
  }
}

/// Predicate Messages Dh Config.
abstract class MessagesDhConfigBase extends TlConstructor {
  /// Predicate Messages Dh Config constructor.
  const MessagesDhConfigBase._() : super._();
}

/// Messages Dh Config Not Modified.
///
/// ID: `c0e24635`.
class MessagesDhConfigNotModified extends MessagesDhConfigBase {
  /// Messages Dh Config Not Modified constructor.
  const MessagesDhConfigNotModified({
    required this.random,
  }) : super._();

  /// Deserialize.
  factory MessagesDhConfigNotModified.deserialize(Uint8List buffer) {
    // final random = _readbytes(buffer);
    // final result = MessagesDhConfigNotModified(random: random,);

    // return result;

    throw Exception();
  }

  /// Random.
  final Uint8List random;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc0e24635);
    buffer.writeBytes(random);
  }
}

/// Messages Dh Config.
///
/// ID: `2c221edd`.
class MessagesDhConfig extends MessagesDhConfigBase {
  /// Messages Dh Config constructor.
  const MessagesDhConfig({
    required this.g,
    required this.p,
    required this.version,
    required this.random,
  }) : super._();

  /// Deserialize.
  factory MessagesDhConfig.deserialize(Uint8List buffer) {
    // final g = _readint(buffer);
    // final p = _readbytes(buffer);
    // final version = _readint(buffer);
    // final random = _readbytes(buffer);
    // final result = MessagesDhConfig(g: g, p: p, version: version, random: random,);

    // return result;

    throw Exception();
  }

  /// G.
  final int g;

  /// P.
  final Uint8List p;

  /// Version.
  final int version;

  /// Random.
  final Uint8List random;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2c221edd);
    buffer.writeInt(g);
    buffer.writeBytes(p);
    buffer.writeInt(version);
    buffer.writeBytes(random);
  }
}

/// Predicate Messages Sent Encrypted Message.
abstract class MessagesSentEncryptedMessageBase extends TlConstructor {
  /// Predicate Messages Sent Encrypted Message constructor.
  const MessagesSentEncryptedMessageBase._() : super._();
}

/// Messages Sent Encrypted Message.
///
/// ID: `560f8935`.
class MessagesSentEncryptedMessage extends MessagesSentEncryptedMessageBase {
  /// Messages Sent Encrypted Message constructor.
  const MessagesSentEncryptedMessage({
    required this.date,
  }) : super._();

  /// Deserialize.
  factory MessagesSentEncryptedMessage.deserialize(Uint8List buffer) {
    // final date = _readint(buffer);
    // final result = MessagesSentEncryptedMessage(date: date,);

    // return result;

    throw Exception();
  }

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x560f8935);
    buffer.writeDateTime(date);
  }
}

/// Messages Sent Encrypted File.
///
/// ID: `9493ff32`.
class MessagesSentEncryptedFile extends MessagesSentEncryptedMessageBase {
  /// Messages Sent Encrypted File constructor.
  const MessagesSentEncryptedFile({
    required this.date,
    required this.file,
  }) : super._();

  /// Deserialize.
  factory MessagesSentEncryptedFile.deserialize(Uint8List buffer) {
    // final date = _readint(buffer);
    // final file = _readEncryptedFile(buffer);
    // final result = MessagesSentEncryptedFile(date: date, file: file,);

    // return result;

    throw Exception();
  }

  /// Date.
  final DateTime date;

  /// File.
  final EncryptedFileBase file;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9493ff32);
    buffer.writeDateTime(date);
    buffer.writeObject(file);
  }
}

/// Predicate Input Document.
abstract class InputDocumentBase extends TlConstructor {
  /// Predicate Input Document constructor.
  const InputDocumentBase._() : super._();
}

/// Input Document Empty.
///
/// ID: `72f0eaae`.
class InputDocumentEmpty extends InputDocumentBase {
  /// Input Document Empty constructor.
  const InputDocumentEmpty() : super._();

  /// Deserialize.
  factory InputDocumentEmpty.deserialize(Uint8List buffer) {
    // final result = InputDocumentEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x72f0eaae);
  }
}

/// Input Document.
///
/// ID: `1abfb575`.
class InputDocument extends InputDocumentBase {
  /// Input Document constructor.
  const InputDocument({
    required this.id,
    required this.accessHash,
    required this.fileReference,
  }) : super._();

  /// Deserialize.
  factory InputDocument.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final fileReference = _readbytes(buffer);
    // final result = InputDocument(id: id, accessHash: accessHash, fileReference: fileReference,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// File Reference.
  final Uint8List fileReference;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1abfb575);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeBytes(fileReference);
  }
}

/// Predicate Document.
abstract class DocumentBase extends TlConstructor {
  /// Predicate Document constructor.
  const DocumentBase._() : super._();
}

/// Document Empty.
///
/// ID: `36f8c871`.
class DocumentEmpty extends DocumentBase {
  /// Document Empty constructor.
  const DocumentEmpty({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory DocumentEmpty.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final result = DocumentEmpty(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x36f8c871);
    buffer.writeLong(id);
  }
}

/// Document.
///
/// ID: `8fd4c4d8`.
class Document extends DocumentBase {
  /// Document constructor.
  const Document({
    required this.id,
    required this.accessHash,
    required this.fileReference,
    required this.date,
    required this.mimeType,
    required this.size,
    this.thumbs,
    this.videoThumbs,
    required this.dcId,
    required this.attributes,
  }) : super._();

  /// Deserialize.
  factory Document.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final fileReference = _readbytes(buffer);
    // final date = _readint(buffer);
    // final mimeType = _readstring(buffer);
    // final size = _readlong(buffer);
    // final thumbs = _readflags.0?Vector<PhotoSize>(buffer);
    // final videoThumbs = _readflags.1?Vector<VideoSize>(buffer);
    // final dcId = _readint(buffer);
    // final attributes = _readVector<DocumentAttribute>(buffer);
    // final result = Document(flags: flags, id: id, accessHash: accessHash, fileReference: fileReference, date: date, mimeType: mimeType, size: size, thumbs: thumbs, videoThumbs: videoThumbs, dcId: dcId, attributes: attributes,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: thumbs != null,
      b01: videoThumbs != null,
    );

    return v;
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// File Reference.
  final Uint8List fileReference;

  /// Date.
  final DateTime date;

  /// Mime Type.
  final String mimeType;

  /// Size.
  final int size;

  /// Thumbs.
  final List<PhotoSizeBase>? thumbs;

  /// Video Thumbs.
  final List<VideoSizeBase>? videoThumbs;

  /// Dc Id.
  final int dcId;

  /// Attributes.
  final List<DocumentAttributeBase> attributes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8fd4c4d8);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeBytes(fileReference);
    buffer.writeDateTime(date);
    buffer.writeString(mimeType);
    buffer.writeLong(size);
    final localThumbsCopy = thumbs;
    if (localThumbsCopy != null) {
      buffer.writeVectorObject(localThumbsCopy);
    }
    final localVideoThumbsCopy = videoThumbs;
    if (localVideoThumbsCopy != null) {
      buffer.writeVectorObject(localVideoThumbsCopy);
    }
    buffer.writeInt(dcId);
    buffer.writeVectorObject(attributes);
  }
}

/// Predicate Help Support.
abstract class HelpSupportBase extends TlConstructor {
  /// Predicate Help Support constructor.
  const HelpSupportBase._() : super._();
}

/// Help Support.
///
/// ID: `17c6b5f6`.
class HelpSupport extends HelpSupportBase {
  /// Help Support constructor.
  const HelpSupport({
    required this.phoneNumber,
    required this.user,
  }) : super._();

  /// Deserialize.
  factory HelpSupport.deserialize(Uint8List buffer) {
    // final phoneNumber = _readstring(buffer);
    // final user = _readUser(buffer);
    // final result = HelpSupport(phoneNumber: phoneNumber, user: user,);

    // return result;

    throw Exception();
  }

  /// Phone Number.
  final String phoneNumber;

  /// User.
  final UserBase user;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x17c6b5f6);
    buffer.writeString(phoneNumber);
    buffer.writeObject(user);
  }
}

/// Predicate Notify Peer.
abstract class NotifyPeerBase extends TlConstructor {
  /// Predicate Notify Peer constructor.
  const NotifyPeerBase._() : super._();
}

/// Notify Peer.
///
/// ID: `9fd40bd8`.
class NotifyPeer extends NotifyPeerBase {
  /// Notify Peer constructor.
  const NotifyPeer({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory NotifyPeer.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final result = NotifyPeer(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9fd40bd8);
    buffer.writeObject(peer);
  }
}

/// Notify Users.
///
/// ID: `b4c83b4c`.
class NotifyUsers extends NotifyPeerBase {
  /// Notify Users constructor.
  const NotifyUsers() : super._();

  /// Deserialize.
  factory NotifyUsers.deserialize(Uint8List buffer) {
    // final result = NotifyUsers();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb4c83b4c);
  }
}

/// Notify Chats.
///
/// ID: `c007cec3`.
class NotifyChats extends NotifyPeerBase {
  /// Notify Chats constructor.
  const NotifyChats() : super._();

  /// Deserialize.
  factory NotifyChats.deserialize(Uint8List buffer) {
    // final result = NotifyChats();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc007cec3);
  }
}

/// Notify Broadcasts.
///
/// ID: `d612e8ef`.
class NotifyBroadcasts extends NotifyPeerBase {
  /// Notify Broadcasts constructor.
  const NotifyBroadcasts() : super._();

  /// Deserialize.
  factory NotifyBroadcasts.deserialize(Uint8List buffer) {
    // final result = NotifyBroadcasts();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd612e8ef);
  }
}

/// Notify Forum Topic.
///
/// ID: `226e6308`.
class NotifyForumTopic extends NotifyPeerBase {
  /// Notify Forum Topic constructor.
  const NotifyForumTopic({
    required this.peer,
    required this.topMsgId,
  }) : super._();

  /// Deserialize.
  factory NotifyForumTopic.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final topMsgId = _readint(buffer);
    // final result = NotifyForumTopic(peer: peer, topMsgId: topMsgId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Top Msg Id.
  final int topMsgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x226e6308);
    buffer.writeObject(peer);
    buffer.writeInt(topMsgId);
  }
}

/// Predicate Send Message Action.
abstract class SendMessageActionBase extends TlConstructor {
  /// Predicate Send Message Action constructor.
  const SendMessageActionBase._() : super._();
}

/// Send Message Typing Action.
///
/// ID: `16bf744e`.
class SendMessageTypingAction extends SendMessageActionBase {
  /// Send Message Typing Action constructor.
  const SendMessageTypingAction() : super._();

  /// Deserialize.
  factory SendMessageTypingAction.deserialize(Uint8List buffer) {
    // final result = SendMessageTypingAction();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x16bf744e);
  }
}

/// Send Message Cancel Action.
///
/// ID: `fd5ec8f5`.
class SendMessageCancelAction extends SendMessageActionBase {
  /// Send Message Cancel Action constructor.
  const SendMessageCancelAction() : super._();

  /// Deserialize.
  factory SendMessageCancelAction.deserialize(Uint8List buffer) {
    // final result = SendMessageCancelAction();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfd5ec8f5);
  }
}

/// Send Message Record Video Action.
///
/// ID: `a187d66f`.
class SendMessageRecordVideoAction extends SendMessageActionBase {
  /// Send Message Record Video Action constructor.
  const SendMessageRecordVideoAction() : super._();

  /// Deserialize.
  factory SendMessageRecordVideoAction.deserialize(Uint8List buffer) {
    // final result = SendMessageRecordVideoAction();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa187d66f);
  }
}

/// Send Message Upload Video Action.
///
/// ID: `e9763aec`.
class SendMessageUploadVideoAction extends SendMessageActionBase {
  /// Send Message Upload Video Action constructor.
  const SendMessageUploadVideoAction({
    required this.progress,
  }) : super._();

  /// Deserialize.
  factory SendMessageUploadVideoAction.deserialize(Uint8List buffer) {
    // final progress = _readint(buffer);
    // final result = SendMessageUploadVideoAction(progress: progress,);

    // return result;

    throw Exception();
  }

  /// Progress.
  final int progress;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe9763aec);
    buffer.writeInt(progress);
  }
}

/// Send Message Record Audio Action.
///
/// ID: `d52f73f7`.
class SendMessageRecordAudioAction extends SendMessageActionBase {
  /// Send Message Record Audio Action constructor.
  const SendMessageRecordAudioAction() : super._();

  /// Deserialize.
  factory SendMessageRecordAudioAction.deserialize(Uint8List buffer) {
    // final result = SendMessageRecordAudioAction();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd52f73f7);
  }
}

/// Send Message Upload Audio Action.
///
/// ID: `f351d7ab`.
class SendMessageUploadAudioAction extends SendMessageActionBase {
  /// Send Message Upload Audio Action constructor.
  const SendMessageUploadAudioAction({
    required this.progress,
  }) : super._();

  /// Deserialize.
  factory SendMessageUploadAudioAction.deserialize(Uint8List buffer) {
    // final progress = _readint(buffer);
    // final result = SendMessageUploadAudioAction(progress: progress,);

    // return result;

    throw Exception();
  }

  /// Progress.
  final int progress;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf351d7ab);
    buffer.writeInt(progress);
  }
}

/// Send Message Upload Photo Action.
///
/// ID: `d1d34a26`.
class SendMessageUploadPhotoAction extends SendMessageActionBase {
  /// Send Message Upload Photo Action constructor.
  const SendMessageUploadPhotoAction({
    required this.progress,
  }) : super._();

  /// Deserialize.
  factory SendMessageUploadPhotoAction.deserialize(Uint8List buffer) {
    // final progress = _readint(buffer);
    // final result = SendMessageUploadPhotoAction(progress: progress,);

    // return result;

    throw Exception();
  }

  /// Progress.
  final int progress;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd1d34a26);
    buffer.writeInt(progress);
  }
}

/// Send Message Upload Document Action.
///
/// ID: `aa0cd9e4`.
class SendMessageUploadDocumentAction extends SendMessageActionBase {
  /// Send Message Upload Document Action constructor.
  const SendMessageUploadDocumentAction({
    required this.progress,
  }) : super._();

  /// Deserialize.
  factory SendMessageUploadDocumentAction.deserialize(Uint8List buffer) {
    // final progress = _readint(buffer);
    // final result = SendMessageUploadDocumentAction(progress: progress,);

    // return result;

    throw Exception();
  }

  /// Progress.
  final int progress;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xaa0cd9e4);
    buffer.writeInt(progress);
  }
}

/// Send Message Geo Location Action.
///
/// ID: `176f8ba1`.
class SendMessageGeoLocationAction extends SendMessageActionBase {
  /// Send Message Geo Location Action constructor.
  const SendMessageGeoLocationAction() : super._();

  /// Deserialize.
  factory SendMessageGeoLocationAction.deserialize(Uint8List buffer) {
    // final result = SendMessageGeoLocationAction();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x176f8ba1);
  }
}

/// Send Message Choose Contact Action.
///
/// ID: `628cbc6f`.
class SendMessageChooseContactAction extends SendMessageActionBase {
  /// Send Message Choose Contact Action constructor.
  const SendMessageChooseContactAction() : super._();

  /// Deserialize.
  factory SendMessageChooseContactAction.deserialize(Uint8List buffer) {
    // final result = SendMessageChooseContactAction();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x628cbc6f);
  }
}

/// Send Message Game Play Action.
///
/// ID: `dd6a8f48`.
class SendMessageGamePlayAction extends SendMessageActionBase {
  /// Send Message Game Play Action constructor.
  const SendMessageGamePlayAction() : super._();

  /// Deserialize.
  factory SendMessageGamePlayAction.deserialize(Uint8List buffer) {
    // final result = SendMessageGamePlayAction();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdd6a8f48);
  }
}

/// Send Message Record Round Action.
///
/// ID: `88f27fbc`.
class SendMessageRecordRoundAction extends SendMessageActionBase {
  /// Send Message Record Round Action constructor.
  const SendMessageRecordRoundAction() : super._();

  /// Deserialize.
  factory SendMessageRecordRoundAction.deserialize(Uint8List buffer) {
    // final result = SendMessageRecordRoundAction();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x88f27fbc);
  }
}

/// Send Message Upload Round Action.
///
/// ID: `243e1c66`.
class SendMessageUploadRoundAction extends SendMessageActionBase {
  /// Send Message Upload Round Action constructor.
  const SendMessageUploadRoundAction({
    required this.progress,
  }) : super._();

  /// Deserialize.
  factory SendMessageUploadRoundAction.deserialize(Uint8List buffer) {
    // final progress = _readint(buffer);
    // final result = SendMessageUploadRoundAction(progress: progress,);

    // return result;

    throw Exception();
  }

  /// Progress.
  final int progress;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x243e1c66);
    buffer.writeInt(progress);
  }
}

/// Speaking In Group Call Action.
///
/// ID: `d92c2285`.
class SpeakingInGroupCallAction extends SendMessageActionBase {
  /// Speaking In Group Call Action constructor.
  const SpeakingInGroupCallAction() : super._();

  /// Deserialize.
  factory SpeakingInGroupCallAction.deserialize(Uint8List buffer) {
    // final result = SpeakingInGroupCallAction();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd92c2285);
  }
}

/// Send Message History Import Action.
///
/// ID: `dbda9246`.
class SendMessageHistoryImportAction extends SendMessageActionBase {
  /// Send Message History Import Action constructor.
  const SendMessageHistoryImportAction({
    required this.progress,
  }) : super._();

  /// Deserialize.
  factory SendMessageHistoryImportAction.deserialize(Uint8List buffer) {
    // final progress = _readint(buffer);
    // final result = SendMessageHistoryImportAction(progress: progress,);

    // return result;

    throw Exception();
  }

  /// Progress.
  final int progress;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdbda9246);
    buffer.writeInt(progress);
  }
}

/// Send Message Choose Sticker Action.
///
/// ID: `b05ac6b1`.
class SendMessageChooseStickerAction extends SendMessageActionBase {
  /// Send Message Choose Sticker Action constructor.
  const SendMessageChooseStickerAction() : super._();

  /// Deserialize.
  factory SendMessageChooseStickerAction.deserialize(Uint8List buffer) {
    // final result = SendMessageChooseStickerAction();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb05ac6b1);
  }
}

/// Send Message Emoji Interaction.
///
/// ID: `25972bcb`.
class SendMessageEmojiInteraction extends SendMessageActionBase {
  /// Send Message Emoji Interaction constructor.
  const SendMessageEmojiInteraction({
    required this.emoticon,
    required this.msgId,
    required this.interaction,
  }) : super._();

  /// Deserialize.
  factory SendMessageEmojiInteraction.deserialize(Uint8List buffer) {
    // final emoticon = _readstring(buffer);
    // final msgId = _readint(buffer);
    // final interaction = _readDataJSON(buffer);
    // final result = SendMessageEmojiInteraction(emoticon: emoticon, msgId: msgId, interaction: interaction,);

    // return result;

    throw Exception();
  }

  /// Emoticon.
  final String emoticon;

  /// Msg Id.
  final int msgId;

  /// Interaction.
  final DataJSONBase interaction;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x25972bcb);
    buffer.writeString(emoticon);
    buffer.writeInt(msgId);
    buffer.writeObject(interaction);
  }
}

/// Send Message Emoji Interaction Seen.
///
/// ID: `b665902e`.
class SendMessageEmojiInteractionSeen extends SendMessageActionBase {
  /// Send Message Emoji Interaction Seen constructor.
  const SendMessageEmojiInteractionSeen({
    required this.emoticon,
  }) : super._();

  /// Deserialize.
  factory SendMessageEmojiInteractionSeen.deserialize(Uint8List buffer) {
    // final emoticon = _readstring(buffer);
    // final result = SendMessageEmojiInteractionSeen(emoticon: emoticon,);

    // return result;

    throw Exception();
  }

  /// Emoticon.
  final String emoticon;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb665902e);
    buffer.writeString(emoticon);
  }
}

/// Predicate Contacts Found.
abstract class ContactsFoundBase extends TlConstructor {
  /// Predicate Contacts Found constructor.
  const ContactsFoundBase._() : super._();
}

/// Contacts Found.
///
/// ID: `b3134d9d`.
class ContactsFound extends ContactsFoundBase {
  /// Contacts Found constructor.
  const ContactsFound({
    required this.myResults,
    required this.results,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ContactsFound.deserialize(Uint8List buffer) {
    // final myResults = _readVector<Peer>(buffer);
    // final results = _readVector<Peer>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ContactsFound(myResults: myResults, results: results, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// My Results.
  final List<PeerBase> myResults;

  /// Results.
  final List<PeerBase> results;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb3134d9d);
    buffer.writeVectorObject(myResults);
    buffer.writeVectorObject(results);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Input Privacy Key.
abstract class InputPrivacyKeyBase extends TlConstructor {
  /// Predicate Input Privacy Key constructor.
  const InputPrivacyKeyBase._() : super._();
}

/// Input Privacy Key Status Timestamp.
///
/// ID: `4f96cb18`.
class InputPrivacyKeyStatusTimestamp extends InputPrivacyKeyBase {
  /// Input Privacy Key Status Timestamp constructor.
  const InputPrivacyKeyStatusTimestamp() : super._();

  /// Deserialize.
  factory InputPrivacyKeyStatusTimestamp.deserialize(Uint8List buffer) {
    // final result = InputPrivacyKeyStatusTimestamp();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4f96cb18);
  }
}

/// Input Privacy Key Chat Invite.
///
/// ID: `bdfb0426`.
class InputPrivacyKeyChatInvite extends InputPrivacyKeyBase {
  /// Input Privacy Key Chat Invite constructor.
  const InputPrivacyKeyChatInvite() : super._();

  /// Deserialize.
  factory InputPrivacyKeyChatInvite.deserialize(Uint8List buffer) {
    // final result = InputPrivacyKeyChatInvite();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbdfb0426);
  }
}

/// Input Privacy Key Phone Call.
///
/// ID: `fabadc5f`.
class InputPrivacyKeyPhoneCall extends InputPrivacyKeyBase {
  /// Input Privacy Key Phone Call constructor.
  const InputPrivacyKeyPhoneCall() : super._();

  /// Deserialize.
  factory InputPrivacyKeyPhoneCall.deserialize(Uint8List buffer) {
    // final result = InputPrivacyKeyPhoneCall();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfabadc5f);
  }
}

/// Input Privacy Key Phone P2 P.
///
/// ID: `db9e70d2`.
class InputPrivacyKeyPhoneP2P extends InputPrivacyKeyBase {
  /// Input Privacy Key Phone P2 P constructor.
  const InputPrivacyKeyPhoneP2P() : super._();

  /// Deserialize.
  factory InputPrivacyKeyPhoneP2P.deserialize(Uint8List buffer) {
    // final result = InputPrivacyKeyPhoneP2P();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdb9e70d2);
  }
}

/// Input Privacy Key Forwards.
///
/// ID: `a4dd4c08`.
class InputPrivacyKeyForwards extends InputPrivacyKeyBase {
  /// Input Privacy Key Forwards constructor.
  const InputPrivacyKeyForwards() : super._();

  /// Deserialize.
  factory InputPrivacyKeyForwards.deserialize(Uint8List buffer) {
    // final result = InputPrivacyKeyForwards();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa4dd4c08);
  }
}

/// Input Privacy Key Profile Photo.
///
/// ID: `5719bacc`.
class InputPrivacyKeyProfilePhoto extends InputPrivacyKeyBase {
  /// Input Privacy Key Profile Photo constructor.
  const InputPrivacyKeyProfilePhoto() : super._();

  /// Deserialize.
  factory InputPrivacyKeyProfilePhoto.deserialize(Uint8List buffer) {
    // final result = InputPrivacyKeyProfilePhoto();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5719bacc);
  }
}

/// Input Privacy Key Phone Number.
///
/// ID: `0352dafa`.
class InputPrivacyKeyPhoneNumber extends InputPrivacyKeyBase {
  /// Input Privacy Key Phone Number constructor.
  const InputPrivacyKeyPhoneNumber() : super._();

  /// Deserialize.
  factory InputPrivacyKeyPhoneNumber.deserialize(Uint8List buffer) {
    // final result = InputPrivacyKeyPhoneNumber();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0352dafa);
  }
}

/// Input Privacy Key Added By Phone.
///
/// ID: `d1219bdd`.
class InputPrivacyKeyAddedByPhone extends InputPrivacyKeyBase {
  /// Input Privacy Key Added By Phone constructor.
  const InputPrivacyKeyAddedByPhone() : super._();

  /// Deserialize.
  factory InputPrivacyKeyAddedByPhone.deserialize(Uint8List buffer) {
    // final result = InputPrivacyKeyAddedByPhone();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd1219bdd);
  }
}

/// Input Privacy Key Voice Messages.
///
/// ID: `aee69d68`.
class InputPrivacyKeyVoiceMessages extends InputPrivacyKeyBase {
  /// Input Privacy Key Voice Messages constructor.
  const InputPrivacyKeyVoiceMessages() : super._();

  /// Deserialize.
  factory InputPrivacyKeyVoiceMessages.deserialize(Uint8List buffer) {
    // final result = InputPrivacyKeyVoiceMessages();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xaee69d68);
  }
}

/// Input Privacy Key About.
///
/// ID: `3823cc40`.
class InputPrivacyKeyAbout extends InputPrivacyKeyBase {
  /// Input Privacy Key About constructor.
  const InputPrivacyKeyAbout() : super._();

  /// Deserialize.
  factory InputPrivacyKeyAbout.deserialize(Uint8List buffer) {
    // final result = InputPrivacyKeyAbout();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3823cc40);
  }
}

/// Predicate Privacy Key.
abstract class PrivacyKeyBase extends TlConstructor {
  /// Predicate Privacy Key constructor.
  const PrivacyKeyBase._() : super._();
}

/// Privacy Key Status Timestamp.
///
/// ID: `bc2eab30`.
class PrivacyKeyStatusTimestamp extends PrivacyKeyBase {
  /// Privacy Key Status Timestamp constructor.
  const PrivacyKeyStatusTimestamp() : super._();

  /// Deserialize.
  factory PrivacyKeyStatusTimestamp.deserialize(Uint8List buffer) {
    // final result = PrivacyKeyStatusTimestamp();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbc2eab30);
  }
}

/// Privacy Key Chat Invite.
///
/// ID: `500e6dfa`.
class PrivacyKeyChatInvite extends PrivacyKeyBase {
  /// Privacy Key Chat Invite constructor.
  const PrivacyKeyChatInvite() : super._();

  /// Deserialize.
  factory PrivacyKeyChatInvite.deserialize(Uint8List buffer) {
    // final result = PrivacyKeyChatInvite();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x500e6dfa);
  }
}

/// Privacy Key Phone Call.
///
/// ID: `3d662b7b`.
class PrivacyKeyPhoneCall extends PrivacyKeyBase {
  /// Privacy Key Phone Call constructor.
  const PrivacyKeyPhoneCall() : super._();

  /// Deserialize.
  factory PrivacyKeyPhoneCall.deserialize(Uint8List buffer) {
    // final result = PrivacyKeyPhoneCall();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3d662b7b);
  }
}

/// Privacy Key Phone P2 P.
///
/// ID: `39491cc8`.
class PrivacyKeyPhoneP2P extends PrivacyKeyBase {
  /// Privacy Key Phone P2 P constructor.
  const PrivacyKeyPhoneP2P() : super._();

  /// Deserialize.
  factory PrivacyKeyPhoneP2P.deserialize(Uint8List buffer) {
    // final result = PrivacyKeyPhoneP2P();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x39491cc8);
  }
}

/// Privacy Key Forwards.
///
/// ID: `69ec56a3`.
class PrivacyKeyForwards extends PrivacyKeyBase {
  /// Privacy Key Forwards constructor.
  const PrivacyKeyForwards() : super._();

  /// Deserialize.
  factory PrivacyKeyForwards.deserialize(Uint8List buffer) {
    // final result = PrivacyKeyForwards();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x69ec56a3);
  }
}

/// Privacy Key Profile Photo.
///
/// ID: `96151fed`.
class PrivacyKeyProfilePhoto extends PrivacyKeyBase {
  /// Privacy Key Profile Photo constructor.
  const PrivacyKeyProfilePhoto() : super._();

  /// Deserialize.
  factory PrivacyKeyProfilePhoto.deserialize(Uint8List buffer) {
    // final result = PrivacyKeyProfilePhoto();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x96151fed);
  }
}

/// Privacy Key Phone Number.
///
/// ID: `d19ae46d`.
class PrivacyKeyPhoneNumber extends PrivacyKeyBase {
  /// Privacy Key Phone Number constructor.
  const PrivacyKeyPhoneNumber() : super._();

  /// Deserialize.
  factory PrivacyKeyPhoneNumber.deserialize(Uint8List buffer) {
    // final result = PrivacyKeyPhoneNumber();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd19ae46d);
  }
}

/// Privacy Key Added By Phone.
///
/// ID: `42ffd42b`.
class PrivacyKeyAddedByPhone extends PrivacyKeyBase {
  /// Privacy Key Added By Phone constructor.
  const PrivacyKeyAddedByPhone() : super._();

  /// Deserialize.
  factory PrivacyKeyAddedByPhone.deserialize(Uint8List buffer) {
    // final result = PrivacyKeyAddedByPhone();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x42ffd42b);
  }
}

/// Privacy Key Voice Messages.
///
/// ID: `0697f414`.
class PrivacyKeyVoiceMessages extends PrivacyKeyBase {
  /// Privacy Key Voice Messages constructor.
  const PrivacyKeyVoiceMessages() : super._();

  /// Deserialize.
  factory PrivacyKeyVoiceMessages.deserialize(Uint8List buffer) {
    // final result = PrivacyKeyVoiceMessages();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0697f414);
  }
}

/// Privacy Key About.
///
/// ID: `a486b761`.
class PrivacyKeyAbout extends PrivacyKeyBase {
  /// Privacy Key About constructor.
  const PrivacyKeyAbout() : super._();

  /// Deserialize.
  factory PrivacyKeyAbout.deserialize(Uint8List buffer) {
    // final result = PrivacyKeyAbout();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa486b761);
  }
}

/// Predicate Input Privacy Rule.
abstract class InputPrivacyRuleBase extends TlConstructor {
  /// Predicate Input Privacy Rule constructor.
  const InputPrivacyRuleBase._() : super._();
}

/// Input Privacy Value Allow Contacts.
///
/// ID: `0d09e07b`.
class InputPrivacyValueAllowContacts extends InputPrivacyRuleBase {
  /// Input Privacy Value Allow Contacts constructor.
  const InputPrivacyValueAllowContacts() : super._();

  /// Deserialize.
  factory InputPrivacyValueAllowContacts.deserialize(Uint8List buffer) {
    // final result = InputPrivacyValueAllowContacts();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0d09e07b);
  }
}

/// Input Privacy Value Allow All.
///
/// ID: `184b35ce`.
class InputPrivacyValueAllowAll extends InputPrivacyRuleBase {
  /// Input Privacy Value Allow All constructor.
  const InputPrivacyValueAllowAll() : super._();

  /// Deserialize.
  factory InputPrivacyValueAllowAll.deserialize(Uint8List buffer) {
    // final result = InputPrivacyValueAllowAll();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x184b35ce);
  }
}

/// Input Privacy Value Allow Users.
///
/// ID: `131cc67f`.
class InputPrivacyValueAllowUsers extends InputPrivacyRuleBase {
  /// Input Privacy Value Allow Users constructor.
  const InputPrivacyValueAllowUsers({
    required this.users,
  }) : super._();

  /// Deserialize.
  factory InputPrivacyValueAllowUsers.deserialize(Uint8List buffer) {
    // final users = _readVector<InputUser>(buffer);
    // final result = InputPrivacyValueAllowUsers(users: users,);

    // return result;

    throw Exception();
  }

  /// Users.
  final List<InputUserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x131cc67f);
    buffer.writeVectorObject(users);
  }
}

/// Input Privacy Value Disallow Contacts.
///
/// ID: `0ba52007`.
class InputPrivacyValueDisallowContacts extends InputPrivacyRuleBase {
  /// Input Privacy Value Disallow Contacts constructor.
  const InputPrivacyValueDisallowContacts() : super._();

  /// Deserialize.
  factory InputPrivacyValueDisallowContacts.deserialize(Uint8List buffer) {
    // final result = InputPrivacyValueDisallowContacts();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0ba52007);
  }
}

/// Input Privacy Value Disallow All.
///
/// ID: `d66b66c9`.
class InputPrivacyValueDisallowAll extends InputPrivacyRuleBase {
  /// Input Privacy Value Disallow All constructor.
  const InputPrivacyValueDisallowAll() : super._();

  /// Deserialize.
  factory InputPrivacyValueDisallowAll.deserialize(Uint8List buffer) {
    // final result = InputPrivacyValueDisallowAll();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd66b66c9);
  }
}

/// Input Privacy Value Disallow Users.
///
/// ID: `90110467`.
class InputPrivacyValueDisallowUsers extends InputPrivacyRuleBase {
  /// Input Privacy Value Disallow Users constructor.
  const InputPrivacyValueDisallowUsers({
    required this.users,
  }) : super._();

  /// Deserialize.
  factory InputPrivacyValueDisallowUsers.deserialize(Uint8List buffer) {
    // final users = _readVector<InputUser>(buffer);
    // final result = InputPrivacyValueDisallowUsers(users: users,);

    // return result;

    throw Exception();
  }

  /// Users.
  final List<InputUserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x90110467);
    buffer.writeVectorObject(users);
  }
}

/// Input Privacy Value Allow Chat Participants.
///
/// ID: `840649cf`.
class InputPrivacyValueAllowChatParticipants extends InputPrivacyRuleBase {
  /// Input Privacy Value Allow Chat Participants constructor.
  const InputPrivacyValueAllowChatParticipants({
    required this.chats,
  }) : super._();

  /// Deserialize.
  factory InputPrivacyValueAllowChatParticipants.deserialize(Uint8List buffer) {
    // final chats = _readVector<long>(buffer);
    // final result = InputPrivacyValueAllowChatParticipants(chats: chats,);

    // return result;

    throw Exception();
  }

  /// Chats.
  final List<int> chats;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x840649cf);
    buffer.writeVectorLong(chats);
  }
}

/// Input Privacy Value Disallow Chat Participants.
///
/// ID: `e94f0f86`.
class InputPrivacyValueDisallowChatParticipants extends InputPrivacyRuleBase {
  /// Input Privacy Value Disallow Chat Participants constructor.
  const InputPrivacyValueDisallowChatParticipants({
    required this.chats,
  }) : super._();

  /// Deserialize.
  factory InputPrivacyValueDisallowChatParticipants.deserialize(
      Uint8List buffer) {
    // final chats = _readVector<long>(buffer);
    // final result = InputPrivacyValueDisallowChatParticipants(chats: chats,);

    // return result;

    throw Exception();
  }

  /// Chats.
  final List<int> chats;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe94f0f86);
    buffer.writeVectorLong(chats);
  }
}

/// Input Privacy Value Allow Close Friends.
///
/// ID: `2f453e49`.
class InputPrivacyValueAllowCloseFriends extends InputPrivacyRuleBase {
  /// Input Privacy Value Allow Close Friends constructor.
  const InputPrivacyValueAllowCloseFriends() : super._();

  /// Deserialize.
  factory InputPrivacyValueAllowCloseFriends.deserialize(Uint8List buffer) {
    // final result = InputPrivacyValueAllowCloseFriends();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2f453e49);
  }
}

/// Predicate Privacy Rule.
abstract class PrivacyRuleBase extends TlConstructor {
  /// Predicate Privacy Rule constructor.
  const PrivacyRuleBase._() : super._();
}

/// Privacy Value Allow Contacts.
///
/// ID: `fffe1bac`.
class PrivacyValueAllowContacts extends PrivacyRuleBase {
  /// Privacy Value Allow Contacts constructor.
  const PrivacyValueAllowContacts() : super._();

  /// Deserialize.
  factory PrivacyValueAllowContacts.deserialize(Uint8List buffer) {
    // final result = PrivacyValueAllowContacts();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfffe1bac);
  }
}

/// Privacy Value Allow All.
///
/// ID: `65427b82`.
class PrivacyValueAllowAll extends PrivacyRuleBase {
  /// Privacy Value Allow All constructor.
  const PrivacyValueAllowAll() : super._();

  /// Deserialize.
  factory PrivacyValueAllowAll.deserialize(Uint8List buffer) {
    // final result = PrivacyValueAllowAll();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x65427b82);
  }
}

/// Privacy Value Allow Users.
///
/// ID: `b8905fb2`.
class PrivacyValueAllowUsers extends PrivacyRuleBase {
  /// Privacy Value Allow Users constructor.
  const PrivacyValueAllowUsers({
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PrivacyValueAllowUsers.deserialize(Uint8List buffer) {
    // final users = _readVector<long>(buffer);
    // final result = PrivacyValueAllowUsers(users: users,);

    // return result;

    throw Exception();
  }

  /// Users.
  final List<int> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb8905fb2);
    buffer.writeVectorLong(users);
  }
}

/// Privacy Value Disallow Contacts.
///
/// ID: `f888fa1a`.
class PrivacyValueDisallowContacts extends PrivacyRuleBase {
  /// Privacy Value Disallow Contacts constructor.
  const PrivacyValueDisallowContacts() : super._();

  /// Deserialize.
  factory PrivacyValueDisallowContacts.deserialize(Uint8List buffer) {
    // final result = PrivacyValueDisallowContacts();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf888fa1a);
  }
}

/// Privacy Value Disallow All.
///
/// ID: `8b73e763`.
class PrivacyValueDisallowAll extends PrivacyRuleBase {
  /// Privacy Value Disallow All constructor.
  const PrivacyValueDisallowAll() : super._();

  /// Deserialize.
  factory PrivacyValueDisallowAll.deserialize(Uint8List buffer) {
    // final result = PrivacyValueDisallowAll();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8b73e763);
  }
}

/// Privacy Value Disallow Users.
///
/// ID: `e4621141`.
class PrivacyValueDisallowUsers extends PrivacyRuleBase {
  /// Privacy Value Disallow Users constructor.
  const PrivacyValueDisallowUsers({
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PrivacyValueDisallowUsers.deserialize(Uint8List buffer) {
    // final users = _readVector<long>(buffer);
    // final result = PrivacyValueDisallowUsers(users: users,);

    // return result;

    throw Exception();
  }

  /// Users.
  final List<int> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe4621141);
    buffer.writeVectorLong(users);
  }
}

/// Privacy Value Allow Chat Participants.
///
/// ID: `6b134e8e`.
class PrivacyValueAllowChatParticipants extends PrivacyRuleBase {
  /// Privacy Value Allow Chat Participants constructor.
  const PrivacyValueAllowChatParticipants({
    required this.chats,
  }) : super._();

  /// Deserialize.
  factory PrivacyValueAllowChatParticipants.deserialize(Uint8List buffer) {
    // final chats = _readVector<long>(buffer);
    // final result = PrivacyValueAllowChatParticipants(chats: chats,);

    // return result;

    throw Exception();
  }

  /// Chats.
  final List<int> chats;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6b134e8e);
    buffer.writeVectorLong(chats);
  }
}

/// Privacy Value Disallow Chat Participants.
///
/// ID: `41c87565`.
class PrivacyValueDisallowChatParticipants extends PrivacyRuleBase {
  /// Privacy Value Disallow Chat Participants constructor.
  const PrivacyValueDisallowChatParticipants({
    required this.chats,
  }) : super._();

  /// Deserialize.
  factory PrivacyValueDisallowChatParticipants.deserialize(Uint8List buffer) {
    // final chats = _readVector<long>(buffer);
    // final result = PrivacyValueDisallowChatParticipants(chats: chats,);

    // return result;

    throw Exception();
  }

  /// Chats.
  final List<int> chats;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x41c87565);
    buffer.writeVectorLong(chats);
  }
}

/// Privacy Value Allow Close Friends.
///
/// ID: `f7e8d89b`.
class PrivacyValueAllowCloseFriends extends PrivacyRuleBase {
  /// Privacy Value Allow Close Friends constructor.
  const PrivacyValueAllowCloseFriends() : super._();

  /// Deserialize.
  factory PrivacyValueAllowCloseFriends.deserialize(Uint8List buffer) {
    // final result = PrivacyValueAllowCloseFriends();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf7e8d89b);
  }
}

/// Predicate Account Privacy Rules.
abstract class AccountPrivacyRulesBase extends TlConstructor {
  /// Predicate Account Privacy Rules constructor.
  const AccountPrivacyRulesBase._() : super._();
}

/// Account Privacy Rules.
///
/// ID: `50a04e45`.
class AccountPrivacyRules extends AccountPrivacyRulesBase {
  /// Account Privacy Rules constructor.
  const AccountPrivacyRules({
    required this.rules,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory AccountPrivacyRules.deserialize(Uint8List buffer) {
    // final rules = _readVector<PrivacyRule>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = AccountPrivacyRules(rules: rules, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Rules.
  final List<PrivacyRuleBase> rules;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x50a04e45);
    buffer.writeVectorObject(rules);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Account Days T T L.
abstract class AccountDaysTTLBase extends TlConstructor {
  /// Predicate Account Days T T L constructor.
  const AccountDaysTTLBase._() : super._();
}

/// Account Days T T L.
///
/// ID: `b8d0afdf`.
class AccountDaysTTL extends AccountDaysTTLBase {
  /// Account Days T T L constructor.
  const AccountDaysTTL({
    required this.days,
  }) : super._();

  /// Deserialize.
  factory AccountDaysTTL.deserialize(Uint8List buffer) {
    // final days = _readint(buffer);
    // final result = AccountDaysTTL(days: days,);

    // return result;

    throw Exception();
  }

  /// Days.
  final int days;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb8d0afdf);
    buffer.writeInt(days);
  }
}

/// Predicate Document Attribute.
abstract class DocumentAttributeBase extends TlConstructor {
  /// Predicate Document Attribute constructor.
  const DocumentAttributeBase._() : super._();
}

/// Document Attribute Image Size.
///
/// ID: `6c37c15c`.
class DocumentAttributeImageSize extends DocumentAttributeBase {
  /// Document Attribute Image Size constructor.
  const DocumentAttributeImageSize({
    required this.w,
    required this.h,
  }) : super._();

  /// Deserialize.
  factory DocumentAttributeImageSize.deserialize(Uint8List buffer) {
    // final w = _readint(buffer);
    // final h = _readint(buffer);
    // final result = DocumentAttributeImageSize(w: w, h: h,);

    // return result;

    throw Exception();
  }

  /// W.
  final int w;

  /// H.
  final int h;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6c37c15c);
    buffer.writeInt(w);
    buffer.writeInt(h);
  }
}

/// Document Attribute Animated.
///
/// ID: `11b58939`.
class DocumentAttributeAnimated extends DocumentAttributeBase {
  /// Document Attribute Animated constructor.
  const DocumentAttributeAnimated() : super._();

  /// Deserialize.
  factory DocumentAttributeAnimated.deserialize(Uint8List buffer) {
    // final result = DocumentAttributeAnimated();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x11b58939);
  }
}

/// Document Attribute Sticker.
///
/// ID: `6319d612`.
class DocumentAttributeSticker extends DocumentAttributeBase {
  /// Document Attribute Sticker constructor.
  const DocumentAttributeSticker({
    required this.mask,
    required this.alt,
    required this.stickerset,
    this.maskCoords,
  }) : super._();

  /// Deserialize.
  factory DocumentAttributeSticker.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final mask = _readflags.1?true(buffer);
    // final alt = _readstring(buffer);
    // final stickerset = _readInputStickerSet(buffer);
    // final maskCoords = _readflags.0?MaskCoords(buffer);
    // final result = DocumentAttributeSticker(flags: flags, mask: mask, alt: alt, stickerset: stickerset, maskCoords: maskCoords,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: mask,
      b00: maskCoords != null,
    );

    return v;
  }

  /// mask: bit 1 of flags.1?true
  final bool mask;

  /// Alt.
  final String alt;

  /// Stickerset.
  final InputStickerSetBase stickerset;

  /// Mask Coords.
  final MaskCoordsBase? maskCoords;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6319d612);
    buffer.writeInt(flags);
    buffer.writeString(alt);
    buffer.writeObject(stickerset);
    final localMaskCoordsCopy = maskCoords;
    if (localMaskCoordsCopy != null) {
      buffer.writeObject(localMaskCoordsCopy);
    }
  }
}

/// Document Attribute Video.
///
/// ID: `d38ff1c2`.
class DocumentAttributeVideo extends DocumentAttributeBase {
  /// Document Attribute Video constructor.
  const DocumentAttributeVideo({
    required this.roundMessage,
    required this.supportsStreaming,
    required this.nosound,
    required this.duration,
    required this.w,
    required this.h,
    this.preloadPrefixSize,
  }) : super._();

  /// Deserialize.
  factory DocumentAttributeVideo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final roundMessage = _readflags.0?true(buffer);
    // final supportsStreaming = _readflags.1?true(buffer);
    // final nosound = _readflags.3?true(buffer);
    // final duration = _readdouble(buffer);
    // final w = _readint(buffer);
    // final h = _readint(buffer);
    // final preloadPrefixSize = _readflags.2?int(buffer);
    // final result = DocumentAttributeVideo(flags: flags, roundMessage: roundMessage, supportsStreaming: supportsStreaming, nosound: nosound, duration: duration, w: w, h: h, preloadPrefixSize: preloadPrefixSize,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: roundMessage,
      b01: supportsStreaming,
      b03: nosound,
      b02: preloadPrefixSize != null,
    );

    return v;
  }

  /// round_message: bit 0 of flags.0?true
  final bool roundMessage;

  /// supports_streaming: bit 1 of flags.1?true
  final bool supportsStreaming;

  /// nosound: bit 3 of flags.3?true
  final bool nosound;

  /// Duration.
  final double duration;

  /// W.
  final int w;

  /// H.
  final int h;

  /// Preload Prefix Size.
  final int? preloadPrefixSize;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd38ff1c2);
    buffer.writeInt(flags);
    buffer.writeDouble(duration);
    buffer.writeInt(w);
    buffer.writeInt(h);
    final localPreloadPrefixSizeCopy = preloadPrefixSize;
    if (localPreloadPrefixSizeCopy != null) {
      buffer.writeInt(localPreloadPrefixSizeCopy);
    }
  }
}

/// Document Attribute Audio.
///
/// ID: `9852f9c6`.
class DocumentAttributeAudio extends DocumentAttributeBase {
  /// Document Attribute Audio constructor.
  const DocumentAttributeAudio({
    required this.voice,
    required this.duration,
    this.title,
    this.performer,
    this.waveform,
  }) : super._();

  /// Deserialize.
  factory DocumentAttributeAudio.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final voice = _readflags.10?true(buffer);
    // final duration = _readint(buffer);
    // final title = _readflags.0?string(buffer);
    // final performer = _readflags.1?string(buffer);
    // final waveform = _readflags.2?bytes(buffer);
    // final result = DocumentAttributeAudio(flags: flags, voice: voice, duration: duration, title: title, performer: performer, waveform: waveform,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b10: voice,
      b00: title != null,
      b01: performer != null,
      b02: waveform != null,
    );

    return v;
  }

  /// voice: bit 10 of flags.10?true
  final bool voice;

  /// Duration.
  final int duration;

  /// Title.
  final String? title;

  /// Performer.
  final String? performer;

  /// Waveform.
  final Uint8List? waveform;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9852f9c6);
    buffer.writeInt(flags);
    buffer.writeInt(duration);
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localPerformerCopy = performer;
    if (localPerformerCopy != null) {
      buffer.writeString(localPerformerCopy);
    }
    final localWaveformCopy = waveform;
    if (localWaveformCopy != null) {
      buffer.writeBytes(localWaveformCopy);
    }
  }
}

/// Document Attribute Filename.
///
/// ID: `15590068`.
class DocumentAttributeFilename extends DocumentAttributeBase {
  /// Document Attribute Filename constructor.
  const DocumentAttributeFilename({
    required this.fileName,
  }) : super._();

  /// Deserialize.
  factory DocumentAttributeFilename.deserialize(Uint8List buffer) {
    // final fileName = _readstring(buffer);
    // final result = DocumentAttributeFilename(fileName: fileName,);

    // return result;

    throw Exception();
  }

  /// File Name.
  final String fileName;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x15590068);
    buffer.writeString(fileName);
  }
}

/// Document Attribute Has Stickers.
///
/// ID: `9801d2f7`.
class DocumentAttributeHasStickers extends DocumentAttributeBase {
  /// Document Attribute Has Stickers constructor.
  const DocumentAttributeHasStickers() : super._();

  /// Deserialize.
  factory DocumentAttributeHasStickers.deserialize(Uint8List buffer) {
    // final result = DocumentAttributeHasStickers();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9801d2f7);
  }
}

/// Document Attribute Custom Emoji.
///
/// ID: `fd149899`.
class DocumentAttributeCustomEmoji extends DocumentAttributeBase {
  /// Document Attribute Custom Emoji constructor.
  const DocumentAttributeCustomEmoji({
    required this.free,
    required this.textColor,
    required this.alt,
    required this.stickerset,
  }) : super._();

  /// Deserialize.
  factory DocumentAttributeCustomEmoji.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final free = _readflags.0?true(buffer);
    // final textColor = _readflags.1?true(buffer);
    // final alt = _readstring(buffer);
    // final stickerset = _readInputStickerSet(buffer);
    // final result = DocumentAttributeCustomEmoji(flags: flags, free: free, textColor: textColor, alt: alt, stickerset: stickerset,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: free,
      b01: textColor,
    );

    return v;
  }

  /// free: bit 0 of flags.0?true
  final bool free;

  /// text_color: bit 1 of flags.1?true
  final bool textColor;

  /// Alt.
  final String alt;

  /// Stickerset.
  final InputStickerSetBase stickerset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfd149899);
    buffer.writeInt(flags);
    buffer.writeString(alt);
    buffer.writeObject(stickerset);
  }
}

/// Predicate Messages Stickers.
abstract class MessagesStickersBase extends TlConstructor {
  /// Predicate Messages Stickers constructor.
  const MessagesStickersBase._() : super._();
}

/// Messages Stickers Not Modified.
///
/// ID: `f1749a22`.
class MessagesStickersNotModified extends MessagesStickersBase {
  /// Messages Stickers Not Modified constructor.
  const MessagesStickersNotModified() : super._();

  /// Deserialize.
  factory MessagesStickersNotModified.deserialize(Uint8List buffer) {
    // final result = MessagesStickersNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf1749a22);
  }
}

/// Messages Stickers.
///
/// ID: `30a6ec7e`.
class MessagesStickers extends MessagesStickersBase {
  /// Messages Stickers constructor.
  const MessagesStickers({
    required this.hash,
    required this.stickers,
  }) : super._();

  /// Deserialize.
  factory MessagesStickers.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final stickers = _readVector<Document>(buffer);
    // final result = MessagesStickers(hash: hash, stickers: stickers,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Stickers.
  final List<DocumentBase> stickers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x30a6ec7e);
    buffer.writeLong(hash);
    buffer.writeVectorObject(stickers);
  }
}

/// Predicate Sticker Pack.
abstract class StickerPackBase extends TlConstructor {
  /// Predicate Sticker Pack constructor.
  const StickerPackBase._() : super._();
}

/// Sticker Pack.
///
/// ID: `12b299d4`.
class StickerPack extends StickerPackBase {
  /// Sticker Pack constructor.
  const StickerPack({
    required this.emoticon,
    required this.documents,
  }) : super._();

  /// Deserialize.
  factory StickerPack.deserialize(Uint8List buffer) {
    // final emoticon = _readstring(buffer);
    // final documents = _readVector<long>(buffer);
    // final result = StickerPack(emoticon: emoticon, documents: documents,);

    // return result;

    throw Exception();
  }

  /// Emoticon.
  final String emoticon;

  /// Documents.
  final List<int> documents;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x12b299d4);
    buffer.writeString(emoticon);
    buffer.writeVectorLong(documents);
  }
}

/// Predicate Messages All Stickers.
abstract class MessagesAllStickersBase extends TlConstructor {
  /// Predicate Messages All Stickers constructor.
  const MessagesAllStickersBase._() : super._();
}

/// Messages All Stickers Not Modified.
///
/// ID: `e86602c3`.
class MessagesAllStickersNotModified extends MessagesAllStickersBase {
  /// Messages All Stickers Not Modified constructor.
  const MessagesAllStickersNotModified() : super._();

  /// Deserialize.
  factory MessagesAllStickersNotModified.deserialize(Uint8List buffer) {
    // final result = MessagesAllStickersNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe86602c3);
  }
}

/// Messages All Stickers.
///
/// ID: `cdbbcebb`.
class MessagesAllStickers extends MessagesAllStickersBase {
  /// Messages All Stickers constructor.
  const MessagesAllStickers({
    required this.hash,
    required this.sets,
  }) : super._();

  /// Deserialize.
  factory MessagesAllStickers.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final sets = _readVector<StickerSet>(buffer);
    // final result = MessagesAllStickers(hash: hash, sets: sets,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Sets.
  final List<StickerSetBase> sets;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcdbbcebb);
    buffer.writeLong(hash);
    buffer.writeVectorObject(sets);
  }
}

/// Predicate Messages Affected Messages.
abstract class MessagesAffectedMessagesBase extends TlConstructor {
  /// Predicate Messages Affected Messages constructor.
  const MessagesAffectedMessagesBase._() : super._();
}

/// Messages Affected Messages.
///
/// ID: `84d19185`.
class MessagesAffectedMessages extends MessagesAffectedMessagesBase {
  /// Messages Affected Messages constructor.
  const MessagesAffectedMessages({
    required this.pts,
    required this.ptsCount,
  }) : super._();

  /// Deserialize.
  factory MessagesAffectedMessages.deserialize(Uint8List buffer) {
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final result = MessagesAffectedMessages(pts: pts, ptsCount: ptsCount,);

    // return result;

    throw Exception();
  }

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x84d19185);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
  }
}

/// Predicate Web Page.
abstract class WebPageBase extends TlConstructor {
  /// Predicate Web Page constructor.
  const WebPageBase._() : super._();
}

/// Web Page Empty.
///
/// ID: `211a1788`.
class WebPageEmpty extends WebPageBase {
  /// Web Page Empty constructor.
  const WebPageEmpty({
    required this.id,
    this.url,
  }) : super._();

  /// Deserialize.
  factory WebPageEmpty.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final id = _readlong(buffer);
    // final url = _readflags.0?string(buffer);
    // final result = WebPageEmpty(flags: flags, id: id, url: url,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: url != null,
    );

    return v;
  }

  /// Id.
  final int id;

  /// Url.
  final String? url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x211a1788);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    final localUrlCopy = url;
    if (localUrlCopy != null) {
      buffer.writeString(localUrlCopy);
    }
  }
}

/// Web Page Pending.
///
/// ID: `b0d13e47`.
class WebPagePending extends WebPageBase {
  /// Web Page Pending constructor.
  const WebPagePending({
    required this.id,
    this.url,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory WebPagePending.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final id = _readlong(buffer);
    // final url = _readflags.0?string(buffer);
    // final date = _readint(buffer);
    // final result = WebPagePending(flags: flags, id: id, url: url, date: date,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: url != null,
    );

    return v;
  }

  /// Id.
  final int id;

  /// Url.
  final String? url;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb0d13e47);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    final localUrlCopy = url;
    if (localUrlCopy != null) {
      buffer.writeString(localUrlCopy);
    }
    buffer.writeDateTime(date);
  }
}

/// Web Page.
///
/// ID: `e89c45b2`.
class WebPage extends WebPageBase {
  /// Web Page constructor.
  const WebPage({
    required this.hasLargeMedia,
    required this.id,
    required this.url,
    required this.displayUrl,
    required this.hash,
    this.type,
    this.siteName,
    this.title,
    this.description,
    this.photo,
    this.embedUrl,
    this.embedType,
    this.embedWidth,
    this.embedHeight,
    this.duration,
    this.author,
    this.document,
    this.cachedPage,
    this.attributes,
  }) : super._();

  /// Deserialize.
  factory WebPage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final hasLargeMedia = _readflags.13?true(buffer);
    // final id = _readlong(buffer);
    // final url = _readstring(buffer);
    // final displayUrl = _readstring(buffer);
    // final hash = _readint(buffer);
    // final type = _readflags.0?string(buffer);
    // final siteName = _readflags.1?string(buffer);
    // final title = _readflags.2?string(buffer);
    // final description = _readflags.3?string(buffer);
    // final photo = _readflags.4?Photo(buffer);
    // final embedUrl = _readflags.5?string(buffer);
    // final embedType = _readflags.5?string(buffer);
    // final embedWidth = _readflags.6?int(buffer);
    // final embedHeight = _readflags.6?int(buffer);
    // final duration = _readflags.7?int(buffer);
    // final author = _readflags.8?string(buffer);
    // final document = _readflags.9?Document(buffer);
    // final cachedPage = _readflags.10?Page(buffer);
    // final attributes = _readflags.12?Vector<WebPageAttribute>(buffer);
    // final result = WebPage(flags: flags, hasLargeMedia: hasLargeMedia, id: id, url: url, displayUrl: displayUrl, hash: hash, type: type, siteName: siteName, title: title, description: description, photo: photo, embedUrl: embedUrl, embedType: embedType, embedWidth: embedWidth, embedHeight: embedHeight, duration: duration, author: author, document: document, cachedPage: cachedPage, attributes: attributes,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b13: hasLargeMedia,
      b00: type != null,
      b01: siteName != null,
      b02: title != null,
      b03: description != null,
      b04: photo != null,
      b05: embedUrl != null || embedType != null,
      b06: embedWidth != null || embedHeight != null,
      b07: duration != null,
      b08: author != null,
      b09: document != null,
      b10: cachedPage != null,
      b12: attributes != null,
    );

    return v;
  }

  /// has_large_media: bit 13 of flags.13?true
  final bool hasLargeMedia;

  /// Id.
  final int id;

  /// Url.
  final String url;

  /// Display Url.
  final String displayUrl;

  /// Hash.
  final int hash;

  /// Type.
  final String? type;

  /// Site Name.
  final String? siteName;

  /// Title.
  final String? title;

  /// Description.
  final String? description;

  /// Photo.
  final PhotoBase? photo;

  /// Embed Url.
  final String? embedUrl;

  /// Embed Type.
  final String? embedType;

  /// Embed Width.
  final int? embedWidth;

  /// Embed Height.
  final int? embedHeight;

  /// Duration.
  final int? duration;

  /// Author.
  final String? author;

  /// Document.
  final DocumentBase? document;

  /// Cached Page.
  final PageBase? cachedPage;

  /// Attributes.
  final List<WebPageAttributeBase>? attributes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe89c45b2);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeString(url);
    buffer.writeString(displayUrl);
    buffer.writeInt(hash);
    final localTypeCopy = type;
    if (localTypeCopy != null) {
      buffer.writeString(localTypeCopy);
    }
    final localSiteNameCopy = siteName;
    if (localSiteNameCopy != null) {
      buffer.writeString(localSiteNameCopy);
    }
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localDescriptionCopy = description;
    if (localDescriptionCopy != null) {
      buffer.writeString(localDescriptionCopy);
    }
    final localPhotoCopy = photo;
    if (localPhotoCopy != null) {
      buffer.writeObject(localPhotoCopy);
    }
    final localEmbedUrlCopy = embedUrl;
    if (localEmbedUrlCopy != null) {
      buffer.writeString(localEmbedUrlCopy);
    }
    final localEmbedTypeCopy = embedType;
    if (localEmbedTypeCopy != null) {
      buffer.writeString(localEmbedTypeCopy);
    }
    final localEmbedWidthCopy = embedWidth;
    if (localEmbedWidthCopy != null) {
      buffer.writeInt(localEmbedWidthCopy);
    }
    final localEmbedHeightCopy = embedHeight;
    if (localEmbedHeightCopy != null) {
      buffer.writeInt(localEmbedHeightCopy);
    }
    final localDurationCopy = duration;
    if (localDurationCopy != null) {
      buffer.writeInt(localDurationCopy);
    }
    final localAuthorCopy = author;
    if (localAuthorCopy != null) {
      buffer.writeString(localAuthorCopy);
    }
    final localDocumentCopy = document;
    if (localDocumentCopy != null) {
      buffer.writeObject(localDocumentCopy);
    }
    final localCachedPageCopy = cachedPage;
    if (localCachedPageCopy != null) {
      buffer.writeObject(localCachedPageCopy);
    }
    final localAttributesCopy = attributes;
    if (localAttributesCopy != null) {
      buffer.writeVectorObject(localAttributesCopy);
    }
  }
}

/// Web Page Not Modified.
///
/// ID: `7311ca11`.
class WebPageNotModified extends WebPageBase {
  /// Web Page Not Modified constructor.
  const WebPageNotModified({
    this.cachedPageViews,
  }) : super._();

  /// Deserialize.
  factory WebPageNotModified.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final cachedPageViews = _readflags.0?int(buffer);
    // final result = WebPageNotModified(flags: flags, cachedPageViews: cachedPageViews,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: cachedPageViews != null,
    );

    return v;
  }

  /// Cached Page Views.
  final int? cachedPageViews;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7311ca11);
    buffer.writeInt(flags);
    final localCachedPageViewsCopy = cachedPageViews;
    if (localCachedPageViewsCopy != null) {
      buffer.writeInt(localCachedPageViewsCopy);
    }
  }
}

/// Predicate Authorization.
abstract class AuthorizationBase extends TlConstructor {
  /// Predicate Authorization constructor.
  const AuthorizationBase._() : super._();
}

/// Authorization.
///
/// ID: `ad01d61d`.
class Authorization extends AuthorizationBase {
  /// Authorization constructor.
  const Authorization({
    required this.current,
    required this.officialApp,
    required this.passwordPending,
    required this.encryptedRequestsDisabled,
    required this.callRequestsDisabled,
    required this.unconfirmed,
    required this.hash,
    required this.deviceModel,
    required this.platform,
    required this.systemVersion,
    required this.apiId,
    required this.appName,
    required this.appVersion,
    required this.dateCreated,
    required this.dateActive,
    required this.ip,
    required this.country,
    required this.region,
  }) : super._();

  /// Deserialize.
  factory Authorization.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final current = _readflags.0?true(buffer);
    // final officialApp = _readflags.1?true(buffer);
    // final passwordPending = _readflags.2?true(buffer);
    // final encryptedRequestsDisabled = _readflags.3?true(buffer);
    // final callRequestsDisabled = _readflags.4?true(buffer);
    // final unconfirmed = _readflags.5?true(buffer);
    // final hash = _readlong(buffer);
    // final deviceModel = _readstring(buffer);
    // final platform = _readstring(buffer);
    // final systemVersion = _readstring(buffer);
    // final apiId = _readint(buffer);
    // final appName = _readstring(buffer);
    // final appVersion = _readstring(buffer);
    // final dateCreated = _readint(buffer);
    // final dateActive = _readint(buffer);
    // final ip = _readstring(buffer);
    // final country = _readstring(buffer);
    // final region = _readstring(buffer);
    // final result = Authorization(flags: flags, current: current, officialApp: officialApp, passwordPending: passwordPending, encryptedRequestsDisabled: encryptedRequestsDisabled, callRequestsDisabled: callRequestsDisabled, unconfirmed: unconfirmed, hash: hash, deviceModel: deviceModel, platform: platform, systemVersion: systemVersion, apiId: apiId, appName: appName, appVersion: appVersion, dateCreated: dateCreated, dateActive: dateActive, ip: ip, country: country, region: region,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: current,
      b01: officialApp,
      b02: passwordPending,
      b03: encryptedRequestsDisabled,
      b04: callRequestsDisabled,
      b05: unconfirmed,
    );

    return v;
  }

  /// current: bit 0 of flags.0?true
  final bool current;

  /// official_app: bit 1 of flags.1?true
  final bool officialApp;

  /// password_pending: bit 2 of flags.2?true
  final bool passwordPending;

  /// encrypted_requests_disabled: bit 3 of flags.3?true
  final bool encryptedRequestsDisabled;

  /// call_requests_disabled: bit 4 of flags.4?true
  final bool callRequestsDisabled;

  /// unconfirmed: bit 5 of flags.5?true
  final bool unconfirmed;

  /// Hash.
  final int hash;

  /// Device Model.
  final String deviceModel;

  /// Platform.
  final String platform;

  /// System Version.
  final String systemVersion;

  /// Api Id.
  final int apiId;

  /// App Name.
  final String appName;

  /// App Version.
  final String appVersion;

  /// Date Created.
  final DateTime dateCreated;

  /// Date Active.
  final DateTime dateActive;

  /// Ip.
  final String ip;

  /// Country.
  final String country;

  /// Region.
  final String region;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xad01d61d);
    buffer.writeInt(flags);
    buffer.writeLong(hash);
    buffer.writeString(deviceModel);
    buffer.writeString(platform);
    buffer.writeString(systemVersion);
    buffer.writeInt(apiId);
    buffer.writeString(appName);
    buffer.writeString(appVersion);
    buffer.writeDateTime(dateCreated);
    buffer.writeDateTime(dateActive);
    buffer.writeString(ip);
    buffer.writeString(country);
    buffer.writeString(region);
  }
}

/// Predicate Account Authorizations.
abstract class AccountAuthorizationsBase extends TlConstructor {
  /// Predicate Account Authorizations constructor.
  const AccountAuthorizationsBase._() : super._();
}

/// Account Authorizations.
///
/// ID: `4bff8ea0`.
class AccountAuthorizations extends AccountAuthorizationsBase {
  /// Account Authorizations constructor.
  const AccountAuthorizations({
    required this.authorizationTtlDays,
    required this.authorizations,
  }) : super._();

  /// Deserialize.
  factory AccountAuthorizations.deserialize(Uint8List buffer) {
    // final authorizationTtlDays = _readint(buffer);
    // final authorizations = _readVector<Authorization>(buffer);
    // final result = AccountAuthorizations(authorizationTtlDays: authorizationTtlDays, authorizations: authorizations,);

    // return result;

    throw Exception();
  }

  /// Authorization Ttl Days.
  final int authorizationTtlDays;

  /// Authorizations.
  final List<AuthorizationBase> authorizations;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4bff8ea0);
    buffer.writeInt(authorizationTtlDays);
    buffer.writeVectorObject(authorizations);
  }
}

/// Predicate Account Password.
abstract class AccountPasswordBase extends TlConstructor {
  /// Predicate Account Password constructor.
  const AccountPasswordBase._() : super._();
}

/// Account Password.
///
/// ID: `957b50fb`.
class AccountPassword extends AccountPasswordBase {
  /// Account Password constructor.
  const AccountPassword({
    required this.hasRecovery,
    required this.hasSecureValues,
    required this.hasPassword,
    this.currentAlgo,
    this.srpB,
    this.srpId,
    this.hint,
    this.emailUnconfirmedPattern,
    required this.newAlgo,
    required this.newSecureAlgo,
    required this.secureRandom,
    this.pendingResetDate,
    this.loginEmailPattern,
  }) : super._();

  /// Deserialize.
  factory AccountPassword.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final hasRecovery = _readflags.0?true(buffer);
    // final hasSecureValues = _readflags.1?true(buffer);
    // final hasPassword = _readflags.2?true(buffer);
    // final currentAlgo = _readflags.2?PasswordKdfAlgo(buffer);
    // final srpB = _readflags.2?bytes(buffer);
    // final srpId = _readflags.2?long(buffer);
    // final hint = _readflags.3?string(buffer);
    // final emailUnconfirmedPattern = _readflags.4?string(buffer);
    // final newAlgo = _readPasswordKdfAlgo(buffer);
    // final newSecureAlgo = _readSecurePasswordKdfAlgo(buffer);
    // final secureRandom = _readbytes(buffer);
    // final pendingResetDate = _readflags.5?int(buffer);
    // final loginEmailPattern = _readflags.6?string(buffer);
    // final result = AccountPassword(flags: flags, hasRecovery: hasRecovery, hasSecureValues: hasSecureValues, hasPassword: hasPassword, currentAlgo: currentAlgo, srpB: srpB, srpId: srpId, hint: hint, emailUnconfirmedPattern: emailUnconfirmedPattern, newAlgo: newAlgo, newSecureAlgo: newSecureAlgo, secureRandom: secureRandom, pendingResetDate: pendingResetDate, loginEmailPattern: loginEmailPattern,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: hasRecovery,
      b01: hasSecureValues,
      b02: hasPassword || currentAlgo != null || srpB != null || srpId != null,
      b03: hint != null,
      b04: emailUnconfirmedPattern != null,
      b05: pendingResetDate != null,
      b06: loginEmailPattern != null,
    );

    return v;
  }

  /// has_recovery: bit 0 of flags.0?true
  final bool hasRecovery;

  /// has_secure_values: bit 1 of flags.1?true
  final bool hasSecureValues;

  /// has_password: bit 2 of flags.2?true
  final bool hasPassword;

  /// Current Algo.
  final PasswordKdfAlgoBase? currentAlgo;

  /// Srp B.
  final Uint8List? srpB;

  /// Srp Id.
  final int? srpId;

  /// Hint.
  final String? hint;

  /// Email Unconfirmed Pattern.
  final String? emailUnconfirmedPattern;

  /// New Algo.
  final PasswordKdfAlgoBase newAlgo;

  /// New Secure Algo.
  final SecurePasswordKdfAlgoBase newSecureAlgo;

  /// Secure Random.
  final Uint8List secureRandom;

  /// Pending Reset Date.
  final DateTime? pendingResetDate;

  /// Login Email Pattern.
  final String? loginEmailPattern;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x957b50fb);
    buffer.writeInt(flags);
    final localCurrentAlgoCopy = currentAlgo;
    if (localCurrentAlgoCopy != null) {
      buffer.writeObject(localCurrentAlgoCopy);
    }
    final localSrpBCopy = srpB;
    if (localSrpBCopy != null) {
      buffer.writeBytes(localSrpBCopy);
    }
    final localSrpIdCopy = srpId;
    if (localSrpIdCopy != null) {
      buffer.writeLong(localSrpIdCopy);
    }
    final localHintCopy = hint;
    if (localHintCopy != null) {
      buffer.writeString(localHintCopy);
    }
    final localEmailUnconfirmedPatternCopy = emailUnconfirmedPattern;
    if (localEmailUnconfirmedPatternCopy != null) {
      buffer.writeString(localEmailUnconfirmedPatternCopy);
    }
    buffer.writeObject(newAlgo);
    buffer.writeObject(newSecureAlgo);
    buffer.writeBytes(secureRandom);
    final localPendingResetDateCopy = pendingResetDate;
    if (localPendingResetDateCopy != null) {
      buffer.writeDateTime(localPendingResetDateCopy);
    }
    final localLoginEmailPatternCopy = loginEmailPattern;
    if (localLoginEmailPatternCopy != null) {
      buffer.writeString(localLoginEmailPatternCopy);
    }
  }
}

/// Predicate Account Password Settings.
abstract class AccountPasswordSettingsBase extends TlConstructor {
  /// Predicate Account Password Settings constructor.
  const AccountPasswordSettingsBase._() : super._();
}

/// Account Password Settings.
///
/// ID: `9a5c33e5`.
class AccountPasswordSettings extends AccountPasswordSettingsBase {
  /// Account Password Settings constructor.
  const AccountPasswordSettings({
    this.email,
    this.secureSettings,
  }) : super._();

  /// Deserialize.
  factory AccountPasswordSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final email = _readflags.0?string(buffer);
    // final secureSettings = _readflags.1?SecureSecretSettings(buffer);
    // final result = AccountPasswordSettings(flags: flags, email: email, secureSettings: secureSettings,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: email != null,
      b01: secureSettings != null,
    );

    return v;
  }

  /// Email.
  final String? email;

  /// Secure Settings.
  final SecureSecretSettingsBase? secureSettings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9a5c33e5);
    buffer.writeInt(flags);
    final localEmailCopy = email;
    if (localEmailCopy != null) {
      buffer.writeString(localEmailCopy);
    }
    final localSecureSettingsCopy = secureSettings;
    if (localSecureSettingsCopy != null) {
      buffer.writeObject(localSecureSettingsCopy);
    }
  }
}

/// Predicate Account Password Input Settings.
abstract class AccountPasswordInputSettingsBase extends TlConstructor {
  /// Predicate Account Password Input Settings constructor.
  const AccountPasswordInputSettingsBase._() : super._();
}

/// Account Password Input Settings.
///
/// ID: `c23727c9`.
class AccountPasswordInputSettings extends AccountPasswordInputSettingsBase {
  /// Account Password Input Settings constructor.
  const AccountPasswordInputSettings({
    this.newAlgo,
    this.newPasswordHash,
    this.hint,
    this.email,
    this.newSecureSettings,
  }) : super._();

  /// Deserialize.
  factory AccountPasswordInputSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final newAlgo = _readflags.0?PasswordKdfAlgo(buffer);
    // final newPasswordHash = _readflags.0?bytes(buffer);
    // final hint = _readflags.0?string(buffer);
    // final email = _readflags.1?string(buffer);
    // final newSecureSettings = _readflags.2?SecureSecretSettings(buffer);
    // final result = AccountPasswordInputSettings(flags: flags, newAlgo: newAlgo, newPasswordHash: newPasswordHash, hint: hint, email: email, newSecureSettings: newSecureSettings,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: newAlgo != null || newPasswordHash != null || hint != null,
      b01: email != null,
      b02: newSecureSettings != null,
    );

    return v;
  }

  /// New Algo.
  final PasswordKdfAlgoBase? newAlgo;

  /// New Password Hash.
  final Uint8List? newPasswordHash;

  /// Hint.
  final String? hint;

  /// Email.
  final String? email;

  /// New Secure Settings.
  final SecureSecretSettingsBase? newSecureSettings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc23727c9);
    buffer.writeInt(flags);
    final localNewAlgoCopy = newAlgo;
    if (localNewAlgoCopy != null) {
      buffer.writeObject(localNewAlgoCopy);
    }
    final localNewPasswordHashCopy = newPasswordHash;
    if (localNewPasswordHashCopy != null) {
      buffer.writeBytes(localNewPasswordHashCopy);
    }
    final localHintCopy = hint;
    if (localHintCopy != null) {
      buffer.writeString(localHintCopy);
    }
    final localEmailCopy = email;
    if (localEmailCopy != null) {
      buffer.writeString(localEmailCopy);
    }
    final localNewSecureSettingsCopy = newSecureSettings;
    if (localNewSecureSettingsCopy != null) {
      buffer.writeObject(localNewSecureSettingsCopy);
    }
  }
}

/// Predicate Auth Password Recovery.
abstract class AuthPasswordRecoveryBase extends TlConstructor {
  /// Predicate Auth Password Recovery constructor.
  const AuthPasswordRecoveryBase._() : super._();
}

/// Auth Password Recovery.
///
/// ID: `137948a5`.
class AuthPasswordRecovery extends AuthPasswordRecoveryBase {
  /// Auth Password Recovery constructor.
  const AuthPasswordRecovery({
    required this.emailPattern,
  }) : super._();

  /// Deserialize.
  factory AuthPasswordRecovery.deserialize(Uint8List buffer) {
    // final emailPattern = _readstring(buffer);
    // final result = AuthPasswordRecovery(emailPattern: emailPattern,);

    // return result;

    throw Exception();
  }

  /// Email Pattern.
  final String emailPattern;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x137948a5);
    buffer.writeString(emailPattern);
  }
}

/// Predicate Received Notify Message.
abstract class ReceivedNotifyMessageBase extends TlConstructor {
  /// Predicate Received Notify Message constructor.
  const ReceivedNotifyMessageBase._() : super._();
}

/// Received Notify Message.
///
/// ID: `a384b779`.
class ReceivedNotifyMessage extends ReceivedNotifyMessageBase {
  /// Received Notify Message constructor.
  const ReceivedNotifyMessage({
    required this.id,
    required this.flags,
  }) : super._();

  /// Deserialize.
  factory ReceivedNotifyMessage.deserialize(Uint8List buffer) {
    // final id = _readint(buffer);
    // final flags = _readint(buffer);
    // final result = ReceivedNotifyMessage(id: id, flags: flags,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Flags.
  final int flags;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa384b779);
    buffer.writeInt(id);
    buffer.writeInt(flags);
  }
}

/// Predicate Exported Chat Invite.
abstract class ExportedChatInviteBase extends TlConstructor {
  /// Predicate Exported Chat Invite constructor.
  const ExportedChatInviteBase._() : super._();
}

/// Chat Invite Exported.
///
/// ID: `0ab4a819`.
class ChatInviteExported extends ExportedChatInviteBase {
  /// Chat Invite Exported constructor.
  const ChatInviteExported({
    required this.revoked,
    required this.permanent,
    required this.requestNeeded,
    required this.link,
    required this.adminId,
    required this.date,
    this.startDate,
    this.expireDate,
    this.usageLimit,
    this.usage,
    this.requested,
    this.title,
  }) : super._();

  /// Deserialize.
  factory ChatInviteExported.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final revoked = _readflags.0?true(buffer);
    // final permanent = _readflags.5?true(buffer);
    // final requestNeeded = _readflags.6?true(buffer);
    // final link = _readstring(buffer);
    // final adminId = _readlong(buffer);
    // final date = _readint(buffer);
    // final startDate = _readflags.4?int(buffer);
    // final expireDate = _readflags.1?int(buffer);
    // final usageLimit = _readflags.2?int(buffer);
    // final usage = _readflags.3?int(buffer);
    // final requested = _readflags.7?int(buffer);
    // final title = _readflags.8?string(buffer);
    // final result = ChatInviteExported(flags: flags, revoked: revoked, permanent: permanent, requestNeeded: requestNeeded, link: link, adminId: adminId, date: date, startDate: startDate, expireDate: expireDate, usageLimit: usageLimit, usage: usage, requested: requested, title: title,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: revoked,
      b05: permanent,
      b06: requestNeeded,
      b04: startDate != null,
      b01: expireDate != null,
      b02: usageLimit != null,
      b03: usage != null,
      b07: requested != null,
      b08: title != null,
    );

    return v;
  }

  /// revoked: bit 0 of flags.0?true
  final bool revoked;

  /// permanent: bit 5 of flags.5?true
  final bool permanent;

  /// request_needed: bit 6 of flags.6?true
  final bool requestNeeded;

  /// Link.
  final String link;

  /// Admin Id.
  final int adminId;

  /// Date.
  final DateTime date;

  /// Start Date.
  final DateTime? startDate;

  /// Expire Date.
  final DateTime? expireDate;

  /// Usage Limit.
  final int? usageLimit;

  /// Usage.
  final int? usage;

  /// Requested.
  final int? requested;

  /// Title.
  final String? title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0ab4a819);
    buffer.writeInt(flags);
    buffer.writeString(link);
    buffer.writeLong(adminId);
    buffer.writeDateTime(date);
    final localStartDateCopy = startDate;
    if (localStartDateCopy != null) {
      buffer.writeDateTime(localStartDateCopy);
    }
    final localExpireDateCopy = expireDate;
    if (localExpireDateCopy != null) {
      buffer.writeDateTime(localExpireDateCopy);
    }
    final localUsageLimitCopy = usageLimit;
    if (localUsageLimitCopy != null) {
      buffer.writeInt(localUsageLimitCopy);
    }
    final localUsageCopy = usage;
    if (localUsageCopy != null) {
      buffer.writeInt(localUsageCopy);
    }
    final localRequestedCopy = requested;
    if (localRequestedCopy != null) {
      buffer.writeInt(localRequestedCopy);
    }
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
  }
}

/// Chat Invite Public Join Requests.
///
/// ID: `ed107ab7`.
class ChatInvitePublicJoinRequests extends ExportedChatInviteBase {
  /// Chat Invite Public Join Requests constructor.
  const ChatInvitePublicJoinRequests() : super._();

  /// Deserialize.
  factory ChatInvitePublicJoinRequests.deserialize(Uint8List buffer) {
    // final result = ChatInvitePublicJoinRequests();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xed107ab7);
  }
}

/// Predicate Chat Invite.
abstract class ChatInviteBase extends TlConstructor {
  /// Predicate Chat Invite constructor.
  const ChatInviteBase._() : super._();
}

/// Chat Invite Already.
///
/// ID: `5a686d7c`.
class ChatInviteAlready extends ChatInviteBase {
  /// Chat Invite Already constructor.
  const ChatInviteAlready({
    required this.chat,
  }) : super._();

  /// Deserialize.
  factory ChatInviteAlready.deserialize(Uint8List buffer) {
    // final chat = _readChat(buffer);
    // final result = ChatInviteAlready(chat: chat,);

    // return result;

    throw Exception();
  }

  /// Chat.
  final ChatBase chat;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5a686d7c);
    buffer.writeObject(chat);
  }
}

/// Chat Invite.
///
/// ID: `cde0ec40`.
class ChatInvite extends ChatInviteBase {
  /// Chat Invite constructor.
  const ChatInvite({
    required this.channel,
    required this.broadcast,
    required this.public,
    required this.megagroup,
    required this.requestNeeded,
    required this.verified,
    required this.scam,
    required this.fake,
    required this.title,
    this.about,
    required this.photo,
    required this.participantsCount,
    this.participants,
    required this.color,
  }) : super._();

  /// Deserialize.
  factory ChatInvite.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final channel = _readflags.0?true(buffer);
    // final broadcast = _readflags.1?true(buffer);
    // final public = _readflags.2?true(buffer);
    // final megagroup = _readflags.3?true(buffer);
    // final requestNeeded = _readflags.6?true(buffer);
    // final verified = _readflags.7?true(buffer);
    // final scam = _readflags.8?true(buffer);
    // final fake = _readflags.9?true(buffer);
    // final title = _readstring(buffer);
    // final about = _readflags.5?string(buffer);
    // final photo = _readPhoto(buffer);
    // final participantsCount = _readint(buffer);
    // final participants = _readflags.4?Vector<User>(buffer);
    // final color = _readint(buffer);
    // final result = ChatInvite(flags: flags, channel: channel, broadcast: broadcast, public: public, megagroup: megagroup, requestNeeded: requestNeeded, verified: verified, scam: scam, fake: fake, title: title, about: about, photo: photo, participantsCount: participantsCount, participants: participants, color: color,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: channel,
      b01: broadcast,
      b02: public,
      b03: megagroup,
      b06: requestNeeded,
      b07: verified,
      b08: scam,
      b09: fake,
      b05: about != null,
      b04: participants != null,
    );

    return v;
  }

  /// channel: bit 0 of flags.0?true
  final bool channel;

  /// broadcast: bit 1 of flags.1?true
  final bool broadcast;

  /// public: bit 2 of flags.2?true
  final bool public;

  /// megagroup: bit 3 of flags.3?true
  final bool megagroup;

  /// request_needed: bit 6 of flags.6?true
  final bool requestNeeded;

  /// verified: bit 7 of flags.7?true
  final bool verified;

  /// scam: bit 8 of flags.8?true
  final bool scam;

  /// fake: bit 9 of flags.9?true
  final bool fake;

  /// Title.
  final String title;

  /// About.
  final String? about;

  /// Photo.
  final PhotoBase photo;

  /// Participants Count.
  final int participantsCount;

  /// Participants.
  final List<UserBase>? participants;

  /// Color.
  final int color;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcde0ec40);
    buffer.writeInt(flags);
    buffer.writeString(title);
    final localAboutCopy = about;
    if (localAboutCopy != null) {
      buffer.writeString(localAboutCopy);
    }
    buffer.writeObject(photo);
    buffer.writeInt(participantsCount);
    final localParticipantsCopy = participants;
    if (localParticipantsCopy != null) {
      buffer.writeVectorObject(localParticipantsCopy);
    }
    buffer.writeInt(color);
  }
}

/// Chat Invite Peek.
///
/// ID: `61695cb0`.
class ChatInvitePeek extends ChatInviteBase {
  /// Chat Invite Peek constructor.
  const ChatInvitePeek({
    required this.chat,
    required this.expires,
  }) : super._();

  /// Deserialize.
  factory ChatInvitePeek.deserialize(Uint8List buffer) {
    // final chat = _readChat(buffer);
    // final expires = _readint(buffer);
    // final result = ChatInvitePeek(chat: chat, expires: expires,);

    // return result;

    throw Exception();
  }

  /// Chat.
  final ChatBase chat;

  /// Expires.
  final int expires;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x61695cb0);
    buffer.writeObject(chat);
    buffer.writeInt(expires);
  }
}

/// Predicate Input Sticker Set.
abstract class InputStickerSetBase extends TlConstructor {
  /// Predicate Input Sticker Set constructor.
  const InputStickerSetBase._() : super._();
}

/// Input Sticker Set Empty.
///
/// ID: `ffb62b95`.
class InputStickerSetEmpty extends InputStickerSetBase {
  /// Input Sticker Set Empty constructor.
  const InputStickerSetEmpty() : super._();

  /// Deserialize.
  factory InputStickerSetEmpty.deserialize(Uint8List buffer) {
    // final result = InputStickerSetEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xffb62b95);
  }
}

/// Input Sticker Set I D.
///
/// ID: `9de7a269`.
class InputStickerSetID extends InputStickerSetBase {
  /// Input Sticker Set I D constructor.
  const InputStickerSetID({
    required this.id,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputStickerSetID.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputStickerSetID(id: id, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9de7a269);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
  }
}

/// Input Sticker Set Short Name.
///
/// ID: `861cc8a0`.
class InputStickerSetShortName extends InputStickerSetBase {
  /// Input Sticker Set Short Name constructor.
  const InputStickerSetShortName({
    required this.shortName,
  }) : super._();

  /// Deserialize.
  factory InputStickerSetShortName.deserialize(Uint8List buffer) {
    // final shortName = _readstring(buffer);
    // final result = InputStickerSetShortName(shortName: shortName,);

    // return result;

    throw Exception();
  }

  /// Short Name.
  final String shortName;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x861cc8a0);
    buffer.writeString(shortName);
  }
}

/// Input Sticker Set Animated Emoji.
///
/// ID: `028703c8`.
class InputStickerSetAnimatedEmoji extends InputStickerSetBase {
  /// Input Sticker Set Animated Emoji constructor.
  const InputStickerSetAnimatedEmoji() : super._();

  /// Deserialize.
  factory InputStickerSetAnimatedEmoji.deserialize(Uint8List buffer) {
    // final result = InputStickerSetAnimatedEmoji();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x028703c8);
  }
}

/// Input Sticker Set Dice.
///
/// ID: `e67f520e`.
class InputStickerSetDice extends InputStickerSetBase {
  /// Input Sticker Set Dice constructor.
  const InputStickerSetDice({
    required this.emoticon,
  }) : super._();

  /// Deserialize.
  factory InputStickerSetDice.deserialize(Uint8List buffer) {
    // final emoticon = _readstring(buffer);
    // final result = InputStickerSetDice(emoticon: emoticon,);

    // return result;

    throw Exception();
  }

  /// Emoticon.
  final String emoticon;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe67f520e);
    buffer.writeString(emoticon);
  }
}

/// Input Sticker Set Animated Emoji Animations.
///
/// ID: `0cde3739`.
class InputStickerSetAnimatedEmojiAnimations extends InputStickerSetBase {
  /// Input Sticker Set Animated Emoji Animations constructor.
  const InputStickerSetAnimatedEmojiAnimations() : super._();

  /// Deserialize.
  factory InputStickerSetAnimatedEmojiAnimations.deserialize(Uint8List buffer) {
    // final result = InputStickerSetAnimatedEmojiAnimations();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0cde3739);
  }
}

/// Input Sticker Set Premium Gifts.
///
/// ID: `c88b3b02`.
class InputStickerSetPremiumGifts extends InputStickerSetBase {
  /// Input Sticker Set Premium Gifts constructor.
  const InputStickerSetPremiumGifts() : super._();

  /// Deserialize.
  factory InputStickerSetPremiumGifts.deserialize(Uint8List buffer) {
    // final result = InputStickerSetPremiumGifts();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc88b3b02);
  }
}

/// Input Sticker Set Emoji Generic Animations.
///
/// ID: `04c4d4ce`.
class InputStickerSetEmojiGenericAnimations extends InputStickerSetBase {
  /// Input Sticker Set Emoji Generic Animations constructor.
  const InputStickerSetEmojiGenericAnimations() : super._();

  /// Deserialize.
  factory InputStickerSetEmojiGenericAnimations.deserialize(Uint8List buffer) {
    // final result = InputStickerSetEmojiGenericAnimations();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x04c4d4ce);
  }
}

/// Input Sticker Set Emoji Default Statuses.
///
/// ID: `29d0f5ee`.
class InputStickerSetEmojiDefaultStatuses extends InputStickerSetBase {
  /// Input Sticker Set Emoji Default Statuses constructor.
  const InputStickerSetEmojiDefaultStatuses() : super._();

  /// Deserialize.
  factory InputStickerSetEmojiDefaultStatuses.deserialize(Uint8List buffer) {
    // final result = InputStickerSetEmojiDefaultStatuses();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x29d0f5ee);
  }
}

/// Input Sticker Set Emoji Default Topic Icons.
///
/// ID: `44c1f8e9`.
class InputStickerSetEmojiDefaultTopicIcons extends InputStickerSetBase {
  /// Input Sticker Set Emoji Default Topic Icons constructor.
  const InputStickerSetEmojiDefaultTopicIcons() : super._();

  /// Deserialize.
  factory InputStickerSetEmojiDefaultTopicIcons.deserialize(Uint8List buffer) {
    // final result = InputStickerSetEmojiDefaultTopicIcons();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x44c1f8e9);
  }
}

/// Input Sticker Set Emoji Channel Default Statuses.
///
/// ID: `49748553`.
class InputStickerSetEmojiChannelDefaultStatuses extends InputStickerSetBase {
  /// Input Sticker Set Emoji Channel Default Statuses constructor.
  const InputStickerSetEmojiChannelDefaultStatuses() : super._();

  /// Deserialize.
  factory InputStickerSetEmojiChannelDefaultStatuses.deserialize(
      Uint8List buffer) {
    // final result = InputStickerSetEmojiChannelDefaultStatuses();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x49748553);
  }
}

/// Predicate Sticker Set.
abstract class StickerSetBase extends TlConstructor {
  /// Predicate Sticker Set constructor.
  const StickerSetBase._() : super._();
}

/// Sticker Set.
///
/// ID: `2dd14edc`.
class StickerSet extends StickerSetBase {
  /// Sticker Set constructor.
  const StickerSet({
    required this.archived,
    required this.official,
    required this.masks,
    required this.animated,
    required this.videos,
    required this.emojis,
    required this.textColor,
    required this.channelEmojiStatus,
    this.installedDate,
    required this.id,
    required this.accessHash,
    required this.title,
    required this.shortName,
    this.thumbs,
    this.thumbDcId,
    this.thumbVersion,
    this.thumbDocumentId,
    required this.count,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory StickerSet.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final archived = _readflags.1?true(buffer);
    // final official = _readflags.2?true(buffer);
    // final masks = _readflags.3?true(buffer);
    // final animated = _readflags.5?true(buffer);
    // final videos = _readflags.6?true(buffer);
    // final emojis = _readflags.7?true(buffer);
    // final textColor = _readflags.9?true(buffer);
    // final channelEmojiStatus = _readflags.10?true(buffer);
    // final installedDate = _readflags.0?int(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final title = _readstring(buffer);
    // final shortName = _readstring(buffer);
    // final thumbs = _readflags.4?Vector<PhotoSize>(buffer);
    // final thumbDcId = _readflags.4?int(buffer);
    // final thumbVersion = _readflags.4?int(buffer);
    // final thumbDocumentId = _readflags.8?long(buffer);
    // final count = _readint(buffer);
    // final hash = _readint(buffer);
    // final result = StickerSet(flags: flags, archived: archived, official: official, masks: masks, animated: animated, videos: videos, emojis: emojis, textColor: textColor, channelEmojiStatus: channelEmojiStatus, installedDate: installedDate, id: id, accessHash: accessHash, title: title, shortName: shortName, thumbs: thumbs, thumbDcId: thumbDcId, thumbVersion: thumbVersion, thumbDocumentId: thumbDocumentId, count: count, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: archived,
      b02: official,
      b03: masks,
      b05: animated,
      b06: videos,
      b07: emojis,
      b09: textColor,
      b10: channelEmojiStatus,
      b00: installedDate != null,
      b04: thumbs != null || thumbDcId != null || thumbVersion != null,
      b08: thumbDocumentId != null,
    );

    return v;
  }

  /// archived: bit 1 of flags.1?true
  final bool archived;

  /// official: bit 2 of flags.2?true
  final bool official;

  /// masks: bit 3 of flags.3?true
  final bool masks;

  /// animated: bit 5 of flags.5?true
  final bool animated;

  /// videos: bit 6 of flags.6?true
  final bool videos;

  /// emojis: bit 7 of flags.7?true
  final bool emojis;

  /// text_color: bit 9 of flags.9?true
  final bool textColor;

  /// channel_emoji_status: bit 10 of flags.10?true
  final bool channelEmojiStatus;

  /// Installed Date.
  final DateTime? installedDate;

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Title.
  final String title;

  /// Short Name.
  final String shortName;

  /// Thumbs.
  final List<PhotoSizeBase>? thumbs;

  /// Thumb Dc Id.
  final int? thumbDcId;

  /// Thumb Version.
  final int? thumbVersion;

  /// Thumb Document Id.
  final int? thumbDocumentId;

  /// Count.
  final int count;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2dd14edc);
    buffer.writeInt(flags);
    final localInstalledDateCopy = installedDate;
    if (localInstalledDateCopy != null) {
      buffer.writeDateTime(localInstalledDateCopy);
    }
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeString(title);
    buffer.writeString(shortName);
    final localThumbsCopy = thumbs;
    if (localThumbsCopy != null) {
      buffer.writeVectorObject(localThumbsCopy);
    }
    final localThumbDcIdCopy = thumbDcId;
    if (localThumbDcIdCopy != null) {
      buffer.writeInt(localThumbDcIdCopy);
    }
    final localThumbVersionCopy = thumbVersion;
    if (localThumbVersionCopy != null) {
      buffer.writeInt(localThumbVersionCopy);
    }
    final localThumbDocumentIdCopy = thumbDocumentId;
    if (localThumbDocumentIdCopy != null) {
      buffer.writeLong(localThumbDocumentIdCopy);
    }
    buffer.writeInt(count);
    buffer.writeInt(hash);
  }
}

/// Predicate Messages Sticker Set.
abstract class MessagesStickerSetBase extends TlConstructor {
  /// Predicate Messages Sticker Set constructor.
  const MessagesStickerSetBase._() : super._();
}

/// Messages Sticker Set.
///
/// ID: `6e153f16`.
class MessagesStickerSet extends MessagesStickerSetBase {
  /// Messages Sticker Set constructor.
  const MessagesStickerSet({
    required this.set,
    required this.packs,
    required this.keywords,
    required this.documents,
  }) : super._();

  /// Deserialize.
  factory MessagesStickerSet.deserialize(Uint8List buffer) {
    // final set = _readStickerSet(buffer);
    // final packs = _readVector<StickerPack>(buffer);
    // final keywords = _readVector<StickerKeyword>(buffer);
    // final documents = _readVector<Document>(buffer);
    // final result = MessagesStickerSet(set: set, packs: packs, keywords: keywords, documents: documents,);

    // return result;

    throw Exception();
  }

  /// Set.
  final StickerSetBase set;

  /// Packs.
  final List<StickerPackBase> packs;

  /// Keywords.
  final List<StickerKeywordBase> keywords;

  /// Documents.
  final List<DocumentBase> documents;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6e153f16);
    buffer.writeObject(set);
    buffer.writeVectorObject(packs);
    buffer.writeVectorObject(keywords);
    buffer.writeVectorObject(documents);
  }
}

/// Messages Sticker Set Not Modified.
///
/// ID: `d3f924eb`.
class MessagesStickerSetNotModified extends MessagesStickerSetBase {
  /// Messages Sticker Set Not Modified constructor.
  const MessagesStickerSetNotModified() : super._();

  /// Deserialize.
  factory MessagesStickerSetNotModified.deserialize(Uint8List buffer) {
    // final result = MessagesStickerSetNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd3f924eb);
  }
}

/// Predicate Bot Command.
abstract class BotCommandBase extends TlConstructor {
  /// Predicate Bot Command constructor.
  const BotCommandBase._() : super._();
}

/// Bot Command.
///
/// ID: `c27ac8c7`.
class BotCommand extends BotCommandBase {
  /// Bot Command constructor.
  const BotCommand({
    required this.command,
    required this.description,
  }) : super._();

  /// Deserialize.
  factory BotCommand.deserialize(Uint8List buffer) {
    // final command = _readstring(buffer);
    // final description = _readstring(buffer);
    // final result = BotCommand(command: command, description: description,);

    // return result;

    throw Exception();
  }

  /// Command.
  final String command;

  /// Description.
  final String description;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc27ac8c7);
    buffer.writeString(command);
    buffer.writeString(description);
  }
}

/// Predicate Bot Info.
abstract class BotInfoBase extends TlConstructor {
  /// Predicate Bot Info constructor.
  const BotInfoBase._() : super._();
}

/// Bot Info.
///
/// ID: `8f300b57`.
class BotInfo extends BotInfoBase {
  /// Bot Info constructor.
  const BotInfo({
    this.userId,
    this.description,
    this.descriptionPhoto,
    this.descriptionDocument,
    this.commands,
    this.menuButton,
  }) : super._();

  /// Deserialize.
  factory BotInfo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final userId = _readflags.0?long(buffer);
    // final description = _readflags.1?string(buffer);
    // final descriptionPhoto = _readflags.4?Photo(buffer);
    // final descriptionDocument = _readflags.5?Document(buffer);
    // final commands = _readflags.2?Vector<BotCommand>(buffer);
    // final menuButton = _readflags.3?BotMenuButton(buffer);
    // final result = BotInfo(flags: flags, userId: userId, description: description, descriptionPhoto: descriptionPhoto, descriptionDocument: descriptionDocument, commands: commands, menuButton: menuButton,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: userId != null,
      b01: description != null,
      b04: descriptionPhoto != null,
      b05: descriptionDocument != null,
      b02: commands != null,
      b03: menuButton != null,
    );

    return v;
  }

  /// User Id.
  final int? userId;

  /// Description.
  final String? description;

  /// Description Photo.
  final PhotoBase? descriptionPhoto;

  /// Description Document.
  final DocumentBase? descriptionDocument;

  /// Commands.
  final List<BotCommandBase>? commands;

  /// Menu Button.
  final BotMenuButtonBase? menuButton;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8f300b57);
    buffer.writeInt(flags);
    final localUserIdCopy = userId;
    if (localUserIdCopy != null) {
      buffer.writeLong(localUserIdCopy);
    }
    final localDescriptionCopy = description;
    if (localDescriptionCopy != null) {
      buffer.writeString(localDescriptionCopy);
    }
    final localDescriptionPhotoCopy = descriptionPhoto;
    if (localDescriptionPhotoCopy != null) {
      buffer.writeObject(localDescriptionPhotoCopy);
    }
    final localDescriptionDocumentCopy = descriptionDocument;
    if (localDescriptionDocumentCopy != null) {
      buffer.writeObject(localDescriptionDocumentCopy);
    }
    final localCommandsCopy = commands;
    if (localCommandsCopy != null) {
      buffer.writeVectorObject(localCommandsCopy);
    }
    final localMenuButtonCopy = menuButton;
    if (localMenuButtonCopy != null) {
      buffer.writeObject(localMenuButtonCopy);
    }
  }
}

/// Predicate Keyboard Button.
abstract class KeyboardButtonBase extends TlConstructor {
  /// Predicate Keyboard Button constructor.
  const KeyboardButtonBase._() : super._();
}

/// Keyboard Button.
///
/// ID: `a2fa4880`.
class KeyboardButton extends KeyboardButtonBase {
  /// Keyboard Button constructor.
  const KeyboardButton({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory KeyboardButton.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final result = KeyboardButton(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa2fa4880);
    buffer.writeString(text);
  }
}

/// Keyboard Button Url.
///
/// ID: `258aff05`.
class KeyboardButtonUrl extends KeyboardButtonBase {
  /// Keyboard Button Url constructor.
  const KeyboardButtonUrl({
    required this.text,
    required this.url,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonUrl.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final url = _readstring(buffer);
    // final result = KeyboardButtonUrl(text: text, url: url,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x258aff05);
    buffer.writeString(text);
    buffer.writeString(url);
  }
}

/// Keyboard Button Callback.
///
/// ID: `35bbdb6b`.
class KeyboardButtonCallback extends KeyboardButtonBase {
  /// Keyboard Button Callback constructor.
  const KeyboardButtonCallback({
    required this.requiresPassword,
    required this.text,
    required this.data,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonCallback.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final requiresPassword = _readflags.0?true(buffer);
    // final text = _readstring(buffer);
    // final data = _readbytes(buffer);
    // final result = KeyboardButtonCallback(flags: flags, requiresPassword: requiresPassword, text: text, data: data,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: requiresPassword,
    );

    return v;
  }

  /// requires_password: bit 0 of flags.0?true
  final bool requiresPassword;

  /// Text.
  final String text;

  /// Data.
  final Uint8List data;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x35bbdb6b);
    buffer.writeInt(flags);
    buffer.writeString(text);
    buffer.writeBytes(data);
  }
}

/// Keyboard Button Request Phone.
///
/// ID: `b16a6c29`.
class KeyboardButtonRequestPhone extends KeyboardButtonBase {
  /// Keyboard Button Request Phone constructor.
  const KeyboardButtonRequestPhone({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonRequestPhone.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final result = KeyboardButtonRequestPhone(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb16a6c29);
    buffer.writeString(text);
  }
}

/// Keyboard Button Request Geo Location.
///
/// ID: `fc796b3f`.
class KeyboardButtonRequestGeoLocation extends KeyboardButtonBase {
  /// Keyboard Button Request Geo Location constructor.
  const KeyboardButtonRequestGeoLocation({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonRequestGeoLocation.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final result = KeyboardButtonRequestGeoLocation(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfc796b3f);
    buffer.writeString(text);
  }
}

/// Keyboard Button Switch Inline.
///
/// ID: `93b9fbb5`.
class KeyboardButtonSwitchInline extends KeyboardButtonBase {
  /// Keyboard Button Switch Inline constructor.
  const KeyboardButtonSwitchInline({
    required this.samePeer,
    required this.text,
    required this.query,
    this.peerTypes,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonSwitchInline.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final samePeer = _readflags.0?true(buffer);
    // final text = _readstring(buffer);
    // final query = _readstring(buffer);
    // final peerTypes = _readflags.1?Vector<InlineQueryPeerType>(buffer);
    // final result = KeyboardButtonSwitchInline(flags: flags, samePeer: samePeer, text: text, query: query, peerTypes: peerTypes,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: samePeer,
      b01: peerTypes != null,
    );

    return v;
  }

  /// same_peer: bit 0 of flags.0?true
  final bool samePeer;

  /// Text.
  final String text;

  /// Query.
  final String query;

  /// Peer Types.
  final List<InlineQueryPeerTypeBase>? peerTypes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x93b9fbb5);
    buffer.writeInt(flags);
    buffer.writeString(text);
    buffer.writeString(query);
    final localPeerTypesCopy = peerTypes;
    if (localPeerTypesCopy != null) {
      buffer.writeVectorObject(localPeerTypesCopy);
    }
  }
}

/// Keyboard Button Game.
///
/// ID: `50f41ccf`.
class KeyboardButtonGame extends KeyboardButtonBase {
  /// Keyboard Button Game constructor.
  const KeyboardButtonGame({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonGame.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final result = KeyboardButtonGame(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x50f41ccf);
    buffer.writeString(text);
  }
}

/// Keyboard Button Buy.
///
/// ID: `afd93fbb`.
class KeyboardButtonBuy extends KeyboardButtonBase {
  /// Keyboard Button Buy constructor.
  const KeyboardButtonBuy({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonBuy.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final result = KeyboardButtonBuy(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xafd93fbb);
    buffer.writeString(text);
  }
}

/// Keyboard Button Url Auth.
///
/// ID: `10b78d29`.
class KeyboardButtonUrlAuth extends KeyboardButtonBase {
  /// Keyboard Button Url Auth constructor.
  const KeyboardButtonUrlAuth({
    required this.text,
    this.fwdText,
    required this.url,
    required this.buttonId,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonUrlAuth.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final text = _readstring(buffer);
    // final fwdText = _readflags.0?string(buffer);
    // final url = _readstring(buffer);
    // final buttonId = _readint(buffer);
    // final result = KeyboardButtonUrlAuth(flags: flags, text: text, fwdText: fwdText, url: url, buttonId: buttonId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: fwdText != null,
    );

    return v;
  }

  /// Text.
  final String text;

  /// Fwd Text.
  final String? fwdText;

  /// Url.
  final String url;

  /// Button Id.
  final int buttonId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x10b78d29);
    buffer.writeInt(flags);
    buffer.writeString(text);
    final localFwdTextCopy = fwdText;
    if (localFwdTextCopy != null) {
      buffer.writeString(localFwdTextCopy);
    }
    buffer.writeString(url);
    buffer.writeInt(buttonId);
  }
}

/// Input Keyboard Button Url Auth.
///
/// ID: `d02e7fd4`.
class InputKeyboardButtonUrlAuth extends KeyboardButtonBase {
  /// Input Keyboard Button Url Auth constructor.
  const InputKeyboardButtonUrlAuth({
    required this.requestWriteAccess,
    required this.text,
    this.fwdText,
    required this.url,
    required this.bot,
  }) : super._();

  /// Deserialize.
  factory InputKeyboardButtonUrlAuth.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final requestWriteAccess = _readflags.0?true(buffer);
    // final text = _readstring(buffer);
    // final fwdText = _readflags.1?string(buffer);
    // final url = _readstring(buffer);
    // final bot = _readInputUser(buffer);
    // final result = InputKeyboardButtonUrlAuth(flags: flags, requestWriteAccess: requestWriteAccess, text: text, fwdText: fwdText, url: url, bot: bot,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: requestWriteAccess,
      b01: fwdText != null,
    );

    return v;
  }

  /// request_write_access: bit 0 of flags.0?true
  final bool requestWriteAccess;

  /// Text.
  final String text;

  /// Fwd Text.
  final String? fwdText;

  /// Url.
  final String url;

  /// Bot.
  final InputUserBase bot;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd02e7fd4);
    buffer.writeInt(flags);
    buffer.writeString(text);
    final localFwdTextCopy = fwdText;
    if (localFwdTextCopy != null) {
      buffer.writeString(localFwdTextCopy);
    }
    buffer.writeString(url);
    buffer.writeObject(bot);
  }
}

/// Keyboard Button Request Poll.
///
/// ID: `bbc7515d`.
class KeyboardButtonRequestPoll extends KeyboardButtonBase {
  /// Keyboard Button Request Poll constructor.
  const KeyboardButtonRequestPoll({
    this.quiz,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonRequestPoll.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final quiz = _readflags.0?Bool(buffer);
    // final text = _readstring(buffer);
    // final result = KeyboardButtonRequestPoll(flags: flags, quiz: quiz, text: text,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: quiz != null,
    );

    return v;
  }

  /// Quiz.
  final bool? quiz;

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbbc7515d);
    buffer.writeInt(flags);
    final localQuizCopy = quiz;
    if (localQuizCopy != null) {
      buffer.writeBool(localQuizCopy);
    }
    buffer.writeString(text);
  }
}

/// Input Keyboard Button User Profile.
///
/// ID: `e988037b`.
class InputKeyboardButtonUserProfile extends KeyboardButtonBase {
  /// Input Keyboard Button User Profile constructor.
  const InputKeyboardButtonUserProfile({
    required this.text,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory InputKeyboardButtonUserProfile.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final userId = _readInputUser(buffer);
    // final result = InputKeyboardButtonUserProfile(text: text, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// User Id.
  final InputUserBase userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe988037b);
    buffer.writeString(text);
    buffer.writeObject(userId);
  }
}

/// Keyboard Button User Profile.
///
/// ID: `308660c1`.
class KeyboardButtonUserProfile extends KeyboardButtonBase {
  /// Keyboard Button User Profile constructor.
  const KeyboardButtonUserProfile({
    required this.text,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonUserProfile.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final userId = _readlong(buffer);
    // final result = KeyboardButtonUserProfile(text: text, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// User Id.
  final int userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x308660c1);
    buffer.writeString(text);
    buffer.writeLong(userId);
  }
}

/// Keyboard Button Web View.
///
/// ID: `13767230`.
class KeyboardButtonWebView extends KeyboardButtonBase {
  /// Keyboard Button Web View constructor.
  const KeyboardButtonWebView({
    required this.text,
    required this.url,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonWebView.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final url = _readstring(buffer);
    // final result = KeyboardButtonWebView(text: text, url: url,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x13767230);
    buffer.writeString(text);
    buffer.writeString(url);
  }
}

/// Keyboard Button Simple Web View.
///
/// ID: `a0c0505c`.
class KeyboardButtonSimpleWebView extends KeyboardButtonBase {
  /// Keyboard Button Simple Web View constructor.
  const KeyboardButtonSimpleWebView({
    required this.text,
    required this.url,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonSimpleWebView.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final url = _readstring(buffer);
    // final result = KeyboardButtonSimpleWebView(text: text, url: url,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa0c0505c);
    buffer.writeString(text);
    buffer.writeString(url);
  }
}

/// Keyboard Button Request Peer.
///
/// ID: `53d7bfd8`.
class KeyboardButtonRequestPeer extends KeyboardButtonBase {
  /// Keyboard Button Request Peer constructor.
  const KeyboardButtonRequestPeer({
    required this.text,
    required this.buttonId,
    required this.peerType,
    required this.maxQuantity,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonRequestPeer.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final buttonId = _readint(buffer);
    // final peerType = _readRequestPeerType(buffer);
    // final maxQuantity = _readint(buffer);
    // final result = KeyboardButtonRequestPeer(text: text, buttonId: buttonId, peerType: peerType, maxQuantity: maxQuantity,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Button Id.
  final int buttonId;

  /// Peer Type.
  final RequestPeerTypeBase peerType;

  /// Max Quantity.
  final int maxQuantity;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x53d7bfd8);
    buffer.writeString(text);
    buffer.writeInt(buttonId);
    buffer.writeObject(peerType);
    buffer.writeInt(maxQuantity);
  }
}

/// Predicate Keyboard Button Row.
abstract class KeyboardButtonRowBase extends TlConstructor {
  /// Predicate Keyboard Button Row constructor.
  const KeyboardButtonRowBase._() : super._();
}

/// Keyboard Button Row.
///
/// ID: `77608b83`.
class KeyboardButtonRow extends KeyboardButtonRowBase {
  /// Keyboard Button Row constructor.
  const KeyboardButtonRow({
    required this.buttons,
  }) : super._();

  /// Deserialize.
  factory KeyboardButtonRow.deserialize(Uint8List buffer) {
    // final buttons = _readVector<KeyboardButton>(buffer);
    // final result = KeyboardButtonRow(buttons: buttons,);

    // return result;

    throw Exception();
  }

  /// Buttons.
  final List<KeyboardButtonBase> buttons;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x77608b83);
    buffer.writeVectorObject(buttons);
  }
}

/// Predicate Reply Markup.
abstract class ReplyMarkupBase extends TlConstructor {
  /// Predicate Reply Markup constructor.
  const ReplyMarkupBase._() : super._();
}

/// Reply Keyboard Hide.
///
/// ID: `a03e5b85`.
class ReplyKeyboardHide extends ReplyMarkupBase {
  /// Reply Keyboard Hide constructor.
  const ReplyKeyboardHide({
    required this.selective,
  }) : super._();

  /// Deserialize.
  factory ReplyKeyboardHide.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final selective = _readflags.2?true(buffer);
    // final result = ReplyKeyboardHide(flags: flags, selective: selective,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: selective,
    );

    return v;
  }

  /// selective: bit 2 of flags.2?true
  final bool selective;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa03e5b85);
    buffer.writeInt(flags);
  }
}

/// Reply Keyboard Force Reply.
///
/// ID: `86b40b08`.
class ReplyKeyboardForceReply extends ReplyMarkupBase {
  /// Reply Keyboard Force Reply constructor.
  const ReplyKeyboardForceReply({
    required this.singleUse,
    required this.selective,
    this.placeholder,
  }) : super._();

  /// Deserialize.
  factory ReplyKeyboardForceReply.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final singleUse = _readflags.1?true(buffer);
    // final selective = _readflags.2?true(buffer);
    // final placeholder = _readflags.3?string(buffer);
    // final result = ReplyKeyboardForceReply(flags: flags, singleUse: singleUse, selective: selective, placeholder: placeholder,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: singleUse,
      b02: selective,
      b03: placeholder != null,
    );

    return v;
  }

  /// single_use: bit 1 of flags.1?true
  final bool singleUse;

  /// selective: bit 2 of flags.2?true
  final bool selective;

  /// Placeholder.
  final String? placeholder;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x86b40b08);
    buffer.writeInt(flags);
    final localPlaceholderCopy = placeholder;
    if (localPlaceholderCopy != null) {
      buffer.writeString(localPlaceholderCopy);
    }
  }
}

/// Reply Keyboard Markup.
///
/// ID: `85dd99d1`.
class ReplyKeyboardMarkup extends ReplyMarkupBase {
  /// Reply Keyboard Markup constructor.
  const ReplyKeyboardMarkup({
    required this.resize,
    required this.singleUse,
    required this.selective,
    required this.persistent,
    required this.rows,
    this.placeholder,
  }) : super._();

  /// Deserialize.
  factory ReplyKeyboardMarkup.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final resize = _readflags.0?true(buffer);
    // final singleUse = _readflags.1?true(buffer);
    // final selective = _readflags.2?true(buffer);
    // final persistent = _readflags.4?true(buffer);
    // final rows = _readVector<KeyboardButtonRow>(buffer);
    // final placeholder = _readflags.3?string(buffer);
    // final result = ReplyKeyboardMarkup(flags: flags, resize: resize, singleUse: singleUse, selective: selective, persistent: persistent, rows: rows, placeholder: placeholder,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: resize,
      b01: singleUse,
      b02: selective,
      b04: persistent,
      b03: placeholder != null,
    );

    return v;
  }

  /// resize: bit 0 of flags.0?true
  final bool resize;

  /// single_use: bit 1 of flags.1?true
  final bool singleUse;

  /// selective: bit 2 of flags.2?true
  final bool selective;

  /// persistent: bit 4 of flags.4?true
  final bool persistent;

  /// Rows.
  final List<KeyboardButtonRowBase> rows;

  /// Placeholder.
  final String? placeholder;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x85dd99d1);
    buffer.writeInt(flags);
    buffer.writeVectorObject(rows);
    final localPlaceholderCopy = placeholder;
    if (localPlaceholderCopy != null) {
      buffer.writeString(localPlaceholderCopy);
    }
  }
}

/// Reply Inline Markup.
///
/// ID: `48a30254`.
class ReplyInlineMarkup extends ReplyMarkupBase {
  /// Reply Inline Markup constructor.
  const ReplyInlineMarkup({
    required this.rows,
  }) : super._();

  /// Deserialize.
  factory ReplyInlineMarkup.deserialize(Uint8List buffer) {
    // final rows = _readVector<KeyboardButtonRow>(buffer);
    // final result = ReplyInlineMarkup(rows: rows,);

    // return result;

    throw Exception();
  }

  /// Rows.
  final List<KeyboardButtonRowBase> rows;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x48a30254);
    buffer.writeVectorObject(rows);
  }
}

/// Predicate Message Entity.
abstract class MessageEntityBase extends TlConstructor {
  /// Predicate Message Entity constructor.
  const MessageEntityBase._() : super._();
}

/// Message Entity Unknown.
///
/// ID: `bb92ba95`.
class MessageEntityUnknown extends MessageEntityBase {
  /// Message Entity Unknown constructor.
  const MessageEntityUnknown({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityUnknown.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityUnknown(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbb92ba95);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Mention.
///
/// ID: `fa04579d`.
class MessageEntityMention extends MessageEntityBase {
  /// Message Entity Mention constructor.
  const MessageEntityMention({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityMention.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityMention(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfa04579d);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Hashtag.
///
/// ID: `6f635b0d`.
class MessageEntityHashtag extends MessageEntityBase {
  /// Message Entity Hashtag constructor.
  const MessageEntityHashtag({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityHashtag.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityHashtag(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6f635b0d);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Bot Command.
///
/// ID: `6cef8ac7`.
class MessageEntityBotCommand extends MessageEntityBase {
  /// Message Entity Bot Command constructor.
  const MessageEntityBotCommand({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityBotCommand.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityBotCommand(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6cef8ac7);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Url.
///
/// ID: `6ed02538`.
class MessageEntityUrl extends MessageEntityBase {
  /// Message Entity Url constructor.
  const MessageEntityUrl({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityUrl.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityUrl(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6ed02538);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Email.
///
/// ID: `64e475c2`.
class MessageEntityEmail extends MessageEntityBase {
  /// Message Entity Email constructor.
  const MessageEntityEmail({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityEmail.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityEmail(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x64e475c2);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Bold.
///
/// ID: `bd610bc9`.
class MessageEntityBold extends MessageEntityBase {
  /// Message Entity Bold constructor.
  const MessageEntityBold({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityBold.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityBold(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbd610bc9);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Italic.
///
/// ID: `826f8b60`.
class MessageEntityItalic extends MessageEntityBase {
  /// Message Entity Italic constructor.
  const MessageEntityItalic({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityItalic.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityItalic(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x826f8b60);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Code.
///
/// ID: `28a20571`.
class MessageEntityCode extends MessageEntityBase {
  /// Message Entity Code constructor.
  const MessageEntityCode({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityCode.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityCode(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x28a20571);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Pre.
///
/// ID: `73924be0`.
class MessageEntityPre extends MessageEntityBase {
  /// Message Entity Pre constructor.
  const MessageEntityPre({
    required this.offset,
    required this.length,
    required this.language,
  }) : super._();

  /// Deserialize.
  factory MessageEntityPre.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final language = _readstring(buffer);
    // final result = MessageEntityPre(offset: offset, length: length, language: language,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Language.
  final String language;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x73924be0);
    buffer.writeInt(offset);
    buffer.writeInt(length);
    buffer.writeString(language);
  }
}

/// Message Entity Text Url.
///
/// ID: `76a6d327`.
class MessageEntityTextUrl extends MessageEntityBase {
  /// Message Entity Text Url constructor.
  const MessageEntityTextUrl({
    required this.offset,
    required this.length,
    required this.url,
  }) : super._();

  /// Deserialize.
  factory MessageEntityTextUrl.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final url = _readstring(buffer);
    // final result = MessageEntityTextUrl(offset: offset, length: length, url: url,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x76a6d327);
    buffer.writeInt(offset);
    buffer.writeInt(length);
    buffer.writeString(url);
  }
}

/// Message Entity Mention Name.
///
/// ID: `dc7b1140`.
class MessageEntityMentionName extends MessageEntityBase {
  /// Message Entity Mention Name constructor.
  const MessageEntityMentionName({
    required this.offset,
    required this.length,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory MessageEntityMentionName.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final userId = _readlong(buffer);
    // final result = MessageEntityMentionName(offset: offset, length: length, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// User Id.
  final int userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdc7b1140);
    buffer.writeInt(offset);
    buffer.writeInt(length);
    buffer.writeLong(userId);
  }
}

/// Input Message Entity Mention Name.
///
/// ID: `208e68c9`.
class InputMessageEntityMentionName extends MessageEntityBase {
  /// Input Message Entity Mention Name constructor.
  const InputMessageEntityMentionName({
    required this.offset,
    required this.length,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory InputMessageEntityMentionName.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final userId = _readInputUser(buffer);
    // final result = InputMessageEntityMentionName(offset: offset, length: length, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// User Id.
  final InputUserBase userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x208e68c9);
    buffer.writeInt(offset);
    buffer.writeInt(length);
    buffer.writeObject(userId);
  }
}

/// Message Entity Phone.
///
/// ID: `9b69e34b`.
class MessageEntityPhone extends MessageEntityBase {
  /// Message Entity Phone constructor.
  const MessageEntityPhone({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityPhone.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityPhone(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9b69e34b);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Cashtag.
///
/// ID: `4c4e743f`.
class MessageEntityCashtag extends MessageEntityBase {
  /// Message Entity Cashtag constructor.
  const MessageEntityCashtag({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityCashtag.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityCashtag(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4c4e743f);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Underline.
///
/// ID: `9c4e7e8b`.
class MessageEntityUnderline extends MessageEntityBase {
  /// Message Entity Underline constructor.
  const MessageEntityUnderline({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityUnderline.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityUnderline(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9c4e7e8b);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Strike.
///
/// ID: `bf0693d4`.
class MessageEntityStrike extends MessageEntityBase {
  /// Message Entity Strike constructor.
  const MessageEntityStrike({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityStrike.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityStrike(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbf0693d4);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Bank Card.
///
/// ID: `761e6af4`.
class MessageEntityBankCard extends MessageEntityBase {
  /// Message Entity Bank Card constructor.
  const MessageEntityBankCard({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityBankCard.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityBankCard(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x761e6af4);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Spoiler.
///
/// ID: `32ca960f`.
class MessageEntitySpoiler extends MessageEntityBase {
  /// Message Entity Spoiler constructor.
  const MessageEntitySpoiler({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntitySpoiler.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntitySpoiler(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x32ca960f);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Message Entity Custom Emoji.
///
/// ID: `c8cf05f8`.
class MessageEntityCustomEmoji extends MessageEntityBase {
  /// Message Entity Custom Emoji constructor.
  const MessageEntityCustomEmoji({
    required this.offset,
    required this.length,
    required this.documentId,
  }) : super._();

  /// Deserialize.
  factory MessageEntityCustomEmoji.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final documentId = _readlong(buffer);
    // final result = MessageEntityCustomEmoji(offset: offset, length: length, documentId: documentId,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Document Id.
  final int documentId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc8cf05f8);
    buffer.writeInt(offset);
    buffer.writeInt(length);
    buffer.writeLong(documentId);
  }
}

/// Message Entity Blockquote.
///
/// ID: `020df5d0`.
class MessageEntityBlockquote extends MessageEntityBase {
  /// Message Entity Blockquote constructor.
  const MessageEntityBlockquote({
    required this.offset,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory MessageEntityBlockquote.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final length = _readint(buffer);
    // final result = MessageEntityBlockquote(offset: offset, length: length,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x020df5d0);
    buffer.writeInt(offset);
    buffer.writeInt(length);
  }
}

/// Predicate Input Channel.
abstract class InputChannelBase extends TlConstructor {
  /// Predicate Input Channel constructor.
  const InputChannelBase._() : super._();
}

/// Input Channel Empty.
///
/// ID: `ee8c1e86`.
class InputChannelEmpty extends InputChannelBase {
  /// Input Channel Empty constructor.
  const InputChannelEmpty() : super._();

  /// Deserialize.
  factory InputChannelEmpty.deserialize(Uint8List buffer) {
    // final result = InputChannelEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xee8c1e86);
  }
}

/// Input Channel.
///
/// ID: `f35aec28`.
class InputChannel extends InputChannelBase {
  /// Input Channel constructor.
  const InputChannel({
    required this.channelId,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputChannel.deserialize(Uint8List buffer) {
    // final channelId = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputChannel(channelId: channelId, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Channel Id.
  final int channelId;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf35aec28);
    buffer.writeLong(channelId);
    buffer.writeLong(accessHash);
  }
}

/// Input Channel From Message.
///
/// ID: `5b934f9d`.
class InputChannelFromMessage extends InputChannelBase {
  /// Input Channel From Message constructor.
  const InputChannelFromMessage({
    required this.peer,
    required this.msgId,
    required this.channelId,
  }) : super._();

  /// Deserialize.
  factory InputChannelFromMessage.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final channelId = _readlong(buffer);
    // final result = InputChannelFromMessage(peer: peer, msgId: msgId, channelId: channelId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Channel Id.
  final int channelId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5b934f9d);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeLong(channelId);
  }
}

/// Predicate Contacts Resolved Peer.
abstract class ContactsResolvedPeerBase extends TlConstructor {
  /// Predicate Contacts Resolved Peer constructor.
  const ContactsResolvedPeerBase._() : super._();
}

/// Contacts Resolved Peer.
///
/// ID: `7f077ad9`.
class ContactsResolvedPeer extends ContactsResolvedPeerBase {
  /// Contacts Resolved Peer constructor.
  const ContactsResolvedPeer({
    required this.peer,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ContactsResolvedPeer.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ContactsResolvedPeer(peer: peer, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7f077ad9);
    buffer.writeObject(peer);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Message Range.
abstract class MessageRangeBase extends TlConstructor {
  /// Predicate Message Range constructor.
  const MessageRangeBase._() : super._();
}

/// Message Range.
///
/// ID: `0ae30253`.
class MessageRange extends MessageRangeBase {
  /// Message Range constructor.
  const MessageRange({
    required this.minId,
    required this.maxId,
  }) : super._();

  /// Deserialize.
  factory MessageRange.deserialize(Uint8List buffer) {
    // final minId = _readint(buffer);
    // final maxId = _readint(buffer);
    // final result = MessageRange(minId: minId, maxId: maxId,);

    // return result;

    throw Exception();
  }

  /// Min Id.
  final int minId;

  /// Max Id.
  final int maxId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0ae30253);
    buffer.writeInt(minId);
    buffer.writeInt(maxId);
  }
}

/// Predicate Updates Channel Difference.
abstract class UpdatesChannelDifferenceBase extends TlConstructor {
  /// Predicate Updates Channel Difference constructor.
  const UpdatesChannelDifferenceBase._() : super._();
}

/// Updates Channel Difference Empty.
///
/// ID: `3e11affb`.
class UpdatesChannelDifferenceEmpty extends UpdatesChannelDifferenceBase {
  /// Updates Channel Difference Empty constructor.
  const UpdatesChannelDifferenceEmpty({
    required this.ffinal,
    required this.pts,
    this.timeout,
  }) : super._();

  /// Deserialize.
  factory UpdatesChannelDifferenceEmpty.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final ffinal = _readflags.0?true(buffer);
    // final pts = _readint(buffer);
    // final timeout = _readflags.1?int(buffer);
    // final result = UpdatesChannelDifferenceEmpty(flags: flags, ffinal: ffinal, pts: pts, timeout: timeout,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: ffinal,
      b01: timeout != null,
    );

    return v;
  }

  /// final: bit 0 of flags.0?true
  final bool ffinal;

  /// Pts.
  final int pts;

  /// Timeout.
  final int? timeout;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3e11affb);
    buffer.writeInt(flags);
    buffer.writeInt(pts);
    final localTimeoutCopy = timeout;
    if (localTimeoutCopy != null) {
      buffer.writeInt(localTimeoutCopy);
    }
  }
}

/// Updates Channel Difference Too Long.
///
/// ID: `a4bcc6fe`.
class UpdatesChannelDifferenceTooLong extends UpdatesChannelDifferenceBase {
  /// Updates Channel Difference Too Long constructor.
  const UpdatesChannelDifferenceTooLong({
    required this.ffinal,
    this.timeout,
    required this.dialog,
    required this.messages,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory UpdatesChannelDifferenceTooLong.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final ffinal = _readflags.0?true(buffer);
    // final timeout = _readflags.1?int(buffer);
    // final dialog = _readDialog(buffer);
    // final messages = _readVector<Message>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = UpdatesChannelDifferenceTooLong(flags: flags, ffinal: ffinal, timeout: timeout, dialog: dialog, messages: messages, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: ffinal,
      b01: timeout != null,
    );

    return v;
  }

  /// final: bit 0 of flags.0?true
  final bool ffinal;

  /// Timeout.
  final int? timeout;

  /// Dialog.
  final DialogBase dialog;

  /// Messages.
  final List<MessageBase> messages;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa4bcc6fe);
    buffer.writeInt(flags);
    final localTimeoutCopy = timeout;
    if (localTimeoutCopy != null) {
      buffer.writeInt(localTimeoutCopy);
    }
    buffer.writeObject(dialog);
    buffer.writeVectorObject(messages);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Updates Channel Difference.
///
/// ID: `2064674e`.
class UpdatesChannelDifference extends UpdatesChannelDifferenceBase {
  /// Updates Channel Difference constructor.
  const UpdatesChannelDifference({
    required this.ffinal,
    required this.pts,
    this.timeout,
    required this.newMessages,
    required this.otherUpdates,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory UpdatesChannelDifference.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final ffinal = _readflags.0?true(buffer);
    // final pts = _readint(buffer);
    // final timeout = _readflags.1?int(buffer);
    // final newMessages = _readVector<Message>(buffer);
    // final otherUpdates = _readVector<Update>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = UpdatesChannelDifference(flags: flags, ffinal: ffinal, pts: pts, timeout: timeout, newMessages: newMessages, otherUpdates: otherUpdates, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: ffinal,
      b01: timeout != null,
    );

    return v;
  }

  /// final: bit 0 of flags.0?true
  final bool ffinal;

  /// Pts.
  final int pts;

  /// Timeout.
  final int? timeout;

  /// New Messages.
  final List<MessageBase> newMessages;

  /// Other Updates.
  final List<UpdateBase> otherUpdates;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2064674e);
    buffer.writeInt(flags);
    buffer.writeInt(pts);
    final localTimeoutCopy = timeout;
    if (localTimeoutCopy != null) {
      buffer.writeInt(localTimeoutCopy);
    }
    buffer.writeVectorObject(newMessages);
    buffer.writeVectorObject(otherUpdates);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Channel Messages Filter.
abstract class ChannelMessagesFilterBase extends TlConstructor {
  /// Predicate Channel Messages Filter constructor.
  const ChannelMessagesFilterBase._() : super._();
}

/// Channel Messages Filter Empty.
///
/// ID: `94d42ee7`.
class ChannelMessagesFilterEmpty extends ChannelMessagesFilterBase {
  /// Channel Messages Filter Empty constructor.
  const ChannelMessagesFilterEmpty() : super._();

  /// Deserialize.
  factory ChannelMessagesFilterEmpty.deserialize(Uint8List buffer) {
    // final result = ChannelMessagesFilterEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x94d42ee7);
  }
}

/// Channel Messages Filter.
///
/// ID: `cd77d957`.
class ChannelMessagesFilter extends ChannelMessagesFilterBase {
  /// Channel Messages Filter constructor.
  const ChannelMessagesFilter({
    required this.excludeNewMessages,
    required this.ranges,
  }) : super._();

  /// Deserialize.
  factory ChannelMessagesFilter.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final excludeNewMessages = _readflags.1?true(buffer);
    // final ranges = _readVector<MessageRange>(buffer);
    // final result = ChannelMessagesFilter(flags: flags, excludeNewMessages: excludeNewMessages, ranges: ranges,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: excludeNewMessages,
    );

    return v;
  }

  /// exclude_new_messages: bit 1 of flags.1?true
  final bool excludeNewMessages;

  /// Ranges.
  final List<MessageRangeBase> ranges;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcd77d957);
    buffer.writeInt(flags);
    buffer.writeVectorObject(ranges);
  }
}

/// Predicate Channel Participant.
abstract class ChannelParticipantBase extends TlConstructor {
  /// Predicate Channel Participant constructor.
  const ChannelParticipantBase._() : super._();
}

/// Channel Participant.
///
/// ID: `c00c07c0`.
class ChannelParticipant extends ChannelParticipantBase {
  /// Channel Participant constructor.
  const ChannelParticipant({
    required this.userId,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory ChannelParticipant.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final date = _readint(buffer);
    // final result = ChannelParticipant(userId: userId, date: date,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc00c07c0);
    buffer.writeLong(userId);
    buffer.writeDateTime(date);
  }
}

/// Channel Participant Self.
///
/// ID: `35a8bfa7`.
class ChannelParticipantSelf extends ChannelParticipantBase {
  /// Channel Participant Self constructor.
  const ChannelParticipantSelf({
    required this.viaRequest,
    required this.userId,
    required this.inviterId,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory ChannelParticipantSelf.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final viaRequest = _readflags.0?true(buffer);
    // final userId = _readlong(buffer);
    // final inviterId = _readlong(buffer);
    // final date = _readint(buffer);
    // final result = ChannelParticipantSelf(flags: flags, viaRequest: viaRequest, userId: userId, inviterId: inviterId, date: date,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: viaRequest,
    );

    return v;
  }

  /// via_request: bit 0 of flags.0?true
  final bool viaRequest;

  /// User Id.
  final int userId;

  /// Inviter Id.
  final int inviterId;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x35a8bfa7);
    buffer.writeInt(flags);
    buffer.writeLong(userId);
    buffer.writeLong(inviterId);
    buffer.writeDateTime(date);
  }
}

/// Channel Participant Creator.
///
/// ID: `2fe601d3`.
class ChannelParticipantCreator extends ChannelParticipantBase {
  /// Channel Participant Creator constructor.
  const ChannelParticipantCreator({
    required this.userId,
    required this.adminRights,
    this.rank,
  }) : super._();

  /// Deserialize.
  factory ChannelParticipantCreator.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final userId = _readlong(buffer);
    // final adminRights = _readChatAdminRights(buffer);
    // final rank = _readflags.0?string(buffer);
    // final result = ChannelParticipantCreator(flags: flags, userId: userId, adminRights: adminRights, rank: rank,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: rank != null,
    );

    return v;
  }

  /// User Id.
  final int userId;

  /// Admin Rights.
  final ChatAdminRightsBase adminRights;

  /// Rank.
  final String? rank;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2fe601d3);
    buffer.writeInt(flags);
    buffer.writeLong(userId);
    buffer.writeObject(adminRights);
    final localRankCopy = rank;
    if (localRankCopy != null) {
      buffer.writeString(localRankCopy);
    }
  }
}

/// Channel Participant Admin.
///
/// ID: `34c3bb53`.
class ChannelParticipantAdmin extends ChannelParticipantBase {
  /// Channel Participant Admin constructor.
  const ChannelParticipantAdmin({
    required this.canEdit,
    required this.self,
    required this.userId,
    this.inviterId,
    required this.promotedBy,
    required this.date,
    required this.adminRights,
    this.rank,
  }) : super._();

  /// Deserialize.
  factory ChannelParticipantAdmin.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final canEdit = _readflags.0?true(buffer);
    // final self = _readflags.1?true(buffer);
    // final userId = _readlong(buffer);
    // final inviterId = _readflags.1?long(buffer);
    // final promotedBy = _readlong(buffer);
    // final date = _readint(buffer);
    // final adminRights = _readChatAdminRights(buffer);
    // final rank = _readflags.2?string(buffer);
    // final result = ChannelParticipantAdmin(flags: flags, canEdit: canEdit, self: self, userId: userId, inviterId: inviterId, promotedBy: promotedBy, date: date, adminRights: adminRights, rank: rank,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: canEdit,
      b01: self || inviterId != null,
      b02: rank != null,
    );

    return v;
  }

  /// can_edit: bit 0 of flags.0?true
  final bool canEdit;

  /// self: bit 1 of flags.1?true
  final bool self;

  /// User Id.
  final int userId;

  /// Inviter Id.
  final int? inviterId;

  /// Promoted By.
  final int promotedBy;

  /// Date.
  final DateTime date;

  /// Admin Rights.
  final ChatAdminRightsBase adminRights;

  /// Rank.
  final String? rank;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x34c3bb53);
    buffer.writeInt(flags);
    buffer.writeLong(userId);
    final localInviterIdCopy = inviterId;
    if (localInviterIdCopy != null) {
      buffer.writeLong(localInviterIdCopy);
    }
    buffer.writeLong(promotedBy);
    buffer.writeDateTime(date);
    buffer.writeObject(adminRights);
    final localRankCopy = rank;
    if (localRankCopy != null) {
      buffer.writeString(localRankCopy);
    }
  }
}

/// Channel Participant Banned.
///
/// ID: `6df8014e`.
class ChannelParticipantBanned extends ChannelParticipantBase {
  /// Channel Participant Banned constructor.
  const ChannelParticipantBanned({
    required this.left,
    required this.peer,
    required this.kickedBy,
    required this.date,
    required this.bannedRights,
  }) : super._();

  /// Deserialize.
  factory ChannelParticipantBanned.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final left = _readflags.0?true(buffer);
    // final peer = _readPeer(buffer);
    // final kickedBy = _readlong(buffer);
    // final date = _readint(buffer);
    // final bannedRights = _readChatBannedRights(buffer);
    // final result = ChannelParticipantBanned(flags: flags, left: left, peer: peer, kickedBy: kickedBy, date: date, bannedRights: bannedRights,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: left,
    );

    return v;
  }

  /// left: bit 0 of flags.0?true
  final bool left;

  /// Peer.
  final PeerBase peer;

  /// Kicked By.
  final int kickedBy;

  /// Date.
  final DateTime date;

  /// Banned Rights.
  final ChatBannedRightsBase bannedRights;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6df8014e);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeLong(kickedBy);
    buffer.writeDateTime(date);
    buffer.writeObject(bannedRights);
  }
}

/// Channel Participant Left.
///
/// ID: `1b03f006`.
class ChannelParticipantLeft extends ChannelParticipantBase {
  /// Channel Participant Left constructor.
  const ChannelParticipantLeft({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory ChannelParticipantLeft.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final result = ChannelParticipantLeft(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1b03f006);
    buffer.writeObject(peer);
  }
}

/// Predicate Channel Participants Filter.
abstract class ChannelParticipantsFilterBase extends TlConstructor {
  /// Predicate Channel Participants Filter constructor.
  const ChannelParticipantsFilterBase._() : super._();
}

/// Channel Participants Recent.
///
/// ID: `de3f3c79`.
class ChannelParticipantsRecent extends ChannelParticipantsFilterBase {
  /// Channel Participants Recent constructor.
  const ChannelParticipantsRecent() : super._();

  /// Deserialize.
  factory ChannelParticipantsRecent.deserialize(Uint8List buffer) {
    // final result = ChannelParticipantsRecent();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xde3f3c79);
  }
}

/// Channel Participants Admins.
///
/// ID: `b4608969`.
class ChannelParticipantsAdmins extends ChannelParticipantsFilterBase {
  /// Channel Participants Admins constructor.
  const ChannelParticipantsAdmins() : super._();

  /// Deserialize.
  factory ChannelParticipantsAdmins.deserialize(Uint8List buffer) {
    // final result = ChannelParticipantsAdmins();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb4608969);
  }
}

/// Channel Participants Kicked.
///
/// ID: `a3b54985`.
class ChannelParticipantsKicked extends ChannelParticipantsFilterBase {
  /// Channel Participants Kicked constructor.
  const ChannelParticipantsKicked({
    required this.q,
  }) : super._();

  /// Deserialize.
  factory ChannelParticipantsKicked.deserialize(Uint8List buffer) {
    // final q = _readstring(buffer);
    // final result = ChannelParticipantsKicked(q: q,);

    // return result;

    throw Exception();
  }

  /// Q.
  final String q;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa3b54985);
    buffer.writeString(q);
  }
}

/// Channel Participants Bots.
///
/// ID: `b0d1865b`.
class ChannelParticipantsBots extends ChannelParticipantsFilterBase {
  /// Channel Participants Bots constructor.
  const ChannelParticipantsBots() : super._();

  /// Deserialize.
  factory ChannelParticipantsBots.deserialize(Uint8List buffer) {
    // final result = ChannelParticipantsBots();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb0d1865b);
  }
}

/// Channel Participants Banned.
///
/// ID: `1427a5e1`.
class ChannelParticipantsBanned extends ChannelParticipantsFilterBase {
  /// Channel Participants Banned constructor.
  const ChannelParticipantsBanned({
    required this.q,
  }) : super._();

  /// Deserialize.
  factory ChannelParticipantsBanned.deserialize(Uint8List buffer) {
    // final q = _readstring(buffer);
    // final result = ChannelParticipantsBanned(q: q,);

    // return result;

    throw Exception();
  }

  /// Q.
  final String q;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1427a5e1);
    buffer.writeString(q);
  }
}

/// Channel Participants Search.
///
/// ID: `0656ac4b`.
class ChannelParticipantsSearch extends ChannelParticipantsFilterBase {
  /// Channel Participants Search constructor.
  const ChannelParticipantsSearch({
    required this.q,
  }) : super._();

  /// Deserialize.
  factory ChannelParticipantsSearch.deserialize(Uint8List buffer) {
    // final q = _readstring(buffer);
    // final result = ChannelParticipantsSearch(q: q,);

    // return result;

    throw Exception();
  }

  /// Q.
  final String q;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0656ac4b);
    buffer.writeString(q);
  }
}

/// Channel Participants Contacts.
///
/// ID: `bb6ae88d`.
class ChannelParticipantsContacts extends ChannelParticipantsFilterBase {
  /// Channel Participants Contacts constructor.
  const ChannelParticipantsContacts({
    required this.q,
  }) : super._();

  /// Deserialize.
  factory ChannelParticipantsContacts.deserialize(Uint8List buffer) {
    // final q = _readstring(buffer);
    // final result = ChannelParticipantsContacts(q: q,);

    // return result;

    throw Exception();
  }

  /// Q.
  final String q;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbb6ae88d);
    buffer.writeString(q);
  }
}

/// Channel Participants Mentions.
///
/// ID: `e04b5ceb`.
class ChannelParticipantsMentions extends ChannelParticipantsFilterBase {
  /// Channel Participants Mentions constructor.
  const ChannelParticipantsMentions({
    this.q,
    this.topMsgId,
  }) : super._();

  /// Deserialize.
  factory ChannelParticipantsMentions.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final q = _readflags.0?string(buffer);
    // final topMsgId = _readflags.1?int(buffer);
    // final result = ChannelParticipantsMentions(flags: flags, q: q, topMsgId: topMsgId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: q != null,
      b01: topMsgId != null,
    );

    return v;
  }

  /// Q.
  final String? q;

  /// Top Msg Id.
  final int? topMsgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe04b5ceb);
    buffer.writeInt(flags);
    final localQCopy = q;
    if (localQCopy != null) {
      buffer.writeString(localQCopy);
    }
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
  }
}

/// Predicate Channels Channel Participants.
abstract class ChannelsChannelParticipantsBase extends TlConstructor {
  /// Predicate Channels Channel Participants constructor.
  const ChannelsChannelParticipantsBase._() : super._();
}

/// Channels Channel Participants.
///
/// ID: `9ab0feaf`.
class ChannelsChannelParticipants extends ChannelsChannelParticipantsBase {
  /// Channels Channel Participants constructor.
  const ChannelsChannelParticipants({
    required this.count,
    required this.participants,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ChannelsChannelParticipants.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final participants = _readVector<ChannelParticipant>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ChannelsChannelParticipants(count: count, participants: participants, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Participants.
  final List<ChannelParticipantBase> participants;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9ab0feaf);
    buffer.writeInt(count);
    buffer.writeVectorObject(participants);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Channels Channel Participants Not Modified.
///
/// ID: `f0173fe9`.
class ChannelsChannelParticipantsNotModified
    extends ChannelsChannelParticipantsBase {
  /// Channels Channel Participants Not Modified constructor.
  const ChannelsChannelParticipantsNotModified() : super._();

  /// Deserialize.
  factory ChannelsChannelParticipantsNotModified.deserialize(Uint8List buffer) {
    // final result = ChannelsChannelParticipantsNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf0173fe9);
  }
}

/// Predicate Channels Channel Participant.
abstract class ChannelsChannelParticipantBase extends TlConstructor {
  /// Predicate Channels Channel Participant constructor.
  const ChannelsChannelParticipantBase._() : super._();
}

/// Channels Channel Participant.
///
/// ID: `dfb80317`.
class ChannelsChannelParticipant extends ChannelsChannelParticipantBase {
  /// Channels Channel Participant constructor.
  const ChannelsChannelParticipant({
    required this.participant,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ChannelsChannelParticipant.deserialize(Uint8List buffer) {
    // final participant = _readChannelParticipant(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ChannelsChannelParticipant(participant: participant, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Participant.
  final ChannelParticipantBase participant;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdfb80317);
    buffer.writeObject(participant);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Help Terms Of Service.
abstract class HelpTermsOfServiceBase extends TlConstructor {
  /// Predicate Help Terms Of Service constructor.
  const HelpTermsOfServiceBase._() : super._();
}

/// Help Terms Of Service.
///
/// ID: `780a0310`.
class HelpTermsOfService extends HelpTermsOfServiceBase {
  /// Help Terms Of Service constructor.
  const HelpTermsOfService({
    required this.popup,
    required this.id,
    required this.text,
    required this.entities,
    this.minAgeConfirm,
  }) : super._();

  /// Deserialize.
  factory HelpTermsOfService.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final popup = _readflags.0?true(buffer);
    // final id = _readDataJSON(buffer);
    // final text = _readstring(buffer);
    // final entities = _readVector<MessageEntity>(buffer);
    // final minAgeConfirm = _readflags.1?int(buffer);
    // final result = HelpTermsOfService(flags: flags, popup: popup, id: id, text: text, entities: entities, minAgeConfirm: minAgeConfirm,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: popup,
      b01: minAgeConfirm != null,
    );

    return v;
  }

  /// popup: bit 0 of flags.0?true
  final bool popup;

  /// Id.
  final DataJSONBase id;

  /// Text.
  final String text;

  /// Entities.
  final List<MessageEntityBase> entities;

  /// Min Age Confirm.
  final int? minAgeConfirm;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x780a0310);
    buffer.writeInt(flags);
    buffer.writeObject(id);
    buffer.writeString(text);
    buffer.writeVectorObject(entities);
    final localMinAgeConfirmCopy = minAgeConfirm;
    if (localMinAgeConfirmCopy != null) {
      buffer.writeInt(localMinAgeConfirmCopy);
    }
  }
}

/// Predicate Messages Saved Gifs.
abstract class MessagesSavedGifsBase extends TlConstructor {
  /// Predicate Messages Saved Gifs constructor.
  const MessagesSavedGifsBase._() : super._();
}

/// Messages Saved Gifs Not Modified.
///
/// ID: `e8025ca2`.
class MessagesSavedGifsNotModified extends MessagesSavedGifsBase {
  /// Messages Saved Gifs Not Modified constructor.
  const MessagesSavedGifsNotModified() : super._();

  /// Deserialize.
  factory MessagesSavedGifsNotModified.deserialize(Uint8List buffer) {
    // final result = MessagesSavedGifsNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe8025ca2);
  }
}

/// Messages Saved Gifs.
///
/// ID: `84a02a0d`.
class MessagesSavedGifs extends MessagesSavedGifsBase {
  /// Messages Saved Gifs constructor.
  const MessagesSavedGifs({
    required this.hash,
    required this.gifs,
  }) : super._();

  /// Deserialize.
  factory MessagesSavedGifs.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final gifs = _readVector<Document>(buffer);
    // final result = MessagesSavedGifs(hash: hash, gifs: gifs,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Gifs.
  final List<DocumentBase> gifs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x84a02a0d);
    buffer.writeLong(hash);
    buffer.writeVectorObject(gifs);
  }
}

/// Predicate Input Bot Inline Message.
abstract class InputBotInlineMessageBase extends TlConstructor {
  /// Predicate Input Bot Inline Message constructor.
  const InputBotInlineMessageBase._() : super._();
}

/// Input Bot Inline Message Media Auto.
///
/// ID: `3380c786`.
class InputBotInlineMessageMediaAuto extends InputBotInlineMessageBase {
  /// Input Bot Inline Message Media Auto constructor.
  const InputBotInlineMessageMediaAuto({
    required this.invertMedia,
    required this.message,
    this.entities,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineMessageMediaAuto.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final invertMedia = _readflags.3?true(buffer);
    // final message = _readstring(buffer);
    // final entities = _readflags.1?Vector<MessageEntity>(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = InputBotInlineMessageMediaAuto(flags: flags, invertMedia: invertMedia, message: message, entities: entities, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: invertMedia,
      b01: entities != null,
      b02: replyMarkup != null,
    );

    return v;
  }

  /// invert_media: bit 3 of flags.3?true
  final bool invertMedia;

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3380c786);
    buffer.writeInt(flags);
    buffer.writeString(message);
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Input Bot Inline Message Text.
///
/// ID: `3dcd7a87`.
class InputBotInlineMessageText extends InputBotInlineMessageBase {
  /// Input Bot Inline Message Text constructor.
  const InputBotInlineMessageText({
    required this.noWebpage,
    required this.invertMedia,
    required this.message,
    this.entities,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineMessageText.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final noWebpage = _readflags.0?true(buffer);
    // final invertMedia = _readflags.3?true(buffer);
    // final message = _readstring(buffer);
    // final entities = _readflags.1?Vector<MessageEntity>(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = InputBotInlineMessageText(flags: flags, noWebpage: noWebpage, invertMedia: invertMedia, message: message, entities: entities, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: noWebpage,
      b03: invertMedia,
      b01: entities != null,
      b02: replyMarkup != null,
    );

    return v;
  }

  /// no_webpage: bit 0 of flags.0?true
  final bool noWebpage;

  /// invert_media: bit 3 of flags.3?true
  final bool invertMedia;

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3dcd7a87);
    buffer.writeInt(flags);
    buffer.writeString(message);
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Input Bot Inline Message Media Geo.
///
/// ID: `96929a85`.
class InputBotInlineMessageMediaGeo extends InputBotInlineMessageBase {
  /// Input Bot Inline Message Media Geo constructor.
  const InputBotInlineMessageMediaGeo({
    required this.geoPoint,
    this.heading,
    this.period,
    this.proximityNotificationRadius,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineMessageMediaGeo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final geoPoint = _readInputGeoPoint(buffer);
    // final heading = _readflags.0?int(buffer);
    // final period = _readflags.1?int(buffer);
    // final proximityNotificationRadius = _readflags.3?int(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = InputBotInlineMessageMediaGeo(flags: flags, geoPoint: geoPoint, heading: heading, period: period, proximityNotificationRadius: proximityNotificationRadius, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: heading != null,
      b01: period != null,
      b03: proximityNotificationRadius != null,
      b02: replyMarkup != null,
    );

    return v;
  }

  /// Geo Point.
  final InputGeoPointBase geoPoint;

  /// Heading.
  final int? heading;

  /// Period.
  final int? period;

  /// Proximity Notification Radius.
  final int? proximityNotificationRadius;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x96929a85);
    buffer.writeInt(flags);
    buffer.writeObject(geoPoint);
    final localHeadingCopy = heading;
    if (localHeadingCopy != null) {
      buffer.writeInt(localHeadingCopy);
    }
    final localPeriodCopy = period;
    if (localPeriodCopy != null) {
      buffer.writeInt(localPeriodCopy);
    }
    final localProximityNotificationRadiusCopy = proximityNotificationRadius;
    if (localProximityNotificationRadiusCopy != null) {
      buffer.writeInt(localProximityNotificationRadiusCopy);
    }
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Input Bot Inline Message Media Venue.
///
/// ID: `417bbf11`.
class InputBotInlineMessageMediaVenue extends InputBotInlineMessageBase {
  /// Input Bot Inline Message Media Venue constructor.
  const InputBotInlineMessageMediaVenue({
    required this.geoPoint,
    required this.title,
    required this.address,
    required this.provider,
    required this.venueId,
    required this.venueType,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineMessageMediaVenue.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final geoPoint = _readInputGeoPoint(buffer);
    // final title = _readstring(buffer);
    // final address = _readstring(buffer);
    // final provider = _readstring(buffer);
    // final venueId = _readstring(buffer);
    // final venueType = _readstring(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = InputBotInlineMessageMediaVenue(flags: flags, geoPoint: geoPoint, title: title, address: address, provider: provider, venueId: venueId, venueType: venueType, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: replyMarkup != null,
    );

    return v;
  }

  /// Geo Point.
  final InputGeoPointBase geoPoint;

  /// Title.
  final String title;

  /// Address.
  final String address;

  /// Provider.
  final String provider;

  /// Venue Id.
  final String venueId;

  /// Venue Type.
  final String venueType;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x417bbf11);
    buffer.writeInt(flags);
    buffer.writeObject(geoPoint);
    buffer.writeString(title);
    buffer.writeString(address);
    buffer.writeString(provider);
    buffer.writeString(venueId);
    buffer.writeString(venueType);
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Input Bot Inline Message Media Contact.
///
/// ID: `a6edbffd`.
class InputBotInlineMessageMediaContact extends InputBotInlineMessageBase {
  /// Input Bot Inline Message Media Contact constructor.
  const InputBotInlineMessageMediaContact({
    required this.phoneNumber,
    required this.firstName,
    required this.lastName,
    required this.vcard,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineMessageMediaContact.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final phoneNumber = _readstring(buffer);
    // final firstName = _readstring(buffer);
    // final lastName = _readstring(buffer);
    // final vcard = _readstring(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = InputBotInlineMessageMediaContact(flags: flags, phoneNumber: phoneNumber, firstName: firstName, lastName: lastName, vcard: vcard, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: replyMarkup != null,
    );

    return v;
  }

  /// Phone Number.
  final String phoneNumber;

  /// First Name.
  final String firstName;

  /// Last Name.
  final String lastName;

  /// Vcard.
  final String vcard;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa6edbffd);
    buffer.writeInt(flags);
    buffer.writeString(phoneNumber);
    buffer.writeString(firstName);
    buffer.writeString(lastName);
    buffer.writeString(vcard);
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Input Bot Inline Message Game.
///
/// ID: `4b425864`.
class InputBotInlineMessageGame extends InputBotInlineMessageBase {
  /// Input Bot Inline Message Game constructor.
  const InputBotInlineMessageGame({
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineMessageGame.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = InputBotInlineMessageGame(flags: flags, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: replyMarkup != null,
    );

    return v;
  }

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4b425864);
    buffer.writeInt(flags);
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Input Bot Inline Message Media Invoice.
///
/// ID: `d7e78225`.
class InputBotInlineMessageMediaInvoice extends InputBotInlineMessageBase {
  /// Input Bot Inline Message Media Invoice constructor.
  const InputBotInlineMessageMediaInvoice({
    required this.title,
    required this.description,
    this.photo,
    required this.invoice,
    required this.payload,
    required this.provider,
    required this.providerData,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineMessageMediaInvoice.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final title = _readstring(buffer);
    // final description = _readstring(buffer);
    // final photo = _readflags.0?InputWebDocument(buffer);
    // final invoice = _readInvoice(buffer);
    // final payload = _readbytes(buffer);
    // final provider = _readstring(buffer);
    // final providerData = _readDataJSON(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = InputBotInlineMessageMediaInvoice(flags: flags, title: title, description: description, photo: photo, invoice: invoice, payload: payload, provider: provider, providerData: providerData, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: photo != null,
      b02: replyMarkup != null,
    );

    return v;
  }

  /// Title.
  final String title;

  /// Description.
  final String description;

  /// Photo.
  final InputWebDocumentBase? photo;

  /// Invoice.
  final InvoiceBase invoice;

  /// Payload.
  final Uint8List payload;

  /// Provider.
  final String provider;

  /// Provider Data.
  final DataJSONBase providerData;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd7e78225);
    buffer.writeInt(flags);
    buffer.writeString(title);
    buffer.writeString(description);
    final localPhotoCopy = photo;
    if (localPhotoCopy != null) {
      buffer.writeObject(localPhotoCopy);
    }
    buffer.writeObject(invoice);
    buffer.writeBytes(payload);
    buffer.writeString(provider);
    buffer.writeObject(providerData);
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Input Bot Inline Message Media Web Page.
///
/// ID: `bddcc510`.
class InputBotInlineMessageMediaWebPage extends InputBotInlineMessageBase {
  /// Input Bot Inline Message Media Web Page constructor.
  const InputBotInlineMessageMediaWebPage({
    required this.invertMedia,
    required this.forceLargeMedia,
    required this.forceSmallMedia,
    required this.optional,
    required this.message,
    this.entities,
    required this.url,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineMessageMediaWebPage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final invertMedia = _readflags.3?true(buffer);
    // final forceLargeMedia = _readflags.4?true(buffer);
    // final forceSmallMedia = _readflags.5?true(buffer);
    // final optional = _readflags.6?true(buffer);
    // final message = _readstring(buffer);
    // final entities = _readflags.1?Vector<MessageEntity>(buffer);
    // final url = _readstring(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = InputBotInlineMessageMediaWebPage(flags: flags, invertMedia: invertMedia, forceLargeMedia: forceLargeMedia, forceSmallMedia: forceSmallMedia, optional: optional, message: message, entities: entities, url: url, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: invertMedia,
      b04: forceLargeMedia,
      b05: forceSmallMedia,
      b06: optional,
      b01: entities != null,
      b02: replyMarkup != null,
    );

    return v;
  }

  /// invert_media: bit 3 of flags.3?true
  final bool invertMedia;

  /// force_large_media: bit 4 of flags.4?true
  final bool forceLargeMedia;

  /// force_small_media: bit 5 of flags.5?true
  final bool forceSmallMedia;

  /// optional: bit 6 of flags.6?true
  final bool optional;

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Url.
  final String url;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbddcc510);
    buffer.writeInt(flags);
    buffer.writeString(message);
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    buffer.writeString(url);
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Predicate Input Bot Inline Result.
abstract class InputBotInlineResultBase extends TlConstructor {
  /// Predicate Input Bot Inline Result constructor.
  const InputBotInlineResultBase._() : super._();
}

/// Input Bot Inline Result.
///
/// ID: `88bf9319`.
class InputBotInlineResult extends InputBotInlineResultBase {
  /// Input Bot Inline Result constructor.
  const InputBotInlineResult({
    required this.id,
    required this.type,
    this.title,
    this.description,
    this.url,
    this.thumb,
    this.content,
    required this.sendMessage,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineResult.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final id = _readstring(buffer);
    // final type = _readstring(buffer);
    // final title = _readflags.1?string(buffer);
    // final description = _readflags.2?string(buffer);
    // final url = _readflags.3?string(buffer);
    // final thumb = _readflags.4?InputWebDocument(buffer);
    // final content = _readflags.5?InputWebDocument(buffer);
    // final sendMessage = _readInputBotInlineMessage(buffer);
    // final result = InputBotInlineResult(flags: flags, id: id, type: type, title: title, description: description, url: url, thumb: thumb, content: content, sendMessage: sendMessage,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: title != null,
      b02: description != null,
      b03: url != null,
      b04: thumb != null,
      b05: content != null,
    );

    return v;
  }

  /// Id.
  final String id;

  /// Type.
  final String type;

  /// Title.
  final String? title;

  /// Description.
  final String? description;

  /// Url.
  final String? url;

  /// Thumb.
  final InputWebDocumentBase? thumb;

  /// Content.
  final InputWebDocumentBase? content;

  /// Send Message.
  final InputBotInlineMessageBase sendMessage;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x88bf9319);
    buffer.writeInt(flags);
    buffer.writeString(id);
    buffer.writeString(type);
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localDescriptionCopy = description;
    if (localDescriptionCopy != null) {
      buffer.writeString(localDescriptionCopy);
    }
    final localUrlCopy = url;
    if (localUrlCopy != null) {
      buffer.writeString(localUrlCopy);
    }
    final localThumbCopy = thumb;
    if (localThumbCopy != null) {
      buffer.writeObject(localThumbCopy);
    }
    final localContentCopy = content;
    if (localContentCopy != null) {
      buffer.writeObject(localContentCopy);
    }
    buffer.writeObject(sendMessage);
  }
}

/// Input Bot Inline Result Photo.
///
/// ID: `a8d864a7`.
class InputBotInlineResultPhoto extends InputBotInlineResultBase {
  /// Input Bot Inline Result Photo constructor.
  const InputBotInlineResultPhoto({
    required this.id,
    required this.type,
    required this.photo,
    required this.sendMessage,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineResultPhoto.deserialize(Uint8List buffer) {
    // final id = _readstring(buffer);
    // final type = _readstring(buffer);
    // final photo = _readInputPhoto(buffer);
    // final sendMessage = _readInputBotInlineMessage(buffer);
    // final result = InputBotInlineResultPhoto(id: id, type: type, photo: photo, sendMessage: sendMessage,);

    // return result;

    throw Exception();
  }

  /// Id.
  final String id;

  /// Type.
  final String type;

  /// Photo.
  final InputPhotoBase photo;

  /// Send Message.
  final InputBotInlineMessageBase sendMessage;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa8d864a7);
    buffer.writeString(id);
    buffer.writeString(type);
    buffer.writeObject(photo);
    buffer.writeObject(sendMessage);
  }
}

/// Input Bot Inline Result Document.
///
/// ID: `fff8fdc4`.
class InputBotInlineResultDocument extends InputBotInlineResultBase {
  /// Input Bot Inline Result Document constructor.
  const InputBotInlineResultDocument({
    required this.id,
    required this.type,
    this.title,
    this.description,
    required this.document,
    required this.sendMessage,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineResultDocument.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final id = _readstring(buffer);
    // final type = _readstring(buffer);
    // final title = _readflags.1?string(buffer);
    // final description = _readflags.2?string(buffer);
    // final document = _readInputDocument(buffer);
    // final sendMessage = _readInputBotInlineMessage(buffer);
    // final result = InputBotInlineResultDocument(flags: flags, id: id, type: type, title: title, description: description, document: document, sendMessage: sendMessage,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: title != null,
      b02: description != null,
    );

    return v;
  }

  /// Id.
  final String id;

  /// Type.
  final String type;

  /// Title.
  final String? title;

  /// Description.
  final String? description;

  /// Document.
  final InputDocumentBase document;

  /// Send Message.
  final InputBotInlineMessageBase sendMessage;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfff8fdc4);
    buffer.writeInt(flags);
    buffer.writeString(id);
    buffer.writeString(type);
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localDescriptionCopy = description;
    if (localDescriptionCopy != null) {
      buffer.writeString(localDescriptionCopy);
    }
    buffer.writeObject(document);
    buffer.writeObject(sendMessage);
  }
}

/// Input Bot Inline Result Game.
///
/// ID: `4fa417f2`.
class InputBotInlineResultGame extends InputBotInlineResultBase {
  /// Input Bot Inline Result Game constructor.
  const InputBotInlineResultGame({
    required this.id,
    required this.shortName,
    required this.sendMessage,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineResultGame.deserialize(Uint8List buffer) {
    // final id = _readstring(buffer);
    // final shortName = _readstring(buffer);
    // final sendMessage = _readInputBotInlineMessage(buffer);
    // final result = InputBotInlineResultGame(id: id, shortName: shortName, sendMessage: sendMessage,);

    // return result;

    throw Exception();
  }

  /// Id.
  final String id;

  /// Short Name.
  final String shortName;

  /// Send Message.
  final InputBotInlineMessageBase sendMessage;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4fa417f2);
    buffer.writeString(id);
    buffer.writeString(shortName);
    buffer.writeObject(sendMessage);
  }
}

/// Predicate Bot Inline Message.
abstract class BotInlineMessageBase extends TlConstructor {
  /// Predicate Bot Inline Message constructor.
  const BotInlineMessageBase._() : super._();
}

/// Bot Inline Message Media Auto.
///
/// ID: `764cf810`.
class BotInlineMessageMediaAuto extends BotInlineMessageBase {
  /// Bot Inline Message Media Auto constructor.
  const BotInlineMessageMediaAuto({
    required this.invertMedia,
    required this.message,
    this.entities,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory BotInlineMessageMediaAuto.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final invertMedia = _readflags.3?true(buffer);
    // final message = _readstring(buffer);
    // final entities = _readflags.1?Vector<MessageEntity>(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = BotInlineMessageMediaAuto(flags: flags, invertMedia: invertMedia, message: message, entities: entities, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: invertMedia,
      b01: entities != null,
      b02: replyMarkup != null,
    );

    return v;
  }

  /// invert_media: bit 3 of flags.3?true
  final bool invertMedia;

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x764cf810);
    buffer.writeInt(flags);
    buffer.writeString(message);
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Bot Inline Message Text.
///
/// ID: `8c7f65e2`.
class BotInlineMessageText extends BotInlineMessageBase {
  /// Bot Inline Message Text constructor.
  const BotInlineMessageText({
    required this.noWebpage,
    required this.invertMedia,
    required this.message,
    this.entities,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory BotInlineMessageText.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final noWebpage = _readflags.0?true(buffer);
    // final invertMedia = _readflags.3?true(buffer);
    // final message = _readstring(buffer);
    // final entities = _readflags.1?Vector<MessageEntity>(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = BotInlineMessageText(flags: flags, noWebpage: noWebpage, invertMedia: invertMedia, message: message, entities: entities, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: noWebpage,
      b03: invertMedia,
      b01: entities != null,
      b02: replyMarkup != null,
    );

    return v;
  }

  /// no_webpage: bit 0 of flags.0?true
  final bool noWebpage;

  /// invert_media: bit 3 of flags.3?true
  final bool invertMedia;

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8c7f65e2);
    buffer.writeInt(flags);
    buffer.writeString(message);
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Bot Inline Message Media Geo.
///
/// ID: `051846fd`.
class BotInlineMessageMediaGeo extends BotInlineMessageBase {
  /// Bot Inline Message Media Geo constructor.
  const BotInlineMessageMediaGeo({
    required this.geo,
    this.heading,
    this.period,
    this.proximityNotificationRadius,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory BotInlineMessageMediaGeo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final geo = _readGeoPoint(buffer);
    // final heading = _readflags.0?int(buffer);
    // final period = _readflags.1?int(buffer);
    // final proximityNotificationRadius = _readflags.3?int(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = BotInlineMessageMediaGeo(flags: flags, geo: geo, heading: heading, period: period, proximityNotificationRadius: proximityNotificationRadius, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: heading != null,
      b01: period != null,
      b03: proximityNotificationRadius != null,
      b02: replyMarkup != null,
    );

    return v;
  }

  /// Geo.
  final GeoPointBase geo;

  /// Heading.
  final int? heading;

  /// Period.
  final int? period;

  /// Proximity Notification Radius.
  final int? proximityNotificationRadius;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x051846fd);
    buffer.writeInt(flags);
    buffer.writeObject(geo);
    final localHeadingCopy = heading;
    if (localHeadingCopy != null) {
      buffer.writeInt(localHeadingCopy);
    }
    final localPeriodCopy = period;
    if (localPeriodCopy != null) {
      buffer.writeInt(localPeriodCopy);
    }
    final localProximityNotificationRadiusCopy = proximityNotificationRadius;
    if (localProximityNotificationRadiusCopy != null) {
      buffer.writeInt(localProximityNotificationRadiusCopy);
    }
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Bot Inline Message Media Venue.
///
/// ID: `8a86659c`.
class BotInlineMessageMediaVenue extends BotInlineMessageBase {
  /// Bot Inline Message Media Venue constructor.
  const BotInlineMessageMediaVenue({
    required this.geo,
    required this.title,
    required this.address,
    required this.provider,
    required this.venueId,
    required this.venueType,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory BotInlineMessageMediaVenue.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final geo = _readGeoPoint(buffer);
    // final title = _readstring(buffer);
    // final address = _readstring(buffer);
    // final provider = _readstring(buffer);
    // final venueId = _readstring(buffer);
    // final venueType = _readstring(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = BotInlineMessageMediaVenue(flags: flags, geo: geo, title: title, address: address, provider: provider, venueId: venueId, venueType: venueType, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: replyMarkup != null,
    );

    return v;
  }

  /// Geo.
  final GeoPointBase geo;

  /// Title.
  final String title;

  /// Address.
  final String address;

  /// Provider.
  final String provider;

  /// Venue Id.
  final String venueId;

  /// Venue Type.
  final String venueType;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8a86659c);
    buffer.writeInt(flags);
    buffer.writeObject(geo);
    buffer.writeString(title);
    buffer.writeString(address);
    buffer.writeString(provider);
    buffer.writeString(venueId);
    buffer.writeString(venueType);
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Bot Inline Message Media Contact.
///
/// ID: `18d1cdc2`.
class BotInlineMessageMediaContact extends BotInlineMessageBase {
  /// Bot Inline Message Media Contact constructor.
  const BotInlineMessageMediaContact({
    required this.phoneNumber,
    required this.firstName,
    required this.lastName,
    required this.vcard,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory BotInlineMessageMediaContact.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final phoneNumber = _readstring(buffer);
    // final firstName = _readstring(buffer);
    // final lastName = _readstring(buffer);
    // final vcard = _readstring(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = BotInlineMessageMediaContact(flags: flags, phoneNumber: phoneNumber, firstName: firstName, lastName: lastName, vcard: vcard, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: replyMarkup != null,
    );

    return v;
  }

  /// Phone Number.
  final String phoneNumber;

  /// First Name.
  final String firstName;

  /// Last Name.
  final String lastName;

  /// Vcard.
  final String vcard;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x18d1cdc2);
    buffer.writeInt(flags);
    buffer.writeString(phoneNumber);
    buffer.writeString(firstName);
    buffer.writeString(lastName);
    buffer.writeString(vcard);
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Bot Inline Message Media Invoice.
///
/// ID: `354a9b09`.
class BotInlineMessageMediaInvoice extends BotInlineMessageBase {
  /// Bot Inline Message Media Invoice constructor.
  const BotInlineMessageMediaInvoice({
    required this.shippingAddressRequested,
    required this.test,
    required this.title,
    required this.description,
    this.photo,
    required this.currency,
    required this.totalAmount,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory BotInlineMessageMediaInvoice.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final shippingAddressRequested = _readflags.1?true(buffer);
    // final test = _readflags.3?true(buffer);
    // final title = _readstring(buffer);
    // final description = _readstring(buffer);
    // final photo = _readflags.0?WebDocument(buffer);
    // final currency = _readstring(buffer);
    // final totalAmount = _readlong(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = BotInlineMessageMediaInvoice(flags: flags, shippingAddressRequested: shippingAddressRequested, test: test, title: title, description: description, photo: photo, currency: currency, totalAmount: totalAmount, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: shippingAddressRequested,
      b03: test,
      b00: photo != null,
      b02: replyMarkup != null,
    );

    return v;
  }

  /// shipping_address_requested: bit 1 of flags.1?true
  final bool shippingAddressRequested;

  /// test: bit 3 of flags.3?true
  final bool test;

  /// Title.
  final String title;

  /// Description.
  final String description;

  /// Photo.
  final WebDocumentBase? photo;

  /// Currency.
  final String currency;

  /// Total Amount.
  final int totalAmount;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x354a9b09);
    buffer.writeInt(flags);
    buffer.writeString(title);
    buffer.writeString(description);
    final localPhotoCopy = photo;
    if (localPhotoCopy != null) {
      buffer.writeObject(localPhotoCopy);
    }
    buffer.writeString(currency);
    buffer.writeLong(totalAmount);
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Bot Inline Message Media Web Page.
///
/// ID: `809ad9a6`.
class BotInlineMessageMediaWebPage extends BotInlineMessageBase {
  /// Bot Inline Message Media Web Page constructor.
  const BotInlineMessageMediaWebPage({
    required this.invertMedia,
    required this.forceLargeMedia,
    required this.forceSmallMedia,
    required this.manual,
    required this.safe,
    required this.message,
    this.entities,
    required this.url,
    this.replyMarkup,
  }) : super._();

  /// Deserialize.
  factory BotInlineMessageMediaWebPage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final invertMedia = _readflags.3?true(buffer);
    // final forceLargeMedia = _readflags.4?true(buffer);
    // final forceSmallMedia = _readflags.5?true(buffer);
    // final manual = _readflags.7?true(buffer);
    // final safe = _readflags.8?true(buffer);
    // final message = _readstring(buffer);
    // final entities = _readflags.1?Vector<MessageEntity>(buffer);
    // final url = _readstring(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final result = BotInlineMessageMediaWebPage(flags: flags, invertMedia: invertMedia, forceLargeMedia: forceLargeMedia, forceSmallMedia: forceSmallMedia, manual: manual, safe: safe, message: message, entities: entities, url: url, replyMarkup: replyMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: invertMedia,
      b04: forceLargeMedia,
      b05: forceSmallMedia,
      b07: manual,
      b08: safe,
      b01: entities != null,
      b02: replyMarkup != null,
    );

    return v;
  }

  /// invert_media: bit 3 of flags.3?true
  final bool invertMedia;

  /// force_large_media: bit 4 of flags.4?true
  final bool forceLargeMedia;

  /// force_small_media: bit 5 of flags.5?true
  final bool forceSmallMedia;

  /// manual: bit 7 of flags.7?true
  final bool manual;

  /// safe: bit 8 of flags.8?true
  final bool safe;

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Url.
  final String url;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x809ad9a6);
    buffer.writeInt(flags);
    buffer.writeString(message);
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    buffer.writeString(url);
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
  }
}

/// Predicate Bot Inline Result.
abstract class BotInlineResultBase extends TlConstructor {
  /// Predicate Bot Inline Result constructor.
  const BotInlineResultBase._() : super._();
}

/// Bot Inline Result.
///
/// ID: `11965f3a`.
class BotInlineResult extends BotInlineResultBase {
  /// Bot Inline Result constructor.
  const BotInlineResult({
    required this.id,
    required this.type,
    this.title,
    this.description,
    this.url,
    this.thumb,
    this.content,
    required this.sendMessage,
  }) : super._();

  /// Deserialize.
  factory BotInlineResult.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final id = _readstring(buffer);
    // final type = _readstring(buffer);
    // final title = _readflags.1?string(buffer);
    // final description = _readflags.2?string(buffer);
    // final url = _readflags.3?string(buffer);
    // final thumb = _readflags.4?WebDocument(buffer);
    // final content = _readflags.5?WebDocument(buffer);
    // final sendMessage = _readBotInlineMessage(buffer);
    // final result = BotInlineResult(flags: flags, id: id, type: type, title: title, description: description, url: url, thumb: thumb, content: content, sendMessage: sendMessage,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: title != null,
      b02: description != null,
      b03: url != null,
      b04: thumb != null,
      b05: content != null,
    );

    return v;
  }

  /// Id.
  final String id;

  /// Type.
  final String type;

  /// Title.
  final String? title;

  /// Description.
  final String? description;

  /// Url.
  final String? url;

  /// Thumb.
  final WebDocumentBase? thumb;

  /// Content.
  final WebDocumentBase? content;

  /// Send Message.
  final BotInlineMessageBase sendMessage;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x11965f3a);
    buffer.writeInt(flags);
    buffer.writeString(id);
    buffer.writeString(type);
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localDescriptionCopy = description;
    if (localDescriptionCopy != null) {
      buffer.writeString(localDescriptionCopy);
    }
    final localUrlCopy = url;
    if (localUrlCopy != null) {
      buffer.writeString(localUrlCopy);
    }
    final localThumbCopy = thumb;
    if (localThumbCopy != null) {
      buffer.writeObject(localThumbCopy);
    }
    final localContentCopy = content;
    if (localContentCopy != null) {
      buffer.writeObject(localContentCopy);
    }
    buffer.writeObject(sendMessage);
  }
}

/// Bot Inline Media Result.
///
/// ID: `17db940b`.
class BotInlineMediaResult extends BotInlineResultBase {
  /// Bot Inline Media Result constructor.
  const BotInlineMediaResult({
    required this.id,
    required this.type,
    this.photo,
    this.document,
    this.title,
    this.description,
    required this.sendMessage,
  }) : super._();

  /// Deserialize.
  factory BotInlineMediaResult.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final id = _readstring(buffer);
    // final type = _readstring(buffer);
    // final photo = _readflags.0?Photo(buffer);
    // final document = _readflags.1?Document(buffer);
    // final title = _readflags.2?string(buffer);
    // final description = _readflags.3?string(buffer);
    // final sendMessage = _readBotInlineMessage(buffer);
    // final result = BotInlineMediaResult(flags: flags, id: id, type: type, photo: photo, document: document, title: title, description: description, sendMessage: sendMessage,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: photo != null,
      b01: document != null,
      b02: title != null,
      b03: description != null,
    );

    return v;
  }

  /// Id.
  final String id;

  /// Type.
  final String type;

  /// Photo.
  final PhotoBase? photo;

  /// Document.
  final DocumentBase? document;

  /// Title.
  final String? title;

  /// Description.
  final String? description;

  /// Send Message.
  final BotInlineMessageBase sendMessage;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x17db940b);
    buffer.writeInt(flags);
    buffer.writeString(id);
    buffer.writeString(type);
    final localPhotoCopy = photo;
    if (localPhotoCopy != null) {
      buffer.writeObject(localPhotoCopy);
    }
    final localDocumentCopy = document;
    if (localDocumentCopy != null) {
      buffer.writeObject(localDocumentCopy);
    }
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localDescriptionCopy = description;
    if (localDescriptionCopy != null) {
      buffer.writeString(localDescriptionCopy);
    }
    buffer.writeObject(sendMessage);
  }
}

/// Predicate Messages Bot Results.
abstract class MessagesBotResultsBase extends TlConstructor {
  /// Predicate Messages Bot Results constructor.
  const MessagesBotResultsBase._() : super._();
}

/// Messages Bot Results.
///
/// ID: `e021f2f6`.
class MessagesBotResults extends MessagesBotResultsBase {
  /// Messages Bot Results constructor.
  const MessagesBotResults({
    required this.gallery,
    required this.queryId,
    this.nextOffset,
    this.switchPm,
    this.switchWebview,
    required this.results,
    required this.cacheTime,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesBotResults.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final gallery = _readflags.0?true(buffer);
    // final queryId = _readlong(buffer);
    // final nextOffset = _readflags.1?string(buffer);
    // final switchPm = _readflags.2?InlineBotSwitchPM(buffer);
    // final switchWebview = _readflags.3?InlineBotWebView(buffer);
    // final results = _readVector<BotInlineResult>(buffer);
    // final cacheTime = _readint(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesBotResults(flags: flags, gallery: gallery, queryId: queryId, nextOffset: nextOffset, switchPm: switchPm, switchWebview: switchWebview, results: results, cacheTime: cacheTime, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: gallery,
      b01: nextOffset != null,
      b02: switchPm != null,
      b03: switchWebview != null,
    );

    return v;
  }

  /// gallery: bit 0 of flags.0?true
  final bool gallery;

  /// Query Id.
  final int queryId;

  /// Next Offset.
  final String? nextOffset;

  /// Switch Pm.
  final InlineBotSwitchPMBase? switchPm;

  /// Switch Webview.
  final InlineBotWebViewBase? switchWebview;

  /// Results.
  final List<BotInlineResultBase> results;

  /// Cache Time.
  final int cacheTime;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe021f2f6);
    buffer.writeInt(flags);
    buffer.writeLong(queryId);
    final localNextOffsetCopy = nextOffset;
    if (localNextOffsetCopy != null) {
      buffer.writeString(localNextOffsetCopy);
    }
    final localSwitchPmCopy = switchPm;
    if (localSwitchPmCopy != null) {
      buffer.writeObject(localSwitchPmCopy);
    }
    final localSwitchWebviewCopy = switchWebview;
    if (localSwitchWebviewCopy != null) {
      buffer.writeObject(localSwitchWebviewCopy);
    }
    buffer.writeVectorObject(results);
    buffer.writeInt(cacheTime);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Exported Message Link.
abstract class ExportedMessageLinkBase extends TlConstructor {
  /// Predicate Exported Message Link constructor.
  const ExportedMessageLinkBase._() : super._();
}

/// Exported Message Link.
///
/// ID: `5dab1af4`.
class ExportedMessageLink extends ExportedMessageLinkBase {
  /// Exported Message Link constructor.
  const ExportedMessageLink({
    required this.link,
    required this.html,
  }) : super._();

  /// Deserialize.
  factory ExportedMessageLink.deserialize(Uint8List buffer) {
    // final link = _readstring(buffer);
    // final html = _readstring(buffer);
    // final result = ExportedMessageLink(link: link, html: html,);

    // return result;

    throw Exception();
  }

  /// Link.
  final String link;

  /// Html.
  final String html;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5dab1af4);
    buffer.writeString(link);
    buffer.writeString(html);
  }
}

/// Predicate Message Fwd Header.
abstract class MessageFwdHeaderBase extends TlConstructor {
  /// Predicate Message Fwd Header constructor.
  const MessageFwdHeaderBase._() : super._();
}

/// Message Fwd Header.
///
/// ID: `4e4df4bb`.
class MessageFwdHeader extends MessageFwdHeaderBase {
  /// Message Fwd Header constructor.
  const MessageFwdHeader({
    required this.imported,
    required this.savedOut,
    this.fromId,
    this.fromName,
    required this.date,
    this.channelPost,
    this.postAuthor,
    this.savedFromPeer,
    this.savedFromMsgId,
    this.savedFromId,
    this.savedFromName,
    this.savedDate,
    this.psaType,
  }) : super._();

  /// Deserialize.
  factory MessageFwdHeader.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final imported = _readflags.7?true(buffer);
    // final savedOut = _readflags.11?true(buffer);
    // final fromId = _readflags.0?Peer(buffer);
    // final fromName = _readflags.5?string(buffer);
    // final date = _readint(buffer);
    // final channelPost = _readflags.2?int(buffer);
    // final postAuthor = _readflags.3?string(buffer);
    // final savedFromPeer = _readflags.4?Peer(buffer);
    // final savedFromMsgId = _readflags.4?int(buffer);
    // final savedFromId = _readflags.8?Peer(buffer);
    // final savedFromName = _readflags.9?string(buffer);
    // final savedDate = _readflags.10?int(buffer);
    // final psaType = _readflags.6?string(buffer);
    // final result = MessageFwdHeader(flags: flags, imported: imported, savedOut: savedOut, fromId: fromId, fromName: fromName, date: date, channelPost: channelPost, postAuthor: postAuthor, savedFromPeer: savedFromPeer, savedFromMsgId: savedFromMsgId, savedFromId: savedFromId, savedFromName: savedFromName, savedDate: savedDate, psaType: psaType,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b07: imported,
      b11: savedOut,
      b00: fromId != null,
      b05: fromName != null,
      b02: channelPost != null,
      b03: postAuthor != null,
      b04: savedFromPeer != null || savedFromMsgId != null,
      b08: savedFromId != null,
      b09: savedFromName != null,
      b10: savedDate != null,
      b06: psaType != null,
    );

    return v;
  }

  /// imported: bit 7 of flags.7?true
  final bool imported;

  /// saved_out: bit 11 of flags.11?true
  final bool savedOut;

  /// From Id.
  final PeerBase? fromId;

  /// From Name.
  final String? fromName;

  /// Date.
  final DateTime date;

  /// Channel Post.
  final int? channelPost;

  /// Post Author.
  final String? postAuthor;

  /// Saved From Peer.
  final PeerBase? savedFromPeer;

  /// Saved From Msg Id.
  final int? savedFromMsgId;

  /// Saved From Id.
  final PeerBase? savedFromId;

  /// Saved From Name.
  final String? savedFromName;

  /// Saved Date.
  final DateTime? savedDate;

  /// Psa Type.
  final String? psaType;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4e4df4bb);
    buffer.writeInt(flags);
    final localFromIdCopy = fromId;
    if (localFromIdCopy != null) {
      buffer.writeObject(localFromIdCopy);
    }
    final localFromNameCopy = fromName;
    if (localFromNameCopy != null) {
      buffer.writeString(localFromNameCopy);
    }
    buffer.writeDateTime(date);
    final localChannelPostCopy = channelPost;
    if (localChannelPostCopy != null) {
      buffer.writeInt(localChannelPostCopy);
    }
    final localPostAuthorCopy = postAuthor;
    if (localPostAuthorCopy != null) {
      buffer.writeString(localPostAuthorCopy);
    }
    final localSavedFromPeerCopy = savedFromPeer;
    if (localSavedFromPeerCopy != null) {
      buffer.writeObject(localSavedFromPeerCopy);
    }
    final localSavedFromMsgIdCopy = savedFromMsgId;
    if (localSavedFromMsgIdCopy != null) {
      buffer.writeInt(localSavedFromMsgIdCopy);
    }
    final localSavedFromIdCopy = savedFromId;
    if (localSavedFromIdCopy != null) {
      buffer.writeObject(localSavedFromIdCopy);
    }
    final localSavedFromNameCopy = savedFromName;
    if (localSavedFromNameCopy != null) {
      buffer.writeString(localSavedFromNameCopy);
    }
    final localSavedDateCopy = savedDate;
    if (localSavedDateCopy != null) {
      buffer.writeDateTime(localSavedDateCopy);
    }
    final localPsaTypeCopy = psaType;
    if (localPsaTypeCopy != null) {
      buffer.writeString(localPsaTypeCopy);
    }
  }
}

/// Predicate Auth Code Type.
abstract class AuthCodeTypeBase extends TlConstructor {
  /// Predicate Auth Code Type constructor.
  const AuthCodeTypeBase._() : super._();
}

/// Auth Code Type Sms.
///
/// ID: `72a3158c`.
class AuthCodeTypeSms extends AuthCodeTypeBase {
  /// Auth Code Type Sms constructor.
  const AuthCodeTypeSms() : super._();

  /// Deserialize.
  factory AuthCodeTypeSms.deserialize(Uint8List buffer) {
    // final result = AuthCodeTypeSms();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x72a3158c);
  }
}

/// Auth Code Type Call.
///
/// ID: `741cd3e3`.
class AuthCodeTypeCall extends AuthCodeTypeBase {
  /// Auth Code Type Call constructor.
  const AuthCodeTypeCall() : super._();

  /// Deserialize.
  factory AuthCodeTypeCall.deserialize(Uint8List buffer) {
    // final result = AuthCodeTypeCall();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x741cd3e3);
  }
}

/// Auth Code Type Flash Call.
///
/// ID: `226ccefb`.
class AuthCodeTypeFlashCall extends AuthCodeTypeBase {
  /// Auth Code Type Flash Call constructor.
  const AuthCodeTypeFlashCall() : super._();

  /// Deserialize.
  factory AuthCodeTypeFlashCall.deserialize(Uint8List buffer) {
    // final result = AuthCodeTypeFlashCall();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x226ccefb);
  }
}

/// Auth Code Type Missed Call.
///
/// ID: `d61ad6ee`.
class AuthCodeTypeMissedCall extends AuthCodeTypeBase {
  /// Auth Code Type Missed Call constructor.
  const AuthCodeTypeMissedCall() : super._();

  /// Deserialize.
  factory AuthCodeTypeMissedCall.deserialize(Uint8List buffer) {
    // final result = AuthCodeTypeMissedCall();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd61ad6ee);
  }
}

/// Auth Code Type Fragment Sms.
///
/// ID: `06ed998c`.
class AuthCodeTypeFragmentSms extends AuthCodeTypeBase {
  /// Auth Code Type Fragment Sms constructor.
  const AuthCodeTypeFragmentSms() : super._();

  /// Deserialize.
  factory AuthCodeTypeFragmentSms.deserialize(Uint8List buffer) {
    // final result = AuthCodeTypeFragmentSms();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x06ed998c);
  }
}

/// Predicate Auth Sent Code Type.
abstract class AuthSentCodeTypeBase extends TlConstructor {
  /// Predicate Auth Sent Code Type constructor.
  const AuthSentCodeTypeBase._() : super._();
}

/// Auth Sent Code Type App.
///
/// ID: `3dbb5986`.
class AuthSentCodeTypeApp extends AuthSentCodeTypeBase {
  /// Auth Sent Code Type App constructor.
  const AuthSentCodeTypeApp({
    required this.length,
  }) : super._();

  /// Deserialize.
  factory AuthSentCodeTypeApp.deserialize(Uint8List buffer) {
    // final length = _readint(buffer);
    // final result = AuthSentCodeTypeApp(length: length,);

    // return result;

    throw Exception();
  }

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3dbb5986);
    buffer.writeInt(length);
  }
}

/// Auth Sent Code Type Sms.
///
/// ID: `c000bba2`.
class AuthSentCodeTypeSms extends AuthSentCodeTypeBase {
  /// Auth Sent Code Type Sms constructor.
  const AuthSentCodeTypeSms({
    required this.length,
  }) : super._();

  /// Deserialize.
  factory AuthSentCodeTypeSms.deserialize(Uint8List buffer) {
    // final length = _readint(buffer);
    // final result = AuthSentCodeTypeSms(length: length,);

    // return result;

    throw Exception();
  }

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc000bba2);
    buffer.writeInt(length);
  }
}

/// Auth Sent Code Type Call.
///
/// ID: `5353e5a7`.
class AuthSentCodeTypeCall extends AuthSentCodeTypeBase {
  /// Auth Sent Code Type Call constructor.
  const AuthSentCodeTypeCall({
    required this.length,
  }) : super._();

  /// Deserialize.
  factory AuthSentCodeTypeCall.deserialize(Uint8List buffer) {
    // final length = _readint(buffer);
    // final result = AuthSentCodeTypeCall(length: length,);

    // return result;

    throw Exception();
  }

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5353e5a7);
    buffer.writeInt(length);
  }
}

/// Auth Sent Code Type Flash Call.
///
/// ID: `ab03c6d9`.
class AuthSentCodeTypeFlashCall extends AuthSentCodeTypeBase {
  /// Auth Sent Code Type Flash Call constructor.
  const AuthSentCodeTypeFlashCall({
    required this.pattern,
  }) : super._();

  /// Deserialize.
  factory AuthSentCodeTypeFlashCall.deserialize(Uint8List buffer) {
    // final pattern = _readstring(buffer);
    // final result = AuthSentCodeTypeFlashCall(pattern: pattern,);

    // return result;

    throw Exception();
  }

  /// Pattern.
  final String pattern;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xab03c6d9);
    buffer.writeString(pattern);
  }
}

/// Auth Sent Code Type Missed Call.
///
/// ID: `82006484`.
class AuthSentCodeTypeMissedCall extends AuthSentCodeTypeBase {
  /// Auth Sent Code Type Missed Call constructor.
  const AuthSentCodeTypeMissedCall({
    required this.prefix,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory AuthSentCodeTypeMissedCall.deserialize(Uint8List buffer) {
    // final prefix = _readstring(buffer);
    // final length = _readint(buffer);
    // final result = AuthSentCodeTypeMissedCall(prefix: prefix, length: length,);

    // return result;

    throw Exception();
  }

  /// Prefix.
  final String prefix;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x82006484);
    buffer.writeString(prefix);
    buffer.writeInt(length);
  }
}

/// Auth Sent Code Type Email Code.
///
/// ID: `f450f59b`.
class AuthSentCodeTypeEmailCode extends AuthSentCodeTypeBase {
  /// Auth Sent Code Type Email Code constructor.
  const AuthSentCodeTypeEmailCode({
    required this.appleSigninAllowed,
    required this.googleSigninAllowed,
    required this.emailPattern,
    required this.length,
    this.resetAvailablePeriod,
    this.resetPendingDate,
  }) : super._();

  /// Deserialize.
  factory AuthSentCodeTypeEmailCode.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final appleSigninAllowed = _readflags.0?true(buffer);
    // final googleSigninAllowed = _readflags.1?true(buffer);
    // final emailPattern = _readstring(buffer);
    // final length = _readint(buffer);
    // final resetAvailablePeriod = _readflags.3?int(buffer);
    // final resetPendingDate = _readflags.4?int(buffer);
    // final result = AuthSentCodeTypeEmailCode(flags: flags, appleSigninAllowed: appleSigninAllowed, googleSigninAllowed: googleSigninAllowed, emailPattern: emailPattern, length: length, resetAvailablePeriod: resetAvailablePeriod, resetPendingDate: resetPendingDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: appleSigninAllowed,
      b01: googleSigninAllowed,
      b03: resetAvailablePeriod != null,
      b04: resetPendingDate != null,
    );

    return v;
  }

  /// apple_signin_allowed: bit 0 of flags.0?true
  final bool appleSigninAllowed;

  /// google_signin_allowed: bit 1 of flags.1?true
  final bool googleSigninAllowed;

  /// Email Pattern.
  final String emailPattern;

  /// Length.
  final int length;

  /// Reset Available Period.
  final int? resetAvailablePeriod;

  /// Reset Pending Date.
  final DateTime? resetPendingDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf450f59b);
    buffer.writeInt(flags);
    buffer.writeString(emailPattern);
    buffer.writeInt(length);
    final localResetAvailablePeriodCopy = resetAvailablePeriod;
    if (localResetAvailablePeriodCopy != null) {
      buffer.writeInt(localResetAvailablePeriodCopy);
    }
    final localResetPendingDateCopy = resetPendingDate;
    if (localResetPendingDateCopy != null) {
      buffer.writeDateTime(localResetPendingDateCopy);
    }
  }
}

/// Auth Sent Code Type Set Up Email Required.
///
/// ID: `a5491dea`.
class AuthSentCodeTypeSetUpEmailRequired extends AuthSentCodeTypeBase {
  /// Auth Sent Code Type Set Up Email Required constructor.
  const AuthSentCodeTypeSetUpEmailRequired({
    required this.appleSigninAllowed,
    required this.googleSigninAllowed,
  }) : super._();

  /// Deserialize.
  factory AuthSentCodeTypeSetUpEmailRequired.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final appleSigninAllowed = _readflags.0?true(buffer);
    // final googleSigninAllowed = _readflags.1?true(buffer);
    // final result = AuthSentCodeTypeSetUpEmailRequired(flags: flags, appleSigninAllowed: appleSigninAllowed, googleSigninAllowed: googleSigninAllowed,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: appleSigninAllowed,
      b01: googleSigninAllowed,
    );

    return v;
  }

  /// apple_signin_allowed: bit 0 of flags.0?true
  final bool appleSigninAllowed;

  /// google_signin_allowed: bit 1 of flags.1?true
  final bool googleSigninAllowed;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa5491dea);
    buffer.writeInt(flags);
  }
}

/// Auth Sent Code Type Fragment Sms.
///
/// ID: `d9565c39`.
class AuthSentCodeTypeFragmentSms extends AuthSentCodeTypeBase {
  /// Auth Sent Code Type Fragment Sms constructor.
  const AuthSentCodeTypeFragmentSms({
    required this.url,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory AuthSentCodeTypeFragmentSms.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final length = _readint(buffer);
    // final result = AuthSentCodeTypeFragmentSms(url: url, length: length,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd9565c39);
    buffer.writeString(url);
    buffer.writeInt(length);
  }
}

/// Auth Sent Code Type Firebase Sms.
///
/// ID: `e57b1432`.
class AuthSentCodeTypeFirebaseSms extends AuthSentCodeTypeBase {
  /// Auth Sent Code Type Firebase Sms constructor.
  const AuthSentCodeTypeFirebaseSms({
    this.nonce,
    this.receipt,
    this.pushTimeout,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory AuthSentCodeTypeFirebaseSms.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final nonce = _readflags.0?bytes(buffer);
    // final receipt = _readflags.1?string(buffer);
    // final pushTimeout = _readflags.1?int(buffer);
    // final length = _readint(buffer);
    // final result = AuthSentCodeTypeFirebaseSms(flags: flags, nonce: nonce, receipt: receipt, pushTimeout: pushTimeout, length: length,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: nonce != null,
      b01: receipt != null || pushTimeout != null,
    );

    return v;
  }

  /// Nonce.
  final Uint8List? nonce;

  /// Receipt.
  final String? receipt;

  /// Push Timeout.
  final int? pushTimeout;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe57b1432);
    buffer.writeInt(flags);
    final localNonceCopy = nonce;
    if (localNonceCopy != null) {
      buffer.writeBytes(localNonceCopy);
    }
    final localReceiptCopy = receipt;
    if (localReceiptCopy != null) {
      buffer.writeString(localReceiptCopy);
    }
    final localPushTimeoutCopy = pushTimeout;
    if (localPushTimeoutCopy != null) {
      buffer.writeInt(localPushTimeoutCopy);
    }
    buffer.writeInt(length);
  }
}

/// Predicate Messages Bot Callback Answer.
abstract class MessagesBotCallbackAnswerBase extends TlConstructor {
  /// Predicate Messages Bot Callback Answer constructor.
  const MessagesBotCallbackAnswerBase._() : super._();
}

/// Messages Bot Callback Answer.
///
/// ID: `36585ea4`.
class MessagesBotCallbackAnswer extends MessagesBotCallbackAnswerBase {
  /// Messages Bot Callback Answer constructor.
  const MessagesBotCallbackAnswer({
    required this.alert,
    required this.hasUrl,
    required this.nativeUi,
    this.message,
    this.url,
    required this.cacheTime,
  }) : super._();

  /// Deserialize.
  factory MessagesBotCallbackAnswer.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final alert = _readflags.1?true(buffer);
    // final hasUrl = _readflags.3?true(buffer);
    // final nativeUi = _readflags.4?true(buffer);
    // final message = _readflags.0?string(buffer);
    // final url = _readflags.2?string(buffer);
    // final cacheTime = _readint(buffer);
    // final result = MessagesBotCallbackAnswer(flags: flags, alert: alert, hasUrl: hasUrl, nativeUi: nativeUi, message: message, url: url, cacheTime: cacheTime,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: alert,
      b03: hasUrl,
      b04: nativeUi,
      b00: message != null,
      b02: url != null,
    );

    return v;
  }

  /// alert: bit 1 of flags.1?true
  final bool alert;

  /// has_url: bit 3 of flags.3?true
  final bool hasUrl;

  /// native_ui: bit 4 of flags.4?true
  final bool nativeUi;

  /// Message.
  final String? message;

  /// Url.
  final String? url;

  /// Cache Time.
  final int cacheTime;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x36585ea4);
    buffer.writeInt(flags);
    final localMessageCopy = message;
    if (localMessageCopy != null) {
      buffer.writeString(localMessageCopy);
    }
    final localUrlCopy = url;
    if (localUrlCopy != null) {
      buffer.writeString(localUrlCopy);
    }
    buffer.writeInt(cacheTime);
  }
}

/// Predicate Messages Message Edit Data.
abstract class MessagesMessageEditDataBase extends TlConstructor {
  /// Predicate Messages Message Edit Data constructor.
  const MessagesMessageEditDataBase._() : super._();
}

/// Messages Message Edit Data.
///
/// ID: `26b5dde6`.
class MessagesMessageEditData extends MessagesMessageEditDataBase {
  /// Messages Message Edit Data constructor.
  const MessagesMessageEditData({
    required this.caption,
  }) : super._();

  /// Deserialize.
  factory MessagesMessageEditData.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final caption = _readflags.0?true(buffer);
    // final result = MessagesMessageEditData(flags: flags, caption: caption,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: caption,
    );

    return v;
  }

  /// caption: bit 0 of flags.0?true
  final bool caption;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x26b5dde6);
    buffer.writeInt(flags);
  }
}

/// Predicate Input Bot Inline Message I D.
abstract class InputBotInlineMessageIDBase extends TlConstructor {
  /// Predicate Input Bot Inline Message I D constructor.
  const InputBotInlineMessageIDBase._() : super._();
}

/// Input Bot Inline Message I D.
///
/// ID: `890c3d89`.
class InputBotInlineMessageID extends InputBotInlineMessageIDBase {
  /// Input Bot Inline Message I D constructor.
  const InputBotInlineMessageID({
    required this.dcId,
    required this.id,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineMessageID.deserialize(Uint8List buffer) {
    // final dcId = _readint(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputBotInlineMessageID(dcId: dcId, id: id, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Dc Id.
  final int dcId;

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x890c3d89);
    buffer.writeInt(dcId);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
  }
}

/// Input Bot Inline Message I D64.
///
/// ID: `b6d915d7`.
class InputBotInlineMessageID64 extends InputBotInlineMessageIDBase {
  /// Input Bot Inline Message I D64 constructor.
  const InputBotInlineMessageID64({
    required this.dcId,
    required this.ownerId,
    required this.id,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputBotInlineMessageID64.deserialize(Uint8List buffer) {
    // final dcId = _readint(buffer);
    // final ownerId = _readlong(buffer);
    // final id = _readint(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputBotInlineMessageID64(dcId: dcId, ownerId: ownerId, id: id, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Dc Id.
  final int dcId;

  /// Owner Id.
  final int ownerId;

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb6d915d7);
    buffer.writeInt(dcId);
    buffer.writeLong(ownerId);
    buffer.writeInt(id);
    buffer.writeLong(accessHash);
  }
}

/// Predicate Inline Bot Switch P M.
abstract class InlineBotSwitchPMBase extends TlConstructor {
  /// Predicate Inline Bot Switch P M constructor.
  const InlineBotSwitchPMBase._() : super._();
}

/// Inline Bot Switch P M.
///
/// ID: `3c20629f`.
class InlineBotSwitchPM extends InlineBotSwitchPMBase {
  /// Inline Bot Switch P M constructor.
  const InlineBotSwitchPM({
    required this.text,
    required this.startParam,
  }) : super._();

  /// Deserialize.
  factory InlineBotSwitchPM.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final startParam = _readstring(buffer);
    // final result = InlineBotSwitchPM(text: text, startParam: startParam,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Start Param.
  final String startParam;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3c20629f);
    buffer.writeString(text);
    buffer.writeString(startParam);
  }
}

/// Predicate Messages Peer Dialogs.
abstract class MessagesPeerDialogsBase extends TlConstructor {
  /// Predicate Messages Peer Dialogs constructor.
  const MessagesPeerDialogsBase._() : super._();
}

/// Messages Peer Dialogs.
///
/// ID: `3371c354`.
class MessagesPeerDialogs extends MessagesPeerDialogsBase {
  /// Messages Peer Dialogs constructor.
  const MessagesPeerDialogs({
    required this.dialogs,
    required this.messages,
    required this.chats,
    required this.users,
    required this.state,
  }) : super._();

  /// Deserialize.
  factory MessagesPeerDialogs.deserialize(Uint8List buffer) {
    // final dialogs = _readVector<Dialog>(buffer);
    // final messages = _readVector<Message>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final state = _readupdates.State(buffer);
    // final result = MessagesPeerDialogs(dialogs: dialogs, messages: messages, chats: chats, users: users, state: state,);

    // return result;

    throw Exception();
  }

  /// Dialogs.
  final List<DialogBase> dialogs;

  /// Messages.
  final List<MessageBase> messages;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// State.
  final UpdatesStateBase state;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3371c354);
    buffer.writeVectorObject(dialogs);
    buffer.writeVectorObject(messages);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
    buffer.writeObject(state);
  }
}

/// Predicate Top Peer.
abstract class TopPeerBase extends TlConstructor {
  /// Predicate Top Peer constructor.
  const TopPeerBase._() : super._();
}

/// Top Peer.
///
/// ID: `edcdc05b`.
class TopPeer extends TopPeerBase {
  /// Top Peer constructor.
  const TopPeer({
    required this.peer,
    required this.rating,
  }) : super._();

  /// Deserialize.
  factory TopPeer.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final rating = _readdouble(buffer);
    // final result = TopPeer(peer: peer, rating: rating,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Rating.
  final double rating;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xedcdc05b);
    buffer.writeObject(peer);
    buffer.writeDouble(rating);
  }
}

/// Predicate Top Peer Category.
abstract class TopPeerCategoryBase extends TlConstructor {
  /// Predicate Top Peer Category constructor.
  const TopPeerCategoryBase._() : super._();
}

/// Top Peer Category Bots P M.
///
/// ID: `ab661b5b`.
class TopPeerCategoryBotsPM extends TopPeerCategoryBase {
  /// Top Peer Category Bots P M constructor.
  const TopPeerCategoryBotsPM() : super._();

  /// Deserialize.
  factory TopPeerCategoryBotsPM.deserialize(Uint8List buffer) {
    // final result = TopPeerCategoryBotsPM();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xab661b5b);
  }
}

/// Top Peer Category Bots Inline.
///
/// ID: `148677e2`.
class TopPeerCategoryBotsInline extends TopPeerCategoryBase {
  /// Top Peer Category Bots Inline constructor.
  const TopPeerCategoryBotsInline() : super._();

  /// Deserialize.
  factory TopPeerCategoryBotsInline.deserialize(Uint8List buffer) {
    // final result = TopPeerCategoryBotsInline();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x148677e2);
  }
}

/// Top Peer Category Correspondents.
///
/// ID: `0637b7ed`.
class TopPeerCategoryCorrespondents extends TopPeerCategoryBase {
  /// Top Peer Category Correspondents constructor.
  const TopPeerCategoryCorrespondents() : super._();

  /// Deserialize.
  factory TopPeerCategoryCorrespondents.deserialize(Uint8List buffer) {
    // final result = TopPeerCategoryCorrespondents();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0637b7ed);
  }
}

/// Top Peer Category Groups.
///
/// ID: `bd17a14a`.
class TopPeerCategoryGroups extends TopPeerCategoryBase {
  /// Top Peer Category Groups constructor.
  const TopPeerCategoryGroups() : super._();

  /// Deserialize.
  factory TopPeerCategoryGroups.deserialize(Uint8List buffer) {
    // final result = TopPeerCategoryGroups();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbd17a14a);
  }
}

/// Top Peer Category Channels.
///
/// ID: `161d9628`.
class TopPeerCategoryChannels extends TopPeerCategoryBase {
  /// Top Peer Category Channels constructor.
  const TopPeerCategoryChannels() : super._();

  /// Deserialize.
  factory TopPeerCategoryChannels.deserialize(Uint8List buffer) {
    // final result = TopPeerCategoryChannels();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x161d9628);
  }
}

/// Top Peer Category Phone Calls.
///
/// ID: `1e76a78c`.
class TopPeerCategoryPhoneCalls extends TopPeerCategoryBase {
  /// Top Peer Category Phone Calls constructor.
  const TopPeerCategoryPhoneCalls() : super._();

  /// Deserialize.
  factory TopPeerCategoryPhoneCalls.deserialize(Uint8List buffer) {
    // final result = TopPeerCategoryPhoneCalls();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1e76a78c);
  }
}

/// Top Peer Category Forward Users.
///
/// ID: `a8406ca9`.
class TopPeerCategoryForwardUsers extends TopPeerCategoryBase {
  /// Top Peer Category Forward Users constructor.
  const TopPeerCategoryForwardUsers() : super._();

  /// Deserialize.
  factory TopPeerCategoryForwardUsers.deserialize(Uint8List buffer) {
    // final result = TopPeerCategoryForwardUsers();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa8406ca9);
  }
}

/// Top Peer Category Forward Chats.
///
/// ID: `fbeec0f0`.
class TopPeerCategoryForwardChats extends TopPeerCategoryBase {
  /// Top Peer Category Forward Chats constructor.
  const TopPeerCategoryForwardChats() : super._();

  /// Deserialize.
  factory TopPeerCategoryForwardChats.deserialize(Uint8List buffer) {
    // final result = TopPeerCategoryForwardChats();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfbeec0f0);
  }
}

/// Predicate Top Peer Category Peers.
abstract class TopPeerCategoryPeersBase extends TlConstructor {
  /// Predicate Top Peer Category Peers constructor.
  const TopPeerCategoryPeersBase._() : super._();
}

/// Top Peer Category Peers.
///
/// ID: `fb834291`.
class TopPeerCategoryPeers extends TopPeerCategoryPeersBase {
  /// Top Peer Category Peers constructor.
  const TopPeerCategoryPeers({
    required this.category,
    required this.count,
    required this.peers,
  }) : super._();

  /// Deserialize.
  factory TopPeerCategoryPeers.deserialize(Uint8List buffer) {
    // final category = _readTopPeerCategory(buffer);
    // final count = _readint(buffer);
    // final peers = _readVector<TopPeer>(buffer);
    // final result = TopPeerCategoryPeers(category: category, count: count, peers: peers,);

    // return result;

    throw Exception();
  }

  /// Category.
  final TopPeerCategoryBase category;

  /// Count.
  final int count;

  /// Peers.
  final List<TopPeerBase> peers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfb834291);
    buffer.writeObject(category);
    buffer.writeInt(count);
    buffer.writeVectorObject(peers);
  }
}

/// Predicate Contacts Top Peers.
abstract class ContactsTopPeersBase extends TlConstructor {
  /// Predicate Contacts Top Peers constructor.
  const ContactsTopPeersBase._() : super._();
}

/// Contacts Top Peers Not Modified.
///
/// ID: `de266ef5`.
class ContactsTopPeersNotModified extends ContactsTopPeersBase {
  /// Contacts Top Peers Not Modified constructor.
  const ContactsTopPeersNotModified() : super._();

  /// Deserialize.
  factory ContactsTopPeersNotModified.deserialize(Uint8List buffer) {
    // final result = ContactsTopPeersNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xde266ef5);
  }
}

/// Contacts Top Peers.
///
/// ID: `70b772a8`.
class ContactsTopPeers extends ContactsTopPeersBase {
  /// Contacts Top Peers constructor.
  const ContactsTopPeers({
    required this.categories,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ContactsTopPeers.deserialize(Uint8List buffer) {
    // final categories = _readVector<TopPeerCategoryPeers>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ContactsTopPeers(categories: categories, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Categories.
  final List<TopPeerCategoryPeersBase> categories;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x70b772a8);
    buffer.writeVectorObject(categories);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Contacts Top Peers Disabled.
///
/// ID: `b52c939d`.
class ContactsTopPeersDisabled extends ContactsTopPeersBase {
  /// Contacts Top Peers Disabled constructor.
  const ContactsTopPeersDisabled() : super._();

  /// Deserialize.
  factory ContactsTopPeersDisabled.deserialize(Uint8List buffer) {
    // final result = ContactsTopPeersDisabled();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb52c939d);
  }
}

/// Predicate Draft Message.
abstract class DraftMessageBase extends TlConstructor {
  /// Predicate Draft Message constructor.
  const DraftMessageBase._() : super._();
}

/// Draft Message Empty.
///
/// ID: `1b0c841a`.
class DraftMessageEmpty extends DraftMessageBase {
  /// Draft Message Empty constructor.
  const DraftMessageEmpty({
    this.date,
  }) : super._();

  /// Deserialize.
  factory DraftMessageEmpty.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final date = _readflags.0?int(buffer);
    // final result = DraftMessageEmpty(flags: flags, date: date,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: date != null,
    );

    return v;
  }

  /// Date.
  final DateTime? date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1b0c841a);
    buffer.writeInt(flags);
    final localDateCopy = date;
    if (localDateCopy != null) {
      buffer.writeDateTime(localDateCopy);
    }
  }
}

/// Draft Message.
///
/// ID: `3fccf7ef`.
class DraftMessage extends DraftMessageBase {
  /// Draft Message constructor.
  const DraftMessage({
    required this.noWebpage,
    required this.invertMedia,
    this.replyTo,
    required this.message,
    this.entities,
    this.media,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory DraftMessage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final noWebpage = _readflags.1?true(buffer);
    // final invertMedia = _readflags.6?true(buffer);
    // final replyTo = _readflags.4?InputReplyTo(buffer);
    // final message = _readstring(buffer);
    // final entities = _readflags.3?Vector<MessageEntity>(buffer);
    // final media = _readflags.5?InputMedia(buffer);
    // final date = _readint(buffer);
    // final result = DraftMessage(flags: flags, noWebpage: noWebpage, invertMedia: invertMedia, replyTo: replyTo, message: message, entities: entities, media: media, date: date,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: noWebpage,
      b06: invertMedia,
      b04: replyTo != null,
      b03: entities != null,
      b05: media != null,
    );

    return v;
  }

  /// no_webpage: bit 1 of flags.1?true
  final bool noWebpage;

  /// invert_media: bit 6 of flags.6?true
  final bool invertMedia;

  /// Reply To.
  final InputReplyToBase? replyTo;

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Media.
  final InputMediaBase? media;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3fccf7ef);
    buffer.writeInt(flags);
    final localReplyToCopy = replyTo;
    if (localReplyToCopy != null) {
      buffer.writeObject(localReplyToCopy);
    }
    buffer.writeString(message);
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localMediaCopy = media;
    if (localMediaCopy != null) {
      buffer.writeObject(localMediaCopy);
    }
    buffer.writeDateTime(date);
  }
}

/// Predicate Messages Featured Stickers.
abstract class MessagesFeaturedStickersBase extends TlConstructor {
  /// Predicate Messages Featured Stickers constructor.
  const MessagesFeaturedStickersBase._() : super._();
}

/// Messages Featured Stickers Not Modified.
///
/// ID: `c6dc0c66`.
class MessagesFeaturedStickersNotModified extends MessagesFeaturedStickersBase {
  /// Messages Featured Stickers Not Modified constructor.
  const MessagesFeaturedStickersNotModified({
    required this.count,
  }) : super._();

  /// Deserialize.
  factory MessagesFeaturedStickersNotModified.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final result = MessagesFeaturedStickersNotModified(count: count,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc6dc0c66);
    buffer.writeInt(count);
  }
}

/// Messages Featured Stickers.
///
/// ID: `be382906`.
class MessagesFeaturedStickers extends MessagesFeaturedStickersBase {
  /// Messages Featured Stickers constructor.
  const MessagesFeaturedStickers({
    required this.premium,
    required this.hash,
    required this.count,
    required this.sets,
    required this.unread,
  }) : super._();

  /// Deserialize.
  factory MessagesFeaturedStickers.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final premium = _readflags.0?true(buffer);
    // final hash = _readlong(buffer);
    // final count = _readint(buffer);
    // final sets = _readVector<StickerSetCovered>(buffer);
    // final unread = _readVector<long>(buffer);
    // final result = MessagesFeaturedStickers(flags: flags, premium: premium, hash: hash, count: count, sets: sets, unread: unread,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: premium,
    );

    return v;
  }

  /// premium: bit 0 of flags.0?true
  final bool premium;

  /// Hash.
  final int hash;

  /// Count.
  final int count;

  /// Sets.
  final List<StickerSetCoveredBase> sets;

  /// Unread.
  final List<int> unread;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbe382906);
    buffer.writeInt(flags);
    buffer.writeLong(hash);
    buffer.writeInt(count);
    buffer.writeVectorObject(sets);
    buffer.writeVectorLong(unread);
  }
}

/// Predicate Messages Recent Stickers.
abstract class MessagesRecentStickersBase extends TlConstructor {
  /// Predicate Messages Recent Stickers constructor.
  const MessagesRecentStickersBase._() : super._();
}

/// Messages Recent Stickers Not Modified.
///
/// ID: `0b17f890`.
class MessagesRecentStickersNotModified extends MessagesRecentStickersBase {
  /// Messages Recent Stickers Not Modified constructor.
  const MessagesRecentStickersNotModified() : super._();

  /// Deserialize.
  factory MessagesRecentStickersNotModified.deserialize(Uint8List buffer) {
    // final result = MessagesRecentStickersNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0b17f890);
  }
}

/// Messages Recent Stickers.
///
/// ID: `88d37c56`.
class MessagesRecentStickers extends MessagesRecentStickersBase {
  /// Messages Recent Stickers constructor.
  const MessagesRecentStickers({
    required this.hash,
    required this.packs,
    required this.stickers,
    required this.dates,
  }) : super._();

  /// Deserialize.
  factory MessagesRecentStickers.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final packs = _readVector<StickerPack>(buffer);
    // final stickers = _readVector<Document>(buffer);
    // final dates = _readVector<int>(buffer);
    // final result = MessagesRecentStickers(hash: hash, packs: packs, stickers: stickers, dates: dates,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Packs.
  final List<StickerPackBase> packs;

  /// Stickers.
  final List<DocumentBase> stickers;

  /// Dates.
  final List<int> dates;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x88d37c56);
    buffer.writeLong(hash);
    buffer.writeVectorObject(packs);
    buffer.writeVectorObject(stickers);
    buffer.writeVectorInt(dates);
  }
}

/// Predicate Messages Archived Stickers.
abstract class MessagesArchivedStickersBase extends TlConstructor {
  /// Predicate Messages Archived Stickers constructor.
  const MessagesArchivedStickersBase._() : super._();
}

/// Messages Archived Stickers.
///
/// ID: `4fcba9c8`.
class MessagesArchivedStickers extends MessagesArchivedStickersBase {
  /// Messages Archived Stickers constructor.
  const MessagesArchivedStickers({
    required this.count,
    required this.sets,
  }) : super._();

  /// Deserialize.
  factory MessagesArchivedStickers.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final sets = _readVector<StickerSetCovered>(buffer);
    // final result = MessagesArchivedStickers(count: count, sets: sets,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Sets.
  final List<StickerSetCoveredBase> sets;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4fcba9c8);
    buffer.writeInt(count);
    buffer.writeVectorObject(sets);
  }
}

/// Predicate Messages Sticker Set Install Result.
abstract class MessagesStickerSetInstallResultBase extends TlConstructor {
  /// Predicate Messages Sticker Set Install Result constructor.
  const MessagesStickerSetInstallResultBase._() : super._();
}

/// Messages Sticker Set Install Result Success.
///
/// ID: `38641628`.
class MessagesStickerSetInstallResultSuccess
    extends MessagesStickerSetInstallResultBase {
  /// Messages Sticker Set Install Result Success constructor.
  const MessagesStickerSetInstallResultSuccess() : super._();

  /// Deserialize.
  factory MessagesStickerSetInstallResultSuccess.deserialize(Uint8List buffer) {
    // final result = MessagesStickerSetInstallResultSuccess();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x38641628);
  }
}

/// Messages Sticker Set Install Result Archive.
///
/// ID: `35e410a8`.
class MessagesStickerSetInstallResultArchive
    extends MessagesStickerSetInstallResultBase {
  /// Messages Sticker Set Install Result Archive constructor.
  const MessagesStickerSetInstallResultArchive({
    required this.sets,
  }) : super._();

  /// Deserialize.
  factory MessagesStickerSetInstallResultArchive.deserialize(Uint8List buffer) {
    // final sets = _readVector<StickerSetCovered>(buffer);
    // final result = MessagesStickerSetInstallResultArchive(sets: sets,);

    // return result;

    throw Exception();
  }

  /// Sets.
  final List<StickerSetCoveredBase> sets;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x35e410a8);
    buffer.writeVectorObject(sets);
  }
}

/// Predicate Sticker Set Covered.
abstract class StickerSetCoveredBase extends TlConstructor {
  /// Predicate Sticker Set Covered constructor.
  const StickerSetCoveredBase._() : super._();
}

/// Sticker Set Covered.
///
/// ID: `6410a5d2`.
class StickerSetCovered extends StickerSetCoveredBase {
  /// Sticker Set Covered constructor.
  const StickerSetCovered({
    required this.set,
    required this.cover,
  }) : super._();

  /// Deserialize.
  factory StickerSetCovered.deserialize(Uint8List buffer) {
    // final set = _readStickerSet(buffer);
    // final cover = _readDocument(buffer);
    // final result = StickerSetCovered(set: set, cover: cover,);

    // return result;

    throw Exception();
  }

  /// Set.
  final StickerSetBase set;

  /// Cover.
  final DocumentBase cover;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6410a5d2);
    buffer.writeObject(set);
    buffer.writeObject(cover);
  }
}

/// Sticker Set Multi Covered.
///
/// ID: `3407e51b`.
class StickerSetMultiCovered extends StickerSetCoveredBase {
  /// Sticker Set Multi Covered constructor.
  const StickerSetMultiCovered({
    required this.set,
    required this.covers,
  }) : super._();

  /// Deserialize.
  factory StickerSetMultiCovered.deserialize(Uint8List buffer) {
    // final set = _readStickerSet(buffer);
    // final covers = _readVector<Document>(buffer);
    // final result = StickerSetMultiCovered(set: set, covers: covers,);

    // return result;

    throw Exception();
  }

  /// Set.
  final StickerSetBase set;

  /// Covers.
  final List<DocumentBase> covers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3407e51b);
    buffer.writeObject(set);
    buffer.writeVectorObject(covers);
  }
}

/// Sticker Set Full Covered.
///
/// ID: `40d13c0e`.
class StickerSetFullCovered extends StickerSetCoveredBase {
  /// Sticker Set Full Covered constructor.
  const StickerSetFullCovered({
    required this.set,
    required this.packs,
    required this.keywords,
    required this.documents,
  }) : super._();

  /// Deserialize.
  factory StickerSetFullCovered.deserialize(Uint8List buffer) {
    // final set = _readStickerSet(buffer);
    // final packs = _readVector<StickerPack>(buffer);
    // final keywords = _readVector<StickerKeyword>(buffer);
    // final documents = _readVector<Document>(buffer);
    // final result = StickerSetFullCovered(set: set, packs: packs, keywords: keywords, documents: documents,);

    // return result;

    throw Exception();
  }

  /// Set.
  final StickerSetBase set;

  /// Packs.
  final List<StickerPackBase> packs;

  /// Keywords.
  final List<StickerKeywordBase> keywords;

  /// Documents.
  final List<DocumentBase> documents;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x40d13c0e);
    buffer.writeObject(set);
    buffer.writeVectorObject(packs);
    buffer.writeVectorObject(keywords);
    buffer.writeVectorObject(documents);
  }
}

/// Sticker Set No Covered.
///
/// ID: `77b15d1c`.
class StickerSetNoCovered extends StickerSetCoveredBase {
  /// Sticker Set No Covered constructor.
  const StickerSetNoCovered({
    required this.set,
  }) : super._();

  /// Deserialize.
  factory StickerSetNoCovered.deserialize(Uint8List buffer) {
    // final set = _readStickerSet(buffer);
    // final result = StickerSetNoCovered(set: set,);

    // return result;

    throw Exception();
  }

  /// Set.
  final StickerSetBase set;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x77b15d1c);
    buffer.writeObject(set);
  }
}

/// Predicate Mask Coords.
abstract class MaskCoordsBase extends TlConstructor {
  /// Predicate Mask Coords constructor.
  const MaskCoordsBase._() : super._();
}

/// Mask Coords.
///
/// ID: `aed6dbb2`.
class MaskCoords extends MaskCoordsBase {
  /// Mask Coords constructor.
  const MaskCoords({
    required this.n,
    required this.x,
    required this.y,
    required this.zoom,
  }) : super._();

  /// Deserialize.
  factory MaskCoords.deserialize(Uint8List buffer) {
    // final n = _readint(buffer);
    // final x = _readdouble(buffer);
    // final y = _readdouble(buffer);
    // final zoom = _readdouble(buffer);
    // final result = MaskCoords(n: n, x: x, y: y, zoom: zoom,);

    // return result;

    throw Exception();
  }

  /// N.
  final int n;

  /// X.
  final double x;

  /// Y.
  final double y;

  /// Zoom.
  final double zoom;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xaed6dbb2);
    buffer.writeInt(n);
    buffer.writeDouble(x);
    buffer.writeDouble(y);
    buffer.writeDouble(zoom);
  }
}

/// Predicate Input Stickered Media.
abstract class InputStickeredMediaBase extends TlConstructor {
  /// Predicate Input Stickered Media constructor.
  const InputStickeredMediaBase._() : super._();
}

/// Input Stickered Media Photo.
///
/// ID: `4a992157`.
class InputStickeredMediaPhoto extends InputStickeredMediaBase {
  /// Input Stickered Media Photo constructor.
  const InputStickeredMediaPhoto({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory InputStickeredMediaPhoto.deserialize(Uint8List buffer) {
    // final id = _readInputPhoto(buffer);
    // final result = InputStickeredMediaPhoto(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final InputPhotoBase id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4a992157);
    buffer.writeObject(id);
  }
}

/// Input Stickered Media Document.
///
/// ID: `0438865b`.
class InputStickeredMediaDocument extends InputStickeredMediaBase {
  /// Input Stickered Media Document constructor.
  const InputStickeredMediaDocument({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory InputStickeredMediaDocument.deserialize(Uint8List buffer) {
    // final id = _readInputDocument(buffer);
    // final result = InputStickeredMediaDocument(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final InputDocumentBase id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0438865b);
    buffer.writeObject(id);
  }
}

/// Predicate Game.
abstract class GameBase extends TlConstructor {
  /// Predicate Game constructor.
  const GameBase._() : super._();
}

/// Game.
///
/// ID: `bdf9653b`.
class Game extends GameBase {
  /// Game constructor.
  const Game({
    required this.id,
    required this.accessHash,
    required this.shortName,
    required this.title,
    required this.description,
    required this.photo,
    this.document,
  }) : super._();

  /// Deserialize.
  factory Game.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final shortName = _readstring(buffer);
    // final title = _readstring(buffer);
    // final description = _readstring(buffer);
    // final photo = _readPhoto(buffer);
    // final document = _readflags.0?Document(buffer);
    // final result = Game(flags: flags, id: id, accessHash: accessHash, shortName: shortName, title: title, description: description, photo: photo, document: document,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: document != null,
    );

    return v;
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Short Name.
  final String shortName;

  /// Title.
  final String title;

  /// Description.
  final String description;

  /// Photo.
  final PhotoBase photo;

  /// Document.
  final DocumentBase? document;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbdf9653b);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeString(shortName);
    buffer.writeString(title);
    buffer.writeString(description);
    buffer.writeObject(photo);
    final localDocumentCopy = document;
    if (localDocumentCopy != null) {
      buffer.writeObject(localDocumentCopy);
    }
  }
}

/// Predicate Input Game.
abstract class InputGameBase extends TlConstructor {
  /// Predicate Input Game constructor.
  const InputGameBase._() : super._();
}

/// Input Game I D.
///
/// ID: `032c3e77`.
class InputGameID extends InputGameBase {
  /// Input Game I D constructor.
  const InputGameID({
    required this.id,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputGameID.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputGameID(id: id, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x032c3e77);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
  }
}

/// Input Game Short Name.
///
/// ID: `c331e80a`.
class InputGameShortName extends InputGameBase {
  /// Input Game Short Name constructor.
  const InputGameShortName({
    required this.botId,
    required this.shortName,
  }) : super._();

  /// Deserialize.
  factory InputGameShortName.deserialize(Uint8List buffer) {
    // final botId = _readInputUser(buffer);
    // final shortName = _readstring(buffer);
    // final result = InputGameShortName(botId: botId, shortName: shortName,);

    // return result;

    throw Exception();
  }

  /// Bot Id.
  final InputUserBase botId;

  /// Short Name.
  final String shortName;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc331e80a);
    buffer.writeObject(botId);
    buffer.writeString(shortName);
  }
}

/// Predicate High Score.
abstract class HighScoreBase extends TlConstructor {
  /// Predicate High Score constructor.
  const HighScoreBase._() : super._();
}

/// High Score.
///
/// ID: `73a379eb`.
class HighScore extends HighScoreBase {
  /// High Score constructor.
  const HighScore({
    required this.pos,
    required this.userId,
    required this.score,
  }) : super._();

  /// Deserialize.
  factory HighScore.deserialize(Uint8List buffer) {
    // final pos = _readint(buffer);
    // final userId = _readlong(buffer);
    // final score = _readint(buffer);
    // final result = HighScore(pos: pos, userId: userId, score: score,);

    // return result;

    throw Exception();
  }

  /// Pos.
  final int pos;

  /// User Id.
  final int userId;

  /// Score.
  final int score;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x73a379eb);
    buffer.writeInt(pos);
    buffer.writeLong(userId);
    buffer.writeInt(score);
  }
}

/// Predicate Messages High Scores.
abstract class MessagesHighScoresBase extends TlConstructor {
  /// Predicate Messages High Scores constructor.
  const MessagesHighScoresBase._() : super._();
}

/// Messages High Scores.
///
/// ID: `9a3bfd99`.
class MessagesHighScores extends MessagesHighScoresBase {
  /// Messages High Scores constructor.
  const MessagesHighScores({
    required this.scores,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesHighScores.deserialize(Uint8List buffer) {
    // final scores = _readVector<HighScore>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesHighScores(scores: scores, users: users,);

    // return result;

    throw Exception();
  }

  /// Scores.
  final List<HighScoreBase> scores;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9a3bfd99);
    buffer.writeVectorObject(scores);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Rich Text.
abstract class RichTextBase extends TlConstructor {
  /// Predicate Rich Text constructor.
  const RichTextBase._() : super._();
}

/// Text Empty.
///
/// ID: `dc3d824f`.
class TextEmpty extends RichTextBase {
  /// Text Empty constructor.
  const TextEmpty() : super._();

  /// Deserialize.
  factory TextEmpty.deserialize(Uint8List buffer) {
    // final result = TextEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdc3d824f);
  }
}

/// Text Plain.
///
/// ID: `744694e0`.
class TextPlain extends RichTextBase {
  /// Text Plain constructor.
  const TextPlain({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory TextPlain.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final result = TextPlain(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x744694e0);
    buffer.writeString(text);
  }
}

/// Text Bold.
///
/// ID: `6724abc4`.
class TextBold extends RichTextBase {
  /// Text Bold constructor.
  const TextBold({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory TextBold.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = TextBold(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6724abc4);
    buffer.writeObject(text);
  }
}

/// Text Italic.
///
/// ID: `d912a59c`.
class TextItalic extends RichTextBase {
  /// Text Italic constructor.
  const TextItalic({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory TextItalic.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = TextItalic(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd912a59c);
    buffer.writeObject(text);
  }
}

/// Text Underline.
///
/// ID: `c12622c4`.
class TextUnderline extends RichTextBase {
  /// Text Underline constructor.
  const TextUnderline({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory TextUnderline.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = TextUnderline(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc12622c4);
    buffer.writeObject(text);
  }
}

/// Text Strike.
///
/// ID: `9bf8bb95`.
class TextStrike extends RichTextBase {
  /// Text Strike constructor.
  const TextStrike({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory TextStrike.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = TextStrike(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9bf8bb95);
    buffer.writeObject(text);
  }
}

/// Text Fixed.
///
/// ID: `6c3f19b9`.
class TextFixed extends RichTextBase {
  /// Text Fixed constructor.
  const TextFixed({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory TextFixed.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = TextFixed(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6c3f19b9);
    buffer.writeObject(text);
  }
}

/// Text Url.
///
/// ID: `3c2884c1`.
class TextUrl extends RichTextBase {
  /// Text Url constructor.
  const TextUrl({
    required this.text,
    required this.url,
    required this.webpageId,
  }) : super._();

  /// Deserialize.
  factory TextUrl.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final url = _readstring(buffer);
    // final webpageId = _readlong(buffer);
    // final result = TextUrl(text: text, url: url, webpageId: webpageId,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Url.
  final String url;

  /// Webpage Id.
  final int webpageId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3c2884c1);
    buffer.writeObject(text);
    buffer.writeString(url);
    buffer.writeLong(webpageId);
  }
}

/// Text Email.
///
/// ID: `de5a0dd6`.
class TextEmail extends RichTextBase {
  /// Text Email constructor.
  const TextEmail({
    required this.text,
    required this.email,
  }) : super._();

  /// Deserialize.
  factory TextEmail.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final email = _readstring(buffer);
    // final result = TextEmail(text: text, email: email,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Email.
  final String email;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xde5a0dd6);
    buffer.writeObject(text);
    buffer.writeString(email);
  }
}

/// Text Concat.
///
/// ID: `7e6260d7`.
class TextConcat extends RichTextBase {
  /// Text Concat constructor.
  const TextConcat({
    required this.texts,
  }) : super._();

  /// Deserialize.
  factory TextConcat.deserialize(Uint8List buffer) {
    // final texts = _readVector<RichText>(buffer);
    // final result = TextConcat(texts: texts,);

    // return result;

    throw Exception();
  }

  /// Texts.
  final List<RichTextBase> texts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7e6260d7);
    buffer.writeVectorObject(texts);
  }
}

/// Text Subscript.
///
/// ID: `ed6a8504`.
class TextSubscript extends RichTextBase {
  /// Text Subscript constructor.
  const TextSubscript({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory TextSubscript.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = TextSubscript(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xed6a8504);
    buffer.writeObject(text);
  }
}

/// Text Superscript.
///
/// ID: `c7fb5e01`.
class TextSuperscript extends RichTextBase {
  /// Text Superscript constructor.
  const TextSuperscript({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory TextSuperscript.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = TextSuperscript(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc7fb5e01);
    buffer.writeObject(text);
  }
}

/// Text Marked.
///
/// ID: `034b8621`.
class TextMarked extends RichTextBase {
  /// Text Marked constructor.
  const TextMarked({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory TextMarked.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = TextMarked(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x034b8621);
    buffer.writeObject(text);
  }
}

/// Text Phone.
///
/// ID: `1ccb966a`.
class TextPhone extends RichTextBase {
  /// Text Phone constructor.
  const TextPhone({
    required this.text,
    required this.phone,
  }) : super._();

  /// Deserialize.
  factory TextPhone.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final phone = _readstring(buffer);
    // final result = TextPhone(text: text, phone: phone,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Phone.
  final String phone;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1ccb966a);
    buffer.writeObject(text);
    buffer.writeString(phone);
  }
}

/// Text Image.
///
/// ID: `081ccf4f`.
class TextImage extends RichTextBase {
  /// Text Image constructor.
  const TextImage({
    required this.documentId,
    required this.w,
    required this.h,
  }) : super._();

  /// Deserialize.
  factory TextImage.deserialize(Uint8List buffer) {
    // final documentId = _readlong(buffer);
    // final w = _readint(buffer);
    // final h = _readint(buffer);
    // final result = TextImage(documentId: documentId, w: w, h: h,);

    // return result;

    throw Exception();
  }

  /// Document Id.
  final int documentId;

  /// W.
  final int w;

  /// H.
  final int h;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x081ccf4f);
    buffer.writeLong(documentId);
    buffer.writeInt(w);
    buffer.writeInt(h);
  }
}

/// Text Anchor.
///
/// ID: `35553762`.
class TextAnchor extends RichTextBase {
  /// Text Anchor constructor.
  const TextAnchor({
    required this.text,
    required this.name,
  }) : super._();

  /// Deserialize.
  factory TextAnchor.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final name = _readstring(buffer);
    // final result = TextAnchor(text: text, name: name,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Name.
  final String name;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x35553762);
    buffer.writeObject(text);
    buffer.writeString(name);
  }
}

/// Predicate Page Block.
abstract class PageBlockBase extends TlConstructor {
  /// Predicate Page Block constructor.
  const PageBlockBase._() : super._();
}

/// Page Block Unsupported.
///
/// ID: `13567e8a`.
class PageBlockUnsupported extends PageBlockBase {
  /// Page Block Unsupported constructor.
  const PageBlockUnsupported() : super._();

  /// Deserialize.
  factory PageBlockUnsupported.deserialize(Uint8List buffer) {
    // final result = PageBlockUnsupported();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x13567e8a);
  }
}

/// Page Block Title.
///
/// ID: `70abc3fd`.
class PageBlockTitle extends PageBlockBase {
  /// Page Block Title constructor.
  const PageBlockTitle({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory PageBlockTitle.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = PageBlockTitle(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x70abc3fd);
    buffer.writeObject(text);
  }
}

/// Page Block Subtitle.
///
/// ID: `8ffa9a1f`.
class PageBlockSubtitle extends PageBlockBase {
  /// Page Block Subtitle constructor.
  const PageBlockSubtitle({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory PageBlockSubtitle.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = PageBlockSubtitle(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8ffa9a1f);
    buffer.writeObject(text);
  }
}

/// Page Block Author Date.
///
/// ID: `baafe5e0`.
class PageBlockAuthorDate extends PageBlockBase {
  /// Page Block Author Date constructor.
  const PageBlockAuthorDate({
    required this.author,
    required this.publishedDate,
  }) : super._();

  /// Deserialize.
  factory PageBlockAuthorDate.deserialize(Uint8List buffer) {
    // final author = _readRichText(buffer);
    // final publishedDate = _readint(buffer);
    // final result = PageBlockAuthorDate(author: author, publishedDate: publishedDate,);

    // return result;

    throw Exception();
  }

  /// Author.
  final RichTextBase author;

  /// Published Date.
  final DateTime publishedDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbaafe5e0);
    buffer.writeObject(author);
    buffer.writeDateTime(publishedDate);
  }
}

/// Page Block Header.
///
/// ID: `bfd064ec`.
class PageBlockHeader extends PageBlockBase {
  /// Page Block Header constructor.
  const PageBlockHeader({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory PageBlockHeader.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = PageBlockHeader(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbfd064ec);
    buffer.writeObject(text);
  }
}

/// Page Block Subheader.
///
/// ID: `f12bb6e1`.
class PageBlockSubheader extends PageBlockBase {
  /// Page Block Subheader constructor.
  const PageBlockSubheader({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory PageBlockSubheader.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = PageBlockSubheader(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf12bb6e1);
    buffer.writeObject(text);
  }
}

/// Page Block Paragraph.
///
/// ID: `467a0766`.
class PageBlockParagraph extends PageBlockBase {
  /// Page Block Paragraph constructor.
  const PageBlockParagraph({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory PageBlockParagraph.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = PageBlockParagraph(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x467a0766);
    buffer.writeObject(text);
  }
}

/// Page Block Preformatted.
///
/// ID: `c070d93e`.
class PageBlockPreformatted extends PageBlockBase {
  /// Page Block Preformatted constructor.
  const PageBlockPreformatted({
    required this.text,
    required this.language,
  }) : super._();

  /// Deserialize.
  factory PageBlockPreformatted.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final language = _readstring(buffer);
    // final result = PageBlockPreformatted(text: text, language: language,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Language.
  final String language;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc070d93e);
    buffer.writeObject(text);
    buffer.writeString(language);
  }
}

/// Page Block Footer.
///
/// ID: `48870999`.
class PageBlockFooter extends PageBlockBase {
  /// Page Block Footer constructor.
  const PageBlockFooter({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory PageBlockFooter.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = PageBlockFooter(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x48870999);
    buffer.writeObject(text);
  }
}

/// Page Block Divider.
///
/// ID: `db20b188`.
class PageBlockDivider extends PageBlockBase {
  /// Page Block Divider constructor.
  const PageBlockDivider() : super._();

  /// Deserialize.
  factory PageBlockDivider.deserialize(Uint8List buffer) {
    // final result = PageBlockDivider();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdb20b188);
  }
}

/// Page Block Anchor.
///
/// ID: `ce0d37b0`.
class PageBlockAnchor extends PageBlockBase {
  /// Page Block Anchor constructor.
  const PageBlockAnchor({
    required this.name,
  }) : super._();

  /// Deserialize.
  factory PageBlockAnchor.deserialize(Uint8List buffer) {
    // final name = _readstring(buffer);
    // final result = PageBlockAnchor(name: name,);

    // return result;

    throw Exception();
  }

  /// Name.
  final String name;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xce0d37b0);
    buffer.writeString(name);
  }
}

/// Page Block List.
///
/// ID: `e4e88011`.
class PageBlockList extends PageBlockBase {
  /// Page Block List constructor.
  const PageBlockList({
    required this.items,
  }) : super._();

  /// Deserialize.
  factory PageBlockList.deserialize(Uint8List buffer) {
    // final items = _readVector<PageListItem>(buffer);
    // final result = PageBlockList(items: items,);

    // return result;

    throw Exception();
  }

  /// Items.
  final List<PageListItemBase> items;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe4e88011);
    buffer.writeVectorObject(items);
  }
}

/// Page Block Blockquote.
///
/// ID: `263d7c26`.
class PageBlockBlockquote extends PageBlockBase {
  /// Page Block Blockquote constructor.
  const PageBlockBlockquote({
    required this.text,
    required this.caption,
  }) : super._();

  /// Deserialize.
  factory PageBlockBlockquote.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final caption = _readRichText(buffer);
    // final result = PageBlockBlockquote(text: text, caption: caption,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Caption.
  final RichTextBase caption;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x263d7c26);
    buffer.writeObject(text);
    buffer.writeObject(caption);
  }
}

/// Page Block Pullquote.
///
/// ID: `4f4456d3`.
class PageBlockPullquote extends PageBlockBase {
  /// Page Block Pullquote constructor.
  const PageBlockPullquote({
    required this.text,
    required this.caption,
  }) : super._();

  /// Deserialize.
  factory PageBlockPullquote.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final caption = _readRichText(buffer);
    // final result = PageBlockPullquote(text: text, caption: caption,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Caption.
  final RichTextBase caption;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4f4456d3);
    buffer.writeObject(text);
    buffer.writeObject(caption);
  }
}

/// Page Block Photo.
///
/// ID: `1759c560`.
class PageBlockPhoto extends PageBlockBase {
  /// Page Block Photo constructor.
  const PageBlockPhoto({
    required this.photoId,
    required this.caption,
    this.url,
    this.webpageId,
  }) : super._();

  /// Deserialize.
  factory PageBlockPhoto.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final photoId = _readlong(buffer);
    // final caption = _readPageCaption(buffer);
    // final url = _readflags.0?string(buffer);
    // final webpageId = _readflags.0?long(buffer);
    // final result = PageBlockPhoto(flags: flags, photoId: photoId, caption: caption, url: url, webpageId: webpageId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: url != null || webpageId != null,
    );

    return v;
  }

  /// Photo Id.
  final int photoId;

  /// Caption.
  final PageCaptionBase caption;

  /// Url.
  final String? url;

  /// Webpage Id.
  final int? webpageId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1759c560);
    buffer.writeInt(flags);
    buffer.writeLong(photoId);
    buffer.writeObject(caption);
    final localUrlCopy = url;
    if (localUrlCopy != null) {
      buffer.writeString(localUrlCopy);
    }
    final localWebpageIdCopy = webpageId;
    if (localWebpageIdCopy != null) {
      buffer.writeLong(localWebpageIdCopy);
    }
  }
}

/// Page Block Video.
///
/// ID: `7c8fe7b6`.
class PageBlockVideo extends PageBlockBase {
  /// Page Block Video constructor.
  const PageBlockVideo({
    required this.autoplay,
    required this.loop,
    required this.videoId,
    required this.caption,
  }) : super._();

  /// Deserialize.
  factory PageBlockVideo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final autoplay = _readflags.0?true(buffer);
    // final loop = _readflags.1?true(buffer);
    // final videoId = _readlong(buffer);
    // final caption = _readPageCaption(buffer);
    // final result = PageBlockVideo(flags: flags, autoplay: autoplay, loop: loop, videoId: videoId, caption: caption,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: autoplay,
      b01: loop,
    );

    return v;
  }

  /// autoplay: bit 0 of flags.0?true
  final bool autoplay;

  /// loop: bit 1 of flags.1?true
  final bool loop;

  /// Video Id.
  final int videoId;

  /// Caption.
  final PageCaptionBase caption;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7c8fe7b6);
    buffer.writeInt(flags);
    buffer.writeLong(videoId);
    buffer.writeObject(caption);
  }
}

/// Page Block Cover.
///
/// ID: `39f23300`.
class PageBlockCover extends PageBlockBase {
  /// Page Block Cover constructor.
  const PageBlockCover({
    required this.cover,
  }) : super._();

  /// Deserialize.
  factory PageBlockCover.deserialize(Uint8List buffer) {
    // final cover = _readPageBlock(buffer);
    // final result = PageBlockCover(cover: cover,);

    // return result;

    throw Exception();
  }

  /// Cover.
  final PageBlockBase cover;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x39f23300);
    buffer.writeObject(cover);
  }
}

/// Page Block Embed.
///
/// ID: `a8718dc5`.
class PageBlockEmbed extends PageBlockBase {
  /// Page Block Embed constructor.
  const PageBlockEmbed({
    required this.fullWidth,
    required this.allowScrolling,
    this.url,
    this.html,
    this.posterPhotoId,
    this.w,
    this.h,
    required this.caption,
  }) : super._();

  /// Deserialize.
  factory PageBlockEmbed.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final fullWidth = _readflags.0?true(buffer);
    // final allowScrolling = _readflags.3?true(buffer);
    // final url = _readflags.1?string(buffer);
    // final html = _readflags.2?string(buffer);
    // final posterPhotoId = _readflags.4?long(buffer);
    // final w = _readflags.5?int(buffer);
    // final h = _readflags.5?int(buffer);
    // final caption = _readPageCaption(buffer);
    // final result = PageBlockEmbed(flags: flags, fullWidth: fullWidth, allowScrolling: allowScrolling, url: url, html: html, posterPhotoId: posterPhotoId, w: w, h: h, caption: caption,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: fullWidth,
      b03: allowScrolling,
      b01: url != null,
      b02: html != null,
      b04: posterPhotoId != null,
      b05: w != null || h != null,
    );

    return v;
  }

  /// full_width: bit 0 of flags.0?true
  final bool fullWidth;

  /// allow_scrolling: bit 3 of flags.3?true
  final bool allowScrolling;

  /// Url.
  final String? url;

  /// Html.
  final String? html;

  /// Poster Photo Id.
  final int? posterPhotoId;

  /// W.
  final int? w;

  /// H.
  final int? h;

  /// Caption.
  final PageCaptionBase caption;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa8718dc5);
    buffer.writeInt(flags);
    final localUrlCopy = url;
    if (localUrlCopy != null) {
      buffer.writeString(localUrlCopy);
    }
    final localHtmlCopy = html;
    if (localHtmlCopy != null) {
      buffer.writeString(localHtmlCopy);
    }
    final localPosterPhotoIdCopy = posterPhotoId;
    if (localPosterPhotoIdCopy != null) {
      buffer.writeLong(localPosterPhotoIdCopy);
    }
    final localWCopy = w;
    if (localWCopy != null) {
      buffer.writeInt(localWCopy);
    }
    final localHCopy = h;
    if (localHCopy != null) {
      buffer.writeInt(localHCopy);
    }
    buffer.writeObject(caption);
  }
}

/// Page Block Embed Post.
///
/// ID: `f259a80b`.
class PageBlockEmbedPost extends PageBlockBase {
  /// Page Block Embed Post constructor.
  const PageBlockEmbedPost({
    required this.url,
    required this.webpageId,
    required this.authorPhotoId,
    required this.author,
    required this.date,
    required this.blocks,
    required this.caption,
  }) : super._();

  /// Deserialize.
  factory PageBlockEmbedPost.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final webpageId = _readlong(buffer);
    // final authorPhotoId = _readlong(buffer);
    // final author = _readstring(buffer);
    // final date = _readint(buffer);
    // final blocks = _readVector<PageBlock>(buffer);
    // final caption = _readPageCaption(buffer);
    // final result = PageBlockEmbedPost(url: url, webpageId: webpageId, authorPhotoId: authorPhotoId, author: author, date: date, blocks: blocks, caption: caption,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Webpage Id.
  final int webpageId;

  /// Author Photo Id.
  final int authorPhotoId;

  /// Author.
  final String author;

  /// Date.
  final DateTime date;

  /// Blocks.
  final List<PageBlockBase> blocks;

  /// Caption.
  final PageCaptionBase caption;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf259a80b);
    buffer.writeString(url);
    buffer.writeLong(webpageId);
    buffer.writeLong(authorPhotoId);
    buffer.writeString(author);
    buffer.writeDateTime(date);
    buffer.writeVectorObject(blocks);
    buffer.writeObject(caption);
  }
}

/// Page Block Collage.
///
/// ID: `65a0fa4d`.
class PageBlockCollage extends PageBlockBase {
  /// Page Block Collage constructor.
  const PageBlockCollage({
    required this.items,
    required this.caption,
  }) : super._();

  /// Deserialize.
  factory PageBlockCollage.deserialize(Uint8List buffer) {
    // final items = _readVector<PageBlock>(buffer);
    // final caption = _readPageCaption(buffer);
    // final result = PageBlockCollage(items: items, caption: caption,);

    // return result;

    throw Exception();
  }

  /// Items.
  final List<PageBlockBase> items;

  /// Caption.
  final PageCaptionBase caption;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x65a0fa4d);
    buffer.writeVectorObject(items);
    buffer.writeObject(caption);
  }
}

/// Page Block Slideshow.
///
/// ID: `031f9590`.
class PageBlockSlideshow extends PageBlockBase {
  /// Page Block Slideshow constructor.
  const PageBlockSlideshow({
    required this.items,
    required this.caption,
  }) : super._();

  /// Deserialize.
  factory PageBlockSlideshow.deserialize(Uint8List buffer) {
    // final items = _readVector<PageBlock>(buffer);
    // final caption = _readPageCaption(buffer);
    // final result = PageBlockSlideshow(items: items, caption: caption,);

    // return result;

    throw Exception();
  }

  /// Items.
  final List<PageBlockBase> items;

  /// Caption.
  final PageCaptionBase caption;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x031f9590);
    buffer.writeVectorObject(items);
    buffer.writeObject(caption);
  }
}

/// Page Block Channel.
///
/// ID: `ef1751b5`.
class PageBlockChannel extends PageBlockBase {
  /// Page Block Channel constructor.
  const PageBlockChannel({
    required this.channel,
  }) : super._();

  /// Deserialize.
  factory PageBlockChannel.deserialize(Uint8List buffer) {
    // final channel = _readChat(buffer);
    // final result = PageBlockChannel(channel: channel,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final ChatBase channel;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xef1751b5);
    buffer.writeObject(channel);
  }
}

/// Page Block Audio.
///
/// ID: `804361ea`.
class PageBlockAudio extends PageBlockBase {
  /// Page Block Audio constructor.
  const PageBlockAudio({
    required this.audioId,
    required this.caption,
  }) : super._();

  /// Deserialize.
  factory PageBlockAudio.deserialize(Uint8List buffer) {
    // final audioId = _readlong(buffer);
    // final caption = _readPageCaption(buffer);
    // final result = PageBlockAudio(audioId: audioId, caption: caption,);

    // return result;

    throw Exception();
  }

  /// Audio Id.
  final int audioId;

  /// Caption.
  final PageCaptionBase caption;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x804361ea);
    buffer.writeLong(audioId);
    buffer.writeObject(caption);
  }
}

/// Page Block Kicker.
///
/// ID: `1e148390`.
class PageBlockKicker extends PageBlockBase {
  /// Page Block Kicker constructor.
  const PageBlockKicker({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory PageBlockKicker.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = PageBlockKicker(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1e148390);
    buffer.writeObject(text);
  }
}

/// Page Block Table.
///
/// ID: `bf4dea82`.
class PageBlockTable extends PageBlockBase {
  /// Page Block Table constructor.
  const PageBlockTable({
    required this.bordered,
    required this.striped,
    required this.title,
    required this.rows,
  }) : super._();

  /// Deserialize.
  factory PageBlockTable.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final bordered = _readflags.0?true(buffer);
    // final striped = _readflags.1?true(buffer);
    // final title = _readRichText(buffer);
    // final rows = _readVector<PageTableRow>(buffer);
    // final result = PageBlockTable(flags: flags, bordered: bordered, striped: striped, title: title, rows: rows,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: bordered,
      b01: striped,
    );

    return v;
  }

  /// bordered: bit 0 of flags.0?true
  final bool bordered;

  /// striped: bit 1 of flags.1?true
  final bool striped;

  /// Title.
  final RichTextBase title;

  /// Rows.
  final List<PageTableRowBase> rows;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbf4dea82);
    buffer.writeInt(flags);
    buffer.writeObject(title);
    buffer.writeVectorObject(rows);
  }
}

/// Page Block Ordered List.
///
/// ID: `9a8ae1e1`.
class PageBlockOrderedList extends PageBlockBase {
  /// Page Block Ordered List constructor.
  const PageBlockOrderedList({
    required this.items,
  }) : super._();

  /// Deserialize.
  factory PageBlockOrderedList.deserialize(Uint8List buffer) {
    // final items = _readVector<PageListOrderedItem>(buffer);
    // final result = PageBlockOrderedList(items: items,);

    // return result;

    throw Exception();
  }

  /// Items.
  final List<PageListOrderedItemBase> items;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9a8ae1e1);
    buffer.writeVectorObject(items);
  }
}

/// Page Block Details.
///
/// ID: `76768bed`.
class PageBlockDetails extends PageBlockBase {
  /// Page Block Details constructor.
  const PageBlockDetails({
    required this.open,
    required this.blocks,
    required this.title,
  }) : super._();

  /// Deserialize.
  factory PageBlockDetails.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final open = _readflags.0?true(buffer);
    // final blocks = _readVector<PageBlock>(buffer);
    // final title = _readRichText(buffer);
    // final result = PageBlockDetails(flags: flags, open: open, blocks: blocks, title: title,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: open,
    );

    return v;
  }

  /// open: bit 0 of flags.0?true
  final bool open;

  /// Blocks.
  final List<PageBlockBase> blocks;

  /// Title.
  final RichTextBase title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x76768bed);
    buffer.writeInt(flags);
    buffer.writeVectorObject(blocks);
    buffer.writeObject(title);
  }
}

/// Page Block Related Articles.
///
/// ID: `16115a96`.
class PageBlockRelatedArticles extends PageBlockBase {
  /// Page Block Related Articles constructor.
  const PageBlockRelatedArticles({
    required this.title,
    required this.articles,
  }) : super._();

  /// Deserialize.
  factory PageBlockRelatedArticles.deserialize(Uint8List buffer) {
    // final title = _readRichText(buffer);
    // final articles = _readVector<PageRelatedArticle>(buffer);
    // final result = PageBlockRelatedArticles(title: title, articles: articles,);

    // return result;

    throw Exception();
  }

  /// Title.
  final RichTextBase title;

  /// Articles.
  final List<PageRelatedArticleBase> articles;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x16115a96);
    buffer.writeObject(title);
    buffer.writeVectorObject(articles);
  }
}

/// Page Block Map.
///
/// ID: `a44f3ef6`.
class PageBlockMap extends PageBlockBase {
  /// Page Block Map constructor.
  const PageBlockMap({
    required this.geo,
    required this.zoom,
    required this.w,
    required this.h,
    required this.caption,
  }) : super._();

  /// Deserialize.
  factory PageBlockMap.deserialize(Uint8List buffer) {
    // final geo = _readGeoPoint(buffer);
    // final zoom = _readint(buffer);
    // final w = _readint(buffer);
    // final h = _readint(buffer);
    // final caption = _readPageCaption(buffer);
    // final result = PageBlockMap(geo: geo, zoom: zoom, w: w, h: h, caption: caption,);

    // return result;

    throw Exception();
  }

  /// Geo.
  final GeoPointBase geo;

  /// Zoom.
  final int zoom;

  /// W.
  final int w;

  /// H.
  final int h;

  /// Caption.
  final PageCaptionBase caption;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa44f3ef6);
    buffer.writeObject(geo);
    buffer.writeInt(zoom);
    buffer.writeInt(w);
    buffer.writeInt(h);
    buffer.writeObject(caption);
  }
}

/// Predicate Phone Call Discard Reason.
abstract class PhoneCallDiscardReasonBase extends TlConstructor {
  /// Predicate Phone Call Discard Reason constructor.
  const PhoneCallDiscardReasonBase._() : super._();
}

/// Phone Call Discard Reason Missed.
///
/// ID: `85e42301`.
class PhoneCallDiscardReasonMissed extends PhoneCallDiscardReasonBase {
  /// Phone Call Discard Reason Missed constructor.
  const PhoneCallDiscardReasonMissed() : super._();

  /// Deserialize.
  factory PhoneCallDiscardReasonMissed.deserialize(Uint8List buffer) {
    // final result = PhoneCallDiscardReasonMissed();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x85e42301);
  }
}

/// Phone Call Discard Reason Disconnect.
///
/// ID: `e095c1a0`.
class PhoneCallDiscardReasonDisconnect extends PhoneCallDiscardReasonBase {
  /// Phone Call Discard Reason Disconnect constructor.
  const PhoneCallDiscardReasonDisconnect() : super._();

  /// Deserialize.
  factory PhoneCallDiscardReasonDisconnect.deserialize(Uint8List buffer) {
    // final result = PhoneCallDiscardReasonDisconnect();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe095c1a0);
  }
}

/// Phone Call Discard Reason Hangup.
///
/// ID: `57adc690`.
class PhoneCallDiscardReasonHangup extends PhoneCallDiscardReasonBase {
  /// Phone Call Discard Reason Hangup constructor.
  const PhoneCallDiscardReasonHangup() : super._();

  /// Deserialize.
  factory PhoneCallDiscardReasonHangup.deserialize(Uint8List buffer) {
    // final result = PhoneCallDiscardReasonHangup();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x57adc690);
  }
}

/// Phone Call Discard Reason Busy.
///
/// ID: `faf7e8c9`.
class PhoneCallDiscardReasonBusy extends PhoneCallDiscardReasonBase {
  /// Phone Call Discard Reason Busy constructor.
  const PhoneCallDiscardReasonBusy() : super._();

  /// Deserialize.
  factory PhoneCallDiscardReasonBusy.deserialize(Uint8List buffer) {
    // final result = PhoneCallDiscardReasonBusy();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfaf7e8c9);
  }
}

/// Predicate Data J S O N.
abstract class DataJSONBase extends TlConstructor {
  /// Predicate Data J S O N constructor.
  const DataJSONBase._() : super._();
}

/// Data J S O N.
///
/// ID: `7d748d04`.
class DataJSON extends DataJSONBase {
  /// Data J S O N constructor.
  const DataJSON({
    required this.data,
  }) : super._();

  /// Deserialize.
  factory DataJSON.deserialize(Uint8List buffer) {
    // final data = _readstring(buffer);
    // final result = DataJSON(data: data,);

    // return result;

    throw Exception();
  }

  /// Data.
  final String data;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7d748d04);
    buffer.writeString(data);
  }
}

/// Predicate Labeled Price.
abstract class LabeledPriceBase extends TlConstructor {
  /// Predicate Labeled Price constructor.
  const LabeledPriceBase._() : super._();
}

/// Labeled Price.
///
/// ID: `cb296bf8`.
class LabeledPrice extends LabeledPriceBase {
  /// Labeled Price constructor.
  const LabeledPrice({
    required this.label,
    required this.amount,
  }) : super._();

  /// Deserialize.
  factory LabeledPrice.deserialize(Uint8List buffer) {
    // final label = _readstring(buffer);
    // final amount = _readlong(buffer);
    // final result = LabeledPrice(label: label, amount: amount,);

    // return result;

    throw Exception();
  }

  /// Label.
  final String label;

  /// Amount.
  final int amount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcb296bf8);
    buffer.writeString(label);
    buffer.writeLong(amount);
  }
}

/// Predicate Invoice.
abstract class InvoiceBase extends TlConstructor {
  /// Predicate Invoice constructor.
  const InvoiceBase._() : super._();
}

/// Invoice.
///
/// ID: `5db95a15`.
class Invoice extends InvoiceBase {
  /// Invoice constructor.
  const Invoice({
    required this.test,
    required this.nameRequested,
    required this.phoneRequested,
    required this.emailRequested,
    required this.shippingAddressRequested,
    required this.flexible,
    required this.phoneToProvider,
    required this.emailToProvider,
    required this.recurring,
    required this.currency,
    required this.prices,
    this.maxTipAmount,
    this.suggestedTipAmounts,
    this.termsUrl,
  }) : super._();

  /// Deserialize.
  factory Invoice.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final test = _readflags.0?true(buffer);
    // final nameRequested = _readflags.1?true(buffer);
    // final phoneRequested = _readflags.2?true(buffer);
    // final emailRequested = _readflags.3?true(buffer);
    // final shippingAddressRequested = _readflags.4?true(buffer);
    // final flexible = _readflags.5?true(buffer);
    // final phoneToProvider = _readflags.6?true(buffer);
    // final emailToProvider = _readflags.7?true(buffer);
    // final recurring = _readflags.9?true(buffer);
    // final currency = _readstring(buffer);
    // final prices = _readVector<LabeledPrice>(buffer);
    // final maxTipAmount = _readflags.8?long(buffer);
    // final suggestedTipAmounts = _readflags.8?Vector<long>(buffer);
    // final termsUrl = _readflags.10?string(buffer);
    // final result = Invoice(flags: flags, test: test, nameRequested: nameRequested, phoneRequested: phoneRequested, emailRequested: emailRequested, shippingAddressRequested: shippingAddressRequested, flexible: flexible, phoneToProvider: phoneToProvider, emailToProvider: emailToProvider, recurring: recurring, currency: currency, prices: prices, maxTipAmount: maxTipAmount, suggestedTipAmounts: suggestedTipAmounts, termsUrl: termsUrl,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: test,
      b01: nameRequested,
      b02: phoneRequested,
      b03: emailRequested,
      b04: shippingAddressRequested,
      b05: flexible,
      b06: phoneToProvider,
      b07: emailToProvider,
      b09: recurring,
      b08: maxTipAmount != null || suggestedTipAmounts != null,
      b10: termsUrl != null,
    );

    return v;
  }

  /// test: bit 0 of flags.0?true
  final bool test;

  /// name_requested: bit 1 of flags.1?true
  final bool nameRequested;

  /// phone_requested: bit 2 of flags.2?true
  final bool phoneRequested;

  /// email_requested: bit 3 of flags.3?true
  final bool emailRequested;

  /// shipping_address_requested: bit 4 of flags.4?true
  final bool shippingAddressRequested;

  /// flexible: bit 5 of flags.5?true
  final bool flexible;

  /// phone_to_provider: bit 6 of flags.6?true
  final bool phoneToProvider;

  /// email_to_provider: bit 7 of flags.7?true
  final bool emailToProvider;

  /// recurring: bit 9 of flags.9?true
  final bool recurring;

  /// Currency.
  final String currency;

  /// Prices.
  final List<LabeledPriceBase> prices;

  /// Max Tip Amount.
  final int? maxTipAmount;

  /// Suggested Tip Amounts.
  final List<int>? suggestedTipAmounts;

  /// Terms Url.
  final String? termsUrl;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5db95a15);
    buffer.writeInt(flags);
    buffer.writeString(currency);
    buffer.writeVectorObject(prices);
    final localMaxTipAmountCopy = maxTipAmount;
    if (localMaxTipAmountCopy != null) {
      buffer.writeLong(localMaxTipAmountCopy);
    }
    final localSuggestedTipAmountsCopy = suggestedTipAmounts;
    if (localSuggestedTipAmountsCopy != null) {
      buffer.writeVectorLong(localSuggestedTipAmountsCopy);
    }
    final localTermsUrlCopy = termsUrl;
    if (localTermsUrlCopy != null) {
      buffer.writeString(localTermsUrlCopy);
    }
  }
}

/// Predicate Payment Charge.
abstract class PaymentChargeBase extends TlConstructor {
  /// Predicate Payment Charge constructor.
  const PaymentChargeBase._() : super._();
}

/// Payment Charge.
///
/// ID: `ea02c27e`.
class PaymentCharge extends PaymentChargeBase {
  /// Payment Charge constructor.
  const PaymentCharge({
    required this.id,
    required this.providerChargeId,
  }) : super._();

  /// Deserialize.
  factory PaymentCharge.deserialize(Uint8List buffer) {
    // final id = _readstring(buffer);
    // final providerChargeId = _readstring(buffer);
    // final result = PaymentCharge(id: id, providerChargeId: providerChargeId,);

    // return result;

    throw Exception();
  }

  /// Id.
  final String id;

  /// Provider Charge Id.
  final String providerChargeId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xea02c27e);
    buffer.writeString(id);
    buffer.writeString(providerChargeId);
  }
}

/// Predicate Post Address.
abstract class PostAddressBase extends TlConstructor {
  /// Predicate Post Address constructor.
  const PostAddressBase._() : super._();
}

/// Post Address.
///
/// ID: `1e8caaeb`.
class PostAddress extends PostAddressBase {
  /// Post Address constructor.
  const PostAddress({
    required this.streetLine1,
    required this.streetLine2,
    required this.city,
    required this.state,
    required this.countryIso2,
    required this.postCode,
  }) : super._();

  /// Deserialize.
  factory PostAddress.deserialize(Uint8List buffer) {
    // final streetLine1 = _readstring(buffer);
    // final streetLine2 = _readstring(buffer);
    // final city = _readstring(buffer);
    // final state = _readstring(buffer);
    // final countryIso2 = _readstring(buffer);
    // final postCode = _readstring(buffer);
    // final result = PostAddress(streetLine1: streetLine1, streetLine2: streetLine2, city: city, state: state, countryIso2: countryIso2, postCode: postCode,);

    // return result;

    throw Exception();
  }

  /// Street Line1.
  final String streetLine1;

  /// Street Line2.
  final String streetLine2;

  /// City.
  final String city;

  /// State.
  final String state;

  /// Country Iso2.
  final String countryIso2;

  /// Post Code.
  final String postCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1e8caaeb);
    buffer.writeString(streetLine1);
    buffer.writeString(streetLine2);
    buffer.writeString(city);
    buffer.writeString(state);
    buffer.writeString(countryIso2);
    buffer.writeString(postCode);
  }
}

/// Predicate Payment Requested Info.
abstract class PaymentRequestedInfoBase extends TlConstructor {
  /// Predicate Payment Requested Info constructor.
  const PaymentRequestedInfoBase._() : super._();
}

/// Payment Requested Info.
///
/// ID: `909c3f94`.
class PaymentRequestedInfo extends PaymentRequestedInfoBase {
  /// Payment Requested Info constructor.
  const PaymentRequestedInfo({
    this.name,
    this.phone,
    this.email,
    this.shippingAddress,
  }) : super._();

  /// Deserialize.
  factory PaymentRequestedInfo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final name = _readflags.0?string(buffer);
    // final phone = _readflags.1?string(buffer);
    // final email = _readflags.2?string(buffer);
    // final shippingAddress = _readflags.3?PostAddress(buffer);
    // final result = PaymentRequestedInfo(flags: flags, name: name, phone: phone, email: email, shippingAddress: shippingAddress,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: name != null,
      b01: phone != null,
      b02: email != null,
      b03: shippingAddress != null,
    );

    return v;
  }

  /// Name.
  final String? name;

  /// Phone.
  final String? phone;

  /// Email.
  final String? email;

  /// Shipping Address.
  final PostAddressBase? shippingAddress;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x909c3f94);
    buffer.writeInt(flags);
    final localNameCopy = name;
    if (localNameCopy != null) {
      buffer.writeString(localNameCopy);
    }
    final localPhoneCopy = phone;
    if (localPhoneCopy != null) {
      buffer.writeString(localPhoneCopy);
    }
    final localEmailCopy = email;
    if (localEmailCopy != null) {
      buffer.writeString(localEmailCopy);
    }
    final localShippingAddressCopy = shippingAddress;
    if (localShippingAddressCopy != null) {
      buffer.writeObject(localShippingAddressCopy);
    }
  }
}

/// Predicate Payment Saved Credentials.
abstract class PaymentSavedCredentialsBase extends TlConstructor {
  /// Predicate Payment Saved Credentials constructor.
  const PaymentSavedCredentialsBase._() : super._();
}

/// Payment Saved Credentials Card.
///
/// ID: `cdc27a1f`.
class PaymentSavedCredentialsCard extends PaymentSavedCredentialsBase {
  /// Payment Saved Credentials Card constructor.
  const PaymentSavedCredentialsCard({
    required this.id,
    required this.title,
  }) : super._();

  /// Deserialize.
  factory PaymentSavedCredentialsCard.deserialize(Uint8List buffer) {
    // final id = _readstring(buffer);
    // final title = _readstring(buffer);
    // final result = PaymentSavedCredentialsCard(id: id, title: title,);

    // return result;

    throw Exception();
  }

  /// Id.
  final String id;

  /// Title.
  final String title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcdc27a1f);
    buffer.writeString(id);
    buffer.writeString(title);
  }
}

/// Predicate Web Document.
abstract class WebDocumentBase extends TlConstructor {
  /// Predicate Web Document constructor.
  const WebDocumentBase._() : super._();
}

/// Web Document.
///
/// ID: `1c570ed1`.
class WebDocument extends WebDocumentBase {
  /// Web Document constructor.
  const WebDocument({
    required this.url,
    required this.accessHash,
    required this.size,
    required this.mimeType,
    required this.attributes,
  }) : super._();

  /// Deserialize.
  factory WebDocument.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final accessHash = _readlong(buffer);
    // final size = _readint(buffer);
    // final mimeType = _readstring(buffer);
    // final attributes = _readVector<DocumentAttribute>(buffer);
    // final result = WebDocument(url: url, accessHash: accessHash, size: size, mimeType: mimeType, attributes: attributes,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Access Hash.
  final int accessHash;

  /// Size.
  final int size;

  /// Mime Type.
  final String mimeType;

  /// Attributes.
  final List<DocumentAttributeBase> attributes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1c570ed1);
    buffer.writeString(url);
    buffer.writeLong(accessHash);
    buffer.writeInt(size);
    buffer.writeString(mimeType);
    buffer.writeVectorObject(attributes);
  }
}

/// Web Document No Proxy.
///
/// ID: `f9c8bcc6`.
class WebDocumentNoProxy extends WebDocumentBase {
  /// Web Document No Proxy constructor.
  const WebDocumentNoProxy({
    required this.url,
    required this.size,
    required this.mimeType,
    required this.attributes,
  }) : super._();

  /// Deserialize.
  factory WebDocumentNoProxy.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final size = _readint(buffer);
    // final mimeType = _readstring(buffer);
    // final attributes = _readVector<DocumentAttribute>(buffer);
    // final result = WebDocumentNoProxy(url: url, size: size, mimeType: mimeType, attributes: attributes,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Size.
  final int size;

  /// Mime Type.
  final String mimeType;

  /// Attributes.
  final List<DocumentAttributeBase> attributes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf9c8bcc6);
    buffer.writeString(url);
    buffer.writeInt(size);
    buffer.writeString(mimeType);
    buffer.writeVectorObject(attributes);
  }
}

/// Predicate Input Web Document.
abstract class InputWebDocumentBase extends TlConstructor {
  /// Predicate Input Web Document constructor.
  const InputWebDocumentBase._() : super._();
}

/// Input Web Document.
///
/// ID: `9bed434d`.
class InputWebDocument extends InputWebDocumentBase {
  /// Input Web Document constructor.
  const InputWebDocument({
    required this.url,
    required this.size,
    required this.mimeType,
    required this.attributes,
  }) : super._();

  /// Deserialize.
  factory InputWebDocument.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final size = _readint(buffer);
    // final mimeType = _readstring(buffer);
    // final attributes = _readVector<DocumentAttribute>(buffer);
    // final result = InputWebDocument(url: url, size: size, mimeType: mimeType, attributes: attributes,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Size.
  final int size;

  /// Mime Type.
  final String mimeType;

  /// Attributes.
  final List<DocumentAttributeBase> attributes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9bed434d);
    buffer.writeString(url);
    buffer.writeInt(size);
    buffer.writeString(mimeType);
    buffer.writeVectorObject(attributes);
  }
}

/// Predicate Input Web File Location.
abstract class InputWebFileLocationBase extends TlConstructor {
  /// Predicate Input Web File Location constructor.
  const InputWebFileLocationBase._() : super._();
}

/// Input Web File Location.
///
/// ID: `c239d686`.
class InputWebFileLocation extends InputWebFileLocationBase {
  /// Input Web File Location constructor.
  const InputWebFileLocation({
    required this.url,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputWebFileLocation.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputWebFileLocation(url: url, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc239d686);
    buffer.writeString(url);
    buffer.writeLong(accessHash);
  }
}

/// Input Web File Geo Point Location.
///
/// ID: `9f2221c9`.
class InputWebFileGeoPointLocation extends InputWebFileLocationBase {
  /// Input Web File Geo Point Location constructor.
  const InputWebFileGeoPointLocation({
    required this.geoPoint,
    required this.accessHash,
    required this.w,
    required this.h,
    required this.zoom,
    required this.scale,
  }) : super._();

  /// Deserialize.
  factory InputWebFileGeoPointLocation.deserialize(Uint8List buffer) {
    // final geoPoint = _readInputGeoPoint(buffer);
    // final accessHash = _readlong(buffer);
    // final w = _readint(buffer);
    // final h = _readint(buffer);
    // final zoom = _readint(buffer);
    // final scale = _readint(buffer);
    // final result = InputWebFileGeoPointLocation(geoPoint: geoPoint, accessHash: accessHash, w: w, h: h, zoom: zoom, scale: scale,);

    // return result;

    throw Exception();
  }

  /// Geo Point.
  final InputGeoPointBase geoPoint;

  /// Access Hash.
  final int accessHash;

  /// W.
  final int w;

  /// H.
  final int h;

  /// Zoom.
  final int zoom;

  /// Scale.
  final int scale;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9f2221c9);
    buffer.writeObject(geoPoint);
    buffer.writeLong(accessHash);
    buffer.writeInt(w);
    buffer.writeInt(h);
    buffer.writeInt(zoom);
    buffer.writeInt(scale);
  }
}

/// Input Web File Audio Album Thumb Location.
///
/// ID: `f46fe924`.
class InputWebFileAudioAlbumThumbLocation extends InputWebFileLocationBase {
  /// Input Web File Audio Album Thumb Location constructor.
  const InputWebFileAudioAlbumThumbLocation({
    required this.small,
    this.document,
    this.title,
    this.performer,
  }) : super._();

  /// Deserialize.
  factory InputWebFileAudioAlbumThumbLocation.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final small = _readflags.2?true(buffer);
    // final document = _readflags.0?InputDocument(buffer);
    // final title = _readflags.1?string(buffer);
    // final performer = _readflags.1?string(buffer);
    // final result = InputWebFileAudioAlbumThumbLocation(flags: flags, small: small, document: document, title: title, performer: performer,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: small,
      b00: document != null,
      b01: title != null || performer != null,
    );

    return v;
  }

  /// small: bit 2 of flags.2?true
  final bool small;

  /// Document.
  final InputDocumentBase? document;

  /// Title.
  final String? title;

  /// Performer.
  final String? performer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf46fe924);
    buffer.writeInt(flags);
    final localDocumentCopy = document;
    if (localDocumentCopy != null) {
      buffer.writeObject(localDocumentCopy);
    }
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localPerformerCopy = performer;
    if (localPerformerCopy != null) {
      buffer.writeString(localPerformerCopy);
    }
  }
}

/// Predicate Upload Web File.
abstract class UploadWebFileBase extends TlConstructor {
  /// Predicate Upload Web File constructor.
  const UploadWebFileBase._() : super._();
}

/// Upload Web File.
///
/// ID: `21e753bc`.
class UploadWebFile extends UploadWebFileBase {
  /// Upload Web File constructor.
  const UploadWebFile({
    required this.size,
    required this.mimeType,
    required this.fileType,
    required this.mtime,
    required this.bytes,
  }) : super._();

  /// Deserialize.
  factory UploadWebFile.deserialize(Uint8List buffer) {
    // final size = _readint(buffer);
    // final mimeType = _readstring(buffer);
    // final fileType = _readstorage.FileType(buffer);
    // final mtime = _readint(buffer);
    // final bytes = _readbytes(buffer);
    // final result = UploadWebFile(size: size, mimeType: mimeType, fileType: fileType, mtime: mtime, bytes: bytes,);

    // return result;

    throw Exception();
  }

  /// Size.
  final int size;

  /// Mime Type.
  final String mimeType;

  /// File Type.
  final StorageFileTypeBase fileType;

  /// Mtime.
  final int mtime;

  /// Bytes.
  final Uint8List bytes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x21e753bc);
    buffer.writeInt(size);
    buffer.writeString(mimeType);
    buffer.writeObject(fileType);
    buffer.writeInt(mtime);
    buffer.writeBytes(bytes);
  }
}

/// Predicate Payments Payment Form.
abstract class PaymentsPaymentFormBase extends TlConstructor {
  /// Predicate Payments Payment Form constructor.
  const PaymentsPaymentFormBase._() : super._();
}

/// Payments Payment Form.
///
/// ID: `a0058751`.
class PaymentsPaymentForm extends PaymentsPaymentFormBase {
  /// Payments Payment Form constructor.
  const PaymentsPaymentForm({
    required this.canSaveCredentials,
    required this.passwordMissing,
    required this.formId,
    required this.botId,
    required this.title,
    required this.description,
    this.photo,
    required this.invoice,
    required this.providerId,
    required this.url,
    this.nativeProvider,
    this.nativeParams,
    this.additionalMethods,
    this.savedInfo,
    this.savedCredentials,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PaymentsPaymentForm.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final canSaveCredentials = _readflags.2?true(buffer);
    // final passwordMissing = _readflags.3?true(buffer);
    // final formId = _readlong(buffer);
    // final botId = _readlong(buffer);
    // final title = _readstring(buffer);
    // final description = _readstring(buffer);
    // final photo = _readflags.5?WebDocument(buffer);
    // final invoice = _readInvoice(buffer);
    // final providerId = _readlong(buffer);
    // final url = _readstring(buffer);
    // final nativeProvider = _readflags.4?string(buffer);
    // final nativeParams = _readflags.4?DataJSON(buffer);
    // final additionalMethods = _readflags.6?Vector<PaymentFormMethod>(buffer);
    // final savedInfo = _readflags.0?PaymentRequestedInfo(buffer);
    // final savedCredentials = _readflags.1?Vector<PaymentSavedCredentials>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = PaymentsPaymentForm(flags: flags, canSaveCredentials: canSaveCredentials, passwordMissing: passwordMissing, formId: formId, botId: botId, title: title, description: description, photo: photo, invoice: invoice, providerId: providerId, url: url, nativeProvider: nativeProvider, nativeParams: nativeParams, additionalMethods: additionalMethods, savedInfo: savedInfo, savedCredentials: savedCredentials, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: canSaveCredentials,
      b03: passwordMissing,
      b05: photo != null,
      b04: nativeProvider != null || nativeParams != null,
      b06: additionalMethods != null,
      b00: savedInfo != null,
      b01: savedCredentials != null,
    );

    return v;
  }

  /// can_save_credentials: bit 2 of flags.2?true
  final bool canSaveCredentials;

  /// password_missing: bit 3 of flags.3?true
  final bool passwordMissing;

  /// Form Id.
  final int formId;

  /// Bot Id.
  final int botId;

  /// Title.
  final String title;

  /// Description.
  final String description;

  /// Photo.
  final WebDocumentBase? photo;

  /// Invoice.
  final InvoiceBase invoice;

  /// Provider Id.
  final int providerId;

  /// Url.
  final String url;

  /// Native Provider.
  final String? nativeProvider;

  /// Native Params.
  final DataJSONBase? nativeParams;

  /// Additional Methods.
  final List<PaymentFormMethodBase>? additionalMethods;

  /// Saved Info.
  final PaymentRequestedInfoBase? savedInfo;

  /// Saved Credentials.
  final List<PaymentSavedCredentialsBase>? savedCredentials;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa0058751);
    buffer.writeInt(flags);
    buffer.writeLong(formId);
    buffer.writeLong(botId);
    buffer.writeString(title);
    buffer.writeString(description);
    final localPhotoCopy = photo;
    if (localPhotoCopy != null) {
      buffer.writeObject(localPhotoCopy);
    }
    buffer.writeObject(invoice);
    buffer.writeLong(providerId);
    buffer.writeString(url);
    final localNativeProviderCopy = nativeProvider;
    if (localNativeProviderCopy != null) {
      buffer.writeString(localNativeProviderCopy);
    }
    final localNativeParamsCopy = nativeParams;
    if (localNativeParamsCopy != null) {
      buffer.writeObject(localNativeParamsCopy);
    }
    final localAdditionalMethodsCopy = additionalMethods;
    if (localAdditionalMethodsCopy != null) {
      buffer.writeVectorObject(localAdditionalMethodsCopy);
    }
    final localSavedInfoCopy = savedInfo;
    if (localSavedInfoCopy != null) {
      buffer.writeObject(localSavedInfoCopy);
    }
    final localSavedCredentialsCopy = savedCredentials;
    if (localSavedCredentialsCopy != null) {
      buffer.writeVectorObject(localSavedCredentialsCopy);
    }
    buffer.writeVectorObject(users);
  }
}

/// Predicate Payments Validated Requested Info.
abstract class PaymentsValidatedRequestedInfoBase extends TlConstructor {
  /// Predicate Payments Validated Requested Info constructor.
  const PaymentsValidatedRequestedInfoBase._() : super._();
}

/// Payments Validated Requested Info.
///
/// ID: `d1451883`.
class PaymentsValidatedRequestedInfo
    extends PaymentsValidatedRequestedInfoBase {
  /// Payments Validated Requested Info constructor.
  const PaymentsValidatedRequestedInfo({
    this.id,
    this.shippingOptions,
  }) : super._();

  /// Deserialize.
  factory PaymentsValidatedRequestedInfo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final id = _readflags.0?string(buffer);
    // final shippingOptions = _readflags.1?Vector<ShippingOption>(buffer);
    // final result = PaymentsValidatedRequestedInfo(flags: flags, id: id, shippingOptions: shippingOptions,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: id != null,
      b01: shippingOptions != null,
    );

    return v;
  }

  /// Id.
  final String? id;

  /// Shipping Options.
  final List<ShippingOptionBase>? shippingOptions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd1451883);
    buffer.writeInt(flags);
    final localIdCopy = id;
    if (localIdCopy != null) {
      buffer.writeString(localIdCopy);
    }
    final localShippingOptionsCopy = shippingOptions;
    if (localShippingOptionsCopy != null) {
      buffer.writeVectorObject(localShippingOptionsCopy);
    }
  }
}

/// Predicate Payments Payment Result.
abstract class PaymentsPaymentResultBase extends TlConstructor {
  /// Predicate Payments Payment Result constructor.
  const PaymentsPaymentResultBase._() : super._();
}

/// Payments Payment Result.
///
/// ID: `4e5f810d`.
class PaymentsPaymentResult extends PaymentsPaymentResultBase {
  /// Payments Payment Result constructor.
  const PaymentsPaymentResult({
    required this.updates,
  }) : super._();

  /// Deserialize.
  factory PaymentsPaymentResult.deserialize(Uint8List buffer) {
    // final updates = _readUpdates(buffer);
    // final result = PaymentsPaymentResult(updates: updates,);

    // return result;

    throw Exception();
  }

  /// Updates.
  final UpdatesBase updates;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4e5f810d);
    buffer.writeObject(updates);
  }
}

/// Payments Payment Verification Needed.
///
/// ID: `d8411139`.
class PaymentsPaymentVerificationNeeded extends PaymentsPaymentResultBase {
  /// Payments Payment Verification Needed constructor.
  const PaymentsPaymentVerificationNeeded({
    required this.url,
  }) : super._();

  /// Deserialize.
  factory PaymentsPaymentVerificationNeeded.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final result = PaymentsPaymentVerificationNeeded(url: url,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd8411139);
    buffer.writeString(url);
  }
}

/// Predicate Payments Payment Receipt.
abstract class PaymentsPaymentReceiptBase extends TlConstructor {
  /// Predicate Payments Payment Receipt constructor.
  const PaymentsPaymentReceiptBase._() : super._();
}

/// Payments Payment Receipt.
///
/// ID: `70c4fe03`.
class PaymentsPaymentReceipt extends PaymentsPaymentReceiptBase {
  /// Payments Payment Receipt constructor.
  const PaymentsPaymentReceipt({
    required this.date,
    required this.botId,
    required this.providerId,
    required this.title,
    required this.description,
    this.photo,
    required this.invoice,
    this.info,
    this.shipping,
    this.tipAmount,
    required this.currency,
    required this.totalAmount,
    required this.credentialsTitle,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PaymentsPaymentReceipt.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final date = _readint(buffer);
    // final botId = _readlong(buffer);
    // final providerId = _readlong(buffer);
    // final title = _readstring(buffer);
    // final description = _readstring(buffer);
    // final photo = _readflags.2?WebDocument(buffer);
    // final invoice = _readInvoice(buffer);
    // final info = _readflags.0?PaymentRequestedInfo(buffer);
    // final shipping = _readflags.1?ShippingOption(buffer);
    // final tipAmount = _readflags.3?long(buffer);
    // final currency = _readstring(buffer);
    // final totalAmount = _readlong(buffer);
    // final credentialsTitle = _readstring(buffer);
    // final users = _readVector<User>(buffer);
    // final result = PaymentsPaymentReceipt(flags: flags, date: date, botId: botId, providerId: providerId, title: title, description: description, photo: photo, invoice: invoice, info: info, shipping: shipping, tipAmount: tipAmount, currency: currency, totalAmount: totalAmount, credentialsTitle: credentialsTitle, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: photo != null,
      b00: info != null,
      b01: shipping != null,
      b03: tipAmount != null,
    );

    return v;
  }

  /// Date.
  final DateTime date;

  /// Bot Id.
  final int botId;

  /// Provider Id.
  final int providerId;

  /// Title.
  final String title;

  /// Description.
  final String description;

  /// Photo.
  final WebDocumentBase? photo;

  /// Invoice.
  final InvoiceBase invoice;

  /// Info.
  final PaymentRequestedInfoBase? info;

  /// Shipping.
  final ShippingOptionBase? shipping;

  /// Tip Amount.
  final int? tipAmount;

  /// Currency.
  final String currency;

  /// Total Amount.
  final int totalAmount;

  /// Credentials Title.
  final String credentialsTitle;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x70c4fe03);
    buffer.writeInt(flags);
    buffer.writeDateTime(date);
    buffer.writeLong(botId);
    buffer.writeLong(providerId);
    buffer.writeString(title);
    buffer.writeString(description);
    final localPhotoCopy = photo;
    if (localPhotoCopy != null) {
      buffer.writeObject(localPhotoCopy);
    }
    buffer.writeObject(invoice);
    final localInfoCopy = info;
    if (localInfoCopy != null) {
      buffer.writeObject(localInfoCopy);
    }
    final localShippingCopy = shipping;
    if (localShippingCopy != null) {
      buffer.writeObject(localShippingCopy);
    }
    final localTipAmountCopy = tipAmount;
    if (localTipAmountCopy != null) {
      buffer.writeLong(localTipAmountCopy);
    }
    buffer.writeString(currency);
    buffer.writeLong(totalAmount);
    buffer.writeString(credentialsTitle);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Payments Saved Info.
abstract class PaymentsSavedInfoBase extends TlConstructor {
  /// Predicate Payments Saved Info constructor.
  const PaymentsSavedInfoBase._() : super._();
}

/// Payments Saved Info.
///
/// ID: `fb8fe43c`.
class PaymentsSavedInfo extends PaymentsSavedInfoBase {
  /// Payments Saved Info constructor.
  const PaymentsSavedInfo({
    required this.hasSavedCredentials,
    this.savedInfo,
  }) : super._();

  /// Deserialize.
  factory PaymentsSavedInfo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final hasSavedCredentials = _readflags.1?true(buffer);
    // final savedInfo = _readflags.0?PaymentRequestedInfo(buffer);
    // final result = PaymentsSavedInfo(flags: flags, hasSavedCredentials: hasSavedCredentials, savedInfo: savedInfo,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: hasSavedCredentials,
      b00: savedInfo != null,
    );

    return v;
  }

  /// has_saved_credentials: bit 1 of flags.1?true
  final bool hasSavedCredentials;

  /// Saved Info.
  final PaymentRequestedInfoBase? savedInfo;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfb8fe43c);
    buffer.writeInt(flags);
    final localSavedInfoCopy = savedInfo;
    if (localSavedInfoCopy != null) {
      buffer.writeObject(localSavedInfoCopy);
    }
  }
}

/// Predicate Input Payment Credentials.
abstract class InputPaymentCredentialsBase extends TlConstructor {
  /// Predicate Input Payment Credentials constructor.
  const InputPaymentCredentialsBase._() : super._();
}

/// Input Payment Credentials Saved.
///
/// ID: `c10eb2cf`.
class InputPaymentCredentialsSaved extends InputPaymentCredentialsBase {
  /// Input Payment Credentials Saved constructor.
  const InputPaymentCredentialsSaved({
    required this.id,
    required this.tmpPassword,
  }) : super._();

  /// Deserialize.
  factory InputPaymentCredentialsSaved.deserialize(Uint8List buffer) {
    // final id = _readstring(buffer);
    // final tmpPassword = _readbytes(buffer);
    // final result = InputPaymentCredentialsSaved(id: id, tmpPassword: tmpPassword,);

    // return result;

    throw Exception();
  }

  /// Id.
  final String id;

  /// Tmp Password.
  final Uint8List tmpPassword;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc10eb2cf);
    buffer.writeString(id);
    buffer.writeBytes(tmpPassword);
  }
}

/// Input Payment Credentials.
///
/// ID: `3417d728`.
class InputPaymentCredentials extends InputPaymentCredentialsBase {
  /// Input Payment Credentials constructor.
  const InputPaymentCredentials({
    required this.save,
    required this.data,
  }) : super._();

  /// Deserialize.
  factory InputPaymentCredentials.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final save = _readflags.0?true(buffer);
    // final data = _readDataJSON(buffer);
    // final result = InputPaymentCredentials(flags: flags, save: save, data: data,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: save,
    );

    return v;
  }

  /// save: bit 0 of flags.0?true
  final bool save;

  /// Data.
  final DataJSONBase data;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3417d728);
    buffer.writeInt(flags);
    buffer.writeObject(data);
  }
}

/// Input Payment Credentials Apple Pay.
///
/// ID: `0aa1c39f`.
class InputPaymentCredentialsApplePay extends InputPaymentCredentialsBase {
  /// Input Payment Credentials Apple Pay constructor.
  const InputPaymentCredentialsApplePay({
    required this.paymentData,
  }) : super._();

  /// Deserialize.
  factory InputPaymentCredentialsApplePay.deserialize(Uint8List buffer) {
    // final paymentData = _readDataJSON(buffer);
    // final result = InputPaymentCredentialsApplePay(paymentData: paymentData,);

    // return result;

    throw Exception();
  }

  /// Payment Data.
  final DataJSONBase paymentData;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0aa1c39f);
    buffer.writeObject(paymentData);
  }
}

/// Input Payment Credentials Google Pay.
///
/// ID: `8ac32801`.
class InputPaymentCredentialsGooglePay extends InputPaymentCredentialsBase {
  /// Input Payment Credentials Google Pay constructor.
  const InputPaymentCredentialsGooglePay({
    required this.paymentToken,
  }) : super._();

  /// Deserialize.
  factory InputPaymentCredentialsGooglePay.deserialize(Uint8List buffer) {
    // final paymentToken = _readDataJSON(buffer);
    // final result = InputPaymentCredentialsGooglePay(paymentToken: paymentToken,);

    // return result;

    throw Exception();
  }

  /// Payment Token.
  final DataJSONBase paymentToken;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8ac32801);
    buffer.writeObject(paymentToken);
  }
}

/// Predicate Account Tmp Password.
abstract class AccountTmpPasswordBase extends TlConstructor {
  /// Predicate Account Tmp Password constructor.
  const AccountTmpPasswordBase._() : super._();
}

/// Account Tmp Password.
///
/// ID: `db64fd34`.
class AccountTmpPassword extends AccountTmpPasswordBase {
  /// Account Tmp Password constructor.
  const AccountTmpPassword({
    required this.tmpPassword,
    required this.validUntil,
  }) : super._();

  /// Deserialize.
  factory AccountTmpPassword.deserialize(Uint8List buffer) {
    // final tmpPassword = _readbytes(buffer);
    // final validUntil = _readint(buffer);
    // final result = AccountTmpPassword(tmpPassword: tmpPassword, validUntil: validUntil,);

    // return result;

    throw Exception();
  }

  /// Tmp Password.
  final Uint8List tmpPassword;

  /// Valid Until.
  final int validUntil;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdb64fd34);
    buffer.writeBytes(tmpPassword);
    buffer.writeInt(validUntil);
  }
}

/// Predicate Shipping Option.
abstract class ShippingOptionBase extends TlConstructor {
  /// Predicate Shipping Option constructor.
  const ShippingOptionBase._() : super._();
}

/// Shipping Option.
///
/// ID: `b6213cdf`.
class ShippingOption extends ShippingOptionBase {
  /// Shipping Option constructor.
  const ShippingOption({
    required this.id,
    required this.title,
    required this.prices,
  }) : super._();

  /// Deserialize.
  factory ShippingOption.deserialize(Uint8List buffer) {
    // final id = _readstring(buffer);
    // final title = _readstring(buffer);
    // final prices = _readVector<LabeledPrice>(buffer);
    // final result = ShippingOption(id: id, title: title, prices: prices,);

    // return result;

    throw Exception();
  }

  /// Id.
  final String id;

  /// Title.
  final String title;

  /// Prices.
  final List<LabeledPriceBase> prices;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb6213cdf);
    buffer.writeString(id);
    buffer.writeString(title);
    buffer.writeVectorObject(prices);
  }
}

/// Predicate Input Sticker Set Item.
abstract class InputStickerSetItemBase extends TlConstructor {
  /// Predicate Input Sticker Set Item constructor.
  const InputStickerSetItemBase._() : super._();
}

/// Input Sticker Set Item.
///
/// ID: `32da9e9c`.
class InputStickerSetItem extends InputStickerSetItemBase {
  /// Input Sticker Set Item constructor.
  const InputStickerSetItem({
    required this.document,
    required this.emoji,
    this.maskCoords,
    this.keywords,
  }) : super._();

  /// Deserialize.
  factory InputStickerSetItem.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final document = _readInputDocument(buffer);
    // final emoji = _readstring(buffer);
    // final maskCoords = _readflags.0?MaskCoords(buffer);
    // final keywords = _readflags.1?string(buffer);
    // final result = InputStickerSetItem(flags: flags, document: document, emoji: emoji, maskCoords: maskCoords, keywords: keywords,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: maskCoords != null,
      b01: keywords != null,
    );

    return v;
  }

  /// Document.
  final InputDocumentBase document;

  /// Emoji.
  final String emoji;

  /// Mask Coords.
  final MaskCoordsBase? maskCoords;

  /// Keywords.
  final String? keywords;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x32da9e9c);
    buffer.writeInt(flags);
    buffer.writeObject(document);
    buffer.writeString(emoji);
    final localMaskCoordsCopy = maskCoords;
    if (localMaskCoordsCopy != null) {
      buffer.writeObject(localMaskCoordsCopy);
    }
    final localKeywordsCopy = keywords;
    if (localKeywordsCopy != null) {
      buffer.writeString(localKeywordsCopy);
    }
  }
}

/// Predicate Input Phone Call.
abstract class InputPhoneCallBase extends TlConstructor {
  /// Predicate Input Phone Call constructor.
  const InputPhoneCallBase._() : super._();
}

/// Input Phone Call.
///
/// ID: `1e36fded`.
class InputPhoneCall extends InputPhoneCallBase {
  /// Input Phone Call constructor.
  const InputPhoneCall({
    required this.id,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputPhoneCall.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputPhoneCall(id: id, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1e36fded);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
  }
}

/// Predicate Phone Call.
abstract class PhoneCallBase extends TlConstructor {
  /// Predicate Phone Call constructor.
  const PhoneCallBase._() : super._();
}

/// Phone Call Empty.
///
/// ID: `5366c915`.
class PhoneCallEmpty extends PhoneCallBase {
  /// Phone Call Empty constructor.
  const PhoneCallEmpty({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory PhoneCallEmpty.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final result = PhoneCallEmpty(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5366c915);
    buffer.writeLong(id);
  }
}

/// Phone Call Waiting.
///
/// ID: `c5226f17`.
class PhoneCallWaiting extends PhoneCallBase {
  /// Phone Call Waiting constructor.
  const PhoneCallWaiting({
    required this.video,
    required this.id,
    required this.accessHash,
    required this.date,
    required this.adminId,
    required this.participantId,
    required this.protocol,
    this.receiveDate,
  }) : super._();

  /// Deserialize.
  factory PhoneCallWaiting.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final video = _readflags.6?true(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final date = _readint(buffer);
    // final adminId = _readlong(buffer);
    // final participantId = _readlong(buffer);
    // final protocol = _readPhoneCallProtocol(buffer);
    // final receiveDate = _readflags.0?int(buffer);
    // final result = PhoneCallWaiting(flags: flags, video: video, id: id, accessHash: accessHash, date: date, adminId: adminId, participantId: participantId, protocol: protocol, receiveDate: receiveDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b06: video,
      b00: receiveDate != null,
    );

    return v;
  }

  /// video: bit 6 of flags.6?true
  final bool video;

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Date.
  final DateTime date;

  /// Admin Id.
  final int adminId;

  /// Participant Id.
  final int participantId;

  /// Protocol.
  final PhoneCallProtocolBase protocol;

  /// Receive Date.
  final DateTime? receiveDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc5226f17);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeDateTime(date);
    buffer.writeLong(adminId);
    buffer.writeLong(participantId);
    buffer.writeObject(protocol);
    final localReceiveDateCopy = receiveDate;
    if (localReceiveDateCopy != null) {
      buffer.writeDateTime(localReceiveDateCopy);
    }
  }
}

/// Phone Call Requested.
///
/// ID: `14b0ed0c`.
class PhoneCallRequested extends PhoneCallBase {
  /// Phone Call Requested constructor.
  const PhoneCallRequested({
    required this.video,
    required this.id,
    required this.accessHash,
    required this.date,
    required this.adminId,
    required this.participantId,
    required this.gAHash,
    required this.protocol,
  }) : super._();

  /// Deserialize.
  factory PhoneCallRequested.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final video = _readflags.6?true(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final date = _readint(buffer);
    // final adminId = _readlong(buffer);
    // final participantId = _readlong(buffer);
    // final gAHash = _readbytes(buffer);
    // final protocol = _readPhoneCallProtocol(buffer);
    // final result = PhoneCallRequested(flags: flags, video: video, id: id, accessHash: accessHash, date: date, adminId: adminId, participantId: participantId, gAHash: gAHash, protocol: protocol,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b06: video,
    );

    return v;
  }

  /// video: bit 6 of flags.6?true
  final bool video;

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Date.
  final DateTime date;

  /// Admin Id.
  final int adminId;

  /// Participant Id.
  final int participantId;

  /// G A Hash.
  final Uint8List gAHash;

  /// Protocol.
  final PhoneCallProtocolBase protocol;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x14b0ed0c);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeDateTime(date);
    buffer.writeLong(adminId);
    buffer.writeLong(participantId);
    buffer.writeBytes(gAHash);
    buffer.writeObject(protocol);
  }
}

/// Phone Call Accepted.
///
/// ID: `3660c311`.
class PhoneCallAccepted extends PhoneCallBase {
  /// Phone Call Accepted constructor.
  const PhoneCallAccepted({
    required this.video,
    required this.id,
    required this.accessHash,
    required this.date,
    required this.adminId,
    required this.participantId,
    required this.gB,
    required this.protocol,
  }) : super._();

  /// Deserialize.
  factory PhoneCallAccepted.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final video = _readflags.6?true(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final date = _readint(buffer);
    // final adminId = _readlong(buffer);
    // final participantId = _readlong(buffer);
    // final gB = _readbytes(buffer);
    // final protocol = _readPhoneCallProtocol(buffer);
    // final result = PhoneCallAccepted(flags: flags, video: video, id: id, accessHash: accessHash, date: date, adminId: adminId, participantId: participantId, gB: gB, protocol: protocol,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b06: video,
    );

    return v;
  }

  /// video: bit 6 of flags.6?true
  final bool video;

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Date.
  final DateTime date;

  /// Admin Id.
  final int adminId;

  /// Participant Id.
  final int participantId;

  /// G B.
  final Uint8List gB;

  /// Protocol.
  final PhoneCallProtocolBase protocol;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3660c311);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeDateTime(date);
    buffer.writeLong(adminId);
    buffer.writeLong(participantId);
    buffer.writeBytes(gB);
    buffer.writeObject(protocol);
  }
}

/// Phone Call.
///
/// ID: `967f7c67`.
class PhoneCall extends PhoneCallBase {
  /// Phone Call constructor.
  const PhoneCall({
    required this.p2pAllowed,
    required this.video,
    required this.id,
    required this.accessHash,
    required this.date,
    required this.adminId,
    required this.participantId,
    required this.gAOrB,
    required this.keyFingerprint,
    required this.protocol,
    required this.connections,
    required this.startDate,
  }) : super._();

  /// Deserialize.
  factory PhoneCall.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final p2pAllowed = _readflags.5?true(buffer);
    // final video = _readflags.6?true(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final date = _readint(buffer);
    // final adminId = _readlong(buffer);
    // final participantId = _readlong(buffer);
    // final gAOrB = _readbytes(buffer);
    // final keyFingerprint = _readlong(buffer);
    // final protocol = _readPhoneCallProtocol(buffer);
    // final connections = _readVector<PhoneConnection>(buffer);
    // final startDate = _readint(buffer);
    // final result = PhoneCall(flags: flags, p2pAllowed: p2pAllowed, video: video, id: id, accessHash: accessHash, date: date, adminId: adminId, participantId: participantId, gAOrB: gAOrB, keyFingerprint: keyFingerprint, protocol: protocol, connections: connections, startDate: startDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b05: p2pAllowed,
      b06: video,
    );

    return v;
  }

  /// p2p_allowed: bit 5 of flags.5?true
  final bool p2pAllowed;

  /// video: bit 6 of flags.6?true
  final bool video;

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Date.
  final DateTime date;

  /// Admin Id.
  final int adminId;

  /// Participant Id.
  final int participantId;

  /// G A Or B.
  final Uint8List gAOrB;

  /// Key Fingerprint.
  final int keyFingerprint;

  /// Protocol.
  final PhoneCallProtocolBase protocol;

  /// Connections.
  final List<PhoneConnectionBase> connections;

  /// Start Date.
  final DateTime startDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x967f7c67);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeDateTime(date);
    buffer.writeLong(adminId);
    buffer.writeLong(participantId);
    buffer.writeBytes(gAOrB);
    buffer.writeLong(keyFingerprint);
    buffer.writeObject(protocol);
    buffer.writeVectorObject(connections);
    buffer.writeDateTime(startDate);
  }
}

/// Phone Call Discarded.
///
/// ID: `50ca4de1`.
class PhoneCallDiscarded extends PhoneCallBase {
  /// Phone Call Discarded constructor.
  const PhoneCallDiscarded({
    required this.needRating,
    required this.needDebug,
    required this.video,
    required this.id,
    this.reason,
    this.duration,
  }) : super._();

  /// Deserialize.
  factory PhoneCallDiscarded.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final needRating = _readflags.2?true(buffer);
    // final needDebug = _readflags.3?true(buffer);
    // final video = _readflags.6?true(buffer);
    // final id = _readlong(buffer);
    // final reason = _readflags.0?PhoneCallDiscardReason(buffer);
    // final duration = _readflags.1?int(buffer);
    // final result = PhoneCallDiscarded(flags: flags, needRating: needRating, needDebug: needDebug, video: video, id: id, reason: reason, duration: duration,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: needRating,
      b03: needDebug,
      b06: video,
      b00: reason != null,
      b01: duration != null,
    );

    return v;
  }

  /// need_rating: bit 2 of flags.2?true
  final bool needRating;

  /// need_debug: bit 3 of flags.3?true
  final bool needDebug;

  /// video: bit 6 of flags.6?true
  final bool video;

  /// Id.
  final int id;

  /// Reason.
  final PhoneCallDiscardReasonBase? reason;

  /// Duration.
  final int? duration;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x50ca4de1);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    final localReasonCopy = reason;
    if (localReasonCopy != null) {
      buffer.writeObject(localReasonCopy);
    }
    final localDurationCopy = duration;
    if (localDurationCopy != null) {
      buffer.writeInt(localDurationCopy);
    }
  }
}

/// Predicate Phone Connection.
abstract class PhoneConnectionBase extends TlConstructor {
  /// Predicate Phone Connection constructor.
  const PhoneConnectionBase._() : super._();
}

/// Phone Connection.
///
/// ID: `9cc123c7`.
class PhoneConnection extends PhoneConnectionBase {
  /// Phone Connection constructor.
  const PhoneConnection({
    required this.tcp,
    required this.id,
    required this.ip,
    required this.ipv6,
    required this.port,
    required this.peerTag,
  }) : super._();

  /// Deserialize.
  factory PhoneConnection.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final tcp = _readflags.0?true(buffer);
    // final id = _readlong(buffer);
    // final ip = _readstring(buffer);
    // final ipv6 = _readstring(buffer);
    // final port = _readint(buffer);
    // final peerTag = _readbytes(buffer);
    // final result = PhoneConnection(flags: flags, tcp: tcp, id: id, ip: ip, ipv6: ipv6, port: port, peerTag: peerTag,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: tcp,
    );

    return v;
  }

  /// tcp: bit 0 of flags.0?true
  final bool tcp;

  /// Id.
  final int id;

  /// Ip.
  final String ip;

  /// Ipv6.
  final String ipv6;

  /// Port.
  final int port;

  /// Peer Tag.
  final Uint8List peerTag;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9cc123c7);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeString(ip);
    buffer.writeString(ipv6);
    buffer.writeInt(port);
    buffer.writeBytes(peerTag);
  }
}

/// Phone Connection Webrtc.
///
/// ID: `635fe375`.
class PhoneConnectionWebrtc extends PhoneConnectionBase {
  /// Phone Connection Webrtc constructor.
  const PhoneConnectionWebrtc({
    required this.turn,
    required this.stun,
    required this.id,
    required this.ip,
    required this.ipv6,
    required this.port,
    required this.username,
    required this.password,
  }) : super._();

  /// Deserialize.
  factory PhoneConnectionWebrtc.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final turn = _readflags.0?true(buffer);
    // final stun = _readflags.1?true(buffer);
    // final id = _readlong(buffer);
    // final ip = _readstring(buffer);
    // final ipv6 = _readstring(buffer);
    // final port = _readint(buffer);
    // final username = _readstring(buffer);
    // final password = _readstring(buffer);
    // final result = PhoneConnectionWebrtc(flags: flags, turn: turn, stun: stun, id: id, ip: ip, ipv6: ipv6, port: port, username: username, password: password,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: turn,
      b01: stun,
    );

    return v;
  }

  /// turn: bit 0 of flags.0?true
  final bool turn;

  /// stun: bit 1 of flags.1?true
  final bool stun;

  /// Id.
  final int id;

  /// Ip.
  final String ip;

  /// Ipv6.
  final String ipv6;

  /// Port.
  final int port;

  /// Username.
  final String username;

  /// Password.
  final String password;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x635fe375);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeString(ip);
    buffer.writeString(ipv6);
    buffer.writeInt(port);
    buffer.writeString(username);
    buffer.writeString(password);
  }
}

/// Predicate Phone Call Protocol.
abstract class PhoneCallProtocolBase extends TlConstructor {
  /// Predicate Phone Call Protocol constructor.
  const PhoneCallProtocolBase._() : super._();
}

/// Phone Call Protocol.
///
/// ID: `fc878fc8`.
class PhoneCallProtocol extends PhoneCallProtocolBase {
  /// Phone Call Protocol constructor.
  const PhoneCallProtocol({
    required this.udpP2p,
    required this.udpReflector,
    required this.minLayer,
    required this.maxLayer,
    required this.libraryVersions,
  }) : super._();

  /// Deserialize.
  factory PhoneCallProtocol.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final udpP2p = _readflags.0?true(buffer);
    // final udpReflector = _readflags.1?true(buffer);
    // final minLayer = _readint(buffer);
    // final maxLayer = _readint(buffer);
    // final libraryVersions = _readVector<string>(buffer);
    // final result = PhoneCallProtocol(flags: flags, udpP2p: udpP2p, udpReflector: udpReflector, minLayer: minLayer, maxLayer: maxLayer, libraryVersions: libraryVersions,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: udpP2p,
      b01: udpReflector,
    );

    return v;
  }

  /// udp_p2p: bit 0 of flags.0?true
  final bool udpP2p;

  /// udp_reflector: bit 1 of flags.1?true
  final bool udpReflector;

  /// Min Layer.
  final int minLayer;

  /// Max Layer.
  final int maxLayer;

  /// Library Versions.
  final List<String> libraryVersions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfc878fc8);
    buffer.writeInt(flags);
    buffer.writeInt(minLayer);
    buffer.writeInt(maxLayer);
    buffer.writeVectorString(libraryVersions);
  }
}

/// Predicate Phone Phone Call.
abstract class PhonePhoneCallBase extends TlConstructor {
  /// Predicate Phone Phone Call constructor.
  const PhonePhoneCallBase._() : super._();
}

/// Phone Phone Call.
///
/// ID: `ec82e140`.
class PhonePhoneCall extends PhonePhoneCallBase {
  /// Phone Phone Call constructor.
  const PhonePhoneCall({
    required this.phoneCall,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PhonePhoneCall.deserialize(Uint8List buffer) {
    // final phoneCall = _readPhoneCall(buffer);
    // final users = _readVector<User>(buffer);
    // final result = PhonePhoneCall(phoneCall: phoneCall, users: users,);

    // return result;

    throw Exception();
  }

  /// Phone Call.
  final PhoneCallBase phoneCall;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xec82e140);
    buffer.writeObject(phoneCall);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Upload Cdn File.
abstract class UploadCdnFileBase extends TlConstructor {
  /// Predicate Upload Cdn File constructor.
  const UploadCdnFileBase._() : super._();
}

/// Upload Cdn File Reupload Needed.
///
/// ID: `eea8e46e`.
class UploadCdnFileReuploadNeeded extends UploadCdnFileBase {
  /// Upload Cdn File Reupload Needed constructor.
  const UploadCdnFileReuploadNeeded({
    required this.requestToken,
  }) : super._();

  /// Deserialize.
  factory UploadCdnFileReuploadNeeded.deserialize(Uint8List buffer) {
    // final requestToken = _readbytes(buffer);
    // final result = UploadCdnFileReuploadNeeded(requestToken: requestToken,);

    // return result;

    throw Exception();
  }

  /// Request Token.
  final Uint8List requestToken;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xeea8e46e);
    buffer.writeBytes(requestToken);
  }
}

/// Upload Cdn File.
///
/// ID: `a99fca4f`.
class UploadCdnFile extends UploadCdnFileBase {
  /// Upload Cdn File constructor.
  const UploadCdnFile({
    required this.bytes,
  }) : super._();

  /// Deserialize.
  factory UploadCdnFile.deserialize(Uint8List buffer) {
    // final bytes = _readbytes(buffer);
    // final result = UploadCdnFile(bytes: bytes,);

    // return result;

    throw Exception();
  }

  /// Bytes.
  final Uint8List bytes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa99fca4f);
    buffer.writeBytes(bytes);
  }
}

/// Predicate Cdn Public Key.
abstract class CdnPublicKeyBase extends TlConstructor {
  /// Predicate Cdn Public Key constructor.
  const CdnPublicKeyBase._() : super._();
}

/// Cdn Public Key.
///
/// ID: `c982eaba`.
class CdnPublicKey extends CdnPublicKeyBase {
  /// Cdn Public Key constructor.
  const CdnPublicKey({
    required this.dcId,
    required this.publicKey,
  }) : super._();

  /// Deserialize.
  factory CdnPublicKey.deserialize(Uint8List buffer) {
    // final dcId = _readint(buffer);
    // final publicKey = _readstring(buffer);
    // final result = CdnPublicKey(dcId: dcId, publicKey: publicKey,);

    // return result;

    throw Exception();
  }

  /// Dc Id.
  final int dcId;

  /// Public Key.
  final String publicKey;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc982eaba);
    buffer.writeInt(dcId);
    buffer.writeString(publicKey);
  }
}

/// Predicate Cdn Config.
abstract class CdnConfigBase extends TlConstructor {
  /// Predicate Cdn Config constructor.
  const CdnConfigBase._() : super._();
}

/// Cdn Config.
///
/// ID: `5725e40a`.
class CdnConfig extends CdnConfigBase {
  /// Cdn Config constructor.
  const CdnConfig({
    required this.publicKeys,
  }) : super._();

  /// Deserialize.
  factory CdnConfig.deserialize(Uint8List buffer) {
    // final publicKeys = _readVector<CdnPublicKey>(buffer);
    // final result = CdnConfig(publicKeys: publicKeys,);

    // return result;

    throw Exception();
  }

  /// Public Keys.
  final List<CdnPublicKeyBase> publicKeys;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5725e40a);
    buffer.writeVectorObject(publicKeys);
  }
}

/// Predicate Lang Pack String.
abstract class LangPackStringBase extends TlConstructor {
  /// Predicate Lang Pack String constructor.
  const LangPackStringBase._() : super._();
}

/// Lang Pack String.
///
/// ID: `cad181f6`.
class LangPackString extends LangPackStringBase {
  /// Lang Pack String constructor.
  const LangPackString({
    required this.key,
    required this.value,
  }) : super._();

  /// Deserialize.
  factory LangPackString.deserialize(Uint8List buffer) {
    // final key = _readstring(buffer);
    // final value = _readstring(buffer);
    // final result = LangPackString(key: key, value: value,);

    // return result;

    throw Exception();
  }

  /// Key.
  final String key;

  /// Value.
  final String value;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcad181f6);
    buffer.writeString(key);
    buffer.writeString(value);
  }
}

/// Lang Pack String Pluralized.
///
/// ID: `6c47ac9f`.
class LangPackStringPluralized extends LangPackStringBase {
  /// Lang Pack String Pluralized constructor.
  const LangPackStringPluralized({
    required this.key,
    this.zeroValue,
    this.oneValue,
    this.twoValue,
    this.fewValue,
    this.manyValue,
    required this.otherValue,
  }) : super._();

  /// Deserialize.
  factory LangPackStringPluralized.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final key = _readstring(buffer);
    // final zeroValue = _readflags.0?string(buffer);
    // final oneValue = _readflags.1?string(buffer);
    // final twoValue = _readflags.2?string(buffer);
    // final fewValue = _readflags.3?string(buffer);
    // final manyValue = _readflags.4?string(buffer);
    // final otherValue = _readstring(buffer);
    // final result = LangPackStringPluralized(flags: flags, key: key, zeroValue: zeroValue, oneValue: oneValue, twoValue: twoValue, fewValue: fewValue, manyValue: manyValue, otherValue: otherValue,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: zeroValue != null,
      b01: oneValue != null,
      b02: twoValue != null,
      b03: fewValue != null,
      b04: manyValue != null,
    );

    return v;
  }

  /// Key.
  final String key;

  /// Zero Value.
  final String? zeroValue;

  /// One Value.
  final String? oneValue;

  /// Two Value.
  final String? twoValue;

  /// Few Value.
  final String? fewValue;

  /// Many Value.
  final String? manyValue;

  /// Other Value.
  final String otherValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6c47ac9f);
    buffer.writeInt(flags);
    buffer.writeString(key);
    final localZeroValueCopy = zeroValue;
    if (localZeroValueCopy != null) {
      buffer.writeString(localZeroValueCopy);
    }
    final localOneValueCopy = oneValue;
    if (localOneValueCopy != null) {
      buffer.writeString(localOneValueCopy);
    }
    final localTwoValueCopy = twoValue;
    if (localTwoValueCopy != null) {
      buffer.writeString(localTwoValueCopy);
    }
    final localFewValueCopy = fewValue;
    if (localFewValueCopy != null) {
      buffer.writeString(localFewValueCopy);
    }
    final localManyValueCopy = manyValue;
    if (localManyValueCopy != null) {
      buffer.writeString(localManyValueCopy);
    }
    buffer.writeString(otherValue);
  }
}

/// Lang Pack String Deleted.
///
/// ID: `2979eeb2`.
class LangPackStringDeleted extends LangPackStringBase {
  /// Lang Pack String Deleted constructor.
  const LangPackStringDeleted({
    required this.key,
  }) : super._();

  /// Deserialize.
  factory LangPackStringDeleted.deserialize(Uint8List buffer) {
    // final key = _readstring(buffer);
    // final result = LangPackStringDeleted(key: key,);

    // return result;

    throw Exception();
  }

  /// Key.
  final String key;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2979eeb2);
    buffer.writeString(key);
  }
}

/// Predicate Lang Pack Difference.
abstract class LangPackDifferenceBase extends TlConstructor {
  /// Predicate Lang Pack Difference constructor.
  const LangPackDifferenceBase._() : super._();
}

/// Lang Pack Difference.
///
/// ID: `f385c1f6`.
class LangPackDifference extends LangPackDifferenceBase {
  /// Lang Pack Difference constructor.
  const LangPackDifference({
    required this.langCode,
    required this.fromVersion,
    required this.version,
    required this.strings,
  }) : super._();

  /// Deserialize.
  factory LangPackDifference.deserialize(Uint8List buffer) {
    // final langCode = _readstring(buffer);
    // final fromVersion = _readint(buffer);
    // final version = _readint(buffer);
    // final strings = _readVector<LangPackString>(buffer);
    // final result = LangPackDifference(langCode: langCode, fromVersion: fromVersion, version: version, strings: strings,);

    // return result;

    throw Exception();
  }

  /// Lang Code.
  final String langCode;

  /// From Version.
  final int fromVersion;

  /// Version.
  final int version;

  /// Strings.
  final List<LangPackStringBase> strings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf385c1f6);
    buffer.writeString(langCode);
    buffer.writeInt(fromVersion);
    buffer.writeInt(version);
    buffer.writeVectorObject(strings);
  }
}

/// Predicate Lang Pack Language.
abstract class LangPackLanguageBase extends TlConstructor {
  /// Predicate Lang Pack Language constructor.
  const LangPackLanguageBase._() : super._();
}

/// Lang Pack Language.
///
/// ID: `eeca5ce3`.
class LangPackLanguage extends LangPackLanguageBase {
  /// Lang Pack Language constructor.
  const LangPackLanguage({
    required this.official,
    required this.rtl,
    required this.beta,
    required this.name,
    required this.nativeName,
    required this.langCode,
    this.baseLangCode,
    required this.pluralCode,
    required this.stringsCount,
    required this.translatedCount,
    required this.translationsUrl,
  }) : super._();

  /// Deserialize.
  factory LangPackLanguage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final official = _readflags.0?true(buffer);
    // final rtl = _readflags.2?true(buffer);
    // final beta = _readflags.3?true(buffer);
    // final name = _readstring(buffer);
    // final nativeName = _readstring(buffer);
    // final langCode = _readstring(buffer);
    // final baseLangCode = _readflags.1?string(buffer);
    // final pluralCode = _readstring(buffer);
    // final stringsCount = _readint(buffer);
    // final translatedCount = _readint(buffer);
    // final translationsUrl = _readstring(buffer);
    // final result = LangPackLanguage(flags: flags, official: official, rtl: rtl, beta: beta, name: name, nativeName: nativeName, langCode: langCode, baseLangCode: baseLangCode, pluralCode: pluralCode, stringsCount: stringsCount, translatedCount: translatedCount, translationsUrl: translationsUrl,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: official,
      b02: rtl,
      b03: beta,
      b01: baseLangCode != null,
    );

    return v;
  }

  /// official: bit 0 of flags.0?true
  final bool official;

  /// rtl: bit 2 of flags.2?true
  final bool rtl;

  /// beta: bit 3 of flags.3?true
  final bool beta;

  /// Name.
  final String name;

  /// Native Name.
  final String nativeName;

  /// Lang Code.
  final String langCode;

  /// Base Lang Code.
  final String? baseLangCode;

  /// Plural Code.
  final String pluralCode;

  /// Strings Count.
  final int stringsCount;

  /// Translated Count.
  final int translatedCount;

  /// Translations Url.
  final String translationsUrl;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xeeca5ce3);
    buffer.writeInt(flags);
    buffer.writeString(name);
    buffer.writeString(nativeName);
    buffer.writeString(langCode);
    final localBaseLangCodeCopy = baseLangCode;
    if (localBaseLangCodeCopy != null) {
      buffer.writeString(localBaseLangCodeCopy);
    }
    buffer.writeString(pluralCode);
    buffer.writeInt(stringsCount);
    buffer.writeInt(translatedCount);
    buffer.writeString(translationsUrl);
  }
}

/// Predicate Channel Admin Log Event Action.
abstract class ChannelAdminLogEventActionBase extends TlConstructor {
  /// Predicate Channel Admin Log Event Action constructor.
  const ChannelAdminLogEventActionBase._() : super._();
}

/// Channel Admin Log Event Action Change Title.
///
/// ID: `e6dfb825`.
class ChannelAdminLogEventActionChangeTitle
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change Title constructor.
  const ChannelAdminLogEventActionChangeTitle({
    required this.prevValue,
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangeTitle.deserialize(Uint8List buffer) {
    // final prevValue = _readstring(buffer);
    // final newValue = _readstring(buffer);
    // final result = ChannelAdminLogEventActionChangeTitle(prevValue: prevValue, newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// Prev Value.
  final String prevValue;

  /// New Value.
  final String newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe6dfb825);
    buffer.writeString(prevValue);
    buffer.writeString(newValue);
  }
}

/// Channel Admin Log Event Action Change About.
///
/// ID: `55188a2e`.
class ChannelAdminLogEventActionChangeAbout
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change About constructor.
  const ChannelAdminLogEventActionChangeAbout({
    required this.prevValue,
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangeAbout.deserialize(Uint8List buffer) {
    // final prevValue = _readstring(buffer);
    // final newValue = _readstring(buffer);
    // final result = ChannelAdminLogEventActionChangeAbout(prevValue: prevValue, newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// Prev Value.
  final String prevValue;

  /// New Value.
  final String newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x55188a2e);
    buffer.writeString(prevValue);
    buffer.writeString(newValue);
  }
}

/// Channel Admin Log Event Action Change Username.
///
/// ID: `6a4afc38`.
class ChannelAdminLogEventActionChangeUsername
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change Username constructor.
  const ChannelAdminLogEventActionChangeUsername({
    required this.prevValue,
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangeUsername.deserialize(
      Uint8List buffer) {
    // final prevValue = _readstring(buffer);
    // final newValue = _readstring(buffer);
    // final result = ChannelAdminLogEventActionChangeUsername(prevValue: prevValue, newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// Prev Value.
  final String prevValue;

  /// New Value.
  final String newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6a4afc38);
    buffer.writeString(prevValue);
    buffer.writeString(newValue);
  }
}

/// Channel Admin Log Event Action Change Photo.
///
/// ID: `434bd2af`.
class ChannelAdminLogEventActionChangePhoto
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change Photo constructor.
  const ChannelAdminLogEventActionChangePhoto({
    required this.prevPhoto,
    required this.newPhoto,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangePhoto.deserialize(Uint8List buffer) {
    // final prevPhoto = _readPhoto(buffer);
    // final newPhoto = _readPhoto(buffer);
    // final result = ChannelAdminLogEventActionChangePhoto(prevPhoto: prevPhoto, newPhoto: newPhoto,);

    // return result;

    throw Exception();
  }

  /// Prev Photo.
  final PhotoBase prevPhoto;

  /// New Photo.
  final PhotoBase newPhoto;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x434bd2af);
    buffer.writeObject(prevPhoto);
    buffer.writeObject(newPhoto);
  }
}

/// Channel Admin Log Event Action Toggle Invites.
///
/// ID: `1b7907ae`.
class ChannelAdminLogEventActionToggleInvites
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Toggle Invites constructor.
  const ChannelAdminLogEventActionToggleInvites({
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionToggleInvites.deserialize(
      Uint8List buffer) {
    // final newValue = _readBool(buffer);
    // final result = ChannelAdminLogEventActionToggleInvites(newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// New Value.
  final bool newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1b7907ae);
    buffer.writeBool(newValue);
  }
}

/// Channel Admin Log Event Action Toggle Signatures.
///
/// ID: `26ae0971`.
class ChannelAdminLogEventActionToggleSignatures
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Toggle Signatures constructor.
  const ChannelAdminLogEventActionToggleSignatures({
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionToggleSignatures.deserialize(
      Uint8List buffer) {
    // final newValue = _readBool(buffer);
    // final result = ChannelAdminLogEventActionToggleSignatures(newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// New Value.
  final bool newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x26ae0971);
    buffer.writeBool(newValue);
  }
}

/// Channel Admin Log Event Action Update Pinned.
///
/// ID: `e9e82c18`.
class ChannelAdminLogEventActionUpdatePinned
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Update Pinned constructor.
  const ChannelAdminLogEventActionUpdatePinned({
    required this.message,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionUpdatePinned.deserialize(Uint8List buffer) {
    // final message = _readMessage(buffer);
    // final result = ChannelAdminLogEventActionUpdatePinned(message: message,);

    // return result;

    throw Exception();
  }

  /// Message.
  final MessageBase message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe9e82c18);
    buffer.writeObject(message);
  }
}

/// Channel Admin Log Event Action Edit Message.
///
/// ID: `709b2405`.
class ChannelAdminLogEventActionEditMessage
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Edit Message constructor.
  const ChannelAdminLogEventActionEditMessage({
    required this.prevMessage,
    required this.newMessage,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionEditMessage.deserialize(Uint8List buffer) {
    // final prevMessage = _readMessage(buffer);
    // final newMessage = _readMessage(buffer);
    // final result = ChannelAdminLogEventActionEditMessage(prevMessage: prevMessage, newMessage: newMessage,);

    // return result;

    throw Exception();
  }

  /// Prev Message.
  final MessageBase prevMessage;

  /// New Message.
  final MessageBase newMessage;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x709b2405);
    buffer.writeObject(prevMessage);
    buffer.writeObject(newMessage);
  }
}

/// Channel Admin Log Event Action Delete Message.
///
/// ID: `42e047bb`.
class ChannelAdminLogEventActionDeleteMessage
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Delete Message constructor.
  const ChannelAdminLogEventActionDeleteMessage({
    required this.message,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionDeleteMessage.deserialize(
      Uint8List buffer) {
    // final message = _readMessage(buffer);
    // final result = ChannelAdminLogEventActionDeleteMessage(message: message,);

    // return result;

    throw Exception();
  }

  /// Message.
  final MessageBase message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x42e047bb);
    buffer.writeObject(message);
  }
}

/// Channel Admin Log Event Action Participant Join.
///
/// ID: `183040d3`.
class ChannelAdminLogEventActionParticipantJoin
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Participant Join constructor.
  const ChannelAdminLogEventActionParticipantJoin() : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionParticipantJoin.deserialize(
      Uint8List buffer) {
    // final result = ChannelAdminLogEventActionParticipantJoin();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x183040d3);
  }
}

/// Channel Admin Log Event Action Participant Leave.
///
/// ID: `f89777f2`.
class ChannelAdminLogEventActionParticipantLeave
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Participant Leave constructor.
  const ChannelAdminLogEventActionParticipantLeave() : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionParticipantLeave.deserialize(
      Uint8List buffer) {
    // final result = ChannelAdminLogEventActionParticipantLeave();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf89777f2);
  }
}

/// Channel Admin Log Event Action Participant Invite.
///
/// ID: `e31c34d8`.
class ChannelAdminLogEventActionParticipantInvite
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Participant Invite constructor.
  const ChannelAdminLogEventActionParticipantInvite({
    required this.participant,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionParticipantInvite.deserialize(
      Uint8List buffer) {
    // final participant = _readChannelParticipant(buffer);
    // final result = ChannelAdminLogEventActionParticipantInvite(participant: participant,);

    // return result;

    throw Exception();
  }

  /// Participant.
  final ChannelParticipantBase participant;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe31c34d8);
    buffer.writeObject(participant);
  }
}

/// Channel Admin Log Event Action Participant Toggle Ban.
///
/// ID: `e6d83d7e`.
class ChannelAdminLogEventActionParticipantToggleBan
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Participant Toggle Ban constructor.
  const ChannelAdminLogEventActionParticipantToggleBan({
    required this.prevParticipant,
    required this.newParticipant,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionParticipantToggleBan.deserialize(
      Uint8List buffer) {
    // final prevParticipant = _readChannelParticipant(buffer);
    // final newParticipant = _readChannelParticipant(buffer);
    // final result = ChannelAdminLogEventActionParticipantToggleBan(prevParticipant: prevParticipant, newParticipant: newParticipant,);

    // return result;

    throw Exception();
  }

  /// Prev Participant.
  final ChannelParticipantBase prevParticipant;

  /// New Participant.
  final ChannelParticipantBase newParticipant;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe6d83d7e);
    buffer.writeObject(prevParticipant);
    buffer.writeObject(newParticipant);
  }
}

/// Channel Admin Log Event Action Participant Toggle Admin.
///
/// ID: `d5676710`.
class ChannelAdminLogEventActionParticipantToggleAdmin
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Participant Toggle Admin constructor.
  const ChannelAdminLogEventActionParticipantToggleAdmin({
    required this.prevParticipant,
    required this.newParticipant,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionParticipantToggleAdmin.deserialize(
      Uint8List buffer) {
    // final prevParticipant = _readChannelParticipant(buffer);
    // final newParticipant = _readChannelParticipant(buffer);
    // final result = ChannelAdminLogEventActionParticipantToggleAdmin(prevParticipant: prevParticipant, newParticipant: newParticipant,);

    // return result;

    throw Exception();
  }

  /// Prev Participant.
  final ChannelParticipantBase prevParticipant;

  /// New Participant.
  final ChannelParticipantBase newParticipant;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd5676710);
    buffer.writeObject(prevParticipant);
    buffer.writeObject(newParticipant);
  }
}

/// Channel Admin Log Event Action Change Sticker Set.
///
/// ID: `b1c3caa7`.
class ChannelAdminLogEventActionChangeStickerSet
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change Sticker Set constructor.
  const ChannelAdminLogEventActionChangeStickerSet({
    required this.prevStickerset,
    required this.newStickerset,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangeStickerSet.deserialize(
      Uint8List buffer) {
    // final prevStickerset = _readInputStickerSet(buffer);
    // final newStickerset = _readInputStickerSet(buffer);
    // final result = ChannelAdminLogEventActionChangeStickerSet(prevStickerset: prevStickerset, newStickerset: newStickerset,);

    // return result;

    throw Exception();
  }

  /// Prev Stickerset.
  final InputStickerSetBase prevStickerset;

  /// New Stickerset.
  final InputStickerSetBase newStickerset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb1c3caa7);
    buffer.writeObject(prevStickerset);
    buffer.writeObject(newStickerset);
  }
}

/// Channel Admin Log Event Action Toggle Pre History Hidden.
///
/// ID: `5f5c95f1`.
class ChannelAdminLogEventActionTogglePreHistoryHidden
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Toggle Pre History Hidden constructor.
  const ChannelAdminLogEventActionTogglePreHistoryHidden({
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionTogglePreHistoryHidden.deserialize(
      Uint8List buffer) {
    // final newValue = _readBool(buffer);
    // final result = ChannelAdminLogEventActionTogglePreHistoryHidden(newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// New Value.
  final bool newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5f5c95f1);
    buffer.writeBool(newValue);
  }
}

/// Channel Admin Log Event Action Default Banned Rights.
///
/// ID: `2df5fc0a`.
class ChannelAdminLogEventActionDefaultBannedRights
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Default Banned Rights constructor.
  const ChannelAdminLogEventActionDefaultBannedRights({
    required this.prevBannedRights,
    required this.newBannedRights,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionDefaultBannedRights.deserialize(
      Uint8List buffer) {
    // final prevBannedRights = _readChatBannedRights(buffer);
    // final newBannedRights = _readChatBannedRights(buffer);
    // final result = ChannelAdminLogEventActionDefaultBannedRights(prevBannedRights: prevBannedRights, newBannedRights: newBannedRights,);

    // return result;

    throw Exception();
  }

  /// Prev Banned Rights.
  final ChatBannedRightsBase prevBannedRights;

  /// New Banned Rights.
  final ChatBannedRightsBase newBannedRights;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2df5fc0a);
    buffer.writeObject(prevBannedRights);
    buffer.writeObject(newBannedRights);
  }
}

/// Channel Admin Log Event Action Stop Poll.
///
/// ID: `8f079643`.
class ChannelAdminLogEventActionStopPoll
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Stop Poll constructor.
  const ChannelAdminLogEventActionStopPoll({
    required this.message,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionStopPoll.deserialize(Uint8List buffer) {
    // final message = _readMessage(buffer);
    // final result = ChannelAdminLogEventActionStopPoll(message: message,);

    // return result;

    throw Exception();
  }

  /// Message.
  final MessageBase message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8f079643);
    buffer.writeObject(message);
  }
}

/// Channel Admin Log Event Action Change Linked Chat.
///
/// ID: `050c7ac8`.
class ChannelAdminLogEventActionChangeLinkedChat
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change Linked Chat constructor.
  const ChannelAdminLogEventActionChangeLinkedChat({
    required this.prevValue,
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangeLinkedChat.deserialize(
      Uint8List buffer) {
    // final prevValue = _readlong(buffer);
    // final newValue = _readlong(buffer);
    // final result = ChannelAdminLogEventActionChangeLinkedChat(prevValue: prevValue, newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// Prev Value.
  final int prevValue;

  /// New Value.
  final int newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x050c7ac8);
    buffer.writeLong(prevValue);
    buffer.writeLong(newValue);
  }
}

/// Channel Admin Log Event Action Change Location.
///
/// ID: `0e6b76ae`.
class ChannelAdminLogEventActionChangeLocation
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change Location constructor.
  const ChannelAdminLogEventActionChangeLocation({
    required this.prevValue,
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangeLocation.deserialize(
      Uint8List buffer) {
    // final prevValue = _readChannelLocation(buffer);
    // final newValue = _readChannelLocation(buffer);
    // final result = ChannelAdminLogEventActionChangeLocation(prevValue: prevValue, newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// Prev Value.
  final ChannelLocationBase prevValue;

  /// New Value.
  final ChannelLocationBase newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0e6b76ae);
    buffer.writeObject(prevValue);
    buffer.writeObject(newValue);
  }
}

/// Channel Admin Log Event Action Toggle Slow Mode.
///
/// ID: `53909779`.
class ChannelAdminLogEventActionToggleSlowMode
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Toggle Slow Mode constructor.
  const ChannelAdminLogEventActionToggleSlowMode({
    required this.prevValue,
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionToggleSlowMode.deserialize(
      Uint8List buffer) {
    // final prevValue = _readint(buffer);
    // final newValue = _readint(buffer);
    // final result = ChannelAdminLogEventActionToggleSlowMode(prevValue: prevValue, newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// Prev Value.
  final int prevValue;

  /// New Value.
  final int newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x53909779);
    buffer.writeInt(prevValue);
    buffer.writeInt(newValue);
  }
}

/// Channel Admin Log Event Action Start Group Call.
///
/// ID: `23209745`.
class ChannelAdminLogEventActionStartGroupCall
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Start Group Call constructor.
  const ChannelAdminLogEventActionStartGroupCall({
    required this.call,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionStartGroupCall.deserialize(
      Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final result = ChannelAdminLogEventActionStartGroupCall(call: call,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x23209745);
    buffer.writeObject(call);
  }
}

/// Channel Admin Log Event Action Discard Group Call.
///
/// ID: `db9f9140`.
class ChannelAdminLogEventActionDiscardGroupCall
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Discard Group Call constructor.
  const ChannelAdminLogEventActionDiscardGroupCall({
    required this.call,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionDiscardGroupCall.deserialize(
      Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final result = ChannelAdminLogEventActionDiscardGroupCall(call: call,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdb9f9140);
    buffer.writeObject(call);
  }
}

/// Channel Admin Log Event Action Participant Mute.
///
/// ID: `f92424d2`.
class ChannelAdminLogEventActionParticipantMute
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Participant Mute constructor.
  const ChannelAdminLogEventActionParticipantMute({
    required this.participant,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionParticipantMute.deserialize(
      Uint8List buffer) {
    // final participant = _readGroupCallParticipant(buffer);
    // final result = ChannelAdminLogEventActionParticipantMute(participant: participant,);

    // return result;

    throw Exception();
  }

  /// Participant.
  final GroupCallParticipantBase participant;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf92424d2);
    buffer.writeObject(participant);
  }
}

/// Channel Admin Log Event Action Participant Unmute.
///
/// ID: `e64429c0`.
class ChannelAdminLogEventActionParticipantUnmute
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Participant Unmute constructor.
  const ChannelAdminLogEventActionParticipantUnmute({
    required this.participant,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionParticipantUnmute.deserialize(
      Uint8List buffer) {
    // final participant = _readGroupCallParticipant(buffer);
    // final result = ChannelAdminLogEventActionParticipantUnmute(participant: participant,);

    // return result;

    throw Exception();
  }

  /// Participant.
  final GroupCallParticipantBase participant;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe64429c0);
    buffer.writeObject(participant);
  }
}

/// Channel Admin Log Event Action Toggle Group Call Setting.
///
/// ID: `56d6a247`.
class ChannelAdminLogEventActionToggleGroupCallSetting
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Toggle Group Call Setting constructor.
  const ChannelAdminLogEventActionToggleGroupCallSetting({
    required this.joinMuted,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionToggleGroupCallSetting.deserialize(
      Uint8List buffer) {
    // final joinMuted = _readBool(buffer);
    // final result = ChannelAdminLogEventActionToggleGroupCallSetting(joinMuted: joinMuted,);

    // return result;

    throw Exception();
  }

  /// Join Muted.
  final bool joinMuted;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x56d6a247);
    buffer.writeBool(joinMuted);
  }
}

/// Channel Admin Log Event Action Participant Join By Invite.
///
/// ID: `fe9fc158`.
class ChannelAdminLogEventActionParticipantJoinByInvite
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Participant Join By Invite constructor.
  const ChannelAdminLogEventActionParticipantJoinByInvite({
    required this.viaChatlist,
    required this.invite,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionParticipantJoinByInvite.deserialize(
      Uint8List buffer) {
    // final flags = _read#(buffer);
    // final viaChatlist = _readflags.0?true(buffer);
    // final invite = _readExportedChatInvite(buffer);
    // final result = ChannelAdminLogEventActionParticipantJoinByInvite(flags: flags, viaChatlist: viaChatlist, invite: invite,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: viaChatlist,
    );

    return v;
  }

  /// via_chatlist: bit 0 of flags.0?true
  final bool viaChatlist;

  /// Invite.
  final ExportedChatInviteBase invite;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfe9fc158);
    buffer.writeInt(flags);
    buffer.writeObject(invite);
  }
}

/// Channel Admin Log Event Action Exported Invite Delete.
///
/// ID: `5a50fca4`.
class ChannelAdminLogEventActionExportedInviteDelete
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Exported Invite Delete constructor.
  const ChannelAdminLogEventActionExportedInviteDelete({
    required this.invite,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionExportedInviteDelete.deserialize(
      Uint8List buffer) {
    // final invite = _readExportedChatInvite(buffer);
    // final result = ChannelAdminLogEventActionExportedInviteDelete(invite: invite,);

    // return result;

    throw Exception();
  }

  /// Invite.
  final ExportedChatInviteBase invite;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5a50fca4);
    buffer.writeObject(invite);
  }
}

/// Channel Admin Log Event Action Exported Invite Revoke.
///
/// ID: `410a134e`.
class ChannelAdminLogEventActionExportedInviteRevoke
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Exported Invite Revoke constructor.
  const ChannelAdminLogEventActionExportedInviteRevoke({
    required this.invite,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionExportedInviteRevoke.deserialize(
      Uint8List buffer) {
    // final invite = _readExportedChatInvite(buffer);
    // final result = ChannelAdminLogEventActionExportedInviteRevoke(invite: invite,);

    // return result;

    throw Exception();
  }

  /// Invite.
  final ExportedChatInviteBase invite;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x410a134e);
    buffer.writeObject(invite);
  }
}

/// Channel Admin Log Event Action Exported Invite Edit.
///
/// ID: `e90ebb59`.
class ChannelAdminLogEventActionExportedInviteEdit
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Exported Invite Edit constructor.
  const ChannelAdminLogEventActionExportedInviteEdit({
    required this.prevInvite,
    required this.newInvite,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionExportedInviteEdit.deserialize(
      Uint8List buffer) {
    // final prevInvite = _readExportedChatInvite(buffer);
    // final newInvite = _readExportedChatInvite(buffer);
    // final result = ChannelAdminLogEventActionExportedInviteEdit(prevInvite: prevInvite, newInvite: newInvite,);

    // return result;

    throw Exception();
  }

  /// Prev Invite.
  final ExportedChatInviteBase prevInvite;

  /// New Invite.
  final ExportedChatInviteBase newInvite;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe90ebb59);
    buffer.writeObject(prevInvite);
    buffer.writeObject(newInvite);
  }
}

/// Channel Admin Log Event Action Participant Volume.
///
/// ID: `3e7f6847`.
class ChannelAdminLogEventActionParticipantVolume
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Participant Volume constructor.
  const ChannelAdminLogEventActionParticipantVolume({
    required this.participant,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionParticipantVolume.deserialize(
      Uint8List buffer) {
    // final participant = _readGroupCallParticipant(buffer);
    // final result = ChannelAdminLogEventActionParticipantVolume(participant: participant,);

    // return result;

    throw Exception();
  }

  /// Participant.
  final GroupCallParticipantBase participant;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3e7f6847);
    buffer.writeObject(participant);
  }
}

/// Channel Admin Log Event Action Change History T T L.
///
/// ID: `6e941a38`.
class ChannelAdminLogEventActionChangeHistoryTTL
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change History T T L constructor.
  const ChannelAdminLogEventActionChangeHistoryTTL({
    required this.prevValue,
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangeHistoryTTL.deserialize(
      Uint8List buffer) {
    // final prevValue = _readint(buffer);
    // final newValue = _readint(buffer);
    // final result = ChannelAdminLogEventActionChangeHistoryTTL(prevValue: prevValue, newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// Prev Value.
  final int prevValue;

  /// New Value.
  final int newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6e941a38);
    buffer.writeInt(prevValue);
    buffer.writeInt(newValue);
  }
}

/// Channel Admin Log Event Action Participant Join By Request.
///
/// ID: `afb6144a`.
class ChannelAdminLogEventActionParticipantJoinByRequest
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Participant Join By Request constructor.
  const ChannelAdminLogEventActionParticipantJoinByRequest({
    required this.invite,
    required this.approvedBy,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionParticipantJoinByRequest.deserialize(
      Uint8List buffer) {
    // final invite = _readExportedChatInvite(buffer);
    // final approvedBy = _readlong(buffer);
    // final result = ChannelAdminLogEventActionParticipantJoinByRequest(invite: invite, approvedBy: approvedBy,);

    // return result;

    throw Exception();
  }

  /// Invite.
  final ExportedChatInviteBase invite;

  /// Approved By.
  final int approvedBy;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xafb6144a);
    buffer.writeObject(invite);
    buffer.writeLong(approvedBy);
  }
}

/// Channel Admin Log Event Action Toggle No Forwards.
///
/// ID: `cb2ac766`.
class ChannelAdminLogEventActionToggleNoForwards
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Toggle No Forwards constructor.
  const ChannelAdminLogEventActionToggleNoForwards({
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionToggleNoForwards.deserialize(
      Uint8List buffer) {
    // final newValue = _readBool(buffer);
    // final result = ChannelAdminLogEventActionToggleNoForwards(newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// New Value.
  final bool newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcb2ac766);
    buffer.writeBool(newValue);
  }
}

/// Channel Admin Log Event Action Send Message.
///
/// ID: `278f2868`.
class ChannelAdminLogEventActionSendMessage
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Send Message constructor.
  const ChannelAdminLogEventActionSendMessage({
    required this.message,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionSendMessage.deserialize(Uint8List buffer) {
    // final message = _readMessage(buffer);
    // final result = ChannelAdminLogEventActionSendMessage(message: message,);

    // return result;

    throw Exception();
  }

  /// Message.
  final MessageBase message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x278f2868);
    buffer.writeObject(message);
  }
}

/// Channel Admin Log Event Action Change Available Reactions.
///
/// ID: `be4e0ef8`.
class ChannelAdminLogEventActionChangeAvailableReactions
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change Available Reactions constructor.
  const ChannelAdminLogEventActionChangeAvailableReactions({
    required this.prevValue,
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangeAvailableReactions.deserialize(
      Uint8List buffer) {
    // final prevValue = _readChatReactions(buffer);
    // final newValue = _readChatReactions(buffer);
    // final result = ChannelAdminLogEventActionChangeAvailableReactions(prevValue: prevValue, newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// Prev Value.
  final ChatReactionsBase prevValue;

  /// New Value.
  final ChatReactionsBase newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbe4e0ef8);
    buffer.writeObject(prevValue);
    buffer.writeObject(newValue);
  }
}

/// Channel Admin Log Event Action Change Usernames.
///
/// ID: `f04fb3a9`.
class ChannelAdminLogEventActionChangeUsernames
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change Usernames constructor.
  const ChannelAdminLogEventActionChangeUsernames({
    required this.prevValue,
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangeUsernames.deserialize(
      Uint8List buffer) {
    // final prevValue = _readVector<string>(buffer);
    // final newValue = _readVector<string>(buffer);
    // final result = ChannelAdminLogEventActionChangeUsernames(prevValue: prevValue, newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// Prev Value.
  final List<String> prevValue;

  /// New Value.
  final List<String> newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf04fb3a9);
    buffer.writeVectorString(prevValue);
    buffer.writeVectorString(newValue);
  }
}

/// Channel Admin Log Event Action Toggle Forum.
///
/// ID: `02cc6383`.
class ChannelAdminLogEventActionToggleForum
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Toggle Forum constructor.
  const ChannelAdminLogEventActionToggleForum({
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionToggleForum.deserialize(Uint8List buffer) {
    // final newValue = _readBool(buffer);
    // final result = ChannelAdminLogEventActionToggleForum(newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// New Value.
  final bool newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x02cc6383);
    buffer.writeBool(newValue);
  }
}

/// Channel Admin Log Event Action Create Topic.
///
/// ID: `58707d28`.
class ChannelAdminLogEventActionCreateTopic
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Create Topic constructor.
  const ChannelAdminLogEventActionCreateTopic({
    required this.topic,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionCreateTopic.deserialize(Uint8List buffer) {
    // final topic = _readForumTopic(buffer);
    // final result = ChannelAdminLogEventActionCreateTopic(topic: topic,);

    // return result;

    throw Exception();
  }

  /// Topic.
  final ForumTopicBase topic;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x58707d28);
    buffer.writeObject(topic);
  }
}

/// Channel Admin Log Event Action Edit Topic.
///
/// ID: `f06fe208`.
class ChannelAdminLogEventActionEditTopic
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Edit Topic constructor.
  const ChannelAdminLogEventActionEditTopic({
    required this.prevTopic,
    required this.newTopic,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionEditTopic.deserialize(Uint8List buffer) {
    // final prevTopic = _readForumTopic(buffer);
    // final newTopic = _readForumTopic(buffer);
    // final result = ChannelAdminLogEventActionEditTopic(prevTopic: prevTopic, newTopic: newTopic,);

    // return result;

    throw Exception();
  }

  /// Prev Topic.
  final ForumTopicBase prevTopic;

  /// New Topic.
  final ForumTopicBase newTopic;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf06fe208);
    buffer.writeObject(prevTopic);
    buffer.writeObject(newTopic);
  }
}

/// Channel Admin Log Event Action Delete Topic.
///
/// ID: `ae168909`.
class ChannelAdminLogEventActionDeleteTopic
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Delete Topic constructor.
  const ChannelAdminLogEventActionDeleteTopic({
    required this.topic,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionDeleteTopic.deserialize(Uint8List buffer) {
    // final topic = _readForumTopic(buffer);
    // final result = ChannelAdminLogEventActionDeleteTopic(topic: topic,);

    // return result;

    throw Exception();
  }

  /// Topic.
  final ForumTopicBase topic;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xae168909);
    buffer.writeObject(topic);
  }
}

/// Channel Admin Log Event Action Pin Topic.
///
/// ID: `5d8d353b`.
class ChannelAdminLogEventActionPinTopic
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Pin Topic constructor.
  const ChannelAdminLogEventActionPinTopic({
    this.prevTopic,
    this.newTopic,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionPinTopic.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final prevTopic = _readflags.0?ForumTopic(buffer);
    // final newTopic = _readflags.1?ForumTopic(buffer);
    // final result = ChannelAdminLogEventActionPinTopic(flags: flags, prevTopic: prevTopic, newTopic: newTopic,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: prevTopic != null,
      b01: newTopic != null,
    );

    return v;
  }

  /// Prev Topic.
  final ForumTopicBase? prevTopic;

  /// New Topic.
  final ForumTopicBase? newTopic;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5d8d353b);
    buffer.writeInt(flags);
    final localPrevTopicCopy = prevTopic;
    if (localPrevTopicCopy != null) {
      buffer.writeObject(localPrevTopicCopy);
    }
    final localNewTopicCopy = newTopic;
    if (localNewTopicCopy != null) {
      buffer.writeObject(localNewTopicCopy);
    }
  }
}

/// Channel Admin Log Event Action Toggle Anti Spam.
///
/// ID: `64f36dfc`.
class ChannelAdminLogEventActionToggleAntiSpam
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Toggle Anti Spam constructor.
  const ChannelAdminLogEventActionToggleAntiSpam({
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionToggleAntiSpam.deserialize(
      Uint8List buffer) {
    // final newValue = _readBool(buffer);
    // final result = ChannelAdminLogEventActionToggleAntiSpam(newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// New Value.
  final bool newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x64f36dfc);
    buffer.writeBool(newValue);
  }
}

/// Channel Admin Log Event Action Change Peer Color.
///
/// ID: `5796e780`.
class ChannelAdminLogEventActionChangePeerColor
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change Peer Color constructor.
  const ChannelAdminLogEventActionChangePeerColor({
    required this.prevValue,
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangePeerColor.deserialize(
      Uint8List buffer) {
    // final prevValue = _readPeerColor(buffer);
    // final newValue = _readPeerColor(buffer);
    // final result = ChannelAdminLogEventActionChangePeerColor(prevValue: prevValue, newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// Prev Value.
  final PeerColorBase prevValue;

  /// New Value.
  final PeerColorBase newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5796e780);
    buffer.writeObject(prevValue);
    buffer.writeObject(newValue);
  }
}

/// Channel Admin Log Event Action Change Profile Peer Color.
///
/// ID: `5e477b25`.
class ChannelAdminLogEventActionChangeProfilePeerColor
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change Profile Peer Color constructor.
  const ChannelAdminLogEventActionChangeProfilePeerColor({
    required this.prevValue,
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangeProfilePeerColor.deserialize(
      Uint8List buffer) {
    // final prevValue = _readPeerColor(buffer);
    // final newValue = _readPeerColor(buffer);
    // final result = ChannelAdminLogEventActionChangeProfilePeerColor(prevValue: prevValue, newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// Prev Value.
  final PeerColorBase prevValue;

  /// New Value.
  final PeerColorBase newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5e477b25);
    buffer.writeObject(prevValue);
    buffer.writeObject(newValue);
  }
}

/// Channel Admin Log Event Action Change Wallpaper.
///
/// ID: `31bb5d52`.
class ChannelAdminLogEventActionChangeWallpaper
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change Wallpaper constructor.
  const ChannelAdminLogEventActionChangeWallpaper({
    required this.prevValue,
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangeWallpaper.deserialize(
      Uint8List buffer) {
    // final prevValue = _readWallPaper(buffer);
    // final newValue = _readWallPaper(buffer);
    // final result = ChannelAdminLogEventActionChangeWallpaper(prevValue: prevValue, newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// Prev Value.
  final WallPaperBase prevValue;

  /// New Value.
  final WallPaperBase newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x31bb5d52);
    buffer.writeObject(prevValue);
    buffer.writeObject(newValue);
  }
}

/// Channel Admin Log Event Action Change Emoji Status.
///
/// ID: `3ea9feb1`.
class ChannelAdminLogEventActionChangeEmojiStatus
    extends ChannelAdminLogEventActionBase {
  /// Channel Admin Log Event Action Change Emoji Status constructor.
  const ChannelAdminLogEventActionChangeEmojiStatus({
    required this.prevValue,
    required this.newValue,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventActionChangeEmojiStatus.deserialize(
      Uint8List buffer) {
    // final prevValue = _readEmojiStatus(buffer);
    // final newValue = _readEmojiStatus(buffer);
    // final result = ChannelAdminLogEventActionChangeEmojiStatus(prevValue: prevValue, newValue: newValue,);

    // return result;

    throw Exception();
  }

  /// Prev Value.
  final EmojiStatusBase prevValue;

  /// New Value.
  final EmojiStatusBase newValue;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3ea9feb1);
    buffer.writeObject(prevValue);
    buffer.writeObject(newValue);
  }
}

/// Predicate Channel Admin Log Event.
abstract class ChannelAdminLogEventBase extends TlConstructor {
  /// Predicate Channel Admin Log Event constructor.
  const ChannelAdminLogEventBase._() : super._();
}

/// Channel Admin Log Event.
///
/// ID: `1fad68cd`.
class ChannelAdminLogEvent extends ChannelAdminLogEventBase {
  /// Channel Admin Log Event constructor.
  const ChannelAdminLogEvent({
    required this.id,
    required this.date,
    required this.userId,
    required this.action,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEvent.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final date = _readint(buffer);
    // final userId = _readlong(buffer);
    // final action = _readChannelAdminLogEventAction(buffer);
    // final result = ChannelAdminLogEvent(id: id, date: date, userId: userId, action: action,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Date.
  final DateTime date;

  /// User Id.
  final int userId;

  /// Action.
  final ChannelAdminLogEventActionBase action;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1fad68cd);
    buffer.writeLong(id);
    buffer.writeDateTime(date);
    buffer.writeLong(userId);
    buffer.writeObject(action);
  }
}

/// Predicate Channels Admin Log Results.
abstract class ChannelsAdminLogResultsBase extends TlConstructor {
  /// Predicate Channels Admin Log Results constructor.
  const ChannelsAdminLogResultsBase._() : super._();
}

/// Channels Admin Log Results.
///
/// ID: `ed8af74d`.
class ChannelsAdminLogResults extends ChannelsAdminLogResultsBase {
  /// Channels Admin Log Results constructor.
  const ChannelsAdminLogResults({
    required this.events,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ChannelsAdminLogResults.deserialize(Uint8List buffer) {
    // final events = _readVector<ChannelAdminLogEvent>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ChannelsAdminLogResults(events: events, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Events.
  final List<ChannelAdminLogEventBase> events;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xed8af74d);
    buffer.writeVectorObject(events);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Channel Admin Log Events Filter.
abstract class ChannelAdminLogEventsFilterBase extends TlConstructor {
  /// Predicate Channel Admin Log Events Filter constructor.
  const ChannelAdminLogEventsFilterBase._() : super._();
}

/// Channel Admin Log Events Filter.
///
/// ID: `ea107ae4`.
class ChannelAdminLogEventsFilter extends ChannelAdminLogEventsFilterBase {
  /// Channel Admin Log Events Filter constructor.
  const ChannelAdminLogEventsFilter({
    required this.join,
    required this.leave,
    required this.invite,
    required this.ban,
    required this.unban,
    required this.kick,
    required this.unkick,
    required this.promote,
    required this.demote,
    required this.info,
    required this.settings,
    required this.pinned,
    required this.edit,
    required this.delete,
    required this.groupCall,
    required this.invites,
    required this.send,
    required this.forums,
  }) : super._();

  /// Deserialize.
  factory ChannelAdminLogEventsFilter.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final join = _readflags.0?true(buffer);
    // final leave = _readflags.1?true(buffer);
    // final invite = _readflags.2?true(buffer);
    // final ban = _readflags.3?true(buffer);
    // final unban = _readflags.4?true(buffer);
    // final kick = _readflags.5?true(buffer);
    // final unkick = _readflags.6?true(buffer);
    // final promote = _readflags.7?true(buffer);
    // final demote = _readflags.8?true(buffer);
    // final info = _readflags.9?true(buffer);
    // final settings = _readflags.10?true(buffer);
    // final pinned = _readflags.11?true(buffer);
    // final edit = _readflags.12?true(buffer);
    // final delete = _readflags.13?true(buffer);
    // final groupCall = _readflags.14?true(buffer);
    // final invites = _readflags.15?true(buffer);
    // final send = _readflags.16?true(buffer);
    // final forums = _readflags.17?true(buffer);
    // final result = ChannelAdminLogEventsFilter(flags: flags, join: join, leave: leave, invite: invite, ban: ban, unban: unban, kick: kick, unkick: unkick, promote: promote, demote: demote, info: info, settings: settings, pinned: pinned, edit: edit, delete: delete, groupCall: groupCall, invites: invites, send: send, forums: forums,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: join,
      b01: leave,
      b02: invite,
      b03: ban,
      b04: unban,
      b05: kick,
      b06: unkick,
      b07: promote,
      b08: demote,
      b09: info,
      b10: settings,
      b11: pinned,
      b12: edit,
      b13: delete,
      b14: groupCall,
      b15: invites,
      b16: send,
      b17: forums,
    );

    return v;
  }

  /// join: bit 0 of flags.0?true
  final bool join;

  /// leave: bit 1 of flags.1?true
  final bool leave;

  /// invite: bit 2 of flags.2?true
  final bool invite;

  /// ban: bit 3 of flags.3?true
  final bool ban;

  /// unban: bit 4 of flags.4?true
  final bool unban;

  /// kick: bit 5 of flags.5?true
  final bool kick;

  /// unkick: bit 6 of flags.6?true
  final bool unkick;

  /// promote: bit 7 of flags.7?true
  final bool promote;

  /// demote: bit 8 of flags.8?true
  final bool demote;

  /// info: bit 9 of flags.9?true
  final bool info;

  /// settings: bit 10 of flags.10?true
  final bool settings;

  /// pinned: bit 11 of flags.11?true
  final bool pinned;

  /// edit: bit 12 of flags.12?true
  final bool edit;

  /// delete: bit 13 of flags.13?true
  final bool delete;

  /// group_call: bit 14 of flags.14?true
  final bool groupCall;

  /// invites: bit 15 of flags.15?true
  final bool invites;

  /// send: bit 16 of flags.16?true
  final bool send;

  /// forums: bit 17 of flags.17?true
  final bool forums;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xea107ae4);
    buffer.writeInt(flags);
  }
}

/// Predicate Popular Contact.
abstract class PopularContactBase extends TlConstructor {
  /// Predicate Popular Contact constructor.
  const PopularContactBase._() : super._();
}

/// Popular Contact.
///
/// ID: `5ce14175`.
class PopularContact extends PopularContactBase {
  /// Popular Contact constructor.
  const PopularContact({
    required this.clientId,
    required this.importers,
  }) : super._();

  /// Deserialize.
  factory PopularContact.deserialize(Uint8List buffer) {
    // final clientId = _readlong(buffer);
    // final importers = _readint(buffer);
    // final result = PopularContact(clientId: clientId, importers: importers,);

    // return result;

    throw Exception();
  }

  /// Client Id.
  final int clientId;

  /// Importers.
  final int importers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5ce14175);
    buffer.writeLong(clientId);
    buffer.writeInt(importers);
  }
}

/// Predicate Messages Faved Stickers.
abstract class MessagesFavedStickersBase extends TlConstructor {
  /// Predicate Messages Faved Stickers constructor.
  const MessagesFavedStickersBase._() : super._();
}

/// Messages Faved Stickers Not Modified.
///
/// ID: `9e8fa6d3`.
class MessagesFavedStickersNotModified extends MessagesFavedStickersBase {
  /// Messages Faved Stickers Not Modified constructor.
  const MessagesFavedStickersNotModified() : super._();

  /// Deserialize.
  factory MessagesFavedStickersNotModified.deserialize(Uint8List buffer) {
    // final result = MessagesFavedStickersNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9e8fa6d3);
  }
}

/// Messages Faved Stickers.
///
/// ID: `2cb51097`.
class MessagesFavedStickers extends MessagesFavedStickersBase {
  /// Messages Faved Stickers constructor.
  const MessagesFavedStickers({
    required this.hash,
    required this.packs,
    required this.stickers,
  }) : super._();

  /// Deserialize.
  factory MessagesFavedStickers.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final packs = _readVector<StickerPack>(buffer);
    // final stickers = _readVector<Document>(buffer);
    // final result = MessagesFavedStickers(hash: hash, packs: packs, stickers: stickers,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Packs.
  final List<StickerPackBase> packs;

  /// Stickers.
  final List<DocumentBase> stickers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2cb51097);
    buffer.writeLong(hash);
    buffer.writeVectorObject(packs);
    buffer.writeVectorObject(stickers);
  }
}

/// Predicate Recent Me Url.
abstract class RecentMeUrlBase extends TlConstructor {
  /// Predicate Recent Me Url constructor.
  const RecentMeUrlBase._() : super._();
}

/// Recent Me Url Unknown.
///
/// ID: `46e1d13d`.
class RecentMeUrlUnknown extends RecentMeUrlBase {
  /// Recent Me Url Unknown constructor.
  const RecentMeUrlUnknown({
    required this.url,
  }) : super._();

  /// Deserialize.
  factory RecentMeUrlUnknown.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final result = RecentMeUrlUnknown(url: url,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x46e1d13d);
    buffer.writeString(url);
  }
}

/// Recent Me Url User.
///
/// ID: `b92c09e2`.
class RecentMeUrlUser extends RecentMeUrlBase {
  /// Recent Me Url User constructor.
  const RecentMeUrlUser({
    required this.url,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory RecentMeUrlUser.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final userId = _readlong(buffer);
    // final result = RecentMeUrlUser(url: url, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// User Id.
  final int userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb92c09e2);
    buffer.writeString(url);
    buffer.writeLong(userId);
  }
}

/// Recent Me Url Chat.
///
/// ID: `b2da71d2`.
class RecentMeUrlChat extends RecentMeUrlBase {
  /// Recent Me Url Chat constructor.
  const RecentMeUrlChat({
    required this.url,
    required this.chatId,
  }) : super._();

  /// Deserialize.
  factory RecentMeUrlChat.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final chatId = _readlong(buffer);
    // final result = RecentMeUrlChat(url: url, chatId: chatId,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Chat Id.
  final int chatId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb2da71d2);
    buffer.writeString(url);
    buffer.writeLong(chatId);
  }
}

/// Recent Me Url Chat Invite.
///
/// ID: `eb49081d`.
class RecentMeUrlChatInvite extends RecentMeUrlBase {
  /// Recent Me Url Chat Invite constructor.
  const RecentMeUrlChatInvite({
    required this.url,
    required this.chatInvite,
  }) : super._();

  /// Deserialize.
  factory RecentMeUrlChatInvite.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final chatInvite = _readChatInvite(buffer);
    // final result = RecentMeUrlChatInvite(url: url, chatInvite: chatInvite,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Chat Invite.
  final ChatInviteBase chatInvite;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xeb49081d);
    buffer.writeString(url);
    buffer.writeObject(chatInvite);
  }
}

/// Recent Me Url Sticker Set.
///
/// ID: `bc0a57dc`.
class RecentMeUrlStickerSet extends RecentMeUrlBase {
  /// Recent Me Url Sticker Set constructor.
  const RecentMeUrlStickerSet({
    required this.url,
    required this.set,
  }) : super._();

  /// Deserialize.
  factory RecentMeUrlStickerSet.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final set = _readStickerSetCovered(buffer);
    // final result = RecentMeUrlStickerSet(url: url, set: set,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Set.
  final StickerSetCoveredBase set;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbc0a57dc);
    buffer.writeString(url);
    buffer.writeObject(set);
  }
}

/// Predicate Help Recent Me Urls.
abstract class HelpRecentMeUrlsBase extends TlConstructor {
  /// Predicate Help Recent Me Urls constructor.
  const HelpRecentMeUrlsBase._() : super._();
}

/// Help Recent Me Urls.
///
/// ID: `0e0310d7`.
class HelpRecentMeUrls extends HelpRecentMeUrlsBase {
  /// Help Recent Me Urls constructor.
  const HelpRecentMeUrls({
    required this.urls,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory HelpRecentMeUrls.deserialize(Uint8List buffer) {
    // final urls = _readVector<RecentMeUrl>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = HelpRecentMeUrls(urls: urls, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Urls.
  final List<RecentMeUrlBase> urls;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0e0310d7);
    buffer.writeVectorObject(urls);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Input Single Media.
abstract class InputSingleMediaBase extends TlConstructor {
  /// Predicate Input Single Media constructor.
  const InputSingleMediaBase._() : super._();
}

/// Input Single Media.
///
/// ID: `1cc6e91f`.
class InputSingleMedia extends InputSingleMediaBase {
  /// Input Single Media constructor.
  const InputSingleMedia({
    required this.media,
    required this.randomId,
    required this.message,
    this.entities,
  }) : super._();

  /// Deserialize.
  factory InputSingleMedia.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final media = _readInputMedia(buffer);
    // final randomId = _readlong(buffer);
    // final message = _readstring(buffer);
    // final entities = _readflags.0?Vector<MessageEntity>(buffer);
    // final result = InputSingleMedia(flags: flags, media: media, randomId: randomId, message: message, entities: entities,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: entities != null,
    );

    return v;
  }

  /// Media.
  final InputMediaBase media;

  /// Random Id.
  final int randomId;

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1cc6e91f);
    buffer.writeInt(flags);
    buffer.writeObject(media);
    buffer.writeLong(randomId);
    buffer.writeString(message);
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
  }
}

/// Predicate Web Authorization.
abstract class WebAuthorizationBase extends TlConstructor {
  /// Predicate Web Authorization constructor.
  const WebAuthorizationBase._() : super._();
}

/// Web Authorization.
///
/// ID: `a6f8f452`.
class WebAuthorization extends WebAuthorizationBase {
  /// Web Authorization constructor.
  const WebAuthorization({
    required this.hash,
    required this.botId,
    required this.domain,
    required this.browser,
    required this.platform,
    required this.dateCreated,
    required this.dateActive,
    required this.ip,
    required this.region,
  }) : super._();

  /// Deserialize.
  factory WebAuthorization.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final botId = _readlong(buffer);
    // final domain = _readstring(buffer);
    // final browser = _readstring(buffer);
    // final platform = _readstring(buffer);
    // final dateCreated = _readint(buffer);
    // final dateActive = _readint(buffer);
    // final ip = _readstring(buffer);
    // final region = _readstring(buffer);
    // final result = WebAuthorization(hash: hash, botId: botId, domain: domain, browser: browser, platform: platform, dateCreated: dateCreated, dateActive: dateActive, ip: ip, region: region,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Bot Id.
  final int botId;

  /// Domain.
  final String domain;

  /// Browser.
  final String browser;

  /// Platform.
  final String platform;

  /// Date Created.
  final DateTime dateCreated;

  /// Date Active.
  final DateTime dateActive;

  /// Ip.
  final String ip;

  /// Region.
  final String region;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa6f8f452);
    buffer.writeLong(hash);
    buffer.writeLong(botId);
    buffer.writeString(domain);
    buffer.writeString(browser);
    buffer.writeString(platform);
    buffer.writeDateTime(dateCreated);
    buffer.writeDateTime(dateActive);
    buffer.writeString(ip);
    buffer.writeString(region);
  }
}

/// Predicate Account Web Authorizations.
abstract class AccountWebAuthorizationsBase extends TlConstructor {
  /// Predicate Account Web Authorizations constructor.
  const AccountWebAuthorizationsBase._() : super._();
}

/// Account Web Authorizations.
///
/// ID: `ed56c9fc`.
class AccountWebAuthorizations extends AccountWebAuthorizationsBase {
  /// Account Web Authorizations constructor.
  const AccountWebAuthorizations({
    required this.authorizations,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory AccountWebAuthorizations.deserialize(Uint8List buffer) {
    // final authorizations = _readVector<WebAuthorization>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = AccountWebAuthorizations(authorizations: authorizations, users: users,);

    // return result;

    throw Exception();
  }

  /// Authorizations.
  final List<WebAuthorizationBase> authorizations;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xed56c9fc);
    buffer.writeVectorObject(authorizations);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Input Message.
abstract class InputMessageBase extends TlConstructor {
  /// Predicate Input Message constructor.
  const InputMessageBase._() : super._();
}

/// Input Message I D.
///
/// ID: `a676a322`.
class InputMessageID extends InputMessageBase {
  /// Input Message I D constructor.
  const InputMessageID({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory InputMessageID.deserialize(Uint8List buffer) {
    // final id = _readint(buffer);
    // final result = InputMessageID(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa676a322);
    buffer.writeInt(id);
  }
}

/// Input Message Reply To.
///
/// ID: `bad88395`.
class InputMessageReplyTo extends InputMessageBase {
  /// Input Message Reply To constructor.
  const InputMessageReplyTo({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory InputMessageReplyTo.deserialize(Uint8List buffer) {
    // final id = _readint(buffer);
    // final result = InputMessageReplyTo(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbad88395);
    buffer.writeInt(id);
  }
}

/// Input Message Pinned.
///
/// ID: `86872538`.
class InputMessagePinned extends InputMessageBase {
  /// Input Message Pinned constructor.
  const InputMessagePinned() : super._();

  /// Deserialize.
  factory InputMessagePinned.deserialize(Uint8List buffer) {
    // final result = InputMessagePinned();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x86872538);
  }
}

/// Input Message Callback Query.
///
/// ID: `acfa1a7e`.
class InputMessageCallbackQuery extends InputMessageBase {
  /// Input Message Callback Query constructor.
  const InputMessageCallbackQuery({
    required this.id,
    required this.queryId,
  }) : super._();

  /// Deserialize.
  factory InputMessageCallbackQuery.deserialize(Uint8List buffer) {
    // final id = _readint(buffer);
    // final queryId = _readlong(buffer);
    // final result = InputMessageCallbackQuery(id: id, queryId: queryId,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Query Id.
  final int queryId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xacfa1a7e);
    buffer.writeInt(id);
    buffer.writeLong(queryId);
  }
}

/// Predicate Input Dialog Peer.
abstract class InputDialogPeerBase extends TlConstructor {
  /// Predicate Input Dialog Peer constructor.
  const InputDialogPeerBase._() : super._();
}

/// Input Dialog Peer.
///
/// ID: `fcaafeb7`.
class InputDialogPeer extends InputDialogPeerBase {
  /// Input Dialog Peer constructor.
  const InputDialogPeer({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory InputDialogPeer.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = InputDialogPeer(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfcaafeb7);
    buffer.writeObject(peer);
  }
}

/// Input Dialog Peer Folder.
///
/// ID: `64600527`.
class InputDialogPeerFolder extends InputDialogPeerBase {
  /// Input Dialog Peer Folder constructor.
  const InputDialogPeerFolder({
    required this.folderId,
  }) : super._();

  /// Deserialize.
  factory InputDialogPeerFolder.deserialize(Uint8List buffer) {
    // final folderId = _readint(buffer);
    // final result = InputDialogPeerFolder(folderId: folderId,);

    // return result;

    throw Exception();
  }

  /// Folder Id.
  final int folderId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x64600527);
    buffer.writeInt(folderId);
  }
}

/// Predicate Dialog Peer.
abstract class DialogPeerBase extends TlConstructor {
  /// Predicate Dialog Peer constructor.
  const DialogPeerBase._() : super._();
}

/// Dialog Peer.
///
/// ID: `e56dbf05`.
class DialogPeer extends DialogPeerBase {
  /// Dialog Peer constructor.
  const DialogPeer({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory DialogPeer.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final result = DialogPeer(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe56dbf05);
    buffer.writeObject(peer);
  }
}

/// Dialog Peer Folder.
///
/// ID: `514519e2`.
class DialogPeerFolder extends DialogPeerBase {
  /// Dialog Peer Folder constructor.
  const DialogPeerFolder({
    required this.folderId,
  }) : super._();

  /// Deserialize.
  factory DialogPeerFolder.deserialize(Uint8List buffer) {
    // final folderId = _readint(buffer);
    // final result = DialogPeerFolder(folderId: folderId,);

    // return result;

    throw Exception();
  }

  /// Folder Id.
  final int folderId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x514519e2);
    buffer.writeInt(folderId);
  }
}

/// Predicate Messages Found Sticker Sets.
abstract class MessagesFoundStickerSetsBase extends TlConstructor {
  /// Predicate Messages Found Sticker Sets constructor.
  const MessagesFoundStickerSetsBase._() : super._();
}

/// Messages Found Sticker Sets Not Modified.
///
/// ID: `0d54b65d`.
class MessagesFoundStickerSetsNotModified extends MessagesFoundStickerSetsBase {
  /// Messages Found Sticker Sets Not Modified constructor.
  const MessagesFoundStickerSetsNotModified() : super._();

  /// Deserialize.
  factory MessagesFoundStickerSetsNotModified.deserialize(Uint8List buffer) {
    // final result = MessagesFoundStickerSetsNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0d54b65d);
  }
}

/// Messages Found Sticker Sets.
///
/// ID: `8af09dd2`.
class MessagesFoundStickerSets extends MessagesFoundStickerSetsBase {
  /// Messages Found Sticker Sets constructor.
  const MessagesFoundStickerSets({
    required this.hash,
    required this.sets,
  }) : super._();

  /// Deserialize.
  factory MessagesFoundStickerSets.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final sets = _readVector<StickerSetCovered>(buffer);
    // final result = MessagesFoundStickerSets(hash: hash, sets: sets,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Sets.
  final List<StickerSetCoveredBase> sets;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8af09dd2);
    buffer.writeLong(hash);
    buffer.writeVectorObject(sets);
  }
}

/// Predicate File Hash.
abstract class FileHashBase extends TlConstructor {
  /// Predicate File Hash constructor.
  const FileHashBase._() : super._();
}

/// File Hash.
///
/// ID: `f39b035c`.
class FileHash extends FileHashBase {
  /// File Hash constructor.
  const FileHash({
    required this.offset,
    required this.limit,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory FileHash.deserialize(Uint8List buffer) {
    // final offset = _readlong(buffer);
    // final limit = _readint(buffer);
    // final hash = _readbytes(buffer);
    // final result = FileHash(offset: offset, limit: limit, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Limit.
  final int limit;

  /// Hash.
  final Uint8List hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf39b035c);
    buffer.writeLong(offset);
    buffer.writeInt(limit);
    buffer.writeBytes(hash);
  }
}

/// Predicate Input Client Proxy.
abstract class InputClientProxyBase extends TlConstructor {
  /// Predicate Input Client Proxy constructor.
  const InputClientProxyBase._() : super._();
}

/// Input Client Proxy.
///
/// ID: `75588b3f`.
class InputClientProxy extends InputClientProxyBase {
  /// Input Client Proxy constructor.
  const InputClientProxy({
    required this.address,
    required this.port,
  }) : super._();

  /// Deserialize.
  factory InputClientProxy.deserialize(Uint8List buffer) {
    // final address = _readstring(buffer);
    // final port = _readint(buffer);
    // final result = InputClientProxy(address: address, port: port,);

    // return result;

    throw Exception();
  }

  /// Address.
  final String address;

  /// Port.
  final int port;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x75588b3f);
    buffer.writeString(address);
    buffer.writeInt(port);
  }
}

/// Predicate Help Terms Of Service Update.
abstract class HelpTermsOfServiceUpdateBase extends TlConstructor {
  /// Predicate Help Terms Of Service Update constructor.
  const HelpTermsOfServiceUpdateBase._() : super._();
}

/// Help Terms Of Service Update Empty.
///
/// ID: `e3309f7f`.
class HelpTermsOfServiceUpdateEmpty extends HelpTermsOfServiceUpdateBase {
  /// Help Terms Of Service Update Empty constructor.
  const HelpTermsOfServiceUpdateEmpty({
    required this.expires,
  }) : super._();

  /// Deserialize.
  factory HelpTermsOfServiceUpdateEmpty.deserialize(Uint8List buffer) {
    // final expires = _readint(buffer);
    // final result = HelpTermsOfServiceUpdateEmpty(expires: expires,);

    // return result;

    throw Exception();
  }

  /// Expires.
  final int expires;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe3309f7f);
    buffer.writeInt(expires);
  }
}

/// Help Terms Of Service Update.
///
/// ID: `28ecf961`.
class HelpTermsOfServiceUpdate extends HelpTermsOfServiceUpdateBase {
  /// Help Terms Of Service Update constructor.
  const HelpTermsOfServiceUpdate({
    required this.expires,
    required this.termsOfService,
  }) : super._();

  /// Deserialize.
  factory HelpTermsOfServiceUpdate.deserialize(Uint8List buffer) {
    // final expires = _readint(buffer);
    // final termsOfService = _readhelp.TermsOfService(buffer);
    // final result = HelpTermsOfServiceUpdate(expires: expires, termsOfService: termsOfService,);

    // return result;

    throw Exception();
  }

  /// Expires.
  final int expires;

  /// Terms Of Service.
  final HelpTermsOfServiceBase termsOfService;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x28ecf961);
    buffer.writeInt(expires);
    buffer.writeObject(termsOfService);
  }
}

/// Predicate Input Secure File.
abstract class InputSecureFileBase extends TlConstructor {
  /// Predicate Input Secure File constructor.
  const InputSecureFileBase._() : super._();
}

/// Input Secure File Uploaded.
///
/// ID: `3334b0f0`.
class InputSecureFileUploaded extends InputSecureFileBase {
  /// Input Secure File Uploaded constructor.
  const InputSecureFileUploaded({
    required this.id,
    required this.parts,
    required this.md5Checksum,
    required this.fileHash,
    required this.secret,
  }) : super._();

  /// Deserialize.
  factory InputSecureFileUploaded.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final parts = _readint(buffer);
    // final md5Checksum = _readstring(buffer);
    // final fileHash = _readbytes(buffer);
    // final secret = _readbytes(buffer);
    // final result = InputSecureFileUploaded(id: id, parts: parts, md5Checksum: md5Checksum, fileHash: fileHash, secret: secret,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Parts.
  final int parts;

  /// Md5 Checksum.
  final String md5Checksum;

  /// File Hash.
  final Uint8List fileHash;

  /// Secret.
  final Uint8List secret;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3334b0f0);
    buffer.writeLong(id);
    buffer.writeInt(parts);
    buffer.writeString(md5Checksum);
    buffer.writeBytes(fileHash);
    buffer.writeBytes(secret);
  }
}

/// Input Secure File.
///
/// ID: `5367e5be`.
class InputSecureFile extends InputSecureFileBase {
  /// Input Secure File constructor.
  const InputSecureFile({
    required this.id,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputSecureFile.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputSecureFile(id: id, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5367e5be);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
  }
}

/// Predicate Secure File.
abstract class SecureFileBase extends TlConstructor {
  /// Predicate Secure File constructor.
  const SecureFileBase._() : super._();
}

/// Secure File Empty.
///
/// ID: `64199744`.
class SecureFileEmpty extends SecureFileBase {
  /// Secure File Empty constructor.
  const SecureFileEmpty() : super._();

  /// Deserialize.
  factory SecureFileEmpty.deserialize(Uint8List buffer) {
    // final result = SecureFileEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x64199744);
  }
}

/// Secure File.
///
/// ID: `7d09c27e`.
class SecureFile extends SecureFileBase {
  /// Secure File constructor.
  const SecureFile({
    required this.id,
    required this.accessHash,
    required this.size,
    required this.dcId,
    required this.date,
    required this.fileHash,
    required this.secret,
  }) : super._();

  /// Deserialize.
  factory SecureFile.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final size = _readlong(buffer);
    // final dcId = _readint(buffer);
    // final date = _readint(buffer);
    // final fileHash = _readbytes(buffer);
    // final secret = _readbytes(buffer);
    // final result = SecureFile(id: id, accessHash: accessHash, size: size, dcId: dcId, date: date, fileHash: fileHash, secret: secret,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Size.
  final int size;

  /// Dc Id.
  final int dcId;

  /// Date.
  final DateTime date;

  /// File Hash.
  final Uint8List fileHash;

  /// Secret.
  final Uint8List secret;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7d09c27e);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeLong(size);
    buffer.writeInt(dcId);
    buffer.writeDateTime(date);
    buffer.writeBytes(fileHash);
    buffer.writeBytes(secret);
  }
}

/// Predicate Secure Data.
abstract class SecureDataBase extends TlConstructor {
  /// Predicate Secure Data constructor.
  const SecureDataBase._() : super._();
}

/// Secure Data.
///
/// ID: `8aeabec3`.
class SecureData extends SecureDataBase {
  /// Secure Data constructor.
  const SecureData({
    required this.data,
    required this.dataHash,
    required this.secret,
  }) : super._();

  /// Deserialize.
  factory SecureData.deserialize(Uint8List buffer) {
    // final data = _readbytes(buffer);
    // final dataHash = _readbytes(buffer);
    // final secret = _readbytes(buffer);
    // final result = SecureData(data: data, dataHash: dataHash, secret: secret,);

    // return result;

    throw Exception();
  }

  /// Data.
  final Uint8List data;

  /// Data Hash.
  final Uint8List dataHash;

  /// Secret.
  final Uint8List secret;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8aeabec3);
    buffer.writeBytes(data);
    buffer.writeBytes(dataHash);
    buffer.writeBytes(secret);
  }
}

/// Predicate Secure Plain Data.
abstract class SecurePlainDataBase extends TlConstructor {
  /// Predicate Secure Plain Data constructor.
  const SecurePlainDataBase._() : super._();
}

/// Secure Plain Phone.
///
/// ID: `7d6099dd`.
class SecurePlainPhone extends SecurePlainDataBase {
  /// Secure Plain Phone constructor.
  const SecurePlainPhone({
    required this.phone,
  }) : super._();

  /// Deserialize.
  factory SecurePlainPhone.deserialize(Uint8List buffer) {
    // final phone = _readstring(buffer);
    // final result = SecurePlainPhone(phone: phone,);

    // return result;

    throw Exception();
  }

  /// Phone.
  final String phone;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7d6099dd);
    buffer.writeString(phone);
  }
}

/// Secure Plain Email.
///
/// ID: `21ec5a5f`.
class SecurePlainEmail extends SecurePlainDataBase {
  /// Secure Plain Email constructor.
  const SecurePlainEmail({
    required this.email,
  }) : super._();

  /// Deserialize.
  factory SecurePlainEmail.deserialize(Uint8List buffer) {
    // final email = _readstring(buffer);
    // final result = SecurePlainEmail(email: email,);

    // return result;

    throw Exception();
  }

  /// Email.
  final String email;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x21ec5a5f);
    buffer.writeString(email);
  }
}

/// Predicate Secure Value Type.
abstract class SecureValueTypeBase extends TlConstructor {
  /// Predicate Secure Value Type constructor.
  const SecureValueTypeBase._() : super._();
}

/// Secure Value Type Personal Details.
///
/// ID: `9d2a81e3`.
class SecureValueTypePersonalDetails extends SecureValueTypeBase {
  /// Secure Value Type Personal Details constructor.
  const SecureValueTypePersonalDetails() : super._();

  /// Deserialize.
  factory SecureValueTypePersonalDetails.deserialize(Uint8List buffer) {
    // final result = SecureValueTypePersonalDetails();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9d2a81e3);
  }
}

/// Secure Value Type Passport.
///
/// ID: `3dac6a00`.
class SecureValueTypePassport extends SecureValueTypeBase {
  /// Secure Value Type Passport constructor.
  const SecureValueTypePassport() : super._();

  /// Deserialize.
  factory SecureValueTypePassport.deserialize(Uint8List buffer) {
    // final result = SecureValueTypePassport();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3dac6a00);
  }
}

/// Secure Value Type Driver License.
///
/// ID: `06e425c4`.
class SecureValueTypeDriverLicense extends SecureValueTypeBase {
  /// Secure Value Type Driver License constructor.
  const SecureValueTypeDriverLicense() : super._();

  /// Deserialize.
  factory SecureValueTypeDriverLicense.deserialize(Uint8List buffer) {
    // final result = SecureValueTypeDriverLicense();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x06e425c4);
  }
}

/// Secure Value Type Identity Card.
///
/// ID: `a0d0744b`.
class SecureValueTypeIdentityCard extends SecureValueTypeBase {
  /// Secure Value Type Identity Card constructor.
  const SecureValueTypeIdentityCard() : super._();

  /// Deserialize.
  factory SecureValueTypeIdentityCard.deserialize(Uint8List buffer) {
    // final result = SecureValueTypeIdentityCard();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa0d0744b);
  }
}

/// Secure Value Type Internal Passport.
///
/// ID: `99a48f23`.
class SecureValueTypeInternalPassport extends SecureValueTypeBase {
  /// Secure Value Type Internal Passport constructor.
  const SecureValueTypeInternalPassport() : super._();

  /// Deserialize.
  factory SecureValueTypeInternalPassport.deserialize(Uint8List buffer) {
    // final result = SecureValueTypeInternalPassport();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x99a48f23);
  }
}

/// Secure Value Type Address.
///
/// ID: `cbe31e26`.
class SecureValueTypeAddress extends SecureValueTypeBase {
  /// Secure Value Type Address constructor.
  const SecureValueTypeAddress() : super._();

  /// Deserialize.
  factory SecureValueTypeAddress.deserialize(Uint8List buffer) {
    // final result = SecureValueTypeAddress();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcbe31e26);
  }
}

/// Secure Value Type Utility Bill.
///
/// ID: `fc36954e`.
class SecureValueTypeUtilityBill extends SecureValueTypeBase {
  /// Secure Value Type Utility Bill constructor.
  const SecureValueTypeUtilityBill() : super._();

  /// Deserialize.
  factory SecureValueTypeUtilityBill.deserialize(Uint8List buffer) {
    // final result = SecureValueTypeUtilityBill();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfc36954e);
  }
}

/// Secure Value Type Bank Statement.
///
/// ID: `89137c0d`.
class SecureValueTypeBankStatement extends SecureValueTypeBase {
  /// Secure Value Type Bank Statement constructor.
  const SecureValueTypeBankStatement() : super._();

  /// Deserialize.
  factory SecureValueTypeBankStatement.deserialize(Uint8List buffer) {
    // final result = SecureValueTypeBankStatement();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x89137c0d);
  }
}

/// Secure Value Type Rental Agreement.
///
/// ID: `8b883488`.
class SecureValueTypeRentalAgreement extends SecureValueTypeBase {
  /// Secure Value Type Rental Agreement constructor.
  const SecureValueTypeRentalAgreement() : super._();

  /// Deserialize.
  factory SecureValueTypeRentalAgreement.deserialize(Uint8List buffer) {
    // final result = SecureValueTypeRentalAgreement();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8b883488);
  }
}

/// Secure Value Type Passport Registration.
///
/// ID: `99e3806a`.
class SecureValueTypePassportRegistration extends SecureValueTypeBase {
  /// Secure Value Type Passport Registration constructor.
  const SecureValueTypePassportRegistration() : super._();

  /// Deserialize.
  factory SecureValueTypePassportRegistration.deserialize(Uint8List buffer) {
    // final result = SecureValueTypePassportRegistration();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x99e3806a);
  }
}

/// Secure Value Type Temporary Registration.
///
/// ID: `ea02ec33`.
class SecureValueTypeTemporaryRegistration extends SecureValueTypeBase {
  /// Secure Value Type Temporary Registration constructor.
  const SecureValueTypeTemporaryRegistration() : super._();

  /// Deserialize.
  factory SecureValueTypeTemporaryRegistration.deserialize(Uint8List buffer) {
    // final result = SecureValueTypeTemporaryRegistration();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xea02ec33);
  }
}

/// Secure Value Type Phone.
///
/// ID: `b320aadb`.
class SecureValueTypePhone extends SecureValueTypeBase {
  /// Secure Value Type Phone constructor.
  const SecureValueTypePhone() : super._();

  /// Deserialize.
  factory SecureValueTypePhone.deserialize(Uint8List buffer) {
    // final result = SecureValueTypePhone();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb320aadb);
  }
}

/// Secure Value Type Email.
///
/// ID: `8e3ca7ee`.
class SecureValueTypeEmail extends SecureValueTypeBase {
  /// Secure Value Type Email constructor.
  const SecureValueTypeEmail() : super._();

  /// Deserialize.
  factory SecureValueTypeEmail.deserialize(Uint8List buffer) {
    // final result = SecureValueTypeEmail();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8e3ca7ee);
  }
}

/// Predicate Secure Value.
abstract class SecureValueBase extends TlConstructor {
  /// Predicate Secure Value constructor.
  const SecureValueBase._() : super._();
}

/// Secure Value.
///
/// ID: `187fa0ca`.
class SecureValue extends SecureValueBase {
  /// Secure Value constructor.
  const SecureValue({
    required this.type,
    this.data,
    this.frontSide,
    this.reverseSide,
    this.selfie,
    this.translation,
    this.files,
    this.plainData,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory SecureValue.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final type = _readSecureValueType(buffer);
    // final data = _readflags.0?SecureData(buffer);
    // final frontSide = _readflags.1?SecureFile(buffer);
    // final reverseSide = _readflags.2?SecureFile(buffer);
    // final selfie = _readflags.3?SecureFile(buffer);
    // final translation = _readflags.6?Vector<SecureFile>(buffer);
    // final files = _readflags.4?Vector<SecureFile>(buffer);
    // final plainData = _readflags.5?SecurePlainData(buffer);
    // final hash = _readbytes(buffer);
    // final result = SecureValue(flags: flags, type: type, data: data, frontSide: frontSide, reverseSide: reverseSide, selfie: selfie, translation: translation, files: files, plainData: plainData, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: data != null,
      b01: frontSide != null,
      b02: reverseSide != null,
      b03: selfie != null,
      b06: translation != null,
      b04: files != null,
      b05: plainData != null,
    );

    return v;
  }

  /// Type.
  final SecureValueTypeBase type;

  /// Data.
  final SecureDataBase? data;

  /// Front Side.
  final SecureFileBase? frontSide;

  /// Reverse Side.
  final SecureFileBase? reverseSide;

  /// Selfie.
  final SecureFileBase? selfie;

  /// Translation.
  final List<SecureFileBase>? translation;

  /// Files.
  final List<SecureFileBase>? files;

  /// Plain Data.
  final SecurePlainDataBase? plainData;

  /// Hash.
  final Uint8List hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x187fa0ca);
    buffer.writeInt(flags);
    buffer.writeObject(type);
    final localDataCopy = data;
    if (localDataCopy != null) {
      buffer.writeObject(localDataCopy);
    }
    final localFrontSideCopy = frontSide;
    if (localFrontSideCopy != null) {
      buffer.writeObject(localFrontSideCopy);
    }
    final localReverseSideCopy = reverseSide;
    if (localReverseSideCopy != null) {
      buffer.writeObject(localReverseSideCopy);
    }
    final localSelfieCopy = selfie;
    if (localSelfieCopy != null) {
      buffer.writeObject(localSelfieCopy);
    }
    final localTranslationCopy = translation;
    if (localTranslationCopy != null) {
      buffer.writeVectorObject(localTranslationCopy);
    }
    final localFilesCopy = files;
    if (localFilesCopy != null) {
      buffer.writeVectorObject(localFilesCopy);
    }
    final localPlainDataCopy = plainData;
    if (localPlainDataCopy != null) {
      buffer.writeObject(localPlainDataCopy);
    }
    buffer.writeBytes(hash);
  }
}

/// Predicate Input Secure Value.
abstract class InputSecureValueBase extends TlConstructor {
  /// Predicate Input Secure Value constructor.
  const InputSecureValueBase._() : super._();
}

/// Input Secure Value.
///
/// ID: `db21d0a7`.
class InputSecureValue extends InputSecureValueBase {
  /// Input Secure Value constructor.
  const InputSecureValue({
    required this.type,
    this.data,
    this.frontSide,
    this.reverseSide,
    this.selfie,
    this.translation,
    this.files,
    this.plainData,
  }) : super._();

  /// Deserialize.
  factory InputSecureValue.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final type = _readSecureValueType(buffer);
    // final data = _readflags.0?SecureData(buffer);
    // final frontSide = _readflags.1?InputSecureFile(buffer);
    // final reverseSide = _readflags.2?InputSecureFile(buffer);
    // final selfie = _readflags.3?InputSecureFile(buffer);
    // final translation = _readflags.6?Vector<InputSecureFile>(buffer);
    // final files = _readflags.4?Vector<InputSecureFile>(buffer);
    // final plainData = _readflags.5?SecurePlainData(buffer);
    // final result = InputSecureValue(flags: flags, type: type, data: data, frontSide: frontSide, reverseSide: reverseSide, selfie: selfie, translation: translation, files: files, plainData: plainData,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: data != null,
      b01: frontSide != null,
      b02: reverseSide != null,
      b03: selfie != null,
      b06: translation != null,
      b04: files != null,
      b05: plainData != null,
    );

    return v;
  }

  /// Type.
  final SecureValueTypeBase type;

  /// Data.
  final SecureDataBase? data;

  /// Front Side.
  final InputSecureFileBase? frontSide;

  /// Reverse Side.
  final InputSecureFileBase? reverseSide;

  /// Selfie.
  final InputSecureFileBase? selfie;

  /// Translation.
  final List<InputSecureFileBase>? translation;

  /// Files.
  final List<InputSecureFileBase>? files;

  /// Plain Data.
  final SecurePlainDataBase? plainData;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdb21d0a7);
    buffer.writeInt(flags);
    buffer.writeObject(type);
    final localDataCopy = data;
    if (localDataCopy != null) {
      buffer.writeObject(localDataCopy);
    }
    final localFrontSideCopy = frontSide;
    if (localFrontSideCopy != null) {
      buffer.writeObject(localFrontSideCopy);
    }
    final localReverseSideCopy = reverseSide;
    if (localReverseSideCopy != null) {
      buffer.writeObject(localReverseSideCopy);
    }
    final localSelfieCopy = selfie;
    if (localSelfieCopy != null) {
      buffer.writeObject(localSelfieCopy);
    }
    final localTranslationCopy = translation;
    if (localTranslationCopy != null) {
      buffer.writeVectorObject(localTranslationCopy);
    }
    final localFilesCopy = files;
    if (localFilesCopy != null) {
      buffer.writeVectorObject(localFilesCopy);
    }
    final localPlainDataCopy = plainData;
    if (localPlainDataCopy != null) {
      buffer.writeObject(localPlainDataCopy);
    }
  }
}

/// Predicate Secure Value Hash.
abstract class SecureValueHashBase extends TlConstructor {
  /// Predicate Secure Value Hash constructor.
  const SecureValueHashBase._() : super._();
}

/// Secure Value Hash.
///
/// ID: `ed1ecdb0`.
class SecureValueHash extends SecureValueHashBase {
  /// Secure Value Hash constructor.
  const SecureValueHash({
    required this.type,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory SecureValueHash.deserialize(Uint8List buffer) {
    // final type = _readSecureValueType(buffer);
    // final hash = _readbytes(buffer);
    // final result = SecureValueHash(type: type, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Type.
  final SecureValueTypeBase type;

  /// Hash.
  final Uint8List hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xed1ecdb0);
    buffer.writeObject(type);
    buffer.writeBytes(hash);
  }
}

/// Predicate Secure Value Error.
abstract class SecureValueErrorBase extends TlConstructor {
  /// Predicate Secure Value Error constructor.
  const SecureValueErrorBase._() : super._();
}

/// Secure Value Error Data.
///
/// ID: `e8a40bd9`.
class SecureValueErrorData extends SecureValueErrorBase {
  /// Secure Value Error Data constructor.
  const SecureValueErrorData({
    required this.type,
    required this.dataHash,
    required this.field,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory SecureValueErrorData.deserialize(Uint8List buffer) {
    // final type = _readSecureValueType(buffer);
    // final dataHash = _readbytes(buffer);
    // final field = _readstring(buffer);
    // final text = _readstring(buffer);
    // final result = SecureValueErrorData(type: type, dataHash: dataHash, field: field, text: text,);

    // return result;

    throw Exception();
  }

  /// Type.
  final SecureValueTypeBase type;

  /// Data Hash.
  final Uint8List dataHash;

  /// Field.
  final String field;

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe8a40bd9);
    buffer.writeObject(type);
    buffer.writeBytes(dataHash);
    buffer.writeString(field);
    buffer.writeString(text);
  }
}

/// Secure Value Error Front Side.
///
/// ID: `00be3dfa`.
class SecureValueErrorFrontSide extends SecureValueErrorBase {
  /// Secure Value Error Front Side constructor.
  const SecureValueErrorFrontSide({
    required this.type,
    required this.fileHash,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory SecureValueErrorFrontSide.deserialize(Uint8List buffer) {
    // final type = _readSecureValueType(buffer);
    // final fileHash = _readbytes(buffer);
    // final text = _readstring(buffer);
    // final result = SecureValueErrorFrontSide(type: type, fileHash: fileHash, text: text,);

    // return result;

    throw Exception();
  }

  /// Type.
  final SecureValueTypeBase type;

  /// File Hash.
  final Uint8List fileHash;

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x00be3dfa);
    buffer.writeObject(type);
    buffer.writeBytes(fileHash);
    buffer.writeString(text);
  }
}

/// Secure Value Error Reverse Side.
///
/// ID: `868a2aa5`.
class SecureValueErrorReverseSide extends SecureValueErrorBase {
  /// Secure Value Error Reverse Side constructor.
  const SecureValueErrorReverseSide({
    required this.type,
    required this.fileHash,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory SecureValueErrorReverseSide.deserialize(Uint8List buffer) {
    // final type = _readSecureValueType(buffer);
    // final fileHash = _readbytes(buffer);
    // final text = _readstring(buffer);
    // final result = SecureValueErrorReverseSide(type: type, fileHash: fileHash, text: text,);

    // return result;

    throw Exception();
  }

  /// Type.
  final SecureValueTypeBase type;

  /// File Hash.
  final Uint8List fileHash;

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x868a2aa5);
    buffer.writeObject(type);
    buffer.writeBytes(fileHash);
    buffer.writeString(text);
  }
}

/// Secure Value Error Selfie.
///
/// ID: `e537ced6`.
class SecureValueErrorSelfie extends SecureValueErrorBase {
  /// Secure Value Error Selfie constructor.
  const SecureValueErrorSelfie({
    required this.type,
    required this.fileHash,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory SecureValueErrorSelfie.deserialize(Uint8List buffer) {
    // final type = _readSecureValueType(buffer);
    // final fileHash = _readbytes(buffer);
    // final text = _readstring(buffer);
    // final result = SecureValueErrorSelfie(type: type, fileHash: fileHash, text: text,);

    // return result;

    throw Exception();
  }

  /// Type.
  final SecureValueTypeBase type;

  /// File Hash.
  final Uint8List fileHash;

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe537ced6);
    buffer.writeObject(type);
    buffer.writeBytes(fileHash);
    buffer.writeString(text);
  }
}

/// Secure Value Error File.
///
/// ID: `7a700873`.
class SecureValueErrorFile extends SecureValueErrorBase {
  /// Secure Value Error File constructor.
  const SecureValueErrorFile({
    required this.type,
    required this.fileHash,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory SecureValueErrorFile.deserialize(Uint8List buffer) {
    // final type = _readSecureValueType(buffer);
    // final fileHash = _readbytes(buffer);
    // final text = _readstring(buffer);
    // final result = SecureValueErrorFile(type: type, fileHash: fileHash, text: text,);

    // return result;

    throw Exception();
  }

  /// Type.
  final SecureValueTypeBase type;

  /// File Hash.
  final Uint8List fileHash;

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7a700873);
    buffer.writeObject(type);
    buffer.writeBytes(fileHash);
    buffer.writeString(text);
  }
}

/// Secure Value Error Files.
///
/// ID: `666220e9`.
class SecureValueErrorFiles extends SecureValueErrorBase {
  /// Secure Value Error Files constructor.
  const SecureValueErrorFiles({
    required this.type,
    required this.fileHash,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory SecureValueErrorFiles.deserialize(Uint8List buffer) {
    // final type = _readSecureValueType(buffer);
    // final fileHash = _readVector<bytes>(buffer);
    // final text = _readstring(buffer);
    // final result = SecureValueErrorFiles(type: type, fileHash: fileHash, text: text,);

    // return result;

    throw Exception();
  }

  /// Type.
  final SecureValueTypeBase type;

  /// File Hash.
  final List<Uint8List> fileHash;

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x666220e9);
    buffer.writeObject(type);
    buffer.writeVectorBytes(fileHash);
    buffer.writeString(text);
  }
}

/// Secure Value Error.
///
/// ID: `869d758f`.
class SecureValueError extends SecureValueErrorBase {
  /// Secure Value Error constructor.
  const SecureValueError({
    required this.type,
    required this.hash,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory SecureValueError.deserialize(Uint8List buffer) {
    // final type = _readSecureValueType(buffer);
    // final hash = _readbytes(buffer);
    // final text = _readstring(buffer);
    // final result = SecureValueError(type: type, hash: hash, text: text,);

    // return result;

    throw Exception();
  }

  /// Type.
  final SecureValueTypeBase type;

  /// Hash.
  final Uint8List hash;

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x869d758f);
    buffer.writeObject(type);
    buffer.writeBytes(hash);
    buffer.writeString(text);
  }
}

/// Secure Value Error Translation File.
///
/// ID: `a1144770`.
class SecureValueErrorTranslationFile extends SecureValueErrorBase {
  /// Secure Value Error Translation File constructor.
  const SecureValueErrorTranslationFile({
    required this.type,
    required this.fileHash,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory SecureValueErrorTranslationFile.deserialize(Uint8List buffer) {
    // final type = _readSecureValueType(buffer);
    // final fileHash = _readbytes(buffer);
    // final text = _readstring(buffer);
    // final result = SecureValueErrorTranslationFile(type: type, fileHash: fileHash, text: text,);

    // return result;

    throw Exception();
  }

  /// Type.
  final SecureValueTypeBase type;

  /// File Hash.
  final Uint8List fileHash;

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa1144770);
    buffer.writeObject(type);
    buffer.writeBytes(fileHash);
    buffer.writeString(text);
  }
}

/// Secure Value Error Translation Files.
///
/// ID: `34636dd8`.
class SecureValueErrorTranslationFiles extends SecureValueErrorBase {
  /// Secure Value Error Translation Files constructor.
  const SecureValueErrorTranslationFiles({
    required this.type,
    required this.fileHash,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory SecureValueErrorTranslationFiles.deserialize(Uint8List buffer) {
    // final type = _readSecureValueType(buffer);
    // final fileHash = _readVector<bytes>(buffer);
    // final text = _readstring(buffer);
    // final result = SecureValueErrorTranslationFiles(type: type, fileHash: fileHash, text: text,);

    // return result;

    throw Exception();
  }

  /// Type.
  final SecureValueTypeBase type;

  /// File Hash.
  final List<Uint8List> fileHash;

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x34636dd8);
    buffer.writeObject(type);
    buffer.writeVectorBytes(fileHash);
    buffer.writeString(text);
  }
}

/// Predicate Secure Credentials Encrypted.
abstract class SecureCredentialsEncryptedBase extends TlConstructor {
  /// Predicate Secure Credentials Encrypted constructor.
  const SecureCredentialsEncryptedBase._() : super._();
}

/// Secure Credentials Encrypted.
///
/// ID: `33f0ea47`.
class SecureCredentialsEncrypted extends SecureCredentialsEncryptedBase {
  /// Secure Credentials Encrypted constructor.
  const SecureCredentialsEncrypted({
    required this.data,
    required this.hash,
    required this.secret,
  }) : super._();

  /// Deserialize.
  factory SecureCredentialsEncrypted.deserialize(Uint8List buffer) {
    // final data = _readbytes(buffer);
    // final hash = _readbytes(buffer);
    // final secret = _readbytes(buffer);
    // final result = SecureCredentialsEncrypted(data: data, hash: hash, secret: secret,);

    // return result;

    throw Exception();
  }

  /// Data.
  final Uint8List data;

  /// Hash.
  final Uint8List hash;

  /// Secret.
  final Uint8List secret;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x33f0ea47);
    buffer.writeBytes(data);
    buffer.writeBytes(hash);
    buffer.writeBytes(secret);
  }
}

/// Predicate Account Authorization Form.
abstract class AccountAuthorizationFormBase extends TlConstructor {
  /// Predicate Account Authorization Form constructor.
  const AccountAuthorizationFormBase._() : super._();
}

/// Account Authorization Form.
///
/// ID: `ad2e1cd8`.
class AccountAuthorizationForm extends AccountAuthorizationFormBase {
  /// Account Authorization Form constructor.
  const AccountAuthorizationForm({
    required this.requiredTypes,
    required this.values,
    required this.errors,
    required this.users,
    this.privacyPolicyUrl,
  }) : super._();

  /// Deserialize.
  factory AccountAuthorizationForm.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final requiredTypes = _readVector<SecureRequiredType>(buffer);
    // final values = _readVector<SecureValue>(buffer);
    // final errors = _readVector<SecureValueError>(buffer);
    // final users = _readVector<User>(buffer);
    // final privacyPolicyUrl = _readflags.0?string(buffer);
    // final result = AccountAuthorizationForm(flags: flags, requiredTypes: requiredTypes, values: values, errors: errors, users: users, privacyPolicyUrl: privacyPolicyUrl,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: privacyPolicyUrl != null,
    );

    return v;
  }

  /// Required Types.
  final List<SecureRequiredTypeBase> requiredTypes;

  /// Values.
  final List<SecureValueBase> values;

  /// Errors.
  final List<SecureValueErrorBase> errors;

  /// Users.
  final List<UserBase> users;

  /// Privacy Policy Url.
  final String? privacyPolicyUrl;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xad2e1cd8);
    buffer.writeInt(flags);
    buffer.writeVectorObject(requiredTypes);
    buffer.writeVectorObject(values);
    buffer.writeVectorObject(errors);
    buffer.writeVectorObject(users);
    final localPrivacyPolicyUrlCopy = privacyPolicyUrl;
    if (localPrivacyPolicyUrlCopy != null) {
      buffer.writeString(localPrivacyPolicyUrlCopy);
    }
  }
}

/// Predicate Account Sent Email Code.
abstract class AccountSentEmailCodeBase extends TlConstructor {
  /// Predicate Account Sent Email Code constructor.
  const AccountSentEmailCodeBase._() : super._();
}

/// Account Sent Email Code.
///
/// ID: `811f854f`.
class AccountSentEmailCode extends AccountSentEmailCodeBase {
  /// Account Sent Email Code constructor.
  const AccountSentEmailCode({
    required this.emailPattern,
    required this.length,
  }) : super._();

  /// Deserialize.
  factory AccountSentEmailCode.deserialize(Uint8List buffer) {
    // final emailPattern = _readstring(buffer);
    // final length = _readint(buffer);
    // final result = AccountSentEmailCode(emailPattern: emailPattern, length: length,);

    // return result;

    throw Exception();
  }

  /// Email Pattern.
  final String emailPattern;

  /// Length.
  final int length;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x811f854f);
    buffer.writeString(emailPattern);
    buffer.writeInt(length);
  }
}

/// Predicate Help Deep Link Info.
abstract class HelpDeepLinkInfoBase extends TlConstructor {
  /// Predicate Help Deep Link Info constructor.
  const HelpDeepLinkInfoBase._() : super._();
}

/// Help Deep Link Info Empty.
///
/// ID: `66afa166`.
class HelpDeepLinkInfoEmpty extends HelpDeepLinkInfoBase {
  /// Help Deep Link Info Empty constructor.
  const HelpDeepLinkInfoEmpty() : super._();

  /// Deserialize.
  factory HelpDeepLinkInfoEmpty.deserialize(Uint8List buffer) {
    // final result = HelpDeepLinkInfoEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x66afa166);
  }
}

/// Help Deep Link Info.
///
/// ID: `6a4ee832`.
class HelpDeepLinkInfo extends HelpDeepLinkInfoBase {
  /// Help Deep Link Info constructor.
  const HelpDeepLinkInfo({
    required this.updateApp,
    required this.message,
    this.entities,
  }) : super._();

  /// Deserialize.
  factory HelpDeepLinkInfo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final updateApp = _readflags.0?true(buffer);
    // final message = _readstring(buffer);
    // final entities = _readflags.1?Vector<MessageEntity>(buffer);
    // final result = HelpDeepLinkInfo(flags: flags, updateApp: updateApp, message: message, entities: entities,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: updateApp,
      b01: entities != null,
    );

    return v;
  }

  /// update_app: bit 0 of flags.0?true
  final bool updateApp;

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6a4ee832);
    buffer.writeInt(flags);
    buffer.writeString(message);
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
  }
}

/// Predicate Saved Contact.
abstract class SavedContactBase extends TlConstructor {
  /// Predicate Saved Contact constructor.
  const SavedContactBase._() : super._();
}

/// Saved Phone Contact.
///
/// ID: `1142bd56`.
class SavedPhoneContact extends SavedContactBase {
  /// Saved Phone Contact constructor.
  const SavedPhoneContact({
    required this.phone,
    required this.firstName,
    required this.lastName,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory SavedPhoneContact.deserialize(Uint8List buffer) {
    // final phone = _readstring(buffer);
    // final firstName = _readstring(buffer);
    // final lastName = _readstring(buffer);
    // final date = _readint(buffer);
    // final result = SavedPhoneContact(phone: phone, firstName: firstName, lastName: lastName, date: date,);

    // return result;

    throw Exception();
  }

  /// Phone.
  final String phone;

  /// First Name.
  final String firstName;

  /// Last Name.
  final String lastName;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1142bd56);
    buffer.writeString(phone);
    buffer.writeString(firstName);
    buffer.writeString(lastName);
    buffer.writeDateTime(date);
  }
}

/// Predicate Account Takeout.
abstract class AccountTakeoutBase extends TlConstructor {
  /// Predicate Account Takeout constructor.
  const AccountTakeoutBase._() : super._();
}

/// Account Takeout.
///
/// ID: `4dba4501`.
class AccountTakeout extends AccountTakeoutBase {
  /// Account Takeout constructor.
  const AccountTakeout({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory AccountTakeout.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final result = AccountTakeout(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4dba4501);
    buffer.writeLong(id);
  }
}

/// Predicate Password Kdf Algo.
abstract class PasswordKdfAlgoBase extends TlConstructor {
  /// Predicate Password Kdf Algo constructor.
  const PasswordKdfAlgoBase._() : super._();
}

/// Password Kdf Algo Unknown.
///
/// ID: `d45ab096`.
class PasswordKdfAlgoUnknown extends PasswordKdfAlgoBase {
  /// Password Kdf Algo Unknown constructor.
  const PasswordKdfAlgoUnknown() : super._();

  /// Deserialize.
  factory PasswordKdfAlgoUnknown.deserialize(Uint8List buffer) {
    // final result = PasswordKdfAlgoUnknown();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd45ab096);
  }
}

/// Password Kdf Algo S H A256 S H A256 P B K D F2 H M A C S H A512iter100000 S H A256 Mod Pow.
///
/// ID: `3a912d4a`.
class PasswordKdfAlgoSHA256SHA256PBKDF2HMACSHA512iter100000SHA256ModPow
    extends PasswordKdfAlgoBase {
  /// Password Kdf Algo S H A256 S H A256 P B K D F2 H M A C S H A512iter100000 S H A256 Mod Pow constructor.
  const PasswordKdfAlgoSHA256SHA256PBKDF2HMACSHA512iter100000SHA256ModPow({
    required this.salt1,
    required this.salt2,
    required this.g,
    required this.p,
  }) : super._();

  /// Deserialize.
  factory PasswordKdfAlgoSHA256SHA256PBKDF2HMACSHA512iter100000SHA256ModPow.deserialize(
      Uint8List buffer) {
    // final salt1 = _readbytes(buffer);
    // final salt2 = _readbytes(buffer);
    // final g = _readint(buffer);
    // final p = _readbytes(buffer);
    // final result = PasswordKdfAlgoSHA256SHA256PBKDF2HMACSHA512iter100000SHA256ModPow(salt1: salt1, salt2: salt2, g: g, p: p,);

    // return result;

    throw Exception();
  }

  /// Salt1.
  final Uint8List salt1;

  /// Salt2.
  final Uint8List salt2;

  /// G.
  final int g;

  /// P.
  final Uint8List p;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3a912d4a);
    buffer.writeBytes(salt1);
    buffer.writeBytes(salt2);
    buffer.writeInt(g);
    buffer.writeBytes(p);
  }
}

/// Predicate Secure Password Kdf Algo.
abstract class SecurePasswordKdfAlgoBase extends TlConstructor {
  /// Predicate Secure Password Kdf Algo constructor.
  const SecurePasswordKdfAlgoBase._() : super._();
}

/// Secure Password Kdf Algo Unknown.
///
/// ID: `004a8537`.
class SecurePasswordKdfAlgoUnknown extends SecurePasswordKdfAlgoBase {
  /// Secure Password Kdf Algo Unknown constructor.
  const SecurePasswordKdfAlgoUnknown() : super._();

  /// Deserialize.
  factory SecurePasswordKdfAlgoUnknown.deserialize(Uint8List buffer) {
    // final result = SecurePasswordKdfAlgoUnknown();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x004a8537);
  }
}

/// Secure Password Kdf Algo P B K D F2 H M A C S H A512iter100000.
///
/// ID: `bbf2dda0`.
class SecurePasswordKdfAlgoPBKDF2HMACSHA512iter100000
    extends SecurePasswordKdfAlgoBase {
  /// Secure Password Kdf Algo P B K D F2 H M A C S H A512iter100000 constructor.
  const SecurePasswordKdfAlgoPBKDF2HMACSHA512iter100000({
    required this.salt,
  }) : super._();

  /// Deserialize.
  factory SecurePasswordKdfAlgoPBKDF2HMACSHA512iter100000.deserialize(
      Uint8List buffer) {
    // final salt = _readbytes(buffer);
    // final result = SecurePasswordKdfAlgoPBKDF2HMACSHA512iter100000(salt: salt,);

    // return result;

    throw Exception();
  }

  /// Salt.
  final Uint8List salt;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbbf2dda0);
    buffer.writeBytes(salt);
  }
}

/// Secure Password Kdf Algo S H A512.
///
/// ID: `86471d92`.
class SecurePasswordKdfAlgoSHA512 extends SecurePasswordKdfAlgoBase {
  /// Secure Password Kdf Algo S H A512 constructor.
  const SecurePasswordKdfAlgoSHA512({
    required this.salt,
  }) : super._();

  /// Deserialize.
  factory SecurePasswordKdfAlgoSHA512.deserialize(Uint8List buffer) {
    // final salt = _readbytes(buffer);
    // final result = SecurePasswordKdfAlgoSHA512(salt: salt,);

    // return result;

    throw Exception();
  }

  /// Salt.
  final Uint8List salt;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x86471d92);
    buffer.writeBytes(salt);
  }
}

/// Predicate Secure Secret Settings.
abstract class SecureSecretSettingsBase extends TlConstructor {
  /// Predicate Secure Secret Settings constructor.
  const SecureSecretSettingsBase._() : super._();
}

/// Secure Secret Settings.
///
/// ID: `1527bcac`.
class SecureSecretSettings extends SecureSecretSettingsBase {
  /// Secure Secret Settings constructor.
  const SecureSecretSettings({
    required this.secureAlgo,
    required this.secureSecret,
    required this.secureSecretId,
  }) : super._();

  /// Deserialize.
  factory SecureSecretSettings.deserialize(Uint8List buffer) {
    // final secureAlgo = _readSecurePasswordKdfAlgo(buffer);
    // final secureSecret = _readbytes(buffer);
    // final secureSecretId = _readlong(buffer);
    // final result = SecureSecretSettings(secureAlgo: secureAlgo, secureSecret: secureSecret, secureSecretId: secureSecretId,);

    // return result;

    throw Exception();
  }

  /// Secure Algo.
  final SecurePasswordKdfAlgoBase secureAlgo;

  /// Secure Secret.
  final Uint8List secureSecret;

  /// Secure Secret Id.
  final int secureSecretId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1527bcac);
    buffer.writeObject(secureAlgo);
    buffer.writeBytes(secureSecret);
    buffer.writeLong(secureSecretId);
  }
}

/// Predicate Input Check Password S R P.
abstract class InputCheckPasswordSRPBase extends TlConstructor {
  /// Predicate Input Check Password S R P constructor.
  const InputCheckPasswordSRPBase._() : super._();
}

/// Input Check Password Empty.
///
/// ID: `9880f658`.
class InputCheckPasswordEmpty extends InputCheckPasswordSRPBase {
  /// Input Check Password Empty constructor.
  const InputCheckPasswordEmpty() : super._();

  /// Deserialize.
  factory InputCheckPasswordEmpty.deserialize(Uint8List buffer) {
    // final result = InputCheckPasswordEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9880f658);
  }
}

/// Input Check Password S R P.
///
/// ID: `d27ff082`.
class InputCheckPasswordSRP extends InputCheckPasswordSRPBase {
  /// Input Check Password S R P constructor.
  const InputCheckPasswordSRP({
    required this.srpId,
    required this.a,
    required this.m1,
  }) : super._();

  /// Deserialize.
  factory InputCheckPasswordSRP.deserialize(Uint8List buffer) {
    // final srpId = _readlong(buffer);
    // final a = _readbytes(buffer);
    // final m1 = _readbytes(buffer);
    // final result = InputCheckPasswordSRP(srpId: srpId, a: a, m1: m1,);

    // return result;

    throw Exception();
  }

  /// Srp Id.
  final int srpId;

  /// A.
  final Uint8List a;

  /// M1.
  final Uint8List m1;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd27ff082);
    buffer.writeLong(srpId);
    buffer.writeBytes(a);
    buffer.writeBytes(m1);
  }
}

/// Predicate Secure Required Type.
abstract class SecureRequiredTypeBase extends TlConstructor {
  /// Predicate Secure Required Type constructor.
  const SecureRequiredTypeBase._() : super._();
}

/// Secure Required Type.
///
/// ID: `829d99da`.
class SecureRequiredType extends SecureRequiredTypeBase {
  /// Secure Required Type constructor.
  const SecureRequiredType({
    required this.nativeNames,
    required this.selfieRequired,
    required this.translationRequired,
    required this.type,
  }) : super._();

  /// Deserialize.
  factory SecureRequiredType.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final nativeNames = _readflags.0?true(buffer);
    // final selfieRequired = _readflags.1?true(buffer);
    // final translationRequired = _readflags.2?true(buffer);
    // final type = _readSecureValueType(buffer);
    // final result = SecureRequiredType(flags: flags, nativeNames: nativeNames, selfieRequired: selfieRequired, translationRequired: translationRequired, type: type,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: nativeNames,
      b01: selfieRequired,
      b02: translationRequired,
    );

    return v;
  }

  /// native_names: bit 0 of flags.0?true
  final bool nativeNames;

  /// selfie_required: bit 1 of flags.1?true
  final bool selfieRequired;

  /// translation_required: bit 2 of flags.2?true
  final bool translationRequired;

  /// Type.
  final SecureValueTypeBase type;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x829d99da);
    buffer.writeInt(flags);
    buffer.writeObject(type);
  }
}

/// Secure Required Type One Of.
///
/// ID: `027477b4`.
class SecureRequiredTypeOneOf extends SecureRequiredTypeBase {
  /// Secure Required Type One Of constructor.
  const SecureRequiredTypeOneOf({
    required this.types,
  }) : super._();

  /// Deserialize.
  factory SecureRequiredTypeOneOf.deserialize(Uint8List buffer) {
    // final types = _readVector<SecureRequiredType>(buffer);
    // final result = SecureRequiredTypeOneOf(types: types,);

    // return result;

    throw Exception();
  }

  /// Types.
  final List<SecureRequiredTypeBase> types;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x027477b4);
    buffer.writeVectorObject(types);
  }
}

/// Predicate Help Passport Config.
abstract class HelpPassportConfigBase extends TlConstructor {
  /// Predicate Help Passport Config constructor.
  const HelpPassportConfigBase._() : super._();
}

/// Help Passport Config Not Modified.
///
/// ID: `bfb9f457`.
class HelpPassportConfigNotModified extends HelpPassportConfigBase {
  /// Help Passport Config Not Modified constructor.
  const HelpPassportConfigNotModified() : super._();

  /// Deserialize.
  factory HelpPassportConfigNotModified.deserialize(Uint8List buffer) {
    // final result = HelpPassportConfigNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbfb9f457);
  }
}

/// Help Passport Config.
///
/// ID: `a098d6af`.
class HelpPassportConfig extends HelpPassportConfigBase {
  /// Help Passport Config constructor.
  const HelpPassportConfig({
    required this.hash,
    required this.countriesLangs,
  }) : super._();

  /// Deserialize.
  factory HelpPassportConfig.deserialize(Uint8List buffer) {
    // final hash = _readint(buffer);
    // final countriesLangs = _readDataJSON(buffer);
    // final result = HelpPassportConfig(hash: hash, countriesLangs: countriesLangs,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Countries Langs.
  final DataJSONBase countriesLangs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa098d6af);
    buffer.writeInt(hash);
    buffer.writeObject(countriesLangs);
  }
}

/// Predicate Input App Event.
abstract class InputAppEventBase extends TlConstructor {
  /// Predicate Input App Event constructor.
  const InputAppEventBase._() : super._();
}

/// Input App Event.
///
/// ID: `1d1b1245`.
class InputAppEvent extends InputAppEventBase {
  /// Input App Event constructor.
  const InputAppEvent({
    required this.time,
    required this.type,
    required this.peer,
    required this.data,
  }) : super._();

  /// Deserialize.
  factory InputAppEvent.deserialize(Uint8List buffer) {
    // final time = _readdouble(buffer);
    // final type = _readstring(buffer);
    // final peer = _readlong(buffer);
    // final data = _readJSONValue(buffer);
    // final result = InputAppEvent(time: time, type: type, peer: peer, data: data,);

    // return result;

    throw Exception();
  }

  /// Time.
  final double time;

  /// Type.
  final String type;

  /// Peer.
  final int peer;

  /// Data.
  final JSONValueBase data;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1d1b1245);
    buffer.writeDouble(time);
    buffer.writeString(type);
    buffer.writeLong(peer);
    buffer.writeObject(data);
  }
}

/// Predicate J S O N Object Value.
abstract class JSONObjectValueBase extends TlConstructor {
  /// Predicate J S O N Object Value constructor.
  const JSONObjectValueBase._() : super._();
}

/// Json Object Value.
///
/// ID: `c0de1bd9`.
class JsonObjectValue extends JSONObjectValueBase {
  /// Json Object Value constructor.
  const JsonObjectValue({
    required this.key,
    required this.value,
  }) : super._();

  /// Deserialize.
  factory JsonObjectValue.deserialize(Uint8List buffer) {
    // final key = _readstring(buffer);
    // final value = _readJSONValue(buffer);
    // final result = JsonObjectValue(key: key, value: value,);

    // return result;

    throw Exception();
  }

  /// Key.
  final String key;

  /// Value.
  final JSONValueBase value;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc0de1bd9);
    buffer.writeString(key);
    buffer.writeObject(value);
  }
}

/// Predicate J S O N Value.
abstract class JSONValueBase extends TlConstructor {
  /// Predicate J S O N Value constructor.
  const JSONValueBase._() : super._();
}

/// Json Null.
///
/// ID: `3f6d7b68`.
class JsonNull extends JSONValueBase {
  /// Json Null constructor.
  const JsonNull() : super._();

  /// Deserialize.
  factory JsonNull.deserialize(Uint8List buffer) {
    // final result = JsonNull();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3f6d7b68);
  }
}

/// Json Bool.
///
/// ID: `c7345e6a`.
class JsonBool extends JSONValueBase {
  /// Json Bool constructor.
  const JsonBool({
    required this.value,
  }) : super._();

  /// Deserialize.
  factory JsonBool.deserialize(Uint8List buffer) {
    // final value = _readBool(buffer);
    // final result = JsonBool(value: value,);

    // return result;

    throw Exception();
  }

  /// Value.
  final bool value;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc7345e6a);
    buffer.writeBool(value);
  }
}

/// Json Number.
///
/// ID: `2be0dfa4`.
class JsonNumber extends JSONValueBase {
  /// Json Number constructor.
  const JsonNumber({
    required this.value,
  }) : super._();

  /// Deserialize.
  factory JsonNumber.deserialize(Uint8List buffer) {
    // final value = _readdouble(buffer);
    // final result = JsonNumber(value: value,);

    // return result;

    throw Exception();
  }

  /// Value.
  final double value;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2be0dfa4);
    buffer.writeDouble(value);
  }
}

/// Json String.
///
/// ID: `b71e767a`.
class JsonString extends JSONValueBase {
  /// Json String constructor.
  const JsonString({
    required this.value,
  }) : super._();

  /// Deserialize.
  factory JsonString.deserialize(Uint8List buffer) {
    // final value = _readstring(buffer);
    // final result = JsonString(value: value,);

    // return result;

    throw Exception();
  }

  /// Value.
  final String value;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb71e767a);
    buffer.writeString(value);
  }
}

/// Json Array.
///
/// ID: `f7444763`.
class JsonArray extends JSONValueBase {
  /// Json Array constructor.
  const JsonArray({
    required this.value,
  }) : super._();

  /// Deserialize.
  factory JsonArray.deserialize(Uint8List buffer) {
    // final value = _readVector<JSONValue>(buffer);
    // final result = JsonArray(value: value,);

    // return result;

    throw Exception();
  }

  /// Value.
  final List<JSONValueBase> value;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf7444763);
    buffer.writeVectorObject(value);
  }
}

/// Json Object.
///
/// ID: `99c1d49d`.
class JsonObject extends JSONValueBase {
  /// Json Object constructor.
  const JsonObject({
    required this.value,
  }) : super._();

  /// Deserialize.
  factory JsonObject.deserialize(Uint8List buffer) {
    // final value = _readVector<JSONObjectValue>(buffer);
    // final result = JsonObject(value: value,);

    // return result;

    throw Exception();
  }

  /// Value.
  final List<JSONObjectValueBase> value;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x99c1d49d);
    buffer.writeVectorObject(value);
  }
}

/// Predicate Page Table Cell.
abstract class PageTableCellBase extends TlConstructor {
  /// Predicate Page Table Cell constructor.
  const PageTableCellBase._() : super._();
}

/// Page Table Cell.
///
/// ID: `34566b6a`.
class PageTableCell extends PageTableCellBase {
  /// Page Table Cell constructor.
  const PageTableCell({
    required this.header,
    required this.alignCenter,
    required this.alignRight,
    required this.valignMiddle,
    required this.valignBottom,
    this.text,
    this.colspan,
    this.rowspan,
  }) : super._();

  /// Deserialize.
  factory PageTableCell.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final header = _readflags.0?true(buffer);
    // final alignCenter = _readflags.3?true(buffer);
    // final alignRight = _readflags.4?true(buffer);
    // final valignMiddle = _readflags.5?true(buffer);
    // final valignBottom = _readflags.6?true(buffer);
    // final text = _readflags.7?RichText(buffer);
    // final colspan = _readflags.1?int(buffer);
    // final rowspan = _readflags.2?int(buffer);
    // final result = PageTableCell(flags: flags, header: header, alignCenter: alignCenter, alignRight: alignRight, valignMiddle: valignMiddle, valignBottom: valignBottom, text: text, colspan: colspan, rowspan: rowspan,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: header,
      b03: alignCenter,
      b04: alignRight,
      b05: valignMiddle,
      b06: valignBottom,
      b07: text != null,
      b01: colspan != null,
      b02: rowspan != null,
    );

    return v;
  }

  /// header: bit 0 of flags.0?true
  final bool header;

  /// align_center: bit 3 of flags.3?true
  final bool alignCenter;

  /// align_right: bit 4 of flags.4?true
  final bool alignRight;

  /// valign_middle: bit 5 of flags.5?true
  final bool valignMiddle;

  /// valign_bottom: bit 6 of flags.6?true
  final bool valignBottom;

  /// Text.
  final RichTextBase? text;

  /// Colspan.
  final int? colspan;

  /// Rowspan.
  final int? rowspan;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x34566b6a);
    buffer.writeInt(flags);
    final localTextCopy = text;
    if (localTextCopy != null) {
      buffer.writeObject(localTextCopy);
    }
    final localColspanCopy = colspan;
    if (localColspanCopy != null) {
      buffer.writeInt(localColspanCopy);
    }
    final localRowspanCopy = rowspan;
    if (localRowspanCopy != null) {
      buffer.writeInt(localRowspanCopy);
    }
  }
}

/// Predicate Page Table Row.
abstract class PageTableRowBase extends TlConstructor {
  /// Predicate Page Table Row constructor.
  const PageTableRowBase._() : super._();
}

/// Page Table Row.
///
/// ID: `e0c0c5e5`.
class PageTableRow extends PageTableRowBase {
  /// Page Table Row constructor.
  const PageTableRow({
    required this.cells,
  }) : super._();

  /// Deserialize.
  factory PageTableRow.deserialize(Uint8List buffer) {
    // final cells = _readVector<PageTableCell>(buffer);
    // final result = PageTableRow(cells: cells,);

    // return result;

    throw Exception();
  }

  /// Cells.
  final List<PageTableCellBase> cells;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe0c0c5e5);
    buffer.writeVectorObject(cells);
  }
}

/// Predicate Page Caption.
abstract class PageCaptionBase extends TlConstructor {
  /// Predicate Page Caption constructor.
  const PageCaptionBase._() : super._();
}

/// Page Caption.
///
/// ID: `6f747657`.
class PageCaption extends PageCaptionBase {
  /// Page Caption constructor.
  const PageCaption({
    required this.text,
    required this.credit,
  }) : super._();

  /// Deserialize.
  factory PageCaption.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final credit = _readRichText(buffer);
    // final result = PageCaption(text: text, credit: credit,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Credit.
  final RichTextBase credit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6f747657);
    buffer.writeObject(text);
    buffer.writeObject(credit);
  }
}

/// Predicate Page List Item.
abstract class PageListItemBase extends TlConstructor {
  /// Predicate Page List Item constructor.
  const PageListItemBase._() : super._();
}

/// Page List Item Text.
///
/// ID: `b92fb6cd`.
class PageListItemText extends PageListItemBase {
  /// Page List Item Text constructor.
  const PageListItemText({
    required this.text,
  }) : super._();

  /// Deserialize.
  factory PageListItemText.deserialize(Uint8List buffer) {
    // final text = _readRichText(buffer);
    // final result = PageListItemText(text: text,);

    // return result;

    throw Exception();
  }

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb92fb6cd);
    buffer.writeObject(text);
  }
}

/// Page List Item Blocks.
///
/// ID: `25e073fc`.
class PageListItemBlocks extends PageListItemBase {
  /// Page List Item Blocks constructor.
  const PageListItemBlocks({
    required this.blocks,
  }) : super._();

  /// Deserialize.
  factory PageListItemBlocks.deserialize(Uint8List buffer) {
    // final blocks = _readVector<PageBlock>(buffer);
    // final result = PageListItemBlocks(blocks: blocks,);

    // return result;

    throw Exception();
  }

  /// Blocks.
  final List<PageBlockBase> blocks;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x25e073fc);
    buffer.writeVectorObject(blocks);
  }
}

/// Predicate Page List Ordered Item.
abstract class PageListOrderedItemBase extends TlConstructor {
  /// Predicate Page List Ordered Item constructor.
  const PageListOrderedItemBase._() : super._();
}

/// Page List Ordered Item Text.
///
/// ID: `5e068047`.
class PageListOrderedItemText extends PageListOrderedItemBase {
  /// Page List Ordered Item Text constructor.
  const PageListOrderedItemText({
    required this.num,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory PageListOrderedItemText.deserialize(Uint8List buffer) {
    // final num = _readstring(buffer);
    // final text = _readRichText(buffer);
    // final result = PageListOrderedItemText(num: num, text: text,);

    // return result;

    throw Exception();
  }

  /// Num.
  final String num;

  /// Text.
  final RichTextBase text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5e068047);
    buffer.writeString(num);
    buffer.writeObject(text);
  }
}

/// Page List Ordered Item Blocks.
///
/// ID: `98dd8936`.
class PageListOrderedItemBlocks extends PageListOrderedItemBase {
  /// Page List Ordered Item Blocks constructor.
  const PageListOrderedItemBlocks({
    required this.num,
    required this.blocks,
  }) : super._();

  /// Deserialize.
  factory PageListOrderedItemBlocks.deserialize(Uint8List buffer) {
    // final num = _readstring(buffer);
    // final blocks = _readVector<PageBlock>(buffer);
    // final result = PageListOrderedItemBlocks(num: num, blocks: blocks,);

    // return result;

    throw Exception();
  }

  /// Num.
  final String num;

  /// Blocks.
  final List<PageBlockBase> blocks;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x98dd8936);
    buffer.writeString(num);
    buffer.writeVectorObject(blocks);
  }
}

/// Predicate Page Related Article.
abstract class PageRelatedArticleBase extends TlConstructor {
  /// Predicate Page Related Article constructor.
  const PageRelatedArticleBase._() : super._();
}

/// Page Related Article.
///
/// ID: `b390dc08`.
class PageRelatedArticle extends PageRelatedArticleBase {
  /// Page Related Article constructor.
  const PageRelatedArticle({
    required this.url,
    required this.webpageId,
    this.title,
    this.description,
    this.photoId,
    this.author,
    this.publishedDate,
  }) : super._();

  /// Deserialize.
  factory PageRelatedArticle.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final url = _readstring(buffer);
    // final webpageId = _readlong(buffer);
    // final title = _readflags.0?string(buffer);
    // final description = _readflags.1?string(buffer);
    // final photoId = _readflags.2?long(buffer);
    // final author = _readflags.3?string(buffer);
    // final publishedDate = _readflags.4?int(buffer);
    // final result = PageRelatedArticle(flags: flags, url: url, webpageId: webpageId, title: title, description: description, photoId: photoId, author: author, publishedDate: publishedDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: title != null,
      b01: description != null,
      b02: photoId != null,
      b03: author != null,
      b04: publishedDate != null,
    );

    return v;
  }

  /// Url.
  final String url;

  /// Webpage Id.
  final int webpageId;

  /// Title.
  final String? title;

  /// Description.
  final String? description;

  /// Photo Id.
  final int? photoId;

  /// Author.
  final String? author;

  /// Published Date.
  final DateTime? publishedDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb390dc08);
    buffer.writeInt(flags);
    buffer.writeString(url);
    buffer.writeLong(webpageId);
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localDescriptionCopy = description;
    if (localDescriptionCopy != null) {
      buffer.writeString(localDescriptionCopy);
    }
    final localPhotoIdCopy = photoId;
    if (localPhotoIdCopy != null) {
      buffer.writeLong(localPhotoIdCopy);
    }
    final localAuthorCopy = author;
    if (localAuthorCopy != null) {
      buffer.writeString(localAuthorCopy);
    }
    final localPublishedDateCopy = publishedDate;
    if (localPublishedDateCopy != null) {
      buffer.writeDateTime(localPublishedDateCopy);
    }
  }
}

/// Predicate Page.
abstract class PageBase extends TlConstructor {
  /// Predicate Page constructor.
  const PageBase._() : super._();
}

/// Page.
///
/// ID: `98657f0d`.
class Page extends PageBase {
  /// Page constructor.
  const Page({
    required this.part,
    required this.rtl,
    required this.v2,
    required this.url,
    required this.blocks,
    required this.photos,
    required this.documents,
    this.views,
  }) : super._();

  /// Deserialize.
  factory Page.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final part = _readflags.0?true(buffer);
    // final rtl = _readflags.1?true(buffer);
    // final v2 = _readflags.2?true(buffer);
    // final url = _readstring(buffer);
    // final blocks = _readVector<PageBlock>(buffer);
    // final photos = _readVector<Photo>(buffer);
    // final documents = _readVector<Document>(buffer);
    // final views = _readflags.3?int(buffer);
    // final result = Page(flags: flags, part: part, rtl: rtl, v2: v2, url: url, blocks: blocks, photos: photos, documents: documents, views: views,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: part,
      b01: rtl,
      b02: v2,
      b03: views != null,
    );

    return v;
  }

  /// part: bit 0 of flags.0?true
  final bool part;

  /// rtl: bit 1 of flags.1?true
  final bool rtl;

  /// v2: bit 2 of flags.2?true
  final bool v2;

  /// Url.
  final String url;

  /// Blocks.
  final List<PageBlockBase> blocks;

  /// Photos.
  final List<PhotoBase> photos;

  /// Documents.
  final List<DocumentBase> documents;

  /// Views.
  final int? views;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x98657f0d);
    buffer.writeInt(flags);
    buffer.writeString(url);
    buffer.writeVectorObject(blocks);
    buffer.writeVectorObject(photos);
    buffer.writeVectorObject(documents);
    final localViewsCopy = views;
    if (localViewsCopy != null) {
      buffer.writeInt(localViewsCopy);
    }
  }
}

/// Predicate Help Support Name.
abstract class HelpSupportNameBase extends TlConstructor {
  /// Predicate Help Support Name constructor.
  const HelpSupportNameBase._() : super._();
}

/// Help Support Name.
///
/// ID: `8c05f1c9`.
class HelpSupportName extends HelpSupportNameBase {
  /// Help Support Name constructor.
  const HelpSupportName({
    required this.name,
  }) : super._();

  /// Deserialize.
  factory HelpSupportName.deserialize(Uint8List buffer) {
    // final name = _readstring(buffer);
    // final result = HelpSupportName(name: name,);

    // return result;

    throw Exception();
  }

  /// Name.
  final String name;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8c05f1c9);
    buffer.writeString(name);
  }
}

/// Predicate Help User Info.
abstract class HelpUserInfoBase extends TlConstructor {
  /// Predicate Help User Info constructor.
  const HelpUserInfoBase._() : super._();
}

/// Help User Info Empty.
///
/// ID: `f3ae2eed`.
class HelpUserInfoEmpty extends HelpUserInfoBase {
  /// Help User Info Empty constructor.
  const HelpUserInfoEmpty() : super._();

  /// Deserialize.
  factory HelpUserInfoEmpty.deserialize(Uint8List buffer) {
    // final result = HelpUserInfoEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf3ae2eed);
  }
}

/// Help User Info.
///
/// ID: `01eb3758`.
class HelpUserInfo extends HelpUserInfoBase {
  /// Help User Info constructor.
  const HelpUserInfo({
    required this.message,
    required this.entities,
    required this.author,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory HelpUserInfo.deserialize(Uint8List buffer) {
    // final message = _readstring(buffer);
    // final entities = _readVector<MessageEntity>(buffer);
    // final author = _readstring(buffer);
    // final date = _readint(buffer);
    // final result = HelpUserInfo(message: message, entities: entities, author: author, date: date,);

    // return result;

    throw Exception();
  }

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase> entities;

  /// Author.
  final String author;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x01eb3758);
    buffer.writeString(message);
    buffer.writeVectorObject(entities);
    buffer.writeString(author);
    buffer.writeDateTime(date);
  }
}

/// Predicate Poll Answer.
abstract class PollAnswerBase extends TlConstructor {
  /// Predicate Poll Answer constructor.
  const PollAnswerBase._() : super._();
}

/// Poll Answer.
///
/// ID: `6ca9c2e9`.
class PollAnswer extends PollAnswerBase {
  /// Poll Answer constructor.
  const PollAnswer({
    required this.text,
    required this.option,
  }) : super._();

  /// Deserialize.
  factory PollAnswer.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final option = _readbytes(buffer);
    // final result = PollAnswer(text: text, option: option,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Option.
  final Uint8List option;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6ca9c2e9);
    buffer.writeString(text);
    buffer.writeBytes(option);
  }
}

/// Predicate Poll.
abstract class PollBase extends TlConstructor {
  /// Predicate Poll constructor.
  const PollBase._() : super._();
}

/// Poll.
///
/// ID: `86e18161`.
class Poll extends PollBase {
  /// Poll constructor.
  const Poll({
    required this.id,
    required this.closed,
    required this.publicVoters,
    required this.multipleChoice,
    required this.quiz,
    required this.question,
    required this.answers,
    this.closePeriod,
    this.closeDate,
  }) : super._();

  /// Deserialize.
  factory Poll.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final flags = _read#(buffer);
    // final closed = _readflags.0?true(buffer);
    // final publicVoters = _readflags.1?true(buffer);
    // final multipleChoice = _readflags.2?true(buffer);
    // final quiz = _readflags.3?true(buffer);
    // final question = _readstring(buffer);
    // final answers = _readVector<PollAnswer>(buffer);
    // final closePeriod = _readflags.4?int(buffer);
    // final closeDate = _readflags.5?int(buffer);
    // final result = Poll(id: id, flags: flags, closed: closed, publicVoters: publicVoters, multipleChoice: multipleChoice, quiz: quiz, question: question, answers: answers, closePeriod: closePeriod, closeDate: closeDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: closed,
      b01: publicVoters,
      b02: multipleChoice,
      b03: quiz,
      b04: closePeriod != null,
      b05: closeDate != null,
    );

    return v;
  }

  /// Id.
  final int id;

  /// closed: bit 0 of flags.0?true
  final bool closed;

  /// public_voters: bit 1 of flags.1?true
  final bool publicVoters;

  /// multiple_choice: bit 2 of flags.2?true
  final bool multipleChoice;

  /// quiz: bit 3 of flags.3?true
  final bool quiz;

  /// Question.
  final String question;

  /// Answers.
  final List<PollAnswerBase> answers;

  /// Close Period.
  final int? closePeriod;

  /// Close Date.
  final DateTime? closeDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x86e18161);
    buffer.writeLong(id);
    buffer.writeInt(flags);
    buffer.writeString(question);
    buffer.writeVectorObject(answers);
    final localClosePeriodCopy = closePeriod;
    if (localClosePeriodCopy != null) {
      buffer.writeInt(localClosePeriodCopy);
    }
    final localCloseDateCopy = closeDate;
    if (localCloseDateCopy != null) {
      buffer.writeDateTime(localCloseDateCopy);
    }
  }
}

/// Predicate Poll Answer Voters.
abstract class PollAnswerVotersBase extends TlConstructor {
  /// Predicate Poll Answer Voters constructor.
  const PollAnswerVotersBase._() : super._();
}

/// Poll Answer Voters.
///
/// ID: `3b6ddad2`.
class PollAnswerVoters extends PollAnswerVotersBase {
  /// Poll Answer Voters constructor.
  const PollAnswerVoters({
    required this.chosen,
    required this.correct,
    required this.option,
    required this.voters,
  }) : super._();

  /// Deserialize.
  factory PollAnswerVoters.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final chosen = _readflags.0?true(buffer);
    // final correct = _readflags.1?true(buffer);
    // final option = _readbytes(buffer);
    // final voters = _readint(buffer);
    // final result = PollAnswerVoters(flags: flags, chosen: chosen, correct: correct, option: option, voters: voters,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: chosen,
      b01: correct,
    );

    return v;
  }

  /// chosen: bit 0 of flags.0?true
  final bool chosen;

  /// correct: bit 1 of flags.1?true
  final bool correct;

  /// Option.
  final Uint8List option;

  /// Voters.
  final int voters;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3b6ddad2);
    buffer.writeInt(flags);
    buffer.writeBytes(option);
    buffer.writeInt(voters);
  }
}

/// Predicate Poll Results.
abstract class PollResultsBase extends TlConstructor {
  /// Predicate Poll Results constructor.
  const PollResultsBase._() : super._();
}

/// Poll Results.
///
/// ID: `7adf2420`.
class PollResults extends PollResultsBase {
  /// Poll Results constructor.
  const PollResults({
    required this.min,
    this.results,
    this.totalVoters,
    this.recentVoters,
    this.solution,
    this.solutionEntities,
  }) : super._();

  /// Deserialize.
  factory PollResults.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final min = _readflags.0?true(buffer);
    // final results = _readflags.1?Vector<PollAnswerVoters>(buffer);
    // final totalVoters = _readflags.2?int(buffer);
    // final recentVoters = _readflags.3?Vector<Peer>(buffer);
    // final solution = _readflags.4?string(buffer);
    // final solutionEntities = _readflags.4?Vector<MessageEntity>(buffer);
    // final result = PollResults(flags: flags, min: min, results: results, totalVoters: totalVoters, recentVoters: recentVoters, solution: solution, solutionEntities: solutionEntities,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: min,
      b01: results != null,
      b02: totalVoters != null,
      b03: recentVoters != null,
      b04: solution != null || solutionEntities != null,
    );

    return v;
  }

  /// min: bit 0 of flags.0?true
  final bool min;

  /// Results.
  final List<PollAnswerVotersBase>? results;

  /// Total Voters.
  final int? totalVoters;

  /// Recent Voters.
  final List<PeerBase>? recentVoters;

  /// Solution.
  final String? solution;

  /// Solution Entities.
  final List<MessageEntityBase>? solutionEntities;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7adf2420);
    buffer.writeInt(flags);
    final localResultsCopy = results;
    if (localResultsCopy != null) {
      buffer.writeVectorObject(localResultsCopy);
    }
    final localTotalVotersCopy = totalVoters;
    if (localTotalVotersCopy != null) {
      buffer.writeInt(localTotalVotersCopy);
    }
    final localRecentVotersCopy = recentVoters;
    if (localRecentVotersCopy != null) {
      buffer.writeVectorObject(localRecentVotersCopy);
    }
    final localSolutionCopy = solution;
    if (localSolutionCopy != null) {
      buffer.writeString(localSolutionCopy);
    }
    final localSolutionEntitiesCopy = solutionEntities;
    if (localSolutionEntitiesCopy != null) {
      buffer.writeVectorObject(localSolutionEntitiesCopy);
    }
  }
}

/// Predicate Chat Onlines.
abstract class ChatOnlinesBase extends TlConstructor {
  /// Predicate Chat Onlines constructor.
  const ChatOnlinesBase._() : super._();
}

/// Chat Onlines.
///
/// ID: `f041e250`.
class ChatOnlines extends ChatOnlinesBase {
  /// Chat Onlines constructor.
  const ChatOnlines({
    required this.onlines,
  }) : super._();

  /// Deserialize.
  factory ChatOnlines.deserialize(Uint8List buffer) {
    // final onlines = _readint(buffer);
    // final result = ChatOnlines(onlines: onlines,);

    // return result;

    throw Exception();
  }

  /// Onlines.
  final int onlines;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf041e250);
    buffer.writeInt(onlines);
  }
}

/// Predicate Stats U R L.
abstract class StatsURLBase extends TlConstructor {
  /// Predicate Stats U R L constructor.
  const StatsURLBase._() : super._();
}

/// Stats U R L.
///
/// ID: `47a971e0`.
class StatsURL extends StatsURLBase {
  /// Stats U R L constructor.
  const StatsURL({
    required this.url,
  }) : super._();

  /// Deserialize.
  factory StatsURL.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final result = StatsURL(url: url,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x47a971e0);
    buffer.writeString(url);
  }
}

/// Predicate Chat Admin Rights.
abstract class ChatAdminRightsBase extends TlConstructor {
  /// Predicate Chat Admin Rights constructor.
  const ChatAdminRightsBase._() : super._();
}

/// Chat Admin Rights.
///
/// ID: `5fb224d5`.
class ChatAdminRights extends ChatAdminRightsBase {
  /// Chat Admin Rights constructor.
  const ChatAdminRights({
    required this.changeInfo,
    required this.postMessages,
    required this.editMessages,
    required this.deleteMessages,
    required this.banUsers,
    required this.inviteUsers,
    required this.pinMessages,
    required this.addAdmins,
    required this.anonymous,
    required this.manageCall,
    required this.other,
    required this.manageTopics,
    required this.postStories,
    required this.editStories,
    required this.deleteStories,
  }) : super._();

  /// Deserialize.
  factory ChatAdminRights.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final changeInfo = _readflags.0?true(buffer);
    // final postMessages = _readflags.1?true(buffer);
    // final editMessages = _readflags.2?true(buffer);
    // final deleteMessages = _readflags.3?true(buffer);
    // final banUsers = _readflags.4?true(buffer);
    // final inviteUsers = _readflags.5?true(buffer);
    // final pinMessages = _readflags.7?true(buffer);
    // final addAdmins = _readflags.9?true(buffer);
    // final anonymous = _readflags.10?true(buffer);
    // final manageCall = _readflags.11?true(buffer);
    // final other = _readflags.12?true(buffer);
    // final manageTopics = _readflags.13?true(buffer);
    // final postStories = _readflags.14?true(buffer);
    // final editStories = _readflags.15?true(buffer);
    // final deleteStories = _readflags.16?true(buffer);
    // final result = ChatAdminRights(flags: flags, changeInfo: changeInfo, postMessages: postMessages, editMessages: editMessages, deleteMessages: deleteMessages, banUsers: banUsers, inviteUsers: inviteUsers, pinMessages: pinMessages, addAdmins: addAdmins, anonymous: anonymous, manageCall: manageCall, other: other, manageTopics: manageTopics, postStories: postStories, editStories: editStories, deleteStories: deleteStories,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: changeInfo,
      b01: postMessages,
      b02: editMessages,
      b03: deleteMessages,
      b04: banUsers,
      b05: inviteUsers,
      b07: pinMessages,
      b09: addAdmins,
      b10: anonymous,
      b11: manageCall,
      b12: other,
      b13: manageTopics,
      b14: postStories,
      b15: editStories,
      b16: deleteStories,
    );

    return v;
  }

  /// change_info: bit 0 of flags.0?true
  final bool changeInfo;

  /// post_messages: bit 1 of flags.1?true
  final bool postMessages;

  /// edit_messages: bit 2 of flags.2?true
  final bool editMessages;

  /// delete_messages: bit 3 of flags.3?true
  final bool deleteMessages;

  /// ban_users: bit 4 of flags.4?true
  final bool banUsers;

  /// invite_users: bit 5 of flags.5?true
  final bool inviteUsers;

  /// pin_messages: bit 7 of flags.7?true
  final bool pinMessages;

  /// add_admins: bit 9 of flags.9?true
  final bool addAdmins;

  /// anonymous: bit 10 of flags.10?true
  final bool anonymous;

  /// manage_call: bit 11 of flags.11?true
  final bool manageCall;

  /// other: bit 12 of flags.12?true
  final bool other;

  /// manage_topics: bit 13 of flags.13?true
  final bool manageTopics;

  /// post_stories: bit 14 of flags.14?true
  final bool postStories;

  /// edit_stories: bit 15 of flags.15?true
  final bool editStories;

  /// delete_stories: bit 16 of flags.16?true
  final bool deleteStories;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5fb224d5);
    buffer.writeInt(flags);
  }
}

/// Predicate Chat Banned Rights.
abstract class ChatBannedRightsBase extends TlConstructor {
  /// Predicate Chat Banned Rights constructor.
  const ChatBannedRightsBase._() : super._();
}

/// Chat Banned Rights.
///
/// ID: `9f120418`.
class ChatBannedRights extends ChatBannedRightsBase {
  /// Chat Banned Rights constructor.
  const ChatBannedRights({
    required this.viewMessages,
    required this.sendMessages,
    required this.sendMedia,
    required this.sendStickers,
    required this.sendGifs,
    required this.sendGames,
    required this.sendInline,
    required this.embedLinks,
    required this.sendPolls,
    required this.changeInfo,
    required this.inviteUsers,
    required this.pinMessages,
    required this.manageTopics,
    required this.sendPhotos,
    required this.sendVideos,
    required this.sendRoundvideos,
    required this.sendAudios,
    required this.sendVoices,
    required this.sendDocs,
    required this.sendPlain,
    required this.untilDate,
  }) : super._();

  /// Deserialize.
  factory ChatBannedRights.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final viewMessages = _readflags.0?true(buffer);
    // final sendMessages = _readflags.1?true(buffer);
    // final sendMedia = _readflags.2?true(buffer);
    // final sendStickers = _readflags.3?true(buffer);
    // final sendGifs = _readflags.4?true(buffer);
    // final sendGames = _readflags.5?true(buffer);
    // final sendInline = _readflags.6?true(buffer);
    // final embedLinks = _readflags.7?true(buffer);
    // final sendPolls = _readflags.8?true(buffer);
    // final changeInfo = _readflags.10?true(buffer);
    // final inviteUsers = _readflags.15?true(buffer);
    // final pinMessages = _readflags.17?true(buffer);
    // final manageTopics = _readflags.18?true(buffer);
    // final sendPhotos = _readflags.19?true(buffer);
    // final sendVideos = _readflags.20?true(buffer);
    // final sendRoundvideos = _readflags.21?true(buffer);
    // final sendAudios = _readflags.22?true(buffer);
    // final sendVoices = _readflags.23?true(buffer);
    // final sendDocs = _readflags.24?true(buffer);
    // final sendPlain = _readflags.25?true(buffer);
    // final untilDate = _readint(buffer);
    // final result = ChatBannedRights(flags: flags, viewMessages: viewMessages, sendMessages: sendMessages, sendMedia: sendMedia, sendStickers: sendStickers, sendGifs: sendGifs, sendGames: sendGames, sendInline: sendInline, embedLinks: embedLinks, sendPolls: sendPolls, changeInfo: changeInfo, inviteUsers: inviteUsers, pinMessages: pinMessages, manageTopics: manageTopics, sendPhotos: sendPhotos, sendVideos: sendVideos, sendRoundvideos: sendRoundvideos, sendAudios: sendAudios, sendVoices: sendVoices, sendDocs: sendDocs, sendPlain: sendPlain, untilDate: untilDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: viewMessages,
      b01: sendMessages,
      b02: sendMedia,
      b03: sendStickers,
      b04: sendGifs,
      b05: sendGames,
      b06: sendInline,
      b07: embedLinks,
      b08: sendPolls,
      b10: changeInfo,
      b15: inviteUsers,
      b17: pinMessages,
      b18: manageTopics,
      b19: sendPhotos,
      b20: sendVideos,
      b21: sendRoundvideos,
      b22: sendAudios,
      b23: sendVoices,
      b24: sendDocs,
      b25: sendPlain,
    );

    return v;
  }

  /// view_messages: bit 0 of flags.0?true
  final bool viewMessages;

  /// send_messages: bit 1 of flags.1?true
  final bool sendMessages;

  /// send_media: bit 2 of flags.2?true
  final bool sendMedia;

  /// send_stickers: bit 3 of flags.3?true
  final bool sendStickers;

  /// send_gifs: bit 4 of flags.4?true
  final bool sendGifs;

  /// send_games: bit 5 of flags.5?true
  final bool sendGames;

  /// send_inline: bit 6 of flags.6?true
  final bool sendInline;

  /// embed_links: bit 7 of flags.7?true
  final bool embedLinks;

  /// send_polls: bit 8 of flags.8?true
  final bool sendPolls;

  /// change_info: bit 10 of flags.10?true
  final bool changeInfo;

  /// invite_users: bit 15 of flags.15?true
  final bool inviteUsers;

  /// pin_messages: bit 17 of flags.17?true
  final bool pinMessages;

  /// manage_topics: bit 18 of flags.18?true
  final bool manageTopics;

  /// send_photos: bit 19 of flags.19?true
  final bool sendPhotos;

  /// send_videos: bit 20 of flags.20?true
  final bool sendVideos;

  /// send_roundvideos: bit 21 of flags.21?true
  final bool sendRoundvideos;

  /// send_audios: bit 22 of flags.22?true
  final bool sendAudios;

  /// send_voices: bit 23 of flags.23?true
  final bool sendVoices;

  /// send_docs: bit 24 of flags.24?true
  final bool sendDocs;

  /// send_plain: bit 25 of flags.25?true
  final bool sendPlain;

  /// Until Date.
  final DateTime untilDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9f120418);
    buffer.writeInt(flags);
    buffer.writeDateTime(untilDate);
  }
}

/// Predicate Input Wall Paper.
abstract class InputWallPaperBase extends TlConstructor {
  /// Predicate Input Wall Paper constructor.
  const InputWallPaperBase._() : super._();
}

/// Input Wall Paper.
///
/// ID: `e630b979`.
class InputWallPaper extends InputWallPaperBase {
  /// Input Wall Paper constructor.
  const InputWallPaper({
    required this.id,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputWallPaper.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputWallPaper(id: id, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe630b979);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
  }
}

/// Input Wall Paper Slug.
///
/// ID: `72091c80`.
class InputWallPaperSlug extends InputWallPaperBase {
  /// Input Wall Paper Slug constructor.
  const InputWallPaperSlug({
    required this.slug,
  }) : super._();

  /// Deserialize.
  factory InputWallPaperSlug.deserialize(Uint8List buffer) {
    // final slug = _readstring(buffer);
    // final result = InputWallPaperSlug(slug: slug,);

    // return result;

    throw Exception();
  }

  /// Slug.
  final String slug;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x72091c80);
    buffer.writeString(slug);
  }
}

/// Input Wall Paper No File.
///
/// ID: `967a462e`.
class InputWallPaperNoFile extends InputWallPaperBase {
  /// Input Wall Paper No File constructor.
  const InputWallPaperNoFile({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory InputWallPaperNoFile.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final result = InputWallPaperNoFile(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x967a462e);
    buffer.writeLong(id);
  }
}

/// Predicate Account Wall Papers.
abstract class AccountWallPapersBase extends TlConstructor {
  /// Predicate Account Wall Papers constructor.
  const AccountWallPapersBase._() : super._();
}

/// Account Wall Papers Not Modified.
///
/// ID: `1c199183`.
class AccountWallPapersNotModified extends AccountWallPapersBase {
  /// Account Wall Papers Not Modified constructor.
  const AccountWallPapersNotModified() : super._();

  /// Deserialize.
  factory AccountWallPapersNotModified.deserialize(Uint8List buffer) {
    // final result = AccountWallPapersNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1c199183);
  }
}

/// Account Wall Papers.
///
/// ID: `cdc3858c`.
class AccountWallPapers extends AccountWallPapersBase {
  /// Account Wall Papers constructor.
  const AccountWallPapers({
    required this.hash,
    required this.wallpapers,
  }) : super._();

  /// Deserialize.
  factory AccountWallPapers.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final wallpapers = _readVector<WallPaper>(buffer);
    // final result = AccountWallPapers(hash: hash, wallpapers: wallpapers,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Wallpapers.
  final List<WallPaperBase> wallpapers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcdc3858c);
    buffer.writeLong(hash);
    buffer.writeVectorObject(wallpapers);
  }
}

/// Predicate Code Settings.
abstract class CodeSettingsBase extends TlConstructor {
  /// Predicate Code Settings constructor.
  const CodeSettingsBase._() : super._();
}

/// Code Settings.
///
/// ID: `ad253d78`.
class CodeSettings extends CodeSettingsBase {
  /// Code Settings constructor.
  const CodeSettings({
    required this.allowFlashcall,
    required this.currentNumber,
    required this.allowAppHash,
    required this.allowMissedCall,
    required this.allowFirebase,
    this.logoutTokens,
    this.token,
    this.appSandbox,
  }) : super._();

  /// Deserialize.
  factory CodeSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final allowFlashcall = _readflags.0?true(buffer);
    // final currentNumber = _readflags.1?true(buffer);
    // final allowAppHash = _readflags.4?true(buffer);
    // final allowMissedCall = _readflags.5?true(buffer);
    // final allowFirebase = _readflags.7?true(buffer);
    // final logoutTokens = _readflags.6?Vector<bytes>(buffer);
    // final token = _readflags.8?string(buffer);
    // final appSandbox = _readflags.8?Bool(buffer);
    // final result = CodeSettings(flags: flags, allowFlashcall: allowFlashcall, currentNumber: currentNumber, allowAppHash: allowAppHash, allowMissedCall: allowMissedCall, allowFirebase: allowFirebase, logoutTokens: logoutTokens, token: token, appSandbox: appSandbox,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: allowFlashcall,
      b01: currentNumber,
      b04: allowAppHash,
      b05: allowMissedCall,
      b07: allowFirebase,
      b06: logoutTokens != null,
      b08: token != null || appSandbox != null,
    );

    return v;
  }

  /// allow_flashcall: bit 0 of flags.0?true
  final bool allowFlashcall;

  /// current_number: bit 1 of flags.1?true
  final bool currentNumber;

  /// allow_app_hash: bit 4 of flags.4?true
  final bool allowAppHash;

  /// allow_missed_call: bit 5 of flags.5?true
  final bool allowMissedCall;

  /// allow_firebase: bit 7 of flags.7?true
  final bool allowFirebase;

  /// Logout Tokens.
  final List<Uint8List>? logoutTokens;

  /// Token.
  final String? token;

  /// App Sandbox.
  final bool? appSandbox;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xad253d78);
    buffer.writeInt(flags);
    final localLogoutTokensCopy = logoutTokens;
    if (localLogoutTokensCopy != null) {
      buffer.writeVectorBytes(localLogoutTokensCopy);
    }
    final localTokenCopy = token;
    if (localTokenCopy != null) {
      buffer.writeString(localTokenCopy);
    }
    final localAppSandboxCopy = appSandbox;
    if (localAppSandboxCopy != null) {
      buffer.writeBool(localAppSandboxCopy);
    }
  }
}

/// Predicate Wall Paper Settings.
abstract class WallPaperSettingsBase extends TlConstructor {
  /// Predicate Wall Paper Settings constructor.
  const WallPaperSettingsBase._() : super._();
}

/// Wall Paper Settings.
///
/// ID: `372efcd0`.
class WallPaperSettings extends WallPaperSettingsBase {
  /// Wall Paper Settings constructor.
  const WallPaperSettings({
    required this.blur,
    required this.motion,
    this.backgroundColor,
    this.secondBackgroundColor,
    this.thirdBackgroundColor,
    this.fourthBackgroundColor,
    this.intensity,
    this.rotation,
    this.emoticon,
  }) : super._();

  /// Deserialize.
  factory WallPaperSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final blur = _readflags.1?true(buffer);
    // final motion = _readflags.2?true(buffer);
    // final backgroundColor = _readflags.0?int(buffer);
    // final secondBackgroundColor = _readflags.4?int(buffer);
    // final thirdBackgroundColor = _readflags.5?int(buffer);
    // final fourthBackgroundColor = _readflags.6?int(buffer);
    // final intensity = _readflags.3?int(buffer);
    // final rotation = _readflags.4?int(buffer);
    // final emoticon = _readflags.7?string(buffer);
    // final result = WallPaperSettings(flags: flags, blur: blur, motion: motion, backgroundColor: backgroundColor, secondBackgroundColor: secondBackgroundColor, thirdBackgroundColor: thirdBackgroundColor, fourthBackgroundColor: fourthBackgroundColor, intensity: intensity, rotation: rotation, emoticon: emoticon,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: blur,
      b02: motion,
      b00: backgroundColor != null,
      b04: secondBackgroundColor != null || rotation != null,
      b05: thirdBackgroundColor != null,
      b06: fourthBackgroundColor != null,
      b03: intensity != null,
      b07: emoticon != null,
    );

    return v;
  }

  /// blur: bit 1 of flags.1?true
  final bool blur;

  /// motion: bit 2 of flags.2?true
  final bool motion;

  /// Background Color.
  final int? backgroundColor;

  /// Second Background Color.
  final int? secondBackgroundColor;

  /// Third Background Color.
  final int? thirdBackgroundColor;

  /// Fourth Background Color.
  final int? fourthBackgroundColor;

  /// Intensity.
  final int? intensity;

  /// Rotation.
  final int? rotation;

  /// Emoticon.
  final String? emoticon;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x372efcd0);
    buffer.writeInt(flags);
    final localBackgroundColorCopy = backgroundColor;
    if (localBackgroundColorCopy != null) {
      buffer.writeInt(localBackgroundColorCopy);
    }
    final localSecondBackgroundColorCopy = secondBackgroundColor;
    if (localSecondBackgroundColorCopy != null) {
      buffer.writeInt(localSecondBackgroundColorCopy);
    }
    final localThirdBackgroundColorCopy = thirdBackgroundColor;
    if (localThirdBackgroundColorCopy != null) {
      buffer.writeInt(localThirdBackgroundColorCopy);
    }
    final localFourthBackgroundColorCopy = fourthBackgroundColor;
    if (localFourthBackgroundColorCopy != null) {
      buffer.writeInt(localFourthBackgroundColorCopy);
    }
    final localIntensityCopy = intensity;
    if (localIntensityCopy != null) {
      buffer.writeInt(localIntensityCopy);
    }
    final localRotationCopy = rotation;
    if (localRotationCopy != null) {
      buffer.writeInt(localRotationCopy);
    }
    final localEmoticonCopy = emoticon;
    if (localEmoticonCopy != null) {
      buffer.writeString(localEmoticonCopy);
    }
  }
}

/// Predicate Auto Download Settings.
abstract class AutoDownloadSettingsBase extends TlConstructor {
  /// Predicate Auto Download Settings constructor.
  const AutoDownloadSettingsBase._() : super._();
}

/// Auto Download Settings.
///
/// ID: `baa57628`.
class AutoDownloadSettings extends AutoDownloadSettingsBase {
  /// Auto Download Settings constructor.
  const AutoDownloadSettings({
    required this.disabled,
    required this.videoPreloadLarge,
    required this.audioPreloadNext,
    required this.phonecallsLessData,
    required this.storiesPreload,
    required this.photoSizeMax,
    required this.videoSizeMax,
    required this.fileSizeMax,
    required this.videoUploadMaxbitrate,
    required this.smallQueueActiveOperationsMax,
    required this.largeQueueActiveOperationsMax,
  }) : super._();

  /// Deserialize.
  factory AutoDownloadSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final disabled = _readflags.0?true(buffer);
    // final videoPreloadLarge = _readflags.1?true(buffer);
    // final audioPreloadNext = _readflags.2?true(buffer);
    // final phonecallsLessData = _readflags.3?true(buffer);
    // final storiesPreload = _readflags.4?true(buffer);
    // final photoSizeMax = _readint(buffer);
    // final videoSizeMax = _readlong(buffer);
    // final fileSizeMax = _readlong(buffer);
    // final videoUploadMaxbitrate = _readint(buffer);
    // final smallQueueActiveOperationsMax = _readint(buffer);
    // final largeQueueActiveOperationsMax = _readint(buffer);
    // final result = AutoDownloadSettings(flags: flags, disabled: disabled, videoPreloadLarge: videoPreloadLarge, audioPreloadNext: audioPreloadNext, phonecallsLessData: phonecallsLessData, storiesPreload: storiesPreload, photoSizeMax: photoSizeMax, videoSizeMax: videoSizeMax, fileSizeMax: fileSizeMax, videoUploadMaxbitrate: videoUploadMaxbitrate, smallQueueActiveOperationsMax: smallQueueActiveOperationsMax, largeQueueActiveOperationsMax: largeQueueActiveOperationsMax,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: disabled,
      b01: videoPreloadLarge,
      b02: audioPreloadNext,
      b03: phonecallsLessData,
      b04: storiesPreload,
    );

    return v;
  }

  /// disabled: bit 0 of flags.0?true
  final bool disabled;

  /// video_preload_large: bit 1 of flags.1?true
  final bool videoPreloadLarge;

  /// audio_preload_next: bit 2 of flags.2?true
  final bool audioPreloadNext;

  /// phonecalls_less_data: bit 3 of flags.3?true
  final bool phonecallsLessData;

  /// stories_preload: bit 4 of flags.4?true
  final bool storiesPreload;

  /// Photo Size Max.
  final int photoSizeMax;

  /// Video Size Max.
  final int videoSizeMax;

  /// File Size Max.
  final int fileSizeMax;

  /// Video Upload Maxbitrate.
  final int videoUploadMaxbitrate;

  /// Small Queue Active Operations Max.
  final int smallQueueActiveOperationsMax;

  /// Large Queue Active Operations Max.
  final int largeQueueActiveOperationsMax;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbaa57628);
    buffer.writeInt(flags);
    buffer.writeInt(photoSizeMax);
    buffer.writeLong(videoSizeMax);
    buffer.writeLong(fileSizeMax);
    buffer.writeInt(videoUploadMaxbitrate);
    buffer.writeInt(smallQueueActiveOperationsMax);
    buffer.writeInt(largeQueueActiveOperationsMax);
  }
}

/// Predicate Account Auto Download Settings.
abstract class AccountAutoDownloadSettingsBase extends TlConstructor {
  /// Predicate Account Auto Download Settings constructor.
  const AccountAutoDownloadSettingsBase._() : super._();
}

/// Account Auto Download Settings.
///
/// ID: `63cacf26`.
class AccountAutoDownloadSettings extends AccountAutoDownloadSettingsBase {
  /// Account Auto Download Settings constructor.
  const AccountAutoDownloadSettings({
    required this.low,
    required this.medium,
    required this.high,
  }) : super._();

  /// Deserialize.
  factory AccountAutoDownloadSettings.deserialize(Uint8List buffer) {
    // final low = _readAutoDownloadSettings(buffer);
    // final medium = _readAutoDownloadSettings(buffer);
    // final high = _readAutoDownloadSettings(buffer);
    // final result = AccountAutoDownloadSettings(low: low, medium: medium, high: high,);

    // return result;

    throw Exception();
  }

  /// Low.
  final AutoDownloadSettingsBase low;

  /// Medium.
  final AutoDownloadSettingsBase medium;

  /// High.
  final AutoDownloadSettingsBase high;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x63cacf26);
    buffer.writeObject(low);
    buffer.writeObject(medium);
    buffer.writeObject(high);
  }
}

/// Predicate Emoji Keyword.
abstract class EmojiKeywordBase extends TlConstructor {
  /// Predicate Emoji Keyword constructor.
  const EmojiKeywordBase._() : super._();
}

/// Emoji Keyword.
///
/// ID: `d5b3b9f9`.
class EmojiKeyword extends EmojiKeywordBase {
  /// Emoji Keyword constructor.
  const EmojiKeyword({
    required this.keyword,
    required this.emoticons,
  }) : super._();

  /// Deserialize.
  factory EmojiKeyword.deserialize(Uint8List buffer) {
    // final keyword = _readstring(buffer);
    // final emoticons = _readVector<string>(buffer);
    // final result = EmojiKeyword(keyword: keyword, emoticons: emoticons,);

    // return result;

    throw Exception();
  }

  /// Keyword.
  final String keyword;

  /// Emoticons.
  final List<String> emoticons;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd5b3b9f9);
    buffer.writeString(keyword);
    buffer.writeVectorString(emoticons);
  }
}

/// Emoji Keyword Deleted.
///
/// ID: `236df622`.
class EmojiKeywordDeleted extends EmojiKeywordBase {
  /// Emoji Keyword Deleted constructor.
  const EmojiKeywordDeleted({
    required this.keyword,
    required this.emoticons,
  }) : super._();

  /// Deserialize.
  factory EmojiKeywordDeleted.deserialize(Uint8List buffer) {
    // final keyword = _readstring(buffer);
    // final emoticons = _readVector<string>(buffer);
    // final result = EmojiKeywordDeleted(keyword: keyword, emoticons: emoticons,);

    // return result;

    throw Exception();
  }

  /// Keyword.
  final String keyword;

  /// Emoticons.
  final List<String> emoticons;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x236df622);
    buffer.writeString(keyword);
    buffer.writeVectorString(emoticons);
  }
}

/// Predicate Emoji Keywords Difference.
abstract class EmojiKeywordsDifferenceBase extends TlConstructor {
  /// Predicate Emoji Keywords Difference constructor.
  const EmojiKeywordsDifferenceBase._() : super._();
}

/// Emoji Keywords Difference.
///
/// ID: `5cc761bd`.
class EmojiKeywordsDifference extends EmojiKeywordsDifferenceBase {
  /// Emoji Keywords Difference constructor.
  const EmojiKeywordsDifference({
    required this.langCode,
    required this.fromVersion,
    required this.version,
    required this.keywords,
  }) : super._();

  /// Deserialize.
  factory EmojiKeywordsDifference.deserialize(Uint8List buffer) {
    // final langCode = _readstring(buffer);
    // final fromVersion = _readint(buffer);
    // final version = _readint(buffer);
    // final keywords = _readVector<EmojiKeyword>(buffer);
    // final result = EmojiKeywordsDifference(langCode: langCode, fromVersion: fromVersion, version: version, keywords: keywords,);

    // return result;

    throw Exception();
  }

  /// Lang Code.
  final String langCode;

  /// From Version.
  final int fromVersion;

  /// Version.
  final int version;

  /// Keywords.
  final List<EmojiKeywordBase> keywords;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5cc761bd);
    buffer.writeString(langCode);
    buffer.writeInt(fromVersion);
    buffer.writeInt(version);
    buffer.writeVectorObject(keywords);
  }
}

/// Predicate Emoji U R L.
abstract class EmojiURLBase extends TlConstructor {
  /// Predicate Emoji U R L constructor.
  const EmojiURLBase._() : super._();
}

/// Emoji U R L.
///
/// ID: `a575739d`.
class EmojiURL extends EmojiURLBase {
  /// Emoji U R L constructor.
  const EmojiURL({
    required this.url,
  }) : super._();

  /// Deserialize.
  factory EmojiURL.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final result = EmojiURL(url: url,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa575739d);
    buffer.writeString(url);
  }
}

/// Predicate Emoji Language.
abstract class EmojiLanguageBase extends TlConstructor {
  /// Predicate Emoji Language constructor.
  const EmojiLanguageBase._() : super._();
}

/// Emoji Language.
///
/// ID: `b3fb5361`.
class EmojiLanguage extends EmojiLanguageBase {
  /// Emoji Language constructor.
  const EmojiLanguage({
    required this.langCode,
  }) : super._();

  /// Deserialize.
  factory EmojiLanguage.deserialize(Uint8List buffer) {
    // final langCode = _readstring(buffer);
    // final result = EmojiLanguage(langCode: langCode,);

    // return result;

    throw Exception();
  }

  /// Lang Code.
  final String langCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb3fb5361);
    buffer.writeString(langCode);
  }
}

/// Predicate Folder.
abstract class FolderBase extends TlConstructor {
  /// Predicate Folder constructor.
  const FolderBase._() : super._();
}

/// Folder.
///
/// ID: `ff544e65`.
class Folder extends FolderBase {
  /// Folder constructor.
  const Folder({
    required this.autofillNewBroadcasts,
    required this.autofillPublicGroups,
    required this.autofillNewCorrespondents,
    required this.id,
    required this.title,
    this.photo,
  }) : super._();

  /// Deserialize.
  factory Folder.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final autofillNewBroadcasts = _readflags.0?true(buffer);
    // final autofillPublicGroups = _readflags.1?true(buffer);
    // final autofillNewCorrespondents = _readflags.2?true(buffer);
    // final id = _readint(buffer);
    // final title = _readstring(buffer);
    // final photo = _readflags.3?ChatPhoto(buffer);
    // final result = Folder(flags: flags, autofillNewBroadcasts: autofillNewBroadcasts, autofillPublicGroups: autofillPublicGroups, autofillNewCorrespondents: autofillNewCorrespondents, id: id, title: title, photo: photo,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: autofillNewBroadcasts,
      b01: autofillPublicGroups,
      b02: autofillNewCorrespondents,
      b03: photo != null,
    );

    return v;
  }

  /// autofill_new_broadcasts: bit 0 of flags.0?true
  final bool autofillNewBroadcasts;

  /// autofill_public_groups: bit 1 of flags.1?true
  final bool autofillPublicGroups;

  /// autofill_new_correspondents: bit 2 of flags.2?true
  final bool autofillNewCorrespondents;

  /// Id.
  final int id;

  /// Title.
  final String title;

  /// Photo.
  final ChatPhotoBase? photo;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xff544e65);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    buffer.writeString(title);
    final localPhotoCopy = photo;
    if (localPhotoCopy != null) {
      buffer.writeObject(localPhotoCopy);
    }
  }
}

/// Predicate Input Folder Peer.
abstract class InputFolderPeerBase extends TlConstructor {
  /// Predicate Input Folder Peer constructor.
  const InputFolderPeerBase._() : super._();
}

/// Input Folder Peer.
///
/// ID: `fbd2c296`.
class InputFolderPeer extends InputFolderPeerBase {
  /// Input Folder Peer constructor.
  const InputFolderPeer({
    required this.peer,
    required this.folderId,
  }) : super._();

  /// Deserialize.
  factory InputFolderPeer.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final folderId = _readint(buffer);
    // final result = InputFolderPeer(peer: peer, folderId: folderId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Folder Id.
  final int folderId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfbd2c296);
    buffer.writeObject(peer);
    buffer.writeInt(folderId);
  }
}

/// Predicate Folder Peer.
abstract class FolderPeerBase extends TlConstructor {
  /// Predicate Folder Peer constructor.
  const FolderPeerBase._() : super._();
}

/// Folder Peer.
///
/// ID: `e9baa668`.
class FolderPeer extends FolderPeerBase {
  /// Folder Peer constructor.
  const FolderPeer({
    required this.peer,
    required this.folderId,
  }) : super._();

  /// Deserialize.
  factory FolderPeer.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final folderId = _readint(buffer);
    // final result = FolderPeer(peer: peer, folderId: folderId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Folder Id.
  final int folderId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe9baa668);
    buffer.writeObject(peer);
    buffer.writeInt(folderId);
  }
}

/// Predicate Messages Search Counter.
abstract class MessagesSearchCounterBase extends TlConstructor {
  /// Predicate Messages Search Counter constructor.
  const MessagesSearchCounterBase._() : super._();
}

/// Messages Search Counter.
///
/// ID: `e844ebff`.
class MessagesSearchCounter extends MessagesSearchCounterBase {
  /// Messages Search Counter constructor.
  const MessagesSearchCounter({
    required this.inexact,
    required this.filter,
    required this.count,
  }) : super._();

  /// Deserialize.
  factory MessagesSearchCounter.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final inexact = _readflags.1?true(buffer);
    // final filter = _readMessagesFilter(buffer);
    // final count = _readint(buffer);
    // final result = MessagesSearchCounter(flags: flags, inexact: inexact, filter: filter, count: count,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: inexact,
    );

    return v;
  }

  /// inexact: bit 1 of flags.1?true
  final bool inexact;

  /// Filter.
  final MessagesFilterBase filter;

  /// Count.
  final int count;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe844ebff);
    buffer.writeInt(flags);
    buffer.writeObject(filter);
    buffer.writeInt(count);
  }
}

/// Predicate Url Auth Result.
abstract class UrlAuthResultBase extends TlConstructor {
  /// Predicate Url Auth Result constructor.
  const UrlAuthResultBase._() : super._();
}

/// Url Auth Result Request.
///
/// ID: `92d33a0e`.
class UrlAuthResultRequest extends UrlAuthResultBase {
  /// Url Auth Result Request constructor.
  const UrlAuthResultRequest({
    required this.requestWriteAccess,
    required this.bot,
    required this.domain,
  }) : super._();

  /// Deserialize.
  factory UrlAuthResultRequest.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final requestWriteAccess = _readflags.0?true(buffer);
    // final bot = _readUser(buffer);
    // final domain = _readstring(buffer);
    // final result = UrlAuthResultRequest(flags: flags, requestWriteAccess: requestWriteAccess, bot: bot, domain: domain,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: requestWriteAccess,
    );

    return v;
  }

  /// request_write_access: bit 0 of flags.0?true
  final bool requestWriteAccess;

  /// Bot.
  final UserBase bot;

  /// Domain.
  final String domain;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x92d33a0e);
    buffer.writeInt(flags);
    buffer.writeObject(bot);
    buffer.writeString(domain);
  }
}

/// Url Auth Result Accepted.
///
/// ID: `8f8c0e4e`.
class UrlAuthResultAccepted extends UrlAuthResultBase {
  /// Url Auth Result Accepted constructor.
  const UrlAuthResultAccepted({
    required this.url,
  }) : super._();

  /// Deserialize.
  factory UrlAuthResultAccepted.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final result = UrlAuthResultAccepted(url: url,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8f8c0e4e);
    buffer.writeString(url);
  }
}

/// Url Auth Result Default.
///
/// ID: `a9d6db1f`.
class UrlAuthResultDefault extends UrlAuthResultBase {
  /// Url Auth Result Default constructor.
  const UrlAuthResultDefault() : super._();

  /// Deserialize.
  factory UrlAuthResultDefault.deserialize(Uint8List buffer) {
    // final result = UrlAuthResultDefault();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa9d6db1f);
  }
}

/// Predicate Channel Location.
abstract class ChannelLocationBase extends TlConstructor {
  /// Predicate Channel Location constructor.
  const ChannelLocationBase._() : super._();
}

/// Channel Location Empty.
///
/// ID: `bfb5ad8b`.
class ChannelLocationEmpty extends ChannelLocationBase {
  /// Channel Location Empty constructor.
  const ChannelLocationEmpty() : super._();

  /// Deserialize.
  factory ChannelLocationEmpty.deserialize(Uint8List buffer) {
    // final result = ChannelLocationEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbfb5ad8b);
  }
}

/// Channel Location.
///
/// ID: `209b82db`.
class ChannelLocation extends ChannelLocationBase {
  /// Channel Location constructor.
  const ChannelLocation({
    required this.geoPoint,
    required this.address,
  }) : super._();

  /// Deserialize.
  factory ChannelLocation.deserialize(Uint8List buffer) {
    // final geoPoint = _readGeoPoint(buffer);
    // final address = _readstring(buffer);
    // final result = ChannelLocation(geoPoint: geoPoint, address: address,);

    // return result;

    throw Exception();
  }

  /// Geo Point.
  final GeoPointBase geoPoint;

  /// Address.
  final String address;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x209b82db);
    buffer.writeObject(geoPoint);
    buffer.writeString(address);
  }
}

/// Predicate Peer Located.
abstract class PeerLocatedBase extends TlConstructor {
  /// Predicate Peer Located constructor.
  const PeerLocatedBase._() : super._();
}

/// Peer Located.
///
/// ID: `ca461b5d`.
class PeerLocated extends PeerLocatedBase {
  /// Peer Located constructor.
  const PeerLocated({
    required this.peer,
    required this.expires,
    required this.distance,
  }) : super._();

  /// Deserialize.
  factory PeerLocated.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final expires = _readint(buffer);
    // final distance = _readint(buffer);
    // final result = PeerLocated(peer: peer, expires: expires, distance: distance,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Expires.
  final int expires;

  /// Distance.
  final int distance;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xca461b5d);
    buffer.writeObject(peer);
    buffer.writeInt(expires);
    buffer.writeInt(distance);
  }
}

/// Peer Self Located.
///
/// ID: `f8ec284b`.
class PeerSelfLocated extends PeerLocatedBase {
  /// Peer Self Located constructor.
  const PeerSelfLocated({
    required this.expires,
  }) : super._();

  /// Deserialize.
  factory PeerSelfLocated.deserialize(Uint8List buffer) {
    // final expires = _readint(buffer);
    // final result = PeerSelfLocated(expires: expires,);

    // return result;

    throw Exception();
  }

  /// Expires.
  final int expires;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf8ec284b);
    buffer.writeInt(expires);
  }
}

/// Predicate Restriction Reason.
abstract class RestrictionReasonBase extends TlConstructor {
  /// Predicate Restriction Reason constructor.
  const RestrictionReasonBase._() : super._();
}

/// Restriction Reason.
///
/// ID: `d072acb4`.
class RestrictionReason extends RestrictionReasonBase {
  /// Restriction Reason constructor.
  const RestrictionReason({
    required this.platform,
    required this.reason,
    required this.text,
  }) : super._();

  /// Deserialize.
  factory RestrictionReason.deserialize(Uint8List buffer) {
    // final platform = _readstring(buffer);
    // final reason = _readstring(buffer);
    // final text = _readstring(buffer);
    // final result = RestrictionReason(platform: platform, reason: reason, text: text,);

    // return result;

    throw Exception();
  }

  /// Platform.
  final String platform;

  /// Reason.
  final String reason;

  /// Text.
  final String text;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd072acb4);
    buffer.writeString(platform);
    buffer.writeString(reason);
    buffer.writeString(text);
  }
}

/// Predicate Input Theme.
abstract class InputThemeBase extends TlConstructor {
  /// Predicate Input Theme constructor.
  const InputThemeBase._() : super._();
}

/// Input Theme.
///
/// ID: `3c5693e9`.
class InputTheme extends InputThemeBase {
  /// Input Theme constructor.
  const InputTheme({
    required this.id,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputTheme.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputTheme(id: id, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3c5693e9);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
  }
}

/// Input Theme Slug.
///
/// ID: `f5890df1`.
class InputThemeSlug extends InputThemeBase {
  /// Input Theme Slug constructor.
  const InputThemeSlug({
    required this.slug,
  }) : super._();

  /// Deserialize.
  factory InputThemeSlug.deserialize(Uint8List buffer) {
    // final slug = _readstring(buffer);
    // final result = InputThemeSlug(slug: slug,);

    // return result;

    throw Exception();
  }

  /// Slug.
  final String slug;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf5890df1);
    buffer.writeString(slug);
  }
}

/// Predicate Theme.
abstract class ThemeBase extends TlConstructor {
  /// Predicate Theme constructor.
  const ThemeBase._() : super._();
}

/// Theme.
///
/// ID: `a00e67d6`.
class Theme extends ThemeBase {
  /// Theme constructor.
  const Theme({
    required this.creator,
    required this.ddefault,
    required this.forChat,
    required this.id,
    required this.accessHash,
    required this.slug,
    required this.title,
    this.document,
    this.settings,
    this.emoticon,
    this.installsCount,
  }) : super._();

  /// Deserialize.
  factory Theme.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final creator = _readflags.0?true(buffer);
    // final ddefault = _readflags.1?true(buffer);
    // final forChat = _readflags.5?true(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final slug = _readstring(buffer);
    // final title = _readstring(buffer);
    // final document = _readflags.2?Document(buffer);
    // final settings = _readflags.3?Vector<ThemeSettings>(buffer);
    // final emoticon = _readflags.6?string(buffer);
    // final installsCount = _readflags.4?int(buffer);
    // final result = Theme(flags: flags, creator: creator, ddefault: ddefault, forChat: forChat, id: id, accessHash: accessHash, slug: slug, title: title, document: document, settings: settings, emoticon: emoticon, installsCount: installsCount,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: creator,
      b01: ddefault,
      b05: forChat,
      b02: document != null,
      b03: settings != null,
      b06: emoticon != null,
      b04: installsCount != null,
    );

    return v;
  }

  /// creator: bit 0 of flags.0?true
  final bool creator;

  /// default: bit 1 of flags.1?true
  final bool ddefault;

  /// for_chat: bit 5 of flags.5?true
  final bool forChat;

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Slug.
  final String slug;

  /// Title.
  final String title;

  /// Document.
  final DocumentBase? document;

  /// Settings.
  final List<ThemeSettingsBase>? settings;

  /// Emoticon.
  final String? emoticon;

  /// Installs Count.
  final int? installsCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa00e67d6);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeString(slug);
    buffer.writeString(title);
    final localDocumentCopy = document;
    if (localDocumentCopy != null) {
      buffer.writeObject(localDocumentCopy);
    }
    final localSettingsCopy = settings;
    if (localSettingsCopy != null) {
      buffer.writeVectorObject(localSettingsCopy);
    }
    final localEmoticonCopy = emoticon;
    if (localEmoticonCopy != null) {
      buffer.writeString(localEmoticonCopy);
    }
    final localInstallsCountCopy = installsCount;
    if (localInstallsCountCopy != null) {
      buffer.writeInt(localInstallsCountCopy);
    }
  }
}

/// Predicate Account Themes.
abstract class AccountThemesBase extends TlConstructor {
  /// Predicate Account Themes constructor.
  const AccountThemesBase._() : super._();
}

/// Account Themes Not Modified.
///
/// ID: `f41eb622`.
class AccountThemesNotModified extends AccountThemesBase {
  /// Account Themes Not Modified constructor.
  const AccountThemesNotModified() : super._();

  /// Deserialize.
  factory AccountThemesNotModified.deserialize(Uint8List buffer) {
    // final result = AccountThemesNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf41eb622);
  }
}

/// Account Themes.
///
/// ID: `9a3d8c6d`.
class AccountThemes extends AccountThemesBase {
  /// Account Themes constructor.
  const AccountThemes({
    required this.hash,
    required this.themes,
  }) : super._();

  /// Deserialize.
  factory AccountThemes.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final themes = _readVector<Theme>(buffer);
    // final result = AccountThemes(hash: hash, themes: themes,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Themes.
  final List<ThemeBase> themes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9a3d8c6d);
    buffer.writeLong(hash);
    buffer.writeVectorObject(themes);
  }
}

/// Predicate Auth Login Token.
abstract class AuthLoginTokenBase extends TlConstructor {
  /// Predicate Auth Login Token constructor.
  const AuthLoginTokenBase._() : super._();
}

/// Auth Login Token.
///
/// ID: `629f1980`.
class AuthLoginToken extends AuthLoginTokenBase {
  /// Auth Login Token constructor.
  const AuthLoginToken({
    required this.expires,
    required this.token,
  }) : super._();

  /// Deserialize.
  factory AuthLoginToken.deserialize(Uint8List buffer) {
    // final expires = _readint(buffer);
    // final token = _readbytes(buffer);
    // final result = AuthLoginToken(expires: expires, token: token,);

    // return result;

    throw Exception();
  }

  /// Expires.
  final int expires;

  /// Token.
  final Uint8List token;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x629f1980);
    buffer.writeInt(expires);
    buffer.writeBytes(token);
  }
}

/// Auth Login Token Migrate To.
///
/// ID: `068e9916`.
class AuthLoginTokenMigrateTo extends AuthLoginTokenBase {
  /// Auth Login Token Migrate To constructor.
  const AuthLoginTokenMigrateTo({
    required this.dcId,
    required this.token,
  }) : super._();

  /// Deserialize.
  factory AuthLoginTokenMigrateTo.deserialize(Uint8List buffer) {
    // final dcId = _readint(buffer);
    // final token = _readbytes(buffer);
    // final result = AuthLoginTokenMigrateTo(dcId: dcId, token: token,);

    // return result;

    throw Exception();
  }

  /// Dc Id.
  final int dcId;

  /// Token.
  final Uint8List token;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x068e9916);
    buffer.writeInt(dcId);
    buffer.writeBytes(token);
  }
}

/// Auth Login Token Success.
///
/// ID: `390d5c5e`.
class AuthLoginTokenSuccess extends AuthLoginTokenBase {
  /// Auth Login Token Success constructor.
  const AuthLoginTokenSuccess({
    required this.authorization,
  }) : super._();

  /// Deserialize.
  factory AuthLoginTokenSuccess.deserialize(Uint8List buffer) {
    // final authorization = _readauth.Authorization(buffer);
    // final result = AuthLoginTokenSuccess(authorization: authorization,);

    // return result;

    throw Exception();
  }

  /// Authorization.
  final AuthAuthorizationBase authorization;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x390d5c5e);
    buffer.writeObject(authorization);
  }
}

/// Predicate Account Content Settings.
abstract class AccountContentSettingsBase extends TlConstructor {
  /// Predicate Account Content Settings constructor.
  const AccountContentSettingsBase._() : super._();
}

/// Account Content Settings.
///
/// ID: `57e28221`.
class AccountContentSettings extends AccountContentSettingsBase {
  /// Account Content Settings constructor.
  const AccountContentSettings({
    required this.sensitiveEnabled,
    required this.sensitiveCanChange,
  }) : super._();

  /// Deserialize.
  factory AccountContentSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final sensitiveEnabled = _readflags.0?true(buffer);
    // final sensitiveCanChange = _readflags.1?true(buffer);
    // final result = AccountContentSettings(flags: flags, sensitiveEnabled: sensitiveEnabled, sensitiveCanChange: sensitiveCanChange,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: sensitiveEnabled,
      b01: sensitiveCanChange,
    );

    return v;
  }

  /// sensitive_enabled: bit 0 of flags.0?true
  final bool sensitiveEnabled;

  /// sensitive_can_change: bit 1 of flags.1?true
  final bool sensitiveCanChange;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x57e28221);
    buffer.writeInt(flags);
  }
}

/// Predicate Messages Inactive Chats.
abstract class MessagesInactiveChatsBase extends TlConstructor {
  /// Predicate Messages Inactive Chats constructor.
  const MessagesInactiveChatsBase._() : super._();
}

/// Messages Inactive Chats.
///
/// ID: `a927fec5`.
class MessagesInactiveChats extends MessagesInactiveChatsBase {
  /// Messages Inactive Chats constructor.
  const MessagesInactiveChats({
    required this.dates,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesInactiveChats.deserialize(Uint8List buffer) {
    // final dates = _readVector<int>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesInactiveChats(dates: dates, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Dates.
  final List<int> dates;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa927fec5);
    buffer.writeVectorInt(dates);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Base Theme.
abstract class BaseThemeBase extends TlConstructor {
  /// Predicate Base Theme constructor.
  const BaseThemeBase._() : super._();
}

/// Base Theme Classic.
///
/// ID: `c3a12462`.
class BaseThemeClassic extends BaseThemeBase {
  /// Base Theme Classic constructor.
  const BaseThemeClassic() : super._();

  /// Deserialize.
  factory BaseThemeClassic.deserialize(Uint8List buffer) {
    // final result = BaseThemeClassic();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc3a12462);
  }
}

/// Base Theme Day.
///
/// ID: `fbd81688`.
class BaseThemeDay extends BaseThemeBase {
  /// Base Theme Day constructor.
  const BaseThemeDay() : super._();

  /// Deserialize.
  factory BaseThemeDay.deserialize(Uint8List buffer) {
    // final result = BaseThemeDay();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfbd81688);
  }
}

/// Base Theme Night.
///
/// ID: `b7b31ea8`.
class BaseThemeNight extends BaseThemeBase {
  /// Base Theme Night constructor.
  const BaseThemeNight() : super._();

  /// Deserialize.
  factory BaseThemeNight.deserialize(Uint8List buffer) {
    // final result = BaseThemeNight();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb7b31ea8);
  }
}

/// Base Theme Tinted.
///
/// ID: `6d5f77ee`.
class BaseThemeTinted extends BaseThemeBase {
  /// Base Theme Tinted constructor.
  const BaseThemeTinted() : super._();

  /// Deserialize.
  factory BaseThemeTinted.deserialize(Uint8List buffer) {
    // final result = BaseThemeTinted();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6d5f77ee);
  }
}

/// Base Theme Arctic.
///
/// ID: `5b11125a`.
class BaseThemeArctic extends BaseThemeBase {
  /// Base Theme Arctic constructor.
  const BaseThemeArctic() : super._();

  /// Deserialize.
  factory BaseThemeArctic.deserialize(Uint8List buffer) {
    // final result = BaseThemeArctic();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5b11125a);
  }
}

/// Predicate Input Theme Settings.
abstract class InputThemeSettingsBase extends TlConstructor {
  /// Predicate Input Theme Settings constructor.
  const InputThemeSettingsBase._() : super._();
}

/// Input Theme Settings.
///
/// ID: `8fde504f`.
class InputThemeSettings extends InputThemeSettingsBase {
  /// Input Theme Settings constructor.
  const InputThemeSettings({
    required this.messageColorsAnimated,
    required this.baseTheme,
    required this.accentColor,
    this.outboxAccentColor,
    this.messageColors,
    this.wallpaper,
    this.wallpaperSettings,
  }) : super._();

  /// Deserialize.
  factory InputThemeSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final messageColorsAnimated = _readflags.2?true(buffer);
    // final baseTheme = _readBaseTheme(buffer);
    // final accentColor = _readint(buffer);
    // final outboxAccentColor = _readflags.3?int(buffer);
    // final messageColors = _readflags.0?Vector<int>(buffer);
    // final wallpaper = _readflags.1?InputWallPaper(buffer);
    // final wallpaperSettings = _readflags.1?WallPaperSettings(buffer);
    // final result = InputThemeSettings(flags: flags, messageColorsAnimated: messageColorsAnimated, baseTheme: baseTheme, accentColor: accentColor, outboxAccentColor: outboxAccentColor, messageColors: messageColors, wallpaper: wallpaper, wallpaperSettings: wallpaperSettings,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: messageColorsAnimated,
      b03: outboxAccentColor != null,
      b00: messageColors != null,
      b01: wallpaper != null || wallpaperSettings != null,
    );

    return v;
  }

  /// message_colors_animated: bit 2 of flags.2?true
  final bool messageColorsAnimated;

  /// Base Theme.
  final BaseThemeBase baseTheme;

  /// Accent Color.
  final int accentColor;

  /// Outbox Accent Color.
  final int? outboxAccentColor;

  /// Message Colors.
  final List<int>? messageColors;

  /// Wallpaper.
  final InputWallPaperBase? wallpaper;

  /// Wallpaper Settings.
  final WallPaperSettingsBase? wallpaperSettings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8fde504f);
    buffer.writeInt(flags);
    buffer.writeObject(baseTheme);
    buffer.writeInt(accentColor);
    final localOutboxAccentColorCopy = outboxAccentColor;
    if (localOutboxAccentColorCopy != null) {
      buffer.writeInt(localOutboxAccentColorCopy);
    }
    final localMessageColorsCopy = messageColors;
    if (localMessageColorsCopy != null) {
      buffer.writeVectorInt(localMessageColorsCopy);
    }
    final localWallpaperCopy = wallpaper;
    if (localWallpaperCopy != null) {
      buffer.writeObject(localWallpaperCopy);
    }
    final localWallpaperSettingsCopy = wallpaperSettings;
    if (localWallpaperSettingsCopy != null) {
      buffer.writeObject(localWallpaperSettingsCopy);
    }
  }
}

/// Predicate Theme Settings.
abstract class ThemeSettingsBase extends TlConstructor {
  /// Predicate Theme Settings constructor.
  const ThemeSettingsBase._() : super._();
}

/// Theme Settings.
///
/// ID: `fa58b6d4`.
class ThemeSettings extends ThemeSettingsBase {
  /// Theme Settings constructor.
  const ThemeSettings({
    required this.messageColorsAnimated,
    required this.baseTheme,
    required this.accentColor,
    this.outboxAccentColor,
    this.messageColors,
    this.wallpaper,
  }) : super._();

  /// Deserialize.
  factory ThemeSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final messageColorsAnimated = _readflags.2?true(buffer);
    // final baseTheme = _readBaseTheme(buffer);
    // final accentColor = _readint(buffer);
    // final outboxAccentColor = _readflags.3?int(buffer);
    // final messageColors = _readflags.0?Vector<int>(buffer);
    // final wallpaper = _readflags.1?WallPaper(buffer);
    // final result = ThemeSettings(flags: flags, messageColorsAnimated: messageColorsAnimated, baseTheme: baseTheme, accentColor: accentColor, outboxAccentColor: outboxAccentColor, messageColors: messageColors, wallpaper: wallpaper,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: messageColorsAnimated,
      b03: outboxAccentColor != null,
      b00: messageColors != null,
      b01: wallpaper != null,
    );

    return v;
  }

  /// message_colors_animated: bit 2 of flags.2?true
  final bool messageColorsAnimated;

  /// Base Theme.
  final BaseThemeBase baseTheme;

  /// Accent Color.
  final int accentColor;

  /// Outbox Accent Color.
  final int? outboxAccentColor;

  /// Message Colors.
  final List<int>? messageColors;

  /// Wallpaper.
  final WallPaperBase? wallpaper;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfa58b6d4);
    buffer.writeInt(flags);
    buffer.writeObject(baseTheme);
    buffer.writeInt(accentColor);
    final localOutboxAccentColorCopy = outboxAccentColor;
    if (localOutboxAccentColorCopy != null) {
      buffer.writeInt(localOutboxAccentColorCopy);
    }
    final localMessageColorsCopy = messageColors;
    if (localMessageColorsCopy != null) {
      buffer.writeVectorInt(localMessageColorsCopy);
    }
    final localWallpaperCopy = wallpaper;
    if (localWallpaperCopy != null) {
      buffer.writeObject(localWallpaperCopy);
    }
  }
}

/// Predicate Web Page Attribute.
abstract class WebPageAttributeBase extends TlConstructor {
  /// Predicate Web Page Attribute constructor.
  const WebPageAttributeBase._() : super._();
}

/// Web Page Attribute Theme.
///
/// ID: `54b56617`.
class WebPageAttributeTheme extends WebPageAttributeBase {
  /// Web Page Attribute Theme constructor.
  const WebPageAttributeTheme({
    this.documents,
    this.settings,
  }) : super._();

  /// Deserialize.
  factory WebPageAttributeTheme.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final documents = _readflags.0?Vector<Document>(buffer);
    // final settings = _readflags.1?ThemeSettings(buffer);
    // final result = WebPageAttributeTheme(flags: flags, documents: documents, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: documents != null,
      b01: settings != null,
    );

    return v;
  }

  /// Documents.
  final List<DocumentBase>? documents;

  /// Settings.
  final ThemeSettingsBase? settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x54b56617);
    buffer.writeInt(flags);
    final localDocumentsCopy = documents;
    if (localDocumentsCopy != null) {
      buffer.writeVectorObject(localDocumentsCopy);
    }
    final localSettingsCopy = settings;
    if (localSettingsCopy != null) {
      buffer.writeObject(localSettingsCopy);
    }
  }
}

/// Web Page Attribute Story.
///
/// ID: `2e94c3e7`.
class WebPageAttributeStory extends WebPageAttributeBase {
  /// Web Page Attribute Story constructor.
  const WebPageAttributeStory({
    required this.peer,
    required this.id,
    this.story,
  }) : super._();

  /// Deserialize.
  factory WebPageAttributeStory.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readPeer(buffer);
    // final id = _readint(buffer);
    // final story = _readflags.0?StoryItem(buffer);
    // final result = WebPageAttributeStory(flags: flags, peer: peer, id: id, story: story,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: story != null,
    );

    return v;
  }

  /// Peer.
  final PeerBase peer;

  /// Id.
  final int id;

  /// Story.
  final StoryItemBase? story;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2e94c3e7);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(id);
    final localStoryCopy = story;
    if (localStoryCopy != null) {
      buffer.writeObject(localStoryCopy);
    }
  }
}

/// Predicate Messages Votes List.
abstract class MessagesVotesListBase extends TlConstructor {
  /// Predicate Messages Votes List constructor.
  const MessagesVotesListBase._() : super._();
}

/// Messages Votes List.
///
/// ID: `4899484e`.
class MessagesVotesList extends MessagesVotesListBase {
  /// Messages Votes List constructor.
  const MessagesVotesList({
    required this.count,
    required this.votes,
    required this.chats,
    required this.users,
    this.nextOffset,
  }) : super._();

  /// Deserialize.
  factory MessagesVotesList.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final count = _readint(buffer);
    // final votes = _readVector<MessagePeerVote>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final nextOffset = _readflags.0?string(buffer);
    // final result = MessagesVotesList(flags: flags, count: count, votes: votes, chats: chats, users: users, nextOffset: nextOffset,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: nextOffset != null,
    );

    return v;
  }

  /// Count.
  final int count;

  /// Votes.
  final List<MessagePeerVoteBase> votes;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Next Offset.
  final String? nextOffset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4899484e);
    buffer.writeInt(flags);
    buffer.writeInt(count);
    buffer.writeVectorObject(votes);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
    final localNextOffsetCopy = nextOffset;
    if (localNextOffsetCopy != null) {
      buffer.writeString(localNextOffsetCopy);
    }
  }
}

/// Predicate Bank Card Open Url.
abstract class BankCardOpenUrlBase extends TlConstructor {
  /// Predicate Bank Card Open Url constructor.
  const BankCardOpenUrlBase._() : super._();
}

/// Bank Card Open Url.
///
/// ID: `f568028a`.
class BankCardOpenUrl extends BankCardOpenUrlBase {
  /// Bank Card Open Url constructor.
  const BankCardOpenUrl({
    required this.url,
    required this.name,
  }) : super._();

  /// Deserialize.
  factory BankCardOpenUrl.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final name = _readstring(buffer);
    // final result = BankCardOpenUrl(url: url, name: name,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Name.
  final String name;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf568028a);
    buffer.writeString(url);
    buffer.writeString(name);
  }
}

/// Predicate Payments Bank Card Data.
abstract class PaymentsBankCardDataBase extends TlConstructor {
  /// Predicate Payments Bank Card Data constructor.
  const PaymentsBankCardDataBase._() : super._();
}

/// Payments Bank Card Data.
///
/// ID: `3e24e573`.
class PaymentsBankCardData extends PaymentsBankCardDataBase {
  /// Payments Bank Card Data constructor.
  const PaymentsBankCardData({
    required this.title,
    required this.openUrls,
  }) : super._();

  /// Deserialize.
  factory PaymentsBankCardData.deserialize(Uint8List buffer) {
    // final title = _readstring(buffer);
    // final openUrls = _readVector<BankCardOpenUrl>(buffer);
    // final result = PaymentsBankCardData(title: title, openUrls: openUrls,);

    // return result;

    throw Exception();
  }

  /// Title.
  final String title;

  /// Open Urls.
  final List<BankCardOpenUrlBase> openUrls;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3e24e573);
    buffer.writeString(title);
    buffer.writeVectorObject(openUrls);
  }
}

/// Predicate Dialog Filter.
abstract class DialogFilterBase extends TlConstructor {
  /// Predicate Dialog Filter constructor.
  const DialogFilterBase._() : super._();
}

/// Dialog Filter.
///
/// ID: `7438f7e8`.
class DialogFilter extends DialogFilterBase {
  /// Dialog Filter constructor.
  const DialogFilter({
    required this.contacts,
    required this.nonContacts,
    required this.groups,
    required this.broadcasts,
    required this.bots,
    required this.excludeMuted,
    required this.excludeRead,
    required this.excludeArchived,
    required this.id,
    required this.title,
    this.emoticon,
    required this.pinnedPeers,
    required this.includePeers,
    required this.excludePeers,
  }) : super._();

  /// Deserialize.
  factory DialogFilter.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final contacts = _readflags.0?true(buffer);
    // final nonContacts = _readflags.1?true(buffer);
    // final groups = _readflags.2?true(buffer);
    // final broadcasts = _readflags.3?true(buffer);
    // final bots = _readflags.4?true(buffer);
    // final excludeMuted = _readflags.11?true(buffer);
    // final excludeRead = _readflags.12?true(buffer);
    // final excludeArchived = _readflags.13?true(buffer);
    // final id = _readint(buffer);
    // final title = _readstring(buffer);
    // final emoticon = _readflags.25?string(buffer);
    // final pinnedPeers = _readVector<InputPeer>(buffer);
    // final includePeers = _readVector<InputPeer>(buffer);
    // final excludePeers = _readVector<InputPeer>(buffer);
    // final result = DialogFilter(flags: flags, contacts: contacts, nonContacts: nonContacts, groups: groups, broadcasts: broadcasts, bots: bots, excludeMuted: excludeMuted, excludeRead: excludeRead, excludeArchived: excludeArchived, id: id, title: title, emoticon: emoticon, pinnedPeers: pinnedPeers, includePeers: includePeers, excludePeers: excludePeers,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: contacts,
      b01: nonContacts,
      b02: groups,
      b03: broadcasts,
      b04: bots,
      b11: excludeMuted,
      b12: excludeRead,
      b13: excludeArchived,
      b25: emoticon != null,
    );

    return v;
  }

  /// contacts: bit 0 of flags.0?true
  final bool contacts;

  /// non_contacts: bit 1 of flags.1?true
  final bool nonContacts;

  /// groups: bit 2 of flags.2?true
  final bool groups;

  /// broadcasts: bit 3 of flags.3?true
  final bool broadcasts;

  /// bots: bit 4 of flags.4?true
  final bool bots;

  /// exclude_muted: bit 11 of flags.11?true
  final bool excludeMuted;

  /// exclude_read: bit 12 of flags.12?true
  final bool excludeRead;

  /// exclude_archived: bit 13 of flags.13?true
  final bool excludeArchived;

  /// Id.
  final int id;

  /// Title.
  final String title;

  /// Emoticon.
  final String? emoticon;

  /// Pinned Peers.
  final List<InputPeerBase> pinnedPeers;

  /// Include Peers.
  final List<InputPeerBase> includePeers;

  /// Exclude Peers.
  final List<InputPeerBase> excludePeers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7438f7e8);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    buffer.writeString(title);
    final localEmoticonCopy = emoticon;
    if (localEmoticonCopy != null) {
      buffer.writeString(localEmoticonCopy);
    }
    buffer.writeVectorObject(pinnedPeers);
    buffer.writeVectorObject(includePeers);
    buffer.writeVectorObject(excludePeers);
  }
}

/// Dialog Filter Default.
///
/// ID: `363293ae`.
class DialogFilterDefault extends DialogFilterBase {
  /// Dialog Filter Default constructor.
  const DialogFilterDefault() : super._();

  /// Deserialize.
  factory DialogFilterDefault.deserialize(Uint8List buffer) {
    // final result = DialogFilterDefault();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x363293ae);
  }
}

/// Dialog Filter Chatlist.
///
/// ID: `d64a04a8`.
class DialogFilterChatlist extends DialogFilterBase {
  /// Dialog Filter Chatlist constructor.
  const DialogFilterChatlist({
    required this.hasMyInvites,
    required this.id,
    required this.title,
    this.emoticon,
    required this.pinnedPeers,
    required this.includePeers,
  }) : super._();

  /// Deserialize.
  factory DialogFilterChatlist.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final hasMyInvites = _readflags.26?true(buffer);
    // final id = _readint(buffer);
    // final title = _readstring(buffer);
    // final emoticon = _readflags.25?string(buffer);
    // final pinnedPeers = _readVector<InputPeer>(buffer);
    // final includePeers = _readVector<InputPeer>(buffer);
    // final result = DialogFilterChatlist(flags: flags, hasMyInvites: hasMyInvites, id: id, title: title, emoticon: emoticon, pinnedPeers: pinnedPeers, includePeers: includePeers,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b26: hasMyInvites,
      b25: emoticon != null,
    );

    return v;
  }

  /// has_my_invites: bit 26 of flags.26?true
  final bool hasMyInvites;

  /// Id.
  final int id;

  /// Title.
  final String title;

  /// Emoticon.
  final String? emoticon;

  /// Pinned Peers.
  final List<InputPeerBase> pinnedPeers;

  /// Include Peers.
  final List<InputPeerBase> includePeers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd64a04a8);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    buffer.writeString(title);
    final localEmoticonCopy = emoticon;
    if (localEmoticonCopy != null) {
      buffer.writeString(localEmoticonCopy);
    }
    buffer.writeVectorObject(pinnedPeers);
    buffer.writeVectorObject(includePeers);
  }
}

/// Predicate Dialog Filter Suggested.
abstract class DialogFilterSuggestedBase extends TlConstructor {
  /// Predicate Dialog Filter Suggested constructor.
  const DialogFilterSuggestedBase._() : super._();
}

/// Dialog Filter Suggested.
///
/// ID: `77744d4a`.
class DialogFilterSuggested extends DialogFilterSuggestedBase {
  /// Dialog Filter Suggested constructor.
  const DialogFilterSuggested({
    required this.filter,
    required this.description,
  }) : super._();

  /// Deserialize.
  factory DialogFilterSuggested.deserialize(Uint8List buffer) {
    // final filter = _readDialogFilter(buffer);
    // final description = _readstring(buffer);
    // final result = DialogFilterSuggested(filter: filter, description: description,);

    // return result;

    throw Exception();
  }

  /// Filter.
  final DialogFilterBase filter;

  /// Description.
  final String description;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x77744d4a);
    buffer.writeObject(filter);
    buffer.writeString(description);
  }
}

/// Predicate Stats Date Range Days.
abstract class StatsDateRangeDaysBase extends TlConstructor {
  /// Predicate Stats Date Range Days constructor.
  const StatsDateRangeDaysBase._() : super._();
}

/// Stats Date Range Days.
///
/// ID: `b637edaf`.
class StatsDateRangeDays extends StatsDateRangeDaysBase {
  /// Stats Date Range Days constructor.
  const StatsDateRangeDays({
    required this.minDate,
    required this.maxDate,
  }) : super._();

  /// Deserialize.
  factory StatsDateRangeDays.deserialize(Uint8List buffer) {
    // final minDate = _readint(buffer);
    // final maxDate = _readint(buffer);
    // final result = StatsDateRangeDays(minDate: minDate, maxDate: maxDate,);

    // return result;

    throw Exception();
  }

  /// Min Date.
  final DateTime minDate;

  /// Max Date.
  final DateTime maxDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb637edaf);
    buffer.writeDateTime(minDate);
    buffer.writeDateTime(maxDate);
  }
}

/// Predicate Stats Abs Value And Prev.
abstract class StatsAbsValueAndPrevBase extends TlConstructor {
  /// Predicate Stats Abs Value And Prev constructor.
  const StatsAbsValueAndPrevBase._() : super._();
}

/// Stats Abs Value And Prev.
///
/// ID: `cb43acde`.
class StatsAbsValueAndPrev extends StatsAbsValueAndPrevBase {
  /// Stats Abs Value And Prev constructor.
  const StatsAbsValueAndPrev({
    required this.current,
    required this.previous,
  }) : super._();

  /// Deserialize.
  factory StatsAbsValueAndPrev.deserialize(Uint8List buffer) {
    // final current = _readdouble(buffer);
    // final previous = _readdouble(buffer);
    // final result = StatsAbsValueAndPrev(current: current, previous: previous,);

    // return result;

    throw Exception();
  }

  /// Current.
  final double current;

  /// Previous.
  final double previous;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcb43acde);
    buffer.writeDouble(current);
    buffer.writeDouble(previous);
  }
}

/// Predicate Stats Percent Value.
abstract class StatsPercentValueBase extends TlConstructor {
  /// Predicate Stats Percent Value constructor.
  const StatsPercentValueBase._() : super._();
}

/// Stats Percent Value.
///
/// ID: `cbce2fe0`.
class StatsPercentValue extends StatsPercentValueBase {
  /// Stats Percent Value constructor.
  const StatsPercentValue({
    required this.part,
    required this.total,
  }) : super._();

  /// Deserialize.
  factory StatsPercentValue.deserialize(Uint8List buffer) {
    // final part = _readdouble(buffer);
    // final total = _readdouble(buffer);
    // final result = StatsPercentValue(part: part, total: total,);

    // return result;

    throw Exception();
  }

  /// Part.
  final double part;

  /// Total.
  final double total;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcbce2fe0);
    buffer.writeDouble(part);
    buffer.writeDouble(total);
  }
}

/// Predicate Stats Graph.
abstract class StatsGraphBase extends TlConstructor {
  /// Predicate Stats Graph constructor.
  const StatsGraphBase._() : super._();
}

/// Stats Graph Async.
///
/// ID: `4a27eb2d`.
class StatsGraphAsync extends StatsGraphBase {
  /// Stats Graph Async constructor.
  const StatsGraphAsync({
    required this.token,
  }) : super._();

  /// Deserialize.
  factory StatsGraphAsync.deserialize(Uint8List buffer) {
    // final token = _readstring(buffer);
    // final result = StatsGraphAsync(token: token,);

    // return result;

    throw Exception();
  }

  /// Token.
  final String token;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4a27eb2d);
    buffer.writeString(token);
  }
}

/// Stats Graph Error.
///
/// ID: `bedc9822`.
class StatsGraphError extends StatsGraphBase {
  /// Stats Graph Error constructor.
  const StatsGraphError({
    required this.error,
  }) : super._();

  /// Deserialize.
  factory StatsGraphError.deserialize(Uint8List buffer) {
    // final error = _readstring(buffer);
    // final result = StatsGraphError(error: error,);

    // return result;

    throw Exception();
  }

  /// Error.
  final String error;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbedc9822);
    buffer.writeString(error);
  }
}

/// Stats Graph.
///
/// ID: `8ea464b6`.
class StatsGraph extends StatsGraphBase {
  /// Stats Graph constructor.
  const StatsGraph({
    required this.json,
    this.zoomToken,
  }) : super._();

  /// Deserialize.
  factory StatsGraph.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final json = _readDataJSON(buffer);
    // final zoomToken = _readflags.0?string(buffer);
    // final result = StatsGraph(flags: flags, json: json, zoomToken: zoomToken,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: zoomToken != null,
    );

    return v;
  }

  /// Json.
  final DataJSONBase json;

  /// Zoom Token.
  final String? zoomToken;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8ea464b6);
    buffer.writeInt(flags);
    buffer.writeObject(json);
    final localZoomTokenCopy = zoomToken;
    if (localZoomTokenCopy != null) {
      buffer.writeString(localZoomTokenCopy);
    }
  }
}

/// Predicate Stats Broadcast Stats.
abstract class StatsBroadcastStatsBase extends TlConstructor {
  /// Predicate Stats Broadcast Stats constructor.
  const StatsBroadcastStatsBase._() : super._();
}

/// Stats Broadcast Stats.
///
/// ID: `396ca5fc`.
class StatsBroadcastStats extends StatsBroadcastStatsBase {
  /// Stats Broadcast Stats constructor.
  const StatsBroadcastStats({
    required this.period,
    required this.followers,
    required this.viewsPerPost,
    required this.sharesPerPost,
    required this.reactionsPerPost,
    required this.viewsPerStory,
    required this.sharesPerStory,
    required this.reactionsPerStory,
    required this.enabledNotifications,
    required this.growthGraph,
    required this.followersGraph,
    required this.muteGraph,
    required this.topHoursGraph,
    required this.interactionsGraph,
    required this.ivInteractionsGraph,
    required this.viewsBySourceGraph,
    required this.newFollowersBySourceGraph,
    required this.languagesGraph,
    required this.reactionsByEmotionGraph,
    required this.storyInteractionsGraph,
    required this.storyReactionsByEmotionGraph,
    required this.recentPostsInteractions,
  }) : super._();

  /// Deserialize.
  factory StatsBroadcastStats.deserialize(Uint8List buffer) {
    // final period = _readStatsDateRangeDays(buffer);
    // final followers = _readStatsAbsValueAndPrev(buffer);
    // final viewsPerPost = _readStatsAbsValueAndPrev(buffer);
    // final sharesPerPost = _readStatsAbsValueAndPrev(buffer);
    // final reactionsPerPost = _readStatsAbsValueAndPrev(buffer);
    // final viewsPerStory = _readStatsAbsValueAndPrev(buffer);
    // final sharesPerStory = _readStatsAbsValueAndPrev(buffer);
    // final reactionsPerStory = _readStatsAbsValueAndPrev(buffer);
    // final enabledNotifications = _readStatsPercentValue(buffer);
    // final growthGraph = _readStatsGraph(buffer);
    // final followersGraph = _readStatsGraph(buffer);
    // final muteGraph = _readStatsGraph(buffer);
    // final topHoursGraph = _readStatsGraph(buffer);
    // final interactionsGraph = _readStatsGraph(buffer);
    // final ivInteractionsGraph = _readStatsGraph(buffer);
    // final viewsBySourceGraph = _readStatsGraph(buffer);
    // final newFollowersBySourceGraph = _readStatsGraph(buffer);
    // final languagesGraph = _readStatsGraph(buffer);
    // final reactionsByEmotionGraph = _readStatsGraph(buffer);
    // final storyInteractionsGraph = _readStatsGraph(buffer);
    // final storyReactionsByEmotionGraph = _readStatsGraph(buffer);
    // final recentPostsInteractions = _readVector<PostInteractionCounters>(buffer);
    // final result = StatsBroadcastStats(period: period, followers: followers, viewsPerPost: viewsPerPost, sharesPerPost: sharesPerPost, reactionsPerPost: reactionsPerPost, viewsPerStory: viewsPerStory, sharesPerStory: sharesPerStory, reactionsPerStory: reactionsPerStory, enabledNotifications: enabledNotifications, growthGraph: growthGraph, followersGraph: followersGraph, muteGraph: muteGraph, topHoursGraph: topHoursGraph, interactionsGraph: interactionsGraph, ivInteractionsGraph: ivInteractionsGraph, viewsBySourceGraph: viewsBySourceGraph, newFollowersBySourceGraph: newFollowersBySourceGraph, languagesGraph: languagesGraph, reactionsByEmotionGraph: reactionsByEmotionGraph, storyInteractionsGraph: storyInteractionsGraph, storyReactionsByEmotionGraph: storyReactionsByEmotionGraph, recentPostsInteractions: recentPostsInteractions,);

    // return result;

    throw Exception();
  }

  /// Period.
  final StatsDateRangeDaysBase period;

  /// Followers.
  final StatsAbsValueAndPrevBase followers;

  /// Views Per Post.
  final StatsAbsValueAndPrevBase viewsPerPost;

  /// Shares Per Post.
  final StatsAbsValueAndPrevBase sharesPerPost;

  /// Reactions Per Post.
  final StatsAbsValueAndPrevBase reactionsPerPost;

  /// Views Per Story.
  final StatsAbsValueAndPrevBase viewsPerStory;

  /// Shares Per Story.
  final StatsAbsValueAndPrevBase sharesPerStory;

  /// Reactions Per Story.
  final StatsAbsValueAndPrevBase reactionsPerStory;

  /// Enabled Notifications.
  final StatsPercentValueBase enabledNotifications;

  /// Growth Graph.
  final StatsGraphBase growthGraph;

  /// Followers Graph.
  final StatsGraphBase followersGraph;

  /// Mute Graph.
  final StatsGraphBase muteGraph;

  /// Top Hours Graph.
  final StatsGraphBase topHoursGraph;

  /// Interactions Graph.
  final StatsGraphBase interactionsGraph;

  /// Iv Interactions Graph.
  final StatsGraphBase ivInteractionsGraph;

  /// Views By Source Graph.
  final StatsGraphBase viewsBySourceGraph;

  /// New Followers By Source Graph.
  final StatsGraphBase newFollowersBySourceGraph;

  /// Languages Graph.
  final StatsGraphBase languagesGraph;

  /// Reactions By Emotion Graph.
  final StatsGraphBase reactionsByEmotionGraph;

  /// Story Interactions Graph.
  final StatsGraphBase storyInteractionsGraph;

  /// Story Reactions By Emotion Graph.
  final StatsGraphBase storyReactionsByEmotionGraph;

  /// Recent Posts Interactions.
  final List<PostInteractionCountersBase> recentPostsInteractions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x396ca5fc);
    buffer.writeObject(period);
    buffer.writeObject(followers);
    buffer.writeObject(viewsPerPost);
    buffer.writeObject(sharesPerPost);
    buffer.writeObject(reactionsPerPost);
    buffer.writeObject(viewsPerStory);
    buffer.writeObject(sharesPerStory);
    buffer.writeObject(reactionsPerStory);
    buffer.writeObject(enabledNotifications);
    buffer.writeObject(growthGraph);
    buffer.writeObject(followersGraph);
    buffer.writeObject(muteGraph);
    buffer.writeObject(topHoursGraph);
    buffer.writeObject(interactionsGraph);
    buffer.writeObject(ivInteractionsGraph);
    buffer.writeObject(viewsBySourceGraph);
    buffer.writeObject(newFollowersBySourceGraph);
    buffer.writeObject(languagesGraph);
    buffer.writeObject(reactionsByEmotionGraph);
    buffer.writeObject(storyInteractionsGraph);
    buffer.writeObject(storyReactionsByEmotionGraph);
    buffer.writeVectorObject(recentPostsInteractions);
  }
}

/// Predicate Help Promo Data.
abstract class HelpPromoDataBase extends TlConstructor {
  /// Predicate Help Promo Data constructor.
  const HelpPromoDataBase._() : super._();
}

/// Help Promo Data Empty.
///
/// ID: `98f6ac75`.
class HelpPromoDataEmpty extends HelpPromoDataBase {
  /// Help Promo Data Empty constructor.
  const HelpPromoDataEmpty({
    required this.expires,
  }) : super._();

  /// Deserialize.
  factory HelpPromoDataEmpty.deserialize(Uint8List buffer) {
    // final expires = _readint(buffer);
    // final result = HelpPromoDataEmpty(expires: expires,);

    // return result;

    throw Exception();
  }

  /// Expires.
  final int expires;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x98f6ac75);
    buffer.writeInt(expires);
  }
}

/// Help Promo Data.
///
/// ID: `8c39793f`.
class HelpPromoData extends HelpPromoDataBase {
  /// Help Promo Data constructor.
  const HelpPromoData({
    required this.proxy,
    required this.expires,
    required this.peer,
    required this.chats,
    required this.users,
    this.psaType,
    this.psaMessage,
  }) : super._();

  /// Deserialize.
  factory HelpPromoData.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final proxy = _readflags.0?true(buffer);
    // final expires = _readint(buffer);
    // final peer = _readPeer(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final psaType = _readflags.1?string(buffer);
    // final psaMessage = _readflags.2?string(buffer);
    // final result = HelpPromoData(flags: flags, proxy: proxy, expires: expires, peer: peer, chats: chats, users: users, psaType: psaType, psaMessage: psaMessage,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: proxy,
      b01: psaType != null,
      b02: psaMessage != null,
    );

    return v;
  }

  /// proxy: bit 0 of flags.0?true
  final bool proxy;

  /// Expires.
  final int expires;

  /// Peer.
  final PeerBase peer;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Psa Type.
  final String? psaType;

  /// Psa Message.
  final String? psaMessage;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8c39793f);
    buffer.writeInt(flags);
    buffer.writeInt(expires);
    buffer.writeObject(peer);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
    final localPsaTypeCopy = psaType;
    if (localPsaTypeCopy != null) {
      buffer.writeString(localPsaTypeCopy);
    }
    final localPsaMessageCopy = psaMessage;
    if (localPsaMessageCopy != null) {
      buffer.writeString(localPsaMessageCopy);
    }
  }
}

/// Predicate Video Size.
abstract class VideoSizeBase extends TlConstructor {
  /// Predicate Video Size constructor.
  const VideoSizeBase._() : super._();
}

/// Video Size.
///
/// ID: `de33b094`.
class VideoSize extends VideoSizeBase {
  /// Video Size constructor.
  const VideoSize({
    required this.type,
    required this.w,
    required this.h,
    required this.size,
    this.videoStartTs,
  }) : super._();

  /// Deserialize.
  factory VideoSize.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final type = _readstring(buffer);
    // final w = _readint(buffer);
    // final h = _readint(buffer);
    // final size = _readint(buffer);
    // final videoStartTs = _readflags.0?double(buffer);
    // final result = VideoSize(flags: flags, type: type, w: w, h: h, size: size, videoStartTs: videoStartTs,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: videoStartTs != null,
    );

    return v;
  }

  /// Type.
  final String type;

  /// W.
  final int w;

  /// H.
  final int h;

  /// Size.
  final int size;

  /// Video Start Ts.
  final double? videoStartTs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xde33b094);
    buffer.writeInt(flags);
    buffer.writeString(type);
    buffer.writeInt(w);
    buffer.writeInt(h);
    buffer.writeInt(size);
    final localVideoStartTsCopy = videoStartTs;
    if (localVideoStartTsCopy != null) {
      buffer.writeDouble(localVideoStartTsCopy);
    }
  }
}

/// Video Size Emoji Markup.
///
/// ID: `f85c413c`.
class VideoSizeEmojiMarkup extends VideoSizeBase {
  /// Video Size Emoji Markup constructor.
  const VideoSizeEmojiMarkup({
    required this.emojiId,
    required this.backgroundColors,
  }) : super._();

  /// Deserialize.
  factory VideoSizeEmojiMarkup.deserialize(Uint8List buffer) {
    // final emojiId = _readlong(buffer);
    // final backgroundColors = _readVector<int>(buffer);
    // final result = VideoSizeEmojiMarkup(emojiId: emojiId, backgroundColors: backgroundColors,);

    // return result;

    throw Exception();
  }

  /// Emoji Id.
  final int emojiId;

  /// Background Colors.
  final List<int> backgroundColors;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf85c413c);
    buffer.writeLong(emojiId);
    buffer.writeVectorInt(backgroundColors);
  }
}

/// Video Size Sticker Markup.
///
/// ID: `0da082fe`.
class VideoSizeStickerMarkup extends VideoSizeBase {
  /// Video Size Sticker Markup constructor.
  const VideoSizeStickerMarkup({
    required this.stickerset,
    required this.stickerId,
    required this.backgroundColors,
  }) : super._();

  /// Deserialize.
  factory VideoSizeStickerMarkup.deserialize(Uint8List buffer) {
    // final stickerset = _readInputStickerSet(buffer);
    // final stickerId = _readlong(buffer);
    // final backgroundColors = _readVector<int>(buffer);
    // final result = VideoSizeStickerMarkup(stickerset: stickerset, stickerId: stickerId, backgroundColors: backgroundColors,);

    // return result;

    throw Exception();
  }

  /// Stickerset.
  final InputStickerSetBase stickerset;

  /// Sticker Id.
  final int stickerId;

  /// Background Colors.
  final List<int> backgroundColors;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0da082fe);
    buffer.writeObject(stickerset);
    buffer.writeLong(stickerId);
    buffer.writeVectorInt(backgroundColors);
  }
}

/// Predicate Stats Group Top Poster.
abstract class StatsGroupTopPosterBase extends TlConstructor {
  /// Predicate Stats Group Top Poster constructor.
  const StatsGroupTopPosterBase._() : super._();
}

/// Stats Group Top Poster.
///
/// ID: `9d04af9b`.
class StatsGroupTopPoster extends StatsGroupTopPosterBase {
  /// Stats Group Top Poster constructor.
  const StatsGroupTopPoster({
    required this.userId,
    required this.messages,
    required this.avgChars,
  }) : super._();

  /// Deserialize.
  factory StatsGroupTopPoster.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final messages = _readint(buffer);
    // final avgChars = _readint(buffer);
    // final result = StatsGroupTopPoster(userId: userId, messages: messages, avgChars: avgChars,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Messages.
  final int messages;

  /// Avg Chars.
  final int avgChars;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9d04af9b);
    buffer.writeLong(userId);
    buffer.writeInt(messages);
    buffer.writeInt(avgChars);
  }
}

/// Predicate Stats Group Top Admin.
abstract class StatsGroupTopAdminBase extends TlConstructor {
  /// Predicate Stats Group Top Admin constructor.
  const StatsGroupTopAdminBase._() : super._();
}

/// Stats Group Top Admin.
///
/// ID: `d7584c87`.
class StatsGroupTopAdmin extends StatsGroupTopAdminBase {
  /// Stats Group Top Admin constructor.
  const StatsGroupTopAdmin({
    required this.userId,
    required this.deleted,
    required this.kicked,
    required this.banned,
  }) : super._();

  /// Deserialize.
  factory StatsGroupTopAdmin.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final deleted = _readint(buffer);
    // final kicked = _readint(buffer);
    // final banned = _readint(buffer);
    // final result = StatsGroupTopAdmin(userId: userId, deleted: deleted, kicked: kicked, banned: banned,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Deleted.
  final int deleted;

  /// Kicked.
  final int kicked;

  /// Banned.
  final int banned;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd7584c87);
    buffer.writeLong(userId);
    buffer.writeInt(deleted);
    buffer.writeInt(kicked);
    buffer.writeInt(banned);
  }
}

/// Predicate Stats Group Top Inviter.
abstract class StatsGroupTopInviterBase extends TlConstructor {
  /// Predicate Stats Group Top Inviter constructor.
  const StatsGroupTopInviterBase._() : super._();
}

/// Stats Group Top Inviter.
///
/// ID: `535f779d`.
class StatsGroupTopInviter extends StatsGroupTopInviterBase {
  /// Stats Group Top Inviter constructor.
  const StatsGroupTopInviter({
    required this.userId,
    required this.invitations,
  }) : super._();

  /// Deserialize.
  factory StatsGroupTopInviter.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final invitations = _readint(buffer);
    // final result = StatsGroupTopInviter(userId: userId, invitations: invitations,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Invitations.
  final int invitations;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x535f779d);
    buffer.writeLong(userId);
    buffer.writeInt(invitations);
  }
}

/// Predicate Stats Megagroup Stats.
abstract class StatsMegagroupStatsBase extends TlConstructor {
  /// Predicate Stats Megagroup Stats constructor.
  const StatsMegagroupStatsBase._() : super._();
}

/// Stats Megagroup Stats.
///
/// ID: `ef7ff916`.
class StatsMegagroupStats extends StatsMegagroupStatsBase {
  /// Stats Megagroup Stats constructor.
  const StatsMegagroupStats({
    required this.period,
    required this.members,
    required this.messages,
    required this.viewers,
    required this.posters,
    required this.growthGraph,
    required this.membersGraph,
    required this.newMembersBySourceGraph,
    required this.languagesGraph,
    required this.messagesGraph,
    required this.actionsGraph,
    required this.topHoursGraph,
    required this.weekdaysGraph,
    required this.topPosters,
    required this.topAdmins,
    required this.topInviters,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory StatsMegagroupStats.deserialize(Uint8List buffer) {
    // final period = _readStatsDateRangeDays(buffer);
    // final members = _readStatsAbsValueAndPrev(buffer);
    // final messages = _readStatsAbsValueAndPrev(buffer);
    // final viewers = _readStatsAbsValueAndPrev(buffer);
    // final posters = _readStatsAbsValueAndPrev(buffer);
    // final growthGraph = _readStatsGraph(buffer);
    // final membersGraph = _readStatsGraph(buffer);
    // final newMembersBySourceGraph = _readStatsGraph(buffer);
    // final languagesGraph = _readStatsGraph(buffer);
    // final messagesGraph = _readStatsGraph(buffer);
    // final actionsGraph = _readStatsGraph(buffer);
    // final topHoursGraph = _readStatsGraph(buffer);
    // final weekdaysGraph = _readStatsGraph(buffer);
    // final topPosters = _readVector<StatsGroupTopPoster>(buffer);
    // final topAdmins = _readVector<StatsGroupTopAdmin>(buffer);
    // final topInviters = _readVector<StatsGroupTopInviter>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = StatsMegagroupStats(period: period, members: members, messages: messages, viewers: viewers, posters: posters, growthGraph: growthGraph, membersGraph: membersGraph, newMembersBySourceGraph: newMembersBySourceGraph, languagesGraph: languagesGraph, messagesGraph: messagesGraph, actionsGraph: actionsGraph, topHoursGraph: topHoursGraph, weekdaysGraph: weekdaysGraph, topPosters: topPosters, topAdmins: topAdmins, topInviters: topInviters, users: users,);

    // return result;

    throw Exception();
  }

  /// Period.
  final StatsDateRangeDaysBase period;

  /// Members.
  final StatsAbsValueAndPrevBase members;

  /// Messages.
  final StatsAbsValueAndPrevBase messages;

  /// Viewers.
  final StatsAbsValueAndPrevBase viewers;

  /// Posters.
  final StatsAbsValueAndPrevBase posters;

  /// Growth Graph.
  final StatsGraphBase growthGraph;

  /// Members Graph.
  final StatsGraphBase membersGraph;

  /// New Members By Source Graph.
  final StatsGraphBase newMembersBySourceGraph;

  /// Languages Graph.
  final StatsGraphBase languagesGraph;

  /// Messages Graph.
  final StatsGraphBase messagesGraph;

  /// Actions Graph.
  final StatsGraphBase actionsGraph;

  /// Top Hours Graph.
  final StatsGraphBase topHoursGraph;

  /// Weekdays Graph.
  final StatsGraphBase weekdaysGraph;

  /// Top Posters.
  final List<StatsGroupTopPosterBase> topPosters;

  /// Top Admins.
  final List<StatsGroupTopAdminBase> topAdmins;

  /// Top Inviters.
  final List<StatsGroupTopInviterBase> topInviters;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xef7ff916);
    buffer.writeObject(period);
    buffer.writeObject(members);
    buffer.writeObject(messages);
    buffer.writeObject(viewers);
    buffer.writeObject(posters);
    buffer.writeObject(growthGraph);
    buffer.writeObject(membersGraph);
    buffer.writeObject(newMembersBySourceGraph);
    buffer.writeObject(languagesGraph);
    buffer.writeObject(messagesGraph);
    buffer.writeObject(actionsGraph);
    buffer.writeObject(topHoursGraph);
    buffer.writeObject(weekdaysGraph);
    buffer.writeVectorObject(topPosters);
    buffer.writeVectorObject(topAdmins);
    buffer.writeVectorObject(topInviters);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Global Privacy Settings.
abstract class GlobalPrivacySettingsBase extends TlConstructor {
  /// Predicate Global Privacy Settings constructor.
  const GlobalPrivacySettingsBase._() : super._();
}

/// Global Privacy Settings.
///
/// ID: `734c4ccb`.
class GlobalPrivacySettings extends GlobalPrivacySettingsBase {
  /// Global Privacy Settings constructor.
  const GlobalPrivacySettings({
    required this.archiveAndMuteNewNoncontactPeers,
    required this.keepArchivedUnmuted,
    required this.keepArchivedFolders,
  }) : super._();

  /// Deserialize.
  factory GlobalPrivacySettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final archiveAndMuteNewNoncontactPeers = _readflags.0?true(buffer);
    // final keepArchivedUnmuted = _readflags.1?true(buffer);
    // final keepArchivedFolders = _readflags.2?true(buffer);
    // final result = GlobalPrivacySettings(flags: flags, archiveAndMuteNewNoncontactPeers: archiveAndMuteNewNoncontactPeers, keepArchivedUnmuted: keepArchivedUnmuted, keepArchivedFolders: keepArchivedFolders,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: archiveAndMuteNewNoncontactPeers,
      b01: keepArchivedUnmuted,
      b02: keepArchivedFolders,
    );

    return v;
  }

  /// archive_and_mute_new_noncontact_peers: bit 0 of flags.0?true
  final bool archiveAndMuteNewNoncontactPeers;

  /// keep_archived_unmuted: bit 1 of flags.1?true
  final bool keepArchivedUnmuted;

  /// keep_archived_folders: bit 2 of flags.2?true
  final bool keepArchivedFolders;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x734c4ccb);
    buffer.writeInt(flags);
  }
}

/// Predicate Help Country Code.
abstract class HelpCountryCodeBase extends TlConstructor {
  /// Predicate Help Country Code constructor.
  const HelpCountryCodeBase._() : super._();
}

/// Help Country Code.
///
/// ID: `4203c5ef`.
class HelpCountryCode extends HelpCountryCodeBase {
  /// Help Country Code constructor.
  const HelpCountryCode({
    required this.countryCode,
    this.prefixes,
    this.patterns,
  }) : super._();

  /// Deserialize.
  factory HelpCountryCode.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final countryCode = _readstring(buffer);
    // final prefixes = _readflags.0?Vector<string>(buffer);
    // final patterns = _readflags.1?Vector<string>(buffer);
    // final result = HelpCountryCode(flags: flags, countryCode: countryCode, prefixes: prefixes, patterns: patterns,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: prefixes != null,
      b01: patterns != null,
    );

    return v;
  }

  /// Country Code.
  final String countryCode;

  /// Prefixes.
  final List<String>? prefixes;

  /// Patterns.
  final List<String>? patterns;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4203c5ef);
    buffer.writeInt(flags);
    buffer.writeString(countryCode);
    final localPrefixesCopy = prefixes;
    if (localPrefixesCopy != null) {
      buffer.writeVectorString(localPrefixesCopy);
    }
    final localPatternsCopy = patterns;
    if (localPatternsCopy != null) {
      buffer.writeVectorString(localPatternsCopy);
    }
  }
}

/// Predicate Help Country.
abstract class HelpCountryBase extends TlConstructor {
  /// Predicate Help Country constructor.
  const HelpCountryBase._() : super._();
}

/// Help Country.
///
/// ID: `c3878e23`.
class HelpCountry extends HelpCountryBase {
  /// Help Country constructor.
  const HelpCountry({
    required this.hidden,
    required this.iso2,
    required this.defaultName,
    this.name,
    required this.countryCodes,
  }) : super._();

  /// Deserialize.
  factory HelpCountry.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final hidden = _readflags.0?true(buffer);
    // final iso2 = _readstring(buffer);
    // final defaultName = _readstring(buffer);
    // final name = _readflags.1?string(buffer);
    // final countryCodes = _readVector<help.CountryCode>(buffer);
    // final result = HelpCountry(flags: flags, hidden: hidden, iso2: iso2, defaultName: defaultName, name: name, countryCodes: countryCodes,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: hidden,
      b01: name != null,
    );

    return v;
  }

  /// hidden: bit 0 of flags.0?true
  final bool hidden;

  /// Iso2.
  final String iso2;

  /// Default Name.
  final String defaultName;

  /// Name.
  final String? name;

  /// Country Codes.
  final List<HelpCountryCodeBase> countryCodes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc3878e23);
    buffer.writeInt(flags);
    buffer.writeString(iso2);
    buffer.writeString(defaultName);
    final localNameCopy = name;
    if (localNameCopy != null) {
      buffer.writeString(localNameCopy);
    }
    buffer.writeVectorObject(countryCodes);
  }
}

/// Predicate Help Countries List.
abstract class HelpCountriesListBase extends TlConstructor {
  /// Predicate Help Countries List constructor.
  const HelpCountriesListBase._() : super._();
}

/// Help Countries List Not Modified.
///
/// ID: `93cc1f32`.
class HelpCountriesListNotModified extends HelpCountriesListBase {
  /// Help Countries List Not Modified constructor.
  const HelpCountriesListNotModified() : super._();

  /// Deserialize.
  factory HelpCountriesListNotModified.deserialize(Uint8List buffer) {
    // final result = HelpCountriesListNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x93cc1f32);
  }
}

/// Help Countries List.
///
/// ID: `87d0759e`.
class HelpCountriesList extends HelpCountriesListBase {
  /// Help Countries List constructor.
  const HelpCountriesList({
    required this.countries,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory HelpCountriesList.deserialize(Uint8List buffer) {
    // final countries = _readVector<help.Country>(buffer);
    // final hash = _readint(buffer);
    // final result = HelpCountriesList(countries: countries, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Countries.
  final List<HelpCountryBase> countries;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x87d0759e);
    buffer.writeVectorObject(countries);
    buffer.writeInt(hash);
  }
}

/// Predicate Message Views.
abstract class MessageViewsBase extends TlConstructor {
  /// Predicate Message Views constructor.
  const MessageViewsBase._() : super._();
}

/// Message Views.
///
/// ID: `455b853d`.
class MessageViews extends MessageViewsBase {
  /// Message Views constructor.
  const MessageViews({
    this.views,
    this.forwards,
    this.replies,
  }) : super._();

  /// Deserialize.
  factory MessageViews.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final views = _readflags.0?int(buffer);
    // final forwards = _readflags.1?int(buffer);
    // final replies = _readflags.2?MessageReplies(buffer);
    // final result = MessageViews(flags: flags, views: views, forwards: forwards, replies: replies,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: views != null,
      b01: forwards != null,
      b02: replies != null,
    );

    return v;
  }

  /// Views.
  final int? views;

  /// Forwards.
  final int? forwards;

  /// Replies.
  final MessageRepliesBase? replies;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x455b853d);
    buffer.writeInt(flags);
    final localViewsCopy = views;
    if (localViewsCopy != null) {
      buffer.writeInt(localViewsCopy);
    }
    final localForwardsCopy = forwards;
    if (localForwardsCopy != null) {
      buffer.writeInt(localForwardsCopy);
    }
    final localRepliesCopy = replies;
    if (localRepliesCopy != null) {
      buffer.writeObject(localRepliesCopy);
    }
  }
}

/// Predicate Messages Message Views.
abstract class MessagesMessageViewsBase extends TlConstructor {
  /// Predicate Messages Message Views constructor.
  const MessagesMessageViewsBase._() : super._();
}

/// Messages Message Views.
///
/// ID: `b6c4f543`.
class MessagesMessageViews extends MessagesMessageViewsBase {
  /// Messages Message Views constructor.
  const MessagesMessageViews({
    required this.views,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesMessageViews.deserialize(Uint8List buffer) {
    // final views = _readVector<MessageViews>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesMessageViews(views: views, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Views.
  final List<MessageViewsBase> views;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb6c4f543);
    buffer.writeVectorObject(views);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Messages Discussion Message.
abstract class MessagesDiscussionMessageBase extends TlConstructor {
  /// Predicate Messages Discussion Message constructor.
  const MessagesDiscussionMessageBase._() : super._();
}

/// Messages Discussion Message.
///
/// ID: `a6341782`.
class MessagesDiscussionMessage extends MessagesDiscussionMessageBase {
  /// Messages Discussion Message constructor.
  const MessagesDiscussionMessage({
    required this.messages,
    this.maxId,
    this.readInboxMaxId,
    this.readOutboxMaxId,
    required this.unreadCount,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesDiscussionMessage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final messages = _readVector<Message>(buffer);
    // final maxId = _readflags.0?int(buffer);
    // final readInboxMaxId = _readflags.1?int(buffer);
    // final readOutboxMaxId = _readflags.2?int(buffer);
    // final unreadCount = _readint(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesDiscussionMessage(flags: flags, messages: messages, maxId: maxId, readInboxMaxId: readInboxMaxId, readOutboxMaxId: readOutboxMaxId, unreadCount: unreadCount, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: maxId != null,
      b01: readInboxMaxId != null,
      b02: readOutboxMaxId != null,
    );

    return v;
  }

  /// Messages.
  final List<MessageBase> messages;

  /// Max Id.
  final int? maxId;

  /// Read Inbox Max Id.
  final int? readInboxMaxId;

  /// Read Outbox Max Id.
  final int? readOutboxMaxId;

  /// Unread Count.
  final int unreadCount;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa6341782);
    buffer.writeInt(flags);
    buffer.writeVectorObject(messages);
    final localMaxIdCopy = maxId;
    if (localMaxIdCopy != null) {
      buffer.writeInt(localMaxIdCopy);
    }
    final localReadInboxMaxIdCopy = readInboxMaxId;
    if (localReadInboxMaxIdCopy != null) {
      buffer.writeInt(localReadInboxMaxIdCopy);
    }
    final localReadOutboxMaxIdCopy = readOutboxMaxId;
    if (localReadOutboxMaxIdCopy != null) {
      buffer.writeInt(localReadOutboxMaxIdCopy);
    }
    buffer.writeInt(unreadCount);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Message Reply Header.
abstract class MessageReplyHeaderBase extends TlConstructor {
  /// Predicate Message Reply Header constructor.
  const MessageReplyHeaderBase._() : super._();
}

/// Message Reply Header.
///
/// ID: `afbc09db`.
class MessageReplyHeader extends MessageReplyHeaderBase {
  /// Message Reply Header constructor.
  const MessageReplyHeader({
    required this.replyToScheduled,
    required this.forumTopic,
    required this.quote,
    this.replyToMsgId,
    this.replyToPeerId,
    this.replyFrom,
    this.replyMedia,
    this.replyToTopId,
    this.quoteText,
    this.quoteEntities,
    this.quoteOffset,
  }) : super._();

  /// Deserialize.
  factory MessageReplyHeader.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final replyToScheduled = _readflags.2?true(buffer);
    // final forumTopic = _readflags.3?true(buffer);
    // final quote = _readflags.9?true(buffer);
    // final replyToMsgId = _readflags.4?int(buffer);
    // final replyToPeerId = _readflags.0?Peer(buffer);
    // final replyFrom = _readflags.5?MessageFwdHeader(buffer);
    // final replyMedia = _readflags.8?MessageMedia(buffer);
    // final replyToTopId = _readflags.1?int(buffer);
    // final quoteText = _readflags.6?string(buffer);
    // final quoteEntities = _readflags.7?Vector<MessageEntity>(buffer);
    // final quoteOffset = _readflags.10?int(buffer);
    // final result = MessageReplyHeader(flags: flags, replyToScheduled: replyToScheduled, forumTopic: forumTopic, quote: quote, replyToMsgId: replyToMsgId, replyToPeerId: replyToPeerId, replyFrom: replyFrom, replyMedia: replyMedia, replyToTopId: replyToTopId, quoteText: quoteText, quoteEntities: quoteEntities, quoteOffset: quoteOffset,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: replyToScheduled,
      b03: forumTopic,
      b09: quote,
      b04: replyToMsgId != null,
      b00: replyToPeerId != null,
      b05: replyFrom != null,
      b08: replyMedia != null,
      b01: replyToTopId != null,
      b06: quoteText != null,
      b07: quoteEntities != null,
      b10: quoteOffset != null,
    );

    return v;
  }

  /// reply_to_scheduled: bit 2 of flags.2?true
  final bool replyToScheduled;

  /// forum_topic: bit 3 of flags.3?true
  final bool forumTopic;

  /// quote: bit 9 of flags.9?true
  final bool quote;

  /// Reply To Msg Id.
  final int? replyToMsgId;

  /// Reply To Peer Id.
  final PeerBase? replyToPeerId;

  /// Reply From.
  final MessageFwdHeaderBase? replyFrom;

  /// Reply Media.
  final MessageMediaBase? replyMedia;

  /// Reply To Top Id.
  final int? replyToTopId;

  /// Quote Text.
  final String? quoteText;

  /// Quote Entities.
  final List<MessageEntityBase>? quoteEntities;

  /// Quote Offset.
  final int? quoteOffset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xafbc09db);
    buffer.writeInt(flags);
    final localReplyToMsgIdCopy = replyToMsgId;
    if (localReplyToMsgIdCopy != null) {
      buffer.writeInt(localReplyToMsgIdCopy);
    }
    final localReplyToPeerIdCopy = replyToPeerId;
    if (localReplyToPeerIdCopy != null) {
      buffer.writeObject(localReplyToPeerIdCopy);
    }
    final localReplyFromCopy = replyFrom;
    if (localReplyFromCopy != null) {
      buffer.writeObject(localReplyFromCopy);
    }
    final localReplyMediaCopy = replyMedia;
    if (localReplyMediaCopy != null) {
      buffer.writeObject(localReplyMediaCopy);
    }
    final localReplyToTopIdCopy = replyToTopId;
    if (localReplyToTopIdCopy != null) {
      buffer.writeInt(localReplyToTopIdCopy);
    }
    final localQuoteTextCopy = quoteText;
    if (localQuoteTextCopy != null) {
      buffer.writeString(localQuoteTextCopy);
    }
    final localQuoteEntitiesCopy = quoteEntities;
    if (localQuoteEntitiesCopy != null) {
      buffer.writeVectorObject(localQuoteEntitiesCopy);
    }
    final localQuoteOffsetCopy = quoteOffset;
    if (localQuoteOffsetCopy != null) {
      buffer.writeInt(localQuoteOffsetCopy);
    }
  }
}

/// Message Reply Story Header.
///
/// ID: `9c98bfc1`.
class MessageReplyStoryHeader extends MessageReplyHeaderBase {
  /// Message Reply Story Header constructor.
  const MessageReplyStoryHeader({
    required this.userId,
    required this.storyId,
  }) : super._();

  /// Deserialize.
  factory MessageReplyStoryHeader.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final storyId = _readint(buffer);
    // final result = MessageReplyStoryHeader(userId: userId, storyId: storyId,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Story Id.
  final int storyId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9c98bfc1);
    buffer.writeLong(userId);
    buffer.writeInt(storyId);
  }
}

/// Predicate Message Replies.
abstract class MessageRepliesBase extends TlConstructor {
  /// Predicate Message Replies constructor.
  const MessageRepliesBase._() : super._();
}

/// Message Replies.
///
/// ID: `83d60fc2`.
class MessageReplies extends MessageRepliesBase {
  /// Message Replies constructor.
  const MessageReplies({
    required this.comments,
    required this.replies,
    required this.repliesPts,
    this.recentRepliers,
    this.channelId,
    this.maxId,
    this.readMaxId,
  }) : super._();

  /// Deserialize.
  factory MessageReplies.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final comments = _readflags.0?true(buffer);
    // final replies = _readint(buffer);
    // final repliesPts = _readint(buffer);
    // final recentRepliers = _readflags.1?Vector<Peer>(buffer);
    // final channelId = _readflags.0?long(buffer);
    // final maxId = _readflags.2?int(buffer);
    // final readMaxId = _readflags.3?int(buffer);
    // final result = MessageReplies(flags: flags, comments: comments, replies: replies, repliesPts: repliesPts, recentRepliers: recentRepliers, channelId: channelId, maxId: maxId, readMaxId: readMaxId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: comments || channelId != null,
      b01: recentRepliers != null,
      b02: maxId != null,
      b03: readMaxId != null,
    );

    return v;
  }

  /// comments: bit 0 of flags.0?true
  final bool comments;

  /// Replies.
  final int replies;

  /// Replies Pts.
  final int repliesPts;

  /// Recent Repliers.
  final List<PeerBase>? recentRepliers;

  /// Channel Id.
  final int? channelId;

  /// Max Id.
  final int? maxId;

  /// Read Max Id.
  final int? readMaxId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x83d60fc2);
    buffer.writeInt(flags);
    buffer.writeInt(replies);
    buffer.writeInt(repliesPts);
    final localRecentRepliersCopy = recentRepliers;
    if (localRecentRepliersCopy != null) {
      buffer.writeVectorObject(localRecentRepliersCopy);
    }
    final localChannelIdCopy = channelId;
    if (localChannelIdCopy != null) {
      buffer.writeLong(localChannelIdCopy);
    }
    final localMaxIdCopy = maxId;
    if (localMaxIdCopy != null) {
      buffer.writeInt(localMaxIdCopy);
    }
    final localReadMaxIdCopy = readMaxId;
    if (localReadMaxIdCopy != null) {
      buffer.writeInt(localReadMaxIdCopy);
    }
  }
}

/// Predicate Peer Blocked.
abstract class PeerBlockedBase extends TlConstructor {
  /// Predicate Peer Blocked constructor.
  const PeerBlockedBase._() : super._();
}

/// Peer Blocked.
///
/// ID: `e8fd8014`.
class PeerBlocked extends PeerBlockedBase {
  /// Peer Blocked constructor.
  const PeerBlocked({
    required this.peerId,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory PeerBlocked.deserialize(Uint8List buffer) {
    // final peerId = _readPeer(buffer);
    // final date = _readint(buffer);
    // final result = PeerBlocked(peerId: peerId, date: date,);

    // return result;

    throw Exception();
  }

  /// Peer Id.
  final PeerBase peerId;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe8fd8014);
    buffer.writeObject(peerId);
    buffer.writeDateTime(date);
  }
}

/// Predicate Stats Message Stats.
abstract class StatsMessageStatsBase extends TlConstructor {
  /// Predicate Stats Message Stats constructor.
  const StatsMessageStatsBase._() : super._();
}

/// Stats Message Stats.
///
/// ID: `7fe91c14`.
class StatsMessageStats extends StatsMessageStatsBase {
  /// Stats Message Stats constructor.
  const StatsMessageStats({
    required this.viewsGraph,
    required this.reactionsByEmotionGraph,
  }) : super._();

  /// Deserialize.
  factory StatsMessageStats.deserialize(Uint8List buffer) {
    // final viewsGraph = _readStatsGraph(buffer);
    // final reactionsByEmotionGraph = _readStatsGraph(buffer);
    // final result = StatsMessageStats(viewsGraph: viewsGraph, reactionsByEmotionGraph: reactionsByEmotionGraph,);

    // return result;

    throw Exception();
  }

  /// Views Graph.
  final StatsGraphBase viewsGraph;

  /// Reactions By Emotion Graph.
  final StatsGraphBase reactionsByEmotionGraph;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7fe91c14);
    buffer.writeObject(viewsGraph);
    buffer.writeObject(reactionsByEmotionGraph);
  }
}

/// Predicate Group Call.
abstract class GroupCallBase extends TlConstructor {
  /// Predicate Group Call constructor.
  const GroupCallBase._() : super._();
}

/// Group Call Discarded.
///
/// ID: `7780bcb4`.
class GroupCallDiscarded extends GroupCallBase {
  /// Group Call Discarded constructor.
  const GroupCallDiscarded({
    required this.id,
    required this.accessHash,
    required this.duration,
  }) : super._();

  /// Deserialize.
  factory GroupCallDiscarded.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final duration = _readint(buffer);
    // final result = GroupCallDiscarded(id: id, accessHash: accessHash, duration: duration,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Duration.
  final int duration;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7780bcb4);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeInt(duration);
  }
}

/// Group Call.
///
/// ID: `d597650c`.
class GroupCall extends GroupCallBase {
  /// Group Call constructor.
  const GroupCall({
    required this.joinMuted,
    required this.canChangeJoinMuted,
    required this.joinDateAsc,
    required this.scheduleStartSubscribed,
    required this.canStartVideo,
    required this.recordVideoActive,
    required this.rtmpStream,
    required this.listenersHidden,
    required this.id,
    required this.accessHash,
    required this.participantsCount,
    this.title,
    this.streamDcId,
    this.recordStartDate,
    this.scheduleDate,
    this.unmutedVideoCount,
    required this.unmutedVideoLimit,
    required this.version,
  }) : super._();

  /// Deserialize.
  factory GroupCall.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final joinMuted = _readflags.1?true(buffer);
    // final canChangeJoinMuted = _readflags.2?true(buffer);
    // final joinDateAsc = _readflags.6?true(buffer);
    // final scheduleStartSubscribed = _readflags.8?true(buffer);
    // final canStartVideo = _readflags.9?true(buffer);
    // final recordVideoActive = _readflags.11?true(buffer);
    // final rtmpStream = _readflags.12?true(buffer);
    // final listenersHidden = _readflags.13?true(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final participantsCount = _readint(buffer);
    // final title = _readflags.3?string(buffer);
    // final streamDcId = _readflags.4?int(buffer);
    // final recordStartDate = _readflags.5?int(buffer);
    // final scheduleDate = _readflags.7?int(buffer);
    // final unmutedVideoCount = _readflags.10?int(buffer);
    // final unmutedVideoLimit = _readint(buffer);
    // final version = _readint(buffer);
    // final result = GroupCall(flags: flags, joinMuted: joinMuted, canChangeJoinMuted: canChangeJoinMuted, joinDateAsc: joinDateAsc, scheduleStartSubscribed: scheduleStartSubscribed, canStartVideo: canStartVideo, recordVideoActive: recordVideoActive, rtmpStream: rtmpStream, listenersHidden: listenersHidden, id: id, accessHash: accessHash, participantsCount: participantsCount, title: title, streamDcId: streamDcId, recordStartDate: recordStartDate, scheduleDate: scheduleDate, unmutedVideoCount: unmutedVideoCount, unmutedVideoLimit: unmutedVideoLimit, version: version,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: joinMuted,
      b02: canChangeJoinMuted,
      b06: joinDateAsc,
      b08: scheduleStartSubscribed,
      b09: canStartVideo,
      b11: recordVideoActive,
      b12: rtmpStream,
      b13: listenersHidden,
      b03: title != null,
      b04: streamDcId != null,
      b05: recordStartDate != null,
      b07: scheduleDate != null,
      b10: unmutedVideoCount != null,
    );

    return v;
  }

  /// join_muted: bit 1 of flags.1?true
  final bool joinMuted;

  /// can_change_join_muted: bit 2 of flags.2?true
  final bool canChangeJoinMuted;

  /// join_date_asc: bit 6 of flags.6?true
  final bool joinDateAsc;

  /// schedule_start_subscribed: bit 8 of flags.8?true
  final bool scheduleStartSubscribed;

  /// can_start_video: bit 9 of flags.9?true
  final bool canStartVideo;

  /// record_video_active: bit 11 of flags.11?true
  final bool recordVideoActive;

  /// rtmp_stream: bit 12 of flags.12?true
  final bool rtmpStream;

  /// listeners_hidden: bit 13 of flags.13?true
  final bool listenersHidden;

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Participants Count.
  final int participantsCount;

  /// Title.
  final String? title;

  /// Stream Dc Id.
  final int? streamDcId;

  /// Record Start Date.
  final DateTime? recordStartDate;

  /// Schedule Date.
  final DateTime? scheduleDate;

  /// Unmuted Video Count.
  final int? unmutedVideoCount;

  /// Unmuted Video Limit.
  final int unmutedVideoLimit;

  /// Version.
  final int version;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd597650c);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeInt(participantsCount);
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localStreamDcIdCopy = streamDcId;
    if (localStreamDcIdCopy != null) {
      buffer.writeInt(localStreamDcIdCopy);
    }
    final localRecordStartDateCopy = recordStartDate;
    if (localRecordStartDateCopy != null) {
      buffer.writeDateTime(localRecordStartDateCopy);
    }
    final localScheduleDateCopy = scheduleDate;
    if (localScheduleDateCopy != null) {
      buffer.writeDateTime(localScheduleDateCopy);
    }
    final localUnmutedVideoCountCopy = unmutedVideoCount;
    if (localUnmutedVideoCountCopy != null) {
      buffer.writeInt(localUnmutedVideoCountCopy);
    }
    buffer.writeInt(unmutedVideoLimit);
    buffer.writeInt(version);
  }
}

/// Predicate Input Group Call.
abstract class InputGroupCallBase extends TlConstructor {
  /// Predicate Input Group Call constructor.
  const InputGroupCallBase._() : super._();
}

/// Input Group Call.
///
/// ID: `d8aa840f`.
class InputGroupCall extends InputGroupCallBase {
  /// Input Group Call constructor.
  const InputGroupCall({
    required this.id,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputGroupCall.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputGroupCall(id: id, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd8aa840f);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
  }
}

/// Predicate Group Call Participant.
abstract class GroupCallParticipantBase extends TlConstructor {
  /// Predicate Group Call Participant constructor.
  const GroupCallParticipantBase._() : super._();
}

/// Group Call Participant.
///
/// ID: `eba636fe`.
class GroupCallParticipant extends GroupCallParticipantBase {
  /// Group Call Participant constructor.
  const GroupCallParticipant({
    required this.muted,
    required this.left,
    required this.canSelfUnmute,
    required this.justJoined,
    required this.versioned,
    required this.min,
    required this.mutedByYou,
    required this.volumeByAdmin,
    required this.self,
    required this.videoJoined,
    required this.peer,
    required this.date,
    this.activeDate,
    required this.source,
    this.volume,
    this.about,
    this.raiseHandRating,
    this.video,
    this.presentation,
  }) : super._();

  /// Deserialize.
  factory GroupCallParticipant.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final muted = _readflags.0?true(buffer);
    // final left = _readflags.1?true(buffer);
    // final canSelfUnmute = _readflags.2?true(buffer);
    // final justJoined = _readflags.4?true(buffer);
    // final versioned = _readflags.5?true(buffer);
    // final min = _readflags.8?true(buffer);
    // final mutedByYou = _readflags.9?true(buffer);
    // final volumeByAdmin = _readflags.10?true(buffer);
    // final self = _readflags.12?true(buffer);
    // final videoJoined = _readflags.15?true(buffer);
    // final peer = _readPeer(buffer);
    // final date = _readint(buffer);
    // final activeDate = _readflags.3?int(buffer);
    // final source = _readint(buffer);
    // final volume = _readflags.7?int(buffer);
    // final about = _readflags.11?string(buffer);
    // final raiseHandRating = _readflags.13?long(buffer);
    // final video = _readflags.6?GroupCallParticipantVideo(buffer);
    // final presentation = _readflags.14?GroupCallParticipantVideo(buffer);
    // final result = GroupCallParticipant(flags: flags, muted: muted, left: left, canSelfUnmute: canSelfUnmute, justJoined: justJoined, versioned: versioned, min: min, mutedByYou: mutedByYou, volumeByAdmin: volumeByAdmin, self: self, videoJoined: videoJoined, peer: peer, date: date, activeDate: activeDate, source: source, volume: volume, about: about, raiseHandRating: raiseHandRating, video: video, presentation: presentation,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: muted,
      b01: left,
      b02: canSelfUnmute,
      b04: justJoined,
      b05: versioned,
      b08: min,
      b09: mutedByYou,
      b10: volumeByAdmin,
      b12: self,
      b15: videoJoined,
      b03: activeDate != null,
      b07: volume != null,
      b11: about != null,
      b13: raiseHandRating != null,
      b06: video != null,
      b14: presentation != null,
    );

    return v;
  }

  /// muted: bit 0 of flags.0?true
  final bool muted;

  /// left: bit 1 of flags.1?true
  final bool left;

  /// can_self_unmute: bit 2 of flags.2?true
  final bool canSelfUnmute;

  /// just_joined: bit 4 of flags.4?true
  final bool justJoined;

  /// versioned: bit 5 of flags.5?true
  final bool versioned;

  /// min: bit 8 of flags.8?true
  final bool min;

  /// muted_by_you: bit 9 of flags.9?true
  final bool mutedByYou;

  /// volume_by_admin: bit 10 of flags.10?true
  final bool volumeByAdmin;

  /// self: bit 12 of flags.12?true
  final bool self;

  /// video_joined: bit 15 of flags.15?true
  final bool videoJoined;

  /// Peer.
  final PeerBase peer;

  /// Date.
  final DateTime date;

  /// Active Date.
  final DateTime? activeDate;

  /// Source.
  final int source;

  /// Volume.
  final int? volume;

  /// About.
  final String? about;

  /// Raise Hand Rating.
  final int? raiseHandRating;

  /// Video.
  final GroupCallParticipantVideoBase? video;

  /// Presentation.
  final GroupCallParticipantVideoBase? presentation;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xeba636fe);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeDateTime(date);
    final localActiveDateCopy = activeDate;
    if (localActiveDateCopy != null) {
      buffer.writeDateTime(localActiveDateCopy);
    }
    buffer.writeInt(source);
    final localVolumeCopy = volume;
    if (localVolumeCopy != null) {
      buffer.writeInt(localVolumeCopy);
    }
    final localAboutCopy = about;
    if (localAboutCopy != null) {
      buffer.writeString(localAboutCopy);
    }
    final localRaiseHandRatingCopy = raiseHandRating;
    if (localRaiseHandRatingCopy != null) {
      buffer.writeLong(localRaiseHandRatingCopy);
    }
    final localVideoCopy = video;
    if (localVideoCopy != null) {
      buffer.writeObject(localVideoCopy);
    }
    final localPresentationCopy = presentation;
    if (localPresentationCopy != null) {
      buffer.writeObject(localPresentationCopy);
    }
  }
}

/// Predicate Phone Group Call.
abstract class PhoneGroupCallBase extends TlConstructor {
  /// Predicate Phone Group Call constructor.
  const PhoneGroupCallBase._() : super._();
}

/// Phone Group Call.
///
/// ID: `9e727aad`.
class PhoneGroupCall extends PhoneGroupCallBase {
  /// Phone Group Call constructor.
  const PhoneGroupCall({
    required this.call,
    required this.participants,
    required this.participantsNextOffset,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PhoneGroupCall.deserialize(Uint8List buffer) {
    // final call = _readGroupCall(buffer);
    // final participants = _readVector<GroupCallParticipant>(buffer);
    // final participantsNextOffset = _readstring(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = PhoneGroupCall(call: call, participants: participants, participantsNextOffset: participantsNextOffset, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Call.
  final GroupCallBase call;

  /// Participants.
  final List<GroupCallParticipantBase> participants;

  /// Participants Next Offset.
  final String participantsNextOffset;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9e727aad);
    buffer.writeObject(call);
    buffer.writeVectorObject(participants);
    buffer.writeString(participantsNextOffset);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Phone Group Participants.
abstract class PhoneGroupParticipantsBase extends TlConstructor {
  /// Predicate Phone Group Participants constructor.
  const PhoneGroupParticipantsBase._() : super._();
}

/// Phone Group Participants.
///
/// ID: `f47751b6`.
class PhoneGroupParticipants extends PhoneGroupParticipantsBase {
  /// Phone Group Participants constructor.
  const PhoneGroupParticipants({
    required this.count,
    required this.participants,
    required this.nextOffset,
    required this.chats,
    required this.users,
    required this.version,
  }) : super._();

  /// Deserialize.
  factory PhoneGroupParticipants.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final participants = _readVector<GroupCallParticipant>(buffer);
    // final nextOffset = _readstring(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final version = _readint(buffer);
    // final result = PhoneGroupParticipants(count: count, participants: participants, nextOffset: nextOffset, chats: chats, users: users, version: version,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Participants.
  final List<GroupCallParticipantBase> participants;

  /// Next Offset.
  final String nextOffset;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Version.
  final int version;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf47751b6);
    buffer.writeInt(count);
    buffer.writeVectorObject(participants);
    buffer.writeString(nextOffset);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
    buffer.writeInt(version);
  }
}

/// Predicate Inline Query Peer Type.
abstract class InlineQueryPeerTypeBase extends TlConstructor {
  /// Predicate Inline Query Peer Type constructor.
  const InlineQueryPeerTypeBase._() : super._();
}

/// Inline Query Peer Type Same Bot P M.
///
/// ID: `3081ed9d`.
class InlineQueryPeerTypeSameBotPM extends InlineQueryPeerTypeBase {
  /// Inline Query Peer Type Same Bot P M constructor.
  const InlineQueryPeerTypeSameBotPM() : super._();

  /// Deserialize.
  factory InlineQueryPeerTypeSameBotPM.deserialize(Uint8List buffer) {
    // final result = InlineQueryPeerTypeSameBotPM();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3081ed9d);
  }
}

/// Inline Query Peer Type P M.
///
/// ID: `833c0fac`.
class InlineQueryPeerTypePM extends InlineQueryPeerTypeBase {
  /// Inline Query Peer Type P M constructor.
  const InlineQueryPeerTypePM() : super._();

  /// Deserialize.
  factory InlineQueryPeerTypePM.deserialize(Uint8List buffer) {
    // final result = InlineQueryPeerTypePM();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x833c0fac);
  }
}

/// Inline Query Peer Type Chat.
///
/// ID: `d766c50a`.
class InlineQueryPeerTypeChat extends InlineQueryPeerTypeBase {
  /// Inline Query Peer Type Chat constructor.
  const InlineQueryPeerTypeChat() : super._();

  /// Deserialize.
  factory InlineQueryPeerTypeChat.deserialize(Uint8List buffer) {
    // final result = InlineQueryPeerTypeChat();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd766c50a);
  }
}

/// Inline Query Peer Type Megagroup.
///
/// ID: `5ec4be43`.
class InlineQueryPeerTypeMegagroup extends InlineQueryPeerTypeBase {
  /// Inline Query Peer Type Megagroup constructor.
  const InlineQueryPeerTypeMegagroup() : super._();

  /// Deserialize.
  factory InlineQueryPeerTypeMegagroup.deserialize(Uint8List buffer) {
    // final result = InlineQueryPeerTypeMegagroup();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5ec4be43);
  }
}

/// Inline Query Peer Type Broadcast.
///
/// ID: `6334ee9a`.
class InlineQueryPeerTypeBroadcast extends InlineQueryPeerTypeBase {
  /// Inline Query Peer Type Broadcast constructor.
  const InlineQueryPeerTypeBroadcast() : super._();

  /// Deserialize.
  factory InlineQueryPeerTypeBroadcast.deserialize(Uint8List buffer) {
    // final result = InlineQueryPeerTypeBroadcast();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6334ee9a);
  }
}

/// Inline Query Peer Type Bot P M.
///
/// ID: `0e3b2d0c`.
class InlineQueryPeerTypeBotPM extends InlineQueryPeerTypeBase {
  /// Inline Query Peer Type Bot P M constructor.
  const InlineQueryPeerTypeBotPM() : super._();

  /// Deserialize.
  factory InlineQueryPeerTypeBotPM.deserialize(Uint8List buffer) {
    // final result = InlineQueryPeerTypeBotPM();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0e3b2d0c);
  }
}

/// Predicate Messages History Import.
abstract class MessagesHistoryImportBase extends TlConstructor {
  /// Predicate Messages History Import constructor.
  const MessagesHistoryImportBase._() : super._();
}

/// Messages History Import.
///
/// ID: `1662af0b`.
class MessagesHistoryImport extends MessagesHistoryImportBase {
  /// Messages History Import constructor.
  const MessagesHistoryImport({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesHistoryImport.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final result = MessagesHistoryImport(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1662af0b);
    buffer.writeLong(id);
  }
}

/// Predicate Messages History Import Parsed.
abstract class MessagesHistoryImportParsedBase extends TlConstructor {
  /// Predicate Messages History Import Parsed constructor.
  const MessagesHistoryImportParsedBase._() : super._();
}

/// Messages History Import Parsed.
///
/// ID: `5e0fb7b9`.
class MessagesHistoryImportParsed extends MessagesHistoryImportParsedBase {
  /// Messages History Import Parsed constructor.
  const MessagesHistoryImportParsed({
    required this.pm,
    required this.group,
    this.title,
  }) : super._();

  /// Deserialize.
  factory MessagesHistoryImportParsed.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pm = _readflags.0?true(buffer);
    // final group = _readflags.1?true(buffer);
    // final title = _readflags.2?string(buffer);
    // final result = MessagesHistoryImportParsed(flags: flags, pm: pm, group: group, title: title,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: pm,
      b01: group,
      b02: title != null,
    );

    return v;
  }

  /// pm: bit 0 of flags.0?true
  final bool pm;

  /// group: bit 1 of flags.1?true
  final bool group;

  /// Title.
  final String? title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5e0fb7b9);
    buffer.writeInt(flags);
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
  }
}

/// Predicate Messages Affected Found Messages.
abstract class MessagesAffectedFoundMessagesBase extends TlConstructor {
  /// Predicate Messages Affected Found Messages constructor.
  const MessagesAffectedFoundMessagesBase._() : super._();
}

/// Messages Affected Found Messages.
///
/// ID: `ef8d3e6c`.
class MessagesAffectedFoundMessages extends MessagesAffectedFoundMessagesBase {
  /// Messages Affected Found Messages constructor.
  const MessagesAffectedFoundMessages({
    required this.pts,
    required this.ptsCount,
    required this.offset,
    required this.messages,
  }) : super._();

  /// Deserialize.
  factory MessagesAffectedFoundMessages.deserialize(Uint8List buffer) {
    // final pts = _readint(buffer);
    // final ptsCount = _readint(buffer);
    // final offset = _readint(buffer);
    // final messages = _readVector<int>(buffer);
    // final result = MessagesAffectedFoundMessages(pts: pts, ptsCount: ptsCount, offset: offset, messages: messages,);

    // return result;

    throw Exception();
  }

  /// Pts.
  final int pts;

  /// Pts Count.
  final int ptsCount;

  /// Offset.
  final int offset;

  /// Messages.
  final List<int> messages;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xef8d3e6c);
    buffer.writeInt(pts);
    buffer.writeInt(ptsCount);
    buffer.writeInt(offset);
    buffer.writeVectorInt(messages);
  }
}

/// Predicate Chat Invite Importer.
abstract class ChatInviteImporterBase extends TlConstructor {
  /// Predicate Chat Invite Importer constructor.
  const ChatInviteImporterBase._() : super._();
}

/// Chat Invite Importer.
///
/// ID: `8c5adfd9`.
class ChatInviteImporter extends ChatInviteImporterBase {
  /// Chat Invite Importer constructor.
  const ChatInviteImporter({
    required this.requested,
    required this.viaChatlist,
    required this.userId,
    required this.date,
    this.about,
    this.approvedBy,
  }) : super._();

  /// Deserialize.
  factory ChatInviteImporter.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final requested = _readflags.0?true(buffer);
    // final viaChatlist = _readflags.3?true(buffer);
    // final userId = _readlong(buffer);
    // final date = _readint(buffer);
    // final about = _readflags.2?string(buffer);
    // final approvedBy = _readflags.1?long(buffer);
    // final result = ChatInviteImporter(flags: flags, requested: requested, viaChatlist: viaChatlist, userId: userId, date: date, about: about, approvedBy: approvedBy,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: requested,
      b03: viaChatlist,
      b02: about != null,
      b01: approvedBy != null,
    );

    return v;
  }

  /// requested: bit 0 of flags.0?true
  final bool requested;

  /// via_chatlist: bit 3 of flags.3?true
  final bool viaChatlist;

  /// User Id.
  final int userId;

  /// Date.
  final DateTime date;

  /// About.
  final String? about;

  /// Approved By.
  final int? approvedBy;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8c5adfd9);
    buffer.writeInt(flags);
    buffer.writeLong(userId);
    buffer.writeDateTime(date);
    final localAboutCopy = about;
    if (localAboutCopy != null) {
      buffer.writeString(localAboutCopy);
    }
    final localApprovedByCopy = approvedBy;
    if (localApprovedByCopy != null) {
      buffer.writeLong(localApprovedByCopy);
    }
  }
}

/// Predicate Messages Exported Chat Invites.
abstract class MessagesExportedChatInvitesBase extends TlConstructor {
  /// Predicate Messages Exported Chat Invites constructor.
  const MessagesExportedChatInvitesBase._() : super._();
}

/// Messages Exported Chat Invites.
///
/// ID: `bdc62dcc`.
class MessagesExportedChatInvites extends MessagesExportedChatInvitesBase {
  /// Messages Exported Chat Invites constructor.
  const MessagesExportedChatInvites({
    required this.count,
    required this.invites,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesExportedChatInvites.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final invites = _readVector<ExportedChatInvite>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesExportedChatInvites(count: count, invites: invites, users: users,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Invites.
  final List<ExportedChatInviteBase> invites;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbdc62dcc);
    buffer.writeInt(count);
    buffer.writeVectorObject(invites);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Messages Exported Chat Invite.
abstract class MessagesExportedChatInviteBase extends TlConstructor {
  /// Predicate Messages Exported Chat Invite constructor.
  const MessagesExportedChatInviteBase._() : super._();
}

/// Messages Exported Chat Invite.
///
/// ID: `1871be50`.
class MessagesExportedChatInvite extends MessagesExportedChatInviteBase {
  /// Messages Exported Chat Invite constructor.
  const MessagesExportedChatInvite({
    required this.invite,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesExportedChatInvite.deserialize(Uint8List buffer) {
    // final invite = _readExportedChatInvite(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesExportedChatInvite(invite: invite, users: users,);

    // return result;

    throw Exception();
  }

  /// Invite.
  final ExportedChatInviteBase invite;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1871be50);
    buffer.writeObject(invite);
    buffer.writeVectorObject(users);
  }
}

/// Messages Exported Chat Invite Replaced.
///
/// ID: `222600ef`.
class MessagesExportedChatInviteReplaced
    extends MessagesExportedChatInviteBase {
  /// Messages Exported Chat Invite Replaced constructor.
  const MessagesExportedChatInviteReplaced({
    required this.invite,
    required this.newInvite,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesExportedChatInviteReplaced.deserialize(Uint8List buffer) {
    // final invite = _readExportedChatInvite(buffer);
    // final newInvite = _readExportedChatInvite(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesExportedChatInviteReplaced(invite: invite, newInvite: newInvite, users: users,);

    // return result;

    throw Exception();
  }

  /// Invite.
  final ExportedChatInviteBase invite;

  /// New Invite.
  final ExportedChatInviteBase newInvite;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x222600ef);
    buffer.writeObject(invite);
    buffer.writeObject(newInvite);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Messages Chat Invite Importers.
abstract class MessagesChatInviteImportersBase extends TlConstructor {
  /// Predicate Messages Chat Invite Importers constructor.
  const MessagesChatInviteImportersBase._() : super._();
}

/// Messages Chat Invite Importers.
///
/// ID: `81b6b00a`.
class MessagesChatInviteImporters extends MessagesChatInviteImportersBase {
  /// Messages Chat Invite Importers constructor.
  const MessagesChatInviteImporters({
    required this.count,
    required this.importers,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesChatInviteImporters.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final importers = _readVector<ChatInviteImporter>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesChatInviteImporters(count: count, importers: importers, users: users,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Importers.
  final List<ChatInviteImporterBase> importers;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x81b6b00a);
    buffer.writeInt(count);
    buffer.writeVectorObject(importers);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Chat Admin With Invites.
abstract class ChatAdminWithInvitesBase extends TlConstructor {
  /// Predicate Chat Admin With Invites constructor.
  const ChatAdminWithInvitesBase._() : super._();
}

/// Chat Admin With Invites.
///
/// ID: `f2ecef23`.
class ChatAdminWithInvites extends ChatAdminWithInvitesBase {
  /// Chat Admin With Invites constructor.
  const ChatAdminWithInvites({
    required this.adminId,
    required this.invitesCount,
    required this.revokedInvitesCount,
  }) : super._();

  /// Deserialize.
  factory ChatAdminWithInvites.deserialize(Uint8List buffer) {
    // final adminId = _readlong(buffer);
    // final invitesCount = _readint(buffer);
    // final revokedInvitesCount = _readint(buffer);
    // final result = ChatAdminWithInvites(adminId: adminId, invitesCount: invitesCount, revokedInvitesCount: revokedInvitesCount,);

    // return result;

    throw Exception();
  }

  /// Admin Id.
  final int adminId;

  /// Invites Count.
  final int invitesCount;

  /// Revoked Invites Count.
  final int revokedInvitesCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf2ecef23);
    buffer.writeLong(adminId);
    buffer.writeInt(invitesCount);
    buffer.writeInt(revokedInvitesCount);
  }
}

/// Predicate Messages Chat Admins With Invites.
abstract class MessagesChatAdminsWithInvitesBase extends TlConstructor {
  /// Predicate Messages Chat Admins With Invites constructor.
  const MessagesChatAdminsWithInvitesBase._() : super._();
}

/// Messages Chat Admins With Invites.
///
/// ID: `b69b72d7`.
class MessagesChatAdminsWithInvites extends MessagesChatAdminsWithInvitesBase {
  /// Messages Chat Admins With Invites constructor.
  const MessagesChatAdminsWithInvites({
    required this.admins,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesChatAdminsWithInvites.deserialize(Uint8List buffer) {
    // final admins = _readVector<ChatAdminWithInvites>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesChatAdminsWithInvites(admins: admins, users: users,);

    // return result;

    throw Exception();
  }

  /// Admins.
  final List<ChatAdminWithInvitesBase> admins;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb69b72d7);
    buffer.writeVectorObject(admins);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Messages Checked History Import Peer.
abstract class MessagesCheckedHistoryImportPeerBase extends TlConstructor {
  /// Predicate Messages Checked History Import Peer constructor.
  const MessagesCheckedHistoryImportPeerBase._() : super._();
}

/// Messages Checked History Import Peer.
///
/// ID: `a24de717`.
class MessagesCheckedHistoryImportPeer
    extends MessagesCheckedHistoryImportPeerBase {
  /// Messages Checked History Import Peer constructor.
  const MessagesCheckedHistoryImportPeer({
    required this.confirmText,
  }) : super._();

  /// Deserialize.
  factory MessagesCheckedHistoryImportPeer.deserialize(Uint8List buffer) {
    // final confirmText = _readstring(buffer);
    // final result = MessagesCheckedHistoryImportPeer(confirmText: confirmText,);

    // return result;

    throw Exception();
  }

  /// Confirm Text.
  final String confirmText;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa24de717);
    buffer.writeString(confirmText);
  }
}

/// Predicate Phone Join As Peers.
abstract class PhoneJoinAsPeersBase extends TlConstructor {
  /// Predicate Phone Join As Peers constructor.
  const PhoneJoinAsPeersBase._() : super._();
}

/// Phone Join As Peers.
///
/// ID: `afe5623f`.
class PhoneJoinAsPeers extends PhoneJoinAsPeersBase {
  /// Phone Join As Peers constructor.
  const PhoneJoinAsPeers({
    required this.peers,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PhoneJoinAsPeers.deserialize(Uint8List buffer) {
    // final peers = _readVector<Peer>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = PhoneJoinAsPeers(peers: peers, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Peers.
  final List<PeerBase> peers;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xafe5623f);
    buffer.writeVectorObject(peers);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Phone Exported Group Call Invite.
abstract class PhoneExportedGroupCallInviteBase extends TlConstructor {
  /// Predicate Phone Exported Group Call Invite constructor.
  const PhoneExportedGroupCallInviteBase._() : super._();
}

/// Phone Exported Group Call Invite.
///
/// ID: `204bd158`.
class PhoneExportedGroupCallInvite extends PhoneExportedGroupCallInviteBase {
  /// Phone Exported Group Call Invite constructor.
  const PhoneExportedGroupCallInvite({
    required this.link,
  }) : super._();

  /// Deserialize.
  factory PhoneExportedGroupCallInvite.deserialize(Uint8List buffer) {
    // final link = _readstring(buffer);
    // final result = PhoneExportedGroupCallInvite(link: link,);

    // return result;

    throw Exception();
  }

  /// Link.
  final String link;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x204bd158);
    buffer.writeString(link);
  }
}

/// Predicate Group Call Participant Video Source Group.
abstract class GroupCallParticipantVideoSourceGroupBase extends TlConstructor {
  /// Predicate Group Call Participant Video Source Group constructor.
  const GroupCallParticipantVideoSourceGroupBase._() : super._();
}

/// Group Call Participant Video Source Group.
///
/// ID: `dcb118b7`.
class GroupCallParticipantVideoSourceGroup
    extends GroupCallParticipantVideoSourceGroupBase {
  /// Group Call Participant Video Source Group constructor.
  const GroupCallParticipantVideoSourceGroup({
    required this.semantics,
    required this.sources,
  }) : super._();

  /// Deserialize.
  factory GroupCallParticipantVideoSourceGroup.deserialize(Uint8List buffer) {
    // final semantics = _readstring(buffer);
    // final sources = _readVector<int>(buffer);
    // final result = GroupCallParticipantVideoSourceGroup(semantics: semantics, sources: sources,);

    // return result;

    throw Exception();
  }

  /// Semantics.
  final String semantics;

  /// Sources.
  final List<int> sources;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdcb118b7);
    buffer.writeString(semantics);
    buffer.writeVectorInt(sources);
  }
}

/// Predicate Group Call Participant Video.
abstract class GroupCallParticipantVideoBase extends TlConstructor {
  /// Predicate Group Call Participant Video constructor.
  const GroupCallParticipantVideoBase._() : super._();
}

/// Group Call Participant Video.
///
/// ID: `67753ac8`.
class GroupCallParticipantVideo extends GroupCallParticipantVideoBase {
  /// Group Call Participant Video constructor.
  const GroupCallParticipantVideo({
    required this.paused,
    required this.endpoint,
    required this.sourceGroups,
    this.audioSource,
  }) : super._();

  /// Deserialize.
  factory GroupCallParticipantVideo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final paused = _readflags.0?true(buffer);
    // final endpoint = _readstring(buffer);
    // final sourceGroups = _readVector<GroupCallParticipantVideoSourceGroup>(buffer);
    // final audioSource = _readflags.1?int(buffer);
    // final result = GroupCallParticipantVideo(flags: flags, paused: paused, endpoint: endpoint, sourceGroups: sourceGroups, audioSource: audioSource,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: paused,
      b01: audioSource != null,
    );

    return v;
  }

  /// paused: bit 0 of flags.0?true
  final bool paused;

  /// Endpoint.
  final String endpoint;

  /// Source Groups.
  final List<GroupCallParticipantVideoSourceGroupBase> sourceGroups;

  /// Audio Source.
  final int? audioSource;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x67753ac8);
    buffer.writeInt(flags);
    buffer.writeString(endpoint);
    buffer.writeVectorObject(sourceGroups);
    final localAudioSourceCopy = audioSource;
    if (localAudioSourceCopy != null) {
      buffer.writeInt(localAudioSourceCopy);
    }
  }
}

/// Predicate Stickers Suggested Short Name.
abstract class StickersSuggestedShortNameBase extends TlConstructor {
  /// Predicate Stickers Suggested Short Name constructor.
  const StickersSuggestedShortNameBase._() : super._();
}

/// Stickers Suggested Short Name.
///
/// ID: `85fea03f`.
class StickersSuggestedShortName extends StickersSuggestedShortNameBase {
  /// Stickers Suggested Short Name constructor.
  const StickersSuggestedShortName({
    required this.shortName,
  }) : super._();

  /// Deserialize.
  factory StickersSuggestedShortName.deserialize(Uint8List buffer) {
    // final shortName = _readstring(buffer);
    // final result = StickersSuggestedShortName(shortName: shortName,);

    // return result;

    throw Exception();
  }

  /// Short Name.
  final String shortName;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x85fea03f);
    buffer.writeString(shortName);
  }
}

/// Predicate Bot Command Scope.
abstract class BotCommandScopeBase extends TlConstructor {
  /// Predicate Bot Command Scope constructor.
  const BotCommandScopeBase._() : super._();
}

/// Bot Command Scope Default.
///
/// ID: `2f6cb2ab`.
class BotCommandScopeDefault extends BotCommandScopeBase {
  /// Bot Command Scope Default constructor.
  const BotCommandScopeDefault() : super._();

  /// Deserialize.
  factory BotCommandScopeDefault.deserialize(Uint8List buffer) {
    // final result = BotCommandScopeDefault();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2f6cb2ab);
  }
}

/// Bot Command Scope Users.
///
/// ID: `3c4f04d8`.
class BotCommandScopeUsers extends BotCommandScopeBase {
  /// Bot Command Scope Users constructor.
  const BotCommandScopeUsers() : super._();

  /// Deserialize.
  factory BotCommandScopeUsers.deserialize(Uint8List buffer) {
    // final result = BotCommandScopeUsers();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3c4f04d8);
  }
}

/// Bot Command Scope Chats.
///
/// ID: `6fe1a881`.
class BotCommandScopeChats extends BotCommandScopeBase {
  /// Bot Command Scope Chats constructor.
  const BotCommandScopeChats() : super._();

  /// Deserialize.
  factory BotCommandScopeChats.deserialize(Uint8List buffer) {
    // final result = BotCommandScopeChats();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6fe1a881);
  }
}

/// Bot Command Scope Chat Admins.
///
/// ID: `b9aa606a`.
class BotCommandScopeChatAdmins extends BotCommandScopeBase {
  /// Bot Command Scope Chat Admins constructor.
  const BotCommandScopeChatAdmins() : super._();

  /// Deserialize.
  factory BotCommandScopeChatAdmins.deserialize(Uint8List buffer) {
    // final result = BotCommandScopeChatAdmins();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb9aa606a);
  }
}

/// Bot Command Scope Peer.
///
/// ID: `db9d897d`.
class BotCommandScopePeer extends BotCommandScopeBase {
  /// Bot Command Scope Peer constructor.
  const BotCommandScopePeer({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory BotCommandScopePeer.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = BotCommandScopePeer(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdb9d897d);
    buffer.writeObject(peer);
  }
}

/// Bot Command Scope Peer Admins.
///
/// ID: `3fd863d1`.
class BotCommandScopePeerAdmins extends BotCommandScopeBase {
  /// Bot Command Scope Peer Admins constructor.
  const BotCommandScopePeerAdmins({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory BotCommandScopePeerAdmins.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = BotCommandScopePeerAdmins(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3fd863d1);
    buffer.writeObject(peer);
  }
}

/// Bot Command Scope Peer User.
///
/// ID: `0a1321f3`.
class BotCommandScopePeerUser extends BotCommandScopeBase {
  /// Bot Command Scope Peer User constructor.
  const BotCommandScopePeerUser({
    required this.peer,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory BotCommandScopePeerUser.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final userId = _readInputUser(buffer);
    // final result = BotCommandScopePeerUser(peer: peer, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// User Id.
  final InputUserBase userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0a1321f3);
    buffer.writeObject(peer);
    buffer.writeObject(userId);
  }
}

/// Predicate Account Reset Password Result.
abstract class AccountResetPasswordResultBase extends TlConstructor {
  /// Predicate Account Reset Password Result constructor.
  const AccountResetPasswordResultBase._() : super._();
}

/// Account Reset Password Failed Wait.
///
/// ID: `e3779861`.
class AccountResetPasswordFailedWait extends AccountResetPasswordResultBase {
  /// Account Reset Password Failed Wait constructor.
  const AccountResetPasswordFailedWait({
    required this.retryDate,
  }) : super._();

  /// Deserialize.
  factory AccountResetPasswordFailedWait.deserialize(Uint8List buffer) {
    // final retryDate = _readint(buffer);
    // final result = AccountResetPasswordFailedWait(retryDate: retryDate,);

    // return result;

    throw Exception();
  }

  /// Retry Date.
  final DateTime retryDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe3779861);
    buffer.writeDateTime(retryDate);
  }
}

/// Account Reset Password Requested Wait.
///
/// ID: `e9effc7d`.
class AccountResetPasswordRequestedWait extends AccountResetPasswordResultBase {
  /// Account Reset Password Requested Wait constructor.
  const AccountResetPasswordRequestedWait({
    required this.untilDate,
  }) : super._();

  /// Deserialize.
  factory AccountResetPasswordRequestedWait.deserialize(Uint8List buffer) {
    // final untilDate = _readint(buffer);
    // final result = AccountResetPasswordRequestedWait(untilDate: untilDate,);

    // return result;

    throw Exception();
  }

  /// Until Date.
  final DateTime untilDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe9effc7d);
    buffer.writeDateTime(untilDate);
  }
}

/// Account Reset Password Ok.
///
/// ID: `e926d63e`.
class AccountResetPasswordOk extends AccountResetPasswordResultBase {
  /// Account Reset Password Ok constructor.
  const AccountResetPasswordOk() : super._();

  /// Deserialize.
  factory AccountResetPasswordOk.deserialize(Uint8List buffer) {
    // final result = AccountResetPasswordOk();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe926d63e);
  }
}

/// Predicate Sponsored Message.
abstract class SponsoredMessageBase extends TlConstructor {
  /// Predicate Sponsored Message constructor.
  const SponsoredMessageBase._() : super._();
}

/// Sponsored Message.
///
/// ID: `ed5383f7`.
class SponsoredMessage extends SponsoredMessageBase {
  /// Sponsored Message constructor.
  const SponsoredMessage({
    required this.recommended,
    required this.showPeerPhoto,
    required this.randomId,
    this.fromId,
    this.chatInvite,
    this.chatInviteHash,
    this.channelPost,
    this.startParam,
    this.webpage,
    this.app,
    required this.message,
    this.entities,
    this.buttonText,
    this.sponsorInfo,
    this.additionalInfo,
  }) : super._();

  /// Deserialize.
  factory SponsoredMessage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final recommended = _readflags.5?true(buffer);
    // final showPeerPhoto = _readflags.6?true(buffer);
    // final randomId = _readbytes(buffer);
    // final fromId = _readflags.3?Peer(buffer);
    // final chatInvite = _readflags.4?ChatInvite(buffer);
    // final chatInviteHash = _readflags.4?string(buffer);
    // final channelPost = _readflags.2?int(buffer);
    // final startParam = _readflags.0?string(buffer);
    // final webpage = _readflags.9?SponsoredWebPage(buffer);
    // final app = _readflags.10?BotApp(buffer);
    // final message = _readstring(buffer);
    // final entities = _readflags.1?Vector<MessageEntity>(buffer);
    // final buttonText = _readflags.11?string(buffer);
    // final sponsorInfo = _readflags.7?string(buffer);
    // final additionalInfo = _readflags.8?string(buffer);
    // final result = SponsoredMessage(flags: flags, recommended: recommended, showPeerPhoto: showPeerPhoto, randomId: randomId, fromId: fromId, chatInvite: chatInvite, chatInviteHash: chatInviteHash, channelPost: channelPost, startParam: startParam, webpage: webpage, app: app, message: message, entities: entities, buttonText: buttonText, sponsorInfo: sponsorInfo, additionalInfo: additionalInfo,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b05: recommended,
      b06: showPeerPhoto,
      b03: fromId != null,
      b04: chatInvite != null || chatInviteHash != null,
      b02: channelPost != null,
      b00: startParam != null,
      b09: webpage != null,
      b10: app != null,
      b01: entities != null,
      b11: buttonText != null,
      b07: sponsorInfo != null,
      b08: additionalInfo != null,
    );

    return v;
  }

  /// recommended: bit 5 of flags.5?true
  final bool recommended;

  /// show_peer_photo: bit 6 of flags.6?true
  final bool showPeerPhoto;

  /// Random Id.
  final Uint8List randomId;

  /// From Id.
  final PeerBase? fromId;

  /// Chat Invite.
  final ChatInviteBase? chatInvite;

  /// Chat Invite Hash.
  final String? chatInviteHash;

  /// Channel Post.
  final int? channelPost;

  /// Start Param.
  final String? startParam;

  /// Webpage.
  final SponsoredWebPageBase? webpage;

  /// App.
  final BotAppBase? app;

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Button Text.
  final String? buttonText;

  /// Sponsor Info.
  final String? sponsorInfo;

  /// Additional Info.
  final String? additionalInfo;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xed5383f7);
    buffer.writeInt(flags);
    buffer.writeBytes(randomId);
    final localFromIdCopy = fromId;
    if (localFromIdCopy != null) {
      buffer.writeObject(localFromIdCopy);
    }
    final localChatInviteCopy = chatInvite;
    if (localChatInviteCopy != null) {
      buffer.writeObject(localChatInviteCopy);
    }
    final localChatInviteHashCopy = chatInviteHash;
    if (localChatInviteHashCopy != null) {
      buffer.writeString(localChatInviteHashCopy);
    }
    final localChannelPostCopy = channelPost;
    if (localChannelPostCopy != null) {
      buffer.writeInt(localChannelPostCopy);
    }
    final localStartParamCopy = startParam;
    if (localStartParamCopy != null) {
      buffer.writeString(localStartParamCopy);
    }
    final localWebpageCopy = webpage;
    if (localWebpageCopy != null) {
      buffer.writeObject(localWebpageCopy);
    }
    final localAppCopy = app;
    if (localAppCopy != null) {
      buffer.writeObject(localAppCopy);
    }
    buffer.writeString(message);
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localButtonTextCopy = buttonText;
    if (localButtonTextCopy != null) {
      buffer.writeString(localButtonTextCopy);
    }
    final localSponsorInfoCopy = sponsorInfo;
    if (localSponsorInfoCopy != null) {
      buffer.writeString(localSponsorInfoCopy);
    }
    final localAdditionalInfoCopy = additionalInfo;
    if (localAdditionalInfoCopy != null) {
      buffer.writeString(localAdditionalInfoCopy);
    }
  }
}

/// Predicate Messages Sponsored Messages.
abstract class MessagesSponsoredMessagesBase extends TlConstructor {
  /// Predicate Messages Sponsored Messages constructor.
  const MessagesSponsoredMessagesBase._() : super._();
}

/// Messages Sponsored Messages.
///
/// ID: `c9ee1d87`.
class MessagesSponsoredMessages extends MessagesSponsoredMessagesBase {
  /// Messages Sponsored Messages constructor.
  const MessagesSponsoredMessages({
    this.postsBetween,
    required this.messages,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesSponsoredMessages.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final postsBetween = _readflags.0?int(buffer);
    // final messages = _readVector<SponsoredMessage>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesSponsoredMessages(flags: flags, postsBetween: postsBetween, messages: messages, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: postsBetween != null,
    );

    return v;
  }

  /// Posts Between.
  final int? postsBetween;

  /// Messages.
  final List<SponsoredMessageBase> messages;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc9ee1d87);
    buffer.writeInt(flags);
    final localPostsBetweenCopy = postsBetween;
    if (localPostsBetweenCopy != null) {
      buffer.writeInt(localPostsBetweenCopy);
    }
    buffer.writeVectorObject(messages);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Messages Sponsored Messages Empty.
///
/// ID: `1839490f`.
class MessagesSponsoredMessagesEmpty extends MessagesSponsoredMessagesBase {
  /// Messages Sponsored Messages Empty constructor.
  const MessagesSponsoredMessagesEmpty() : super._();

  /// Deserialize.
  factory MessagesSponsoredMessagesEmpty.deserialize(Uint8List buffer) {
    // final result = MessagesSponsoredMessagesEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1839490f);
  }
}

/// Predicate Search Results Calendar Period.
abstract class SearchResultsCalendarPeriodBase extends TlConstructor {
  /// Predicate Search Results Calendar Period constructor.
  const SearchResultsCalendarPeriodBase._() : super._();
}

/// Search Results Calendar Period.
///
/// ID: `c9b0539f`.
class SearchResultsCalendarPeriod extends SearchResultsCalendarPeriodBase {
  /// Search Results Calendar Period constructor.
  const SearchResultsCalendarPeriod({
    required this.date,
    required this.minMsgId,
    required this.maxMsgId,
    required this.count,
  }) : super._();

  /// Deserialize.
  factory SearchResultsCalendarPeriod.deserialize(Uint8List buffer) {
    // final date = _readint(buffer);
    // final minMsgId = _readint(buffer);
    // final maxMsgId = _readint(buffer);
    // final count = _readint(buffer);
    // final result = SearchResultsCalendarPeriod(date: date, minMsgId: minMsgId, maxMsgId: maxMsgId, count: count,);

    // return result;

    throw Exception();
  }

  /// Date.
  final DateTime date;

  /// Min Msg Id.
  final int minMsgId;

  /// Max Msg Id.
  final int maxMsgId;

  /// Count.
  final int count;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc9b0539f);
    buffer.writeDateTime(date);
    buffer.writeInt(minMsgId);
    buffer.writeInt(maxMsgId);
    buffer.writeInt(count);
  }
}

/// Predicate Messages Search Results Calendar.
abstract class MessagesSearchResultsCalendarBase extends TlConstructor {
  /// Predicate Messages Search Results Calendar constructor.
  const MessagesSearchResultsCalendarBase._() : super._();
}

/// Messages Search Results Calendar.
///
/// ID: `147ee23c`.
class MessagesSearchResultsCalendar extends MessagesSearchResultsCalendarBase {
  /// Messages Search Results Calendar constructor.
  const MessagesSearchResultsCalendar({
    required this.inexact,
    required this.count,
    required this.minDate,
    required this.minMsgId,
    this.offsetIdOffset,
    required this.periods,
    required this.messages,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesSearchResultsCalendar.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final inexact = _readflags.0?true(buffer);
    // final count = _readint(buffer);
    // final minDate = _readint(buffer);
    // final minMsgId = _readint(buffer);
    // final offsetIdOffset = _readflags.1?int(buffer);
    // final periods = _readVector<SearchResultsCalendarPeriod>(buffer);
    // final messages = _readVector<Message>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesSearchResultsCalendar(flags: flags, inexact: inexact, count: count, minDate: minDate, minMsgId: minMsgId, offsetIdOffset: offsetIdOffset, periods: periods, messages: messages, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: inexact,
      b01: offsetIdOffset != null,
    );

    return v;
  }

  /// inexact: bit 0 of flags.0?true
  final bool inexact;

  /// Count.
  final int count;

  /// Min Date.
  final DateTime minDate;

  /// Min Msg Id.
  final int minMsgId;

  /// Offset Id Offset.
  final int? offsetIdOffset;

  /// Periods.
  final List<SearchResultsCalendarPeriodBase> periods;

  /// Messages.
  final List<MessageBase> messages;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x147ee23c);
    buffer.writeInt(flags);
    buffer.writeInt(count);
    buffer.writeDateTime(minDate);
    buffer.writeInt(minMsgId);
    final localOffsetIdOffsetCopy = offsetIdOffset;
    if (localOffsetIdOffsetCopy != null) {
      buffer.writeInt(localOffsetIdOffsetCopy);
    }
    buffer.writeVectorObject(periods);
    buffer.writeVectorObject(messages);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Search Results Position.
abstract class SearchResultsPositionBase extends TlConstructor {
  /// Predicate Search Results Position constructor.
  const SearchResultsPositionBase._() : super._();
}

/// Search Result Position.
///
/// ID: `7f648b67`.
class SearchResultPosition extends SearchResultsPositionBase {
  /// Search Result Position constructor.
  const SearchResultPosition({
    required this.msgId,
    required this.date,
    required this.offset,
  }) : super._();

  /// Deserialize.
  factory SearchResultPosition.deserialize(Uint8List buffer) {
    // final msgId = _readint(buffer);
    // final date = _readint(buffer);
    // final offset = _readint(buffer);
    // final result = SearchResultPosition(msgId: msgId, date: date, offset: offset,);

    // return result;

    throw Exception();
  }

  /// Msg Id.
  final int msgId;

  /// Date.
  final DateTime date;

  /// Offset.
  final int offset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7f648b67);
    buffer.writeInt(msgId);
    buffer.writeDateTime(date);
    buffer.writeInt(offset);
  }
}

/// Predicate Messages Search Results Positions.
abstract class MessagesSearchResultsPositionsBase extends TlConstructor {
  /// Predicate Messages Search Results Positions constructor.
  const MessagesSearchResultsPositionsBase._() : super._();
}

/// Messages Search Results Positions.
///
/// ID: `53b22baf`.
class MessagesSearchResultsPositions
    extends MessagesSearchResultsPositionsBase {
  /// Messages Search Results Positions constructor.
  const MessagesSearchResultsPositions({
    required this.count,
    required this.positions,
  }) : super._();

  /// Deserialize.
  factory MessagesSearchResultsPositions.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final positions = _readVector<SearchResultsPosition>(buffer);
    // final result = MessagesSearchResultsPositions(count: count, positions: positions,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Positions.
  final List<SearchResultsPositionBase> positions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x53b22baf);
    buffer.writeInt(count);
    buffer.writeVectorObject(positions);
  }
}

/// Predicate Channels Send As Peers.
abstract class ChannelsSendAsPeersBase extends TlConstructor {
  /// Predicate Channels Send As Peers constructor.
  const ChannelsSendAsPeersBase._() : super._();
}

/// Channels Send As Peers.
///
/// ID: `f496b0c6`.
class ChannelsSendAsPeers extends ChannelsSendAsPeersBase {
  /// Channels Send As Peers constructor.
  const ChannelsSendAsPeers({
    required this.peers,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ChannelsSendAsPeers.deserialize(Uint8List buffer) {
    // final peers = _readVector<SendAsPeer>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ChannelsSendAsPeers(peers: peers, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Peers.
  final List<SendAsPeerBase> peers;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf496b0c6);
    buffer.writeVectorObject(peers);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Users User Full.
abstract class UsersUserFullBase extends TlConstructor {
  /// Predicate Users User Full constructor.
  const UsersUserFullBase._() : super._();
}

/// Users User Full.
///
/// ID: `3b6d152e`.
class UsersUserFull extends UsersUserFullBase {
  /// Users User Full constructor.
  const UsersUserFull({
    required this.fullUser,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory UsersUserFull.deserialize(Uint8List buffer) {
    // final fullUser = _readUserFull(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = UsersUserFull(fullUser: fullUser, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Full User.
  final UserFullBase fullUser;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3b6d152e);
    buffer.writeObject(fullUser);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Messages Peer Settings.
abstract class MessagesPeerSettingsBase extends TlConstructor {
  /// Predicate Messages Peer Settings constructor.
  const MessagesPeerSettingsBase._() : super._();
}

/// Messages Peer Settings.
///
/// ID: `6880b94d`.
class MessagesPeerSettings extends MessagesPeerSettingsBase {
  /// Messages Peer Settings constructor.
  const MessagesPeerSettings({
    required this.settings,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesPeerSettings.deserialize(Uint8List buffer) {
    // final settings = _readPeerSettings(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesPeerSettings(settings: settings, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Settings.
  final PeerSettingsBase settings;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6880b94d);
    buffer.writeObject(settings);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Auth Logged Out.
abstract class AuthLoggedOutBase extends TlConstructor {
  /// Predicate Auth Logged Out constructor.
  const AuthLoggedOutBase._() : super._();
}

/// Auth Logged Out.
///
/// ID: `c3a2835f`.
class AuthLoggedOut extends AuthLoggedOutBase {
  /// Auth Logged Out constructor.
  const AuthLoggedOut({
    this.futureAuthToken,
  }) : super._();

  /// Deserialize.
  factory AuthLoggedOut.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final futureAuthToken = _readflags.0?bytes(buffer);
    // final result = AuthLoggedOut(flags: flags, futureAuthToken: futureAuthToken,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: futureAuthToken != null,
    );

    return v;
  }

  /// Future Auth Token.
  final Uint8List? futureAuthToken;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc3a2835f);
    buffer.writeInt(flags);
    final localFutureAuthTokenCopy = futureAuthToken;
    if (localFutureAuthTokenCopy != null) {
      buffer.writeBytes(localFutureAuthTokenCopy);
    }
  }
}

/// Predicate Reaction Count.
abstract class ReactionCountBase extends TlConstructor {
  /// Predicate Reaction Count constructor.
  const ReactionCountBase._() : super._();
}

/// Reaction Count.
///
/// ID: `a3d1cb80`.
class ReactionCount extends ReactionCountBase {
  /// Reaction Count constructor.
  const ReactionCount({
    this.chosenOrder,
    required this.reaction,
    required this.count,
  }) : super._();

  /// Deserialize.
  factory ReactionCount.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final chosenOrder = _readflags.0?int(buffer);
    // final reaction = _readReaction(buffer);
    // final count = _readint(buffer);
    // final result = ReactionCount(flags: flags, chosenOrder: chosenOrder, reaction: reaction, count: count,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: chosenOrder != null,
    );

    return v;
  }

  /// Chosen Order.
  final int? chosenOrder;

  /// Reaction.
  final ReactionBase reaction;

  /// Count.
  final int count;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa3d1cb80);
    buffer.writeInt(flags);
    final localChosenOrderCopy = chosenOrder;
    if (localChosenOrderCopy != null) {
      buffer.writeInt(localChosenOrderCopy);
    }
    buffer.writeObject(reaction);
    buffer.writeInt(count);
  }
}

/// Predicate Message Reactions.
abstract class MessageReactionsBase extends TlConstructor {
  /// Predicate Message Reactions constructor.
  const MessageReactionsBase._() : super._();
}

/// Message Reactions.
///
/// ID: `4f2b9479`.
class MessageReactions extends MessageReactionsBase {
  /// Message Reactions constructor.
  const MessageReactions({
    required this.min,
    required this.canSeeList,
    required this.results,
    this.recentReactions,
  }) : super._();

  /// Deserialize.
  factory MessageReactions.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final min = _readflags.0?true(buffer);
    // final canSeeList = _readflags.2?true(buffer);
    // final results = _readVector<ReactionCount>(buffer);
    // final recentReactions = _readflags.1?Vector<MessagePeerReaction>(buffer);
    // final result = MessageReactions(flags: flags, min: min, canSeeList: canSeeList, results: results, recentReactions: recentReactions,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: min,
      b02: canSeeList,
      b01: recentReactions != null,
    );

    return v;
  }

  /// min: bit 0 of flags.0?true
  final bool min;

  /// can_see_list: bit 2 of flags.2?true
  final bool canSeeList;

  /// Results.
  final List<ReactionCountBase> results;

  /// Recent Reactions.
  final List<MessagePeerReactionBase>? recentReactions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4f2b9479);
    buffer.writeInt(flags);
    buffer.writeVectorObject(results);
    final localRecentReactionsCopy = recentReactions;
    if (localRecentReactionsCopy != null) {
      buffer.writeVectorObject(localRecentReactionsCopy);
    }
  }
}

/// Predicate Messages Message Reactions List.
abstract class MessagesMessageReactionsListBase extends TlConstructor {
  /// Predicate Messages Message Reactions List constructor.
  const MessagesMessageReactionsListBase._() : super._();
}

/// Messages Message Reactions List.
///
/// ID: `31bd492d`.
class MessagesMessageReactionsList extends MessagesMessageReactionsListBase {
  /// Messages Message Reactions List constructor.
  const MessagesMessageReactionsList({
    required this.count,
    required this.reactions,
    required this.chats,
    required this.users,
    this.nextOffset,
  }) : super._();

  /// Deserialize.
  factory MessagesMessageReactionsList.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final count = _readint(buffer);
    // final reactions = _readVector<MessagePeerReaction>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final nextOffset = _readflags.0?string(buffer);
    // final result = MessagesMessageReactionsList(flags: flags, count: count, reactions: reactions, chats: chats, users: users, nextOffset: nextOffset,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: nextOffset != null,
    );

    return v;
  }

  /// Count.
  final int count;

  /// Reactions.
  final List<MessagePeerReactionBase> reactions;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Next Offset.
  final String? nextOffset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x31bd492d);
    buffer.writeInt(flags);
    buffer.writeInt(count);
    buffer.writeVectorObject(reactions);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
    final localNextOffsetCopy = nextOffset;
    if (localNextOffsetCopy != null) {
      buffer.writeString(localNextOffsetCopy);
    }
  }
}

/// Predicate Available Reaction.
abstract class AvailableReactionBase extends TlConstructor {
  /// Predicate Available Reaction constructor.
  const AvailableReactionBase._() : super._();
}

/// Available Reaction.
///
/// ID: `c077ec01`.
class AvailableReaction extends AvailableReactionBase {
  /// Available Reaction constructor.
  const AvailableReaction({
    required this.inactive,
    required this.premium,
    required this.reaction,
    required this.title,
    required this.staticIcon,
    required this.appearAnimation,
    required this.selectAnimation,
    required this.activateAnimation,
    required this.effectAnimation,
    this.aroundAnimation,
    this.centerIcon,
  }) : super._();

  /// Deserialize.
  factory AvailableReaction.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final inactive = _readflags.0?true(buffer);
    // final premium = _readflags.2?true(buffer);
    // final reaction = _readstring(buffer);
    // final title = _readstring(buffer);
    // final staticIcon = _readDocument(buffer);
    // final appearAnimation = _readDocument(buffer);
    // final selectAnimation = _readDocument(buffer);
    // final activateAnimation = _readDocument(buffer);
    // final effectAnimation = _readDocument(buffer);
    // final aroundAnimation = _readflags.1?Document(buffer);
    // final centerIcon = _readflags.1?Document(buffer);
    // final result = AvailableReaction(flags: flags, inactive: inactive, premium: premium, reaction: reaction, title: title, staticIcon: staticIcon, appearAnimation: appearAnimation, selectAnimation: selectAnimation, activateAnimation: activateAnimation, effectAnimation: effectAnimation, aroundAnimation: aroundAnimation, centerIcon: centerIcon,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: inactive,
      b02: premium,
      b01: aroundAnimation != null || centerIcon != null,
    );

    return v;
  }

  /// inactive: bit 0 of flags.0?true
  final bool inactive;

  /// premium: bit 2 of flags.2?true
  final bool premium;

  /// Reaction.
  final String reaction;

  /// Title.
  final String title;

  /// Static Icon.
  final DocumentBase staticIcon;

  /// Appear Animation.
  final DocumentBase appearAnimation;

  /// Select Animation.
  final DocumentBase selectAnimation;

  /// Activate Animation.
  final DocumentBase activateAnimation;

  /// Effect Animation.
  final DocumentBase effectAnimation;

  /// Around Animation.
  final DocumentBase? aroundAnimation;

  /// Center Icon.
  final DocumentBase? centerIcon;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc077ec01);
    buffer.writeInt(flags);
    buffer.writeString(reaction);
    buffer.writeString(title);
    buffer.writeObject(staticIcon);
    buffer.writeObject(appearAnimation);
    buffer.writeObject(selectAnimation);
    buffer.writeObject(activateAnimation);
    buffer.writeObject(effectAnimation);
    final localAroundAnimationCopy = aroundAnimation;
    if (localAroundAnimationCopy != null) {
      buffer.writeObject(localAroundAnimationCopy);
    }
    final localCenterIconCopy = centerIcon;
    if (localCenterIconCopy != null) {
      buffer.writeObject(localCenterIconCopy);
    }
  }
}

/// Predicate Messages Available Reactions.
abstract class MessagesAvailableReactionsBase extends TlConstructor {
  /// Predicate Messages Available Reactions constructor.
  const MessagesAvailableReactionsBase._() : super._();
}

/// Messages Available Reactions Not Modified.
///
/// ID: `9f071957`.
class MessagesAvailableReactionsNotModified
    extends MessagesAvailableReactionsBase {
  /// Messages Available Reactions Not Modified constructor.
  const MessagesAvailableReactionsNotModified() : super._();

  /// Deserialize.
  factory MessagesAvailableReactionsNotModified.deserialize(Uint8List buffer) {
    // final result = MessagesAvailableReactionsNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9f071957);
  }
}

/// Messages Available Reactions.
///
/// ID: `768e3aad`.
class MessagesAvailableReactions extends MessagesAvailableReactionsBase {
  /// Messages Available Reactions constructor.
  const MessagesAvailableReactions({
    required this.hash,
    required this.reactions,
  }) : super._();

  /// Deserialize.
  factory MessagesAvailableReactions.deserialize(Uint8List buffer) {
    // final hash = _readint(buffer);
    // final reactions = _readVector<AvailableReaction>(buffer);
    // final result = MessagesAvailableReactions(hash: hash, reactions: reactions,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Reactions.
  final List<AvailableReactionBase> reactions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x768e3aad);
    buffer.writeInt(hash);
    buffer.writeVectorObject(reactions);
  }
}

/// Predicate Message Peer Reaction.
abstract class MessagePeerReactionBase extends TlConstructor {
  /// Predicate Message Peer Reaction constructor.
  const MessagePeerReactionBase._() : super._();
}

/// Message Peer Reaction.
///
/// ID: `8c79b63c`.
class MessagePeerReaction extends MessagePeerReactionBase {
  /// Message Peer Reaction constructor.
  const MessagePeerReaction({
    required this.big,
    required this.unread,
    required this.my,
    required this.peerId,
    required this.date,
    required this.reaction,
  }) : super._();

  /// Deserialize.
  factory MessagePeerReaction.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final big = _readflags.0?true(buffer);
    // final unread = _readflags.1?true(buffer);
    // final my = _readflags.2?true(buffer);
    // final peerId = _readPeer(buffer);
    // final date = _readint(buffer);
    // final reaction = _readReaction(buffer);
    // final result = MessagePeerReaction(flags: flags, big: big, unread: unread, my: my, peerId: peerId, date: date, reaction: reaction,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: big,
      b01: unread,
      b02: my,
    );

    return v;
  }

  /// big: bit 0 of flags.0?true
  final bool big;

  /// unread: bit 1 of flags.1?true
  final bool unread;

  /// my: bit 2 of flags.2?true
  final bool my;

  /// Peer Id.
  final PeerBase peerId;

  /// Date.
  final DateTime date;

  /// Reaction.
  final ReactionBase reaction;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8c79b63c);
    buffer.writeInt(flags);
    buffer.writeObject(peerId);
    buffer.writeDateTime(date);
    buffer.writeObject(reaction);
  }
}

/// Predicate Group Call Stream Channel.
abstract class GroupCallStreamChannelBase extends TlConstructor {
  /// Predicate Group Call Stream Channel constructor.
  const GroupCallStreamChannelBase._() : super._();
}

/// Group Call Stream Channel.
///
/// ID: `80eb48af`.
class GroupCallStreamChannel extends GroupCallStreamChannelBase {
  /// Group Call Stream Channel constructor.
  const GroupCallStreamChannel({
    required this.channel,
    required this.scale,
    required this.lastTimestampMs,
  }) : super._();

  /// Deserialize.
  factory GroupCallStreamChannel.deserialize(Uint8List buffer) {
    // final channel = _readint(buffer);
    // final scale = _readint(buffer);
    // final lastTimestampMs = _readlong(buffer);
    // final result = GroupCallStreamChannel(channel: channel, scale: scale, lastTimestampMs: lastTimestampMs,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final int channel;

  /// Scale.
  final int scale;

  /// Last Timestamp Ms.
  final int lastTimestampMs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x80eb48af);
    buffer.writeInt(channel);
    buffer.writeInt(scale);
    buffer.writeLong(lastTimestampMs);
  }
}

/// Predicate Phone Group Call Stream Channels.
abstract class PhoneGroupCallStreamChannelsBase extends TlConstructor {
  /// Predicate Phone Group Call Stream Channels constructor.
  const PhoneGroupCallStreamChannelsBase._() : super._();
}

/// Phone Group Call Stream Channels.
///
/// ID: `d0e482b2`.
class PhoneGroupCallStreamChannels extends PhoneGroupCallStreamChannelsBase {
  /// Phone Group Call Stream Channels constructor.
  const PhoneGroupCallStreamChannels({
    required this.channels,
  }) : super._();

  /// Deserialize.
  factory PhoneGroupCallStreamChannels.deserialize(Uint8List buffer) {
    // final channels = _readVector<GroupCallStreamChannel>(buffer);
    // final result = PhoneGroupCallStreamChannels(channels: channels,);

    // return result;

    throw Exception();
  }

  /// Channels.
  final List<GroupCallStreamChannelBase> channels;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd0e482b2);
    buffer.writeVectorObject(channels);
  }
}

/// Predicate Phone Group Call Stream Rtmp Url.
abstract class PhoneGroupCallStreamRtmpUrlBase extends TlConstructor {
  /// Predicate Phone Group Call Stream Rtmp Url constructor.
  const PhoneGroupCallStreamRtmpUrlBase._() : super._();
}

/// Phone Group Call Stream Rtmp Url.
///
/// ID: `2dbf3432`.
class PhoneGroupCallStreamRtmpUrl extends PhoneGroupCallStreamRtmpUrlBase {
  /// Phone Group Call Stream Rtmp Url constructor.
  const PhoneGroupCallStreamRtmpUrl({
    required this.url,
    required this.key,
  }) : super._();

  /// Deserialize.
  factory PhoneGroupCallStreamRtmpUrl.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final key = _readstring(buffer);
    // final result = PhoneGroupCallStreamRtmpUrl(url: url, key: key,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Key.
  final String key;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2dbf3432);
    buffer.writeString(url);
    buffer.writeString(key);
  }
}

/// Predicate Attach Menu Bot Icon Color.
abstract class AttachMenuBotIconColorBase extends TlConstructor {
  /// Predicate Attach Menu Bot Icon Color constructor.
  const AttachMenuBotIconColorBase._() : super._();
}

/// Attach Menu Bot Icon Color.
///
/// ID: `4576f3f0`.
class AttachMenuBotIconColor extends AttachMenuBotIconColorBase {
  /// Attach Menu Bot Icon Color constructor.
  const AttachMenuBotIconColor({
    required this.name,
    required this.color,
  }) : super._();

  /// Deserialize.
  factory AttachMenuBotIconColor.deserialize(Uint8List buffer) {
    // final name = _readstring(buffer);
    // final color = _readint(buffer);
    // final result = AttachMenuBotIconColor(name: name, color: color,);

    // return result;

    throw Exception();
  }

  /// Name.
  final String name;

  /// Color.
  final int color;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4576f3f0);
    buffer.writeString(name);
    buffer.writeInt(color);
  }
}

/// Predicate Attach Menu Bot Icon.
abstract class AttachMenuBotIconBase extends TlConstructor {
  /// Predicate Attach Menu Bot Icon constructor.
  const AttachMenuBotIconBase._() : super._();
}

/// Attach Menu Bot Icon.
///
/// ID: `b2a7386b`.
class AttachMenuBotIcon extends AttachMenuBotIconBase {
  /// Attach Menu Bot Icon constructor.
  const AttachMenuBotIcon({
    required this.name,
    required this.icon,
    this.colors,
  }) : super._();

  /// Deserialize.
  factory AttachMenuBotIcon.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final name = _readstring(buffer);
    // final icon = _readDocument(buffer);
    // final colors = _readflags.0?Vector<AttachMenuBotIconColor>(buffer);
    // final result = AttachMenuBotIcon(flags: flags, name: name, icon: icon, colors: colors,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: colors != null,
    );

    return v;
  }

  /// Name.
  final String name;

  /// Icon.
  final DocumentBase icon;

  /// Colors.
  final List<AttachMenuBotIconColorBase>? colors;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb2a7386b);
    buffer.writeInt(flags);
    buffer.writeString(name);
    buffer.writeObject(icon);
    final localColorsCopy = colors;
    if (localColorsCopy != null) {
      buffer.writeVectorObject(localColorsCopy);
    }
  }
}

/// Predicate Attach Menu Bot.
abstract class AttachMenuBotBase extends TlConstructor {
  /// Predicate Attach Menu Bot constructor.
  const AttachMenuBotBase._() : super._();
}

/// Attach Menu Bot.
///
/// ID: `d90d8dfe`.
class AttachMenuBot extends AttachMenuBotBase {
  /// Attach Menu Bot constructor.
  const AttachMenuBot({
    required this.inactive,
    required this.hasSettings,
    required this.requestWriteAccess,
    required this.showInAttachMenu,
    required this.showInSideMenu,
    required this.sideMenuDisclaimerNeeded,
    required this.botId,
    required this.shortName,
    this.peerTypes,
    required this.icons,
  }) : super._();

  /// Deserialize.
  factory AttachMenuBot.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final inactive = _readflags.0?true(buffer);
    // final hasSettings = _readflags.1?true(buffer);
    // final requestWriteAccess = _readflags.2?true(buffer);
    // final showInAttachMenu = _readflags.3?true(buffer);
    // final showInSideMenu = _readflags.4?true(buffer);
    // final sideMenuDisclaimerNeeded = _readflags.5?true(buffer);
    // final botId = _readlong(buffer);
    // final shortName = _readstring(buffer);
    // final peerTypes = _readflags.3?Vector<AttachMenuPeerType>(buffer);
    // final icons = _readVector<AttachMenuBotIcon>(buffer);
    // final result = AttachMenuBot(flags: flags, inactive: inactive, hasSettings: hasSettings, requestWriteAccess: requestWriteAccess, showInAttachMenu: showInAttachMenu, showInSideMenu: showInSideMenu, sideMenuDisclaimerNeeded: sideMenuDisclaimerNeeded, botId: botId, shortName: shortName, peerTypes: peerTypes, icons: icons,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: inactive,
      b01: hasSettings,
      b02: requestWriteAccess,
      b03: showInAttachMenu || peerTypes != null,
      b04: showInSideMenu,
      b05: sideMenuDisclaimerNeeded,
    );

    return v;
  }

  /// inactive: bit 0 of flags.0?true
  final bool inactive;

  /// has_settings: bit 1 of flags.1?true
  final bool hasSettings;

  /// request_write_access: bit 2 of flags.2?true
  final bool requestWriteAccess;

  /// show_in_attach_menu: bit 3 of flags.3?true
  final bool showInAttachMenu;

  /// show_in_side_menu: bit 4 of flags.4?true
  final bool showInSideMenu;

  /// side_menu_disclaimer_needed: bit 5 of flags.5?true
  final bool sideMenuDisclaimerNeeded;

  /// Bot Id.
  final int botId;

  /// Short Name.
  final String shortName;

  /// Peer Types.
  final List<AttachMenuPeerTypeBase>? peerTypes;

  /// Icons.
  final List<AttachMenuBotIconBase> icons;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd90d8dfe);
    buffer.writeInt(flags);
    buffer.writeLong(botId);
    buffer.writeString(shortName);
    final localPeerTypesCopy = peerTypes;
    if (localPeerTypesCopy != null) {
      buffer.writeVectorObject(localPeerTypesCopy);
    }
    buffer.writeVectorObject(icons);
  }
}

/// Predicate Attach Menu Bots.
abstract class AttachMenuBotsBase extends TlConstructor {
  /// Predicate Attach Menu Bots constructor.
  const AttachMenuBotsBase._() : super._();
}

/// Attach Menu Bots Not Modified.
///
/// ID: `f1d88a5c`.
class AttachMenuBotsNotModified extends AttachMenuBotsBase {
  /// Attach Menu Bots Not Modified constructor.
  const AttachMenuBotsNotModified() : super._();

  /// Deserialize.
  factory AttachMenuBotsNotModified.deserialize(Uint8List buffer) {
    // final result = AttachMenuBotsNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf1d88a5c);
  }
}

/// Attach Menu Bots.
///
/// ID: `3c4301c0`.
class AttachMenuBots extends AttachMenuBotsBase {
  /// Attach Menu Bots constructor.
  const AttachMenuBots({
    required this.hash,
    required this.bots,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory AttachMenuBots.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final bots = _readVector<AttachMenuBot>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = AttachMenuBots(hash: hash, bots: bots, users: users,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Bots.
  final List<AttachMenuBotBase> bots;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3c4301c0);
    buffer.writeLong(hash);
    buffer.writeVectorObject(bots);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Attach Menu Bots Bot.
abstract class AttachMenuBotsBotBase extends TlConstructor {
  /// Predicate Attach Menu Bots Bot constructor.
  const AttachMenuBotsBotBase._() : super._();
}

/// Attach Menu Bots Bot.
///
/// ID: `93bf667f`.
class AttachMenuBotsBot extends AttachMenuBotsBotBase {
  /// Attach Menu Bots Bot constructor.
  const AttachMenuBotsBot({
    required this.bot,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory AttachMenuBotsBot.deserialize(Uint8List buffer) {
    // final bot = _readAttachMenuBot(buffer);
    // final users = _readVector<User>(buffer);
    // final result = AttachMenuBotsBot(bot: bot, users: users,);

    // return result;

    throw Exception();
  }

  /// Bot.
  final AttachMenuBotBase bot;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x93bf667f);
    buffer.writeObject(bot);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Web View Result.
abstract class WebViewResultBase extends TlConstructor {
  /// Predicate Web View Result constructor.
  const WebViewResultBase._() : super._();
}

/// Web View Result Url.
///
/// ID: `0c14557c`.
class WebViewResultUrl extends WebViewResultBase {
  /// Web View Result Url constructor.
  const WebViewResultUrl({
    required this.queryId,
    required this.url,
  }) : super._();

  /// Deserialize.
  factory WebViewResultUrl.deserialize(Uint8List buffer) {
    // final queryId = _readlong(buffer);
    // final url = _readstring(buffer);
    // final result = WebViewResultUrl(queryId: queryId, url: url,);

    // return result;

    throw Exception();
  }

  /// Query Id.
  final int queryId;

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0c14557c);
    buffer.writeLong(queryId);
    buffer.writeString(url);
  }
}

/// Predicate Simple Web View Result.
abstract class SimpleWebViewResultBase extends TlConstructor {
  /// Predicate Simple Web View Result constructor.
  const SimpleWebViewResultBase._() : super._();
}

/// Simple Web View Result Url.
///
/// ID: `882f76bb`.
class SimpleWebViewResultUrl extends SimpleWebViewResultBase {
  /// Simple Web View Result Url constructor.
  const SimpleWebViewResultUrl({
    required this.url,
  }) : super._();

  /// Deserialize.
  factory SimpleWebViewResultUrl.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final result = SimpleWebViewResultUrl(url: url,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x882f76bb);
    buffer.writeString(url);
  }
}

/// Predicate Web View Message Sent.
abstract class WebViewMessageSentBase extends TlConstructor {
  /// Predicate Web View Message Sent constructor.
  const WebViewMessageSentBase._() : super._();
}

/// Web View Message Sent.
///
/// ID: `0c94511c`.
class WebViewMessageSent extends WebViewMessageSentBase {
  /// Web View Message Sent constructor.
  const WebViewMessageSent({
    this.msgId,
  }) : super._();

  /// Deserialize.
  factory WebViewMessageSent.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final msgId = _readflags.0?InputBotInlineMessageID(buffer);
    // final result = WebViewMessageSent(flags: flags, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: msgId != null,
    );

    return v;
  }

  /// Msg Id.
  final InputBotInlineMessageIDBase? msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0c94511c);
    buffer.writeInt(flags);
    final localMsgIdCopy = msgId;
    if (localMsgIdCopy != null) {
      buffer.writeObject(localMsgIdCopy);
    }
  }
}

/// Predicate Bot Menu Button.
abstract class BotMenuButtonBase extends TlConstructor {
  /// Predicate Bot Menu Button constructor.
  const BotMenuButtonBase._() : super._();
}

/// Bot Menu Button Default.
///
/// ID: `7533a588`.
class BotMenuButtonDefault extends BotMenuButtonBase {
  /// Bot Menu Button Default constructor.
  const BotMenuButtonDefault() : super._();

  /// Deserialize.
  factory BotMenuButtonDefault.deserialize(Uint8List buffer) {
    // final result = BotMenuButtonDefault();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7533a588);
  }
}

/// Bot Menu Button Commands.
///
/// ID: `4258c205`.
class BotMenuButtonCommands extends BotMenuButtonBase {
  /// Bot Menu Button Commands constructor.
  const BotMenuButtonCommands() : super._();

  /// Deserialize.
  factory BotMenuButtonCommands.deserialize(Uint8List buffer) {
    // final result = BotMenuButtonCommands();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4258c205);
  }
}

/// Bot Menu Button.
///
/// ID: `c7b57ce6`.
class BotMenuButton extends BotMenuButtonBase {
  /// Bot Menu Button constructor.
  const BotMenuButton({
    required this.text,
    required this.url,
  }) : super._();

  /// Deserialize.
  factory BotMenuButton.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final url = _readstring(buffer);
    // final result = BotMenuButton(text: text, url: url,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc7b57ce6);
    buffer.writeString(text);
    buffer.writeString(url);
  }
}

/// Predicate Account Saved Ringtones.
abstract class AccountSavedRingtonesBase extends TlConstructor {
  /// Predicate Account Saved Ringtones constructor.
  const AccountSavedRingtonesBase._() : super._();
}

/// Account Saved Ringtones Not Modified.
///
/// ID: `fbf6e8b1`.
class AccountSavedRingtonesNotModified extends AccountSavedRingtonesBase {
  /// Account Saved Ringtones Not Modified constructor.
  const AccountSavedRingtonesNotModified() : super._();

  /// Deserialize.
  factory AccountSavedRingtonesNotModified.deserialize(Uint8List buffer) {
    // final result = AccountSavedRingtonesNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfbf6e8b1);
  }
}

/// Account Saved Ringtones.
///
/// ID: `c1e92cc5`.
class AccountSavedRingtones extends AccountSavedRingtonesBase {
  /// Account Saved Ringtones constructor.
  const AccountSavedRingtones({
    required this.hash,
    required this.ringtones,
  }) : super._();

  /// Deserialize.
  factory AccountSavedRingtones.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final ringtones = _readVector<Document>(buffer);
    // final result = AccountSavedRingtones(hash: hash, ringtones: ringtones,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Ringtones.
  final List<DocumentBase> ringtones;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc1e92cc5);
    buffer.writeLong(hash);
    buffer.writeVectorObject(ringtones);
  }
}

/// Predicate Notification Sound.
abstract class NotificationSoundBase extends TlConstructor {
  /// Predicate Notification Sound constructor.
  const NotificationSoundBase._() : super._();
}

/// Notification Sound Default.
///
/// ID: `97e8bebe`.
class NotificationSoundDefault extends NotificationSoundBase {
  /// Notification Sound Default constructor.
  const NotificationSoundDefault() : super._();

  /// Deserialize.
  factory NotificationSoundDefault.deserialize(Uint8List buffer) {
    // final result = NotificationSoundDefault();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x97e8bebe);
  }
}

/// Notification Sound None.
///
/// ID: `6f0c34df`.
class NotificationSoundNone extends NotificationSoundBase {
  /// Notification Sound None constructor.
  const NotificationSoundNone() : super._();

  /// Deserialize.
  factory NotificationSoundNone.deserialize(Uint8List buffer) {
    // final result = NotificationSoundNone();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6f0c34df);
  }
}

/// Notification Sound Local.
///
/// ID: `830b9ae4`.
class NotificationSoundLocal extends NotificationSoundBase {
  /// Notification Sound Local constructor.
  const NotificationSoundLocal({
    required this.title,
    required this.data,
  }) : super._();

  /// Deserialize.
  factory NotificationSoundLocal.deserialize(Uint8List buffer) {
    // final title = _readstring(buffer);
    // final data = _readstring(buffer);
    // final result = NotificationSoundLocal(title: title, data: data,);

    // return result;

    throw Exception();
  }

  /// Title.
  final String title;

  /// Data.
  final String data;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x830b9ae4);
    buffer.writeString(title);
    buffer.writeString(data);
  }
}

/// Notification Sound Ringtone.
///
/// ID: `ff6c8049`.
class NotificationSoundRingtone extends NotificationSoundBase {
  /// Notification Sound Ringtone constructor.
  const NotificationSoundRingtone({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory NotificationSoundRingtone.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final result = NotificationSoundRingtone(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xff6c8049);
    buffer.writeLong(id);
  }
}

/// Predicate Account Saved Ringtone.
abstract class AccountSavedRingtoneBase extends TlConstructor {
  /// Predicate Account Saved Ringtone constructor.
  const AccountSavedRingtoneBase._() : super._();
}

/// Account Saved Ringtone.
///
/// ID: `b7263f6d`.
class AccountSavedRingtone extends AccountSavedRingtoneBase {
  /// Account Saved Ringtone constructor.
  const AccountSavedRingtone() : super._();

  /// Deserialize.
  factory AccountSavedRingtone.deserialize(Uint8List buffer) {
    // final result = AccountSavedRingtone();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb7263f6d);
  }
}

/// Account Saved Ringtone Converted.
///
/// ID: `1f307eb7`.
class AccountSavedRingtoneConverted extends AccountSavedRingtoneBase {
  /// Account Saved Ringtone Converted constructor.
  const AccountSavedRingtoneConverted({
    required this.document,
  }) : super._();

  /// Deserialize.
  factory AccountSavedRingtoneConverted.deserialize(Uint8List buffer) {
    // final document = _readDocument(buffer);
    // final result = AccountSavedRingtoneConverted(document: document,);

    // return result;

    throw Exception();
  }

  /// Document.
  final DocumentBase document;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1f307eb7);
    buffer.writeObject(document);
  }
}

/// Predicate Attach Menu Peer Type.
abstract class AttachMenuPeerTypeBase extends TlConstructor {
  /// Predicate Attach Menu Peer Type constructor.
  const AttachMenuPeerTypeBase._() : super._();
}

/// Attach Menu Peer Type Same Bot P M.
///
/// ID: `7d6be90e`.
class AttachMenuPeerTypeSameBotPM extends AttachMenuPeerTypeBase {
  /// Attach Menu Peer Type Same Bot P M constructor.
  const AttachMenuPeerTypeSameBotPM() : super._();

  /// Deserialize.
  factory AttachMenuPeerTypeSameBotPM.deserialize(Uint8List buffer) {
    // final result = AttachMenuPeerTypeSameBotPM();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7d6be90e);
  }
}

/// Attach Menu Peer Type Bot P M.
///
/// ID: `c32bfa1a`.
class AttachMenuPeerTypeBotPM extends AttachMenuPeerTypeBase {
  /// Attach Menu Peer Type Bot P M constructor.
  const AttachMenuPeerTypeBotPM() : super._();

  /// Deserialize.
  factory AttachMenuPeerTypeBotPM.deserialize(Uint8List buffer) {
    // final result = AttachMenuPeerTypeBotPM();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc32bfa1a);
  }
}

/// Attach Menu Peer Type P M.
///
/// ID: `f146d31f`.
class AttachMenuPeerTypePM extends AttachMenuPeerTypeBase {
  /// Attach Menu Peer Type P M constructor.
  const AttachMenuPeerTypePM() : super._();

  /// Deserialize.
  factory AttachMenuPeerTypePM.deserialize(Uint8List buffer) {
    // final result = AttachMenuPeerTypePM();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf146d31f);
  }
}

/// Attach Menu Peer Type Chat.
///
/// ID: `0509113f`.
class AttachMenuPeerTypeChat extends AttachMenuPeerTypeBase {
  /// Attach Menu Peer Type Chat constructor.
  const AttachMenuPeerTypeChat() : super._();

  /// Deserialize.
  factory AttachMenuPeerTypeChat.deserialize(Uint8List buffer) {
    // final result = AttachMenuPeerTypeChat();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0509113f);
  }
}

/// Attach Menu Peer Type Broadcast.
///
/// ID: `7bfbdefc`.
class AttachMenuPeerTypeBroadcast extends AttachMenuPeerTypeBase {
  /// Attach Menu Peer Type Broadcast constructor.
  const AttachMenuPeerTypeBroadcast() : super._();

  /// Deserialize.
  factory AttachMenuPeerTypeBroadcast.deserialize(Uint8List buffer) {
    // final result = AttachMenuPeerTypeBroadcast();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7bfbdefc);
  }
}

/// Predicate Input Invoice.
abstract class InputInvoiceBase extends TlConstructor {
  /// Predicate Input Invoice constructor.
  const InputInvoiceBase._() : super._();
}

/// Input Invoice Message.
///
/// ID: `c5b56859`.
class InputInvoiceMessage extends InputInvoiceBase {
  /// Input Invoice Message constructor.
  const InputInvoiceMessage({
    required this.peer,
    required this.msgId,
  }) : super._();

  /// Deserialize.
  factory InputInvoiceMessage.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final result = InputInvoiceMessage(peer: peer, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc5b56859);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
  }
}

/// Input Invoice Slug.
///
/// ID: `c326caef`.
class InputInvoiceSlug extends InputInvoiceBase {
  /// Input Invoice Slug constructor.
  const InputInvoiceSlug({
    required this.slug,
  }) : super._();

  /// Deserialize.
  factory InputInvoiceSlug.deserialize(Uint8List buffer) {
    // final slug = _readstring(buffer);
    // final result = InputInvoiceSlug(slug: slug,);

    // return result;

    throw Exception();
  }

  /// Slug.
  final String slug;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc326caef);
    buffer.writeString(slug);
  }
}

/// Input Invoice Premium Gift Code.
///
/// ID: `98986c0d`.
class InputInvoicePremiumGiftCode extends InputInvoiceBase {
  /// Input Invoice Premium Gift Code constructor.
  const InputInvoicePremiumGiftCode({
    required this.purpose,
    required this.option,
  }) : super._();

  /// Deserialize.
  factory InputInvoicePremiumGiftCode.deserialize(Uint8List buffer) {
    // final purpose = _readInputStorePaymentPurpose(buffer);
    // final option = _readPremiumGiftCodeOption(buffer);
    // final result = InputInvoicePremiumGiftCode(purpose: purpose, option: option,);

    // return result;

    throw Exception();
  }

  /// Purpose.
  final InputStorePaymentPurposeBase purpose;

  /// Option.
  final PremiumGiftCodeOptionBase option;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x98986c0d);
    buffer.writeObject(purpose);
    buffer.writeObject(option);
  }
}

/// Predicate Payments Exported Invoice.
abstract class PaymentsExportedInvoiceBase extends TlConstructor {
  /// Predicate Payments Exported Invoice constructor.
  const PaymentsExportedInvoiceBase._() : super._();
}

/// Payments Exported Invoice.
///
/// ID: `aed0cbd9`.
class PaymentsExportedInvoice extends PaymentsExportedInvoiceBase {
  /// Payments Exported Invoice constructor.
  const PaymentsExportedInvoice({
    required this.url,
  }) : super._();

  /// Deserialize.
  factory PaymentsExportedInvoice.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final result = PaymentsExportedInvoice(url: url,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xaed0cbd9);
    buffer.writeString(url);
  }
}

/// Predicate Messages Transcribed Audio.
abstract class MessagesTranscribedAudioBase extends TlConstructor {
  /// Predicate Messages Transcribed Audio constructor.
  const MessagesTranscribedAudioBase._() : super._();
}

/// Messages Transcribed Audio.
///
/// ID: `cfb9d957`.
class MessagesTranscribedAudio extends MessagesTranscribedAudioBase {
  /// Messages Transcribed Audio constructor.
  const MessagesTranscribedAudio({
    required this.pending,
    required this.transcriptionId,
    required this.text,
    this.trialRemainsNum,
    this.trialRemainsUntilDate,
  }) : super._();

  /// Deserialize.
  factory MessagesTranscribedAudio.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pending = _readflags.0?true(buffer);
    // final transcriptionId = _readlong(buffer);
    // final text = _readstring(buffer);
    // final trialRemainsNum = _readflags.1?int(buffer);
    // final trialRemainsUntilDate = _readflags.1?int(buffer);
    // final result = MessagesTranscribedAudio(flags: flags, pending: pending, transcriptionId: transcriptionId, text: text, trialRemainsNum: trialRemainsNum, trialRemainsUntilDate: trialRemainsUntilDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: pending,
      b01: trialRemainsNum != null || trialRemainsUntilDate != null,
    );

    return v;
  }

  /// pending: bit 0 of flags.0?true
  final bool pending;

  /// Transcription Id.
  final int transcriptionId;

  /// Text.
  final String text;

  /// Trial Remains Num.
  final int? trialRemainsNum;

  /// Trial Remains Until Date.
  final DateTime? trialRemainsUntilDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcfb9d957);
    buffer.writeInt(flags);
    buffer.writeLong(transcriptionId);
    buffer.writeString(text);
    final localTrialRemainsNumCopy = trialRemainsNum;
    if (localTrialRemainsNumCopy != null) {
      buffer.writeInt(localTrialRemainsNumCopy);
    }
    final localTrialRemainsUntilDateCopy = trialRemainsUntilDate;
    if (localTrialRemainsUntilDateCopy != null) {
      buffer.writeDateTime(localTrialRemainsUntilDateCopy);
    }
  }
}

/// Predicate Help Premium Promo.
abstract class HelpPremiumPromoBase extends TlConstructor {
  /// Predicate Help Premium Promo constructor.
  const HelpPremiumPromoBase._() : super._();
}

/// Help Premium Promo.
///
/// ID: `5334759c`.
class HelpPremiumPromo extends HelpPremiumPromoBase {
  /// Help Premium Promo constructor.
  const HelpPremiumPromo({
    required this.statusText,
    required this.statusEntities,
    required this.videoSections,
    required this.videos,
    required this.periodOptions,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory HelpPremiumPromo.deserialize(Uint8List buffer) {
    // final statusText = _readstring(buffer);
    // final statusEntities = _readVector<MessageEntity>(buffer);
    // final videoSections = _readVector<string>(buffer);
    // final videos = _readVector<Document>(buffer);
    // final periodOptions = _readVector<PremiumSubscriptionOption>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = HelpPremiumPromo(statusText: statusText, statusEntities: statusEntities, videoSections: videoSections, videos: videos, periodOptions: periodOptions, users: users,);

    // return result;

    throw Exception();
  }

  /// Status Text.
  final String statusText;

  /// Status Entities.
  final List<MessageEntityBase> statusEntities;

  /// Video Sections.
  final List<String> videoSections;

  /// Videos.
  final List<DocumentBase> videos;

  /// Period Options.
  final List<PremiumSubscriptionOptionBase> periodOptions;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5334759c);
    buffer.writeString(statusText);
    buffer.writeVectorObject(statusEntities);
    buffer.writeVectorString(videoSections);
    buffer.writeVectorObject(videos);
    buffer.writeVectorObject(periodOptions);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Input Store Payment Purpose.
abstract class InputStorePaymentPurposeBase extends TlConstructor {
  /// Predicate Input Store Payment Purpose constructor.
  const InputStorePaymentPurposeBase._() : super._();
}

/// Input Store Payment Premium Subscription.
///
/// ID: `a6751e66`.
class InputStorePaymentPremiumSubscription
    extends InputStorePaymentPurposeBase {
  /// Input Store Payment Premium Subscription constructor.
  const InputStorePaymentPremiumSubscription({
    required this.restore,
    required this.upgrade,
  }) : super._();

  /// Deserialize.
  factory InputStorePaymentPremiumSubscription.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final restore = _readflags.0?true(buffer);
    // final upgrade = _readflags.1?true(buffer);
    // final result = InputStorePaymentPremiumSubscription(flags: flags, restore: restore, upgrade: upgrade,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: restore,
      b01: upgrade,
    );

    return v;
  }

  /// restore: bit 0 of flags.0?true
  final bool restore;

  /// upgrade: bit 1 of flags.1?true
  final bool upgrade;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa6751e66);
    buffer.writeInt(flags);
  }
}

/// Input Store Payment Gift Premium.
///
/// ID: `616f7fe8`.
class InputStorePaymentGiftPremium extends InputStorePaymentPurposeBase {
  /// Input Store Payment Gift Premium constructor.
  const InputStorePaymentGiftPremium({
    required this.userId,
    required this.currency,
    required this.amount,
  }) : super._();

  /// Deserialize.
  factory InputStorePaymentGiftPremium.deserialize(Uint8List buffer) {
    // final userId = _readInputUser(buffer);
    // final currency = _readstring(buffer);
    // final amount = _readlong(buffer);
    // final result = InputStorePaymentGiftPremium(userId: userId, currency: currency, amount: amount,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final InputUserBase userId;

  /// Currency.
  final String currency;

  /// Amount.
  final int amount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x616f7fe8);
    buffer.writeObject(userId);
    buffer.writeString(currency);
    buffer.writeLong(amount);
  }
}

/// Input Store Payment Premium Gift Code.
///
/// ID: `a3805f3f`.
class InputStorePaymentPremiumGiftCode extends InputStorePaymentPurposeBase {
  /// Input Store Payment Premium Gift Code constructor.
  const InputStorePaymentPremiumGiftCode({
    required this.users,
    this.boostPeer,
    required this.currency,
    required this.amount,
  }) : super._();

  /// Deserialize.
  factory InputStorePaymentPremiumGiftCode.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final users = _readVector<InputUser>(buffer);
    // final boostPeer = _readflags.0?InputPeer(buffer);
    // final currency = _readstring(buffer);
    // final amount = _readlong(buffer);
    // final result = InputStorePaymentPremiumGiftCode(flags: flags, users: users, boostPeer: boostPeer, currency: currency, amount: amount,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: boostPeer != null,
    );

    return v;
  }

  /// Users.
  final List<InputUserBase> users;

  /// Boost Peer.
  final InputPeerBase? boostPeer;

  /// Currency.
  final String currency;

  /// Amount.
  final int amount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa3805f3f);
    buffer.writeInt(flags);
    buffer.writeVectorObject(users);
    final localBoostPeerCopy = boostPeer;
    if (localBoostPeerCopy != null) {
      buffer.writeObject(localBoostPeerCopy);
    }
    buffer.writeString(currency);
    buffer.writeLong(amount);
  }
}

/// Input Store Payment Premium Giveaway.
///
/// ID: `160544ca`.
class InputStorePaymentPremiumGiveaway extends InputStorePaymentPurposeBase {
  /// Input Store Payment Premium Giveaway constructor.
  const InputStorePaymentPremiumGiveaway({
    required this.onlyNewSubscribers,
    required this.winnersAreVisible,
    required this.boostPeer,
    this.additionalPeers,
    this.countriesIso2,
    this.prizeDescription,
    required this.randomId,
    required this.untilDate,
    required this.currency,
    required this.amount,
  }) : super._();

  /// Deserialize.
  factory InputStorePaymentPremiumGiveaway.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final onlyNewSubscribers = _readflags.0?true(buffer);
    // final winnersAreVisible = _readflags.3?true(buffer);
    // final boostPeer = _readInputPeer(buffer);
    // final additionalPeers = _readflags.1?Vector<InputPeer>(buffer);
    // final countriesIso2 = _readflags.2?Vector<string>(buffer);
    // final prizeDescription = _readflags.4?string(buffer);
    // final randomId = _readlong(buffer);
    // final untilDate = _readint(buffer);
    // final currency = _readstring(buffer);
    // final amount = _readlong(buffer);
    // final result = InputStorePaymentPremiumGiveaway(flags: flags, onlyNewSubscribers: onlyNewSubscribers, winnersAreVisible: winnersAreVisible, boostPeer: boostPeer, additionalPeers: additionalPeers, countriesIso2: countriesIso2, prizeDescription: prizeDescription, randomId: randomId, untilDate: untilDate, currency: currency, amount: amount,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: onlyNewSubscribers,
      b03: winnersAreVisible,
      b01: additionalPeers != null,
      b02: countriesIso2 != null,
      b04: prizeDescription != null,
    );

    return v;
  }

  /// only_new_subscribers: bit 0 of flags.0?true
  final bool onlyNewSubscribers;

  /// winners_are_visible: bit 3 of flags.3?true
  final bool winnersAreVisible;

  /// Boost Peer.
  final InputPeerBase boostPeer;

  /// Additional Peers.
  final List<InputPeerBase>? additionalPeers;

  /// Countries Iso2.
  final List<String>? countriesIso2;

  /// Prize Description.
  final String? prizeDescription;

  /// Random Id.
  final int randomId;

  /// Until Date.
  final DateTime untilDate;

  /// Currency.
  final String currency;

  /// Amount.
  final int amount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x160544ca);
    buffer.writeInt(flags);
    buffer.writeObject(boostPeer);
    final localAdditionalPeersCopy = additionalPeers;
    if (localAdditionalPeersCopy != null) {
      buffer.writeVectorObject(localAdditionalPeersCopy);
    }
    final localCountriesIso2Copy = countriesIso2;
    if (localCountriesIso2Copy != null) {
      buffer.writeVectorString(localCountriesIso2Copy);
    }
    final localPrizeDescriptionCopy = prizeDescription;
    if (localPrizeDescriptionCopy != null) {
      buffer.writeString(localPrizeDescriptionCopy);
    }
    buffer.writeLong(randomId);
    buffer.writeDateTime(untilDate);
    buffer.writeString(currency);
    buffer.writeLong(amount);
  }
}

/// Predicate Premium Gift Option.
abstract class PremiumGiftOptionBase extends TlConstructor {
  /// Predicate Premium Gift Option constructor.
  const PremiumGiftOptionBase._() : super._();
}

/// Premium Gift Option.
///
/// ID: `74c34319`.
class PremiumGiftOption extends PremiumGiftOptionBase {
  /// Premium Gift Option constructor.
  const PremiumGiftOption({
    required this.months,
    required this.currency,
    required this.amount,
    required this.botUrl,
    this.storeProduct,
  }) : super._();

  /// Deserialize.
  factory PremiumGiftOption.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final months = _readint(buffer);
    // final currency = _readstring(buffer);
    // final amount = _readlong(buffer);
    // final botUrl = _readstring(buffer);
    // final storeProduct = _readflags.0?string(buffer);
    // final result = PremiumGiftOption(flags: flags, months: months, currency: currency, amount: amount, botUrl: botUrl, storeProduct: storeProduct,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: storeProduct != null,
    );

    return v;
  }

  /// Months.
  final int months;

  /// Currency.
  final String currency;

  /// Amount.
  final int amount;

  /// Bot Url.
  final String botUrl;

  /// Store Product.
  final String? storeProduct;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x74c34319);
    buffer.writeInt(flags);
    buffer.writeInt(months);
    buffer.writeString(currency);
    buffer.writeLong(amount);
    buffer.writeString(botUrl);
    final localStoreProductCopy = storeProduct;
    if (localStoreProductCopy != null) {
      buffer.writeString(localStoreProductCopy);
    }
  }
}

/// Predicate Payment Form Method.
abstract class PaymentFormMethodBase extends TlConstructor {
  /// Predicate Payment Form Method constructor.
  const PaymentFormMethodBase._() : super._();
}

/// Payment Form Method.
///
/// ID: `88f8f21b`.
class PaymentFormMethod extends PaymentFormMethodBase {
  /// Payment Form Method constructor.
  const PaymentFormMethod({
    required this.url,
    required this.title,
  }) : super._();

  /// Deserialize.
  factory PaymentFormMethod.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final title = _readstring(buffer);
    // final result = PaymentFormMethod(url: url, title: title,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Title.
  final String title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x88f8f21b);
    buffer.writeString(url);
    buffer.writeString(title);
  }
}

/// Predicate Emoji Status.
abstract class EmojiStatusBase extends TlConstructor {
  /// Predicate Emoji Status constructor.
  const EmojiStatusBase._() : super._();
}

/// Emoji Status Empty.
///
/// ID: `2de11aae`.
class EmojiStatusEmpty extends EmojiStatusBase {
  /// Emoji Status Empty constructor.
  const EmojiStatusEmpty() : super._();

  /// Deserialize.
  factory EmojiStatusEmpty.deserialize(Uint8List buffer) {
    // final result = EmojiStatusEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2de11aae);
  }
}

/// Emoji Status.
///
/// ID: `929b619d`.
class EmojiStatus extends EmojiStatusBase {
  /// Emoji Status constructor.
  const EmojiStatus({
    required this.documentId,
  }) : super._();

  /// Deserialize.
  factory EmojiStatus.deserialize(Uint8List buffer) {
    // final documentId = _readlong(buffer);
    // final result = EmojiStatus(documentId: documentId,);

    // return result;

    throw Exception();
  }

  /// Document Id.
  final int documentId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x929b619d);
    buffer.writeLong(documentId);
  }
}

/// Emoji Status Until.
///
/// ID: `fa30a8c7`.
class EmojiStatusUntil extends EmojiStatusBase {
  /// Emoji Status Until constructor.
  const EmojiStatusUntil({
    required this.documentId,
    required this.until,
  }) : super._();

  /// Deserialize.
  factory EmojiStatusUntil.deserialize(Uint8List buffer) {
    // final documentId = _readlong(buffer);
    // final until = _readint(buffer);
    // final result = EmojiStatusUntil(documentId: documentId, until: until,);

    // return result;

    throw Exception();
  }

  /// Document Id.
  final int documentId;

  /// Until.
  final int until;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfa30a8c7);
    buffer.writeLong(documentId);
    buffer.writeInt(until);
  }
}

/// Predicate Account Emoji Statuses.
abstract class AccountEmojiStatusesBase extends TlConstructor {
  /// Predicate Account Emoji Statuses constructor.
  const AccountEmojiStatusesBase._() : super._();
}

/// Account Emoji Statuses Not Modified.
///
/// ID: `d08ce645`.
class AccountEmojiStatusesNotModified extends AccountEmojiStatusesBase {
  /// Account Emoji Statuses Not Modified constructor.
  const AccountEmojiStatusesNotModified() : super._();

  /// Deserialize.
  factory AccountEmojiStatusesNotModified.deserialize(Uint8List buffer) {
    // final result = AccountEmojiStatusesNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd08ce645);
  }
}

/// Account Emoji Statuses.
///
/// ID: `90c467d1`.
class AccountEmojiStatuses extends AccountEmojiStatusesBase {
  /// Account Emoji Statuses constructor.
  const AccountEmojiStatuses({
    required this.hash,
    required this.statuses,
  }) : super._();

  /// Deserialize.
  factory AccountEmojiStatuses.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final statuses = _readVector<EmojiStatus>(buffer);
    // final result = AccountEmojiStatuses(hash: hash, statuses: statuses,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Statuses.
  final List<EmojiStatusBase> statuses;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x90c467d1);
    buffer.writeLong(hash);
    buffer.writeVectorObject(statuses);
  }
}

/// Predicate Reaction.
abstract class ReactionBase extends TlConstructor {
  /// Predicate Reaction constructor.
  const ReactionBase._() : super._();
}

/// Reaction Empty.
///
/// ID: `79f5d419`.
class ReactionEmpty extends ReactionBase {
  /// Reaction Empty constructor.
  const ReactionEmpty() : super._();

  /// Deserialize.
  factory ReactionEmpty.deserialize(Uint8List buffer) {
    // final result = ReactionEmpty();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x79f5d419);
  }
}

/// Reaction Emoji.
///
/// ID: `1b2286b8`.
class ReactionEmoji extends ReactionBase {
  /// Reaction Emoji constructor.
  const ReactionEmoji({
    required this.emoticon,
  }) : super._();

  /// Deserialize.
  factory ReactionEmoji.deserialize(Uint8List buffer) {
    // final emoticon = _readstring(buffer);
    // final result = ReactionEmoji(emoticon: emoticon,);

    // return result;

    throw Exception();
  }

  /// Emoticon.
  final String emoticon;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1b2286b8);
    buffer.writeString(emoticon);
  }
}

/// Reaction Custom Emoji.
///
/// ID: `8935fc73`.
class ReactionCustomEmoji extends ReactionBase {
  /// Reaction Custom Emoji constructor.
  const ReactionCustomEmoji({
    required this.documentId,
  }) : super._();

  /// Deserialize.
  factory ReactionCustomEmoji.deserialize(Uint8List buffer) {
    // final documentId = _readlong(buffer);
    // final result = ReactionCustomEmoji(documentId: documentId,);

    // return result;

    throw Exception();
  }

  /// Document Id.
  final int documentId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8935fc73);
    buffer.writeLong(documentId);
  }
}

/// Predicate Chat Reactions.
abstract class ChatReactionsBase extends TlConstructor {
  /// Predicate Chat Reactions constructor.
  const ChatReactionsBase._() : super._();
}

/// Chat Reactions None.
///
/// ID: `eafc32bc`.
class ChatReactionsNone extends ChatReactionsBase {
  /// Chat Reactions None constructor.
  const ChatReactionsNone() : super._();

  /// Deserialize.
  factory ChatReactionsNone.deserialize(Uint8List buffer) {
    // final result = ChatReactionsNone();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xeafc32bc);
  }
}

/// Chat Reactions All.
///
/// ID: `52928bca`.
class ChatReactionsAll extends ChatReactionsBase {
  /// Chat Reactions All constructor.
  const ChatReactionsAll({
    required this.allowCustom,
  }) : super._();

  /// Deserialize.
  factory ChatReactionsAll.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final allowCustom = _readflags.0?true(buffer);
    // final result = ChatReactionsAll(flags: flags, allowCustom: allowCustom,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: allowCustom,
    );

    return v;
  }

  /// allow_custom: bit 0 of flags.0?true
  final bool allowCustom;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x52928bca);
    buffer.writeInt(flags);
  }
}

/// Chat Reactions Some.
///
/// ID: `661d4037`.
class ChatReactionsSome extends ChatReactionsBase {
  /// Chat Reactions Some constructor.
  const ChatReactionsSome({
    required this.reactions,
  }) : super._();

  /// Deserialize.
  factory ChatReactionsSome.deserialize(Uint8List buffer) {
    // final reactions = _readVector<Reaction>(buffer);
    // final result = ChatReactionsSome(reactions: reactions,);

    // return result;

    throw Exception();
  }

  /// Reactions.
  final List<ReactionBase> reactions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x661d4037);
    buffer.writeVectorObject(reactions);
  }
}

/// Predicate Messages Reactions.
abstract class MessagesReactionsBase extends TlConstructor {
  /// Predicate Messages Reactions constructor.
  const MessagesReactionsBase._() : super._();
}

/// Messages Reactions Not Modified.
///
/// ID: `b06fdbdf`.
class MessagesReactionsNotModified extends MessagesReactionsBase {
  /// Messages Reactions Not Modified constructor.
  const MessagesReactionsNotModified() : super._();

  /// Deserialize.
  factory MessagesReactionsNotModified.deserialize(Uint8List buffer) {
    // final result = MessagesReactionsNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb06fdbdf);
  }
}

/// Messages Reactions.
///
/// ID: `eafdf716`.
class MessagesReactions extends MessagesReactionsBase {
  /// Messages Reactions constructor.
  const MessagesReactions({
    required this.hash,
    required this.reactions,
  }) : super._();

  /// Deserialize.
  factory MessagesReactions.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final reactions = _readVector<Reaction>(buffer);
    // final result = MessagesReactions(hash: hash, reactions: reactions,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Reactions.
  final List<ReactionBase> reactions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xeafdf716);
    buffer.writeLong(hash);
    buffer.writeVectorObject(reactions);
  }
}

/// Predicate Email Verify Purpose.
abstract class EmailVerifyPurposeBase extends TlConstructor {
  /// Predicate Email Verify Purpose constructor.
  const EmailVerifyPurposeBase._() : super._();
}

/// Email Verify Purpose Login Setup.
///
/// ID: `4345be73`.
class EmailVerifyPurposeLoginSetup extends EmailVerifyPurposeBase {
  /// Email Verify Purpose Login Setup constructor.
  const EmailVerifyPurposeLoginSetup({
    required this.phoneNumber,
    required this.phoneCodeHash,
  }) : super._();

  /// Deserialize.
  factory EmailVerifyPurposeLoginSetup.deserialize(Uint8List buffer) {
    // final phoneNumber = _readstring(buffer);
    // final phoneCodeHash = _readstring(buffer);
    // final result = EmailVerifyPurposeLoginSetup(phoneNumber: phoneNumber, phoneCodeHash: phoneCodeHash,);

    // return result;

    throw Exception();
  }

  /// Phone Number.
  final String phoneNumber;

  /// Phone Code Hash.
  final String phoneCodeHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4345be73);
    buffer.writeString(phoneNumber);
    buffer.writeString(phoneCodeHash);
  }
}

/// Email Verify Purpose Login Change.
///
/// ID: `527d22eb`.
class EmailVerifyPurposeLoginChange extends EmailVerifyPurposeBase {
  /// Email Verify Purpose Login Change constructor.
  const EmailVerifyPurposeLoginChange() : super._();

  /// Deserialize.
  factory EmailVerifyPurposeLoginChange.deserialize(Uint8List buffer) {
    // final result = EmailVerifyPurposeLoginChange();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x527d22eb);
  }
}

/// Email Verify Purpose Passport.
///
/// ID: `bbf51685`.
class EmailVerifyPurposePassport extends EmailVerifyPurposeBase {
  /// Email Verify Purpose Passport constructor.
  const EmailVerifyPurposePassport() : super._();

  /// Deserialize.
  factory EmailVerifyPurposePassport.deserialize(Uint8List buffer) {
    // final result = EmailVerifyPurposePassport();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbbf51685);
  }
}

/// Predicate Email Verification.
abstract class EmailVerificationBase extends TlConstructor {
  /// Predicate Email Verification constructor.
  const EmailVerificationBase._() : super._();
}

/// Email Verification Code.
///
/// ID: `922e55a9`.
class EmailVerificationCode extends EmailVerificationBase {
  /// Email Verification Code constructor.
  const EmailVerificationCode({
    required this.code,
  }) : super._();

  /// Deserialize.
  factory EmailVerificationCode.deserialize(Uint8List buffer) {
    // final code = _readstring(buffer);
    // final result = EmailVerificationCode(code: code,);

    // return result;

    throw Exception();
  }

  /// Code.
  final String code;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x922e55a9);
    buffer.writeString(code);
  }
}

/// Email Verification Google.
///
/// ID: `db909ec2`.
class EmailVerificationGoogle extends EmailVerificationBase {
  /// Email Verification Google constructor.
  const EmailVerificationGoogle({
    required this.token,
  }) : super._();

  /// Deserialize.
  factory EmailVerificationGoogle.deserialize(Uint8List buffer) {
    // final token = _readstring(buffer);
    // final result = EmailVerificationGoogle(token: token,);

    // return result;

    throw Exception();
  }

  /// Token.
  final String token;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdb909ec2);
    buffer.writeString(token);
  }
}

/// Email Verification Apple.
///
/// ID: `96d074fd`.
class EmailVerificationApple extends EmailVerificationBase {
  /// Email Verification Apple constructor.
  const EmailVerificationApple({
    required this.token,
  }) : super._();

  /// Deserialize.
  factory EmailVerificationApple.deserialize(Uint8List buffer) {
    // final token = _readstring(buffer);
    // final result = EmailVerificationApple(token: token,);

    // return result;

    throw Exception();
  }

  /// Token.
  final String token;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x96d074fd);
    buffer.writeString(token);
  }
}

/// Predicate Account Email Verified.
abstract class AccountEmailVerifiedBase extends TlConstructor {
  /// Predicate Account Email Verified constructor.
  const AccountEmailVerifiedBase._() : super._();
}

/// Account Email Verified.
///
/// ID: `2b96cd1b`.
class AccountEmailVerified extends AccountEmailVerifiedBase {
  /// Account Email Verified constructor.
  const AccountEmailVerified({
    required this.email,
  }) : super._();

  /// Deserialize.
  factory AccountEmailVerified.deserialize(Uint8List buffer) {
    // final email = _readstring(buffer);
    // final result = AccountEmailVerified(email: email,);

    // return result;

    throw Exception();
  }

  /// Email.
  final String email;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2b96cd1b);
    buffer.writeString(email);
  }
}

/// Account Email Verified Login.
///
/// ID: `e1bb0d61`.
class AccountEmailVerifiedLogin extends AccountEmailVerifiedBase {
  /// Account Email Verified Login constructor.
  const AccountEmailVerifiedLogin({
    required this.email,
    required this.sentCode,
  }) : super._();

  /// Deserialize.
  factory AccountEmailVerifiedLogin.deserialize(Uint8List buffer) {
    // final email = _readstring(buffer);
    // final sentCode = _readauth.SentCode(buffer);
    // final result = AccountEmailVerifiedLogin(email: email, sentCode: sentCode,);

    // return result;

    throw Exception();
  }

  /// Email.
  final String email;

  /// Sent Code.
  final AuthSentCodeBase sentCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe1bb0d61);
    buffer.writeString(email);
    buffer.writeObject(sentCode);
  }
}

/// Predicate Premium Subscription Option.
abstract class PremiumSubscriptionOptionBase extends TlConstructor {
  /// Predicate Premium Subscription Option constructor.
  const PremiumSubscriptionOptionBase._() : super._();
}

/// Premium Subscription Option.
///
/// ID: `5f2d1df2`.
class PremiumSubscriptionOption extends PremiumSubscriptionOptionBase {
  /// Premium Subscription Option constructor.
  const PremiumSubscriptionOption({
    required this.current,
    required this.canPurchaseUpgrade,
    this.transaction,
    required this.months,
    required this.currency,
    required this.amount,
    required this.botUrl,
    this.storeProduct,
  }) : super._();

  /// Deserialize.
  factory PremiumSubscriptionOption.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final current = _readflags.1?true(buffer);
    // final canPurchaseUpgrade = _readflags.2?true(buffer);
    // final transaction = _readflags.3?string(buffer);
    // final months = _readint(buffer);
    // final currency = _readstring(buffer);
    // final amount = _readlong(buffer);
    // final botUrl = _readstring(buffer);
    // final storeProduct = _readflags.0?string(buffer);
    // final result = PremiumSubscriptionOption(flags: flags, current: current, canPurchaseUpgrade: canPurchaseUpgrade, transaction: transaction, months: months, currency: currency, amount: amount, botUrl: botUrl, storeProduct: storeProduct,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: current,
      b02: canPurchaseUpgrade,
      b03: transaction != null,
      b00: storeProduct != null,
    );

    return v;
  }

  /// current: bit 1 of flags.1?true
  final bool current;

  /// can_purchase_upgrade: bit 2 of flags.2?true
  final bool canPurchaseUpgrade;

  /// Transaction.
  final String? transaction;

  /// Months.
  final int months;

  /// Currency.
  final String currency;

  /// Amount.
  final int amount;

  /// Bot Url.
  final String botUrl;

  /// Store Product.
  final String? storeProduct;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5f2d1df2);
    buffer.writeInt(flags);
    final localTransactionCopy = transaction;
    if (localTransactionCopy != null) {
      buffer.writeString(localTransactionCopy);
    }
    buffer.writeInt(months);
    buffer.writeString(currency);
    buffer.writeLong(amount);
    buffer.writeString(botUrl);
    final localStoreProductCopy = storeProduct;
    if (localStoreProductCopy != null) {
      buffer.writeString(localStoreProductCopy);
    }
  }
}

/// Predicate Send As Peer.
abstract class SendAsPeerBase extends TlConstructor {
  /// Predicate Send As Peer constructor.
  const SendAsPeerBase._() : super._();
}

/// Send As Peer.
///
/// ID: `b81c7034`.
class SendAsPeer extends SendAsPeerBase {
  /// Send As Peer constructor.
  const SendAsPeer({
    required this.premiumRequired,
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory SendAsPeer.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final premiumRequired = _readflags.0?true(buffer);
    // final peer = _readPeer(buffer);
    // final result = SendAsPeer(flags: flags, premiumRequired: premiumRequired, peer: peer,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: premiumRequired,
    );

    return v;
  }

  /// premium_required: bit 0 of flags.0?true
  final bool premiumRequired;

  /// Peer.
  final PeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb81c7034);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
  }
}

/// Predicate Message Extended Media.
abstract class MessageExtendedMediaBase extends TlConstructor {
  /// Predicate Message Extended Media constructor.
  const MessageExtendedMediaBase._() : super._();
}

/// Message Extended Media Preview.
///
/// ID: `ad628cc8`.
class MessageExtendedMediaPreview extends MessageExtendedMediaBase {
  /// Message Extended Media Preview constructor.
  const MessageExtendedMediaPreview({
    this.w,
    this.h,
    this.thumb,
    this.videoDuration,
  }) : super._();

  /// Deserialize.
  factory MessageExtendedMediaPreview.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final w = _readflags.0?int(buffer);
    // final h = _readflags.0?int(buffer);
    // final thumb = _readflags.1?PhotoSize(buffer);
    // final videoDuration = _readflags.2?int(buffer);
    // final result = MessageExtendedMediaPreview(flags: flags, w: w, h: h, thumb: thumb, videoDuration: videoDuration,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: w != null || h != null,
      b01: thumb != null,
      b02: videoDuration != null,
    );

    return v;
  }

  /// W.
  final int? w;

  /// H.
  final int? h;

  /// Thumb.
  final PhotoSizeBase? thumb;

  /// Video Duration.
  final int? videoDuration;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xad628cc8);
    buffer.writeInt(flags);
    final localWCopy = w;
    if (localWCopy != null) {
      buffer.writeInt(localWCopy);
    }
    final localHCopy = h;
    if (localHCopy != null) {
      buffer.writeInt(localHCopy);
    }
    final localThumbCopy = thumb;
    if (localThumbCopy != null) {
      buffer.writeObject(localThumbCopy);
    }
    final localVideoDurationCopy = videoDuration;
    if (localVideoDurationCopy != null) {
      buffer.writeInt(localVideoDurationCopy);
    }
  }
}

/// Message Extended Media.
///
/// ID: `ee479c64`.
class MessageExtendedMedia extends MessageExtendedMediaBase {
  /// Message Extended Media constructor.
  const MessageExtendedMedia({
    required this.media,
  }) : super._();

  /// Deserialize.
  factory MessageExtendedMedia.deserialize(Uint8List buffer) {
    // final media = _readMessageMedia(buffer);
    // final result = MessageExtendedMedia(media: media,);

    // return result;

    throw Exception();
  }

  /// Media.
  final MessageMediaBase media;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xee479c64);
    buffer.writeObject(media);
  }
}

/// Predicate Sticker Keyword.
abstract class StickerKeywordBase extends TlConstructor {
  /// Predicate Sticker Keyword constructor.
  const StickerKeywordBase._() : super._();
}

/// Sticker Keyword.
///
/// ID: `fcfeb29c`.
class StickerKeyword extends StickerKeywordBase {
  /// Sticker Keyword constructor.
  const StickerKeyword({
    required this.documentId,
    required this.keyword,
  }) : super._();

  /// Deserialize.
  factory StickerKeyword.deserialize(Uint8List buffer) {
    // final documentId = _readlong(buffer);
    // final keyword = _readVector<string>(buffer);
    // final result = StickerKeyword(documentId: documentId, keyword: keyword,);

    // return result;

    throw Exception();
  }

  /// Document Id.
  final int documentId;

  /// Keyword.
  final List<String> keyword;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfcfeb29c);
    buffer.writeLong(documentId);
    buffer.writeVectorString(keyword);
  }
}

/// Predicate Username.
abstract class UsernameBase extends TlConstructor {
  /// Predicate Username constructor.
  const UsernameBase._() : super._();
}

/// Username.
///
/// ID: `b4073647`.
class Username extends UsernameBase {
  /// Username constructor.
  const Username({
    required this.editable,
    required this.active,
    required this.username,
  }) : super._();

  /// Deserialize.
  factory Username.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final editable = _readflags.0?true(buffer);
    // final active = _readflags.1?true(buffer);
    // final username = _readstring(buffer);
    // final result = Username(flags: flags, editable: editable, active: active, username: username,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: editable,
      b01: active,
    );

    return v;
  }

  /// editable: bit 0 of flags.0?true
  final bool editable;

  /// active: bit 1 of flags.1?true
  final bool active;

  /// Username.
  final String username;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb4073647);
    buffer.writeInt(flags);
    buffer.writeString(username);
  }
}

/// Predicate Forum Topic.
abstract class ForumTopicBase extends TlConstructor {
  /// Predicate Forum Topic constructor.
  const ForumTopicBase._() : super._();
}

/// Forum Topic Deleted.
///
/// ID: `023f109b`.
class ForumTopicDeleted extends ForumTopicBase {
  /// Forum Topic Deleted constructor.
  const ForumTopicDeleted({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory ForumTopicDeleted.deserialize(Uint8List buffer) {
    // final id = _readint(buffer);
    // final result = ForumTopicDeleted(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x023f109b);
    buffer.writeInt(id);
  }
}

/// Forum Topic.
///
/// ID: `71701da9`.
class ForumTopic extends ForumTopicBase {
  /// Forum Topic constructor.
  const ForumTopic({
    required this.my,
    required this.closed,
    required this.pinned,
    required this.short,
    required this.hidden,
    required this.id,
    required this.date,
    required this.title,
    required this.iconColor,
    this.iconEmojiId,
    required this.topMessage,
    required this.readInboxMaxId,
    required this.readOutboxMaxId,
    required this.unreadCount,
    required this.unreadMentionsCount,
    required this.unreadReactionsCount,
    required this.fromId,
    required this.notifySettings,
    this.draft,
  }) : super._();

  /// Deserialize.
  factory ForumTopic.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final my = _readflags.1?true(buffer);
    // final closed = _readflags.2?true(buffer);
    // final pinned = _readflags.3?true(buffer);
    // final short = _readflags.5?true(buffer);
    // final hidden = _readflags.6?true(buffer);
    // final id = _readint(buffer);
    // final date = _readint(buffer);
    // final title = _readstring(buffer);
    // final iconColor = _readint(buffer);
    // final iconEmojiId = _readflags.0?long(buffer);
    // final topMessage = _readint(buffer);
    // final readInboxMaxId = _readint(buffer);
    // final readOutboxMaxId = _readint(buffer);
    // final unreadCount = _readint(buffer);
    // final unreadMentionsCount = _readint(buffer);
    // final unreadReactionsCount = _readint(buffer);
    // final fromId = _readPeer(buffer);
    // final notifySettings = _readPeerNotifySettings(buffer);
    // final draft = _readflags.4?DraftMessage(buffer);
    // final result = ForumTopic(flags: flags, my: my, closed: closed, pinned: pinned, short: short, hidden: hidden, id: id, date: date, title: title, iconColor: iconColor, iconEmojiId: iconEmojiId, topMessage: topMessage, readInboxMaxId: readInboxMaxId, readOutboxMaxId: readOutboxMaxId, unreadCount: unreadCount, unreadMentionsCount: unreadMentionsCount, unreadReactionsCount: unreadReactionsCount, fromId: fromId, notifySettings: notifySettings, draft: draft,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: my,
      b02: closed,
      b03: pinned,
      b05: short,
      b06: hidden,
      b00: iconEmojiId != null,
      b04: draft != null,
    );

    return v;
  }

  /// my: bit 1 of flags.1?true
  final bool my;

  /// closed: bit 2 of flags.2?true
  final bool closed;

  /// pinned: bit 3 of flags.3?true
  final bool pinned;

  /// short: bit 5 of flags.5?true
  final bool short;

  /// hidden: bit 6 of flags.6?true
  final bool hidden;

  /// Id.
  final int id;

  /// Date.
  final DateTime date;

  /// Title.
  final String title;

  /// Icon Color.
  final int iconColor;

  /// Icon Emoji Id.
  final int? iconEmojiId;

  /// Top Message.
  final int topMessage;

  /// Read Inbox Max Id.
  final int readInboxMaxId;

  /// Read Outbox Max Id.
  final int readOutboxMaxId;

  /// Unread Count.
  final int unreadCount;

  /// Unread Mentions Count.
  final int unreadMentionsCount;

  /// Unread Reactions Count.
  final int unreadReactionsCount;

  /// From Id.
  final PeerBase fromId;

  /// Notify Settings.
  final PeerNotifySettingsBase notifySettings;

  /// Draft.
  final DraftMessageBase? draft;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x71701da9);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    buffer.writeDateTime(date);
    buffer.writeString(title);
    buffer.writeInt(iconColor);
    final localIconEmojiIdCopy = iconEmojiId;
    if (localIconEmojiIdCopy != null) {
      buffer.writeLong(localIconEmojiIdCopy);
    }
    buffer.writeInt(topMessage);
    buffer.writeInt(readInboxMaxId);
    buffer.writeInt(readOutboxMaxId);
    buffer.writeInt(unreadCount);
    buffer.writeInt(unreadMentionsCount);
    buffer.writeInt(unreadReactionsCount);
    buffer.writeObject(fromId);
    buffer.writeObject(notifySettings);
    final localDraftCopy = draft;
    if (localDraftCopy != null) {
      buffer.writeObject(localDraftCopy);
    }
  }
}

/// Predicate Messages Forum Topics.
abstract class MessagesForumTopicsBase extends TlConstructor {
  /// Predicate Messages Forum Topics constructor.
  const MessagesForumTopicsBase._() : super._();
}

/// Messages Forum Topics.
///
/// ID: `367617d3`.
class MessagesForumTopics extends MessagesForumTopicsBase {
  /// Messages Forum Topics constructor.
  const MessagesForumTopics({
    required this.orderByCreateDate,
    required this.count,
    required this.topics,
    required this.messages,
    required this.chats,
    required this.users,
    required this.pts,
  }) : super._();

  /// Deserialize.
  factory MessagesForumTopics.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final orderByCreateDate = _readflags.0?true(buffer);
    // final count = _readint(buffer);
    // final topics = _readVector<ForumTopic>(buffer);
    // final messages = _readVector<Message>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final pts = _readint(buffer);
    // final result = MessagesForumTopics(flags: flags, orderByCreateDate: orderByCreateDate, count: count, topics: topics, messages: messages, chats: chats, users: users, pts: pts,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: orderByCreateDate,
    );

    return v;
  }

  /// order_by_create_date: bit 0 of flags.0?true
  final bool orderByCreateDate;

  /// Count.
  final int count;

  /// Topics.
  final List<ForumTopicBase> topics;

  /// Messages.
  final List<MessageBase> messages;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Pts.
  final int pts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x367617d3);
    buffer.writeInt(flags);
    buffer.writeInt(count);
    buffer.writeVectorObject(topics);
    buffer.writeVectorObject(messages);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
    buffer.writeInt(pts);
  }
}

/// Predicate Default History T T L.
abstract class DefaultHistoryTTLBase extends TlConstructor {
  /// Predicate Default History T T L constructor.
  const DefaultHistoryTTLBase._() : super._();
}

/// Default History T T L.
///
/// ID: `43b46b20`.
class DefaultHistoryTTL extends DefaultHistoryTTLBase {
  /// Default History T T L constructor.
  const DefaultHistoryTTL({
    required this.period,
  }) : super._();

  /// Deserialize.
  factory DefaultHistoryTTL.deserialize(Uint8List buffer) {
    // final period = _readint(buffer);
    // final result = DefaultHistoryTTL(period: period,);

    // return result;

    throw Exception();
  }

  /// Period.
  final int period;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x43b46b20);
    buffer.writeInt(period);
  }
}

/// Predicate Exported Contact Token.
abstract class ExportedContactTokenBase extends TlConstructor {
  /// Predicate Exported Contact Token constructor.
  const ExportedContactTokenBase._() : super._();
}

/// Exported Contact Token.
///
/// ID: `41bf109b`.
class ExportedContactToken extends ExportedContactTokenBase {
  /// Exported Contact Token constructor.
  const ExportedContactToken({
    required this.url,
    required this.expires,
  }) : super._();

  /// Deserialize.
  factory ExportedContactToken.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final expires = _readint(buffer);
    // final result = ExportedContactToken(url: url, expires: expires,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Expires.
  final int expires;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x41bf109b);
    buffer.writeString(url);
    buffer.writeInt(expires);
  }
}

/// Predicate Request Peer Type.
abstract class RequestPeerTypeBase extends TlConstructor {
  /// Predicate Request Peer Type constructor.
  const RequestPeerTypeBase._() : super._();
}

/// Request Peer Type User.
///
/// ID: `5f3b8a00`.
class RequestPeerTypeUser extends RequestPeerTypeBase {
  /// Request Peer Type User constructor.
  const RequestPeerTypeUser({
    this.bot,
    this.premium,
  }) : super._();

  /// Deserialize.
  factory RequestPeerTypeUser.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final bot = _readflags.0?Bool(buffer);
    // final premium = _readflags.1?Bool(buffer);
    // final result = RequestPeerTypeUser(flags: flags, bot: bot, premium: premium,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: bot != null,
      b01: premium != null,
    );

    return v;
  }

  /// Bot.
  final bool? bot;

  /// Premium.
  final bool? premium;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5f3b8a00);
    buffer.writeInt(flags);
    final localBotCopy = bot;
    if (localBotCopy != null) {
      buffer.writeBool(localBotCopy);
    }
    final localPremiumCopy = premium;
    if (localPremiumCopy != null) {
      buffer.writeBool(localPremiumCopy);
    }
  }
}

/// Request Peer Type Chat.
///
/// ID: `c9f06e1b`.
class RequestPeerTypeChat extends RequestPeerTypeBase {
  /// Request Peer Type Chat constructor.
  const RequestPeerTypeChat({
    required this.creator,
    required this.botParticipant,
    this.hasUsername,
    this.forum,
    this.userAdminRights,
    this.botAdminRights,
  }) : super._();

  /// Deserialize.
  factory RequestPeerTypeChat.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final creator = _readflags.0?true(buffer);
    // final botParticipant = _readflags.5?true(buffer);
    // final hasUsername = _readflags.3?Bool(buffer);
    // final forum = _readflags.4?Bool(buffer);
    // final userAdminRights = _readflags.1?ChatAdminRights(buffer);
    // final botAdminRights = _readflags.2?ChatAdminRights(buffer);
    // final result = RequestPeerTypeChat(flags: flags, creator: creator, botParticipant: botParticipant, hasUsername: hasUsername, forum: forum, userAdminRights: userAdminRights, botAdminRights: botAdminRights,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: creator,
      b05: botParticipant,
      b03: hasUsername != null,
      b04: forum != null,
      b01: userAdminRights != null,
      b02: botAdminRights != null,
    );

    return v;
  }

  /// creator: bit 0 of flags.0?true
  final bool creator;

  /// bot_participant: bit 5 of flags.5?true
  final bool botParticipant;

  /// Has Username.
  final bool? hasUsername;

  /// Forum.
  final bool? forum;

  /// User Admin Rights.
  final ChatAdminRightsBase? userAdminRights;

  /// Bot Admin Rights.
  final ChatAdminRightsBase? botAdminRights;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc9f06e1b);
    buffer.writeInt(flags);
    final localHasUsernameCopy = hasUsername;
    if (localHasUsernameCopy != null) {
      buffer.writeBool(localHasUsernameCopy);
    }
    final localForumCopy = forum;
    if (localForumCopy != null) {
      buffer.writeBool(localForumCopy);
    }
    final localUserAdminRightsCopy = userAdminRights;
    if (localUserAdminRightsCopy != null) {
      buffer.writeObject(localUserAdminRightsCopy);
    }
    final localBotAdminRightsCopy = botAdminRights;
    if (localBotAdminRightsCopy != null) {
      buffer.writeObject(localBotAdminRightsCopy);
    }
  }
}

/// Request Peer Type Broadcast.
///
/// ID: `339bef6c`.
class RequestPeerTypeBroadcast extends RequestPeerTypeBase {
  /// Request Peer Type Broadcast constructor.
  const RequestPeerTypeBroadcast({
    required this.creator,
    this.hasUsername,
    this.userAdminRights,
    this.botAdminRights,
  }) : super._();

  /// Deserialize.
  factory RequestPeerTypeBroadcast.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final creator = _readflags.0?true(buffer);
    // final hasUsername = _readflags.3?Bool(buffer);
    // final userAdminRights = _readflags.1?ChatAdminRights(buffer);
    // final botAdminRights = _readflags.2?ChatAdminRights(buffer);
    // final result = RequestPeerTypeBroadcast(flags: flags, creator: creator, hasUsername: hasUsername, userAdminRights: userAdminRights, botAdminRights: botAdminRights,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: creator,
      b03: hasUsername != null,
      b01: userAdminRights != null,
      b02: botAdminRights != null,
    );

    return v;
  }

  /// creator: bit 0 of flags.0?true
  final bool creator;

  /// Has Username.
  final bool? hasUsername;

  /// User Admin Rights.
  final ChatAdminRightsBase? userAdminRights;

  /// Bot Admin Rights.
  final ChatAdminRightsBase? botAdminRights;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x339bef6c);
    buffer.writeInt(flags);
    final localHasUsernameCopy = hasUsername;
    if (localHasUsernameCopy != null) {
      buffer.writeBool(localHasUsernameCopy);
    }
    final localUserAdminRightsCopy = userAdminRights;
    if (localUserAdminRightsCopy != null) {
      buffer.writeObject(localUserAdminRightsCopy);
    }
    final localBotAdminRightsCopy = botAdminRights;
    if (localBotAdminRightsCopy != null) {
      buffer.writeObject(localBotAdminRightsCopy);
    }
  }
}

/// Predicate Emoji List.
abstract class EmojiListBase extends TlConstructor {
  /// Predicate Emoji List constructor.
  const EmojiListBase._() : super._();
}

/// Emoji List Not Modified.
///
/// ID: `481eadfa`.
class EmojiListNotModified extends EmojiListBase {
  /// Emoji List Not Modified constructor.
  const EmojiListNotModified() : super._();

  /// Deserialize.
  factory EmojiListNotModified.deserialize(Uint8List buffer) {
    // final result = EmojiListNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x481eadfa);
  }
}

/// Emoji List.
///
/// ID: `7a1e11d1`.
class EmojiList extends EmojiListBase {
  /// Emoji List constructor.
  const EmojiList({
    required this.hash,
    required this.documentId,
  }) : super._();

  /// Deserialize.
  factory EmojiList.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final documentId = _readVector<long>(buffer);
    // final result = EmojiList(hash: hash, documentId: documentId,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Document Id.
  final List<int> documentId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7a1e11d1);
    buffer.writeLong(hash);
    buffer.writeVectorLong(documentId);
  }
}

/// Predicate Emoji Group.
abstract class EmojiGroupBase extends TlConstructor {
  /// Predicate Emoji Group constructor.
  const EmojiGroupBase._() : super._();
}

/// Emoji Group.
///
/// ID: `7a9abda9`.
class EmojiGroup extends EmojiGroupBase {
  /// Emoji Group constructor.
  const EmojiGroup({
    required this.title,
    required this.iconEmojiId,
    required this.emoticons,
  }) : super._();

  /// Deserialize.
  factory EmojiGroup.deserialize(Uint8List buffer) {
    // final title = _readstring(buffer);
    // final iconEmojiId = _readlong(buffer);
    // final emoticons = _readVector<string>(buffer);
    // final result = EmojiGroup(title: title, iconEmojiId: iconEmojiId, emoticons: emoticons,);

    // return result;

    throw Exception();
  }

  /// Title.
  final String title;

  /// Icon Emoji Id.
  final int iconEmojiId;

  /// Emoticons.
  final List<String> emoticons;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7a9abda9);
    buffer.writeString(title);
    buffer.writeLong(iconEmojiId);
    buffer.writeVectorString(emoticons);
  }
}

/// Predicate Messages Emoji Groups.
abstract class MessagesEmojiGroupsBase extends TlConstructor {
  /// Predicate Messages Emoji Groups constructor.
  const MessagesEmojiGroupsBase._() : super._();
}

/// Messages Emoji Groups Not Modified.
///
/// ID: `6fb4ad87`.
class MessagesEmojiGroupsNotModified extends MessagesEmojiGroupsBase {
  /// Messages Emoji Groups Not Modified constructor.
  const MessagesEmojiGroupsNotModified() : super._();

  /// Deserialize.
  factory MessagesEmojiGroupsNotModified.deserialize(Uint8List buffer) {
    // final result = MessagesEmojiGroupsNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6fb4ad87);
  }
}

/// Messages Emoji Groups.
///
/// ID: `881fb94b`.
class MessagesEmojiGroups extends MessagesEmojiGroupsBase {
  /// Messages Emoji Groups constructor.
  const MessagesEmojiGroups({
    required this.hash,
    required this.groups,
  }) : super._();

  /// Deserialize.
  factory MessagesEmojiGroups.deserialize(Uint8List buffer) {
    // final hash = _readint(buffer);
    // final groups = _readVector<EmojiGroup>(buffer);
    // final result = MessagesEmojiGroups(hash: hash, groups: groups,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Groups.
  final List<EmojiGroupBase> groups;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x881fb94b);
    buffer.writeInt(hash);
    buffer.writeVectorObject(groups);
  }
}

/// Predicate Text With Entities.
abstract class TextWithEntitiesBase extends TlConstructor {
  /// Predicate Text With Entities constructor.
  const TextWithEntitiesBase._() : super._();
}

/// Text With Entities.
///
/// ID: `751f3146`.
class TextWithEntities extends TextWithEntitiesBase {
  /// Text With Entities constructor.
  const TextWithEntities({
    required this.text,
    required this.entities,
  }) : super._();

  /// Deserialize.
  factory TextWithEntities.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final entities = _readVector<MessageEntity>(buffer);
    // final result = TextWithEntities(text: text, entities: entities,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Entities.
  final List<MessageEntityBase> entities;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x751f3146);
    buffer.writeString(text);
    buffer.writeVectorObject(entities);
  }
}

/// Predicate Messages Translated Text.
abstract class MessagesTranslatedTextBase extends TlConstructor {
  /// Predicate Messages Translated Text constructor.
  const MessagesTranslatedTextBase._() : super._();
}

/// Messages Translate Result.
///
/// ID: `33db32f8`.
class MessagesTranslateResult extends MessagesTranslatedTextBase {
  /// Messages Translate Result constructor.
  const MessagesTranslateResult({
    required this.result,
  }) : super._();

  /// Deserialize.
  factory MessagesTranslateResult.deserialize(Uint8List buffer) {
    // final result = _readVector<TextWithEntities>(buffer);
    // final result = MessagesTranslateResult(result: result,);

    // return result;

    throw Exception();
  }

  /// Result.
  final List<TextWithEntitiesBase> result;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x33db32f8);
    buffer.writeVectorObject(result);
  }
}

/// Predicate Auto Save Settings.
abstract class AutoSaveSettingsBase extends TlConstructor {
  /// Predicate Auto Save Settings constructor.
  const AutoSaveSettingsBase._() : super._();
}

/// Auto Save Settings.
///
/// ID: `c84834ce`.
class AutoSaveSettings extends AutoSaveSettingsBase {
  /// Auto Save Settings constructor.
  const AutoSaveSettings({
    required this.photos,
    required this.videos,
    this.videoMaxSize,
  }) : super._();

  /// Deserialize.
  factory AutoSaveSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final photos = _readflags.0?true(buffer);
    // final videos = _readflags.1?true(buffer);
    // final videoMaxSize = _readflags.2?long(buffer);
    // final result = AutoSaveSettings(flags: flags, photos: photos, videos: videos, videoMaxSize: videoMaxSize,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: photos,
      b01: videos,
      b02: videoMaxSize != null,
    );

    return v;
  }

  /// photos: bit 0 of flags.0?true
  final bool photos;

  /// videos: bit 1 of flags.1?true
  final bool videos;

  /// Video Max Size.
  final int? videoMaxSize;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc84834ce);
    buffer.writeInt(flags);
    final localVideoMaxSizeCopy = videoMaxSize;
    if (localVideoMaxSizeCopy != null) {
      buffer.writeLong(localVideoMaxSizeCopy);
    }
  }
}

/// Predicate Auto Save Exception.
abstract class AutoSaveExceptionBase extends TlConstructor {
  /// Predicate Auto Save Exception constructor.
  const AutoSaveExceptionBase._() : super._();
}

/// Auto Save Exception.
///
/// ID: `81602d47`.
class AutoSaveException extends AutoSaveExceptionBase {
  /// Auto Save Exception constructor.
  const AutoSaveException({
    required this.peer,
    required this.settings,
  }) : super._();

  /// Deserialize.
  factory AutoSaveException.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final settings = _readAutoSaveSettings(buffer);
    // final result = AutoSaveException(peer: peer, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Settings.
  final AutoSaveSettingsBase settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x81602d47);
    buffer.writeObject(peer);
    buffer.writeObject(settings);
  }
}

/// Predicate Account Auto Save Settings.
abstract class AccountAutoSaveSettingsBase extends TlConstructor {
  /// Predicate Account Auto Save Settings constructor.
  const AccountAutoSaveSettingsBase._() : super._();
}

/// Account Auto Save Settings.
///
/// ID: `4c3e069d`.
class AccountAutoSaveSettings extends AccountAutoSaveSettingsBase {
  /// Account Auto Save Settings constructor.
  const AccountAutoSaveSettings({
    required this.usersSettings,
    required this.chatsSettings,
    required this.broadcastsSettings,
    required this.exceptions,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory AccountAutoSaveSettings.deserialize(Uint8List buffer) {
    // final usersSettings = _readAutoSaveSettings(buffer);
    // final chatsSettings = _readAutoSaveSettings(buffer);
    // final broadcastsSettings = _readAutoSaveSettings(buffer);
    // final exceptions = _readVector<AutoSaveException>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = AccountAutoSaveSettings(usersSettings: usersSettings, chatsSettings: chatsSettings, broadcastsSettings: broadcastsSettings, exceptions: exceptions, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Users Settings.
  final AutoSaveSettingsBase usersSettings;

  /// Chats Settings.
  final AutoSaveSettingsBase chatsSettings;

  /// Broadcasts Settings.
  final AutoSaveSettingsBase broadcastsSettings;

  /// Exceptions.
  final List<AutoSaveExceptionBase> exceptions;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4c3e069d);
    buffer.writeObject(usersSettings);
    buffer.writeObject(chatsSettings);
    buffer.writeObject(broadcastsSettings);
    buffer.writeVectorObject(exceptions);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Help App Config.
abstract class HelpAppConfigBase extends TlConstructor {
  /// Predicate Help App Config constructor.
  const HelpAppConfigBase._() : super._();
}

/// Help App Config Not Modified.
///
/// ID: `7cde641d`.
class HelpAppConfigNotModified extends HelpAppConfigBase {
  /// Help App Config Not Modified constructor.
  const HelpAppConfigNotModified() : super._();

  /// Deserialize.
  factory HelpAppConfigNotModified.deserialize(Uint8List buffer) {
    // final result = HelpAppConfigNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7cde641d);
  }
}

/// Help App Config.
///
/// ID: `dd18782e`.
class HelpAppConfig extends HelpAppConfigBase {
  /// Help App Config constructor.
  const HelpAppConfig({
    required this.hash,
    required this.config,
  }) : super._();

  /// Deserialize.
  factory HelpAppConfig.deserialize(Uint8List buffer) {
    // final hash = _readint(buffer);
    // final config = _readJSONValue(buffer);
    // final result = HelpAppConfig(hash: hash, config: config,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Config.
  final JSONValueBase config;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdd18782e);
    buffer.writeInt(hash);
    buffer.writeObject(config);
  }
}

/// Predicate Input Bot App.
abstract class InputBotAppBase extends TlConstructor {
  /// Predicate Input Bot App constructor.
  const InputBotAppBase._() : super._();
}

/// Input Bot App I D.
///
/// ID: `a920bd7a`.
class InputBotAppID extends InputBotAppBase {
  /// Input Bot App I D constructor.
  const InputBotAppID({
    required this.id,
    required this.accessHash,
  }) : super._();

  /// Deserialize.
  factory InputBotAppID.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final result = InputBotAppID(id: id, accessHash: accessHash,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa920bd7a);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
  }
}

/// Input Bot App Short Name.
///
/// ID: `908c0407`.
class InputBotAppShortName extends InputBotAppBase {
  /// Input Bot App Short Name constructor.
  const InputBotAppShortName({
    required this.botId,
    required this.shortName,
  }) : super._();

  /// Deserialize.
  factory InputBotAppShortName.deserialize(Uint8List buffer) {
    // final botId = _readInputUser(buffer);
    // final shortName = _readstring(buffer);
    // final result = InputBotAppShortName(botId: botId, shortName: shortName,);

    // return result;

    throw Exception();
  }

  /// Bot Id.
  final InputUserBase botId;

  /// Short Name.
  final String shortName;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x908c0407);
    buffer.writeObject(botId);
    buffer.writeString(shortName);
  }
}

/// Predicate Bot App.
abstract class BotAppBase extends TlConstructor {
  /// Predicate Bot App constructor.
  const BotAppBase._() : super._();
}

/// Bot App Not Modified.
///
/// ID: `5da674b7`.
class BotAppNotModified extends BotAppBase {
  /// Bot App Not Modified constructor.
  const BotAppNotModified() : super._();

  /// Deserialize.
  factory BotAppNotModified.deserialize(Uint8List buffer) {
    // final result = BotAppNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5da674b7);
  }
}

/// Bot App.
///
/// ID: `95fcd1d6`.
class BotApp extends BotAppBase {
  /// Bot App constructor.
  const BotApp({
    required this.id,
    required this.accessHash,
    required this.shortName,
    required this.title,
    required this.description,
    required this.photo,
    this.document,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory BotApp.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final id = _readlong(buffer);
    // final accessHash = _readlong(buffer);
    // final shortName = _readstring(buffer);
    // final title = _readstring(buffer);
    // final description = _readstring(buffer);
    // final photo = _readPhoto(buffer);
    // final document = _readflags.0?Document(buffer);
    // final hash = _readlong(buffer);
    // final result = BotApp(flags: flags, id: id, accessHash: accessHash, shortName: shortName, title: title, description: description, photo: photo, document: document, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: document != null,
    );

    return v;
  }

  /// Id.
  final int id;

  /// Access Hash.
  final int accessHash;

  /// Short Name.
  final String shortName;

  /// Title.
  final String title;

  /// Description.
  final String description;

  /// Photo.
  final PhotoBase photo;

  /// Document.
  final DocumentBase? document;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x95fcd1d6);
    buffer.writeInt(flags);
    buffer.writeLong(id);
    buffer.writeLong(accessHash);
    buffer.writeString(shortName);
    buffer.writeString(title);
    buffer.writeString(description);
    buffer.writeObject(photo);
    final localDocumentCopy = document;
    if (localDocumentCopy != null) {
      buffer.writeObject(localDocumentCopy);
    }
    buffer.writeLong(hash);
  }
}

/// Predicate Messages Bot App.
abstract class MessagesBotAppBase extends TlConstructor {
  /// Predicate Messages Bot App constructor.
  const MessagesBotAppBase._() : super._();
}

/// Messages Bot App.
///
/// ID: `eb50adf5`.
class MessagesBotApp extends MessagesBotAppBase {
  /// Messages Bot App constructor.
  const MessagesBotApp({
    required this.inactive,
    required this.requestWriteAccess,
    required this.hasSettings,
    required this.app,
  }) : super._();

  /// Deserialize.
  factory MessagesBotApp.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final inactive = _readflags.0?true(buffer);
    // final requestWriteAccess = _readflags.1?true(buffer);
    // final hasSettings = _readflags.2?true(buffer);
    // final app = _readBotApp(buffer);
    // final result = MessagesBotApp(flags: flags, inactive: inactive, requestWriteAccess: requestWriteAccess, hasSettings: hasSettings, app: app,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: inactive,
      b01: requestWriteAccess,
      b02: hasSettings,
    );

    return v;
  }

  /// inactive: bit 0 of flags.0?true
  final bool inactive;

  /// request_write_access: bit 1 of flags.1?true
  final bool requestWriteAccess;

  /// has_settings: bit 2 of flags.2?true
  final bool hasSettings;

  /// App.
  final BotAppBase app;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xeb50adf5);
    buffer.writeInt(flags);
    buffer.writeObject(app);
  }
}

/// Predicate App Web View Result.
abstract class AppWebViewResultBase extends TlConstructor {
  /// Predicate App Web View Result constructor.
  const AppWebViewResultBase._() : super._();
}

/// App Web View Result Url.
///
/// ID: `3c1b4f0d`.
class AppWebViewResultUrl extends AppWebViewResultBase {
  /// App Web View Result Url constructor.
  const AppWebViewResultUrl({
    required this.url,
  }) : super._();

  /// Deserialize.
  factory AppWebViewResultUrl.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final result = AppWebViewResultUrl(url: url,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3c1b4f0d);
    buffer.writeString(url);
  }
}

/// Predicate Inline Bot Web View.
abstract class InlineBotWebViewBase extends TlConstructor {
  /// Predicate Inline Bot Web View constructor.
  const InlineBotWebViewBase._() : super._();
}

/// Inline Bot Web View.
///
/// ID: `b57295d5`.
class InlineBotWebView extends InlineBotWebViewBase {
  /// Inline Bot Web View constructor.
  const InlineBotWebView({
    required this.text,
    required this.url,
  }) : super._();

  /// Deserialize.
  factory InlineBotWebView.deserialize(Uint8List buffer) {
    // final text = _readstring(buffer);
    // final url = _readstring(buffer);
    // final result = InlineBotWebView(text: text, url: url,);

    // return result;

    throw Exception();
  }

  /// Text.
  final String text;

  /// Url.
  final String url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb57295d5);
    buffer.writeString(text);
    buffer.writeString(url);
  }
}

/// Predicate Read Participant Date.
abstract class ReadParticipantDateBase extends TlConstructor {
  /// Predicate Read Participant Date constructor.
  const ReadParticipantDateBase._() : super._();
}

/// Read Participant Date.
///
/// ID: `4a4ff172`.
class ReadParticipantDate extends ReadParticipantDateBase {
  /// Read Participant Date constructor.
  const ReadParticipantDate({
    required this.userId,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory ReadParticipantDate.deserialize(Uint8List buffer) {
    // final userId = _readlong(buffer);
    // final date = _readint(buffer);
    // final result = ReadParticipantDate(userId: userId, date: date,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final int userId;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4a4ff172);
    buffer.writeLong(userId);
    buffer.writeDateTime(date);
  }
}

/// Predicate Input Chatlist.
abstract class InputChatlistBase extends TlConstructor {
  /// Predicate Input Chatlist constructor.
  const InputChatlistBase._() : super._();
}

/// Input Chatlist Dialog Filter.
///
/// ID: `f3e0da33`.
class InputChatlistDialogFilter extends InputChatlistBase {
  /// Input Chatlist Dialog Filter constructor.
  const InputChatlistDialogFilter({
    required this.filterId,
  }) : super._();

  /// Deserialize.
  factory InputChatlistDialogFilter.deserialize(Uint8List buffer) {
    // final filterId = _readint(buffer);
    // final result = InputChatlistDialogFilter(filterId: filterId,);

    // return result;

    throw Exception();
  }

  /// Filter Id.
  final int filterId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf3e0da33);
    buffer.writeInt(filterId);
  }
}

/// Predicate Exported Chatlist Invite.
abstract class ExportedChatlistInviteBase extends TlConstructor {
  /// Predicate Exported Chatlist Invite constructor.
  const ExportedChatlistInviteBase._() : super._();
}

/// Exported Chatlist Invite.
///
/// ID: `0c5181ac`.
class ExportedChatlistInvite extends ExportedChatlistInviteBase {
  /// Exported Chatlist Invite constructor.
  const ExportedChatlistInvite({
    required this.title,
    required this.url,
    required this.peers,
  }) : super._();

  /// Deserialize.
  factory ExportedChatlistInvite.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final title = _readstring(buffer);
    // final url = _readstring(buffer);
    // final peers = _readVector<Peer>(buffer);
    // final result = ExportedChatlistInvite(flags: flags, title: title, url: url, peers: peers,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag();

    return v;
  }

  /// Title.
  final String title;

  /// Url.
  final String url;

  /// Peers.
  final List<PeerBase> peers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0c5181ac);
    buffer.writeInt(flags);
    buffer.writeString(title);
    buffer.writeString(url);
    buffer.writeVectorObject(peers);
  }
}

/// Predicate Chatlists Exported Chatlist Invite.
abstract class ChatlistsExportedChatlistInviteBase extends TlConstructor {
  /// Predicate Chatlists Exported Chatlist Invite constructor.
  const ChatlistsExportedChatlistInviteBase._() : super._();
}

/// Chatlists Exported Chatlist Invite.
///
/// ID: `10e6e3a6`.
class ChatlistsExportedChatlistInvite
    extends ChatlistsExportedChatlistInviteBase {
  /// Chatlists Exported Chatlist Invite constructor.
  const ChatlistsExportedChatlistInvite({
    required this.filter,
    required this.invite,
  }) : super._();

  /// Deserialize.
  factory ChatlistsExportedChatlistInvite.deserialize(Uint8List buffer) {
    // final filter = _readDialogFilter(buffer);
    // final invite = _readExportedChatlistInvite(buffer);
    // final result = ChatlistsExportedChatlistInvite(filter: filter, invite: invite,);

    // return result;

    throw Exception();
  }

  /// Filter.
  final DialogFilterBase filter;

  /// Invite.
  final ExportedChatlistInviteBase invite;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x10e6e3a6);
    buffer.writeObject(filter);
    buffer.writeObject(invite);
  }
}

/// Predicate Chatlists Exported Invites.
abstract class ChatlistsExportedInvitesBase extends TlConstructor {
  /// Predicate Chatlists Exported Invites constructor.
  const ChatlistsExportedInvitesBase._() : super._();
}

/// Chatlists Exported Invites.
///
/// ID: `10ab6dc7`.
class ChatlistsExportedInvites extends ChatlistsExportedInvitesBase {
  /// Chatlists Exported Invites constructor.
  const ChatlistsExportedInvites({
    required this.invites,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ChatlistsExportedInvites.deserialize(Uint8List buffer) {
    // final invites = _readVector<ExportedChatlistInvite>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ChatlistsExportedInvites(invites: invites, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Invites.
  final List<ExportedChatlistInviteBase> invites;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x10ab6dc7);
    buffer.writeVectorObject(invites);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Chatlists Chatlist Invite.
abstract class ChatlistsChatlistInviteBase extends TlConstructor {
  /// Predicate Chatlists Chatlist Invite constructor.
  const ChatlistsChatlistInviteBase._() : super._();
}

/// Chatlists Chatlist Invite Already.
///
/// ID: `fa87f659`.
class ChatlistsChatlistInviteAlready extends ChatlistsChatlistInviteBase {
  /// Chatlists Chatlist Invite Already constructor.
  const ChatlistsChatlistInviteAlready({
    required this.filterId,
    required this.missingPeers,
    required this.alreadyPeers,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ChatlistsChatlistInviteAlready.deserialize(Uint8List buffer) {
    // final filterId = _readint(buffer);
    // final missingPeers = _readVector<Peer>(buffer);
    // final alreadyPeers = _readVector<Peer>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ChatlistsChatlistInviteAlready(filterId: filterId, missingPeers: missingPeers, alreadyPeers: alreadyPeers, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Filter Id.
  final int filterId;

  /// Missing Peers.
  final List<PeerBase> missingPeers;

  /// Already Peers.
  final List<PeerBase> alreadyPeers;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfa87f659);
    buffer.writeInt(filterId);
    buffer.writeVectorObject(missingPeers);
    buffer.writeVectorObject(alreadyPeers);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Chatlists Chatlist Invite.
///
/// ID: `1dcd839d`.
class ChatlistsChatlistInvite extends ChatlistsChatlistInviteBase {
  /// Chatlists Chatlist Invite constructor.
  const ChatlistsChatlistInvite({
    required this.title,
    this.emoticon,
    required this.peers,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ChatlistsChatlistInvite.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final title = _readstring(buffer);
    // final emoticon = _readflags.0?string(buffer);
    // final peers = _readVector<Peer>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ChatlistsChatlistInvite(flags: flags, title: title, emoticon: emoticon, peers: peers, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: emoticon != null,
    );

    return v;
  }

  /// Title.
  final String title;

  /// Emoticon.
  final String? emoticon;

  /// Peers.
  final List<PeerBase> peers;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1dcd839d);
    buffer.writeInt(flags);
    buffer.writeString(title);
    final localEmoticonCopy = emoticon;
    if (localEmoticonCopy != null) {
      buffer.writeString(localEmoticonCopy);
    }
    buffer.writeVectorObject(peers);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Chatlists Chatlist Updates.
abstract class ChatlistsChatlistUpdatesBase extends TlConstructor {
  /// Predicate Chatlists Chatlist Updates constructor.
  const ChatlistsChatlistUpdatesBase._() : super._();
}

/// Chatlists Chatlist Updates.
///
/// ID: `93bd878d`.
class ChatlistsChatlistUpdates extends ChatlistsChatlistUpdatesBase {
  /// Chatlists Chatlist Updates constructor.
  const ChatlistsChatlistUpdates({
    required this.missingPeers,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ChatlistsChatlistUpdates.deserialize(Uint8List buffer) {
    // final missingPeers = _readVector<Peer>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = ChatlistsChatlistUpdates(missingPeers: missingPeers, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Missing Peers.
  final List<PeerBase> missingPeers;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x93bd878d);
    buffer.writeVectorObject(missingPeers);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Bots Bot Info.
abstract class BotsBotInfoBase extends TlConstructor {
  /// Predicate Bots Bot Info constructor.
  const BotsBotInfoBase._() : super._();
}

/// Bots Bot Info.
///
/// ID: `e8a775b0`.
class BotsBotInfo extends BotsBotInfoBase {
  /// Bots Bot Info constructor.
  const BotsBotInfo({
    required this.name,
    required this.about,
    required this.description,
  }) : super._();

  /// Deserialize.
  factory BotsBotInfo.deserialize(Uint8List buffer) {
    // final name = _readstring(buffer);
    // final about = _readstring(buffer);
    // final description = _readstring(buffer);
    // final result = BotsBotInfo(name: name, about: about, description: description,);

    // return result;

    throw Exception();
  }

  /// Name.
  final String name;

  /// About.
  final String about;

  /// Description.
  final String description;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe8a775b0);
    buffer.writeString(name);
    buffer.writeString(about);
    buffer.writeString(description);
  }
}

/// Predicate Message Peer Vote.
abstract class MessagePeerVoteBase extends TlConstructor {
  /// Predicate Message Peer Vote constructor.
  const MessagePeerVoteBase._() : super._();
}

/// Message Peer Vote.
///
/// ID: `b6cc2d5c`.
class MessagePeerVote extends MessagePeerVoteBase {
  /// Message Peer Vote constructor.
  const MessagePeerVote({
    required this.peer,
    required this.option,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory MessagePeerVote.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final option = _readbytes(buffer);
    // final date = _readint(buffer);
    // final result = MessagePeerVote(peer: peer, option: option, date: date,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Option.
  final Uint8List option;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb6cc2d5c);
    buffer.writeObject(peer);
    buffer.writeBytes(option);
    buffer.writeDateTime(date);
  }
}

/// Message Peer Vote Input Option.
///
/// ID: `74cda504`.
class MessagePeerVoteInputOption extends MessagePeerVoteBase {
  /// Message Peer Vote Input Option constructor.
  const MessagePeerVoteInputOption({
    required this.peer,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory MessagePeerVoteInputOption.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final date = _readint(buffer);
    // final result = MessagePeerVoteInputOption(peer: peer, date: date,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x74cda504);
    buffer.writeObject(peer);
    buffer.writeDateTime(date);
  }
}

/// Message Peer Vote Multiple.
///
/// ID: `4628f6e6`.
class MessagePeerVoteMultiple extends MessagePeerVoteBase {
  /// Message Peer Vote Multiple constructor.
  const MessagePeerVoteMultiple({
    required this.peer,
    required this.options,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory MessagePeerVoteMultiple.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final options = _readVector<bytes>(buffer);
    // final date = _readint(buffer);
    // final result = MessagePeerVoteMultiple(peer: peer, options: options, date: date,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Options.
  final List<Uint8List> options;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4628f6e6);
    buffer.writeObject(peer);
    buffer.writeVectorBytes(options);
    buffer.writeDateTime(date);
  }
}

/// Predicate Sponsored Web Page.
abstract class SponsoredWebPageBase extends TlConstructor {
  /// Predicate Sponsored Web Page constructor.
  const SponsoredWebPageBase._() : super._();
}

/// Sponsored Web Page.
///
/// ID: `3db8ec63`.
class SponsoredWebPage extends SponsoredWebPageBase {
  /// Sponsored Web Page constructor.
  const SponsoredWebPage({
    required this.url,
    required this.siteName,
    this.photo,
  }) : super._();

  /// Deserialize.
  factory SponsoredWebPage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final url = _readstring(buffer);
    // final siteName = _readstring(buffer);
    // final photo = _readflags.0?Photo(buffer);
    // final result = SponsoredWebPage(flags: flags, url: url, siteName: siteName, photo: photo,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: photo != null,
    );

    return v;
  }

  /// Url.
  final String url;

  /// Site Name.
  final String siteName;

  /// Photo.
  final PhotoBase? photo;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3db8ec63);
    buffer.writeInt(flags);
    buffer.writeString(url);
    buffer.writeString(siteName);
    final localPhotoCopy = photo;
    if (localPhotoCopy != null) {
      buffer.writeObject(localPhotoCopy);
    }
  }
}

/// Predicate Story Views.
abstract class StoryViewsBase extends TlConstructor {
  /// Predicate Story Views constructor.
  const StoryViewsBase._() : super._();
}

/// Story Views.
///
/// ID: `8d595cd6`.
class StoryViews extends StoryViewsBase {
  /// Story Views constructor.
  const StoryViews({
    required this.hasViewers,
    required this.viewsCount,
    this.forwardsCount,
    this.reactions,
    this.reactionsCount,
    this.recentViewers,
  }) : super._();

  /// Deserialize.
  factory StoryViews.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final hasViewers = _readflags.1?true(buffer);
    // final viewsCount = _readint(buffer);
    // final forwardsCount = _readflags.2?int(buffer);
    // final reactions = _readflags.3?Vector<ReactionCount>(buffer);
    // final reactionsCount = _readflags.4?int(buffer);
    // final recentViewers = _readflags.0?Vector<long>(buffer);
    // final result = StoryViews(flags: flags, hasViewers: hasViewers, viewsCount: viewsCount, forwardsCount: forwardsCount, reactions: reactions, reactionsCount: reactionsCount, recentViewers: recentViewers,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: hasViewers,
      b02: forwardsCount != null,
      b03: reactions != null,
      b04: reactionsCount != null,
      b00: recentViewers != null,
    );

    return v;
  }

  /// has_viewers: bit 1 of flags.1?true
  final bool hasViewers;

  /// Views Count.
  final int viewsCount;

  /// Forwards Count.
  final int? forwardsCount;

  /// Reactions.
  final List<ReactionCountBase>? reactions;

  /// Reactions Count.
  final int? reactionsCount;

  /// Recent Viewers.
  final List<int>? recentViewers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8d595cd6);
    buffer.writeInt(flags);
    buffer.writeInt(viewsCount);
    final localForwardsCountCopy = forwardsCount;
    if (localForwardsCountCopy != null) {
      buffer.writeInt(localForwardsCountCopy);
    }
    final localReactionsCopy = reactions;
    if (localReactionsCopy != null) {
      buffer.writeVectorObject(localReactionsCopy);
    }
    final localReactionsCountCopy = reactionsCount;
    if (localReactionsCountCopy != null) {
      buffer.writeInt(localReactionsCountCopy);
    }
    final localRecentViewersCopy = recentViewers;
    if (localRecentViewersCopy != null) {
      buffer.writeVectorLong(localRecentViewersCopy);
    }
  }
}

/// Predicate Story Item.
abstract class StoryItemBase extends TlConstructor {
  /// Predicate Story Item constructor.
  const StoryItemBase._() : super._();
}

/// Story Item Deleted.
///
/// ID: `51e6ee4f`.
class StoryItemDeleted extends StoryItemBase {
  /// Story Item Deleted constructor.
  const StoryItemDeleted({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory StoryItemDeleted.deserialize(Uint8List buffer) {
    // final id = _readint(buffer);
    // final result = StoryItemDeleted(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x51e6ee4f);
    buffer.writeInt(id);
  }
}

/// Story Item Skipped.
///
/// ID: `ffadc913`.
class StoryItemSkipped extends StoryItemBase {
  /// Story Item Skipped constructor.
  const StoryItemSkipped({
    required this.closeFriends,
    required this.id,
    required this.date,
    required this.expireDate,
  }) : super._();

  /// Deserialize.
  factory StoryItemSkipped.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final closeFriends = _readflags.8?true(buffer);
    // final id = _readint(buffer);
    // final date = _readint(buffer);
    // final expireDate = _readint(buffer);
    // final result = StoryItemSkipped(flags: flags, closeFriends: closeFriends, id: id, date: date, expireDate: expireDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b08: closeFriends,
    );

    return v;
  }

  /// close_friends: bit 8 of flags.8?true
  final bool closeFriends;

  /// Id.
  final int id;

  /// Date.
  final DateTime date;

  /// Expire Date.
  final DateTime expireDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xffadc913);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    buffer.writeDateTime(date);
    buffer.writeDateTime(expireDate);
  }
}

/// Story Item.
///
/// ID: `af6365a1`.
class StoryItem extends StoryItemBase {
  /// Story Item constructor.
  const StoryItem({
    required this.pinned,
    required this.public,
    required this.closeFriends,
    required this.min,
    required this.noforwards,
    required this.edited,
    required this.contacts,
    required this.selectedContacts,
    required this.out,
    required this.id,
    required this.date,
    this.fwdFrom,
    required this.expireDate,
    this.caption,
    this.entities,
    required this.media,
    this.mediaAreas,
    this.privacy,
    this.views,
    this.sentReaction,
  }) : super._();

  /// Deserialize.
  factory StoryItem.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pinned = _readflags.5?true(buffer);
    // final public = _readflags.7?true(buffer);
    // final closeFriends = _readflags.8?true(buffer);
    // final min = _readflags.9?true(buffer);
    // final noforwards = _readflags.10?true(buffer);
    // final edited = _readflags.11?true(buffer);
    // final contacts = _readflags.12?true(buffer);
    // final selectedContacts = _readflags.13?true(buffer);
    // final out = _readflags.16?true(buffer);
    // final id = _readint(buffer);
    // final date = _readint(buffer);
    // final fwdFrom = _readflags.17?StoryFwdHeader(buffer);
    // final expireDate = _readint(buffer);
    // final caption = _readflags.0?string(buffer);
    // final entities = _readflags.1?Vector<MessageEntity>(buffer);
    // final media = _readMessageMedia(buffer);
    // final mediaAreas = _readflags.14?Vector<MediaArea>(buffer);
    // final privacy = _readflags.2?Vector<PrivacyRule>(buffer);
    // final views = _readflags.3?StoryViews(buffer);
    // final sentReaction = _readflags.15?Reaction(buffer);
    // final result = StoryItem(flags: flags, pinned: pinned, public: public, closeFriends: closeFriends, min: min, noforwards: noforwards, edited: edited, contacts: contacts, selectedContacts: selectedContacts, out: out, id: id, date: date, fwdFrom: fwdFrom, expireDate: expireDate, caption: caption, entities: entities, media: media, mediaAreas: mediaAreas, privacy: privacy, views: views, sentReaction: sentReaction,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b05: pinned,
      b07: public,
      b08: closeFriends,
      b09: min,
      b10: noforwards,
      b11: edited,
      b12: contacts,
      b13: selectedContacts,
      b16: out,
      b17: fwdFrom != null,
      b00: caption != null,
      b01: entities != null,
      b14: mediaAreas != null,
      b02: privacy != null,
      b03: views != null,
      b15: sentReaction != null,
    );

    return v;
  }

  /// pinned: bit 5 of flags.5?true
  final bool pinned;

  /// public: bit 7 of flags.7?true
  final bool public;

  /// close_friends: bit 8 of flags.8?true
  final bool closeFriends;

  /// min: bit 9 of flags.9?true
  final bool min;

  /// noforwards: bit 10 of flags.10?true
  final bool noforwards;

  /// edited: bit 11 of flags.11?true
  final bool edited;

  /// contacts: bit 12 of flags.12?true
  final bool contacts;

  /// selected_contacts: bit 13 of flags.13?true
  final bool selectedContacts;

  /// out: bit 16 of flags.16?true
  final bool out;

  /// Id.
  final int id;

  /// Date.
  final DateTime date;

  /// Fwd From.
  final StoryFwdHeaderBase? fwdFrom;

  /// Expire Date.
  final DateTime expireDate;

  /// Caption.
  final String? caption;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Media.
  final MessageMediaBase media;

  /// Media Areas.
  final List<MediaAreaBase>? mediaAreas;

  /// Privacy.
  final List<PrivacyRuleBase>? privacy;

  /// Views.
  final StoryViewsBase? views;

  /// Sent Reaction.
  final ReactionBase? sentReaction;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xaf6365a1);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    buffer.writeDateTime(date);
    final localFwdFromCopy = fwdFrom;
    if (localFwdFromCopy != null) {
      buffer.writeObject(localFwdFromCopy);
    }
    buffer.writeDateTime(expireDate);
    final localCaptionCopy = caption;
    if (localCaptionCopy != null) {
      buffer.writeString(localCaptionCopy);
    }
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    buffer.writeObject(media);
    final localMediaAreasCopy = mediaAreas;
    if (localMediaAreasCopy != null) {
      buffer.writeVectorObject(localMediaAreasCopy);
    }
    final localPrivacyCopy = privacy;
    if (localPrivacyCopy != null) {
      buffer.writeVectorObject(localPrivacyCopy);
    }
    final localViewsCopy = views;
    if (localViewsCopy != null) {
      buffer.writeObject(localViewsCopy);
    }
    final localSentReactionCopy = sentReaction;
    if (localSentReactionCopy != null) {
      buffer.writeObject(localSentReactionCopy);
    }
  }
}

/// Predicate Stories All Stories.
abstract class StoriesAllStoriesBase extends TlConstructor {
  /// Predicate Stories All Stories constructor.
  const StoriesAllStoriesBase._() : super._();
}

/// Stories All Stories Not Modified.
///
/// ID: `1158fe3e`.
class StoriesAllStoriesNotModified extends StoriesAllStoriesBase {
  /// Stories All Stories Not Modified constructor.
  const StoriesAllStoriesNotModified({
    required this.state,
    required this.stealthMode,
  }) : super._();

  /// Deserialize.
  factory StoriesAllStoriesNotModified.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final state = _readstring(buffer);
    // final stealthMode = _readStoriesStealthMode(buffer);
    // final result = StoriesAllStoriesNotModified(flags: flags, state: state, stealthMode: stealthMode,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag();

    return v;
  }

  /// State.
  final String state;

  /// Stealth Mode.
  final StoriesStealthModeBase stealthMode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1158fe3e);
    buffer.writeInt(flags);
    buffer.writeString(state);
    buffer.writeObject(stealthMode);
  }
}

/// Stories All Stories.
///
/// ID: `6efc5e81`.
class StoriesAllStories extends StoriesAllStoriesBase {
  /// Stories All Stories constructor.
  const StoriesAllStories({
    required this.hasMore,
    required this.count,
    required this.state,
    required this.peerStories,
    required this.chats,
    required this.users,
    required this.stealthMode,
  }) : super._();

  /// Deserialize.
  factory StoriesAllStories.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final hasMore = _readflags.0?true(buffer);
    // final count = _readint(buffer);
    // final state = _readstring(buffer);
    // final peerStories = _readVector<PeerStories>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final stealthMode = _readStoriesStealthMode(buffer);
    // final result = StoriesAllStories(flags: flags, hasMore: hasMore, count: count, state: state, peerStories: peerStories, chats: chats, users: users, stealthMode: stealthMode,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: hasMore,
    );

    return v;
  }

  /// has_more: bit 0 of flags.0?true
  final bool hasMore;

  /// Count.
  final int count;

  /// State.
  final String state;

  /// Peer Stories.
  final List<PeerStoriesBase> peerStories;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Stealth Mode.
  final StoriesStealthModeBase stealthMode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6efc5e81);
    buffer.writeInt(flags);
    buffer.writeInt(count);
    buffer.writeString(state);
    buffer.writeVectorObject(peerStories);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
    buffer.writeObject(stealthMode);
  }
}

/// Predicate Stories Stories.
abstract class StoriesStoriesBase extends TlConstructor {
  /// Predicate Stories Stories constructor.
  const StoriesStoriesBase._() : super._();
}

/// Stories Stories.
///
/// ID: `5dd8c3c8`.
class StoriesStories extends StoriesStoriesBase {
  /// Stories Stories constructor.
  const StoriesStories({
    required this.count,
    required this.stories,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory StoriesStories.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final stories = _readVector<StoryItem>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = StoriesStories(count: count, stories: stories, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Stories.
  final List<StoryItemBase> stories;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5dd8c3c8);
    buffer.writeInt(count);
    buffer.writeVectorObject(stories);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Story View.
abstract class StoryViewBase extends TlConstructor {
  /// Predicate Story View constructor.
  const StoryViewBase._() : super._();
}

/// Story View.
///
/// ID: `b0bdeac5`.
class StoryView extends StoryViewBase {
  /// Story View constructor.
  const StoryView({
    required this.blocked,
    required this.blockedMyStoriesFrom,
    required this.userId,
    required this.date,
    this.reaction,
  }) : super._();

  /// Deserialize.
  factory StoryView.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final blocked = _readflags.0?true(buffer);
    // final blockedMyStoriesFrom = _readflags.1?true(buffer);
    // final userId = _readlong(buffer);
    // final date = _readint(buffer);
    // final reaction = _readflags.2?Reaction(buffer);
    // final result = StoryView(flags: flags, blocked: blocked, blockedMyStoriesFrom: blockedMyStoriesFrom, userId: userId, date: date, reaction: reaction,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: blocked,
      b01: blockedMyStoriesFrom,
      b02: reaction != null,
    );

    return v;
  }

  /// blocked: bit 0 of flags.0?true
  final bool blocked;

  /// blocked_my_stories_from: bit 1 of flags.1?true
  final bool blockedMyStoriesFrom;

  /// User Id.
  final int userId;

  /// Date.
  final DateTime date;

  /// Reaction.
  final ReactionBase? reaction;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb0bdeac5);
    buffer.writeInt(flags);
    buffer.writeLong(userId);
    buffer.writeDateTime(date);
    final localReactionCopy = reaction;
    if (localReactionCopy != null) {
      buffer.writeObject(localReactionCopy);
    }
  }
}

/// Story View Public Forward.
///
/// ID: `9083670b`.
class StoryViewPublicForward extends StoryViewBase {
  /// Story View Public Forward constructor.
  const StoryViewPublicForward({
    required this.blocked,
    required this.blockedMyStoriesFrom,
    required this.message,
  }) : super._();

  /// Deserialize.
  factory StoryViewPublicForward.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final blocked = _readflags.0?true(buffer);
    // final blockedMyStoriesFrom = _readflags.1?true(buffer);
    // final message = _readMessage(buffer);
    // final result = StoryViewPublicForward(flags: flags, blocked: blocked, blockedMyStoriesFrom: blockedMyStoriesFrom, message: message,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: blocked,
      b01: blockedMyStoriesFrom,
    );

    return v;
  }

  /// blocked: bit 0 of flags.0?true
  final bool blocked;

  /// blocked_my_stories_from: bit 1 of flags.1?true
  final bool blockedMyStoriesFrom;

  /// Message.
  final MessageBase message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9083670b);
    buffer.writeInt(flags);
    buffer.writeObject(message);
  }
}

/// Story View Public Repost.
///
/// ID: `bd74cf49`.
class StoryViewPublicRepost extends StoryViewBase {
  /// Story View Public Repost constructor.
  const StoryViewPublicRepost({
    required this.blocked,
    required this.blockedMyStoriesFrom,
    required this.peerId,
    required this.story,
  }) : super._();

  /// Deserialize.
  factory StoryViewPublicRepost.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final blocked = _readflags.0?true(buffer);
    // final blockedMyStoriesFrom = _readflags.1?true(buffer);
    // final peerId = _readPeer(buffer);
    // final story = _readStoryItem(buffer);
    // final result = StoryViewPublicRepost(flags: flags, blocked: blocked, blockedMyStoriesFrom: blockedMyStoriesFrom, peerId: peerId, story: story,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: blocked,
      b01: blockedMyStoriesFrom,
    );

    return v;
  }

  /// blocked: bit 0 of flags.0?true
  final bool blocked;

  /// blocked_my_stories_from: bit 1 of flags.1?true
  final bool blockedMyStoriesFrom;

  /// Peer Id.
  final PeerBase peerId;

  /// Story.
  final StoryItemBase story;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbd74cf49);
    buffer.writeInt(flags);
    buffer.writeObject(peerId);
    buffer.writeObject(story);
  }
}

/// Predicate Stories Story Views List.
abstract class StoriesStoryViewsListBase extends TlConstructor {
  /// Predicate Stories Story Views List constructor.
  const StoriesStoryViewsListBase._() : super._();
}

/// Stories Story Views List.
///
/// ID: `59d78fc5`.
class StoriesStoryViewsList extends StoriesStoryViewsListBase {
  /// Stories Story Views List constructor.
  const StoriesStoryViewsList({
    required this.count,
    required this.viewsCount,
    required this.forwardsCount,
    required this.reactionsCount,
    required this.views,
    required this.chats,
    required this.users,
    this.nextOffset,
  }) : super._();

  /// Deserialize.
  factory StoriesStoryViewsList.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final count = _readint(buffer);
    // final viewsCount = _readint(buffer);
    // final forwardsCount = _readint(buffer);
    // final reactionsCount = _readint(buffer);
    // final views = _readVector<StoryView>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final nextOffset = _readflags.0?string(buffer);
    // final result = StoriesStoryViewsList(flags: flags, count: count, viewsCount: viewsCount, forwardsCount: forwardsCount, reactionsCount: reactionsCount, views: views, chats: chats, users: users, nextOffset: nextOffset,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: nextOffset != null,
    );

    return v;
  }

  /// Count.
  final int count;

  /// Views Count.
  final int viewsCount;

  /// Forwards Count.
  final int forwardsCount;

  /// Reactions Count.
  final int reactionsCount;

  /// Views.
  final List<StoryViewBase> views;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Next Offset.
  final String? nextOffset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x59d78fc5);
    buffer.writeInt(flags);
    buffer.writeInt(count);
    buffer.writeInt(viewsCount);
    buffer.writeInt(forwardsCount);
    buffer.writeInt(reactionsCount);
    buffer.writeVectorObject(views);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
    final localNextOffsetCopy = nextOffset;
    if (localNextOffsetCopy != null) {
      buffer.writeString(localNextOffsetCopy);
    }
  }
}

/// Predicate Stories Story Views.
abstract class StoriesStoryViewsBase extends TlConstructor {
  /// Predicate Stories Story Views constructor.
  const StoriesStoryViewsBase._() : super._();
}

/// Stories Story Views.
///
/// ID: `de9eed1d`.
class StoriesStoryViews extends StoriesStoryViewsBase {
  /// Stories Story Views constructor.
  const StoriesStoryViews({
    required this.views,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory StoriesStoryViews.deserialize(Uint8List buffer) {
    // final views = _readVector<StoryViews>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = StoriesStoryViews(views: views, users: users,);

    // return result;

    throw Exception();
  }

  /// Views.
  final List<StoryViewsBase> views;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xde9eed1d);
    buffer.writeVectorObject(views);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Input Reply To.
abstract class InputReplyToBase extends TlConstructor {
  /// Predicate Input Reply To constructor.
  const InputReplyToBase._() : super._();
}

/// Input Reply To Message.
///
/// ID: `22c0f6d5`.
class InputReplyToMessage extends InputReplyToBase {
  /// Input Reply To Message constructor.
  const InputReplyToMessage({
    required this.replyToMsgId,
    this.topMsgId,
    this.replyToPeerId,
    this.quoteText,
    this.quoteEntities,
    this.quoteOffset,
  }) : super._();

  /// Deserialize.
  factory InputReplyToMessage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final replyToMsgId = _readint(buffer);
    // final topMsgId = _readflags.0?int(buffer);
    // final replyToPeerId = _readflags.1?InputPeer(buffer);
    // final quoteText = _readflags.2?string(buffer);
    // final quoteEntities = _readflags.3?Vector<MessageEntity>(buffer);
    // final quoteOffset = _readflags.4?int(buffer);
    // final result = InputReplyToMessage(flags: flags, replyToMsgId: replyToMsgId, topMsgId: topMsgId, replyToPeerId: replyToPeerId, quoteText: quoteText, quoteEntities: quoteEntities, quoteOffset: quoteOffset,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: topMsgId != null,
      b01: replyToPeerId != null,
      b02: quoteText != null,
      b03: quoteEntities != null,
      b04: quoteOffset != null,
    );

    return v;
  }

  /// Reply To Msg Id.
  final int replyToMsgId;

  /// Top Msg Id.
  final int? topMsgId;

  /// Reply To Peer Id.
  final InputPeerBase? replyToPeerId;

  /// Quote Text.
  final String? quoteText;

  /// Quote Entities.
  final List<MessageEntityBase>? quoteEntities;

  /// Quote Offset.
  final int? quoteOffset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x22c0f6d5);
    buffer.writeInt(flags);
    buffer.writeInt(replyToMsgId);
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
    final localReplyToPeerIdCopy = replyToPeerId;
    if (localReplyToPeerIdCopy != null) {
      buffer.writeObject(localReplyToPeerIdCopy);
    }
    final localQuoteTextCopy = quoteText;
    if (localQuoteTextCopy != null) {
      buffer.writeString(localQuoteTextCopy);
    }
    final localQuoteEntitiesCopy = quoteEntities;
    if (localQuoteEntitiesCopy != null) {
      buffer.writeVectorObject(localQuoteEntitiesCopy);
    }
    final localQuoteOffsetCopy = quoteOffset;
    if (localQuoteOffsetCopy != null) {
      buffer.writeInt(localQuoteOffsetCopy);
    }
  }
}

/// Input Reply To Story.
///
/// ID: `15b0f283`.
class InputReplyToStory extends InputReplyToBase {
  /// Input Reply To Story constructor.
  const InputReplyToStory({
    required this.userId,
    required this.storyId,
  }) : super._();

  /// Deserialize.
  factory InputReplyToStory.deserialize(Uint8List buffer) {
    // final userId = _readInputUser(buffer);
    // final storyId = _readint(buffer);
    // final result = InputReplyToStory(userId: userId, storyId: storyId,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final InputUserBase userId;

  /// Story Id.
  final int storyId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x15b0f283);
    buffer.writeObject(userId);
    buffer.writeInt(storyId);
  }
}

/// Predicate Exported Story Link.
abstract class ExportedStoryLinkBase extends TlConstructor {
  /// Predicate Exported Story Link constructor.
  const ExportedStoryLinkBase._() : super._();
}

/// Exported Story Link.
///
/// ID: `3fc9053b`.
class ExportedStoryLink extends ExportedStoryLinkBase {
  /// Exported Story Link constructor.
  const ExportedStoryLink({
    required this.link,
  }) : super._();

  /// Deserialize.
  factory ExportedStoryLink.deserialize(Uint8List buffer) {
    // final link = _readstring(buffer);
    // final result = ExportedStoryLink(link: link,);

    // return result;

    throw Exception();
  }

  /// Link.
  final String link;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3fc9053b);
    buffer.writeString(link);
  }
}

/// Predicate Stories Stealth Mode.
abstract class StoriesStealthModeBase extends TlConstructor {
  /// Predicate Stories Stealth Mode constructor.
  const StoriesStealthModeBase._() : super._();
}

/// Stories Stealth Mode.
///
/// ID: `712e27fd`.
class StoriesStealthMode extends StoriesStealthModeBase {
  /// Stories Stealth Mode constructor.
  const StoriesStealthMode({
    this.activeUntilDate,
    this.cooldownUntilDate,
  }) : super._();

  /// Deserialize.
  factory StoriesStealthMode.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final activeUntilDate = _readflags.0?int(buffer);
    // final cooldownUntilDate = _readflags.1?int(buffer);
    // final result = StoriesStealthMode(flags: flags, activeUntilDate: activeUntilDate, cooldownUntilDate: cooldownUntilDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: activeUntilDate != null,
      b01: cooldownUntilDate != null,
    );

    return v;
  }

  /// Active Until Date.
  final DateTime? activeUntilDate;

  /// Cooldown Until Date.
  final DateTime? cooldownUntilDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x712e27fd);
    buffer.writeInt(flags);
    final localActiveUntilDateCopy = activeUntilDate;
    if (localActiveUntilDateCopy != null) {
      buffer.writeDateTime(localActiveUntilDateCopy);
    }
    final localCooldownUntilDateCopy = cooldownUntilDate;
    if (localCooldownUntilDateCopy != null) {
      buffer.writeDateTime(localCooldownUntilDateCopy);
    }
  }
}

/// Predicate Media Area Coordinates.
abstract class MediaAreaCoordinatesBase extends TlConstructor {
  /// Predicate Media Area Coordinates constructor.
  const MediaAreaCoordinatesBase._() : super._();
}

/// Media Area Coordinates.
///
/// ID: `03d1ea4e`.
class MediaAreaCoordinates extends MediaAreaCoordinatesBase {
  /// Media Area Coordinates constructor.
  const MediaAreaCoordinates({
    required this.x,
    required this.y,
    required this.w,
    required this.h,
    required this.rotation,
  }) : super._();

  /// Deserialize.
  factory MediaAreaCoordinates.deserialize(Uint8List buffer) {
    // final x = _readdouble(buffer);
    // final y = _readdouble(buffer);
    // final w = _readdouble(buffer);
    // final h = _readdouble(buffer);
    // final rotation = _readdouble(buffer);
    // final result = MediaAreaCoordinates(x: x, y: y, w: w, h: h, rotation: rotation,);

    // return result;

    throw Exception();
  }

  /// X.
  final double x;

  /// Y.
  final double y;

  /// W.
  final double w;

  /// H.
  final double h;

  /// Rotation.
  final double rotation;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x03d1ea4e);
    buffer.writeDouble(x);
    buffer.writeDouble(y);
    buffer.writeDouble(w);
    buffer.writeDouble(h);
    buffer.writeDouble(rotation);
  }
}

/// Predicate Media Area.
abstract class MediaAreaBase extends TlConstructor {
  /// Predicate Media Area constructor.
  const MediaAreaBase._() : super._();
}

/// Media Area Venue.
///
/// ID: `be82db9c`.
class MediaAreaVenue extends MediaAreaBase {
  /// Media Area Venue constructor.
  const MediaAreaVenue({
    required this.coordinates,
    required this.geo,
    required this.title,
    required this.address,
    required this.provider,
    required this.venueId,
    required this.venueType,
  }) : super._();

  /// Deserialize.
  factory MediaAreaVenue.deserialize(Uint8List buffer) {
    // final coordinates = _readMediaAreaCoordinates(buffer);
    // final geo = _readGeoPoint(buffer);
    // final title = _readstring(buffer);
    // final address = _readstring(buffer);
    // final provider = _readstring(buffer);
    // final venueId = _readstring(buffer);
    // final venueType = _readstring(buffer);
    // final result = MediaAreaVenue(coordinates: coordinates, geo: geo, title: title, address: address, provider: provider, venueId: venueId, venueType: venueType,);

    // return result;

    throw Exception();
  }

  /// Coordinates.
  final MediaAreaCoordinatesBase coordinates;

  /// Geo.
  final GeoPointBase geo;

  /// Title.
  final String title;

  /// Address.
  final String address;

  /// Provider.
  final String provider;

  /// Venue Id.
  final String venueId;

  /// Venue Type.
  final String venueType;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbe82db9c);
    buffer.writeObject(coordinates);
    buffer.writeObject(geo);
    buffer.writeString(title);
    buffer.writeString(address);
    buffer.writeString(provider);
    buffer.writeString(venueId);
    buffer.writeString(venueType);
  }
}

/// Input Media Area Venue.
///
/// ID: `b282217f`.
class InputMediaAreaVenue extends MediaAreaBase {
  /// Input Media Area Venue constructor.
  const InputMediaAreaVenue({
    required this.coordinates,
    required this.queryId,
    required this.resultId,
  }) : super._();

  /// Deserialize.
  factory InputMediaAreaVenue.deserialize(Uint8List buffer) {
    // final coordinates = _readMediaAreaCoordinates(buffer);
    // final queryId = _readlong(buffer);
    // final resultId = _readstring(buffer);
    // final result = InputMediaAreaVenue(coordinates: coordinates, queryId: queryId, resultId: resultId,);

    // return result;

    throw Exception();
  }

  /// Coordinates.
  final MediaAreaCoordinatesBase coordinates;

  /// Query Id.
  final int queryId;

  /// Result Id.
  final String resultId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb282217f);
    buffer.writeObject(coordinates);
    buffer.writeLong(queryId);
    buffer.writeString(resultId);
  }
}

/// Media Area Geo Point.
///
/// ID: `df8b3b22`.
class MediaAreaGeoPoint extends MediaAreaBase {
  /// Media Area Geo Point constructor.
  const MediaAreaGeoPoint({
    required this.coordinates,
    required this.geo,
  }) : super._();

  /// Deserialize.
  factory MediaAreaGeoPoint.deserialize(Uint8List buffer) {
    // final coordinates = _readMediaAreaCoordinates(buffer);
    // final geo = _readGeoPoint(buffer);
    // final result = MediaAreaGeoPoint(coordinates: coordinates, geo: geo,);

    // return result;

    throw Exception();
  }

  /// Coordinates.
  final MediaAreaCoordinatesBase coordinates;

  /// Geo.
  final GeoPointBase geo;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdf8b3b22);
    buffer.writeObject(coordinates);
    buffer.writeObject(geo);
  }
}

/// Media Area Suggested Reaction.
///
/// ID: `14455871`.
class MediaAreaSuggestedReaction extends MediaAreaBase {
  /// Media Area Suggested Reaction constructor.
  const MediaAreaSuggestedReaction({
    required this.dark,
    required this.flipped,
    required this.coordinates,
    required this.reaction,
  }) : super._();

  /// Deserialize.
  factory MediaAreaSuggestedReaction.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final dark = _readflags.0?true(buffer);
    // final flipped = _readflags.1?true(buffer);
    // final coordinates = _readMediaAreaCoordinates(buffer);
    // final reaction = _readReaction(buffer);
    // final result = MediaAreaSuggestedReaction(flags: flags, dark: dark, flipped: flipped, coordinates: coordinates, reaction: reaction,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: dark,
      b01: flipped,
    );

    return v;
  }

  /// dark: bit 0 of flags.0?true
  final bool dark;

  /// flipped: bit 1 of flags.1?true
  final bool flipped;

  /// Coordinates.
  final MediaAreaCoordinatesBase coordinates;

  /// Reaction.
  final ReactionBase reaction;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x14455871);
    buffer.writeInt(flags);
    buffer.writeObject(coordinates);
    buffer.writeObject(reaction);
  }
}

/// Media Area Channel Post.
///
/// ID: `770416af`.
class MediaAreaChannelPost extends MediaAreaBase {
  /// Media Area Channel Post constructor.
  const MediaAreaChannelPost({
    required this.coordinates,
    required this.channelId,
    required this.msgId,
  }) : super._();

  /// Deserialize.
  factory MediaAreaChannelPost.deserialize(Uint8List buffer) {
    // final coordinates = _readMediaAreaCoordinates(buffer);
    // final channelId = _readlong(buffer);
    // final msgId = _readint(buffer);
    // final result = MediaAreaChannelPost(coordinates: coordinates, channelId: channelId, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Coordinates.
  final MediaAreaCoordinatesBase coordinates;

  /// Channel Id.
  final int channelId;

  /// Msg Id.
  final int msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x770416af);
    buffer.writeObject(coordinates);
    buffer.writeLong(channelId);
    buffer.writeInt(msgId);
  }
}

/// Input Media Area Channel Post.
///
/// ID: `2271f2bf`.
class InputMediaAreaChannelPost extends MediaAreaBase {
  /// Input Media Area Channel Post constructor.
  const InputMediaAreaChannelPost({
    required this.coordinates,
    required this.channel,
    required this.msgId,
  }) : super._();

  /// Deserialize.
  factory InputMediaAreaChannelPost.deserialize(Uint8List buffer) {
    // final coordinates = _readMediaAreaCoordinates(buffer);
    // final channel = _readInputChannel(buffer);
    // final msgId = _readint(buffer);
    // final result = InputMediaAreaChannelPost(coordinates: coordinates, channel: channel, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Coordinates.
  final MediaAreaCoordinatesBase coordinates;

  /// Channel.
  final InputChannelBase channel;

  /// Msg Id.
  final int msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2271f2bf);
    buffer.writeObject(coordinates);
    buffer.writeObject(channel);
    buffer.writeInt(msgId);
  }
}

/// Predicate Peer Stories.
abstract class PeerStoriesBase extends TlConstructor {
  /// Predicate Peer Stories constructor.
  const PeerStoriesBase._() : super._();
}

/// Peer Stories.
///
/// ID: `9a35e999`.
class PeerStories extends PeerStoriesBase {
  /// Peer Stories constructor.
  const PeerStories({
    required this.peer,
    this.maxReadId,
    required this.stories,
  }) : super._();

  /// Deserialize.
  factory PeerStories.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readPeer(buffer);
    // final maxReadId = _readflags.0?int(buffer);
    // final stories = _readVector<StoryItem>(buffer);
    // final result = PeerStories(flags: flags, peer: peer, maxReadId: maxReadId, stories: stories,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: maxReadId != null,
    );

    return v;
  }

  /// Peer.
  final PeerBase peer;

  /// Max Read Id.
  final int? maxReadId;

  /// Stories.
  final List<StoryItemBase> stories;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9a35e999);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localMaxReadIdCopy = maxReadId;
    if (localMaxReadIdCopy != null) {
      buffer.writeInt(localMaxReadIdCopy);
    }
    buffer.writeVectorObject(stories);
  }
}

/// Predicate Stories Peer Stories.
abstract class StoriesPeerStoriesBase extends TlConstructor {
  /// Predicate Stories Peer Stories constructor.
  const StoriesPeerStoriesBase._() : super._();
}

/// Stories Peer Stories.
///
/// ID: `cae68768`.
class StoriesPeerStories extends StoriesPeerStoriesBase {
  /// Stories Peer Stories constructor.
  const StoriesPeerStories({
    required this.stories,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory StoriesPeerStories.deserialize(Uint8List buffer) {
    // final stories = _readPeerStories(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = StoriesPeerStories(stories: stories, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Stories.
  final PeerStoriesBase stories;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcae68768);
    buffer.writeObject(stories);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Messages Web Page.
abstract class MessagesWebPageBase extends TlConstructor {
  /// Predicate Messages Web Page constructor.
  const MessagesWebPageBase._() : super._();
}

/// Messages Web Page.
///
/// ID: `fd5e12bd`.
class MessagesWebPage extends MessagesWebPageBase {
  /// Messages Web Page constructor.
  const MessagesWebPage({
    required this.webpage,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesWebPage.deserialize(Uint8List buffer) {
    // final webpage = _readWebPage(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesWebPage(webpage: webpage, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Webpage.
  final WebPageBase webpage;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfd5e12bd);
    buffer.writeObject(webpage);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Premium Gift Code Option.
abstract class PremiumGiftCodeOptionBase extends TlConstructor {
  /// Predicate Premium Gift Code Option constructor.
  const PremiumGiftCodeOptionBase._() : super._();
}

/// Premium Gift Code Option.
///
/// ID: `257e962b`.
class PremiumGiftCodeOption extends PremiumGiftCodeOptionBase {
  /// Premium Gift Code Option constructor.
  const PremiumGiftCodeOption({
    required this.users,
    required this.months,
    this.storeProduct,
    this.storeQuantity,
    required this.currency,
    required this.amount,
  }) : super._();

  /// Deserialize.
  factory PremiumGiftCodeOption.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final users = _readint(buffer);
    // final months = _readint(buffer);
    // final storeProduct = _readflags.0?string(buffer);
    // final storeQuantity = _readflags.1?int(buffer);
    // final currency = _readstring(buffer);
    // final amount = _readlong(buffer);
    // final result = PremiumGiftCodeOption(flags: flags, users: users, months: months, storeProduct: storeProduct, storeQuantity: storeQuantity, currency: currency, amount: amount,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: storeProduct != null,
      b01: storeQuantity != null,
    );

    return v;
  }

  /// Users.
  final int users;

  /// Months.
  final int months;

  /// Store Product.
  final String? storeProduct;

  /// Store Quantity.
  final int? storeQuantity;

  /// Currency.
  final String currency;

  /// Amount.
  final int amount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x257e962b);
    buffer.writeInt(flags);
    buffer.writeInt(users);
    buffer.writeInt(months);
    final localStoreProductCopy = storeProduct;
    if (localStoreProductCopy != null) {
      buffer.writeString(localStoreProductCopy);
    }
    final localStoreQuantityCopy = storeQuantity;
    if (localStoreQuantityCopy != null) {
      buffer.writeInt(localStoreQuantityCopy);
    }
    buffer.writeString(currency);
    buffer.writeLong(amount);
  }
}

/// Predicate Payments Checked Gift Code.
abstract class PaymentsCheckedGiftCodeBase extends TlConstructor {
  /// Predicate Payments Checked Gift Code constructor.
  const PaymentsCheckedGiftCodeBase._() : super._();
}

/// Payments Checked Gift Code.
///
/// ID: `284a1096`.
class PaymentsCheckedGiftCode extends PaymentsCheckedGiftCodeBase {
  /// Payments Checked Gift Code constructor.
  const PaymentsCheckedGiftCode({
    required this.viaGiveaway,
    this.fromId,
    this.giveawayMsgId,
    this.toId,
    required this.date,
    required this.months,
    this.usedDate,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PaymentsCheckedGiftCode.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final viaGiveaway = _readflags.2?true(buffer);
    // final fromId = _readflags.4?Peer(buffer);
    // final giveawayMsgId = _readflags.3?int(buffer);
    // final toId = _readflags.0?long(buffer);
    // final date = _readint(buffer);
    // final months = _readint(buffer);
    // final usedDate = _readflags.1?int(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = PaymentsCheckedGiftCode(flags: flags, viaGiveaway: viaGiveaway, fromId: fromId, giveawayMsgId: giveawayMsgId, toId: toId, date: date, months: months, usedDate: usedDate, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: viaGiveaway,
      b04: fromId != null,
      b03: giveawayMsgId != null,
      b00: toId != null,
      b01: usedDate != null,
    );

    return v;
  }

  /// via_giveaway: bit 2 of flags.2?true
  final bool viaGiveaway;

  /// From Id.
  final PeerBase? fromId;

  /// Giveaway Msg Id.
  final int? giveawayMsgId;

  /// To Id.
  final int? toId;

  /// Date.
  final DateTime date;

  /// Months.
  final int months;

  /// Used Date.
  final DateTime? usedDate;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x284a1096);
    buffer.writeInt(flags);
    final localFromIdCopy = fromId;
    if (localFromIdCopy != null) {
      buffer.writeObject(localFromIdCopy);
    }
    final localGiveawayMsgIdCopy = giveawayMsgId;
    if (localGiveawayMsgIdCopy != null) {
      buffer.writeInt(localGiveawayMsgIdCopy);
    }
    final localToIdCopy = toId;
    if (localToIdCopy != null) {
      buffer.writeLong(localToIdCopy);
    }
    buffer.writeDateTime(date);
    buffer.writeInt(months);
    final localUsedDateCopy = usedDate;
    if (localUsedDateCopy != null) {
      buffer.writeDateTime(localUsedDateCopy);
    }
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Payments Giveaway Info.
abstract class PaymentsGiveawayInfoBase extends TlConstructor {
  /// Predicate Payments Giveaway Info constructor.
  const PaymentsGiveawayInfoBase._() : super._();
}

/// Payments Giveaway Info.
///
/// ID: `4367daa0`.
class PaymentsGiveawayInfo extends PaymentsGiveawayInfoBase {
  /// Payments Giveaway Info constructor.
  const PaymentsGiveawayInfo({
    required this.participating,
    required this.preparingResults,
    required this.startDate,
    this.joinedTooEarlyDate,
    this.adminDisallowedChatId,
    this.disallowedCountry,
  }) : super._();

  /// Deserialize.
  factory PaymentsGiveawayInfo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final participating = _readflags.0?true(buffer);
    // final preparingResults = _readflags.3?true(buffer);
    // final startDate = _readint(buffer);
    // final joinedTooEarlyDate = _readflags.1?int(buffer);
    // final adminDisallowedChatId = _readflags.2?long(buffer);
    // final disallowedCountry = _readflags.4?string(buffer);
    // final result = PaymentsGiveawayInfo(flags: flags, participating: participating, preparingResults: preparingResults, startDate: startDate, joinedTooEarlyDate: joinedTooEarlyDate, adminDisallowedChatId: adminDisallowedChatId, disallowedCountry: disallowedCountry,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: participating,
      b03: preparingResults,
      b01: joinedTooEarlyDate != null,
      b02: adminDisallowedChatId != null,
      b04: disallowedCountry != null,
    );

    return v;
  }

  /// participating: bit 0 of flags.0?true
  final bool participating;

  /// preparing_results: bit 3 of flags.3?true
  final bool preparingResults;

  /// Start Date.
  final DateTime startDate;

  /// Joined Too Early Date.
  final DateTime? joinedTooEarlyDate;

  /// Admin Disallowed Chat Id.
  final int? adminDisallowedChatId;

  /// Disallowed Country.
  final String? disallowedCountry;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4367daa0);
    buffer.writeInt(flags);
    buffer.writeDateTime(startDate);
    final localJoinedTooEarlyDateCopy = joinedTooEarlyDate;
    if (localJoinedTooEarlyDateCopy != null) {
      buffer.writeDateTime(localJoinedTooEarlyDateCopy);
    }
    final localAdminDisallowedChatIdCopy = adminDisallowedChatId;
    if (localAdminDisallowedChatIdCopy != null) {
      buffer.writeLong(localAdminDisallowedChatIdCopy);
    }
    final localDisallowedCountryCopy = disallowedCountry;
    if (localDisallowedCountryCopy != null) {
      buffer.writeString(localDisallowedCountryCopy);
    }
  }
}

/// Payments Giveaway Info Results.
///
/// ID: `00cd5570`.
class PaymentsGiveawayInfoResults extends PaymentsGiveawayInfoBase {
  /// Payments Giveaway Info Results constructor.
  const PaymentsGiveawayInfoResults({
    required this.winner,
    required this.refunded,
    required this.startDate,
    this.giftCodeSlug,
    required this.finishDate,
    required this.winnersCount,
    required this.activatedCount,
  }) : super._();

  /// Deserialize.
  factory PaymentsGiveawayInfoResults.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final winner = _readflags.0?true(buffer);
    // final refunded = _readflags.1?true(buffer);
    // final startDate = _readint(buffer);
    // final giftCodeSlug = _readflags.0?string(buffer);
    // final finishDate = _readint(buffer);
    // final winnersCount = _readint(buffer);
    // final activatedCount = _readint(buffer);
    // final result = PaymentsGiveawayInfoResults(flags: flags, winner: winner, refunded: refunded, startDate: startDate, giftCodeSlug: giftCodeSlug, finishDate: finishDate, winnersCount: winnersCount, activatedCount: activatedCount,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: winner || giftCodeSlug != null,
      b01: refunded,
    );

    return v;
  }

  /// winner: bit 0 of flags.0?true
  final bool winner;

  /// refunded: bit 1 of flags.1?true
  final bool refunded;

  /// Start Date.
  final DateTime startDate;

  /// Gift Code Slug.
  final String? giftCodeSlug;

  /// Finish Date.
  final DateTime finishDate;

  /// Winners Count.
  final int winnersCount;

  /// Activated Count.
  final int activatedCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x00cd5570);
    buffer.writeInt(flags);
    buffer.writeDateTime(startDate);
    final localGiftCodeSlugCopy = giftCodeSlug;
    if (localGiftCodeSlugCopy != null) {
      buffer.writeString(localGiftCodeSlugCopy);
    }
    buffer.writeDateTime(finishDate);
    buffer.writeInt(winnersCount);
    buffer.writeInt(activatedCount);
  }
}

/// Predicate Prepaid Giveaway.
abstract class PrepaidGiveawayBase extends TlConstructor {
  /// Predicate Prepaid Giveaway constructor.
  const PrepaidGiveawayBase._() : super._();
}

/// Prepaid Giveaway.
///
/// ID: `b2539d54`.
class PrepaidGiveaway extends PrepaidGiveawayBase {
  /// Prepaid Giveaway constructor.
  const PrepaidGiveaway({
    required this.id,
    required this.months,
    required this.quantity,
    required this.date,
  }) : super._();

  /// Deserialize.
  factory PrepaidGiveaway.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final months = _readint(buffer);
    // final quantity = _readint(buffer);
    // final date = _readint(buffer);
    // final result = PrepaidGiveaway(id: id, months: months, quantity: quantity, date: date,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Months.
  final int months;

  /// Quantity.
  final int quantity;

  /// Date.
  final DateTime date;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb2539d54);
    buffer.writeLong(id);
    buffer.writeInt(months);
    buffer.writeInt(quantity);
    buffer.writeDateTime(date);
  }
}

/// Predicate Boost.
abstract class BoostBase extends TlConstructor {
  /// Predicate Boost constructor.
  const BoostBase._() : super._();
}

/// Boost.
///
/// ID: `2a1c8c71`.
class Boost extends BoostBase {
  /// Boost constructor.
  const Boost({
    required this.gift,
    required this.giveaway,
    required this.unclaimed,
    required this.id,
    this.userId,
    this.giveawayMsgId,
    required this.date,
    required this.expires,
    this.usedGiftSlug,
    this.multiplier,
  }) : super._();

  /// Deserialize.
  factory Boost.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final gift = _readflags.1?true(buffer);
    // final giveaway = _readflags.2?true(buffer);
    // final unclaimed = _readflags.3?true(buffer);
    // final id = _readstring(buffer);
    // final userId = _readflags.0?long(buffer);
    // final giveawayMsgId = _readflags.2?int(buffer);
    // final date = _readint(buffer);
    // final expires = _readint(buffer);
    // final usedGiftSlug = _readflags.4?string(buffer);
    // final multiplier = _readflags.5?int(buffer);
    // final result = Boost(flags: flags, gift: gift, giveaway: giveaway, unclaimed: unclaimed, id: id, userId: userId, giveawayMsgId: giveawayMsgId, date: date, expires: expires, usedGiftSlug: usedGiftSlug, multiplier: multiplier,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: gift,
      b02: giveaway || giveawayMsgId != null,
      b03: unclaimed,
      b00: userId != null,
      b04: usedGiftSlug != null,
      b05: multiplier != null,
    );

    return v;
  }

  /// gift: bit 1 of flags.1?true
  final bool gift;

  /// giveaway: bit 2 of flags.2?true
  final bool giveaway;

  /// unclaimed: bit 3 of flags.3?true
  final bool unclaimed;

  /// Id.
  final String id;

  /// User Id.
  final int? userId;

  /// Giveaway Msg Id.
  final int? giveawayMsgId;

  /// Date.
  final DateTime date;

  /// Expires.
  final int expires;

  /// Used Gift Slug.
  final String? usedGiftSlug;

  /// Multiplier.
  final int? multiplier;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2a1c8c71);
    buffer.writeInt(flags);
    buffer.writeString(id);
    final localUserIdCopy = userId;
    if (localUserIdCopy != null) {
      buffer.writeLong(localUserIdCopy);
    }
    final localGiveawayMsgIdCopy = giveawayMsgId;
    if (localGiveawayMsgIdCopy != null) {
      buffer.writeInt(localGiveawayMsgIdCopy);
    }
    buffer.writeDateTime(date);
    buffer.writeInt(expires);
    final localUsedGiftSlugCopy = usedGiftSlug;
    if (localUsedGiftSlugCopy != null) {
      buffer.writeString(localUsedGiftSlugCopy);
    }
    final localMultiplierCopy = multiplier;
    if (localMultiplierCopy != null) {
      buffer.writeInt(localMultiplierCopy);
    }
  }
}

/// Predicate Premium Boosts List.
abstract class PremiumBoostsListBase extends TlConstructor {
  /// Predicate Premium Boosts List constructor.
  const PremiumBoostsListBase._() : super._();
}

/// Premium Boosts List.
///
/// ID: `86f8613c`.
class PremiumBoostsList extends PremiumBoostsListBase {
  /// Premium Boosts List constructor.
  const PremiumBoostsList({
    required this.count,
    required this.boosts,
    this.nextOffset,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PremiumBoostsList.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final count = _readint(buffer);
    // final boosts = _readVector<Boost>(buffer);
    // final nextOffset = _readflags.0?string(buffer);
    // final users = _readVector<User>(buffer);
    // final result = PremiumBoostsList(flags: flags, count: count, boosts: boosts, nextOffset: nextOffset, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: nextOffset != null,
    );

    return v;
  }

  /// Count.
  final int count;

  /// Boosts.
  final List<BoostBase> boosts;

  /// Next Offset.
  final String? nextOffset;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x86f8613c);
    buffer.writeInt(flags);
    buffer.writeInt(count);
    buffer.writeVectorObject(boosts);
    final localNextOffsetCopy = nextOffset;
    if (localNextOffsetCopy != null) {
      buffer.writeString(localNextOffsetCopy);
    }
    buffer.writeVectorObject(users);
  }
}

/// Predicate My Boost.
abstract class MyBoostBase extends TlConstructor {
  /// Predicate My Boost constructor.
  const MyBoostBase._() : super._();
}

/// My Boost.
///
/// ID: `c448415c`.
class MyBoost extends MyBoostBase {
  /// My Boost constructor.
  const MyBoost({
    required this.slot,
    this.peer,
    required this.date,
    required this.expires,
    this.cooldownUntilDate,
  }) : super._();

  /// Deserialize.
  factory MyBoost.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final slot = _readint(buffer);
    // final peer = _readflags.0?Peer(buffer);
    // final date = _readint(buffer);
    // final expires = _readint(buffer);
    // final cooldownUntilDate = _readflags.1?int(buffer);
    // final result = MyBoost(flags: flags, slot: slot, peer: peer, date: date, expires: expires, cooldownUntilDate: cooldownUntilDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: peer != null,
      b01: cooldownUntilDate != null,
    );

    return v;
  }

  /// Slot.
  final int slot;

  /// Peer.
  final PeerBase? peer;

  /// Date.
  final DateTime date;

  /// Expires.
  final int expires;

  /// Cooldown Until Date.
  final DateTime? cooldownUntilDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc448415c);
    buffer.writeInt(flags);
    buffer.writeInt(slot);
    final localPeerCopy = peer;
    if (localPeerCopy != null) {
      buffer.writeObject(localPeerCopy);
    }
    buffer.writeDateTime(date);
    buffer.writeInt(expires);
    final localCooldownUntilDateCopy = cooldownUntilDate;
    if (localCooldownUntilDateCopy != null) {
      buffer.writeDateTime(localCooldownUntilDateCopy);
    }
  }
}

/// Predicate Premium My Boosts.
abstract class PremiumMyBoostsBase extends TlConstructor {
  /// Predicate Premium My Boosts constructor.
  const PremiumMyBoostsBase._() : super._();
}

/// Premium My Boosts.
///
/// ID: `9ae228e2`.
class PremiumMyBoosts extends PremiumMyBoostsBase {
  /// Premium My Boosts constructor.
  const PremiumMyBoosts({
    required this.myBoosts,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PremiumMyBoosts.deserialize(Uint8List buffer) {
    // final myBoosts = _readVector<MyBoost>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = PremiumMyBoosts(myBoosts: myBoosts, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// My Boosts.
  final List<MyBoostBase> myBoosts;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9ae228e2);
    buffer.writeVectorObject(myBoosts);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Premium Boosts Status.
abstract class PremiumBoostsStatusBase extends TlConstructor {
  /// Predicate Premium Boosts Status constructor.
  const PremiumBoostsStatusBase._() : super._();
}

/// Premium Boosts Status.
///
/// ID: `4959427a`.
class PremiumBoostsStatus extends PremiumBoostsStatusBase {
  /// Premium Boosts Status constructor.
  const PremiumBoostsStatus({
    required this.myBoost,
    required this.level,
    required this.currentLevelBoosts,
    required this.boosts,
    this.giftBoosts,
    this.nextLevelBoosts,
    this.premiumAudience,
    required this.boostUrl,
    this.prepaidGiveaways,
    this.myBoostSlots,
  }) : super._();

  /// Deserialize.
  factory PremiumBoostsStatus.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final myBoost = _readflags.2?true(buffer);
    // final level = _readint(buffer);
    // final currentLevelBoosts = _readint(buffer);
    // final boosts = _readint(buffer);
    // final giftBoosts = _readflags.4?int(buffer);
    // final nextLevelBoosts = _readflags.0?int(buffer);
    // final premiumAudience = _readflags.1?StatsPercentValue(buffer);
    // final boostUrl = _readstring(buffer);
    // final prepaidGiveaways = _readflags.3?Vector<PrepaidGiveaway>(buffer);
    // final myBoostSlots = _readflags.2?Vector<int>(buffer);
    // final result = PremiumBoostsStatus(flags: flags, myBoost: myBoost, level: level, currentLevelBoosts: currentLevelBoosts, boosts: boosts, giftBoosts: giftBoosts, nextLevelBoosts: nextLevelBoosts, premiumAudience: premiumAudience, boostUrl: boostUrl, prepaidGiveaways: prepaidGiveaways, myBoostSlots: myBoostSlots,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: myBoost || myBoostSlots != null,
      b04: giftBoosts != null,
      b00: nextLevelBoosts != null,
      b01: premiumAudience != null,
      b03: prepaidGiveaways != null,
    );

    return v;
  }

  /// my_boost: bit 2 of flags.2?true
  final bool myBoost;

  /// Level.
  final int level;

  /// Current Level Boosts.
  final int currentLevelBoosts;

  /// Boosts.
  final int boosts;

  /// Gift Boosts.
  final int? giftBoosts;

  /// Next Level Boosts.
  final int? nextLevelBoosts;

  /// Premium Audience.
  final StatsPercentValueBase? premiumAudience;

  /// Boost Url.
  final String boostUrl;

  /// Prepaid Giveaways.
  final List<PrepaidGiveawayBase>? prepaidGiveaways;

  /// My Boost Slots.
  final List<int>? myBoostSlots;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4959427a);
    buffer.writeInt(flags);
    buffer.writeInt(level);
    buffer.writeInt(currentLevelBoosts);
    buffer.writeInt(boosts);
    final localGiftBoostsCopy = giftBoosts;
    if (localGiftBoostsCopy != null) {
      buffer.writeInt(localGiftBoostsCopy);
    }
    final localNextLevelBoostsCopy = nextLevelBoosts;
    if (localNextLevelBoostsCopy != null) {
      buffer.writeInt(localNextLevelBoostsCopy);
    }
    final localPremiumAudienceCopy = premiumAudience;
    if (localPremiumAudienceCopy != null) {
      buffer.writeObject(localPremiumAudienceCopy);
    }
    buffer.writeString(boostUrl);
    final localPrepaidGiveawaysCopy = prepaidGiveaways;
    if (localPrepaidGiveawaysCopy != null) {
      buffer.writeVectorObject(localPrepaidGiveawaysCopy);
    }
    final localMyBoostSlotsCopy = myBoostSlots;
    if (localMyBoostSlotsCopy != null) {
      buffer.writeVectorInt(localMyBoostSlotsCopy);
    }
  }
}

/// Predicate Story Fwd Header.
abstract class StoryFwdHeaderBase extends TlConstructor {
  /// Predicate Story Fwd Header constructor.
  const StoryFwdHeaderBase._() : super._();
}

/// Story Fwd Header.
///
/// ID: `b826e150`.
class StoryFwdHeader extends StoryFwdHeaderBase {
  /// Story Fwd Header constructor.
  const StoryFwdHeader({
    required this.modified,
    this.from,
    this.fromName,
    this.storyId,
  }) : super._();

  /// Deserialize.
  factory StoryFwdHeader.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final modified = _readflags.3?true(buffer);
    // final from = _readflags.0?Peer(buffer);
    // final fromName = _readflags.1?string(buffer);
    // final storyId = _readflags.2?int(buffer);
    // final result = StoryFwdHeader(flags: flags, modified: modified, from: from, fromName: fromName, storyId: storyId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: modified,
      b00: from != null,
      b01: fromName != null,
      b02: storyId != null,
    );

    return v;
  }

  /// modified: bit 3 of flags.3?true
  final bool modified;

  /// From.
  final PeerBase? from;

  /// From Name.
  final String? fromName;

  /// Story Id.
  final int? storyId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb826e150);
    buffer.writeInt(flags);
    final localFromCopy = from;
    if (localFromCopy != null) {
      buffer.writeObject(localFromCopy);
    }
    final localFromNameCopy = fromName;
    if (localFromNameCopy != null) {
      buffer.writeString(localFromNameCopy);
    }
    final localStoryIdCopy = storyId;
    if (localStoryIdCopy != null) {
      buffer.writeInt(localStoryIdCopy);
    }
  }
}

/// Predicate Post Interaction Counters.
abstract class PostInteractionCountersBase extends TlConstructor {
  /// Predicate Post Interaction Counters constructor.
  const PostInteractionCountersBase._() : super._();
}

/// Post Interaction Counters Message.
///
/// ID: `e7058e7f`.
class PostInteractionCountersMessage extends PostInteractionCountersBase {
  /// Post Interaction Counters Message constructor.
  const PostInteractionCountersMessage({
    required this.msgId,
    required this.views,
    required this.forwards,
    required this.reactions,
  }) : super._();

  /// Deserialize.
  factory PostInteractionCountersMessage.deserialize(Uint8List buffer) {
    // final msgId = _readint(buffer);
    // final views = _readint(buffer);
    // final forwards = _readint(buffer);
    // final reactions = _readint(buffer);
    // final result = PostInteractionCountersMessage(msgId: msgId, views: views, forwards: forwards, reactions: reactions,);

    // return result;

    throw Exception();
  }

  /// Msg Id.
  final int msgId;

  /// Views.
  final int views;

  /// Forwards.
  final int forwards;

  /// Reactions.
  final int reactions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe7058e7f);
    buffer.writeInt(msgId);
    buffer.writeInt(views);
    buffer.writeInt(forwards);
    buffer.writeInt(reactions);
  }
}

/// Post Interaction Counters Story.
///
/// ID: `8a480e27`.
class PostInteractionCountersStory extends PostInteractionCountersBase {
  /// Post Interaction Counters Story constructor.
  const PostInteractionCountersStory({
    required this.storyId,
    required this.views,
    required this.forwards,
    required this.reactions,
  }) : super._();

  /// Deserialize.
  factory PostInteractionCountersStory.deserialize(Uint8List buffer) {
    // final storyId = _readint(buffer);
    // final views = _readint(buffer);
    // final forwards = _readint(buffer);
    // final reactions = _readint(buffer);
    // final result = PostInteractionCountersStory(storyId: storyId, views: views, forwards: forwards, reactions: reactions,);

    // return result;

    throw Exception();
  }

  /// Story Id.
  final int storyId;

  /// Views.
  final int views;

  /// Forwards.
  final int forwards;

  /// Reactions.
  final int reactions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8a480e27);
    buffer.writeInt(storyId);
    buffer.writeInt(views);
    buffer.writeInt(forwards);
    buffer.writeInt(reactions);
  }
}

/// Predicate Stats Story Stats.
abstract class StatsStoryStatsBase extends TlConstructor {
  /// Predicate Stats Story Stats constructor.
  const StatsStoryStatsBase._() : super._();
}

/// Stats Story Stats.
///
/// ID: `50cd067c`.
class StatsStoryStats extends StatsStoryStatsBase {
  /// Stats Story Stats constructor.
  const StatsStoryStats({
    required this.viewsGraph,
    required this.reactionsByEmotionGraph,
  }) : super._();

  /// Deserialize.
  factory StatsStoryStats.deserialize(Uint8List buffer) {
    // final viewsGraph = _readStatsGraph(buffer);
    // final reactionsByEmotionGraph = _readStatsGraph(buffer);
    // final result = StatsStoryStats(viewsGraph: viewsGraph, reactionsByEmotionGraph: reactionsByEmotionGraph,);

    // return result;

    throw Exception();
  }

  /// Views Graph.
  final StatsGraphBase viewsGraph;

  /// Reactions By Emotion Graph.
  final StatsGraphBase reactionsByEmotionGraph;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x50cd067c);
    buffer.writeObject(viewsGraph);
    buffer.writeObject(reactionsByEmotionGraph);
  }
}

/// Predicate Public Forward.
abstract class PublicForwardBase extends TlConstructor {
  /// Predicate Public Forward constructor.
  const PublicForwardBase._() : super._();
}

/// Public Forward Message.
///
/// ID: `01f2bf4a`.
class PublicForwardMessage extends PublicForwardBase {
  /// Public Forward Message constructor.
  const PublicForwardMessage({
    required this.message,
  }) : super._();

  /// Deserialize.
  factory PublicForwardMessage.deserialize(Uint8List buffer) {
    // final message = _readMessage(buffer);
    // final result = PublicForwardMessage(message: message,);

    // return result;

    throw Exception();
  }

  /// Message.
  final MessageBase message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x01f2bf4a);
    buffer.writeObject(message);
  }
}

/// Public Forward Story.
///
/// ID: `edf3add0`.
class PublicForwardStory extends PublicForwardBase {
  /// Public Forward Story constructor.
  const PublicForwardStory({
    required this.peer,
    required this.story,
  }) : super._();

  /// Deserialize.
  factory PublicForwardStory.deserialize(Uint8List buffer) {
    // final peer = _readPeer(buffer);
    // final story = _readStoryItem(buffer);
    // final result = PublicForwardStory(peer: peer, story: story,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final PeerBase peer;

  /// Story.
  final StoryItemBase story;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xedf3add0);
    buffer.writeObject(peer);
    buffer.writeObject(story);
  }
}

/// Predicate Stats Public Forwards.
abstract class StatsPublicForwardsBase extends TlConstructor {
  /// Predicate Stats Public Forwards constructor.
  const StatsPublicForwardsBase._() : super._();
}

/// Stats Public Forwards.
///
/// ID: `93037e20`.
class StatsPublicForwards extends StatsPublicForwardsBase {
  /// Stats Public Forwards constructor.
  const StatsPublicForwards({
    required this.count,
    required this.forwards,
    this.nextOffset,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory StatsPublicForwards.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final count = _readint(buffer);
    // final forwards = _readVector<PublicForward>(buffer);
    // final nextOffset = _readflags.0?string(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = StatsPublicForwards(flags: flags, count: count, forwards: forwards, nextOffset: nextOffset, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: nextOffset != null,
    );

    return v;
  }

  /// Count.
  final int count;

  /// Forwards.
  final List<PublicForwardBase> forwards;

  /// Next Offset.
  final String? nextOffset;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x93037e20);
    buffer.writeInt(flags);
    buffer.writeInt(count);
    buffer.writeVectorObject(forwards);
    final localNextOffsetCopy = nextOffset;
    if (localNextOffsetCopy != null) {
      buffer.writeString(localNextOffsetCopy);
    }
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Predicate Peer Color.
abstract class PeerColorBase extends TlConstructor {
  /// Predicate Peer Color constructor.
  const PeerColorBase._() : super._();
}

/// Peer Color.
///
/// ID: `b54b5acf`.
class PeerColor extends PeerColorBase {
  /// Peer Color constructor.
  const PeerColor({
    this.color,
    this.backgroundEmojiId,
  }) : super._();

  /// Deserialize.
  factory PeerColor.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final color = _readflags.0?int(buffer);
    // final backgroundEmojiId = _readflags.1?long(buffer);
    // final result = PeerColor(flags: flags, color: color, backgroundEmojiId: backgroundEmojiId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: color != null,
      b01: backgroundEmojiId != null,
    );

    return v;
  }

  /// Color.
  final int? color;

  /// Background Emoji Id.
  final int? backgroundEmojiId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb54b5acf);
    buffer.writeInt(flags);
    final localColorCopy = color;
    if (localColorCopy != null) {
      buffer.writeInt(localColorCopy);
    }
    final localBackgroundEmojiIdCopy = backgroundEmojiId;
    if (localBackgroundEmojiIdCopy != null) {
      buffer.writeLong(localBackgroundEmojiIdCopy);
    }
  }
}

/// Predicate Help Peer Color Set.
abstract class HelpPeerColorSetBase extends TlConstructor {
  /// Predicate Help Peer Color Set constructor.
  const HelpPeerColorSetBase._() : super._();
}

/// Help Peer Color Set.
///
/// ID: `26219a58`.
class HelpPeerColorSet extends HelpPeerColorSetBase {
  /// Help Peer Color Set constructor.
  const HelpPeerColorSet({
    required this.colors,
  }) : super._();

  /// Deserialize.
  factory HelpPeerColorSet.deserialize(Uint8List buffer) {
    // final colors = _readVector<int>(buffer);
    // final result = HelpPeerColorSet(colors: colors,);

    // return result;

    throw Exception();
  }

  /// Colors.
  final List<int> colors;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x26219a58);
    buffer.writeVectorInt(colors);
  }
}

/// Help Peer Color Profile Set.
///
/// ID: `767d61eb`.
class HelpPeerColorProfileSet extends HelpPeerColorSetBase {
  /// Help Peer Color Profile Set constructor.
  const HelpPeerColorProfileSet({
    required this.paletteColors,
    required this.bgColors,
    required this.storyColors,
  }) : super._();

  /// Deserialize.
  factory HelpPeerColorProfileSet.deserialize(Uint8List buffer) {
    // final paletteColors = _readVector<int>(buffer);
    // final bgColors = _readVector<int>(buffer);
    // final storyColors = _readVector<int>(buffer);
    // final result = HelpPeerColorProfileSet(paletteColors: paletteColors, bgColors: bgColors, storyColors: storyColors,);

    // return result;

    throw Exception();
  }

  /// Palette Colors.
  final List<int> paletteColors;

  /// Bg Colors.
  final List<int> bgColors;

  /// Story Colors.
  final List<int> storyColors;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x767d61eb);
    buffer.writeVectorInt(paletteColors);
    buffer.writeVectorInt(bgColors);
    buffer.writeVectorInt(storyColors);
  }
}

/// Predicate Help Peer Color Option.
abstract class HelpPeerColorOptionBase extends TlConstructor {
  /// Predicate Help Peer Color Option constructor.
  const HelpPeerColorOptionBase._() : super._();
}

/// Help Peer Color Option.
///
/// ID: `ef8430ab`.
class HelpPeerColorOption extends HelpPeerColorOptionBase {
  /// Help Peer Color Option constructor.
  const HelpPeerColorOption({
    required this.hidden,
    required this.colorId,
    this.colors,
    this.darkColors,
    this.channelMinLevel,
  }) : super._();

  /// Deserialize.
  factory HelpPeerColorOption.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final hidden = _readflags.0?true(buffer);
    // final colorId = _readint(buffer);
    // final colors = _readflags.1?help.PeerColorSet(buffer);
    // final darkColors = _readflags.2?help.PeerColorSet(buffer);
    // final channelMinLevel = _readflags.3?int(buffer);
    // final result = HelpPeerColorOption(flags: flags, hidden: hidden, colorId: colorId, colors: colors, darkColors: darkColors, channelMinLevel: channelMinLevel,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: hidden,
      b01: colors != null,
      b02: darkColors != null,
      b03: channelMinLevel != null,
    );

    return v;
  }

  /// hidden: bit 0 of flags.0?true
  final bool hidden;

  /// Color Id.
  final int colorId;

  /// Colors.
  final HelpPeerColorSetBase? colors;

  /// Dark Colors.
  final HelpPeerColorSetBase? darkColors;

  /// Channel Min Level.
  final int? channelMinLevel;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xef8430ab);
    buffer.writeInt(flags);
    buffer.writeInt(colorId);
    final localColorsCopy = colors;
    if (localColorsCopy != null) {
      buffer.writeObject(localColorsCopy);
    }
    final localDarkColorsCopy = darkColors;
    if (localDarkColorsCopy != null) {
      buffer.writeObject(localDarkColorsCopy);
    }
    final localChannelMinLevelCopy = channelMinLevel;
    if (localChannelMinLevelCopy != null) {
      buffer.writeInt(localChannelMinLevelCopy);
    }
  }
}

/// Predicate Help Peer Colors.
abstract class HelpPeerColorsBase extends TlConstructor {
  /// Predicate Help Peer Colors constructor.
  const HelpPeerColorsBase._() : super._();
}

/// Help Peer Colors Not Modified.
///
/// ID: `2ba1f5ce`.
class HelpPeerColorsNotModified extends HelpPeerColorsBase {
  /// Help Peer Colors Not Modified constructor.
  const HelpPeerColorsNotModified() : super._();

  /// Deserialize.
  factory HelpPeerColorsNotModified.deserialize(Uint8List buffer) {
    // final result = HelpPeerColorsNotModified();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2ba1f5ce);
  }
}

/// Help Peer Colors.
///
/// ID: `00f8ed08`.
class HelpPeerColors extends HelpPeerColorsBase {
  /// Help Peer Colors constructor.
  const HelpPeerColors({
    required this.hash,
    required this.colors,
  }) : super._();

  /// Deserialize.
  factory HelpPeerColors.deserialize(Uint8List buffer) {
    // final hash = _readint(buffer);
    // final colors = _readVector<help.PeerColorOption>(buffer);
    // final result = HelpPeerColors(hash: hash, colors: colors,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Colors.
  final List<HelpPeerColorOptionBase> colors;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x00f8ed08);
    buffer.writeInt(hash);
    buffer.writeVectorObject(colors);
  }
}

/// Predicate Story Reaction.
abstract class StoryReactionBase extends TlConstructor {
  /// Predicate Story Reaction constructor.
  const StoryReactionBase._() : super._();
}

/// Story Reaction.
///
/// ID: `6090d6d5`.
class StoryReaction extends StoryReactionBase {
  /// Story Reaction constructor.
  const StoryReaction({
    required this.peerId,
    required this.date,
    required this.reaction,
  }) : super._();

  /// Deserialize.
  factory StoryReaction.deserialize(Uint8List buffer) {
    // final peerId = _readPeer(buffer);
    // final date = _readint(buffer);
    // final reaction = _readReaction(buffer);
    // final result = StoryReaction(peerId: peerId, date: date, reaction: reaction,);

    // return result;

    throw Exception();
  }

  /// Peer Id.
  final PeerBase peerId;

  /// Date.
  final DateTime date;

  /// Reaction.
  final ReactionBase reaction;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6090d6d5);
    buffer.writeObject(peerId);
    buffer.writeDateTime(date);
    buffer.writeObject(reaction);
  }
}

/// Story Reaction Public Forward.
///
/// ID: `bbab2643`.
class StoryReactionPublicForward extends StoryReactionBase {
  /// Story Reaction Public Forward constructor.
  const StoryReactionPublicForward({
    required this.message,
  }) : super._();

  /// Deserialize.
  factory StoryReactionPublicForward.deserialize(Uint8List buffer) {
    // final message = _readMessage(buffer);
    // final result = StoryReactionPublicForward(message: message,);

    // return result;

    throw Exception();
  }

  /// Message.
  final MessageBase message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbbab2643);
    buffer.writeObject(message);
  }
}

/// Story Reaction Public Repost.
///
/// ID: `cfcd0f13`.
class StoryReactionPublicRepost extends StoryReactionBase {
  /// Story Reaction Public Repost constructor.
  const StoryReactionPublicRepost({
    required this.peerId,
    required this.story,
  }) : super._();

  /// Deserialize.
  factory StoryReactionPublicRepost.deserialize(Uint8List buffer) {
    // final peerId = _readPeer(buffer);
    // final story = _readStoryItem(buffer);
    // final result = StoryReactionPublicRepost(peerId: peerId, story: story,);

    // return result;

    throw Exception();
  }

  /// Peer Id.
  final PeerBase peerId;

  /// Story.
  final StoryItemBase story;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcfcd0f13);
    buffer.writeObject(peerId);
    buffer.writeObject(story);
  }
}

/// Predicate Stories Story Reactions List.
abstract class StoriesStoryReactionsListBase extends TlConstructor {
  /// Predicate Stories Story Reactions List constructor.
  const StoriesStoryReactionsListBase._() : super._();
}

/// Stories Story Reactions List.
///
/// ID: `aa5f789c`.
class StoriesStoryReactionsList extends StoriesStoryReactionsListBase {
  /// Stories Story Reactions List constructor.
  const StoriesStoryReactionsList({
    required this.count,
    required this.reactions,
    required this.chats,
    required this.users,
    this.nextOffset,
  }) : super._();

  /// Deserialize.
  factory StoriesStoryReactionsList.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final count = _readint(buffer);
    // final reactions = _readVector<StoryReaction>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final nextOffset = _readflags.0?string(buffer);
    // final result = StoriesStoryReactionsList(flags: flags, count: count, reactions: reactions, chats: chats, users: users, nextOffset: nextOffset,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: nextOffset != null,
    );

    return v;
  }

  /// Count.
  final int count;

  /// Reactions.
  final List<StoryReactionBase> reactions;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Next Offset.
  final String? nextOffset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xaa5f789c);
    buffer.writeInt(flags);
    buffer.writeInt(count);
    buffer.writeVectorObject(reactions);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
    final localNextOffsetCopy = nextOffset;
    if (localNextOffsetCopy != null) {
      buffer.writeString(localNextOffsetCopy);
    }
  }
}

/// Predicate Saved Dialog.
abstract class SavedDialogBase extends TlConstructor {
  /// Predicate Saved Dialog constructor.
  const SavedDialogBase._() : super._();
}

/// Saved Dialog.
///
/// ID: `bd87cb6c`.
class SavedDialog extends SavedDialogBase {
  /// Saved Dialog constructor.
  const SavedDialog({
    required this.pinned,
    required this.peer,
    required this.topMessage,
  }) : super._();

  /// Deserialize.
  factory SavedDialog.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pinned = _readflags.2?true(buffer);
    // final peer = _readPeer(buffer);
    // final topMessage = _readint(buffer);
    // final result = SavedDialog(flags: flags, pinned: pinned, peer: peer, topMessage: topMessage,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: pinned,
    );

    return v;
  }

  /// pinned: bit 2 of flags.2?true
  final bool pinned;

  /// Peer.
  final PeerBase peer;

  /// Top Message.
  final int topMessage;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbd87cb6c);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(topMessage);
  }
}

/// Predicate Messages Saved Dialogs.
abstract class MessagesSavedDialogsBase extends TlConstructor {
  /// Predicate Messages Saved Dialogs constructor.
  const MessagesSavedDialogsBase._() : super._();
}

/// Messages Saved Dialogs.
///
/// ID: `f83ae221`.
class MessagesSavedDialogs extends MessagesSavedDialogsBase {
  /// Messages Saved Dialogs constructor.
  const MessagesSavedDialogs({
    required this.dialogs,
    required this.messages,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesSavedDialogs.deserialize(Uint8List buffer) {
    // final dialogs = _readVector<SavedDialog>(buffer);
    // final messages = _readVector<Message>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesSavedDialogs(dialogs: dialogs, messages: messages, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Dialogs.
  final List<SavedDialogBase> dialogs;

  /// Messages.
  final List<MessageBase> messages;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf83ae221);
    buffer.writeVectorObject(dialogs);
    buffer.writeVectorObject(messages);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Messages Saved Dialogs Slice.
///
/// ID: `44ba9dd9`.
class MessagesSavedDialogsSlice extends MessagesSavedDialogsBase {
  /// Messages Saved Dialogs Slice constructor.
  const MessagesSavedDialogsSlice({
    required this.count,
    required this.dialogs,
    required this.messages,
    required this.chats,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory MessagesSavedDialogsSlice.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final dialogs = _readVector<SavedDialog>(buffer);
    // final messages = _readVector<Message>(buffer);
    // final chats = _readVector<Chat>(buffer);
    // final users = _readVector<User>(buffer);
    // final result = MessagesSavedDialogsSlice(count: count, dialogs: dialogs, messages: messages, chats: chats, users: users,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Dialogs.
  final List<SavedDialogBase> dialogs;

  /// Messages.
  final List<MessageBase> messages;

  /// Chats.
  final List<ChatBase> chats;

  /// Users.
  final List<UserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x44ba9dd9);
    buffer.writeInt(count);
    buffer.writeVectorObject(dialogs);
    buffer.writeVectorObject(messages);
    buffer.writeVectorObject(chats);
    buffer.writeVectorObject(users);
  }
}

/// Messages Saved Dialogs Not Modified.
///
/// ID: `c01f6fe8`.
class MessagesSavedDialogsNotModified extends MessagesSavedDialogsBase {
  /// Messages Saved Dialogs Not Modified constructor.
  const MessagesSavedDialogsNotModified({
    required this.count,
  }) : super._();

  /// Deserialize.
  factory MessagesSavedDialogsNotModified.deserialize(Uint8List buffer) {
    // final count = _readint(buffer);
    // final result = MessagesSavedDialogsNotModified(count: count,);

    // return result;

    throw Exception();
  }

  /// Count.
  final int count;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc01f6fe8);
    buffer.writeInt(count);
  }
}

/// Invoke After Msg.
///
/// ID: `cb9f372d`.
class InvokeAfterMsg<X> extends TlMethod<X> {
  /// Invoke After Msg constructor.
  const InvokeAfterMsg({
    required this.msgId,
    required this.query,
  }) : super._();

  /// Deserialize.
  factory InvokeAfterMsg.deserialize(Uint8List buffer) {
    // final msgId = _readlong(buffer);
    // final query = _read!X(buffer);
    // final result = InvokeAfterMsg(msgId: msgId, query: query,);

    // return result;

    throw Exception();
  }

  /// Msg Id.
  final int msgId;

  /// Query.
  final TlMethod<X> query;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcb9f372d);
    buffer.writeLong(msgId);
    buffer.writeObject(query);
  }
}

/// Invoke After Msgs.
///
/// ID: `3dc4b4f0`.
class InvokeAfterMsgs<X> extends TlMethod<X> {
  /// Invoke After Msgs constructor.
  const InvokeAfterMsgs({
    required this.msgIds,
    required this.query,
  }) : super._();

  /// Deserialize.
  factory InvokeAfterMsgs.deserialize(Uint8List buffer) {
    // final msgIds = _readVector<long>(buffer);
    // final query = _read!X(buffer);
    // final result = InvokeAfterMsgs(msgIds: msgIds, query: query,);

    // return result;

    throw Exception();
  }

  /// Msg Ids.
  final List<int> msgIds;

  /// Query.
  final TlMethod<X> query;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3dc4b4f0);
    buffer.writeVectorLong(msgIds);
    buffer.writeObject(query);
  }
}

/// Auth Send Code.
///
/// ID: `a677244f`.
class AuthSendCode extends TlMethod<AuthSentCodeBase> {
  /// Auth Send Code constructor.
  const AuthSendCode({
    required this.phoneNumber,
    required this.apiId,
    required this.apiHash,
    required this.settings,
  }) : super._();

  /// Deserialize.
  factory AuthSendCode.deserialize(Uint8List buffer) {
    // final phoneNumber = _readstring(buffer);
    // final apiId = _readint(buffer);
    // final apiHash = _readstring(buffer);
    // final settings = _readCodeSettings(buffer);
    // final result = AuthSendCode(phoneNumber: phoneNumber, apiId: apiId, apiHash: apiHash, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Phone Number.
  final String phoneNumber;

  /// Api Id.
  final int apiId;

  /// Api Hash.
  final String apiHash;

  /// Settings.
  final CodeSettingsBase settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa677244f);
    buffer.writeString(phoneNumber);
    buffer.writeInt(apiId);
    buffer.writeString(apiHash);
    buffer.writeObject(settings);
  }
}

/// Auth Sign Up.
///
/// ID: `80eee427`.
class AuthSignUp extends TlMethod<AuthAuthorizationBase> {
  /// Auth Sign Up constructor.
  const AuthSignUp({
    required this.phoneNumber,
    required this.phoneCodeHash,
    required this.firstName,
    required this.lastName,
  }) : super._();

  /// Deserialize.
  factory AuthSignUp.deserialize(Uint8List buffer) {
    // final phoneNumber = _readstring(buffer);
    // final phoneCodeHash = _readstring(buffer);
    // final firstName = _readstring(buffer);
    // final lastName = _readstring(buffer);
    // final result = AuthSignUp(phoneNumber: phoneNumber, phoneCodeHash: phoneCodeHash, firstName: firstName, lastName: lastName,);

    // return result;

    throw Exception();
  }

  /// Phone Number.
  final String phoneNumber;

  /// Phone Code Hash.
  final String phoneCodeHash;

  /// First Name.
  final String firstName;

  /// Last Name.
  final String lastName;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x80eee427);
    buffer.writeString(phoneNumber);
    buffer.writeString(phoneCodeHash);
    buffer.writeString(firstName);
    buffer.writeString(lastName);
  }
}

/// Auth Sign In.
///
/// ID: `8d52a951`.
class AuthSignIn extends TlMethod<AuthAuthorizationBase> {
  /// Auth Sign In constructor.
  const AuthSignIn({
    required this.phoneNumber,
    required this.phoneCodeHash,
    this.phoneCode,
    this.emailVerification,
  }) : super._();

  /// Deserialize.
  factory AuthSignIn.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final phoneNumber = _readstring(buffer);
    // final phoneCodeHash = _readstring(buffer);
    // final phoneCode = _readflags.0?string(buffer);
    // final emailVerification = _readflags.1?EmailVerification(buffer);
    // final result = AuthSignIn(flags: flags, phoneNumber: phoneNumber, phoneCodeHash: phoneCodeHash, phoneCode: phoneCode, emailVerification: emailVerification,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: phoneCode != null,
      b01: emailVerification != null,
    );

    return v;
  }

  /// Phone Number.
  final String phoneNumber;

  /// Phone Code Hash.
  final String phoneCodeHash;

  /// Phone Code.
  final String? phoneCode;

  /// Email Verification.
  final EmailVerificationBase? emailVerification;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8d52a951);
    buffer.writeInt(flags);
    buffer.writeString(phoneNumber);
    buffer.writeString(phoneCodeHash);
    final localPhoneCodeCopy = phoneCode;
    if (localPhoneCodeCopy != null) {
      buffer.writeString(localPhoneCodeCopy);
    }
    final localEmailVerificationCopy = emailVerification;
    if (localEmailVerificationCopy != null) {
      buffer.writeObject(localEmailVerificationCopy);
    }
  }
}

/// Auth Log Out.
///
/// ID: `3e72ba19`.
class AuthLogOut extends TlMethod<AuthLoggedOutBase> {
  /// Auth Log Out constructor.
  const AuthLogOut() : super._();

  /// Deserialize.
  factory AuthLogOut.deserialize(Uint8List buffer) {
    // final result = AuthLogOut();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3e72ba19);
  }
}

/// Auth Reset Authorizations.
///
/// ID: `9fab0d1a`.
class AuthResetAuthorizations extends TlMethod<bool> {
  /// Auth Reset Authorizations constructor.
  const AuthResetAuthorizations() : super._();

  /// Deserialize.
  factory AuthResetAuthorizations.deserialize(Uint8List buffer) {
    // final result = AuthResetAuthorizations();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9fab0d1a);
  }
}

/// Auth Export Authorization.
///
/// ID: `e5bfffcd`.
class AuthExportAuthorization extends TlMethod<AuthExportedAuthorizationBase> {
  /// Auth Export Authorization constructor.
  const AuthExportAuthorization({
    required this.dcId,
  }) : super._();

  /// Deserialize.
  factory AuthExportAuthorization.deserialize(Uint8List buffer) {
    // final dcId = _readint(buffer);
    // final result = AuthExportAuthorization(dcId: dcId,);

    // return result;

    throw Exception();
  }

  /// Dc Id.
  final int dcId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe5bfffcd);
    buffer.writeInt(dcId);
  }
}

/// Auth Import Authorization.
///
/// ID: `a57a7dad`.
class AuthImportAuthorization extends TlMethod<AuthAuthorizationBase> {
  /// Auth Import Authorization constructor.
  const AuthImportAuthorization({
    required this.id,
    required this.bytes,
  }) : super._();

  /// Deserialize.
  factory AuthImportAuthorization.deserialize(Uint8List buffer) {
    // final id = _readlong(buffer);
    // final bytes = _readbytes(buffer);
    // final result = AuthImportAuthorization(id: id, bytes: bytes,);

    // return result;

    throw Exception();
  }

  /// Id.
  final int id;

  /// Bytes.
  final Uint8List bytes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa57a7dad);
    buffer.writeLong(id);
    buffer.writeBytes(bytes);
  }
}

/// Auth Bind Temp Auth Key.
///
/// ID: `cdd42a05`.
class AuthBindTempAuthKey extends TlMethod<bool> {
  /// Auth Bind Temp Auth Key constructor.
  const AuthBindTempAuthKey({
    required this.permAuthKeyId,
    required this.nonce,
    required this.expiresAt,
    required this.encryptedMessage,
  }) : super._();

  /// Deserialize.
  factory AuthBindTempAuthKey.deserialize(Uint8List buffer) {
    // final permAuthKeyId = _readlong(buffer);
    // final nonce = _readlong(buffer);
    // final expiresAt = _readint(buffer);
    // final encryptedMessage = _readbytes(buffer);
    // final result = AuthBindTempAuthKey(permAuthKeyId: permAuthKeyId, nonce: nonce, expiresAt: expiresAt, encryptedMessage: encryptedMessage,);

    // return result;

    throw Exception();
  }

  /// Perm Auth Key Id.
  final int permAuthKeyId;

  /// Nonce.
  final int nonce;

  /// Expires At.
  final int expiresAt;

  /// Encrypted Message.
  final Uint8List encryptedMessage;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcdd42a05);
    buffer.writeLong(permAuthKeyId);
    buffer.writeLong(nonce);
    buffer.writeInt(expiresAt);
    buffer.writeBytes(encryptedMessage);
  }
}

/// Account Register Device.
///
/// ID: `ec86017a`.
class AccountRegisterDevice extends TlMethod<bool> {
  /// Account Register Device constructor.
  const AccountRegisterDevice({
    required this.noMuted,
    required this.tokenType,
    required this.token,
    required this.appSandbox,
    required this.secret,
    required this.otherUids,
  }) : super._();

  /// Deserialize.
  factory AccountRegisterDevice.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final noMuted = _readflags.0?true(buffer);
    // final tokenType = _readint(buffer);
    // final token = _readstring(buffer);
    // final appSandbox = _readBool(buffer);
    // final secret = _readbytes(buffer);
    // final otherUids = _readVector<long>(buffer);
    // final result = AccountRegisterDevice(flags: flags, noMuted: noMuted, tokenType: tokenType, token: token, appSandbox: appSandbox, secret: secret, otherUids: otherUids,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: noMuted,
    );

    return v;
  }

  /// no_muted: bit 0 of flags.0?true
  final bool noMuted;

  /// Token Type.
  final int tokenType;

  /// Token.
  final String token;

  /// App Sandbox.
  final bool appSandbox;

  /// Secret.
  final Uint8List secret;

  /// Other Uids.
  final List<int> otherUids;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xec86017a);
    buffer.writeInt(flags);
    buffer.writeInt(tokenType);
    buffer.writeString(token);
    buffer.writeBool(appSandbox);
    buffer.writeBytes(secret);
    buffer.writeVectorLong(otherUids);
  }
}

/// Account Unregister Device.
///
/// ID: `6a0d3206`.
class AccountUnregisterDevice extends TlMethod<bool> {
  /// Account Unregister Device constructor.
  const AccountUnregisterDevice({
    required this.tokenType,
    required this.token,
    required this.otherUids,
  }) : super._();

  /// Deserialize.
  factory AccountUnregisterDevice.deserialize(Uint8List buffer) {
    // final tokenType = _readint(buffer);
    // final token = _readstring(buffer);
    // final otherUids = _readVector<long>(buffer);
    // final result = AccountUnregisterDevice(tokenType: tokenType, token: token, otherUids: otherUids,);

    // return result;

    throw Exception();
  }

  /// Token Type.
  final int tokenType;

  /// Token.
  final String token;

  /// Other Uids.
  final List<int> otherUids;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6a0d3206);
    buffer.writeInt(tokenType);
    buffer.writeString(token);
    buffer.writeVectorLong(otherUids);
  }
}

/// Account Update Notify Settings.
///
/// ID: `84be5b93`.
class AccountUpdateNotifySettings extends TlMethod<bool> {
  /// Account Update Notify Settings constructor.
  const AccountUpdateNotifySettings({
    required this.peer,
    required this.settings,
  }) : super._();

  /// Deserialize.
  factory AccountUpdateNotifySettings.deserialize(Uint8List buffer) {
    // final peer = _readInputNotifyPeer(buffer);
    // final settings = _readInputPeerNotifySettings(buffer);
    // final result = AccountUpdateNotifySettings(peer: peer, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputNotifyPeerBase peer;

  /// Settings.
  final InputPeerNotifySettingsBase settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x84be5b93);
    buffer.writeObject(peer);
    buffer.writeObject(settings);
  }
}

/// Account Get Notify Settings.
///
/// ID: `12b3ad31`.
class AccountGetNotifySettings extends TlMethod<PeerNotifySettingsBase> {
  /// Account Get Notify Settings constructor.
  const AccountGetNotifySettings({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory AccountGetNotifySettings.deserialize(Uint8List buffer) {
    // final peer = _readInputNotifyPeer(buffer);
    // final result = AccountGetNotifySettings(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputNotifyPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x12b3ad31);
    buffer.writeObject(peer);
  }
}

/// Account Reset Notify Settings.
///
/// ID: `db7e1747`.
class AccountResetNotifySettings extends TlMethod<bool> {
  /// Account Reset Notify Settings constructor.
  const AccountResetNotifySettings() : super._();

  /// Deserialize.
  factory AccountResetNotifySettings.deserialize(Uint8List buffer) {
    // final result = AccountResetNotifySettings();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdb7e1747);
  }
}

/// Account Update Profile.
///
/// ID: `78515775`.
class AccountUpdateProfile extends TlMethod<UserBase> {
  /// Account Update Profile constructor.
  const AccountUpdateProfile({
    this.firstName,
    this.lastName,
    this.about,
  }) : super._();

  /// Deserialize.
  factory AccountUpdateProfile.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final firstName = _readflags.0?string(buffer);
    // final lastName = _readflags.1?string(buffer);
    // final about = _readflags.2?string(buffer);
    // final result = AccountUpdateProfile(flags: flags, firstName: firstName, lastName: lastName, about: about,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: firstName != null,
      b01: lastName != null,
      b02: about != null,
    );

    return v;
  }

  /// First Name.
  final String? firstName;

  /// Last Name.
  final String? lastName;

  /// About.
  final String? about;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x78515775);
    buffer.writeInt(flags);
    final localFirstNameCopy = firstName;
    if (localFirstNameCopy != null) {
      buffer.writeString(localFirstNameCopy);
    }
    final localLastNameCopy = lastName;
    if (localLastNameCopy != null) {
      buffer.writeString(localLastNameCopy);
    }
    final localAboutCopy = about;
    if (localAboutCopy != null) {
      buffer.writeString(localAboutCopy);
    }
  }
}

/// Account Update Status.
///
/// ID: `6628562c`.
class AccountUpdateStatus extends TlMethod<bool> {
  /// Account Update Status constructor.
  const AccountUpdateStatus({
    required this.offline,
  }) : super._();

  /// Deserialize.
  factory AccountUpdateStatus.deserialize(Uint8List buffer) {
    // final offline = _readBool(buffer);
    // final result = AccountUpdateStatus(offline: offline,);

    // return result;

    throw Exception();
  }

  /// Offline.
  final bool offline;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6628562c);
    buffer.writeBool(offline);
  }
}

/// Account Get Wall Papers.
///
/// ID: `07967d36`.
class AccountGetWallPapers extends TlMethod<AccountWallPapersBase> {
  /// Account Get Wall Papers constructor.
  const AccountGetWallPapers({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory AccountGetWallPapers.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = AccountGetWallPapers(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x07967d36);
    buffer.writeLong(hash);
  }
}

/// Account Report Peer.
///
/// ID: `c5ba3d86`.
class AccountReportPeer extends TlMethod<bool> {
  /// Account Report Peer constructor.
  const AccountReportPeer({
    required this.peer,
    required this.reason,
    required this.message,
  }) : super._();

  /// Deserialize.
  factory AccountReportPeer.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final reason = _readReportReason(buffer);
    // final message = _readstring(buffer);
    // final result = AccountReportPeer(peer: peer, reason: reason, message: message,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Reason.
  final ReportReasonBase reason;

  /// Message.
  final String message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc5ba3d86);
    buffer.writeObject(peer);
    buffer.writeObject(reason);
    buffer.writeString(message);
  }
}

/// Users Get Users.
///
/// ID: `0d91a548`.
class UsersGetUsers extends TlMethod<List<UserBase>> {
  /// Users Get Users constructor.
  const UsersGetUsers({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory UsersGetUsers.deserialize(Uint8List buffer) {
    // final id = _readVector<InputUser>(buffer);
    // final result = UsersGetUsers(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final List<InputUserBase> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0d91a548);
    buffer.writeVectorObject(id);
  }
}

/// Users Get Full User.
///
/// ID: `b60f5918`.
class UsersGetFullUser extends TlMethod<UsersUserFullBase> {
  /// Users Get Full User constructor.
  const UsersGetFullUser({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory UsersGetFullUser.deserialize(Uint8List buffer) {
    // final id = _readInputUser(buffer);
    // final result = UsersGetFullUser(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final InputUserBase id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb60f5918);
    buffer.writeObject(id);
  }
}

/// Contacts Get Contact I Ds.
///
/// ID: `7adc669d`.
class ContactsGetContactIDs extends TlMethod<List<int>> {
  /// Contacts Get Contact I Ds constructor.
  const ContactsGetContactIDs({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory ContactsGetContactIDs.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = ContactsGetContactIDs(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7adc669d);
    buffer.writeLong(hash);
  }
}

/// Contacts Get Statuses.
///
/// ID: `c4a353ee`.
class ContactsGetStatuses extends TlMethod<List<ContactStatusBase>> {
  /// Contacts Get Statuses constructor.
  const ContactsGetStatuses() : super._();

  /// Deserialize.
  factory ContactsGetStatuses.deserialize(Uint8List buffer) {
    // final result = ContactsGetStatuses();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc4a353ee);
  }
}

/// Contacts Get Contacts.
///
/// ID: `5dd69e12`.
class ContactsGetContacts extends TlMethod<ContactsContactsBase> {
  /// Contacts Get Contacts constructor.
  const ContactsGetContacts({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory ContactsGetContacts.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = ContactsGetContacts(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5dd69e12);
    buffer.writeLong(hash);
  }
}

/// Contacts Import Contacts.
///
/// ID: `2c800be5`.
class ContactsImportContacts extends TlMethod<ContactsImportedContactsBase> {
  /// Contacts Import Contacts constructor.
  const ContactsImportContacts({
    required this.contacts,
  }) : super._();

  /// Deserialize.
  factory ContactsImportContacts.deserialize(Uint8List buffer) {
    // final contacts = _readVector<InputContact>(buffer);
    // final result = ContactsImportContacts(contacts: contacts,);

    // return result;

    throw Exception();
  }

  /// Contacts.
  final List<InputContactBase> contacts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2c800be5);
    buffer.writeVectorObject(contacts);
  }
}

/// Contacts Delete Contacts.
///
/// ID: `096a0e00`.
class ContactsDeleteContacts extends TlMethod<UpdatesBase> {
  /// Contacts Delete Contacts constructor.
  const ContactsDeleteContacts({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory ContactsDeleteContacts.deserialize(Uint8List buffer) {
    // final id = _readVector<InputUser>(buffer);
    // final result = ContactsDeleteContacts(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final List<InputUserBase> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x096a0e00);
    buffer.writeVectorObject(id);
  }
}

/// Contacts Delete By Phones.
///
/// ID: `1013fd9e`.
class ContactsDeleteByPhones extends TlMethod<bool> {
  /// Contacts Delete By Phones constructor.
  const ContactsDeleteByPhones({
    required this.phones,
  }) : super._();

  /// Deserialize.
  factory ContactsDeleteByPhones.deserialize(Uint8List buffer) {
    // final phones = _readVector<string>(buffer);
    // final result = ContactsDeleteByPhones(phones: phones,);

    // return result;

    throw Exception();
  }

  /// Phones.
  final List<String> phones;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1013fd9e);
    buffer.writeVectorString(phones);
  }
}

/// Contacts Block.
///
/// ID: `2e2e8734`.
class ContactsBlock extends TlMethod<bool> {
  /// Contacts Block constructor.
  const ContactsBlock({
    required this.myStoriesFrom,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory ContactsBlock.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final myStoriesFrom = _readflags.0?true(buffer);
    // final id = _readInputPeer(buffer);
    // final result = ContactsBlock(flags: flags, myStoriesFrom: myStoriesFrom, id: id,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: myStoriesFrom,
    );

    return v;
  }

  /// my_stories_from: bit 0 of flags.0?true
  final bool myStoriesFrom;

  /// Id.
  final InputPeerBase id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2e2e8734);
    buffer.writeInt(flags);
    buffer.writeObject(id);
  }
}

/// Contacts Unblock.
///
/// ID: `b550d328`.
class ContactsUnblock extends TlMethod<bool> {
  /// Contacts Unblock constructor.
  const ContactsUnblock({
    required this.myStoriesFrom,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory ContactsUnblock.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final myStoriesFrom = _readflags.0?true(buffer);
    // final id = _readInputPeer(buffer);
    // final result = ContactsUnblock(flags: flags, myStoriesFrom: myStoriesFrom, id: id,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: myStoriesFrom,
    );

    return v;
  }

  /// my_stories_from: bit 0 of flags.0?true
  final bool myStoriesFrom;

  /// Id.
  final InputPeerBase id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb550d328);
    buffer.writeInt(flags);
    buffer.writeObject(id);
  }
}

/// Contacts Get Blocked.
///
/// ID: `9a868f80`.
class ContactsGetBlocked extends TlMethod<ContactsBlockedBase> {
  /// Contacts Get Blocked constructor.
  const ContactsGetBlocked({
    required this.myStoriesFrom,
    required this.offset,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory ContactsGetBlocked.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final myStoriesFrom = _readflags.0?true(buffer);
    // final offset = _readint(buffer);
    // final limit = _readint(buffer);
    // final result = ContactsGetBlocked(flags: flags, myStoriesFrom: myStoriesFrom, offset: offset, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: myStoriesFrom,
    );

    return v;
  }

  /// my_stories_from: bit 0 of flags.0?true
  final bool myStoriesFrom;

  /// Offset.
  final int offset;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9a868f80);
    buffer.writeInt(flags);
    buffer.writeInt(offset);
    buffer.writeInt(limit);
  }
}

/// Messages Get Messages.
///
/// ID: `63c66506`.
class MessagesGetMessages extends TlMethod<MessagesMessagesBase> {
  /// Messages Get Messages constructor.
  const MessagesGetMessages({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesGetMessages.deserialize(Uint8List buffer) {
    // final id = _readVector<InputMessage>(buffer);
    // final result = MessagesGetMessages(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final List<InputMessageBase> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x63c66506);
    buffer.writeVectorObject(id);
  }
}

/// Messages Get Dialogs.
///
/// ID: `a0f4cb4f`.
class MessagesGetDialogs extends TlMethod<MessagesDialogsBase> {
  /// Messages Get Dialogs constructor.
  const MessagesGetDialogs({
    required this.excludePinned,
    this.folderId,
    required this.offsetDate,
    required this.offsetId,
    required this.offsetPeer,
    required this.limit,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetDialogs.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final excludePinned = _readflags.0?true(buffer);
    // final folderId = _readflags.1?int(buffer);
    // final offsetDate = _readint(buffer);
    // final offsetId = _readint(buffer);
    // final offsetPeer = _readInputPeer(buffer);
    // final limit = _readint(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesGetDialogs(flags: flags, excludePinned: excludePinned, folderId: folderId, offsetDate: offsetDate, offsetId: offsetId, offsetPeer: offsetPeer, limit: limit, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: excludePinned,
      b01: folderId != null,
    );

    return v;
  }

  /// exclude_pinned: bit 0 of flags.0?true
  final bool excludePinned;

  /// Folder Id.
  final int? folderId;

  /// Offset Date.
  final DateTime offsetDate;

  /// Offset Id.
  final int offsetId;

  /// Offset Peer.
  final InputPeerBase offsetPeer;

  /// Limit.
  final int limit;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa0f4cb4f);
    buffer.writeInt(flags);
    final localFolderIdCopy = folderId;
    if (localFolderIdCopy != null) {
      buffer.writeInt(localFolderIdCopy);
    }
    buffer.writeDateTime(offsetDate);
    buffer.writeInt(offsetId);
    buffer.writeObject(offsetPeer);
    buffer.writeInt(limit);
    buffer.writeLong(hash);
  }
}

/// Messages Get History.
///
/// ID: `4423e6c5`.
class MessagesGetHistory extends TlMethod<MessagesMessagesBase> {
  /// Messages Get History constructor.
  const MessagesGetHistory({
    required this.peer,
    required this.offsetId,
    required this.offsetDate,
    required this.addOffset,
    required this.limit,
    required this.maxId,
    required this.minId,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetHistory.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final offsetId = _readint(buffer);
    // final offsetDate = _readint(buffer);
    // final addOffset = _readint(buffer);
    // final limit = _readint(buffer);
    // final maxId = _readint(buffer);
    // final minId = _readint(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesGetHistory(peer: peer, offsetId: offsetId, offsetDate: offsetDate, addOffset: addOffset, limit: limit, maxId: maxId, minId: minId, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Offset Id.
  final int offsetId;

  /// Offset Date.
  final DateTime offsetDate;

  /// Add Offset.
  final int addOffset;

  /// Limit.
  final int limit;

  /// Max Id.
  final int maxId;

  /// Min Id.
  final int minId;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4423e6c5);
    buffer.writeObject(peer);
    buffer.writeInt(offsetId);
    buffer.writeDateTime(offsetDate);
    buffer.writeInt(addOffset);
    buffer.writeInt(limit);
    buffer.writeInt(maxId);
    buffer.writeInt(minId);
    buffer.writeLong(hash);
  }
}

/// Messages Search.
///
/// ID: `a7b4e929`.
class MessagesSearch extends TlMethod<MessagesMessagesBase> {
  /// Messages Search constructor.
  const MessagesSearch({
    required this.peer,
    required this.q,
    this.fromId,
    this.savedPeerId,
    this.topMsgId,
    required this.filter,
    required this.minDate,
    required this.maxDate,
    required this.offsetId,
    required this.addOffset,
    required this.limit,
    required this.maxId,
    required this.minId,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesSearch.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final q = _readstring(buffer);
    // final fromId = _readflags.0?InputPeer(buffer);
    // final savedPeerId = _readflags.2?InputPeer(buffer);
    // final topMsgId = _readflags.1?int(buffer);
    // final filter = _readMessagesFilter(buffer);
    // final minDate = _readint(buffer);
    // final maxDate = _readint(buffer);
    // final offsetId = _readint(buffer);
    // final addOffset = _readint(buffer);
    // final limit = _readint(buffer);
    // final maxId = _readint(buffer);
    // final minId = _readint(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesSearch(flags: flags, peer: peer, q: q, fromId: fromId, savedPeerId: savedPeerId, topMsgId: topMsgId, filter: filter, minDate: minDate, maxDate: maxDate, offsetId: offsetId, addOffset: addOffset, limit: limit, maxId: maxId, minId: minId, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: fromId != null,
      b02: savedPeerId != null,
      b01: topMsgId != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Q.
  final String q;

  /// From Id.
  final InputPeerBase? fromId;

  /// Saved Peer Id.
  final InputPeerBase? savedPeerId;

  /// Top Msg Id.
  final int? topMsgId;

  /// Filter.
  final MessagesFilterBase filter;

  /// Min Date.
  final DateTime minDate;

  /// Max Date.
  final DateTime maxDate;

  /// Offset Id.
  final int offsetId;

  /// Add Offset.
  final int addOffset;

  /// Limit.
  final int limit;

  /// Max Id.
  final int maxId;

  /// Min Id.
  final int minId;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa7b4e929);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeString(q);
    final localFromIdCopy = fromId;
    if (localFromIdCopy != null) {
      buffer.writeObject(localFromIdCopy);
    }
    final localSavedPeerIdCopy = savedPeerId;
    if (localSavedPeerIdCopy != null) {
      buffer.writeObject(localSavedPeerIdCopy);
    }
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
    buffer.writeObject(filter);
    buffer.writeDateTime(minDate);
    buffer.writeDateTime(maxDate);
    buffer.writeInt(offsetId);
    buffer.writeInt(addOffset);
    buffer.writeInt(limit);
    buffer.writeInt(maxId);
    buffer.writeInt(minId);
    buffer.writeLong(hash);
  }
}

/// Messages Read History.
///
/// ID: `0e306d3a`.
class MessagesReadHistory extends TlMethod<MessagesAffectedMessagesBase> {
  /// Messages Read History constructor.
  const MessagesReadHistory({
    required this.peer,
    required this.maxId,
  }) : super._();

  /// Deserialize.
  factory MessagesReadHistory.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final maxId = _readint(buffer);
    // final result = MessagesReadHistory(peer: peer, maxId: maxId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Max Id.
  final int maxId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0e306d3a);
    buffer.writeObject(peer);
    buffer.writeInt(maxId);
  }
}

/// Messages Delete History.
///
/// ID: `b08f922a`.
class MessagesDeleteHistory extends TlMethod<MessagesAffectedHistoryBase> {
  /// Messages Delete History constructor.
  const MessagesDeleteHistory({
    required this.justClear,
    required this.revoke,
    required this.peer,
    required this.maxId,
    this.minDate,
    this.maxDate,
  }) : super._();

  /// Deserialize.
  factory MessagesDeleteHistory.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final justClear = _readflags.0?true(buffer);
    // final revoke = _readflags.1?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final maxId = _readint(buffer);
    // final minDate = _readflags.2?int(buffer);
    // final maxDate = _readflags.3?int(buffer);
    // final result = MessagesDeleteHistory(flags: flags, justClear: justClear, revoke: revoke, peer: peer, maxId: maxId, minDate: minDate, maxDate: maxDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: justClear,
      b01: revoke,
      b02: minDate != null,
      b03: maxDate != null,
    );

    return v;
  }

  /// just_clear: bit 0 of flags.0?true
  final bool justClear;

  /// revoke: bit 1 of flags.1?true
  final bool revoke;

  /// Peer.
  final InputPeerBase peer;

  /// Max Id.
  final int maxId;

  /// Min Date.
  final DateTime? minDate;

  /// Max Date.
  final DateTime? maxDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb08f922a);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(maxId);
    final localMinDateCopy = minDate;
    if (localMinDateCopy != null) {
      buffer.writeDateTime(localMinDateCopy);
    }
    final localMaxDateCopy = maxDate;
    if (localMaxDateCopy != null) {
      buffer.writeDateTime(localMaxDateCopy);
    }
  }
}

/// Messages Delete Messages.
///
/// ID: `e58e95d2`.
class MessagesDeleteMessages extends TlMethod<MessagesAffectedMessagesBase> {
  /// Messages Delete Messages constructor.
  const MessagesDeleteMessages({
    required this.revoke,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesDeleteMessages.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final revoke = _readflags.0?true(buffer);
    // final id = _readVector<int>(buffer);
    // final result = MessagesDeleteMessages(flags: flags, revoke: revoke, id: id,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: revoke,
    );

    return v;
  }

  /// revoke: bit 0 of flags.0?true
  final bool revoke;

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe58e95d2);
    buffer.writeInt(flags);
    buffer.writeVectorInt(id);
  }
}

/// Messages Received Messages.
///
/// ID: `05a954c0`.
class MessagesReceivedMessages
    extends TlMethod<List<ReceivedNotifyMessageBase>> {
  /// Messages Received Messages constructor.
  const MessagesReceivedMessages({
    required this.maxId,
  }) : super._();

  /// Deserialize.
  factory MessagesReceivedMessages.deserialize(Uint8List buffer) {
    // final maxId = _readint(buffer);
    // final result = MessagesReceivedMessages(maxId: maxId,);

    // return result;

    throw Exception();
  }

  /// Max Id.
  final int maxId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x05a954c0);
    buffer.writeInt(maxId);
  }
}

/// Messages Set Typing.
///
/// ID: `58943ee2`.
class MessagesSetTyping extends TlMethod<bool> {
  /// Messages Set Typing constructor.
  const MessagesSetTyping({
    required this.peer,
    this.topMsgId,
    required this.action,
  }) : super._();

  /// Deserialize.
  factory MessagesSetTyping.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final topMsgId = _readflags.0?int(buffer);
    // final action = _readSendMessageAction(buffer);
    // final result = MessagesSetTyping(flags: flags, peer: peer, topMsgId: topMsgId, action: action,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: topMsgId != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Top Msg Id.
  final int? topMsgId;

  /// Action.
  final SendMessageActionBase action;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x58943ee2);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
    buffer.writeObject(action);
  }
}

/// Messages Send Message.
///
/// ID: `280d096f`.
class MessagesSendMessage extends TlMethod<UpdatesBase> {
  /// Messages Send Message constructor.
  const MessagesSendMessage({
    required this.noWebpage,
    required this.silent,
    required this.background,
    required this.clearDraft,
    required this.noforwards,
    required this.updateStickersetsOrder,
    required this.invertMedia,
    required this.peer,
    this.replyTo,
    required this.message,
    required this.randomId,
    this.replyMarkup,
    this.entities,
    this.scheduleDate,
    this.sendAs,
  }) : super._();

  /// Deserialize.
  factory MessagesSendMessage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final noWebpage = _readflags.1?true(buffer);
    // final silent = _readflags.5?true(buffer);
    // final background = _readflags.6?true(buffer);
    // final clearDraft = _readflags.7?true(buffer);
    // final noforwards = _readflags.14?true(buffer);
    // final updateStickersetsOrder = _readflags.15?true(buffer);
    // final invertMedia = _readflags.16?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final replyTo = _readflags.0?InputReplyTo(buffer);
    // final message = _readstring(buffer);
    // final randomId = _readlong(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final entities = _readflags.3?Vector<MessageEntity>(buffer);
    // final scheduleDate = _readflags.10?int(buffer);
    // final sendAs = _readflags.13?InputPeer(buffer);
    // final result = MessagesSendMessage(flags: flags, noWebpage: noWebpage, silent: silent, background: background, clearDraft: clearDraft, noforwards: noforwards, updateStickersetsOrder: updateStickersetsOrder, invertMedia: invertMedia, peer: peer, replyTo: replyTo, message: message, randomId: randomId, replyMarkup: replyMarkup, entities: entities, scheduleDate: scheduleDate, sendAs: sendAs,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: noWebpage,
      b05: silent,
      b06: background,
      b07: clearDraft,
      b14: noforwards,
      b15: updateStickersetsOrder,
      b16: invertMedia,
      b00: replyTo != null,
      b02: replyMarkup != null,
      b03: entities != null,
      b10: scheduleDate != null,
      b13: sendAs != null,
    );

    return v;
  }

  /// no_webpage: bit 1 of flags.1?true
  final bool noWebpage;

  /// silent: bit 5 of flags.5?true
  final bool silent;

  /// background: bit 6 of flags.6?true
  final bool background;

  /// clear_draft: bit 7 of flags.7?true
  final bool clearDraft;

  /// noforwards: bit 14 of flags.14?true
  final bool noforwards;

  /// update_stickersets_order: bit 15 of flags.15?true
  final bool updateStickersetsOrder;

  /// invert_media: bit 16 of flags.16?true
  final bool invertMedia;

  /// Peer.
  final InputPeerBase peer;

  /// Reply To.
  final InputReplyToBase? replyTo;

  /// Message.
  final String message;

  /// Random Id.
  final int randomId;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Schedule Date.
  final DateTime? scheduleDate;

  /// Send As.
  final InputPeerBase? sendAs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x280d096f);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localReplyToCopy = replyTo;
    if (localReplyToCopy != null) {
      buffer.writeObject(localReplyToCopy);
    }
    buffer.writeString(message);
    buffer.writeLong(randomId);
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localScheduleDateCopy = scheduleDate;
    if (localScheduleDateCopy != null) {
      buffer.writeDateTime(localScheduleDateCopy);
    }
    final localSendAsCopy = sendAs;
    if (localSendAsCopy != null) {
      buffer.writeObject(localSendAsCopy);
    }
  }
}

/// Messages Send Media.
///
/// ID: `72ccc23d`.
class MessagesSendMedia extends TlMethod<UpdatesBase> {
  /// Messages Send Media constructor.
  const MessagesSendMedia({
    required this.silent,
    required this.background,
    required this.clearDraft,
    required this.noforwards,
    required this.updateStickersetsOrder,
    required this.invertMedia,
    required this.peer,
    this.replyTo,
    required this.media,
    required this.message,
    required this.randomId,
    this.replyMarkup,
    this.entities,
    this.scheduleDate,
    this.sendAs,
  }) : super._();

  /// Deserialize.
  factory MessagesSendMedia.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final silent = _readflags.5?true(buffer);
    // final background = _readflags.6?true(buffer);
    // final clearDraft = _readflags.7?true(buffer);
    // final noforwards = _readflags.14?true(buffer);
    // final updateStickersetsOrder = _readflags.15?true(buffer);
    // final invertMedia = _readflags.16?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final replyTo = _readflags.0?InputReplyTo(buffer);
    // final media = _readInputMedia(buffer);
    // final message = _readstring(buffer);
    // final randomId = _readlong(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final entities = _readflags.3?Vector<MessageEntity>(buffer);
    // final scheduleDate = _readflags.10?int(buffer);
    // final sendAs = _readflags.13?InputPeer(buffer);
    // final result = MessagesSendMedia(flags: flags, silent: silent, background: background, clearDraft: clearDraft, noforwards: noforwards, updateStickersetsOrder: updateStickersetsOrder, invertMedia: invertMedia, peer: peer, replyTo: replyTo, media: media, message: message, randomId: randomId, replyMarkup: replyMarkup, entities: entities, scheduleDate: scheduleDate, sendAs: sendAs,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b05: silent,
      b06: background,
      b07: clearDraft,
      b14: noforwards,
      b15: updateStickersetsOrder,
      b16: invertMedia,
      b00: replyTo != null,
      b02: replyMarkup != null,
      b03: entities != null,
      b10: scheduleDate != null,
      b13: sendAs != null,
    );

    return v;
  }

  /// silent: bit 5 of flags.5?true
  final bool silent;

  /// background: bit 6 of flags.6?true
  final bool background;

  /// clear_draft: bit 7 of flags.7?true
  final bool clearDraft;

  /// noforwards: bit 14 of flags.14?true
  final bool noforwards;

  /// update_stickersets_order: bit 15 of flags.15?true
  final bool updateStickersetsOrder;

  /// invert_media: bit 16 of flags.16?true
  final bool invertMedia;

  /// Peer.
  final InputPeerBase peer;

  /// Reply To.
  final InputReplyToBase? replyTo;

  /// Media.
  final InputMediaBase media;

  /// Message.
  final String message;

  /// Random Id.
  final int randomId;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Schedule Date.
  final DateTime? scheduleDate;

  /// Send As.
  final InputPeerBase? sendAs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x72ccc23d);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localReplyToCopy = replyTo;
    if (localReplyToCopy != null) {
      buffer.writeObject(localReplyToCopy);
    }
    buffer.writeObject(media);
    buffer.writeString(message);
    buffer.writeLong(randomId);
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localScheduleDateCopy = scheduleDate;
    if (localScheduleDateCopy != null) {
      buffer.writeDateTime(localScheduleDateCopy);
    }
    final localSendAsCopy = sendAs;
    if (localSendAsCopy != null) {
      buffer.writeObject(localSendAsCopy);
    }
  }
}

/// Messages Forward Messages.
///
/// ID: `c661bbc4`.
class MessagesForwardMessages extends TlMethod<UpdatesBase> {
  /// Messages Forward Messages constructor.
  const MessagesForwardMessages({
    required this.silent,
    required this.background,
    required this.withMyScore,
    required this.dropAuthor,
    required this.dropMediaCaptions,
    required this.noforwards,
    required this.fromPeer,
    required this.id,
    required this.randomId,
    required this.toPeer,
    this.topMsgId,
    this.scheduleDate,
    this.sendAs,
  }) : super._();

  /// Deserialize.
  factory MessagesForwardMessages.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final silent = _readflags.5?true(buffer);
    // final background = _readflags.6?true(buffer);
    // final withMyScore = _readflags.8?true(buffer);
    // final dropAuthor = _readflags.11?true(buffer);
    // final dropMediaCaptions = _readflags.12?true(buffer);
    // final noforwards = _readflags.14?true(buffer);
    // final fromPeer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final randomId = _readVector<long>(buffer);
    // final toPeer = _readInputPeer(buffer);
    // final topMsgId = _readflags.9?int(buffer);
    // final scheduleDate = _readflags.10?int(buffer);
    // final sendAs = _readflags.13?InputPeer(buffer);
    // final result = MessagesForwardMessages(flags: flags, silent: silent, background: background, withMyScore: withMyScore, dropAuthor: dropAuthor, dropMediaCaptions: dropMediaCaptions, noforwards: noforwards, fromPeer: fromPeer, id: id, randomId: randomId, toPeer: toPeer, topMsgId: topMsgId, scheduleDate: scheduleDate, sendAs: sendAs,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b05: silent,
      b06: background,
      b08: withMyScore,
      b11: dropAuthor,
      b12: dropMediaCaptions,
      b14: noforwards,
      b09: topMsgId != null,
      b10: scheduleDate != null,
      b13: sendAs != null,
    );

    return v;
  }

  /// silent: bit 5 of flags.5?true
  final bool silent;

  /// background: bit 6 of flags.6?true
  final bool background;

  /// with_my_score: bit 8 of flags.8?true
  final bool withMyScore;

  /// drop_author: bit 11 of flags.11?true
  final bool dropAuthor;

  /// drop_media_captions: bit 12 of flags.12?true
  final bool dropMediaCaptions;

  /// noforwards: bit 14 of flags.14?true
  final bool noforwards;

  /// From Peer.
  final InputPeerBase fromPeer;

  /// Id.
  final List<int> id;

  /// Random Id.
  final List<int> randomId;

  /// To Peer.
  final InputPeerBase toPeer;

  /// Top Msg Id.
  final int? topMsgId;

  /// Schedule Date.
  final DateTime? scheduleDate;

  /// Send As.
  final InputPeerBase? sendAs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc661bbc4);
    buffer.writeInt(flags);
    buffer.writeObject(fromPeer);
    buffer.writeVectorInt(id);
    buffer.writeVectorLong(randomId);
    buffer.writeObject(toPeer);
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
    final localScheduleDateCopy = scheduleDate;
    if (localScheduleDateCopy != null) {
      buffer.writeDateTime(localScheduleDateCopy);
    }
    final localSendAsCopy = sendAs;
    if (localSendAsCopy != null) {
      buffer.writeObject(localSendAsCopy);
    }
  }
}

/// Messages Report Spam.
///
/// ID: `cf1592db`.
class MessagesReportSpam extends TlMethod<bool> {
  /// Messages Report Spam constructor.
  const MessagesReportSpam({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory MessagesReportSpam.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = MessagesReportSpam(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcf1592db);
    buffer.writeObject(peer);
  }
}

/// Messages Get Peer Settings.
///
/// ID: `efd9a6a2`.
class MessagesGetPeerSettings extends TlMethod<MessagesPeerSettingsBase> {
  /// Messages Get Peer Settings constructor.
  const MessagesGetPeerSettings({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory MessagesGetPeerSettings.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = MessagesGetPeerSettings(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xefd9a6a2);
    buffer.writeObject(peer);
  }
}

/// Messages Report.
///
/// ID: `8953ab4e`.
class MessagesReport extends TlMethod<bool> {
  /// Messages Report constructor.
  const MessagesReport({
    required this.peer,
    required this.id,
    required this.reason,
    required this.message,
  }) : super._();

  /// Deserialize.
  factory MessagesReport.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final reason = _readReportReason(buffer);
    // final message = _readstring(buffer);
    // final result = MessagesReport(peer: peer, id: id, reason: reason, message: message,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final List<int> id;

  /// Reason.
  final ReportReasonBase reason;

  /// Message.
  final String message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8953ab4e);
    buffer.writeObject(peer);
    buffer.writeVectorInt(id);
    buffer.writeObject(reason);
    buffer.writeString(message);
  }
}

/// Messages Get Chats.
///
/// ID: `49e9528f`.
class MessagesGetChats extends TlMethod<MessagesChatsBase> {
  /// Messages Get Chats constructor.
  const MessagesGetChats({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesGetChats.deserialize(Uint8List buffer) {
    // final id = _readVector<long>(buffer);
    // final result = MessagesGetChats(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x49e9528f);
    buffer.writeVectorLong(id);
  }
}

/// Messages Get Full Chat.
///
/// ID: `aeb00b34`.
class MessagesGetFullChat extends TlMethod<MessagesChatFullBase> {
  /// Messages Get Full Chat constructor.
  const MessagesGetFullChat({
    required this.chatId,
  }) : super._();

  /// Deserialize.
  factory MessagesGetFullChat.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final result = MessagesGetFullChat(chatId: chatId,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xaeb00b34);
    buffer.writeLong(chatId);
  }
}

/// Messages Edit Chat Title.
///
/// ID: `73783ffd`.
class MessagesEditChatTitle extends TlMethod<UpdatesBase> {
  /// Messages Edit Chat Title constructor.
  const MessagesEditChatTitle({
    required this.chatId,
    required this.title,
  }) : super._();

  /// Deserialize.
  factory MessagesEditChatTitle.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final title = _readstring(buffer);
    // final result = MessagesEditChatTitle(chatId: chatId, title: title,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// Title.
  final String title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x73783ffd);
    buffer.writeLong(chatId);
    buffer.writeString(title);
  }
}

/// Messages Edit Chat Photo.
///
/// ID: `35ddd674`.
class MessagesEditChatPhoto extends TlMethod<UpdatesBase> {
  /// Messages Edit Chat Photo constructor.
  const MessagesEditChatPhoto({
    required this.chatId,
    required this.photo,
  }) : super._();

  /// Deserialize.
  factory MessagesEditChatPhoto.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final photo = _readInputChatPhoto(buffer);
    // final result = MessagesEditChatPhoto(chatId: chatId, photo: photo,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// Photo.
  final InputChatPhotoBase photo;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x35ddd674);
    buffer.writeLong(chatId);
    buffer.writeObject(photo);
  }
}

/// Messages Add Chat User.
///
/// ID: `f24753e3`.
class MessagesAddChatUser extends TlMethod<UpdatesBase> {
  /// Messages Add Chat User constructor.
  const MessagesAddChatUser({
    required this.chatId,
    required this.userId,
    required this.fwdLimit,
  }) : super._();

  /// Deserialize.
  factory MessagesAddChatUser.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final userId = _readInputUser(buffer);
    // final fwdLimit = _readint(buffer);
    // final result = MessagesAddChatUser(chatId: chatId, userId: userId, fwdLimit: fwdLimit,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// User Id.
  final InputUserBase userId;

  /// Fwd Limit.
  final int fwdLimit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf24753e3);
    buffer.writeLong(chatId);
    buffer.writeObject(userId);
    buffer.writeInt(fwdLimit);
  }
}

/// Messages Delete Chat User.
///
/// ID: `a2185cab`.
class MessagesDeleteChatUser extends TlMethod<UpdatesBase> {
  /// Messages Delete Chat User constructor.
  const MessagesDeleteChatUser({
    required this.revokeHistory,
    required this.chatId,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory MessagesDeleteChatUser.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final revokeHistory = _readflags.0?true(buffer);
    // final chatId = _readlong(buffer);
    // final userId = _readInputUser(buffer);
    // final result = MessagesDeleteChatUser(flags: flags, revokeHistory: revokeHistory, chatId: chatId, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: revokeHistory,
    );

    return v;
  }

  /// revoke_history: bit 0 of flags.0?true
  final bool revokeHistory;

  /// Chat Id.
  final int chatId;

  /// User Id.
  final InputUserBase userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa2185cab);
    buffer.writeInt(flags);
    buffer.writeLong(chatId);
    buffer.writeObject(userId);
  }
}

/// Messages Create Chat.
///
/// ID: `0034a818`.
class MessagesCreateChat extends TlMethod<UpdatesBase> {
  /// Messages Create Chat constructor.
  const MessagesCreateChat({
    required this.users,
    required this.title,
    this.ttlPeriod,
  }) : super._();

  /// Deserialize.
  factory MessagesCreateChat.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final users = _readVector<InputUser>(buffer);
    // final title = _readstring(buffer);
    // final ttlPeriod = _readflags.0?int(buffer);
    // final result = MessagesCreateChat(flags: flags, users: users, title: title, ttlPeriod: ttlPeriod,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: ttlPeriod != null,
    );

    return v;
  }

  /// Users.
  final List<InputUserBase> users;

  /// Title.
  final String title;

  /// Ttl Period.
  final int? ttlPeriod;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0034a818);
    buffer.writeInt(flags);
    buffer.writeVectorObject(users);
    buffer.writeString(title);
    final localTtlPeriodCopy = ttlPeriod;
    if (localTtlPeriodCopy != null) {
      buffer.writeInt(localTtlPeriodCopy);
    }
  }
}

/// Updates Get State.
///
/// ID: `edd4882a`.
class UpdatesGetState extends TlMethod<UpdatesStateBase> {
  /// Updates Get State constructor.
  const UpdatesGetState() : super._();

  /// Deserialize.
  factory UpdatesGetState.deserialize(Uint8List buffer) {
    // final result = UpdatesGetState();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xedd4882a);
  }
}

/// Updates Get Difference.
///
/// ID: `19c2f763`.
class UpdatesGetDifference extends TlMethod<UpdatesDifferenceBase> {
  /// Updates Get Difference constructor.
  const UpdatesGetDifference({
    required this.pts,
    this.ptsLimit,
    this.ptsTotalLimit,
    required this.date,
    required this.qts,
    this.qtsLimit,
  }) : super._();

  /// Deserialize.
  factory UpdatesGetDifference.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pts = _readint(buffer);
    // final ptsLimit = _readflags.1?int(buffer);
    // final ptsTotalLimit = _readflags.0?int(buffer);
    // final date = _readint(buffer);
    // final qts = _readint(buffer);
    // final qtsLimit = _readflags.2?int(buffer);
    // final result = UpdatesGetDifference(flags: flags, pts: pts, ptsLimit: ptsLimit, ptsTotalLimit: ptsTotalLimit, date: date, qts: qts, qtsLimit: qtsLimit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: ptsLimit != null,
      b00: ptsTotalLimit != null,
      b02: qtsLimit != null,
    );

    return v;
  }

  /// Pts.
  final int pts;

  /// Pts Limit.
  final int? ptsLimit;

  /// Pts Total Limit.
  final int? ptsTotalLimit;

  /// Date.
  final DateTime date;

  /// Qts.
  final int qts;

  /// Qts Limit.
  final int? qtsLimit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x19c2f763);
    buffer.writeInt(flags);
    buffer.writeInt(pts);
    final localPtsLimitCopy = ptsLimit;
    if (localPtsLimitCopy != null) {
      buffer.writeInt(localPtsLimitCopy);
    }
    final localPtsTotalLimitCopy = ptsTotalLimit;
    if (localPtsTotalLimitCopy != null) {
      buffer.writeInt(localPtsTotalLimitCopy);
    }
    buffer.writeDateTime(date);
    buffer.writeInt(qts);
    final localQtsLimitCopy = qtsLimit;
    if (localQtsLimitCopy != null) {
      buffer.writeInt(localQtsLimitCopy);
    }
  }
}

/// Photos Update Profile Photo.
///
/// ID: `09e82039`.
class PhotosUpdateProfilePhoto extends TlMethod<PhotosPhotoBase> {
  /// Photos Update Profile Photo constructor.
  const PhotosUpdateProfilePhoto({
    required this.fallback,
    this.bot,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory PhotosUpdateProfilePhoto.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final fallback = _readflags.0?true(buffer);
    // final bot = _readflags.1?InputUser(buffer);
    // final id = _readInputPhoto(buffer);
    // final result = PhotosUpdateProfilePhoto(flags: flags, fallback: fallback, bot: bot, id: id,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: fallback,
      b01: bot != null,
    );

    return v;
  }

  /// fallback: bit 0 of flags.0?true
  final bool fallback;

  /// Bot.
  final InputUserBase? bot;

  /// Id.
  final InputPhotoBase id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x09e82039);
    buffer.writeInt(flags);
    final localBotCopy = bot;
    if (localBotCopy != null) {
      buffer.writeObject(localBotCopy);
    }
    buffer.writeObject(id);
  }
}

/// Photos Upload Profile Photo.
///
/// ID: `0388a3b5`.
class PhotosUploadProfilePhoto extends TlMethod<PhotosPhotoBase> {
  /// Photos Upload Profile Photo constructor.
  const PhotosUploadProfilePhoto({
    required this.fallback,
    this.bot,
    this.file,
    this.video,
    this.videoStartTs,
    this.videoEmojiMarkup,
  }) : super._();

  /// Deserialize.
  factory PhotosUploadProfilePhoto.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final fallback = _readflags.3?true(buffer);
    // final bot = _readflags.5?InputUser(buffer);
    // final file = _readflags.0?InputFile(buffer);
    // final video = _readflags.1?InputFile(buffer);
    // final videoStartTs = _readflags.2?double(buffer);
    // final videoEmojiMarkup = _readflags.4?VideoSize(buffer);
    // final result = PhotosUploadProfilePhoto(flags: flags, fallback: fallback, bot: bot, file: file, video: video, videoStartTs: videoStartTs, videoEmojiMarkup: videoEmojiMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: fallback,
      b05: bot != null,
      b00: file != null,
      b01: video != null,
      b02: videoStartTs != null,
      b04: videoEmojiMarkup != null,
    );

    return v;
  }

  /// fallback: bit 3 of flags.3?true
  final bool fallback;

  /// Bot.
  final InputUserBase? bot;

  /// File.
  final InputFileBase? file;

  /// Video.
  final InputFileBase? video;

  /// Video Start Ts.
  final double? videoStartTs;

  /// Video Emoji Markup.
  final VideoSizeBase? videoEmojiMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0388a3b5);
    buffer.writeInt(flags);
    final localBotCopy = bot;
    if (localBotCopy != null) {
      buffer.writeObject(localBotCopy);
    }
    final localFileCopy = file;
    if (localFileCopy != null) {
      buffer.writeObject(localFileCopy);
    }
    final localVideoCopy = video;
    if (localVideoCopy != null) {
      buffer.writeObject(localVideoCopy);
    }
    final localVideoStartTsCopy = videoStartTs;
    if (localVideoStartTsCopy != null) {
      buffer.writeDouble(localVideoStartTsCopy);
    }
    final localVideoEmojiMarkupCopy = videoEmojiMarkup;
    if (localVideoEmojiMarkupCopy != null) {
      buffer.writeObject(localVideoEmojiMarkupCopy);
    }
  }
}

/// Photos Delete Photos.
///
/// ID: `87cf7f2f`.
class PhotosDeletePhotos extends TlMethod<List<int>> {
  /// Photos Delete Photos constructor.
  const PhotosDeletePhotos({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory PhotosDeletePhotos.deserialize(Uint8List buffer) {
    // final id = _readVector<InputPhoto>(buffer);
    // final result = PhotosDeletePhotos(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final List<InputPhotoBase> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x87cf7f2f);
    buffer.writeVectorObject(id);
  }
}

/// Upload Save File Part.
///
/// ID: `b304a621`.
class UploadSaveFilePart extends TlMethod<bool> {
  /// Upload Save File Part constructor.
  const UploadSaveFilePart({
    required this.fileId,
    required this.filePart,
    required this.bytes,
  }) : super._();

  /// Deserialize.
  factory UploadSaveFilePart.deserialize(Uint8List buffer) {
    // final fileId = _readlong(buffer);
    // final filePart = _readint(buffer);
    // final bytes = _readbytes(buffer);
    // final result = UploadSaveFilePart(fileId: fileId, filePart: filePart, bytes: bytes,);

    // return result;

    throw Exception();
  }

  /// File Id.
  final int fileId;

  /// File Part.
  final int filePart;

  /// Bytes.
  final Uint8List bytes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb304a621);
    buffer.writeLong(fileId);
    buffer.writeInt(filePart);
    buffer.writeBytes(bytes);
  }
}

/// Upload Get File.
///
/// ID: `be5335be`.
class UploadGetFile extends TlMethod<UploadFileBase> {
  /// Upload Get File constructor.
  const UploadGetFile({
    required this.precise,
    required this.cdnSupported,
    required this.location,
    required this.offset,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory UploadGetFile.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final precise = _readflags.0?true(buffer);
    // final cdnSupported = _readflags.1?true(buffer);
    // final location = _readInputFileLocation(buffer);
    // final offset = _readlong(buffer);
    // final limit = _readint(buffer);
    // final result = UploadGetFile(flags: flags, precise: precise, cdnSupported: cdnSupported, location: location, offset: offset, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: precise,
      b01: cdnSupported,
    );

    return v;
  }

  /// precise: bit 0 of flags.0?true
  final bool precise;

  /// cdn_supported: bit 1 of flags.1?true
  final bool cdnSupported;

  /// Location.
  final InputFileLocationBase location;

  /// Offset.
  final int offset;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbe5335be);
    buffer.writeInt(flags);
    buffer.writeObject(location);
    buffer.writeLong(offset);
    buffer.writeInt(limit);
  }
}

/// Help Get Config.
///
/// ID: `c4f9186b`.
class HelpGetConfig extends TlMethod<ConfigBase> {
  /// Help Get Config constructor.
  const HelpGetConfig() : super._();

  /// Deserialize.
  factory HelpGetConfig.deserialize(Uint8List buffer) {
    // final result = HelpGetConfig();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc4f9186b);
  }
}

/// Help Get Nearest Dc.
///
/// ID: `1fb33026`.
class HelpGetNearestDc extends TlMethod<NearestDcBase> {
  /// Help Get Nearest Dc constructor.
  const HelpGetNearestDc() : super._();

  /// Deserialize.
  factory HelpGetNearestDc.deserialize(Uint8List buffer) {
    // final result = HelpGetNearestDc();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1fb33026);
  }
}

/// Help Get App Update.
///
/// ID: `522d5a7d`.
class HelpGetAppUpdate extends TlMethod<HelpAppUpdateBase> {
  /// Help Get App Update constructor.
  const HelpGetAppUpdate({
    required this.source,
  }) : super._();

  /// Deserialize.
  factory HelpGetAppUpdate.deserialize(Uint8List buffer) {
    // final source = _readstring(buffer);
    // final result = HelpGetAppUpdate(source: source,);

    // return result;

    throw Exception();
  }

  /// Source.
  final String source;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x522d5a7d);
    buffer.writeString(source);
  }
}

/// Help Get Invite Text.
///
/// ID: `4d392343`.
class HelpGetInviteText extends TlMethod<HelpInviteTextBase> {
  /// Help Get Invite Text constructor.
  const HelpGetInviteText() : super._();

  /// Deserialize.
  factory HelpGetInviteText.deserialize(Uint8List buffer) {
    // final result = HelpGetInviteText();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4d392343);
  }
}

/// Photos Get User Photos.
///
/// ID: `91cd32a8`.
class PhotosGetUserPhotos extends TlMethod<PhotosPhotosBase> {
  /// Photos Get User Photos constructor.
  const PhotosGetUserPhotos({
    required this.userId,
    required this.offset,
    required this.maxId,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory PhotosGetUserPhotos.deserialize(Uint8List buffer) {
    // final userId = _readInputUser(buffer);
    // final offset = _readint(buffer);
    // final maxId = _readlong(buffer);
    // final limit = _readint(buffer);
    // final result = PhotosGetUserPhotos(userId: userId, offset: offset, maxId: maxId, limit: limit,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final InputUserBase userId;

  /// Offset.
  final int offset;

  /// Max Id.
  final int maxId;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x91cd32a8);
    buffer.writeObject(userId);
    buffer.writeInt(offset);
    buffer.writeLong(maxId);
    buffer.writeInt(limit);
  }
}

/// Messages Get Dh Config.
///
/// ID: `26cf8950`.
class MessagesGetDhConfig extends TlMethod<MessagesDhConfigBase> {
  /// Messages Get Dh Config constructor.
  const MessagesGetDhConfig({
    required this.version,
    required this.randomLength,
  }) : super._();

  /// Deserialize.
  factory MessagesGetDhConfig.deserialize(Uint8List buffer) {
    // final version = _readint(buffer);
    // final randomLength = _readint(buffer);
    // final result = MessagesGetDhConfig(version: version, randomLength: randomLength,);

    // return result;

    throw Exception();
  }

  /// Version.
  final int version;

  /// Random Length.
  final int randomLength;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x26cf8950);
    buffer.writeInt(version);
    buffer.writeInt(randomLength);
  }
}

/// Messages Request Encryption.
///
/// ID: `f64daf43`.
class MessagesRequestEncryption extends TlMethod<EncryptedChatBase> {
  /// Messages Request Encryption constructor.
  const MessagesRequestEncryption({
    required this.userId,
    required this.randomId,
    required this.gA,
  }) : super._();

  /// Deserialize.
  factory MessagesRequestEncryption.deserialize(Uint8List buffer) {
    // final userId = _readInputUser(buffer);
    // final randomId = _readint(buffer);
    // final gA = _readbytes(buffer);
    // final result = MessagesRequestEncryption(userId: userId, randomId: randomId, gA: gA,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final InputUserBase userId;

  /// Random Id.
  final int randomId;

  /// G A.
  final Uint8List gA;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf64daf43);
    buffer.writeObject(userId);
    buffer.writeInt(randomId);
    buffer.writeBytes(gA);
  }
}

/// Messages Accept Encryption.
///
/// ID: `3dbc0415`.
class MessagesAcceptEncryption extends TlMethod<EncryptedChatBase> {
  /// Messages Accept Encryption constructor.
  const MessagesAcceptEncryption({
    required this.peer,
    required this.gB,
    required this.keyFingerprint,
  }) : super._();

  /// Deserialize.
  factory MessagesAcceptEncryption.deserialize(Uint8List buffer) {
    // final peer = _readInputEncryptedChat(buffer);
    // final gB = _readbytes(buffer);
    // final keyFingerprint = _readlong(buffer);
    // final result = MessagesAcceptEncryption(peer: peer, gB: gB, keyFingerprint: keyFingerprint,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputEncryptedChatBase peer;

  /// G B.
  final Uint8List gB;

  /// Key Fingerprint.
  final int keyFingerprint;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3dbc0415);
    buffer.writeObject(peer);
    buffer.writeBytes(gB);
    buffer.writeLong(keyFingerprint);
  }
}

/// Messages Discard Encryption.
///
/// ID: `f393aea0`.
class MessagesDiscardEncryption extends TlMethod<bool> {
  /// Messages Discard Encryption constructor.
  const MessagesDiscardEncryption({
    required this.deleteHistory,
    required this.chatId,
  }) : super._();

  /// Deserialize.
  factory MessagesDiscardEncryption.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final deleteHistory = _readflags.0?true(buffer);
    // final chatId = _readint(buffer);
    // final result = MessagesDiscardEncryption(flags: flags, deleteHistory: deleteHistory, chatId: chatId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: deleteHistory,
    );

    return v;
  }

  /// delete_history: bit 0 of flags.0?true
  final bool deleteHistory;

  /// Chat Id.
  final int chatId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf393aea0);
    buffer.writeInt(flags);
    buffer.writeInt(chatId);
  }
}

/// Messages Set Encrypted Typing.
///
/// ID: `791451ed`.
class MessagesSetEncryptedTyping extends TlMethod<bool> {
  /// Messages Set Encrypted Typing constructor.
  const MessagesSetEncryptedTyping({
    required this.peer,
    required this.typing,
  }) : super._();

  /// Deserialize.
  factory MessagesSetEncryptedTyping.deserialize(Uint8List buffer) {
    // final peer = _readInputEncryptedChat(buffer);
    // final typing = _readBool(buffer);
    // final result = MessagesSetEncryptedTyping(peer: peer, typing: typing,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputEncryptedChatBase peer;

  /// Typing.
  final bool typing;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x791451ed);
    buffer.writeObject(peer);
    buffer.writeBool(typing);
  }
}

/// Messages Read Encrypted History.
///
/// ID: `7f4b690a`.
class MessagesReadEncryptedHistory extends TlMethod<bool> {
  /// Messages Read Encrypted History constructor.
  const MessagesReadEncryptedHistory({
    required this.peer,
    required this.maxDate,
  }) : super._();

  /// Deserialize.
  factory MessagesReadEncryptedHistory.deserialize(Uint8List buffer) {
    // final peer = _readInputEncryptedChat(buffer);
    // final maxDate = _readint(buffer);
    // final result = MessagesReadEncryptedHistory(peer: peer, maxDate: maxDate,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputEncryptedChatBase peer;

  /// Max Date.
  final DateTime maxDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7f4b690a);
    buffer.writeObject(peer);
    buffer.writeDateTime(maxDate);
  }
}

/// Messages Send Encrypted.
///
/// ID: `44fa7a15`.
class MessagesSendEncrypted extends TlMethod<MessagesSentEncryptedMessageBase> {
  /// Messages Send Encrypted constructor.
  const MessagesSendEncrypted({
    required this.silent,
    required this.peer,
    required this.randomId,
    required this.data,
  }) : super._();

  /// Deserialize.
  factory MessagesSendEncrypted.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final silent = _readflags.0?true(buffer);
    // final peer = _readInputEncryptedChat(buffer);
    // final randomId = _readlong(buffer);
    // final data = _readbytes(buffer);
    // final result = MessagesSendEncrypted(flags: flags, silent: silent, peer: peer, randomId: randomId, data: data,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: silent,
    );

    return v;
  }

  /// silent: bit 0 of flags.0?true
  final bool silent;

  /// Peer.
  final InputEncryptedChatBase peer;

  /// Random Id.
  final int randomId;

  /// Data.
  final Uint8List data;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x44fa7a15);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeLong(randomId);
    buffer.writeBytes(data);
  }
}

/// Messages Send Encrypted File.
///
/// ID: `5559481d`.
class MessagesSendEncryptedFile
    extends TlMethod<MessagesSentEncryptedMessageBase> {
  /// Messages Send Encrypted File constructor.
  const MessagesSendEncryptedFile({
    required this.silent,
    required this.peer,
    required this.randomId,
    required this.data,
    required this.file,
  }) : super._();

  /// Deserialize.
  factory MessagesSendEncryptedFile.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final silent = _readflags.0?true(buffer);
    // final peer = _readInputEncryptedChat(buffer);
    // final randomId = _readlong(buffer);
    // final data = _readbytes(buffer);
    // final file = _readInputEncryptedFile(buffer);
    // final result = MessagesSendEncryptedFile(flags: flags, silent: silent, peer: peer, randomId: randomId, data: data, file: file,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: silent,
    );

    return v;
  }

  /// silent: bit 0 of flags.0?true
  final bool silent;

  /// Peer.
  final InputEncryptedChatBase peer;

  /// Random Id.
  final int randomId;

  /// Data.
  final Uint8List data;

  /// File.
  final InputEncryptedFileBase file;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5559481d);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeLong(randomId);
    buffer.writeBytes(data);
    buffer.writeObject(file);
  }
}

/// Messages Send Encrypted Service.
///
/// ID: `32d439a4`.
class MessagesSendEncryptedService
    extends TlMethod<MessagesSentEncryptedMessageBase> {
  /// Messages Send Encrypted Service constructor.
  const MessagesSendEncryptedService({
    required this.peer,
    required this.randomId,
    required this.data,
  }) : super._();

  /// Deserialize.
  factory MessagesSendEncryptedService.deserialize(Uint8List buffer) {
    // final peer = _readInputEncryptedChat(buffer);
    // final randomId = _readlong(buffer);
    // final data = _readbytes(buffer);
    // final result = MessagesSendEncryptedService(peer: peer, randomId: randomId, data: data,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputEncryptedChatBase peer;

  /// Random Id.
  final int randomId;

  /// Data.
  final Uint8List data;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x32d439a4);
    buffer.writeObject(peer);
    buffer.writeLong(randomId);
    buffer.writeBytes(data);
  }
}

/// Messages Received Queue.
///
/// ID: `55a5bb66`.
class MessagesReceivedQueue extends TlMethod<List<int>> {
  /// Messages Received Queue constructor.
  const MessagesReceivedQueue({
    required this.maxQts,
  }) : super._();

  /// Deserialize.
  factory MessagesReceivedQueue.deserialize(Uint8List buffer) {
    // final maxQts = _readint(buffer);
    // final result = MessagesReceivedQueue(maxQts: maxQts,);

    // return result;

    throw Exception();
  }

  /// Max Qts.
  final int maxQts;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x55a5bb66);
    buffer.writeInt(maxQts);
  }
}

/// Messages Report Encrypted Spam.
///
/// ID: `4b0c8c0f`.
class MessagesReportEncryptedSpam extends TlMethod<bool> {
  /// Messages Report Encrypted Spam constructor.
  const MessagesReportEncryptedSpam({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory MessagesReportEncryptedSpam.deserialize(Uint8List buffer) {
    // final peer = _readInputEncryptedChat(buffer);
    // final result = MessagesReportEncryptedSpam(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputEncryptedChatBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4b0c8c0f);
    buffer.writeObject(peer);
  }
}

/// Upload Save Big File Part.
///
/// ID: `de7b673d`.
class UploadSaveBigFilePart extends TlMethod<bool> {
  /// Upload Save Big File Part constructor.
  const UploadSaveBigFilePart({
    required this.fileId,
    required this.filePart,
    required this.fileTotalParts,
    required this.bytes,
  }) : super._();

  /// Deserialize.
  factory UploadSaveBigFilePart.deserialize(Uint8List buffer) {
    // final fileId = _readlong(buffer);
    // final filePart = _readint(buffer);
    // final fileTotalParts = _readint(buffer);
    // final bytes = _readbytes(buffer);
    // final result = UploadSaveBigFilePart(fileId: fileId, filePart: filePart, fileTotalParts: fileTotalParts, bytes: bytes,);

    // return result;

    throw Exception();
  }

  /// File Id.
  final int fileId;

  /// File Part.
  final int filePart;

  /// File Total Parts.
  final int fileTotalParts;

  /// Bytes.
  final Uint8List bytes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xde7b673d);
    buffer.writeLong(fileId);
    buffer.writeInt(filePart);
    buffer.writeInt(fileTotalParts);
    buffer.writeBytes(bytes);
  }
}

/// Init Connection.
///
/// ID: `c1cd5ea9`.
class InitConnection<X> extends TlMethod<X> {
  /// Init Connection constructor.
  const InitConnection({
    required this.apiId,
    required this.deviceModel,
    required this.systemVersion,
    required this.appVersion,
    required this.systemLangCode,
    required this.langPack,
    required this.langCode,
    this.proxy,
    this.params,
    required this.query,
  }) : super._();

  /// Deserialize.
  factory InitConnection.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final apiId = _readint(buffer);
    // final deviceModel = _readstring(buffer);
    // final systemVersion = _readstring(buffer);
    // final appVersion = _readstring(buffer);
    // final systemLangCode = _readstring(buffer);
    // final langPack = _readstring(buffer);
    // final langCode = _readstring(buffer);
    // final proxy = _readflags.0?InputClientProxy(buffer);
    // final params = _readflags.1?JSONValue(buffer);
    // final query = _read!X(buffer);
    // final result = InitConnection(flags: flags, apiId: apiId, deviceModel: deviceModel, systemVersion: systemVersion, appVersion: appVersion, systemLangCode: systemLangCode, langPack: langPack, langCode: langCode, proxy: proxy, params: params, query: query,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: proxy != null,
      b01: params != null,
    );

    return v;
  }

  /// Api Id.
  final int apiId;

  /// Device Model.
  final String deviceModel;

  /// System Version.
  final String systemVersion;

  /// App Version.
  final String appVersion;

  /// System Lang Code.
  final String systemLangCode;

  /// Lang Pack.
  final String langPack;

  /// Lang Code.
  final String langCode;

  /// Proxy.
  final InputClientProxyBase? proxy;

  /// Params.
  final JSONValueBase? params;

  /// Query.
  final TlMethod<X> query;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc1cd5ea9);
    buffer.writeInt(flags);
    buffer.writeInt(apiId);
    buffer.writeString(deviceModel);
    buffer.writeString(systemVersion);
    buffer.writeString(appVersion);
    buffer.writeString(systemLangCode);
    buffer.writeString(langPack);
    buffer.writeString(langCode);
    final localProxyCopy = proxy;
    if (localProxyCopy != null) {
      buffer.writeObject(localProxyCopy);
    }
    final localParamsCopy = params;
    if (localParamsCopy != null) {
      buffer.writeObject(localParamsCopy);
    }
    buffer.writeObject(query);
  }
}

/// Help Get Support.
///
/// ID: `9cdf08cd`.
class HelpGetSupport extends TlMethod<HelpSupportBase> {
  /// Help Get Support constructor.
  const HelpGetSupport() : super._();

  /// Deserialize.
  factory HelpGetSupport.deserialize(Uint8List buffer) {
    // final result = HelpGetSupport();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9cdf08cd);
  }
}

/// Messages Read Message Contents.
///
/// ID: `36a73f77`.
class MessagesReadMessageContents
    extends TlMethod<MessagesAffectedMessagesBase> {
  /// Messages Read Message Contents constructor.
  const MessagesReadMessageContents({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesReadMessageContents.deserialize(Uint8List buffer) {
    // final id = _readVector<int>(buffer);
    // final result = MessagesReadMessageContents(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x36a73f77);
    buffer.writeVectorInt(id);
  }
}

/// Account Check Username.
///
/// ID: `2714d86c`.
class AccountCheckUsername extends TlMethod<bool> {
  /// Account Check Username constructor.
  const AccountCheckUsername({
    required this.username,
  }) : super._();

  /// Deserialize.
  factory AccountCheckUsername.deserialize(Uint8List buffer) {
    // final username = _readstring(buffer);
    // final result = AccountCheckUsername(username: username,);

    // return result;

    throw Exception();
  }

  /// Username.
  final String username;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2714d86c);
    buffer.writeString(username);
  }
}

/// Account Update Username.
///
/// ID: `3e0bdd7c`.
class AccountUpdateUsername extends TlMethod<UserBase> {
  /// Account Update Username constructor.
  const AccountUpdateUsername({
    required this.username,
  }) : super._();

  /// Deserialize.
  factory AccountUpdateUsername.deserialize(Uint8List buffer) {
    // final username = _readstring(buffer);
    // final result = AccountUpdateUsername(username: username,);

    // return result;

    throw Exception();
  }

  /// Username.
  final String username;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3e0bdd7c);
    buffer.writeString(username);
  }
}

/// Contacts Search.
///
/// ID: `11f812d8`.
class ContactsSearch extends TlMethod<ContactsFoundBase> {
  /// Contacts Search constructor.
  const ContactsSearch({
    required this.q,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory ContactsSearch.deserialize(Uint8List buffer) {
    // final q = _readstring(buffer);
    // final limit = _readint(buffer);
    // final result = ContactsSearch(q: q, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Q.
  final String q;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x11f812d8);
    buffer.writeString(q);
    buffer.writeInt(limit);
  }
}

/// Account Get Privacy.
///
/// ID: `dadbc950`.
class AccountGetPrivacy extends TlMethod<AccountPrivacyRulesBase> {
  /// Account Get Privacy constructor.
  const AccountGetPrivacy({
    required this.key,
  }) : super._();

  /// Deserialize.
  factory AccountGetPrivacy.deserialize(Uint8List buffer) {
    // final key = _readInputPrivacyKey(buffer);
    // final result = AccountGetPrivacy(key: key,);

    // return result;

    throw Exception();
  }

  /// Key.
  final InputPrivacyKeyBase key;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdadbc950);
    buffer.writeObject(key);
  }
}

/// Account Set Privacy.
///
/// ID: `c9f81ce8`.
class AccountSetPrivacy extends TlMethod<AccountPrivacyRulesBase> {
  /// Account Set Privacy constructor.
  const AccountSetPrivacy({
    required this.key,
    required this.rules,
  }) : super._();

  /// Deserialize.
  factory AccountSetPrivacy.deserialize(Uint8List buffer) {
    // final key = _readInputPrivacyKey(buffer);
    // final rules = _readVector<InputPrivacyRule>(buffer);
    // final result = AccountSetPrivacy(key: key, rules: rules,);

    // return result;

    throw Exception();
  }

  /// Key.
  final InputPrivacyKeyBase key;

  /// Rules.
  final List<InputPrivacyRuleBase> rules;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc9f81ce8);
    buffer.writeObject(key);
    buffer.writeVectorObject(rules);
  }
}

/// Account Delete Account.
///
/// ID: `a2c0cf74`.
class AccountDeleteAccount extends TlMethod<bool> {
  /// Account Delete Account constructor.
  const AccountDeleteAccount({
    required this.reason,
    this.password,
  }) : super._();

  /// Deserialize.
  factory AccountDeleteAccount.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final reason = _readstring(buffer);
    // final password = _readflags.0?InputCheckPasswordSRP(buffer);
    // final result = AccountDeleteAccount(flags: flags, reason: reason, password: password,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: password != null,
    );

    return v;
  }

  /// Reason.
  final String reason;

  /// Password.
  final InputCheckPasswordSRPBase? password;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa2c0cf74);
    buffer.writeInt(flags);
    buffer.writeString(reason);
    final localPasswordCopy = password;
    if (localPasswordCopy != null) {
      buffer.writeObject(localPasswordCopy);
    }
  }
}

/// Account Get Account T T L.
///
/// ID: `08fc711d`.
class AccountGetAccountTTL extends TlMethod<AccountDaysTTLBase> {
  /// Account Get Account T T L constructor.
  const AccountGetAccountTTL() : super._();

  /// Deserialize.
  factory AccountGetAccountTTL.deserialize(Uint8List buffer) {
    // final result = AccountGetAccountTTL();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x08fc711d);
  }
}

/// Account Set Account T T L.
///
/// ID: `2442485e`.
class AccountSetAccountTTL extends TlMethod<bool> {
  /// Account Set Account T T L constructor.
  const AccountSetAccountTTL({
    required this.ttl,
  }) : super._();

  /// Deserialize.
  factory AccountSetAccountTTL.deserialize(Uint8List buffer) {
    // final ttl = _readAccountDaysTTL(buffer);
    // final result = AccountSetAccountTTL(ttl: ttl,);

    // return result;

    throw Exception();
  }

  /// Ttl.
  final AccountDaysTTLBase ttl;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2442485e);
    buffer.writeObject(ttl);
  }
}

/// Invoke With Layer.
///
/// ID: `da9b0d0d`.
class InvokeWithLayer<X> extends TlMethod<X> {
  /// Invoke With Layer constructor.
  const InvokeWithLayer({
    required this.layer,
    required this.query,
  }) : super._();

  /// Deserialize.
  factory InvokeWithLayer.deserialize(Uint8List buffer) {
    // final layer = _readint(buffer);
    // final query = _read!X(buffer);
    // final result = InvokeWithLayer(layer: layer, query: query,);

    // return result;

    throw Exception();
  }

  /// Layer.
  final int layer;

  /// Query.
  final TlMethod<X> query;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xda9b0d0d);
    buffer.writeInt(layer);
    buffer.writeObject(query);
  }
}

/// Contacts Resolve Username.
///
/// ID: `f93ccba3`.
class ContactsResolveUsername extends TlMethod<ContactsResolvedPeerBase> {
  /// Contacts Resolve Username constructor.
  const ContactsResolveUsername({
    required this.username,
  }) : super._();

  /// Deserialize.
  factory ContactsResolveUsername.deserialize(Uint8List buffer) {
    // final username = _readstring(buffer);
    // final result = ContactsResolveUsername(username: username,);

    // return result;

    throw Exception();
  }

  /// Username.
  final String username;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf93ccba3);
    buffer.writeString(username);
  }
}

/// Account Send Change Phone Code.
///
/// ID: `82574ae5`.
class AccountSendChangePhoneCode extends TlMethod<AuthSentCodeBase> {
  /// Account Send Change Phone Code constructor.
  const AccountSendChangePhoneCode({
    required this.phoneNumber,
    required this.settings,
  }) : super._();

  /// Deserialize.
  factory AccountSendChangePhoneCode.deserialize(Uint8List buffer) {
    // final phoneNumber = _readstring(buffer);
    // final settings = _readCodeSettings(buffer);
    // final result = AccountSendChangePhoneCode(phoneNumber: phoneNumber, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Phone Number.
  final String phoneNumber;

  /// Settings.
  final CodeSettingsBase settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x82574ae5);
    buffer.writeString(phoneNumber);
    buffer.writeObject(settings);
  }
}

/// Account Change Phone.
///
/// ID: `70c32edb`.
class AccountChangePhone extends TlMethod<UserBase> {
  /// Account Change Phone constructor.
  const AccountChangePhone({
    required this.phoneNumber,
    required this.phoneCodeHash,
    required this.phoneCode,
  }) : super._();

  /// Deserialize.
  factory AccountChangePhone.deserialize(Uint8List buffer) {
    // final phoneNumber = _readstring(buffer);
    // final phoneCodeHash = _readstring(buffer);
    // final phoneCode = _readstring(buffer);
    // final result = AccountChangePhone(phoneNumber: phoneNumber, phoneCodeHash: phoneCodeHash, phoneCode: phoneCode,);

    // return result;

    throw Exception();
  }

  /// Phone Number.
  final String phoneNumber;

  /// Phone Code Hash.
  final String phoneCodeHash;

  /// Phone Code.
  final String phoneCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x70c32edb);
    buffer.writeString(phoneNumber);
    buffer.writeString(phoneCodeHash);
    buffer.writeString(phoneCode);
  }
}

/// Messages Get Stickers.
///
/// ID: `d5a5d3a1`.
class MessagesGetStickers extends TlMethod<MessagesStickersBase> {
  /// Messages Get Stickers constructor.
  const MessagesGetStickers({
    required this.emoticon,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetStickers.deserialize(Uint8List buffer) {
    // final emoticon = _readstring(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesGetStickers(emoticon: emoticon, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Emoticon.
  final String emoticon;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd5a5d3a1);
    buffer.writeString(emoticon);
    buffer.writeLong(hash);
  }
}

/// Messages Get All Stickers.
///
/// ID: `b8a0a1a8`.
class MessagesGetAllStickers extends TlMethod<MessagesAllStickersBase> {
  /// Messages Get All Stickers constructor.
  const MessagesGetAllStickers({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetAllStickers.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = MessagesGetAllStickers(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb8a0a1a8);
    buffer.writeLong(hash);
  }
}

/// Account Update Device Locked.
///
/// ID: `38df3532`.
class AccountUpdateDeviceLocked extends TlMethod<bool> {
  /// Account Update Device Locked constructor.
  const AccountUpdateDeviceLocked({
    required this.period,
  }) : super._();

  /// Deserialize.
  factory AccountUpdateDeviceLocked.deserialize(Uint8List buffer) {
    // final period = _readint(buffer);
    // final result = AccountUpdateDeviceLocked(period: period,);

    // return result;

    throw Exception();
  }

  /// Period.
  final int period;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x38df3532);
    buffer.writeInt(period);
  }
}

/// Auth Import Bot Authorization.
///
/// ID: `67a3ff2c`.
class AuthImportBotAuthorization extends TlMethod<AuthAuthorizationBase> {
  /// Auth Import Bot Authorization constructor.
  const AuthImportBotAuthorization({
    required this.flags,
    required this.apiId,
    required this.apiHash,
    required this.botAuthToken,
  }) : super._();

  /// Deserialize.
  factory AuthImportBotAuthorization.deserialize(Uint8List buffer) {
    // final flags = _readint(buffer);
    // final apiId = _readint(buffer);
    // final apiHash = _readstring(buffer);
    // final botAuthToken = _readstring(buffer);
    // final result = AuthImportBotAuthorization(flags: flags, apiId: apiId, apiHash: apiHash, botAuthToken: botAuthToken,);

    // return result;

    throw Exception();
  }

  /// Flags.
  final int flags;

  /// Api Id.
  final int apiId;

  /// Api Hash.
  final String apiHash;

  /// Bot Auth Token.
  final String botAuthToken;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x67a3ff2c);
    buffer.writeInt(flags);
    buffer.writeInt(apiId);
    buffer.writeString(apiHash);
    buffer.writeString(botAuthToken);
  }
}

/// Messages Get Web Page Preview.
///
/// ID: `8b68b0cc`.
class MessagesGetWebPagePreview extends TlMethod<MessageMediaBase> {
  /// Messages Get Web Page Preview constructor.
  const MessagesGetWebPagePreview({
    required this.message,
    this.entities,
  }) : super._();

  /// Deserialize.
  factory MessagesGetWebPagePreview.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final message = _readstring(buffer);
    // final entities = _readflags.3?Vector<MessageEntity>(buffer);
    // final result = MessagesGetWebPagePreview(flags: flags, message: message, entities: entities,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: entities != null,
    );

    return v;
  }

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8b68b0cc);
    buffer.writeInt(flags);
    buffer.writeString(message);
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
  }
}

/// Account Get Authorizations.
///
/// ID: `e320c158`.
class AccountGetAuthorizations extends TlMethod<AccountAuthorizationsBase> {
  /// Account Get Authorizations constructor.
  const AccountGetAuthorizations() : super._();

  /// Deserialize.
  factory AccountGetAuthorizations.deserialize(Uint8List buffer) {
    // final result = AccountGetAuthorizations();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe320c158);
  }
}

/// Account Reset Authorization.
///
/// ID: `df77f3bc`.
class AccountResetAuthorization extends TlMethod<bool> {
  /// Account Reset Authorization constructor.
  const AccountResetAuthorization({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory AccountResetAuthorization.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = AccountResetAuthorization(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdf77f3bc);
    buffer.writeLong(hash);
  }
}

/// Account Get Password.
///
/// ID: `548a30f5`.
class AccountGetPassword extends TlMethod<AccountPasswordBase> {
  /// Account Get Password constructor.
  const AccountGetPassword() : super._();

  /// Deserialize.
  factory AccountGetPassword.deserialize(Uint8List buffer) {
    // final result = AccountGetPassword();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x548a30f5);
  }
}

/// Account Get Password Settings.
///
/// ID: `9cd4eaf9`.
class AccountGetPasswordSettings extends TlMethod<AccountPasswordSettingsBase> {
  /// Account Get Password Settings constructor.
  const AccountGetPasswordSettings({
    required this.password,
  }) : super._();

  /// Deserialize.
  factory AccountGetPasswordSettings.deserialize(Uint8List buffer) {
    // final password = _readInputCheckPasswordSRP(buffer);
    // final result = AccountGetPasswordSettings(password: password,);

    // return result;

    throw Exception();
  }

  /// Password.
  final InputCheckPasswordSRPBase password;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9cd4eaf9);
    buffer.writeObject(password);
  }
}

/// Account Update Password Settings.
///
/// ID: `a59b102f`.
class AccountUpdatePasswordSettings extends TlMethod<bool> {
  /// Account Update Password Settings constructor.
  const AccountUpdatePasswordSettings({
    required this.password,
    required this.newSettings,
  }) : super._();

  /// Deserialize.
  factory AccountUpdatePasswordSettings.deserialize(Uint8List buffer) {
    // final password = _readInputCheckPasswordSRP(buffer);
    // final newSettings = _readaccount.PasswordInputSettings(buffer);
    // final result = AccountUpdatePasswordSettings(password: password, newSettings: newSettings,);

    // return result;

    throw Exception();
  }

  /// Password.
  final InputCheckPasswordSRPBase password;

  /// New Settings.
  final AccountPasswordInputSettingsBase newSettings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa59b102f);
    buffer.writeObject(password);
    buffer.writeObject(newSettings);
  }
}

/// Auth Check Password.
///
/// ID: `d18b4d16`.
class AuthCheckPassword extends TlMethod<AuthAuthorizationBase> {
  /// Auth Check Password constructor.
  const AuthCheckPassword({
    required this.password,
  }) : super._();

  /// Deserialize.
  factory AuthCheckPassword.deserialize(Uint8List buffer) {
    // final password = _readInputCheckPasswordSRP(buffer);
    // final result = AuthCheckPassword(password: password,);

    // return result;

    throw Exception();
  }

  /// Password.
  final InputCheckPasswordSRPBase password;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd18b4d16);
    buffer.writeObject(password);
  }
}

/// Auth Request Password Recovery.
///
/// ID: `d897bc66`.
class AuthRequestPasswordRecovery extends TlMethod<AuthPasswordRecoveryBase> {
  /// Auth Request Password Recovery constructor.
  const AuthRequestPasswordRecovery() : super._();

  /// Deserialize.
  factory AuthRequestPasswordRecovery.deserialize(Uint8List buffer) {
    // final result = AuthRequestPasswordRecovery();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd897bc66);
  }
}

/// Auth Recover Password.
///
/// ID: `37096c70`.
class AuthRecoverPassword extends TlMethod<AuthAuthorizationBase> {
  /// Auth Recover Password constructor.
  const AuthRecoverPassword({
    required this.code,
    this.newSettings,
  }) : super._();

  /// Deserialize.
  factory AuthRecoverPassword.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final code = _readstring(buffer);
    // final newSettings = _readflags.0?account.PasswordInputSettings(buffer);
    // final result = AuthRecoverPassword(flags: flags, code: code, newSettings: newSettings,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: newSettings != null,
    );

    return v;
  }

  /// Code.
  final String code;

  /// New Settings.
  final AccountPasswordInputSettingsBase? newSettings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x37096c70);
    buffer.writeInt(flags);
    buffer.writeString(code);
    final localNewSettingsCopy = newSettings;
    if (localNewSettingsCopy != null) {
      buffer.writeObject(localNewSettingsCopy);
    }
  }
}

/// Invoke Without Updates.
///
/// ID: `bf9459b7`.
class InvokeWithoutUpdates<X> extends TlMethod<X> {
  /// Invoke Without Updates constructor.
  const InvokeWithoutUpdates({
    required this.query,
  }) : super._();

  /// Deserialize.
  factory InvokeWithoutUpdates.deserialize(Uint8List buffer) {
    // final query = _read!X(buffer);
    // final result = InvokeWithoutUpdates(query: query,);

    // return result;

    throw Exception();
  }

  /// Query.
  final TlMethod<X> query;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbf9459b7);
    buffer.writeObject(query);
  }
}

/// Messages Export Chat Invite.
///
/// ID: `a02ce5d5`.
class MessagesExportChatInvite extends TlMethod<ExportedChatInviteBase> {
  /// Messages Export Chat Invite constructor.
  const MessagesExportChatInvite({
    required this.legacyRevokePermanent,
    required this.requestNeeded,
    required this.peer,
    this.expireDate,
    this.usageLimit,
    this.title,
  }) : super._();

  /// Deserialize.
  factory MessagesExportChatInvite.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final legacyRevokePermanent = _readflags.2?true(buffer);
    // final requestNeeded = _readflags.3?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final expireDate = _readflags.0?int(buffer);
    // final usageLimit = _readflags.1?int(buffer);
    // final title = _readflags.4?string(buffer);
    // final result = MessagesExportChatInvite(flags: flags, legacyRevokePermanent: legacyRevokePermanent, requestNeeded: requestNeeded, peer: peer, expireDate: expireDate, usageLimit: usageLimit, title: title,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: legacyRevokePermanent,
      b03: requestNeeded,
      b00: expireDate != null,
      b01: usageLimit != null,
      b04: title != null,
    );

    return v;
  }

  /// legacy_revoke_permanent: bit 2 of flags.2?true
  final bool legacyRevokePermanent;

  /// request_needed: bit 3 of flags.3?true
  final bool requestNeeded;

  /// Peer.
  final InputPeerBase peer;

  /// Expire Date.
  final DateTime? expireDate;

  /// Usage Limit.
  final int? usageLimit;

  /// Title.
  final String? title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa02ce5d5);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localExpireDateCopy = expireDate;
    if (localExpireDateCopy != null) {
      buffer.writeDateTime(localExpireDateCopy);
    }
    final localUsageLimitCopy = usageLimit;
    if (localUsageLimitCopy != null) {
      buffer.writeInt(localUsageLimitCopy);
    }
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
  }
}

/// Messages Check Chat Invite.
///
/// ID: `3eadb1bb`.
class MessagesCheckChatInvite extends TlMethod<ChatInviteBase> {
  /// Messages Check Chat Invite constructor.
  const MessagesCheckChatInvite({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesCheckChatInvite.deserialize(Uint8List buffer) {
    // final hash = _readstring(buffer);
    // final result = MessagesCheckChatInvite(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final String hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3eadb1bb);
    buffer.writeString(hash);
  }
}

/// Messages Import Chat Invite.
///
/// ID: `6c50051c`.
class MessagesImportChatInvite extends TlMethod<UpdatesBase> {
  /// Messages Import Chat Invite constructor.
  const MessagesImportChatInvite({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesImportChatInvite.deserialize(Uint8List buffer) {
    // final hash = _readstring(buffer);
    // final result = MessagesImportChatInvite(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final String hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6c50051c);
    buffer.writeString(hash);
  }
}

/// Messages Get Sticker Set.
///
/// ID: `c8a0ec74`.
class MessagesGetStickerSet extends TlMethod<MessagesStickerSetBase> {
  /// Messages Get Sticker Set constructor.
  const MessagesGetStickerSet({
    required this.stickerset,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetStickerSet.deserialize(Uint8List buffer) {
    // final stickerset = _readInputStickerSet(buffer);
    // final hash = _readint(buffer);
    // final result = MessagesGetStickerSet(stickerset: stickerset, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Stickerset.
  final InputStickerSetBase stickerset;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc8a0ec74);
    buffer.writeObject(stickerset);
    buffer.writeInt(hash);
  }
}

/// Messages Install Sticker Set.
///
/// ID: `c78fe460`.
class MessagesInstallStickerSet
    extends TlMethod<MessagesStickerSetInstallResultBase> {
  /// Messages Install Sticker Set constructor.
  const MessagesInstallStickerSet({
    required this.stickerset,
    required this.archived,
  }) : super._();

  /// Deserialize.
  factory MessagesInstallStickerSet.deserialize(Uint8List buffer) {
    // final stickerset = _readInputStickerSet(buffer);
    // final archived = _readBool(buffer);
    // final result = MessagesInstallStickerSet(stickerset: stickerset, archived: archived,);

    // return result;

    throw Exception();
  }

  /// Stickerset.
  final InputStickerSetBase stickerset;

  /// Archived.
  final bool archived;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc78fe460);
    buffer.writeObject(stickerset);
    buffer.writeBool(archived);
  }
}

/// Messages Uninstall Sticker Set.
///
/// ID: `f96e55de`.
class MessagesUninstallStickerSet extends TlMethod<bool> {
  /// Messages Uninstall Sticker Set constructor.
  const MessagesUninstallStickerSet({
    required this.stickerset,
  }) : super._();

  /// Deserialize.
  factory MessagesUninstallStickerSet.deserialize(Uint8List buffer) {
    // final stickerset = _readInputStickerSet(buffer);
    // final result = MessagesUninstallStickerSet(stickerset: stickerset,);

    // return result;

    throw Exception();
  }

  /// Stickerset.
  final InputStickerSetBase stickerset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf96e55de);
    buffer.writeObject(stickerset);
  }
}

/// Messages Start Bot.
///
/// ID: `e6df7378`.
class MessagesStartBot extends TlMethod<UpdatesBase> {
  /// Messages Start Bot constructor.
  const MessagesStartBot({
    required this.bot,
    required this.peer,
    required this.randomId,
    required this.startParam,
  }) : super._();

  /// Deserialize.
  factory MessagesStartBot.deserialize(Uint8List buffer) {
    // final bot = _readInputUser(buffer);
    // final peer = _readInputPeer(buffer);
    // final randomId = _readlong(buffer);
    // final startParam = _readstring(buffer);
    // final result = MessagesStartBot(bot: bot, peer: peer, randomId: randomId, startParam: startParam,);

    // return result;

    throw Exception();
  }

  /// Bot.
  final InputUserBase bot;

  /// Peer.
  final InputPeerBase peer;

  /// Random Id.
  final int randomId;

  /// Start Param.
  final String startParam;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe6df7378);
    buffer.writeObject(bot);
    buffer.writeObject(peer);
    buffer.writeLong(randomId);
    buffer.writeString(startParam);
  }
}

/// Messages Get Messages Views.
///
/// ID: `5784d3e1`.
class MessagesGetMessagesViews extends TlMethod<MessagesMessageViewsBase> {
  /// Messages Get Messages Views constructor.
  const MessagesGetMessagesViews({
    required this.peer,
    required this.id,
    required this.increment,
  }) : super._();

  /// Deserialize.
  factory MessagesGetMessagesViews.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final increment = _readBool(buffer);
    // final result = MessagesGetMessagesViews(peer: peer, id: id, increment: increment,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final List<int> id;

  /// Increment.
  final bool increment;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5784d3e1);
    buffer.writeObject(peer);
    buffer.writeVectorInt(id);
    buffer.writeBool(increment);
  }
}

/// Channels Read History.
///
/// ID: `cc104937`.
class ChannelsReadHistory extends TlMethod<bool> {
  /// Channels Read History constructor.
  const ChannelsReadHistory({
    required this.channel,
    required this.maxId,
  }) : super._();

  /// Deserialize.
  factory ChannelsReadHistory.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final maxId = _readint(buffer);
    // final result = ChannelsReadHistory(channel: channel, maxId: maxId,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Max Id.
  final int maxId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcc104937);
    buffer.writeObject(channel);
    buffer.writeInt(maxId);
  }
}

/// Channels Delete Messages.
///
/// ID: `84c1fd4e`.
class ChannelsDeleteMessages extends TlMethod<MessagesAffectedMessagesBase> {
  /// Channels Delete Messages constructor.
  const ChannelsDeleteMessages({
    required this.channel,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory ChannelsDeleteMessages.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final id = _readVector<int>(buffer);
    // final result = ChannelsDeleteMessages(channel: channel, id: id,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x84c1fd4e);
    buffer.writeObject(channel);
    buffer.writeVectorInt(id);
  }
}

/// Channels Report Spam.
///
/// ID: `f44a8315`.
class ChannelsReportSpam extends TlMethod<bool> {
  /// Channels Report Spam constructor.
  const ChannelsReportSpam({
    required this.channel,
    required this.participant,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory ChannelsReportSpam.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final participant = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final result = ChannelsReportSpam(channel: channel, participant: participant, id: id,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Participant.
  final InputPeerBase participant;

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf44a8315);
    buffer.writeObject(channel);
    buffer.writeObject(participant);
    buffer.writeVectorInt(id);
  }
}

/// Channels Get Messages.
///
/// ID: `ad8c9a23`.
class ChannelsGetMessages extends TlMethod<MessagesMessagesBase> {
  /// Channels Get Messages constructor.
  const ChannelsGetMessages({
    required this.channel,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory ChannelsGetMessages.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final id = _readVector<InputMessage>(buffer);
    // final result = ChannelsGetMessages(channel: channel, id: id,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Id.
  final List<InputMessageBase> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xad8c9a23);
    buffer.writeObject(channel);
    buffer.writeVectorObject(id);
  }
}

/// Channels Get Participants.
///
/// ID: `77ced9d0`.
class ChannelsGetParticipants
    extends TlMethod<ChannelsChannelParticipantsBase> {
  /// Channels Get Participants constructor.
  const ChannelsGetParticipants({
    required this.channel,
    required this.filter,
    required this.offset,
    required this.limit,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory ChannelsGetParticipants.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final filter = _readChannelParticipantsFilter(buffer);
    // final offset = _readint(buffer);
    // final limit = _readint(buffer);
    // final hash = _readlong(buffer);
    // final result = ChannelsGetParticipants(channel: channel, filter: filter, offset: offset, limit: limit, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Filter.
  final ChannelParticipantsFilterBase filter;

  /// Offset.
  final int offset;

  /// Limit.
  final int limit;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x77ced9d0);
    buffer.writeObject(channel);
    buffer.writeObject(filter);
    buffer.writeInt(offset);
    buffer.writeInt(limit);
    buffer.writeLong(hash);
  }
}

/// Channels Get Participant.
///
/// ID: `a0ab6cc6`.
class ChannelsGetParticipant extends TlMethod<ChannelsChannelParticipantBase> {
  /// Channels Get Participant constructor.
  const ChannelsGetParticipant({
    required this.channel,
    required this.participant,
  }) : super._();

  /// Deserialize.
  factory ChannelsGetParticipant.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final participant = _readInputPeer(buffer);
    // final result = ChannelsGetParticipant(channel: channel, participant: participant,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Participant.
  final InputPeerBase participant;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa0ab6cc6);
    buffer.writeObject(channel);
    buffer.writeObject(participant);
  }
}

/// Channels Get Channels.
///
/// ID: `0a7f6bbb`.
class ChannelsGetChannels extends TlMethod<MessagesChatsBase> {
  /// Channels Get Channels constructor.
  const ChannelsGetChannels({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory ChannelsGetChannels.deserialize(Uint8List buffer) {
    // final id = _readVector<InputChannel>(buffer);
    // final result = ChannelsGetChannels(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final List<InputChannelBase> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0a7f6bbb);
    buffer.writeVectorObject(id);
  }
}

/// Channels Get Full Channel.
///
/// ID: `08736a09`.
class ChannelsGetFullChannel extends TlMethod<MessagesChatFullBase> {
  /// Channels Get Full Channel constructor.
  const ChannelsGetFullChannel({
    required this.channel,
  }) : super._();

  /// Deserialize.
  factory ChannelsGetFullChannel.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final result = ChannelsGetFullChannel(channel: channel,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x08736a09);
    buffer.writeObject(channel);
  }
}

/// Channels Create Channel.
///
/// ID: `91006707`.
class ChannelsCreateChannel extends TlMethod<UpdatesBase> {
  /// Channels Create Channel constructor.
  const ChannelsCreateChannel({
    required this.broadcast,
    required this.megagroup,
    required this.forImport,
    required this.forum,
    required this.title,
    required this.about,
    this.geoPoint,
    this.address,
    this.ttlPeriod,
  }) : super._();

  /// Deserialize.
  factory ChannelsCreateChannel.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final broadcast = _readflags.0?true(buffer);
    // final megagroup = _readflags.1?true(buffer);
    // final forImport = _readflags.3?true(buffer);
    // final forum = _readflags.5?true(buffer);
    // final title = _readstring(buffer);
    // final about = _readstring(buffer);
    // final geoPoint = _readflags.2?InputGeoPoint(buffer);
    // final address = _readflags.2?string(buffer);
    // final ttlPeriod = _readflags.4?int(buffer);
    // final result = ChannelsCreateChannel(flags: flags, broadcast: broadcast, megagroup: megagroup, forImport: forImport, forum: forum, title: title, about: about, geoPoint: geoPoint, address: address, ttlPeriod: ttlPeriod,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: broadcast,
      b01: megagroup,
      b03: forImport,
      b05: forum,
      b02: geoPoint != null || address != null,
      b04: ttlPeriod != null,
    );

    return v;
  }

  /// broadcast: bit 0 of flags.0?true
  final bool broadcast;

  /// megagroup: bit 1 of flags.1?true
  final bool megagroup;

  /// for_import: bit 3 of flags.3?true
  final bool forImport;

  /// forum: bit 5 of flags.5?true
  final bool forum;

  /// Title.
  final String title;

  /// About.
  final String about;

  /// Geo Point.
  final InputGeoPointBase? geoPoint;

  /// Address.
  final String? address;

  /// Ttl Period.
  final int? ttlPeriod;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x91006707);
    buffer.writeInt(flags);
    buffer.writeString(title);
    buffer.writeString(about);
    final localGeoPointCopy = geoPoint;
    if (localGeoPointCopy != null) {
      buffer.writeObject(localGeoPointCopy);
    }
    final localAddressCopy = address;
    if (localAddressCopy != null) {
      buffer.writeString(localAddressCopy);
    }
    final localTtlPeriodCopy = ttlPeriod;
    if (localTtlPeriodCopy != null) {
      buffer.writeInt(localTtlPeriodCopy);
    }
  }
}

/// Channels Edit Admin.
///
/// ID: `d33c8902`.
class ChannelsEditAdmin extends TlMethod<UpdatesBase> {
  /// Channels Edit Admin constructor.
  const ChannelsEditAdmin({
    required this.channel,
    required this.userId,
    required this.adminRights,
    required this.rank,
  }) : super._();

  /// Deserialize.
  factory ChannelsEditAdmin.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final userId = _readInputUser(buffer);
    // final adminRights = _readChatAdminRights(buffer);
    // final rank = _readstring(buffer);
    // final result = ChannelsEditAdmin(channel: channel, userId: userId, adminRights: adminRights, rank: rank,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// User Id.
  final InputUserBase userId;

  /// Admin Rights.
  final ChatAdminRightsBase adminRights;

  /// Rank.
  final String rank;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd33c8902);
    buffer.writeObject(channel);
    buffer.writeObject(userId);
    buffer.writeObject(adminRights);
    buffer.writeString(rank);
  }
}

/// Channels Edit Title.
///
/// ID: `566decd0`.
class ChannelsEditTitle extends TlMethod<UpdatesBase> {
  /// Channels Edit Title constructor.
  const ChannelsEditTitle({
    required this.channel,
    required this.title,
  }) : super._();

  /// Deserialize.
  factory ChannelsEditTitle.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final title = _readstring(buffer);
    // final result = ChannelsEditTitle(channel: channel, title: title,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Title.
  final String title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x566decd0);
    buffer.writeObject(channel);
    buffer.writeString(title);
  }
}

/// Channels Edit Photo.
///
/// ID: `f12e57c9`.
class ChannelsEditPhoto extends TlMethod<UpdatesBase> {
  /// Channels Edit Photo constructor.
  const ChannelsEditPhoto({
    required this.channel,
    required this.photo,
  }) : super._();

  /// Deserialize.
  factory ChannelsEditPhoto.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final photo = _readInputChatPhoto(buffer);
    // final result = ChannelsEditPhoto(channel: channel, photo: photo,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Photo.
  final InputChatPhotoBase photo;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf12e57c9);
    buffer.writeObject(channel);
    buffer.writeObject(photo);
  }
}

/// Channels Check Username.
///
/// ID: `10e6bd2c`.
class ChannelsCheckUsername extends TlMethod<bool> {
  /// Channels Check Username constructor.
  const ChannelsCheckUsername({
    required this.channel,
    required this.username,
  }) : super._();

  /// Deserialize.
  factory ChannelsCheckUsername.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final username = _readstring(buffer);
    // final result = ChannelsCheckUsername(channel: channel, username: username,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Username.
  final String username;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x10e6bd2c);
    buffer.writeObject(channel);
    buffer.writeString(username);
  }
}

/// Channels Update Username.
///
/// ID: `3514b3de`.
class ChannelsUpdateUsername extends TlMethod<bool> {
  /// Channels Update Username constructor.
  const ChannelsUpdateUsername({
    required this.channel,
    required this.username,
  }) : super._();

  /// Deserialize.
  factory ChannelsUpdateUsername.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final username = _readstring(buffer);
    // final result = ChannelsUpdateUsername(channel: channel, username: username,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Username.
  final String username;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3514b3de);
    buffer.writeObject(channel);
    buffer.writeString(username);
  }
}

/// Channels Join Channel.
///
/// ID: `24b524c5`.
class ChannelsJoinChannel extends TlMethod<UpdatesBase> {
  /// Channels Join Channel constructor.
  const ChannelsJoinChannel({
    required this.channel,
  }) : super._();

  /// Deserialize.
  factory ChannelsJoinChannel.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final result = ChannelsJoinChannel(channel: channel,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x24b524c5);
    buffer.writeObject(channel);
  }
}

/// Channels Leave Channel.
///
/// ID: `f836aa95`.
class ChannelsLeaveChannel extends TlMethod<UpdatesBase> {
  /// Channels Leave Channel constructor.
  const ChannelsLeaveChannel({
    required this.channel,
  }) : super._();

  /// Deserialize.
  factory ChannelsLeaveChannel.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final result = ChannelsLeaveChannel(channel: channel,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf836aa95);
    buffer.writeObject(channel);
  }
}

/// Channels Invite To Channel.
///
/// ID: `199f3a6c`.
class ChannelsInviteToChannel extends TlMethod<UpdatesBase> {
  /// Channels Invite To Channel constructor.
  const ChannelsInviteToChannel({
    required this.channel,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory ChannelsInviteToChannel.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final users = _readVector<InputUser>(buffer);
    // final result = ChannelsInviteToChannel(channel: channel, users: users,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Users.
  final List<InputUserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x199f3a6c);
    buffer.writeObject(channel);
    buffer.writeVectorObject(users);
  }
}

/// Channels Delete Channel.
///
/// ID: `c0111fe3`.
class ChannelsDeleteChannel extends TlMethod<UpdatesBase> {
  /// Channels Delete Channel constructor.
  const ChannelsDeleteChannel({
    required this.channel,
  }) : super._();

  /// Deserialize.
  factory ChannelsDeleteChannel.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final result = ChannelsDeleteChannel(channel: channel,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc0111fe3);
    buffer.writeObject(channel);
  }
}

/// Updates Get Channel Difference.
///
/// ID: `03173d78`.
class UpdatesGetChannelDifference
    extends TlMethod<UpdatesChannelDifferenceBase> {
  /// Updates Get Channel Difference constructor.
  const UpdatesGetChannelDifference({
    required this.force,
    required this.channel,
    required this.filter,
    required this.pts,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory UpdatesGetChannelDifference.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final force = _readflags.0?true(buffer);
    // final channel = _readInputChannel(buffer);
    // final filter = _readChannelMessagesFilter(buffer);
    // final pts = _readint(buffer);
    // final limit = _readint(buffer);
    // final result = UpdatesGetChannelDifference(flags: flags, force: force, channel: channel, filter: filter, pts: pts, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: force,
    );

    return v;
  }

  /// force: bit 0 of flags.0?true
  final bool force;

  /// Channel.
  final InputChannelBase channel;

  /// Filter.
  final ChannelMessagesFilterBase filter;

  /// Pts.
  final int pts;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x03173d78);
    buffer.writeInt(flags);
    buffer.writeObject(channel);
    buffer.writeObject(filter);
    buffer.writeInt(pts);
    buffer.writeInt(limit);
  }
}

/// Messages Edit Chat Admin.
///
/// ID: `a85bd1c2`.
class MessagesEditChatAdmin extends TlMethod<bool> {
  /// Messages Edit Chat Admin constructor.
  const MessagesEditChatAdmin({
    required this.chatId,
    required this.userId,
    required this.isAdmin,
  }) : super._();

  /// Deserialize.
  factory MessagesEditChatAdmin.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final userId = _readInputUser(buffer);
    // final isAdmin = _readBool(buffer);
    // final result = MessagesEditChatAdmin(chatId: chatId, userId: userId, isAdmin: isAdmin,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// User Id.
  final InputUserBase userId;

  /// Is Admin.
  final bool isAdmin;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa85bd1c2);
    buffer.writeLong(chatId);
    buffer.writeObject(userId);
    buffer.writeBool(isAdmin);
  }
}

/// Messages Migrate Chat.
///
/// ID: `a2875319`.
class MessagesMigrateChat extends TlMethod<UpdatesBase> {
  /// Messages Migrate Chat constructor.
  const MessagesMigrateChat({
    required this.chatId,
  }) : super._();

  /// Deserialize.
  factory MessagesMigrateChat.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final result = MessagesMigrateChat(chatId: chatId,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa2875319);
    buffer.writeLong(chatId);
  }
}

/// Messages Search Global.
///
/// ID: `4bc6589a`.
class MessagesSearchGlobal extends TlMethod<MessagesMessagesBase> {
  /// Messages Search Global constructor.
  const MessagesSearchGlobal({
    this.folderId,
    required this.q,
    required this.filter,
    required this.minDate,
    required this.maxDate,
    required this.offsetRate,
    required this.offsetPeer,
    required this.offsetId,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory MessagesSearchGlobal.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final folderId = _readflags.0?int(buffer);
    // final q = _readstring(buffer);
    // final filter = _readMessagesFilter(buffer);
    // final minDate = _readint(buffer);
    // final maxDate = _readint(buffer);
    // final offsetRate = _readint(buffer);
    // final offsetPeer = _readInputPeer(buffer);
    // final offsetId = _readint(buffer);
    // final limit = _readint(buffer);
    // final result = MessagesSearchGlobal(flags: flags, folderId: folderId, q: q, filter: filter, minDate: minDate, maxDate: maxDate, offsetRate: offsetRate, offsetPeer: offsetPeer, offsetId: offsetId, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: folderId != null,
    );

    return v;
  }

  /// Folder Id.
  final int? folderId;

  /// Q.
  final String q;

  /// Filter.
  final MessagesFilterBase filter;

  /// Min Date.
  final DateTime minDate;

  /// Max Date.
  final DateTime maxDate;

  /// Offset Rate.
  final int offsetRate;

  /// Offset Peer.
  final InputPeerBase offsetPeer;

  /// Offset Id.
  final int offsetId;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4bc6589a);
    buffer.writeInt(flags);
    final localFolderIdCopy = folderId;
    if (localFolderIdCopy != null) {
      buffer.writeInt(localFolderIdCopy);
    }
    buffer.writeString(q);
    buffer.writeObject(filter);
    buffer.writeDateTime(minDate);
    buffer.writeDateTime(maxDate);
    buffer.writeInt(offsetRate);
    buffer.writeObject(offsetPeer);
    buffer.writeInt(offsetId);
    buffer.writeInt(limit);
  }
}

/// Messages Reorder Sticker Sets.
///
/// ID: `78337739`.
class MessagesReorderStickerSets extends TlMethod<bool> {
  /// Messages Reorder Sticker Sets constructor.
  const MessagesReorderStickerSets({
    required this.masks,
    required this.emojis,
    required this.order,
  }) : super._();

  /// Deserialize.
  factory MessagesReorderStickerSets.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final masks = _readflags.0?true(buffer);
    // final emojis = _readflags.1?true(buffer);
    // final order = _readVector<long>(buffer);
    // final result = MessagesReorderStickerSets(flags: flags, masks: masks, emojis: emojis, order: order,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: masks,
      b01: emojis,
    );

    return v;
  }

  /// masks: bit 0 of flags.0?true
  final bool masks;

  /// emojis: bit 1 of flags.1?true
  final bool emojis;

  /// Order.
  final List<int> order;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x78337739);
    buffer.writeInt(flags);
    buffer.writeVectorLong(order);
  }
}

/// Messages Get Document By Hash.
///
/// ID: `b1f2061f`.
class MessagesGetDocumentByHash extends TlMethod<DocumentBase> {
  /// Messages Get Document By Hash constructor.
  const MessagesGetDocumentByHash({
    required this.sha256,
    required this.size,
    required this.mimeType,
  }) : super._();

  /// Deserialize.
  factory MessagesGetDocumentByHash.deserialize(Uint8List buffer) {
    // final sha256 = _readbytes(buffer);
    // final size = _readlong(buffer);
    // final mimeType = _readstring(buffer);
    // final result = MessagesGetDocumentByHash(sha256: sha256, size: size, mimeType: mimeType,);

    // return result;

    throw Exception();
  }

  /// Sha256.
  final Uint8List sha256;

  /// Size.
  final int size;

  /// Mime Type.
  final String mimeType;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb1f2061f);
    buffer.writeBytes(sha256);
    buffer.writeLong(size);
    buffer.writeString(mimeType);
  }
}

/// Messages Get Saved Gifs.
///
/// ID: `5cf09635`.
class MessagesGetSavedGifs extends TlMethod<MessagesSavedGifsBase> {
  /// Messages Get Saved Gifs constructor.
  const MessagesGetSavedGifs({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetSavedGifs.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = MessagesGetSavedGifs(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5cf09635);
    buffer.writeLong(hash);
  }
}

/// Messages Save Gif.
///
/// ID: `327a30cb`.
class MessagesSaveGif extends TlMethod<bool> {
  /// Messages Save Gif constructor.
  const MessagesSaveGif({
    required this.id,
    required this.unsave,
  }) : super._();

  /// Deserialize.
  factory MessagesSaveGif.deserialize(Uint8List buffer) {
    // final id = _readInputDocument(buffer);
    // final unsave = _readBool(buffer);
    // final result = MessagesSaveGif(id: id, unsave: unsave,);

    // return result;

    throw Exception();
  }

  /// Id.
  final InputDocumentBase id;

  /// Unsave.
  final bool unsave;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x327a30cb);
    buffer.writeObject(id);
    buffer.writeBool(unsave);
  }
}

/// Messages Get Inline Bot Results.
///
/// ID: `514e999d`.
class MessagesGetInlineBotResults extends TlMethod<MessagesBotResultsBase> {
  /// Messages Get Inline Bot Results constructor.
  const MessagesGetInlineBotResults({
    required this.bot,
    required this.peer,
    this.geoPoint,
    required this.query,
    required this.offset,
  }) : super._();

  /// Deserialize.
  factory MessagesGetInlineBotResults.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final bot = _readInputUser(buffer);
    // final peer = _readInputPeer(buffer);
    // final geoPoint = _readflags.0?InputGeoPoint(buffer);
    // final query = _readstring(buffer);
    // final offset = _readstring(buffer);
    // final result = MessagesGetInlineBotResults(flags: flags, bot: bot, peer: peer, geoPoint: geoPoint, query: query, offset: offset,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: geoPoint != null,
    );

    return v;
  }

  /// Bot.
  final InputUserBase bot;

  /// Peer.
  final InputPeerBase peer;

  /// Geo Point.
  final InputGeoPointBase? geoPoint;

  /// Query.
  final String query;

  /// Offset.
  final String offset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x514e999d);
    buffer.writeInt(flags);
    buffer.writeObject(bot);
    buffer.writeObject(peer);
    final localGeoPointCopy = geoPoint;
    if (localGeoPointCopy != null) {
      buffer.writeObject(localGeoPointCopy);
    }
    buffer.writeString(query);
    buffer.writeString(offset);
  }
}

/// Messages Set Inline Bot Results.
///
/// ID: `bb12a419`.
class MessagesSetInlineBotResults extends TlMethod<bool> {
  /// Messages Set Inline Bot Results constructor.
  const MessagesSetInlineBotResults({
    required this.gallery,
    required this.private,
    required this.queryId,
    required this.results,
    required this.cacheTime,
    this.nextOffset,
    this.switchPm,
    this.switchWebview,
  }) : super._();

  /// Deserialize.
  factory MessagesSetInlineBotResults.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final gallery = _readflags.0?true(buffer);
    // final private = _readflags.1?true(buffer);
    // final queryId = _readlong(buffer);
    // final results = _readVector<InputBotInlineResult>(buffer);
    // final cacheTime = _readint(buffer);
    // final nextOffset = _readflags.2?string(buffer);
    // final switchPm = _readflags.3?InlineBotSwitchPM(buffer);
    // final switchWebview = _readflags.4?InlineBotWebView(buffer);
    // final result = MessagesSetInlineBotResults(flags: flags, gallery: gallery, private: private, queryId: queryId, results: results, cacheTime: cacheTime, nextOffset: nextOffset, switchPm: switchPm, switchWebview: switchWebview,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: gallery,
      b01: private,
      b02: nextOffset != null,
      b03: switchPm != null,
      b04: switchWebview != null,
    );

    return v;
  }

  /// gallery: bit 0 of flags.0?true
  final bool gallery;

  /// private: bit 1 of flags.1?true
  final bool private;

  /// Query Id.
  final int queryId;

  /// Results.
  final List<InputBotInlineResultBase> results;

  /// Cache Time.
  final int cacheTime;

  /// Next Offset.
  final String? nextOffset;

  /// Switch Pm.
  final InlineBotSwitchPMBase? switchPm;

  /// Switch Webview.
  final InlineBotWebViewBase? switchWebview;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbb12a419);
    buffer.writeInt(flags);
    buffer.writeLong(queryId);
    buffer.writeVectorObject(results);
    buffer.writeInt(cacheTime);
    final localNextOffsetCopy = nextOffset;
    if (localNextOffsetCopy != null) {
      buffer.writeString(localNextOffsetCopy);
    }
    final localSwitchPmCopy = switchPm;
    if (localSwitchPmCopy != null) {
      buffer.writeObject(localSwitchPmCopy);
    }
    final localSwitchWebviewCopy = switchWebview;
    if (localSwitchWebviewCopy != null) {
      buffer.writeObject(localSwitchWebviewCopy);
    }
  }
}

/// Messages Send Inline Bot Result.
///
/// ID: `f7bc68ba`.
class MessagesSendInlineBotResult extends TlMethod<UpdatesBase> {
  /// Messages Send Inline Bot Result constructor.
  const MessagesSendInlineBotResult({
    required this.silent,
    required this.background,
    required this.clearDraft,
    required this.hideVia,
    required this.peer,
    this.replyTo,
    required this.randomId,
    required this.queryId,
    required this.id,
    this.scheduleDate,
    this.sendAs,
  }) : super._();

  /// Deserialize.
  factory MessagesSendInlineBotResult.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final silent = _readflags.5?true(buffer);
    // final background = _readflags.6?true(buffer);
    // final clearDraft = _readflags.7?true(buffer);
    // final hideVia = _readflags.11?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final replyTo = _readflags.0?InputReplyTo(buffer);
    // final randomId = _readlong(buffer);
    // final queryId = _readlong(buffer);
    // final id = _readstring(buffer);
    // final scheduleDate = _readflags.10?int(buffer);
    // final sendAs = _readflags.13?InputPeer(buffer);
    // final result = MessagesSendInlineBotResult(flags: flags, silent: silent, background: background, clearDraft: clearDraft, hideVia: hideVia, peer: peer, replyTo: replyTo, randomId: randomId, queryId: queryId, id: id, scheduleDate: scheduleDate, sendAs: sendAs,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b05: silent,
      b06: background,
      b07: clearDraft,
      b11: hideVia,
      b00: replyTo != null,
      b10: scheduleDate != null,
      b13: sendAs != null,
    );

    return v;
  }

  /// silent: bit 5 of flags.5?true
  final bool silent;

  /// background: bit 6 of flags.6?true
  final bool background;

  /// clear_draft: bit 7 of flags.7?true
  final bool clearDraft;

  /// hide_via: bit 11 of flags.11?true
  final bool hideVia;

  /// Peer.
  final InputPeerBase peer;

  /// Reply To.
  final InputReplyToBase? replyTo;

  /// Random Id.
  final int randomId;

  /// Query Id.
  final int queryId;

  /// Id.
  final String id;

  /// Schedule Date.
  final DateTime? scheduleDate;

  /// Send As.
  final InputPeerBase? sendAs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf7bc68ba);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localReplyToCopy = replyTo;
    if (localReplyToCopy != null) {
      buffer.writeObject(localReplyToCopy);
    }
    buffer.writeLong(randomId);
    buffer.writeLong(queryId);
    buffer.writeString(id);
    final localScheduleDateCopy = scheduleDate;
    if (localScheduleDateCopy != null) {
      buffer.writeDateTime(localScheduleDateCopy);
    }
    final localSendAsCopy = sendAs;
    if (localSendAsCopy != null) {
      buffer.writeObject(localSendAsCopy);
    }
  }
}

/// Channels Export Message Link.
///
/// ID: `e63fadeb`.
class ChannelsExportMessageLink extends TlMethod<ExportedMessageLinkBase> {
  /// Channels Export Message Link constructor.
  const ChannelsExportMessageLink({
    required this.grouped,
    required this.thread,
    required this.channel,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory ChannelsExportMessageLink.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final grouped = _readflags.0?true(buffer);
    // final thread = _readflags.1?true(buffer);
    // final channel = _readInputChannel(buffer);
    // final id = _readint(buffer);
    // final result = ChannelsExportMessageLink(flags: flags, grouped: grouped, thread: thread, channel: channel, id: id,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: grouped,
      b01: thread,
    );

    return v;
  }

  /// grouped: bit 0 of flags.0?true
  final bool grouped;

  /// thread: bit 1 of flags.1?true
  final bool thread;

  /// Channel.
  final InputChannelBase channel;

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe63fadeb);
    buffer.writeInt(flags);
    buffer.writeObject(channel);
    buffer.writeInt(id);
  }
}

/// Channels Toggle Signatures.
///
/// ID: `1f69b606`.
class ChannelsToggleSignatures extends TlMethod<UpdatesBase> {
  /// Channels Toggle Signatures constructor.
  const ChannelsToggleSignatures({
    required this.channel,
    required this.enabled,
  }) : super._();

  /// Deserialize.
  factory ChannelsToggleSignatures.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final enabled = _readBool(buffer);
    // final result = ChannelsToggleSignatures(channel: channel, enabled: enabled,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Enabled.
  final bool enabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1f69b606);
    buffer.writeObject(channel);
    buffer.writeBool(enabled);
  }
}

/// Auth Resend Code.
///
/// ID: `3ef1a9bf`.
class AuthResendCode extends TlMethod<AuthSentCodeBase> {
  /// Auth Resend Code constructor.
  const AuthResendCode({
    required this.phoneNumber,
    required this.phoneCodeHash,
  }) : super._();

  /// Deserialize.
  factory AuthResendCode.deserialize(Uint8List buffer) {
    // final phoneNumber = _readstring(buffer);
    // final phoneCodeHash = _readstring(buffer);
    // final result = AuthResendCode(phoneNumber: phoneNumber, phoneCodeHash: phoneCodeHash,);

    // return result;

    throw Exception();
  }

  /// Phone Number.
  final String phoneNumber;

  /// Phone Code Hash.
  final String phoneCodeHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3ef1a9bf);
    buffer.writeString(phoneNumber);
    buffer.writeString(phoneCodeHash);
  }
}

/// Auth Cancel Code.
///
/// ID: `1f040578`.
class AuthCancelCode extends TlMethod<bool> {
  /// Auth Cancel Code constructor.
  const AuthCancelCode({
    required this.phoneNumber,
    required this.phoneCodeHash,
  }) : super._();

  /// Deserialize.
  factory AuthCancelCode.deserialize(Uint8List buffer) {
    // final phoneNumber = _readstring(buffer);
    // final phoneCodeHash = _readstring(buffer);
    // final result = AuthCancelCode(phoneNumber: phoneNumber, phoneCodeHash: phoneCodeHash,);

    // return result;

    throw Exception();
  }

  /// Phone Number.
  final String phoneNumber;

  /// Phone Code Hash.
  final String phoneCodeHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1f040578);
    buffer.writeString(phoneNumber);
    buffer.writeString(phoneCodeHash);
  }
}

/// Messages Get Message Edit Data.
///
/// ID: `fda68d36`.
class MessagesGetMessageEditData extends TlMethod<MessagesMessageEditDataBase> {
  /// Messages Get Message Edit Data constructor.
  const MessagesGetMessageEditData({
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesGetMessageEditData.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final result = MessagesGetMessageEditData(peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfda68d36);
    buffer.writeObject(peer);
    buffer.writeInt(id);
  }
}

/// Messages Edit Message.
///
/// ID: `48f71778`.
class MessagesEditMessage extends TlMethod<UpdatesBase> {
  /// Messages Edit Message constructor.
  const MessagesEditMessage({
    required this.noWebpage,
    required this.invertMedia,
    required this.peer,
    required this.id,
    this.message,
    this.media,
    this.replyMarkup,
    this.entities,
    this.scheduleDate,
  }) : super._();

  /// Deserialize.
  factory MessagesEditMessage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final noWebpage = _readflags.1?true(buffer);
    // final invertMedia = _readflags.16?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final message = _readflags.11?string(buffer);
    // final media = _readflags.14?InputMedia(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final entities = _readflags.3?Vector<MessageEntity>(buffer);
    // final scheduleDate = _readflags.15?int(buffer);
    // final result = MessagesEditMessage(flags: flags, noWebpage: noWebpage, invertMedia: invertMedia, peer: peer, id: id, message: message, media: media, replyMarkup: replyMarkup, entities: entities, scheduleDate: scheduleDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: noWebpage,
      b16: invertMedia,
      b11: message != null,
      b14: media != null,
      b02: replyMarkup != null,
      b03: entities != null,
      b15: scheduleDate != null,
    );

    return v;
  }

  /// no_webpage: bit 1 of flags.1?true
  final bool noWebpage;

  /// invert_media: bit 16 of flags.16?true
  final bool invertMedia;

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// Message.
  final String? message;

  /// Media.
  final InputMediaBase? media;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Schedule Date.
  final DateTime? scheduleDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x48f71778);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(id);
    final localMessageCopy = message;
    if (localMessageCopy != null) {
      buffer.writeString(localMessageCopy);
    }
    final localMediaCopy = media;
    if (localMediaCopy != null) {
      buffer.writeObject(localMediaCopy);
    }
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localScheduleDateCopy = scheduleDate;
    if (localScheduleDateCopy != null) {
      buffer.writeDateTime(localScheduleDateCopy);
    }
  }
}

/// Messages Edit Inline Bot Message.
///
/// ID: `83557dba`.
class MessagesEditInlineBotMessage extends TlMethod<bool> {
  /// Messages Edit Inline Bot Message constructor.
  const MessagesEditInlineBotMessage({
    required this.noWebpage,
    required this.invertMedia,
    required this.id,
    this.message,
    this.media,
    this.replyMarkup,
    this.entities,
  }) : super._();

  /// Deserialize.
  factory MessagesEditInlineBotMessage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final noWebpage = _readflags.1?true(buffer);
    // final invertMedia = _readflags.16?true(buffer);
    // final id = _readInputBotInlineMessageID(buffer);
    // final message = _readflags.11?string(buffer);
    // final media = _readflags.14?InputMedia(buffer);
    // final replyMarkup = _readflags.2?ReplyMarkup(buffer);
    // final entities = _readflags.3?Vector<MessageEntity>(buffer);
    // final result = MessagesEditInlineBotMessage(flags: flags, noWebpage: noWebpage, invertMedia: invertMedia, id: id, message: message, media: media, replyMarkup: replyMarkup, entities: entities,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: noWebpage,
      b16: invertMedia,
      b11: message != null,
      b14: media != null,
      b02: replyMarkup != null,
      b03: entities != null,
    );

    return v;
  }

  /// no_webpage: bit 1 of flags.1?true
  final bool noWebpage;

  /// invert_media: bit 16 of flags.16?true
  final bool invertMedia;

  /// Id.
  final InputBotInlineMessageIDBase id;

  /// Message.
  final String? message;

  /// Media.
  final InputMediaBase? media;

  /// Reply Markup.
  final ReplyMarkupBase? replyMarkup;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x83557dba);
    buffer.writeInt(flags);
    buffer.writeObject(id);
    final localMessageCopy = message;
    if (localMessageCopy != null) {
      buffer.writeString(localMessageCopy);
    }
    final localMediaCopy = media;
    if (localMediaCopy != null) {
      buffer.writeObject(localMediaCopy);
    }
    final localReplyMarkupCopy = replyMarkup;
    if (localReplyMarkupCopy != null) {
      buffer.writeObject(localReplyMarkupCopy);
    }
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
  }
}

/// Messages Get Bot Callback Answer.
///
/// ID: `9342ca07`.
class MessagesGetBotCallbackAnswer
    extends TlMethod<MessagesBotCallbackAnswerBase> {
  /// Messages Get Bot Callback Answer constructor.
  const MessagesGetBotCallbackAnswer({
    required this.game,
    required this.peer,
    required this.msgId,
    this.data,
    this.password,
  }) : super._();

  /// Deserialize.
  factory MessagesGetBotCallbackAnswer.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final game = _readflags.1?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final data = _readflags.0?bytes(buffer);
    // final password = _readflags.2?InputCheckPasswordSRP(buffer);
    // final result = MessagesGetBotCallbackAnswer(flags: flags, game: game, peer: peer, msgId: msgId, data: data, password: password,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: game,
      b00: data != null,
      b02: password != null,
    );

    return v;
  }

  /// game: bit 1 of flags.1?true
  final bool game;

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Data.
  final Uint8List? data;

  /// Password.
  final InputCheckPasswordSRPBase? password;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9342ca07);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    final localDataCopy = data;
    if (localDataCopy != null) {
      buffer.writeBytes(localDataCopy);
    }
    final localPasswordCopy = password;
    if (localPasswordCopy != null) {
      buffer.writeObject(localPasswordCopy);
    }
  }
}

/// Messages Set Bot Callback Answer.
///
/// ID: `d58f130a`.
class MessagesSetBotCallbackAnswer extends TlMethod<bool> {
  /// Messages Set Bot Callback Answer constructor.
  const MessagesSetBotCallbackAnswer({
    required this.alert,
    required this.queryId,
    this.message,
    this.url,
    required this.cacheTime,
  }) : super._();

  /// Deserialize.
  factory MessagesSetBotCallbackAnswer.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final alert = _readflags.1?true(buffer);
    // final queryId = _readlong(buffer);
    // final message = _readflags.0?string(buffer);
    // final url = _readflags.2?string(buffer);
    // final cacheTime = _readint(buffer);
    // final result = MessagesSetBotCallbackAnswer(flags: flags, alert: alert, queryId: queryId, message: message, url: url, cacheTime: cacheTime,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: alert,
      b00: message != null,
      b02: url != null,
    );

    return v;
  }

  /// alert: bit 1 of flags.1?true
  final bool alert;

  /// Query Id.
  final int queryId;

  /// Message.
  final String? message;

  /// Url.
  final String? url;

  /// Cache Time.
  final int cacheTime;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd58f130a);
    buffer.writeInt(flags);
    buffer.writeLong(queryId);
    final localMessageCopy = message;
    if (localMessageCopy != null) {
      buffer.writeString(localMessageCopy);
    }
    final localUrlCopy = url;
    if (localUrlCopy != null) {
      buffer.writeString(localUrlCopy);
    }
    buffer.writeInt(cacheTime);
  }
}

/// Contacts Get Top Peers.
///
/// ID: `973478b6`.
class ContactsGetTopPeers extends TlMethod<ContactsTopPeersBase> {
  /// Contacts Get Top Peers constructor.
  const ContactsGetTopPeers({
    required this.correspondents,
    required this.botsPm,
    required this.botsInline,
    required this.phoneCalls,
    required this.forwardUsers,
    required this.forwardChats,
    required this.groups,
    required this.channels,
    required this.offset,
    required this.limit,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory ContactsGetTopPeers.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final correspondents = _readflags.0?true(buffer);
    // final botsPm = _readflags.1?true(buffer);
    // final botsInline = _readflags.2?true(buffer);
    // final phoneCalls = _readflags.3?true(buffer);
    // final forwardUsers = _readflags.4?true(buffer);
    // final forwardChats = _readflags.5?true(buffer);
    // final groups = _readflags.10?true(buffer);
    // final channels = _readflags.15?true(buffer);
    // final offset = _readint(buffer);
    // final limit = _readint(buffer);
    // final hash = _readlong(buffer);
    // final result = ContactsGetTopPeers(flags: flags, correspondents: correspondents, botsPm: botsPm, botsInline: botsInline, phoneCalls: phoneCalls, forwardUsers: forwardUsers, forwardChats: forwardChats, groups: groups, channels: channels, offset: offset, limit: limit, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: correspondents,
      b01: botsPm,
      b02: botsInline,
      b03: phoneCalls,
      b04: forwardUsers,
      b05: forwardChats,
      b10: groups,
      b15: channels,
    );

    return v;
  }

  /// correspondents: bit 0 of flags.0?true
  final bool correspondents;

  /// bots_pm: bit 1 of flags.1?true
  final bool botsPm;

  /// bots_inline: bit 2 of flags.2?true
  final bool botsInline;

  /// phone_calls: bit 3 of flags.3?true
  final bool phoneCalls;

  /// forward_users: bit 4 of flags.4?true
  final bool forwardUsers;

  /// forward_chats: bit 5 of flags.5?true
  final bool forwardChats;

  /// groups: bit 10 of flags.10?true
  final bool groups;

  /// channels: bit 15 of flags.15?true
  final bool channels;

  /// Offset.
  final int offset;

  /// Limit.
  final int limit;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x973478b6);
    buffer.writeInt(flags);
    buffer.writeInt(offset);
    buffer.writeInt(limit);
    buffer.writeLong(hash);
  }
}

/// Contacts Reset Top Peer Rating.
///
/// ID: `1ae373ac`.
class ContactsResetTopPeerRating extends TlMethod<bool> {
  /// Contacts Reset Top Peer Rating constructor.
  const ContactsResetTopPeerRating({
    required this.category,
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory ContactsResetTopPeerRating.deserialize(Uint8List buffer) {
    // final category = _readTopPeerCategory(buffer);
    // final peer = _readInputPeer(buffer);
    // final result = ContactsResetTopPeerRating(category: category, peer: peer,);

    // return result;

    throw Exception();
  }

  /// Category.
  final TopPeerCategoryBase category;

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1ae373ac);
    buffer.writeObject(category);
    buffer.writeObject(peer);
  }
}

/// Messages Get Peer Dialogs.
///
/// ID: `e470bcfd`.
class MessagesGetPeerDialogs extends TlMethod<MessagesPeerDialogsBase> {
  /// Messages Get Peer Dialogs constructor.
  const MessagesGetPeerDialogs({
    required this.peers,
  }) : super._();

  /// Deserialize.
  factory MessagesGetPeerDialogs.deserialize(Uint8List buffer) {
    // final peers = _readVector<InputDialogPeer>(buffer);
    // final result = MessagesGetPeerDialogs(peers: peers,);

    // return result;

    throw Exception();
  }

  /// Peers.
  final List<InputDialogPeerBase> peers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe470bcfd);
    buffer.writeVectorObject(peers);
  }
}

/// Messages Save Draft.
///
/// ID: `7ff3b806`.
class MessagesSaveDraft extends TlMethod<bool> {
  /// Messages Save Draft constructor.
  const MessagesSaveDraft({
    required this.noWebpage,
    required this.invertMedia,
    this.replyTo,
    required this.peer,
    required this.message,
    this.entities,
    this.media,
  }) : super._();

  /// Deserialize.
  factory MessagesSaveDraft.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final noWebpage = _readflags.1?true(buffer);
    // final invertMedia = _readflags.6?true(buffer);
    // final replyTo = _readflags.4?InputReplyTo(buffer);
    // final peer = _readInputPeer(buffer);
    // final message = _readstring(buffer);
    // final entities = _readflags.3?Vector<MessageEntity>(buffer);
    // final media = _readflags.5?InputMedia(buffer);
    // final result = MessagesSaveDraft(flags: flags, noWebpage: noWebpage, invertMedia: invertMedia, replyTo: replyTo, peer: peer, message: message, entities: entities, media: media,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: noWebpage,
      b06: invertMedia,
      b04: replyTo != null,
      b03: entities != null,
      b05: media != null,
    );

    return v;
  }

  /// no_webpage: bit 1 of flags.1?true
  final bool noWebpage;

  /// invert_media: bit 6 of flags.6?true
  final bool invertMedia;

  /// Reply To.
  final InputReplyToBase? replyTo;

  /// Peer.
  final InputPeerBase peer;

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Media.
  final InputMediaBase? media;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7ff3b806);
    buffer.writeInt(flags);
    final localReplyToCopy = replyTo;
    if (localReplyToCopy != null) {
      buffer.writeObject(localReplyToCopy);
    }
    buffer.writeObject(peer);
    buffer.writeString(message);
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localMediaCopy = media;
    if (localMediaCopy != null) {
      buffer.writeObject(localMediaCopy);
    }
  }
}

/// Messages Get All Drafts.
///
/// ID: `6a3f8d65`.
class MessagesGetAllDrafts extends TlMethod<UpdatesBase> {
  /// Messages Get All Drafts constructor.
  const MessagesGetAllDrafts() : super._();

  /// Deserialize.
  factory MessagesGetAllDrafts.deserialize(Uint8List buffer) {
    // final result = MessagesGetAllDrafts();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6a3f8d65);
  }
}

/// Messages Get Featured Stickers.
///
/// ID: `64780b14`.
class MessagesGetFeaturedStickers
    extends TlMethod<MessagesFeaturedStickersBase> {
  /// Messages Get Featured Stickers constructor.
  const MessagesGetFeaturedStickers({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetFeaturedStickers.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = MessagesGetFeaturedStickers(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x64780b14);
    buffer.writeLong(hash);
  }
}

/// Messages Read Featured Stickers.
///
/// ID: `5b118126`.
class MessagesReadFeaturedStickers extends TlMethod<bool> {
  /// Messages Read Featured Stickers constructor.
  const MessagesReadFeaturedStickers({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesReadFeaturedStickers.deserialize(Uint8List buffer) {
    // final id = _readVector<long>(buffer);
    // final result = MessagesReadFeaturedStickers(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5b118126);
    buffer.writeVectorLong(id);
  }
}

/// Messages Get Recent Stickers.
///
/// ID: `9da9403b`.
class MessagesGetRecentStickers extends TlMethod<MessagesRecentStickersBase> {
  /// Messages Get Recent Stickers constructor.
  const MessagesGetRecentStickers({
    required this.attached,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetRecentStickers.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final attached = _readflags.0?true(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesGetRecentStickers(flags: flags, attached: attached, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: attached,
    );

    return v;
  }

  /// attached: bit 0 of flags.0?true
  final bool attached;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9da9403b);
    buffer.writeInt(flags);
    buffer.writeLong(hash);
  }
}

/// Messages Save Recent Sticker.
///
/// ID: `392718f8`.
class MessagesSaveRecentSticker extends TlMethod<bool> {
  /// Messages Save Recent Sticker constructor.
  const MessagesSaveRecentSticker({
    required this.attached,
    required this.id,
    required this.unsave,
  }) : super._();

  /// Deserialize.
  factory MessagesSaveRecentSticker.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final attached = _readflags.0?true(buffer);
    // final id = _readInputDocument(buffer);
    // final unsave = _readBool(buffer);
    // final result = MessagesSaveRecentSticker(flags: flags, attached: attached, id: id, unsave: unsave,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: attached,
    );

    return v;
  }

  /// attached: bit 0 of flags.0?true
  final bool attached;

  /// Id.
  final InputDocumentBase id;

  /// Unsave.
  final bool unsave;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x392718f8);
    buffer.writeInt(flags);
    buffer.writeObject(id);
    buffer.writeBool(unsave);
  }
}

/// Messages Clear Recent Stickers.
///
/// ID: `8999602d`.
class MessagesClearRecentStickers extends TlMethod<bool> {
  /// Messages Clear Recent Stickers constructor.
  const MessagesClearRecentStickers({
    required this.attached,
  }) : super._();

  /// Deserialize.
  factory MessagesClearRecentStickers.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final attached = _readflags.0?true(buffer);
    // final result = MessagesClearRecentStickers(flags: flags, attached: attached,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: attached,
    );

    return v;
  }

  /// attached: bit 0 of flags.0?true
  final bool attached;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8999602d);
    buffer.writeInt(flags);
  }
}

/// Messages Get Archived Stickers.
///
/// ID: `57f17692`.
class MessagesGetArchivedStickers
    extends TlMethod<MessagesArchivedStickersBase> {
  /// Messages Get Archived Stickers constructor.
  const MessagesGetArchivedStickers({
    required this.masks,
    required this.emojis,
    required this.offsetId,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory MessagesGetArchivedStickers.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final masks = _readflags.0?true(buffer);
    // final emojis = _readflags.1?true(buffer);
    // final offsetId = _readlong(buffer);
    // final limit = _readint(buffer);
    // final result = MessagesGetArchivedStickers(flags: flags, masks: masks, emojis: emojis, offsetId: offsetId, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: masks,
      b01: emojis,
    );

    return v;
  }

  /// masks: bit 0 of flags.0?true
  final bool masks;

  /// emojis: bit 1 of flags.1?true
  final bool emojis;

  /// Offset Id.
  final int offsetId;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x57f17692);
    buffer.writeInt(flags);
    buffer.writeLong(offsetId);
    buffer.writeInt(limit);
  }
}

/// Account Send Confirm Phone Code.
///
/// ID: `1b3faa88`.
class AccountSendConfirmPhoneCode extends TlMethod<AuthSentCodeBase> {
  /// Account Send Confirm Phone Code constructor.
  const AccountSendConfirmPhoneCode({
    required this.hash,
    required this.settings,
  }) : super._();

  /// Deserialize.
  factory AccountSendConfirmPhoneCode.deserialize(Uint8List buffer) {
    // final hash = _readstring(buffer);
    // final settings = _readCodeSettings(buffer);
    // final result = AccountSendConfirmPhoneCode(hash: hash, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final String hash;

  /// Settings.
  final CodeSettingsBase settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1b3faa88);
    buffer.writeString(hash);
    buffer.writeObject(settings);
  }
}

/// Account Confirm Phone.
///
/// ID: `5f2178c3`.
class AccountConfirmPhone extends TlMethod<bool> {
  /// Account Confirm Phone constructor.
  const AccountConfirmPhone({
    required this.phoneCodeHash,
    required this.phoneCode,
  }) : super._();

  /// Deserialize.
  factory AccountConfirmPhone.deserialize(Uint8List buffer) {
    // final phoneCodeHash = _readstring(buffer);
    // final phoneCode = _readstring(buffer);
    // final result = AccountConfirmPhone(phoneCodeHash: phoneCodeHash, phoneCode: phoneCode,);

    // return result;

    throw Exception();
  }

  /// Phone Code Hash.
  final String phoneCodeHash;

  /// Phone Code.
  final String phoneCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5f2178c3);
    buffer.writeString(phoneCodeHash);
    buffer.writeString(phoneCode);
  }
}

/// Channels Get Admined Public Channels.
///
/// ID: `f8b036af`.
class ChannelsGetAdminedPublicChannels extends TlMethod<MessagesChatsBase> {
  /// Channels Get Admined Public Channels constructor.
  const ChannelsGetAdminedPublicChannels({
    required this.byLocation,
    required this.checkLimit,
  }) : super._();

  /// Deserialize.
  factory ChannelsGetAdminedPublicChannels.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final byLocation = _readflags.0?true(buffer);
    // final checkLimit = _readflags.1?true(buffer);
    // final result = ChannelsGetAdminedPublicChannels(flags: flags, byLocation: byLocation, checkLimit: checkLimit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: byLocation,
      b01: checkLimit,
    );

    return v;
  }

  /// by_location: bit 0 of flags.0?true
  final bool byLocation;

  /// check_limit: bit 1 of flags.1?true
  final bool checkLimit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf8b036af);
    buffer.writeInt(flags);
  }
}

/// Messages Get Mask Stickers.
///
/// ID: `640f82b8`.
class MessagesGetMaskStickers extends TlMethod<MessagesAllStickersBase> {
  /// Messages Get Mask Stickers constructor.
  const MessagesGetMaskStickers({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetMaskStickers.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = MessagesGetMaskStickers(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x640f82b8);
    buffer.writeLong(hash);
  }
}

/// Messages Get Attached Stickers.
///
/// ID: `cc5b67cc`.
class MessagesGetAttachedStickers
    extends TlMethod<List<StickerSetCoveredBase>> {
  /// Messages Get Attached Stickers constructor.
  const MessagesGetAttachedStickers({
    required this.media,
  }) : super._();

  /// Deserialize.
  factory MessagesGetAttachedStickers.deserialize(Uint8List buffer) {
    // final media = _readInputStickeredMedia(buffer);
    // final result = MessagesGetAttachedStickers(media: media,);

    // return result;

    throw Exception();
  }

  /// Media.
  final InputStickeredMediaBase media;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcc5b67cc);
    buffer.writeObject(media);
  }
}

/// Auth Drop Temp Auth Keys.
///
/// ID: `8e48a188`.
class AuthDropTempAuthKeys extends TlMethod<bool> {
  /// Auth Drop Temp Auth Keys constructor.
  const AuthDropTempAuthKeys({
    required this.exceptAuthKeys,
  }) : super._();

  /// Deserialize.
  factory AuthDropTempAuthKeys.deserialize(Uint8List buffer) {
    // final exceptAuthKeys = _readVector<long>(buffer);
    // final result = AuthDropTempAuthKeys(exceptAuthKeys: exceptAuthKeys,);

    // return result;

    throw Exception();
  }

  /// Except Auth Keys.
  final List<int> exceptAuthKeys;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8e48a188);
    buffer.writeVectorLong(exceptAuthKeys);
  }
}

/// Messages Set Game Score.
///
/// ID: `8ef8ecc0`.
class MessagesSetGameScore extends TlMethod<UpdatesBase> {
  /// Messages Set Game Score constructor.
  const MessagesSetGameScore({
    required this.editMessage,
    required this.force,
    required this.peer,
    required this.id,
    required this.userId,
    required this.score,
  }) : super._();

  /// Deserialize.
  factory MessagesSetGameScore.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final editMessage = _readflags.0?true(buffer);
    // final force = _readflags.1?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final userId = _readInputUser(buffer);
    // final score = _readint(buffer);
    // final result = MessagesSetGameScore(flags: flags, editMessage: editMessage, force: force, peer: peer, id: id, userId: userId, score: score,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: editMessage,
      b01: force,
    );

    return v;
  }

  /// edit_message: bit 0 of flags.0?true
  final bool editMessage;

  /// force: bit 1 of flags.1?true
  final bool force;

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// User Id.
  final InputUserBase userId;

  /// Score.
  final int score;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8ef8ecc0);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(id);
    buffer.writeObject(userId);
    buffer.writeInt(score);
  }
}

/// Messages Set Inline Game Score.
///
/// ID: `15ad9f64`.
class MessagesSetInlineGameScore extends TlMethod<bool> {
  /// Messages Set Inline Game Score constructor.
  const MessagesSetInlineGameScore({
    required this.editMessage,
    required this.force,
    required this.id,
    required this.userId,
    required this.score,
  }) : super._();

  /// Deserialize.
  factory MessagesSetInlineGameScore.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final editMessage = _readflags.0?true(buffer);
    // final force = _readflags.1?true(buffer);
    // final id = _readInputBotInlineMessageID(buffer);
    // final userId = _readInputUser(buffer);
    // final score = _readint(buffer);
    // final result = MessagesSetInlineGameScore(flags: flags, editMessage: editMessage, force: force, id: id, userId: userId, score: score,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: editMessage,
      b01: force,
    );

    return v;
  }

  /// edit_message: bit 0 of flags.0?true
  final bool editMessage;

  /// force: bit 1 of flags.1?true
  final bool force;

  /// Id.
  final InputBotInlineMessageIDBase id;

  /// User Id.
  final InputUserBase userId;

  /// Score.
  final int score;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x15ad9f64);
    buffer.writeInt(flags);
    buffer.writeObject(id);
    buffer.writeObject(userId);
    buffer.writeInt(score);
  }
}

/// Messages Get Game High Scores.
///
/// ID: `e822649d`.
class MessagesGetGameHighScores extends TlMethod<MessagesHighScoresBase> {
  /// Messages Get Game High Scores constructor.
  const MessagesGetGameHighScores({
    required this.peer,
    required this.id,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory MessagesGetGameHighScores.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final userId = _readInputUser(buffer);
    // final result = MessagesGetGameHighScores(peer: peer, id: id, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// User Id.
  final InputUserBase userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe822649d);
    buffer.writeObject(peer);
    buffer.writeInt(id);
    buffer.writeObject(userId);
  }
}

/// Messages Get Inline Game High Scores.
///
/// ID: `0f635e1b`.
class MessagesGetInlineGameHighScores extends TlMethod<MessagesHighScoresBase> {
  /// Messages Get Inline Game High Scores constructor.
  const MessagesGetInlineGameHighScores({
    required this.id,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory MessagesGetInlineGameHighScores.deserialize(Uint8List buffer) {
    // final id = _readInputBotInlineMessageID(buffer);
    // final userId = _readInputUser(buffer);
    // final result = MessagesGetInlineGameHighScores(id: id, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Id.
  final InputBotInlineMessageIDBase id;

  /// User Id.
  final InputUserBase userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0f635e1b);
    buffer.writeObject(id);
    buffer.writeObject(userId);
  }
}

/// Messages Get Common Chats.
///
/// ID: `e40ca104`.
class MessagesGetCommonChats extends TlMethod<MessagesChatsBase> {
  /// Messages Get Common Chats constructor.
  const MessagesGetCommonChats({
    required this.userId,
    required this.maxId,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory MessagesGetCommonChats.deserialize(Uint8List buffer) {
    // final userId = _readInputUser(buffer);
    // final maxId = _readlong(buffer);
    // final limit = _readint(buffer);
    // final result = MessagesGetCommonChats(userId: userId, maxId: maxId, limit: limit,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final InputUserBase userId;

  /// Max Id.
  final int maxId;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe40ca104);
    buffer.writeObject(userId);
    buffer.writeLong(maxId);
    buffer.writeInt(limit);
  }
}

/// Help Set Bot Updates Status.
///
/// ID: `ec22cfcd`.
class HelpSetBotUpdatesStatus extends TlMethod<bool> {
  /// Help Set Bot Updates Status constructor.
  const HelpSetBotUpdatesStatus({
    required this.pendingUpdatesCount,
    required this.message,
  }) : super._();

  /// Deserialize.
  factory HelpSetBotUpdatesStatus.deserialize(Uint8List buffer) {
    // final pendingUpdatesCount = _readint(buffer);
    // final message = _readstring(buffer);
    // final result = HelpSetBotUpdatesStatus(pendingUpdatesCount: pendingUpdatesCount, message: message,);

    // return result;

    throw Exception();
  }

  /// Pending Updates Count.
  final DateTime pendingUpdatesCount;

  /// Message.
  final String message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xec22cfcd);
    buffer.writeDateTime(pendingUpdatesCount);
    buffer.writeString(message);
  }
}

/// Messages Get Web Page.
///
/// ID: `8d9692a3`.
class MessagesGetWebPage extends TlMethod<MessagesWebPageBase> {
  /// Messages Get Web Page constructor.
  const MessagesGetWebPage({
    required this.url,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetWebPage.deserialize(Uint8List buffer) {
    // final url = _readstring(buffer);
    // final hash = _readint(buffer);
    // final result = MessagesGetWebPage(url: url, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Url.
  final String url;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8d9692a3);
    buffer.writeString(url);
    buffer.writeInt(hash);
  }
}

/// Messages Toggle Dialog Pin.
///
/// ID: `a731e257`.
class MessagesToggleDialogPin extends TlMethod<bool> {
  /// Messages Toggle Dialog Pin constructor.
  const MessagesToggleDialogPin({
    required this.pinned,
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory MessagesToggleDialogPin.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pinned = _readflags.0?true(buffer);
    // final peer = _readInputDialogPeer(buffer);
    // final result = MessagesToggleDialogPin(flags: flags, pinned: pinned, peer: peer,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: pinned,
    );

    return v;
  }

  /// pinned: bit 0 of flags.0?true
  final bool pinned;

  /// Peer.
  final InputDialogPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa731e257);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
  }
}

/// Messages Reorder Pinned Dialogs.
///
/// ID: `3b1adf37`.
class MessagesReorderPinnedDialogs extends TlMethod<bool> {
  /// Messages Reorder Pinned Dialogs constructor.
  const MessagesReorderPinnedDialogs({
    required this.force,
    required this.folderId,
    required this.order,
  }) : super._();

  /// Deserialize.
  factory MessagesReorderPinnedDialogs.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final force = _readflags.0?true(buffer);
    // final folderId = _readint(buffer);
    // final order = _readVector<InputDialogPeer>(buffer);
    // final result = MessagesReorderPinnedDialogs(flags: flags, force: force, folderId: folderId, order: order,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: force,
    );

    return v;
  }

  /// force: bit 0 of flags.0?true
  final bool force;

  /// Folder Id.
  final int folderId;

  /// Order.
  final List<InputDialogPeerBase> order;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3b1adf37);
    buffer.writeInt(flags);
    buffer.writeInt(folderId);
    buffer.writeVectorObject(order);
  }
}

/// Messages Get Pinned Dialogs.
///
/// ID: `d6b94df2`.
class MessagesGetPinnedDialogs extends TlMethod<MessagesPeerDialogsBase> {
  /// Messages Get Pinned Dialogs constructor.
  const MessagesGetPinnedDialogs({
    required this.folderId,
  }) : super._();

  /// Deserialize.
  factory MessagesGetPinnedDialogs.deserialize(Uint8List buffer) {
    // final folderId = _readint(buffer);
    // final result = MessagesGetPinnedDialogs(folderId: folderId,);

    // return result;

    throw Exception();
  }

  /// Folder Id.
  final int folderId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd6b94df2);
    buffer.writeInt(folderId);
  }
}

/// Bots Send Custom Request.
///
/// ID: `aa2769ed`.
class BotsSendCustomRequest extends TlMethod<DataJSONBase> {
  /// Bots Send Custom Request constructor.
  const BotsSendCustomRequest({
    required this.customMethod,
    required this.params,
  }) : super._();

  /// Deserialize.
  factory BotsSendCustomRequest.deserialize(Uint8List buffer) {
    // final customMethod = _readstring(buffer);
    // final params = _readDataJSON(buffer);
    // final result = BotsSendCustomRequest(customMethod: customMethod, params: params,);

    // return result;

    throw Exception();
  }

  /// Custom Method.
  final String customMethod;

  /// Params.
  final DataJSONBase params;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xaa2769ed);
    buffer.writeString(customMethod);
    buffer.writeObject(params);
  }
}

/// Bots Answer Webhook J S O N Query.
///
/// ID: `e6213f4d`.
class BotsAnswerWebhookJSONQuery extends TlMethod<bool> {
  /// Bots Answer Webhook J S O N Query constructor.
  const BotsAnswerWebhookJSONQuery({
    required this.queryId,
    required this.data,
  }) : super._();

  /// Deserialize.
  factory BotsAnswerWebhookJSONQuery.deserialize(Uint8List buffer) {
    // final queryId = _readlong(buffer);
    // final data = _readDataJSON(buffer);
    // final result = BotsAnswerWebhookJSONQuery(queryId: queryId, data: data,);

    // return result;

    throw Exception();
  }

  /// Query Id.
  final int queryId;

  /// Data.
  final DataJSONBase data;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe6213f4d);
    buffer.writeLong(queryId);
    buffer.writeObject(data);
  }
}

/// Upload Get Web File.
///
/// ID: `24e6818d`.
class UploadGetWebFile extends TlMethod<UploadWebFileBase> {
  /// Upload Get Web File constructor.
  const UploadGetWebFile({
    required this.location,
    required this.offset,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory UploadGetWebFile.deserialize(Uint8List buffer) {
    // final location = _readInputWebFileLocation(buffer);
    // final offset = _readint(buffer);
    // final limit = _readint(buffer);
    // final result = UploadGetWebFile(location: location, offset: offset, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Location.
  final InputWebFileLocationBase location;

  /// Offset.
  final int offset;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x24e6818d);
    buffer.writeObject(location);
    buffer.writeInt(offset);
    buffer.writeInt(limit);
  }
}

/// Payments Get Payment Form.
///
/// ID: `37148dbb`.
class PaymentsGetPaymentForm extends TlMethod<PaymentsPaymentFormBase> {
  /// Payments Get Payment Form constructor.
  const PaymentsGetPaymentForm({
    required this.invoice,
    this.themeParams,
  }) : super._();

  /// Deserialize.
  factory PaymentsGetPaymentForm.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final invoice = _readInputInvoice(buffer);
    // final themeParams = _readflags.0?DataJSON(buffer);
    // final result = PaymentsGetPaymentForm(flags: flags, invoice: invoice, themeParams: themeParams,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: themeParams != null,
    );

    return v;
  }

  /// Invoice.
  final InputInvoiceBase invoice;

  /// Theme Params.
  final DataJSONBase? themeParams;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x37148dbb);
    buffer.writeInt(flags);
    buffer.writeObject(invoice);
    final localThemeParamsCopy = themeParams;
    if (localThemeParamsCopy != null) {
      buffer.writeObject(localThemeParamsCopy);
    }
  }
}

/// Payments Get Payment Receipt.
///
/// ID: `2478d1cc`.
class PaymentsGetPaymentReceipt extends TlMethod<PaymentsPaymentReceiptBase> {
  /// Payments Get Payment Receipt constructor.
  const PaymentsGetPaymentReceipt({
    required this.peer,
    required this.msgId,
  }) : super._();

  /// Deserialize.
  factory PaymentsGetPaymentReceipt.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final result = PaymentsGetPaymentReceipt(peer: peer, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2478d1cc);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
  }
}

/// Payments Validate Requested Info.
///
/// ID: `b6c8f12b`.
class PaymentsValidateRequestedInfo
    extends TlMethod<PaymentsValidatedRequestedInfoBase> {
  /// Payments Validate Requested Info constructor.
  const PaymentsValidateRequestedInfo({
    required this.save,
    required this.invoice,
    required this.info,
  }) : super._();

  /// Deserialize.
  factory PaymentsValidateRequestedInfo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final save = _readflags.0?true(buffer);
    // final invoice = _readInputInvoice(buffer);
    // final info = _readPaymentRequestedInfo(buffer);
    // final result = PaymentsValidateRequestedInfo(flags: flags, save: save, invoice: invoice, info: info,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: save,
    );

    return v;
  }

  /// save: bit 0 of flags.0?true
  final bool save;

  /// Invoice.
  final InputInvoiceBase invoice;

  /// Info.
  final PaymentRequestedInfoBase info;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb6c8f12b);
    buffer.writeInt(flags);
    buffer.writeObject(invoice);
    buffer.writeObject(info);
  }
}

/// Payments Send Payment Form.
///
/// ID: `2d03522f`.
class PaymentsSendPaymentForm extends TlMethod<PaymentsPaymentResultBase> {
  /// Payments Send Payment Form constructor.
  const PaymentsSendPaymentForm({
    required this.formId,
    required this.invoice,
    this.requestedInfoId,
    this.shippingOptionId,
    required this.credentials,
    this.tipAmount,
  }) : super._();

  /// Deserialize.
  factory PaymentsSendPaymentForm.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final formId = _readlong(buffer);
    // final invoice = _readInputInvoice(buffer);
    // final requestedInfoId = _readflags.0?string(buffer);
    // final shippingOptionId = _readflags.1?string(buffer);
    // final credentials = _readInputPaymentCredentials(buffer);
    // final tipAmount = _readflags.2?long(buffer);
    // final result = PaymentsSendPaymentForm(flags: flags, formId: formId, invoice: invoice, requestedInfoId: requestedInfoId, shippingOptionId: shippingOptionId, credentials: credentials, tipAmount: tipAmount,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: requestedInfoId != null,
      b01: shippingOptionId != null,
      b02: tipAmount != null,
    );

    return v;
  }

  /// Form Id.
  final int formId;

  /// Invoice.
  final InputInvoiceBase invoice;

  /// Requested Info Id.
  final String? requestedInfoId;

  /// Shipping Option Id.
  final String? shippingOptionId;

  /// Credentials.
  final InputPaymentCredentialsBase credentials;

  /// Tip Amount.
  final int? tipAmount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2d03522f);
    buffer.writeInt(flags);
    buffer.writeLong(formId);
    buffer.writeObject(invoice);
    final localRequestedInfoIdCopy = requestedInfoId;
    if (localRequestedInfoIdCopy != null) {
      buffer.writeString(localRequestedInfoIdCopy);
    }
    final localShippingOptionIdCopy = shippingOptionId;
    if (localShippingOptionIdCopy != null) {
      buffer.writeString(localShippingOptionIdCopy);
    }
    buffer.writeObject(credentials);
    final localTipAmountCopy = tipAmount;
    if (localTipAmountCopy != null) {
      buffer.writeLong(localTipAmountCopy);
    }
  }
}

/// Account Get Tmp Password.
///
/// ID: `449e0b51`.
class AccountGetTmpPassword extends TlMethod<AccountTmpPasswordBase> {
  /// Account Get Tmp Password constructor.
  const AccountGetTmpPassword({
    required this.password,
    required this.period,
  }) : super._();

  /// Deserialize.
  factory AccountGetTmpPassword.deserialize(Uint8List buffer) {
    // final password = _readInputCheckPasswordSRP(buffer);
    // final period = _readint(buffer);
    // final result = AccountGetTmpPassword(password: password, period: period,);

    // return result;

    throw Exception();
  }

  /// Password.
  final InputCheckPasswordSRPBase password;

  /// Period.
  final int period;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x449e0b51);
    buffer.writeObject(password);
    buffer.writeInt(period);
  }
}

/// Payments Get Saved Info.
///
/// ID: `227d824b`.
class PaymentsGetSavedInfo extends TlMethod<PaymentsSavedInfoBase> {
  /// Payments Get Saved Info constructor.
  const PaymentsGetSavedInfo() : super._();

  /// Deserialize.
  factory PaymentsGetSavedInfo.deserialize(Uint8List buffer) {
    // final result = PaymentsGetSavedInfo();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x227d824b);
  }
}

/// Payments Clear Saved Info.
///
/// ID: `d83d70c1`.
class PaymentsClearSavedInfo extends TlMethod<bool> {
  /// Payments Clear Saved Info constructor.
  const PaymentsClearSavedInfo({
    required this.credentials,
    required this.info,
  }) : super._();

  /// Deserialize.
  factory PaymentsClearSavedInfo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final credentials = _readflags.0?true(buffer);
    // final info = _readflags.1?true(buffer);
    // final result = PaymentsClearSavedInfo(flags: flags, credentials: credentials, info: info,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: credentials,
      b01: info,
    );

    return v;
  }

  /// credentials: bit 0 of flags.0?true
  final bool credentials;

  /// info: bit 1 of flags.1?true
  final bool info;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd83d70c1);
    buffer.writeInt(flags);
  }
}

/// Messages Set Bot Shipping Results.
///
/// ID: `e5f672fa`.
class MessagesSetBotShippingResults extends TlMethod<bool> {
  /// Messages Set Bot Shipping Results constructor.
  const MessagesSetBotShippingResults({
    required this.queryId,
    this.error,
    this.shippingOptions,
  }) : super._();

  /// Deserialize.
  factory MessagesSetBotShippingResults.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final queryId = _readlong(buffer);
    // final error = _readflags.0?string(buffer);
    // final shippingOptions = _readflags.1?Vector<ShippingOption>(buffer);
    // final result = MessagesSetBotShippingResults(flags: flags, queryId: queryId, error: error, shippingOptions: shippingOptions,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: error != null,
      b01: shippingOptions != null,
    );

    return v;
  }

  /// Query Id.
  final int queryId;

  /// Error.
  final String? error;

  /// Shipping Options.
  final List<ShippingOptionBase>? shippingOptions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe5f672fa);
    buffer.writeInt(flags);
    buffer.writeLong(queryId);
    final localErrorCopy = error;
    if (localErrorCopy != null) {
      buffer.writeString(localErrorCopy);
    }
    final localShippingOptionsCopy = shippingOptions;
    if (localShippingOptionsCopy != null) {
      buffer.writeVectorObject(localShippingOptionsCopy);
    }
  }
}

/// Messages Set Bot Precheckout Results.
///
/// ID: `09c2dd95`.
class MessagesSetBotPrecheckoutResults extends TlMethod<bool> {
  /// Messages Set Bot Precheckout Results constructor.
  const MessagesSetBotPrecheckoutResults({
    required this.success,
    required this.queryId,
    this.error,
  }) : super._();

  /// Deserialize.
  factory MessagesSetBotPrecheckoutResults.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final success = _readflags.1?true(buffer);
    // final queryId = _readlong(buffer);
    // final error = _readflags.0?string(buffer);
    // final result = MessagesSetBotPrecheckoutResults(flags: flags, success: success, queryId: queryId, error: error,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: success,
      b00: error != null,
    );

    return v;
  }

  /// success: bit 1 of flags.1?true
  final bool success;

  /// Query Id.
  final int queryId;

  /// Error.
  final String? error;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x09c2dd95);
    buffer.writeInt(flags);
    buffer.writeLong(queryId);
    final localErrorCopy = error;
    if (localErrorCopy != null) {
      buffer.writeString(localErrorCopy);
    }
  }
}

/// Stickers Create Sticker Set.
///
/// ID: `9021ab67`.
class StickersCreateStickerSet extends TlMethod<MessagesStickerSetBase> {
  /// Stickers Create Sticker Set constructor.
  const StickersCreateStickerSet({
    required this.masks,
    required this.animated,
    required this.videos,
    required this.emojis,
    required this.textColor,
    required this.userId,
    required this.title,
    required this.shortName,
    this.thumb,
    required this.stickers,
    this.software,
  }) : super._();

  /// Deserialize.
  factory StickersCreateStickerSet.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final masks = _readflags.0?true(buffer);
    // final animated = _readflags.1?true(buffer);
    // final videos = _readflags.4?true(buffer);
    // final emojis = _readflags.5?true(buffer);
    // final textColor = _readflags.6?true(buffer);
    // final userId = _readInputUser(buffer);
    // final title = _readstring(buffer);
    // final shortName = _readstring(buffer);
    // final thumb = _readflags.2?InputDocument(buffer);
    // final stickers = _readVector<InputStickerSetItem>(buffer);
    // final software = _readflags.3?string(buffer);
    // final result = StickersCreateStickerSet(flags: flags, masks: masks, animated: animated, videos: videos, emojis: emojis, textColor: textColor, userId: userId, title: title, shortName: shortName, thumb: thumb, stickers: stickers, software: software,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: masks,
      b01: animated,
      b04: videos,
      b05: emojis,
      b06: textColor,
      b02: thumb != null,
      b03: software != null,
    );

    return v;
  }

  /// masks: bit 0 of flags.0?true
  final bool masks;

  /// animated: bit 1 of flags.1?true
  final bool animated;

  /// videos: bit 4 of flags.4?true
  final bool videos;

  /// emojis: bit 5 of flags.5?true
  final bool emojis;

  /// text_color: bit 6 of flags.6?true
  final bool textColor;

  /// User Id.
  final InputUserBase userId;

  /// Title.
  final String title;

  /// Short Name.
  final String shortName;

  /// Thumb.
  final InputDocumentBase? thumb;

  /// Stickers.
  final List<InputStickerSetItemBase> stickers;

  /// Software.
  final String? software;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9021ab67);
    buffer.writeInt(flags);
    buffer.writeObject(userId);
    buffer.writeString(title);
    buffer.writeString(shortName);
    final localThumbCopy = thumb;
    if (localThumbCopy != null) {
      buffer.writeObject(localThumbCopy);
    }
    buffer.writeVectorObject(stickers);
    final localSoftwareCopy = software;
    if (localSoftwareCopy != null) {
      buffer.writeString(localSoftwareCopy);
    }
  }
}

/// Stickers Remove Sticker From Set.
///
/// ID: `f7760f51`.
class StickersRemoveStickerFromSet extends TlMethod<MessagesStickerSetBase> {
  /// Stickers Remove Sticker From Set constructor.
  const StickersRemoveStickerFromSet({
    required this.sticker,
  }) : super._();

  /// Deserialize.
  factory StickersRemoveStickerFromSet.deserialize(Uint8List buffer) {
    // final sticker = _readInputDocument(buffer);
    // final result = StickersRemoveStickerFromSet(sticker: sticker,);

    // return result;

    throw Exception();
  }

  /// Sticker.
  final InputDocumentBase sticker;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf7760f51);
    buffer.writeObject(sticker);
  }
}

/// Stickers Change Sticker Position.
///
/// ID: `ffb6d4ca`.
class StickersChangeStickerPosition extends TlMethod<MessagesStickerSetBase> {
  /// Stickers Change Sticker Position constructor.
  const StickersChangeStickerPosition({
    required this.sticker,
    required this.position,
  }) : super._();

  /// Deserialize.
  factory StickersChangeStickerPosition.deserialize(Uint8List buffer) {
    // final sticker = _readInputDocument(buffer);
    // final position = _readint(buffer);
    // final result = StickersChangeStickerPosition(sticker: sticker, position: position,);

    // return result;

    throw Exception();
  }

  /// Sticker.
  final InputDocumentBase sticker;

  /// Position.
  final int position;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xffb6d4ca);
    buffer.writeObject(sticker);
    buffer.writeInt(position);
  }
}

/// Stickers Add Sticker To Set.
///
/// ID: `8653febe`.
class StickersAddStickerToSet extends TlMethod<MessagesStickerSetBase> {
  /// Stickers Add Sticker To Set constructor.
  const StickersAddStickerToSet({
    required this.stickerset,
    required this.sticker,
  }) : super._();

  /// Deserialize.
  factory StickersAddStickerToSet.deserialize(Uint8List buffer) {
    // final stickerset = _readInputStickerSet(buffer);
    // final sticker = _readInputStickerSetItem(buffer);
    // final result = StickersAddStickerToSet(stickerset: stickerset, sticker: sticker,);

    // return result;

    throw Exception();
  }

  /// Stickerset.
  final InputStickerSetBase stickerset;

  /// Sticker.
  final InputStickerSetItemBase sticker;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8653febe);
    buffer.writeObject(stickerset);
    buffer.writeObject(sticker);
  }
}

/// Messages Upload Media.
///
/// ID: `519bc2b1`.
class MessagesUploadMedia extends TlMethod<MessageMediaBase> {
  /// Messages Upload Media constructor.
  const MessagesUploadMedia({
    required this.peer,
    required this.media,
  }) : super._();

  /// Deserialize.
  factory MessagesUploadMedia.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final media = _readInputMedia(buffer);
    // final result = MessagesUploadMedia(peer: peer, media: media,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Media.
  final InputMediaBase media;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x519bc2b1);
    buffer.writeObject(peer);
    buffer.writeObject(media);
  }
}

/// Phone Get Call Config.
///
/// ID: `55451fa9`.
class PhoneGetCallConfig extends TlMethod<DataJSONBase> {
  /// Phone Get Call Config constructor.
  const PhoneGetCallConfig() : super._();

  /// Deserialize.
  factory PhoneGetCallConfig.deserialize(Uint8List buffer) {
    // final result = PhoneGetCallConfig();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x55451fa9);
  }
}

/// Phone Request Call.
///
/// ID: `42ff96ed`.
class PhoneRequestCall extends TlMethod<PhonePhoneCallBase> {
  /// Phone Request Call constructor.
  const PhoneRequestCall({
    required this.video,
    required this.userId,
    required this.randomId,
    required this.gAHash,
    required this.protocol,
  }) : super._();

  /// Deserialize.
  factory PhoneRequestCall.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final video = _readflags.0?true(buffer);
    // final userId = _readInputUser(buffer);
    // final randomId = _readint(buffer);
    // final gAHash = _readbytes(buffer);
    // final protocol = _readPhoneCallProtocol(buffer);
    // final result = PhoneRequestCall(flags: flags, video: video, userId: userId, randomId: randomId, gAHash: gAHash, protocol: protocol,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: video,
    );

    return v;
  }

  /// video: bit 0 of flags.0?true
  final bool video;

  /// User Id.
  final InputUserBase userId;

  /// Random Id.
  final int randomId;

  /// G A Hash.
  final Uint8List gAHash;

  /// Protocol.
  final PhoneCallProtocolBase protocol;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x42ff96ed);
    buffer.writeInt(flags);
    buffer.writeObject(userId);
    buffer.writeInt(randomId);
    buffer.writeBytes(gAHash);
    buffer.writeObject(protocol);
  }
}

/// Phone Accept Call.
///
/// ID: `3bd2b4a0`.
class PhoneAcceptCall extends TlMethod<PhonePhoneCallBase> {
  /// Phone Accept Call constructor.
  const PhoneAcceptCall({
    required this.peer,
    required this.gB,
    required this.protocol,
  }) : super._();

  /// Deserialize.
  factory PhoneAcceptCall.deserialize(Uint8List buffer) {
    // final peer = _readInputPhoneCall(buffer);
    // final gB = _readbytes(buffer);
    // final protocol = _readPhoneCallProtocol(buffer);
    // final result = PhoneAcceptCall(peer: peer, gB: gB, protocol: protocol,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPhoneCallBase peer;

  /// G B.
  final Uint8List gB;

  /// Protocol.
  final PhoneCallProtocolBase protocol;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3bd2b4a0);
    buffer.writeObject(peer);
    buffer.writeBytes(gB);
    buffer.writeObject(protocol);
  }
}

/// Phone Confirm Call.
///
/// ID: `2efe1722`.
class PhoneConfirmCall extends TlMethod<PhonePhoneCallBase> {
  /// Phone Confirm Call constructor.
  const PhoneConfirmCall({
    required this.peer,
    required this.gA,
    required this.keyFingerprint,
    required this.protocol,
  }) : super._();

  /// Deserialize.
  factory PhoneConfirmCall.deserialize(Uint8List buffer) {
    // final peer = _readInputPhoneCall(buffer);
    // final gA = _readbytes(buffer);
    // final keyFingerprint = _readlong(buffer);
    // final protocol = _readPhoneCallProtocol(buffer);
    // final result = PhoneConfirmCall(peer: peer, gA: gA, keyFingerprint: keyFingerprint, protocol: protocol,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPhoneCallBase peer;

  /// G A.
  final Uint8List gA;

  /// Key Fingerprint.
  final int keyFingerprint;

  /// Protocol.
  final PhoneCallProtocolBase protocol;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2efe1722);
    buffer.writeObject(peer);
    buffer.writeBytes(gA);
    buffer.writeLong(keyFingerprint);
    buffer.writeObject(protocol);
  }
}

/// Phone Received Call.
///
/// ID: `17d54f61`.
class PhoneReceivedCall extends TlMethod<bool> {
  /// Phone Received Call constructor.
  const PhoneReceivedCall({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory PhoneReceivedCall.deserialize(Uint8List buffer) {
    // final peer = _readInputPhoneCall(buffer);
    // final result = PhoneReceivedCall(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPhoneCallBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x17d54f61);
    buffer.writeObject(peer);
  }
}

/// Phone Discard Call.
///
/// ID: `b2cbc1c0`.
class PhoneDiscardCall extends TlMethod<UpdatesBase> {
  /// Phone Discard Call constructor.
  const PhoneDiscardCall({
    required this.video,
    required this.peer,
    required this.duration,
    required this.reason,
    required this.connectionId,
  }) : super._();

  /// Deserialize.
  factory PhoneDiscardCall.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final video = _readflags.0?true(buffer);
    // final peer = _readInputPhoneCall(buffer);
    // final duration = _readint(buffer);
    // final reason = _readPhoneCallDiscardReason(buffer);
    // final connectionId = _readlong(buffer);
    // final result = PhoneDiscardCall(flags: flags, video: video, peer: peer, duration: duration, reason: reason, connectionId: connectionId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: video,
    );

    return v;
  }

  /// video: bit 0 of flags.0?true
  final bool video;

  /// Peer.
  final InputPhoneCallBase peer;

  /// Duration.
  final int duration;

  /// Reason.
  final PhoneCallDiscardReasonBase reason;

  /// Connection Id.
  final int connectionId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb2cbc1c0);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(duration);
    buffer.writeObject(reason);
    buffer.writeLong(connectionId);
  }
}

/// Phone Set Call Rating.
///
/// ID: `59ead627`.
class PhoneSetCallRating extends TlMethod<UpdatesBase> {
  /// Phone Set Call Rating constructor.
  const PhoneSetCallRating({
    required this.userInitiative,
    required this.peer,
    required this.rating,
    required this.comment,
  }) : super._();

  /// Deserialize.
  factory PhoneSetCallRating.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final userInitiative = _readflags.0?true(buffer);
    // final peer = _readInputPhoneCall(buffer);
    // final rating = _readint(buffer);
    // final comment = _readstring(buffer);
    // final result = PhoneSetCallRating(flags: flags, userInitiative: userInitiative, peer: peer, rating: rating, comment: comment,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: userInitiative,
    );

    return v;
  }

  /// user_initiative: bit 0 of flags.0?true
  final bool userInitiative;

  /// Peer.
  final InputPhoneCallBase peer;

  /// Rating.
  final int rating;

  /// Comment.
  final String comment;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x59ead627);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(rating);
    buffer.writeString(comment);
  }
}

/// Phone Save Call Debug.
///
/// ID: `277add7e`.
class PhoneSaveCallDebug extends TlMethod<bool> {
  /// Phone Save Call Debug constructor.
  const PhoneSaveCallDebug({
    required this.peer,
    required this.debug,
  }) : super._();

  /// Deserialize.
  factory PhoneSaveCallDebug.deserialize(Uint8List buffer) {
    // final peer = _readInputPhoneCall(buffer);
    // final debug = _readDataJSON(buffer);
    // final result = PhoneSaveCallDebug(peer: peer, debug: debug,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPhoneCallBase peer;

  /// Debug.
  final DataJSONBase debug;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x277add7e);
    buffer.writeObject(peer);
    buffer.writeObject(debug);
  }
}

/// Upload Get Cdn File.
///
/// ID: `395f69da`.
class UploadGetCdnFile extends TlMethod<UploadCdnFileBase> {
  /// Upload Get Cdn File constructor.
  const UploadGetCdnFile({
    required this.fileToken,
    required this.offset,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory UploadGetCdnFile.deserialize(Uint8List buffer) {
    // final fileToken = _readbytes(buffer);
    // final offset = _readlong(buffer);
    // final limit = _readint(buffer);
    // final result = UploadGetCdnFile(fileToken: fileToken, offset: offset, limit: limit,);

    // return result;

    throw Exception();
  }

  /// File Token.
  final Uint8List fileToken;

  /// Offset.
  final int offset;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x395f69da);
    buffer.writeBytes(fileToken);
    buffer.writeLong(offset);
    buffer.writeInt(limit);
  }
}

/// Upload Reupload Cdn File.
///
/// ID: `9b2754a8`.
class UploadReuploadCdnFile extends TlMethod<List<FileHashBase>> {
  /// Upload Reupload Cdn File constructor.
  const UploadReuploadCdnFile({
    required this.fileToken,
    required this.requestToken,
  }) : super._();

  /// Deserialize.
  factory UploadReuploadCdnFile.deserialize(Uint8List buffer) {
    // final fileToken = _readbytes(buffer);
    // final requestToken = _readbytes(buffer);
    // final result = UploadReuploadCdnFile(fileToken: fileToken, requestToken: requestToken,);

    // return result;

    throw Exception();
  }

  /// File Token.
  final Uint8List fileToken;

  /// Request Token.
  final Uint8List requestToken;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9b2754a8);
    buffer.writeBytes(fileToken);
    buffer.writeBytes(requestToken);
  }
}

/// Help Get Cdn Config.
///
/// ID: `52029342`.
class HelpGetCdnConfig extends TlMethod<CdnConfigBase> {
  /// Help Get Cdn Config constructor.
  const HelpGetCdnConfig() : super._();

  /// Deserialize.
  factory HelpGetCdnConfig.deserialize(Uint8List buffer) {
    // final result = HelpGetCdnConfig();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x52029342);
  }
}

/// Langpack Get Lang Pack.
///
/// ID: `f2f2330a`.
class LangpackGetLangPack extends TlMethod<LangPackDifferenceBase> {
  /// Langpack Get Lang Pack constructor.
  const LangpackGetLangPack({
    required this.langPack,
    required this.langCode,
  }) : super._();

  /// Deserialize.
  factory LangpackGetLangPack.deserialize(Uint8List buffer) {
    // final langPack = _readstring(buffer);
    // final langCode = _readstring(buffer);
    // final result = LangpackGetLangPack(langPack: langPack, langCode: langCode,);

    // return result;

    throw Exception();
  }

  /// Lang Pack.
  final String langPack;

  /// Lang Code.
  final String langCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf2f2330a);
    buffer.writeString(langPack);
    buffer.writeString(langCode);
  }
}

/// Langpack Get Strings.
///
/// ID: `efea3803`.
class LangpackGetStrings extends TlMethod<List<LangPackStringBase>> {
  /// Langpack Get Strings constructor.
  const LangpackGetStrings({
    required this.langPack,
    required this.langCode,
    required this.keys,
  }) : super._();

  /// Deserialize.
  factory LangpackGetStrings.deserialize(Uint8List buffer) {
    // final langPack = _readstring(buffer);
    // final langCode = _readstring(buffer);
    // final keys = _readVector<string>(buffer);
    // final result = LangpackGetStrings(langPack: langPack, langCode: langCode, keys: keys,);

    // return result;

    throw Exception();
  }

  /// Lang Pack.
  final String langPack;

  /// Lang Code.
  final String langCode;

  /// Keys.
  final List<String> keys;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xefea3803);
    buffer.writeString(langPack);
    buffer.writeString(langCode);
    buffer.writeVectorString(keys);
  }
}

/// Langpack Get Difference.
///
/// ID: `cd984aa5`.
class LangpackGetDifference extends TlMethod<LangPackDifferenceBase> {
  /// Langpack Get Difference constructor.
  const LangpackGetDifference({
    required this.langPack,
    required this.langCode,
    required this.fromVersion,
  }) : super._();

  /// Deserialize.
  factory LangpackGetDifference.deserialize(Uint8List buffer) {
    // final langPack = _readstring(buffer);
    // final langCode = _readstring(buffer);
    // final fromVersion = _readint(buffer);
    // final result = LangpackGetDifference(langPack: langPack, langCode: langCode, fromVersion: fromVersion,);

    // return result;

    throw Exception();
  }

  /// Lang Pack.
  final String langPack;

  /// Lang Code.
  final String langCode;

  /// From Version.
  final int fromVersion;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcd984aa5);
    buffer.writeString(langPack);
    buffer.writeString(langCode);
    buffer.writeInt(fromVersion);
  }
}

/// Langpack Get Languages.
///
/// ID: `42c6978f`.
class LangpackGetLanguages extends TlMethod<List<LangPackLanguageBase>> {
  /// Langpack Get Languages constructor.
  const LangpackGetLanguages({
    required this.langPack,
  }) : super._();

  /// Deserialize.
  factory LangpackGetLanguages.deserialize(Uint8List buffer) {
    // final langPack = _readstring(buffer);
    // final result = LangpackGetLanguages(langPack: langPack,);

    // return result;

    throw Exception();
  }

  /// Lang Pack.
  final String langPack;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x42c6978f);
    buffer.writeString(langPack);
  }
}

/// Channels Edit Banned.
///
/// ID: `96e6cd81`.
class ChannelsEditBanned extends TlMethod<UpdatesBase> {
  /// Channels Edit Banned constructor.
  const ChannelsEditBanned({
    required this.channel,
    required this.participant,
    required this.bannedRights,
  }) : super._();

  /// Deserialize.
  factory ChannelsEditBanned.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final participant = _readInputPeer(buffer);
    // final bannedRights = _readChatBannedRights(buffer);
    // final result = ChannelsEditBanned(channel: channel, participant: participant, bannedRights: bannedRights,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Participant.
  final InputPeerBase participant;

  /// Banned Rights.
  final ChatBannedRightsBase bannedRights;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x96e6cd81);
    buffer.writeObject(channel);
    buffer.writeObject(participant);
    buffer.writeObject(bannedRights);
  }
}

/// Channels Get Admin Log.
///
/// ID: `33ddf480`.
class ChannelsGetAdminLog extends TlMethod<ChannelsAdminLogResultsBase> {
  /// Channels Get Admin Log constructor.
  const ChannelsGetAdminLog({
    required this.channel,
    required this.q,
    this.eventsFilter,
    this.admins,
    required this.maxId,
    required this.minId,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory ChannelsGetAdminLog.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final channel = _readInputChannel(buffer);
    // final q = _readstring(buffer);
    // final eventsFilter = _readflags.0?ChannelAdminLogEventsFilter(buffer);
    // final admins = _readflags.1?Vector<InputUser>(buffer);
    // final maxId = _readlong(buffer);
    // final minId = _readlong(buffer);
    // final limit = _readint(buffer);
    // final result = ChannelsGetAdminLog(flags: flags, channel: channel, q: q, eventsFilter: eventsFilter, admins: admins, maxId: maxId, minId: minId, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: eventsFilter != null,
      b01: admins != null,
    );

    return v;
  }

  /// Channel.
  final InputChannelBase channel;

  /// Q.
  final String q;

  /// Events Filter.
  final ChannelAdminLogEventsFilterBase? eventsFilter;

  /// Admins.
  final List<InputUserBase>? admins;

  /// Max Id.
  final int maxId;

  /// Min Id.
  final int minId;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x33ddf480);
    buffer.writeInt(flags);
    buffer.writeObject(channel);
    buffer.writeString(q);
    final localEventsFilterCopy = eventsFilter;
    if (localEventsFilterCopy != null) {
      buffer.writeObject(localEventsFilterCopy);
    }
    final localAdminsCopy = admins;
    if (localAdminsCopy != null) {
      buffer.writeVectorObject(localAdminsCopy);
    }
    buffer.writeLong(maxId);
    buffer.writeLong(minId);
    buffer.writeInt(limit);
  }
}

/// Upload Get Cdn File Hashes.
///
/// ID: `91dc3f31`.
class UploadGetCdnFileHashes extends TlMethod<List<FileHashBase>> {
  /// Upload Get Cdn File Hashes constructor.
  const UploadGetCdnFileHashes({
    required this.fileToken,
    required this.offset,
  }) : super._();

  /// Deserialize.
  factory UploadGetCdnFileHashes.deserialize(Uint8List buffer) {
    // final fileToken = _readbytes(buffer);
    // final offset = _readlong(buffer);
    // final result = UploadGetCdnFileHashes(fileToken: fileToken, offset: offset,);

    // return result;

    throw Exception();
  }

  /// File Token.
  final Uint8List fileToken;

  /// Offset.
  final int offset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x91dc3f31);
    buffer.writeBytes(fileToken);
    buffer.writeLong(offset);
  }
}

/// Messages Send Screenshot Notification.
///
/// ID: `a1405817`.
class MessagesSendScreenshotNotification extends TlMethod<UpdatesBase> {
  /// Messages Send Screenshot Notification constructor.
  const MessagesSendScreenshotNotification({
    required this.peer,
    required this.replyTo,
    required this.randomId,
  }) : super._();

  /// Deserialize.
  factory MessagesSendScreenshotNotification.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final replyTo = _readInputReplyTo(buffer);
    // final randomId = _readlong(buffer);
    // final result = MessagesSendScreenshotNotification(peer: peer, replyTo: replyTo, randomId: randomId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Reply To.
  final InputReplyToBase replyTo;

  /// Random Id.
  final int randomId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa1405817);
    buffer.writeObject(peer);
    buffer.writeObject(replyTo);
    buffer.writeLong(randomId);
  }
}

/// Channels Set Stickers.
///
/// ID: `ea8ca4f9`.
class ChannelsSetStickers extends TlMethod<bool> {
  /// Channels Set Stickers constructor.
  const ChannelsSetStickers({
    required this.channel,
    required this.stickerset,
  }) : super._();

  /// Deserialize.
  factory ChannelsSetStickers.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final stickerset = _readInputStickerSet(buffer);
    // final result = ChannelsSetStickers(channel: channel, stickerset: stickerset,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Stickerset.
  final InputStickerSetBase stickerset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xea8ca4f9);
    buffer.writeObject(channel);
    buffer.writeObject(stickerset);
  }
}

/// Messages Get Faved Stickers.
///
/// ID: `04f1aaa9`.
class MessagesGetFavedStickers extends TlMethod<MessagesFavedStickersBase> {
  /// Messages Get Faved Stickers constructor.
  const MessagesGetFavedStickers({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetFavedStickers.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = MessagesGetFavedStickers(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x04f1aaa9);
    buffer.writeLong(hash);
  }
}

/// Messages Fave Sticker.
///
/// ID: `b9ffc55b`.
class MessagesFaveSticker extends TlMethod<bool> {
  /// Messages Fave Sticker constructor.
  const MessagesFaveSticker({
    required this.id,
    required this.unfave,
  }) : super._();

  /// Deserialize.
  factory MessagesFaveSticker.deserialize(Uint8List buffer) {
    // final id = _readInputDocument(buffer);
    // final unfave = _readBool(buffer);
    // final result = MessagesFaveSticker(id: id, unfave: unfave,);

    // return result;

    throw Exception();
  }

  /// Id.
  final InputDocumentBase id;

  /// Unfave.
  final bool unfave;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb9ffc55b);
    buffer.writeObject(id);
    buffer.writeBool(unfave);
  }
}

/// Channels Read Message Contents.
///
/// ID: `eab5dc38`.
class ChannelsReadMessageContents extends TlMethod<bool> {
  /// Channels Read Message Contents constructor.
  const ChannelsReadMessageContents({
    required this.channel,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory ChannelsReadMessageContents.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final id = _readVector<int>(buffer);
    // final result = ChannelsReadMessageContents(channel: channel, id: id,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xeab5dc38);
    buffer.writeObject(channel);
    buffer.writeVectorInt(id);
  }
}

/// Contacts Reset Saved.
///
/// ID: `879537f1`.
class ContactsResetSaved extends TlMethod<bool> {
  /// Contacts Reset Saved constructor.
  const ContactsResetSaved() : super._();

  /// Deserialize.
  factory ContactsResetSaved.deserialize(Uint8List buffer) {
    // final result = ContactsResetSaved();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x879537f1);
  }
}

/// Messages Get Unread Mentions.
///
/// ID: `f107e790`.
class MessagesGetUnreadMentions extends TlMethod<MessagesMessagesBase> {
  /// Messages Get Unread Mentions constructor.
  const MessagesGetUnreadMentions({
    required this.peer,
    this.topMsgId,
    required this.offsetId,
    required this.addOffset,
    required this.limit,
    required this.maxId,
    required this.minId,
  }) : super._();

  /// Deserialize.
  factory MessagesGetUnreadMentions.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final topMsgId = _readflags.0?int(buffer);
    // final offsetId = _readint(buffer);
    // final addOffset = _readint(buffer);
    // final limit = _readint(buffer);
    // final maxId = _readint(buffer);
    // final minId = _readint(buffer);
    // final result = MessagesGetUnreadMentions(flags: flags, peer: peer, topMsgId: topMsgId, offsetId: offsetId, addOffset: addOffset, limit: limit, maxId: maxId, minId: minId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: topMsgId != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Top Msg Id.
  final int? topMsgId;

  /// Offset Id.
  final int offsetId;

  /// Add Offset.
  final int addOffset;

  /// Limit.
  final int limit;

  /// Max Id.
  final int maxId;

  /// Min Id.
  final int minId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf107e790);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
    buffer.writeInt(offsetId);
    buffer.writeInt(addOffset);
    buffer.writeInt(limit);
    buffer.writeInt(maxId);
    buffer.writeInt(minId);
  }
}

/// Channels Delete History.
///
/// ID: `9baa9647`.
class ChannelsDeleteHistory extends TlMethod<UpdatesBase> {
  /// Channels Delete History constructor.
  const ChannelsDeleteHistory({
    required this.forEveryone,
    required this.channel,
    required this.maxId,
  }) : super._();

  /// Deserialize.
  factory ChannelsDeleteHistory.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final forEveryone = _readflags.0?true(buffer);
    // final channel = _readInputChannel(buffer);
    // final maxId = _readint(buffer);
    // final result = ChannelsDeleteHistory(flags: flags, forEveryone: forEveryone, channel: channel, maxId: maxId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: forEveryone,
    );

    return v;
  }

  /// for_everyone: bit 0 of flags.0?true
  final bool forEveryone;

  /// Channel.
  final InputChannelBase channel;

  /// Max Id.
  final int maxId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9baa9647);
    buffer.writeInt(flags);
    buffer.writeObject(channel);
    buffer.writeInt(maxId);
  }
}

/// Help Get Recent Me Urls.
///
/// ID: `3dc0f114`.
class HelpGetRecentMeUrls extends TlMethod<HelpRecentMeUrlsBase> {
  /// Help Get Recent Me Urls constructor.
  const HelpGetRecentMeUrls({
    required this.referer,
  }) : super._();

  /// Deserialize.
  factory HelpGetRecentMeUrls.deserialize(Uint8List buffer) {
    // final referer = _readstring(buffer);
    // final result = HelpGetRecentMeUrls(referer: referer,);

    // return result;

    throw Exception();
  }

  /// Referer.
  final String referer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3dc0f114);
    buffer.writeString(referer);
  }
}

/// Channels Toggle Pre History Hidden.
///
/// ID: `eabbb94c`.
class ChannelsTogglePreHistoryHidden extends TlMethod<UpdatesBase> {
  /// Channels Toggle Pre History Hidden constructor.
  const ChannelsTogglePreHistoryHidden({
    required this.channel,
    required this.enabled,
  }) : super._();

  /// Deserialize.
  factory ChannelsTogglePreHistoryHidden.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final enabled = _readBool(buffer);
    // final result = ChannelsTogglePreHistoryHidden(channel: channel, enabled: enabled,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Enabled.
  final bool enabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xeabbb94c);
    buffer.writeObject(channel);
    buffer.writeBool(enabled);
  }
}

/// Messages Read Mentions.
///
/// ID: `36e5bf4d`.
class MessagesReadMentions extends TlMethod<MessagesAffectedHistoryBase> {
  /// Messages Read Mentions constructor.
  const MessagesReadMentions({
    required this.peer,
    this.topMsgId,
  }) : super._();

  /// Deserialize.
  factory MessagesReadMentions.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final topMsgId = _readflags.0?int(buffer);
    // final result = MessagesReadMentions(flags: flags, peer: peer, topMsgId: topMsgId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: topMsgId != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Top Msg Id.
  final int? topMsgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x36e5bf4d);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
  }
}

/// Messages Get Recent Locations.
///
/// ID: `702a40e0`.
class MessagesGetRecentLocations extends TlMethod<MessagesMessagesBase> {
  /// Messages Get Recent Locations constructor.
  const MessagesGetRecentLocations({
    required this.peer,
    required this.limit,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetRecentLocations.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final limit = _readint(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesGetRecentLocations(peer: peer, limit: limit, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Limit.
  final int limit;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x702a40e0);
    buffer.writeObject(peer);
    buffer.writeInt(limit);
    buffer.writeLong(hash);
  }
}

/// Messages Send Multi Media.
///
/// ID: `456e8987`.
class MessagesSendMultiMedia extends TlMethod<UpdatesBase> {
  /// Messages Send Multi Media constructor.
  const MessagesSendMultiMedia({
    required this.silent,
    required this.background,
    required this.clearDraft,
    required this.noforwards,
    required this.updateStickersetsOrder,
    required this.invertMedia,
    required this.peer,
    this.replyTo,
    required this.multiMedia,
    this.scheduleDate,
    this.sendAs,
  }) : super._();

  /// Deserialize.
  factory MessagesSendMultiMedia.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final silent = _readflags.5?true(buffer);
    // final background = _readflags.6?true(buffer);
    // final clearDraft = _readflags.7?true(buffer);
    // final noforwards = _readflags.14?true(buffer);
    // final updateStickersetsOrder = _readflags.15?true(buffer);
    // final invertMedia = _readflags.16?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final replyTo = _readflags.0?InputReplyTo(buffer);
    // final multiMedia = _readVector<InputSingleMedia>(buffer);
    // final scheduleDate = _readflags.10?int(buffer);
    // final sendAs = _readflags.13?InputPeer(buffer);
    // final result = MessagesSendMultiMedia(flags: flags, silent: silent, background: background, clearDraft: clearDraft, noforwards: noforwards, updateStickersetsOrder: updateStickersetsOrder, invertMedia: invertMedia, peer: peer, replyTo: replyTo, multiMedia: multiMedia, scheduleDate: scheduleDate, sendAs: sendAs,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b05: silent,
      b06: background,
      b07: clearDraft,
      b14: noforwards,
      b15: updateStickersetsOrder,
      b16: invertMedia,
      b00: replyTo != null,
      b10: scheduleDate != null,
      b13: sendAs != null,
    );

    return v;
  }

  /// silent: bit 5 of flags.5?true
  final bool silent;

  /// background: bit 6 of flags.6?true
  final bool background;

  /// clear_draft: bit 7 of flags.7?true
  final bool clearDraft;

  /// noforwards: bit 14 of flags.14?true
  final bool noforwards;

  /// update_stickersets_order: bit 15 of flags.15?true
  final bool updateStickersetsOrder;

  /// invert_media: bit 16 of flags.16?true
  final bool invertMedia;

  /// Peer.
  final InputPeerBase peer;

  /// Reply To.
  final InputReplyToBase? replyTo;

  /// Multi Media.
  final List<InputSingleMediaBase> multiMedia;

  /// Schedule Date.
  final DateTime? scheduleDate;

  /// Send As.
  final InputPeerBase? sendAs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x456e8987);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localReplyToCopy = replyTo;
    if (localReplyToCopy != null) {
      buffer.writeObject(localReplyToCopy);
    }
    buffer.writeVectorObject(multiMedia);
    final localScheduleDateCopy = scheduleDate;
    if (localScheduleDateCopy != null) {
      buffer.writeDateTime(localScheduleDateCopy);
    }
    final localSendAsCopy = sendAs;
    if (localSendAsCopy != null) {
      buffer.writeObject(localSendAsCopy);
    }
  }
}

/// Messages Upload Encrypted File.
///
/// ID: `5057c497`.
class MessagesUploadEncryptedFile extends TlMethod<EncryptedFileBase> {
  /// Messages Upload Encrypted File constructor.
  const MessagesUploadEncryptedFile({
    required this.peer,
    required this.file,
  }) : super._();

  /// Deserialize.
  factory MessagesUploadEncryptedFile.deserialize(Uint8List buffer) {
    // final peer = _readInputEncryptedChat(buffer);
    // final file = _readInputEncryptedFile(buffer);
    // final result = MessagesUploadEncryptedFile(peer: peer, file: file,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputEncryptedChatBase peer;

  /// File.
  final InputEncryptedFileBase file;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5057c497);
    buffer.writeObject(peer);
    buffer.writeObject(file);
  }
}

/// Account Get Web Authorizations.
///
/// ID: `182e6d6f`.
class AccountGetWebAuthorizations
    extends TlMethod<AccountWebAuthorizationsBase> {
  /// Account Get Web Authorizations constructor.
  const AccountGetWebAuthorizations() : super._();

  /// Deserialize.
  factory AccountGetWebAuthorizations.deserialize(Uint8List buffer) {
    // final result = AccountGetWebAuthorizations();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x182e6d6f);
  }
}

/// Account Reset Web Authorization.
///
/// ID: `2d01b9ef`.
class AccountResetWebAuthorization extends TlMethod<bool> {
  /// Account Reset Web Authorization constructor.
  const AccountResetWebAuthorization({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory AccountResetWebAuthorization.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = AccountResetWebAuthorization(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2d01b9ef);
    buffer.writeLong(hash);
  }
}

/// Account Reset Web Authorizations.
///
/// ID: `682d2594`.
class AccountResetWebAuthorizations extends TlMethod<bool> {
  /// Account Reset Web Authorizations constructor.
  const AccountResetWebAuthorizations() : super._();

  /// Deserialize.
  factory AccountResetWebAuthorizations.deserialize(Uint8List buffer) {
    // final result = AccountResetWebAuthorizations();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x682d2594);
  }
}

/// Messages Search Sticker Sets.
///
/// ID: `35705b8a`.
class MessagesSearchStickerSets extends TlMethod<MessagesFoundStickerSetsBase> {
  /// Messages Search Sticker Sets constructor.
  const MessagesSearchStickerSets({
    required this.excludeFeatured,
    required this.q,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesSearchStickerSets.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final excludeFeatured = _readflags.0?true(buffer);
    // final q = _readstring(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesSearchStickerSets(flags: flags, excludeFeatured: excludeFeatured, q: q, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: excludeFeatured,
    );

    return v;
  }

  /// exclude_featured: bit 0 of flags.0?true
  final bool excludeFeatured;

  /// Q.
  final String q;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x35705b8a);
    buffer.writeInt(flags);
    buffer.writeString(q);
    buffer.writeLong(hash);
  }
}

/// Upload Get File Hashes.
///
/// ID: `9156982a`.
class UploadGetFileHashes extends TlMethod<List<FileHashBase>> {
  /// Upload Get File Hashes constructor.
  const UploadGetFileHashes({
    required this.location,
    required this.offset,
  }) : super._();

  /// Deserialize.
  factory UploadGetFileHashes.deserialize(Uint8List buffer) {
    // final location = _readInputFileLocation(buffer);
    // final offset = _readlong(buffer);
    // final result = UploadGetFileHashes(location: location, offset: offset,);

    // return result;

    throw Exception();
  }

  /// Location.
  final InputFileLocationBase location;

  /// Offset.
  final int offset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9156982a);
    buffer.writeObject(location);
    buffer.writeLong(offset);
  }
}

/// Help Get Terms Of Service Update.
///
/// ID: `2ca51fd1`.
class HelpGetTermsOfServiceUpdate
    extends TlMethod<HelpTermsOfServiceUpdateBase> {
  /// Help Get Terms Of Service Update constructor.
  const HelpGetTermsOfServiceUpdate() : super._();

  /// Deserialize.
  factory HelpGetTermsOfServiceUpdate.deserialize(Uint8List buffer) {
    // final result = HelpGetTermsOfServiceUpdate();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2ca51fd1);
  }
}

/// Help Accept Terms Of Service.
///
/// ID: `ee72f79a`.
class HelpAcceptTermsOfService extends TlMethod<bool> {
  /// Help Accept Terms Of Service constructor.
  const HelpAcceptTermsOfService({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory HelpAcceptTermsOfService.deserialize(Uint8List buffer) {
    // final id = _readDataJSON(buffer);
    // final result = HelpAcceptTermsOfService(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final DataJSONBase id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xee72f79a);
    buffer.writeObject(id);
  }
}

/// Account Get All Secure Values.
///
/// ID: `b288bc7d`.
class AccountGetAllSecureValues extends TlMethod<List<SecureValueBase>> {
  /// Account Get All Secure Values constructor.
  const AccountGetAllSecureValues() : super._();

  /// Deserialize.
  factory AccountGetAllSecureValues.deserialize(Uint8List buffer) {
    // final result = AccountGetAllSecureValues();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb288bc7d);
  }
}

/// Account Get Secure Value.
///
/// ID: `73665bc2`.
class AccountGetSecureValue extends TlMethod<List<SecureValueBase>> {
  /// Account Get Secure Value constructor.
  const AccountGetSecureValue({
    required this.types,
  }) : super._();

  /// Deserialize.
  factory AccountGetSecureValue.deserialize(Uint8List buffer) {
    // final types = _readVector<SecureValueType>(buffer);
    // final result = AccountGetSecureValue(types: types,);

    // return result;

    throw Exception();
  }

  /// Types.
  final List<SecureValueTypeBase> types;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x73665bc2);
    buffer.writeVectorObject(types);
  }
}

/// Account Save Secure Value.
///
/// ID: `899fe31d`.
class AccountSaveSecureValue extends TlMethod<SecureValueBase> {
  /// Account Save Secure Value constructor.
  const AccountSaveSecureValue({
    required this.value,
    required this.secureSecretId,
  }) : super._();

  /// Deserialize.
  factory AccountSaveSecureValue.deserialize(Uint8List buffer) {
    // final value = _readInputSecureValue(buffer);
    // final secureSecretId = _readlong(buffer);
    // final result = AccountSaveSecureValue(value: value, secureSecretId: secureSecretId,);

    // return result;

    throw Exception();
  }

  /// Value.
  final InputSecureValueBase value;

  /// Secure Secret Id.
  final int secureSecretId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x899fe31d);
    buffer.writeObject(value);
    buffer.writeLong(secureSecretId);
  }
}

/// Account Delete Secure Value.
///
/// ID: `b880bc4b`.
class AccountDeleteSecureValue extends TlMethod<bool> {
  /// Account Delete Secure Value constructor.
  const AccountDeleteSecureValue({
    required this.types,
  }) : super._();

  /// Deserialize.
  factory AccountDeleteSecureValue.deserialize(Uint8List buffer) {
    // final types = _readVector<SecureValueType>(buffer);
    // final result = AccountDeleteSecureValue(types: types,);

    // return result;

    throw Exception();
  }

  /// Types.
  final List<SecureValueTypeBase> types;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb880bc4b);
    buffer.writeVectorObject(types);
  }
}

/// Users Set Secure Value Errors.
///
/// ID: `90c894b5`.
class UsersSetSecureValueErrors extends TlMethod<bool> {
  /// Users Set Secure Value Errors constructor.
  const UsersSetSecureValueErrors({
    required this.id,
    required this.errors,
  }) : super._();

  /// Deserialize.
  factory UsersSetSecureValueErrors.deserialize(Uint8List buffer) {
    // final id = _readInputUser(buffer);
    // final errors = _readVector<SecureValueError>(buffer);
    // final result = UsersSetSecureValueErrors(id: id, errors: errors,);

    // return result;

    throw Exception();
  }

  /// Id.
  final InputUserBase id;

  /// Errors.
  final List<SecureValueErrorBase> errors;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x90c894b5);
    buffer.writeObject(id);
    buffer.writeVectorObject(errors);
  }
}

/// Account Get Authorization Form.
///
/// ID: `a929597a`.
class AccountGetAuthorizationForm
    extends TlMethod<AccountAuthorizationFormBase> {
  /// Account Get Authorization Form constructor.
  const AccountGetAuthorizationForm({
    required this.botId,
    required this.scope,
    required this.publicKey,
  }) : super._();

  /// Deserialize.
  factory AccountGetAuthorizationForm.deserialize(Uint8List buffer) {
    // final botId = _readlong(buffer);
    // final scope = _readstring(buffer);
    // final publicKey = _readstring(buffer);
    // final result = AccountGetAuthorizationForm(botId: botId, scope: scope, publicKey: publicKey,);

    // return result;

    throw Exception();
  }

  /// Bot Id.
  final int botId;

  /// Scope.
  final String scope;

  /// Public Key.
  final String publicKey;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa929597a);
    buffer.writeLong(botId);
    buffer.writeString(scope);
    buffer.writeString(publicKey);
  }
}

/// Account Accept Authorization.
///
/// ID: `f3ed4c73`.
class AccountAcceptAuthorization extends TlMethod<bool> {
  /// Account Accept Authorization constructor.
  const AccountAcceptAuthorization({
    required this.botId,
    required this.scope,
    required this.publicKey,
    required this.valueHashes,
    required this.credentials,
  }) : super._();

  /// Deserialize.
  factory AccountAcceptAuthorization.deserialize(Uint8List buffer) {
    // final botId = _readlong(buffer);
    // final scope = _readstring(buffer);
    // final publicKey = _readstring(buffer);
    // final valueHashes = _readVector<SecureValueHash>(buffer);
    // final credentials = _readSecureCredentialsEncrypted(buffer);
    // final result = AccountAcceptAuthorization(botId: botId, scope: scope, publicKey: publicKey, valueHashes: valueHashes, credentials: credentials,);

    // return result;

    throw Exception();
  }

  /// Bot Id.
  final int botId;

  /// Scope.
  final String scope;

  /// Public Key.
  final String publicKey;

  /// Value Hashes.
  final List<SecureValueHashBase> valueHashes;

  /// Credentials.
  final SecureCredentialsEncryptedBase credentials;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf3ed4c73);
    buffer.writeLong(botId);
    buffer.writeString(scope);
    buffer.writeString(publicKey);
    buffer.writeVectorObject(valueHashes);
    buffer.writeObject(credentials);
  }
}

/// Account Send Verify Phone Code.
///
/// ID: `a5a356f9`.
class AccountSendVerifyPhoneCode extends TlMethod<AuthSentCodeBase> {
  /// Account Send Verify Phone Code constructor.
  const AccountSendVerifyPhoneCode({
    required this.phoneNumber,
    required this.settings,
  }) : super._();

  /// Deserialize.
  factory AccountSendVerifyPhoneCode.deserialize(Uint8List buffer) {
    // final phoneNumber = _readstring(buffer);
    // final settings = _readCodeSettings(buffer);
    // final result = AccountSendVerifyPhoneCode(phoneNumber: phoneNumber, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Phone Number.
  final String phoneNumber;

  /// Settings.
  final CodeSettingsBase settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa5a356f9);
    buffer.writeString(phoneNumber);
    buffer.writeObject(settings);
  }
}

/// Account Verify Phone.
///
/// ID: `4dd3a7f6`.
class AccountVerifyPhone extends TlMethod<bool> {
  /// Account Verify Phone constructor.
  const AccountVerifyPhone({
    required this.phoneNumber,
    required this.phoneCodeHash,
    required this.phoneCode,
  }) : super._();

  /// Deserialize.
  factory AccountVerifyPhone.deserialize(Uint8List buffer) {
    // final phoneNumber = _readstring(buffer);
    // final phoneCodeHash = _readstring(buffer);
    // final phoneCode = _readstring(buffer);
    // final result = AccountVerifyPhone(phoneNumber: phoneNumber, phoneCodeHash: phoneCodeHash, phoneCode: phoneCode,);

    // return result;

    throw Exception();
  }

  /// Phone Number.
  final String phoneNumber;

  /// Phone Code Hash.
  final String phoneCodeHash;

  /// Phone Code.
  final String phoneCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4dd3a7f6);
    buffer.writeString(phoneNumber);
    buffer.writeString(phoneCodeHash);
    buffer.writeString(phoneCode);
  }
}

/// Account Send Verify Email Code.
///
/// ID: `98e037bb`.
class AccountSendVerifyEmailCode extends TlMethod<AccountSentEmailCodeBase> {
  /// Account Send Verify Email Code constructor.
  const AccountSendVerifyEmailCode({
    required this.purpose,
    required this.email,
  }) : super._();

  /// Deserialize.
  factory AccountSendVerifyEmailCode.deserialize(Uint8List buffer) {
    // final purpose = _readEmailVerifyPurpose(buffer);
    // final email = _readstring(buffer);
    // final result = AccountSendVerifyEmailCode(purpose: purpose, email: email,);

    // return result;

    throw Exception();
  }

  /// Purpose.
  final EmailVerifyPurposeBase purpose;

  /// Email.
  final String email;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x98e037bb);
    buffer.writeObject(purpose);
    buffer.writeString(email);
  }
}

/// Account Verify Email.
///
/// ID: `032da4cf`.
class AccountVerifyEmail extends TlMethod<AccountEmailVerifiedBase> {
  /// Account Verify Email constructor.
  const AccountVerifyEmail({
    required this.purpose,
    required this.verification,
  }) : super._();

  /// Deserialize.
  factory AccountVerifyEmail.deserialize(Uint8List buffer) {
    // final purpose = _readEmailVerifyPurpose(buffer);
    // final verification = _readEmailVerification(buffer);
    // final result = AccountVerifyEmail(purpose: purpose, verification: verification,);

    // return result;

    throw Exception();
  }

  /// Purpose.
  final EmailVerifyPurposeBase purpose;

  /// Verification.
  final EmailVerificationBase verification;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x032da4cf);
    buffer.writeObject(purpose);
    buffer.writeObject(verification);
  }
}

/// Help Get Deep Link Info.
///
/// ID: `3fedc75f`.
class HelpGetDeepLinkInfo extends TlMethod<HelpDeepLinkInfoBase> {
  /// Help Get Deep Link Info constructor.
  const HelpGetDeepLinkInfo({
    required this.path,
  }) : super._();

  /// Deserialize.
  factory HelpGetDeepLinkInfo.deserialize(Uint8List buffer) {
    // final path = _readstring(buffer);
    // final result = HelpGetDeepLinkInfo(path: path,);

    // return result;

    throw Exception();
  }

  /// Path.
  final String path;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3fedc75f);
    buffer.writeString(path);
  }
}

/// Contacts Get Saved.
///
/// ID: `82f1e39f`.
class ContactsGetSaved extends TlMethod<List<SavedContactBase>> {
  /// Contacts Get Saved constructor.
  const ContactsGetSaved() : super._();

  /// Deserialize.
  factory ContactsGetSaved.deserialize(Uint8List buffer) {
    // final result = ContactsGetSaved();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x82f1e39f);
  }
}

/// Channels Get Left Channels.
///
/// ID: `8341ecc0`.
class ChannelsGetLeftChannels extends TlMethod<MessagesChatsBase> {
  /// Channels Get Left Channels constructor.
  const ChannelsGetLeftChannels({
    required this.offset,
  }) : super._();

  /// Deserialize.
  factory ChannelsGetLeftChannels.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final result = ChannelsGetLeftChannels(offset: offset,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8341ecc0);
    buffer.writeInt(offset);
  }
}

/// Account Init Takeout Session.
///
/// ID: `8ef3eab0`.
class AccountInitTakeoutSession extends TlMethod<AccountTakeoutBase> {
  /// Account Init Takeout Session constructor.
  const AccountInitTakeoutSession({
    required this.contacts,
    required this.messageUsers,
    required this.messageChats,
    required this.messageMegagroups,
    required this.messageChannels,
    required this.files,
    this.fileMaxSize,
  }) : super._();

  /// Deserialize.
  factory AccountInitTakeoutSession.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final contacts = _readflags.0?true(buffer);
    // final messageUsers = _readflags.1?true(buffer);
    // final messageChats = _readflags.2?true(buffer);
    // final messageMegagroups = _readflags.3?true(buffer);
    // final messageChannels = _readflags.4?true(buffer);
    // final files = _readflags.5?true(buffer);
    // final fileMaxSize = _readflags.5?long(buffer);
    // final result = AccountInitTakeoutSession(flags: flags, contacts: contacts, messageUsers: messageUsers, messageChats: messageChats, messageMegagroups: messageMegagroups, messageChannels: messageChannels, files: files, fileMaxSize: fileMaxSize,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: contacts,
      b01: messageUsers,
      b02: messageChats,
      b03: messageMegagroups,
      b04: messageChannels,
      b05: files || fileMaxSize != null,
    );

    return v;
  }

  /// contacts: bit 0 of flags.0?true
  final bool contacts;

  /// message_users: bit 1 of flags.1?true
  final bool messageUsers;

  /// message_chats: bit 2 of flags.2?true
  final bool messageChats;

  /// message_megagroups: bit 3 of flags.3?true
  final bool messageMegagroups;

  /// message_channels: bit 4 of flags.4?true
  final bool messageChannels;

  /// files: bit 5 of flags.5?true
  final bool files;

  /// File Max Size.
  final int? fileMaxSize;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8ef3eab0);
    buffer.writeInt(flags);
    final localFileMaxSizeCopy = fileMaxSize;
    if (localFileMaxSizeCopy != null) {
      buffer.writeLong(localFileMaxSizeCopy);
    }
  }
}

/// Account Finish Takeout Session.
///
/// ID: `1d2652ee`.
class AccountFinishTakeoutSession extends TlMethod<bool> {
  /// Account Finish Takeout Session constructor.
  const AccountFinishTakeoutSession({
    required this.success,
  }) : super._();

  /// Deserialize.
  factory AccountFinishTakeoutSession.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final success = _readflags.0?true(buffer);
    // final result = AccountFinishTakeoutSession(flags: flags, success: success,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: success,
    );

    return v;
  }

  /// success: bit 0 of flags.0?true
  final bool success;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1d2652ee);
    buffer.writeInt(flags);
  }
}

/// Messages Get Split Ranges.
///
/// ID: `1cff7e08`.
class MessagesGetSplitRanges extends TlMethod<List<MessageRangeBase>> {
  /// Messages Get Split Ranges constructor.
  const MessagesGetSplitRanges() : super._();

  /// Deserialize.
  factory MessagesGetSplitRanges.deserialize(Uint8List buffer) {
    // final result = MessagesGetSplitRanges();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1cff7e08);
  }
}

/// Invoke With Messages Range.
///
/// ID: `365275f2`.
class InvokeWithMessagesRange<X> extends TlMethod<X> {
  /// Invoke With Messages Range constructor.
  const InvokeWithMessagesRange({
    required this.range,
    required this.query,
  }) : super._();

  /// Deserialize.
  factory InvokeWithMessagesRange.deserialize(Uint8List buffer) {
    // final range = _readMessageRange(buffer);
    // final query = _read!X(buffer);
    // final result = InvokeWithMessagesRange(range: range, query: query,);

    // return result;

    throw Exception();
  }

  /// Range.
  final MessageRangeBase range;

  /// Query.
  final TlMethod<X> query;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x365275f2);
    buffer.writeObject(range);
    buffer.writeObject(query);
  }
}

/// Invoke With Takeout.
///
/// ID: `aca9fd2e`.
class InvokeWithTakeout<X> extends TlMethod<X> {
  /// Invoke With Takeout constructor.
  const InvokeWithTakeout({
    required this.takeoutId,
    required this.query,
  }) : super._();

  /// Deserialize.
  factory InvokeWithTakeout.deserialize(Uint8List buffer) {
    // final takeoutId = _readlong(buffer);
    // final query = _read!X(buffer);
    // final result = InvokeWithTakeout(takeoutId: takeoutId, query: query,);

    // return result;

    throw Exception();
  }

  /// Takeout Id.
  final int takeoutId;

  /// Query.
  final TlMethod<X> query;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xaca9fd2e);
    buffer.writeLong(takeoutId);
    buffer.writeObject(query);
  }
}

/// Messages Mark Dialog Unread.
///
/// ID: `c286d98f`.
class MessagesMarkDialogUnread extends TlMethod<bool> {
  /// Messages Mark Dialog Unread constructor.
  const MessagesMarkDialogUnread({
    required this.unread,
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory MessagesMarkDialogUnread.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final unread = _readflags.0?true(buffer);
    // final peer = _readInputDialogPeer(buffer);
    // final result = MessagesMarkDialogUnread(flags: flags, unread: unread, peer: peer,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: unread,
    );

    return v;
  }

  /// unread: bit 0 of flags.0?true
  final bool unread;

  /// Peer.
  final InputDialogPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc286d98f);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
  }
}

/// Messages Get Dialog Unread Marks.
///
/// ID: `22e24e22`.
class MessagesGetDialogUnreadMarks extends TlMethod<List<DialogPeerBase>> {
  /// Messages Get Dialog Unread Marks constructor.
  const MessagesGetDialogUnreadMarks() : super._();

  /// Deserialize.
  factory MessagesGetDialogUnreadMarks.deserialize(Uint8List buffer) {
    // final result = MessagesGetDialogUnreadMarks();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x22e24e22);
  }
}

/// Contacts Toggle Top Peers.
///
/// ID: `8514bdda`.
class ContactsToggleTopPeers extends TlMethod<bool> {
  /// Contacts Toggle Top Peers constructor.
  const ContactsToggleTopPeers({
    required this.enabled,
  }) : super._();

  /// Deserialize.
  factory ContactsToggleTopPeers.deserialize(Uint8List buffer) {
    // final enabled = _readBool(buffer);
    // final result = ContactsToggleTopPeers(enabled: enabled,);

    // return result;

    throw Exception();
  }

  /// Enabled.
  final bool enabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8514bdda);
    buffer.writeBool(enabled);
  }
}

/// Messages Clear All Drafts.
///
/// ID: `7e58ee9c`.
class MessagesClearAllDrafts extends TlMethod<bool> {
  /// Messages Clear All Drafts constructor.
  const MessagesClearAllDrafts() : super._();

  /// Deserialize.
  factory MessagesClearAllDrafts.deserialize(Uint8List buffer) {
    // final result = MessagesClearAllDrafts();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7e58ee9c);
  }
}

/// Help Get App Config.
///
/// ID: `61e3f854`.
class HelpGetAppConfig extends TlMethod<HelpAppConfigBase> {
  /// Help Get App Config constructor.
  const HelpGetAppConfig({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory HelpGetAppConfig.deserialize(Uint8List buffer) {
    // final hash = _readint(buffer);
    // final result = HelpGetAppConfig(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x61e3f854);
    buffer.writeInt(hash);
  }
}

/// Help Save App Log.
///
/// ID: `6f02f748`.
class HelpSaveAppLog extends TlMethod<bool> {
  /// Help Save App Log constructor.
  const HelpSaveAppLog({
    required this.events,
  }) : super._();

  /// Deserialize.
  factory HelpSaveAppLog.deserialize(Uint8List buffer) {
    // final events = _readVector<InputAppEvent>(buffer);
    // final result = HelpSaveAppLog(events: events,);

    // return result;

    throw Exception();
  }

  /// Events.
  final List<InputAppEventBase> events;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6f02f748);
    buffer.writeVectorObject(events);
  }
}

/// Help Get Passport Config.
///
/// ID: `c661ad08`.
class HelpGetPassportConfig extends TlMethod<HelpPassportConfigBase> {
  /// Help Get Passport Config constructor.
  const HelpGetPassportConfig({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory HelpGetPassportConfig.deserialize(Uint8List buffer) {
    // final hash = _readint(buffer);
    // final result = HelpGetPassportConfig(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc661ad08);
    buffer.writeInt(hash);
  }
}

/// Langpack Get Language.
///
/// ID: `6a596502`.
class LangpackGetLanguage extends TlMethod<LangPackLanguageBase> {
  /// Langpack Get Language constructor.
  const LangpackGetLanguage({
    required this.langPack,
    required this.langCode,
  }) : super._();

  /// Deserialize.
  factory LangpackGetLanguage.deserialize(Uint8List buffer) {
    // final langPack = _readstring(buffer);
    // final langCode = _readstring(buffer);
    // final result = LangpackGetLanguage(langPack: langPack, langCode: langCode,);

    // return result;

    throw Exception();
  }

  /// Lang Pack.
  final String langPack;

  /// Lang Code.
  final String langCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6a596502);
    buffer.writeString(langPack);
    buffer.writeString(langCode);
  }
}

/// Messages Update Pinned Message.
///
/// ID: `d2aaf7ec`.
class MessagesUpdatePinnedMessage extends TlMethod<UpdatesBase> {
  /// Messages Update Pinned Message constructor.
  const MessagesUpdatePinnedMessage({
    required this.silent,
    required this.unpin,
    required this.pmOneside,
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesUpdatePinnedMessage.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final silent = _readflags.0?true(buffer);
    // final unpin = _readflags.1?true(buffer);
    // final pmOneside = _readflags.2?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final result = MessagesUpdatePinnedMessage(flags: flags, silent: silent, unpin: unpin, pmOneside: pmOneside, peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: silent,
      b01: unpin,
      b02: pmOneside,
    );

    return v;
  }

  /// silent: bit 0 of flags.0?true
  final bool silent;

  /// unpin: bit 1 of flags.1?true
  final bool unpin;

  /// pm_oneside: bit 2 of flags.2?true
  final bool pmOneside;

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd2aaf7ec);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(id);
  }
}

/// Account Confirm Password Email.
///
/// ID: `8fdf1920`.
class AccountConfirmPasswordEmail extends TlMethod<bool> {
  /// Account Confirm Password Email constructor.
  const AccountConfirmPasswordEmail({
    required this.code,
  }) : super._();

  /// Deserialize.
  factory AccountConfirmPasswordEmail.deserialize(Uint8List buffer) {
    // final code = _readstring(buffer);
    // final result = AccountConfirmPasswordEmail(code: code,);

    // return result;

    throw Exception();
  }

  /// Code.
  final String code;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8fdf1920);
    buffer.writeString(code);
  }
}

/// Account Resend Password Email.
///
/// ID: `7a7f2a15`.
class AccountResendPasswordEmail extends TlMethod<bool> {
  /// Account Resend Password Email constructor.
  const AccountResendPasswordEmail() : super._();

  /// Deserialize.
  factory AccountResendPasswordEmail.deserialize(Uint8List buffer) {
    // final result = AccountResendPasswordEmail();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7a7f2a15);
  }
}

/// Account Cancel Password Email.
///
/// ID: `c1cbd5b6`.
class AccountCancelPasswordEmail extends TlMethod<bool> {
  /// Account Cancel Password Email constructor.
  const AccountCancelPasswordEmail() : super._();

  /// Deserialize.
  factory AccountCancelPasswordEmail.deserialize(Uint8List buffer) {
    // final result = AccountCancelPasswordEmail();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc1cbd5b6);
  }
}

/// Help Get Support Name.
///
/// ID: `d360e72c`.
class HelpGetSupportName extends TlMethod<HelpSupportNameBase> {
  /// Help Get Support Name constructor.
  const HelpGetSupportName() : super._();

  /// Deserialize.
  factory HelpGetSupportName.deserialize(Uint8List buffer) {
    // final result = HelpGetSupportName();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd360e72c);
  }
}

/// Help Get User Info.
///
/// ID: `038a08d3`.
class HelpGetUserInfo extends TlMethod<HelpUserInfoBase> {
  /// Help Get User Info constructor.
  const HelpGetUserInfo({
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory HelpGetUserInfo.deserialize(Uint8List buffer) {
    // final userId = _readInputUser(buffer);
    // final result = HelpGetUserInfo(userId: userId,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final InputUserBase userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x038a08d3);
    buffer.writeObject(userId);
  }
}

/// Help Edit User Info.
///
/// ID: `66b91b70`.
class HelpEditUserInfo extends TlMethod<HelpUserInfoBase> {
  /// Help Edit User Info constructor.
  const HelpEditUserInfo({
    required this.userId,
    required this.message,
    required this.entities,
  }) : super._();

  /// Deserialize.
  factory HelpEditUserInfo.deserialize(Uint8List buffer) {
    // final userId = _readInputUser(buffer);
    // final message = _readstring(buffer);
    // final entities = _readVector<MessageEntity>(buffer);
    // final result = HelpEditUserInfo(userId: userId, message: message, entities: entities,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final InputUserBase userId;

  /// Message.
  final String message;

  /// Entities.
  final List<MessageEntityBase> entities;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x66b91b70);
    buffer.writeObject(userId);
    buffer.writeString(message);
    buffer.writeVectorObject(entities);
  }
}

/// Account Get Contact Sign Up Notification.
///
/// ID: `9f07c728`.
class AccountGetContactSignUpNotification extends TlMethod<bool> {
  /// Account Get Contact Sign Up Notification constructor.
  const AccountGetContactSignUpNotification() : super._();

  /// Deserialize.
  factory AccountGetContactSignUpNotification.deserialize(Uint8List buffer) {
    // final result = AccountGetContactSignUpNotification();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9f07c728);
  }
}

/// Account Set Contact Sign Up Notification.
///
/// ID: `cff43f61`.
class AccountSetContactSignUpNotification extends TlMethod<bool> {
  /// Account Set Contact Sign Up Notification constructor.
  const AccountSetContactSignUpNotification({
    required this.silent,
  }) : super._();

  /// Deserialize.
  factory AccountSetContactSignUpNotification.deserialize(Uint8List buffer) {
    // final silent = _readBool(buffer);
    // final result = AccountSetContactSignUpNotification(silent: silent,);

    // return result;

    throw Exception();
  }

  /// Silent.
  final bool silent;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcff43f61);
    buffer.writeBool(silent);
  }
}

/// Account Get Notify Exceptions.
///
/// ID: `53577479`.
class AccountGetNotifyExceptions extends TlMethod<UpdatesBase> {
  /// Account Get Notify Exceptions constructor.
  const AccountGetNotifyExceptions({
    required this.compareSound,
    required this.compareStories,
    this.peer,
  }) : super._();

  /// Deserialize.
  factory AccountGetNotifyExceptions.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final compareSound = _readflags.1?true(buffer);
    // final compareStories = _readflags.2?true(buffer);
    // final peer = _readflags.0?InputNotifyPeer(buffer);
    // final result = AccountGetNotifyExceptions(flags: flags, compareSound: compareSound, compareStories: compareStories, peer: peer,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: compareSound,
      b02: compareStories,
      b00: peer != null,
    );

    return v;
  }

  /// compare_sound: bit 1 of flags.1?true
  final bool compareSound;

  /// compare_stories: bit 2 of flags.2?true
  final bool compareStories;

  /// Peer.
  final InputNotifyPeerBase? peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x53577479);
    buffer.writeInt(flags);
    final localPeerCopy = peer;
    if (localPeerCopy != null) {
      buffer.writeObject(localPeerCopy);
    }
  }
}

/// Messages Send Vote.
///
/// ID: `10ea6184`.
class MessagesSendVote extends TlMethod<UpdatesBase> {
  /// Messages Send Vote constructor.
  const MessagesSendVote({
    required this.peer,
    required this.msgId,
    required this.options,
  }) : super._();

  /// Deserialize.
  factory MessagesSendVote.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final options = _readVector<bytes>(buffer);
    // final result = MessagesSendVote(peer: peer, msgId: msgId, options: options,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Options.
  final List<Uint8List> options;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x10ea6184);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeVectorBytes(options);
  }
}

/// Messages Get Poll Results.
///
/// ID: `73bb643b`.
class MessagesGetPollResults extends TlMethod<UpdatesBase> {
  /// Messages Get Poll Results constructor.
  const MessagesGetPollResults({
    required this.peer,
    required this.msgId,
  }) : super._();

  /// Deserialize.
  factory MessagesGetPollResults.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final result = MessagesGetPollResults(peer: peer, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x73bb643b);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
  }
}

/// Messages Get Onlines.
///
/// ID: `6e2be050`.
class MessagesGetOnlines extends TlMethod<ChatOnlinesBase> {
  /// Messages Get Onlines constructor.
  const MessagesGetOnlines({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory MessagesGetOnlines.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = MessagesGetOnlines(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6e2be050);
    buffer.writeObject(peer);
  }
}

/// Messages Edit Chat About.
///
/// ID: `def60797`.
class MessagesEditChatAbout extends TlMethod<bool> {
  /// Messages Edit Chat About constructor.
  const MessagesEditChatAbout({
    required this.peer,
    required this.about,
  }) : super._();

  /// Deserialize.
  factory MessagesEditChatAbout.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final about = _readstring(buffer);
    // final result = MessagesEditChatAbout(peer: peer, about: about,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// About.
  final String about;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdef60797);
    buffer.writeObject(peer);
    buffer.writeString(about);
  }
}

/// Messages Edit Chat Default Banned Rights.
///
/// ID: `a5866b41`.
class MessagesEditChatDefaultBannedRights extends TlMethod<UpdatesBase> {
  /// Messages Edit Chat Default Banned Rights constructor.
  const MessagesEditChatDefaultBannedRights({
    required this.peer,
    required this.bannedRights,
  }) : super._();

  /// Deserialize.
  factory MessagesEditChatDefaultBannedRights.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final bannedRights = _readChatBannedRights(buffer);
    // final result = MessagesEditChatDefaultBannedRights(peer: peer, bannedRights: bannedRights,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Banned Rights.
  final ChatBannedRightsBase bannedRights;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa5866b41);
    buffer.writeObject(peer);
    buffer.writeObject(bannedRights);
  }
}

/// Account Get Wall Paper.
///
/// ID: `fc8ddbea`.
class AccountGetWallPaper extends TlMethod<WallPaperBase> {
  /// Account Get Wall Paper constructor.
  const AccountGetWallPaper({
    required this.wallpaper,
  }) : super._();

  /// Deserialize.
  factory AccountGetWallPaper.deserialize(Uint8List buffer) {
    // final wallpaper = _readInputWallPaper(buffer);
    // final result = AccountGetWallPaper(wallpaper: wallpaper,);

    // return result;

    throw Exception();
  }

  /// Wallpaper.
  final InputWallPaperBase wallpaper;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfc8ddbea);
    buffer.writeObject(wallpaper);
  }
}

/// Account Upload Wall Paper.
///
/// ID: `e39a8f03`.
class AccountUploadWallPaper extends TlMethod<WallPaperBase> {
  /// Account Upload Wall Paper constructor.
  const AccountUploadWallPaper({
    required this.forChat,
    required this.file,
    required this.mimeType,
    required this.settings,
  }) : super._();

  /// Deserialize.
  factory AccountUploadWallPaper.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final forChat = _readflags.0?true(buffer);
    // final file = _readInputFile(buffer);
    // final mimeType = _readstring(buffer);
    // final settings = _readWallPaperSettings(buffer);
    // final result = AccountUploadWallPaper(flags: flags, forChat: forChat, file: file, mimeType: mimeType, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: forChat,
    );

    return v;
  }

  /// for_chat: bit 0 of flags.0?true
  final bool forChat;

  /// File.
  final InputFileBase file;

  /// Mime Type.
  final String mimeType;

  /// Settings.
  final WallPaperSettingsBase settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe39a8f03);
    buffer.writeInt(flags);
    buffer.writeObject(file);
    buffer.writeString(mimeType);
    buffer.writeObject(settings);
  }
}

/// Account Save Wall Paper.
///
/// ID: `6c5a5b37`.
class AccountSaveWallPaper extends TlMethod<bool> {
  /// Account Save Wall Paper constructor.
  const AccountSaveWallPaper({
    required this.wallpaper,
    required this.unsave,
    required this.settings,
  }) : super._();

  /// Deserialize.
  factory AccountSaveWallPaper.deserialize(Uint8List buffer) {
    // final wallpaper = _readInputWallPaper(buffer);
    // final unsave = _readBool(buffer);
    // final settings = _readWallPaperSettings(buffer);
    // final result = AccountSaveWallPaper(wallpaper: wallpaper, unsave: unsave, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Wallpaper.
  final InputWallPaperBase wallpaper;

  /// Unsave.
  final bool unsave;

  /// Settings.
  final WallPaperSettingsBase settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6c5a5b37);
    buffer.writeObject(wallpaper);
    buffer.writeBool(unsave);
    buffer.writeObject(settings);
  }
}

/// Account Install Wall Paper.
///
/// ID: `feed5769`.
class AccountInstallWallPaper extends TlMethod<bool> {
  /// Account Install Wall Paper constructor.
  const AccountInstallWallPaper({
    required this.wallpaper,
    required this.settings,
  }) : super._();

  /// Deserialize.
  factory AccountInstallWallPaper.deserialize(Uint8List buffer) {
    // final wallpaper = _readInputWallPaper(buffer);
    // final settings = _readWallPaperSettings(buffer);
    // final result = AccountInstallWallPaper(wallpaper: wallpaper, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Wallpaper.
  final InputWallPaperBase wallpaper;

  /// Settings.
  final WallPaperSettingsBase settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfeed5769);
    buffer.writeObject(wallpaper);
    buffer.writeObject(settings);
  }
}

/// Account Reset Wall Papers.
///
/// ID: `bb3b9804`.
class AccountResetWallPapers extends TlMethod<bool> {
  /// Account Reset Wall Papers constructor.
  const AccountResetWallPapers() : super._();

  /// Deserialize.
  factory AccountResetWallPapers.deserialize(Uint8List buffer) {
    // final result = AccountResetWallPapers();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbb3b9804);
  }
}

/// Account Get Auto Download Settings.
///
/// ID: `56da0b3f`.
class AccountGetAutoDownloadSettings
    extends TlMethod<AccountAutoDownloadSettingsBase> {
  /// Account Get Auto Download Settings constructor.
  const AccountGetAutoDownloadSettings() : super._();

  /// Deserialize.
  factory AccountGetAutoDownloadSettings.deserialize(Uint8List buffer) {
    // final result = AccountGetAutoDownloadSettings();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x56da0b3f);
  }
}

/// Account Save Auto Download Settings.
///
/// ID: `76f36233`.
class AccountSaveAutoDownloadSettings extends TlMethod<bool> {
  /// Account Save Auto Download Settings constructor.
  const AccountSaveAutoDownloadSettings({
    required this.low,
    required this.high,
    required this.settings,
  }) : super._();

  /// Deserialize.
  factory AccountSaveAutoDownloadSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final low = _readflags.0?true(buffer);
    // final high = _readflags.1?true(buffer);
    // final settings = _readAutoDownloadSettings(buffer);
    // final result = AccountSaveAutoDownloadSettings(flags: flags, low: low, high: high, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: low,
      b01: high,
    );

    return v;
  }

  /// low: bit 0 of flags.0?true
  final bool low;

  /// high: bit 1 of flags.1?true
  final bool high;

  /// Settings.
  final AutoDownloadSettingsBase settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x76f36233);
    buffer.writeInt(flags);
    buffer.writeObject(settings);
  }
}

/// Messages Get Emoji Keywords.
///
/// ID: `35a0e062`.
class MessagesGetEmojiKeywords extends TlMethod<EmojiKeywordsDifferenceBase> {
  /// Messages Get Emoji Keywords constructor.
  const MessagesGetEmojiKeywords({
    required this.langCode,
  }) : super._();

  /// Deserialize.
  factory MessagesGetEmojiKeywords.deserialize(Uint8List buffer) {
    // final langCode = _readstring(buffer);
    // final result = MessagesGetEmojiKeywords(langCode: langCode,);

    // return result;

    throw Exception();
  }

  /// Lang Code.
  final String langCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x35a0e062);
    buffer.writeString(langCode);
  }
}

/// Messages Get Emoji Keywords Difference.
///
/// ID: `1508b6af`.
class MessagesGetEmojiKeywordsDifference
    extends TlMethod<EmojiKeywordsDifferenceBase> {
  /// Messages Get Emoji Keywords Difference constructor.
  const MessagesGetEmojiKeywordsDifference({
    required this.langCode,
    required this.fromVersion,
  }) : super._();

  /// Deserialize.
  factory MessagesGetEmojiKeywordsDifference.deserialize(Uint8List buffer) {
    // final langCode = _readstring(buffer);
    // final fromVersion = _readint(buffer);
    // final result = MessagesGetEmojiKeywordsDifference(langCode: langCode, fromVersion: fromVersion,);

    // return result;

    throw Exception();
  }

  /// Lang Code.
  final String langCode;

  /// From Version.
  final int fromVersion;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1508b6af);
    buffer.writeString(langCode);
    buffer.writeInt(fromVersion);
  }
}

/// Messages Get Emoji Keywords Languages.
///
/// ID: `4e9963b2`.
class MessagesGetEmojiKeywordsLanguages
    extends TlMethod<List<EmojiLanguageBase>> {
  /// Messages Get Emoji Keywords Languages constructor.
  const MessagesGetEmojiKeywordsLanguages({
    required this.langCodes,
  }) : super._();

  /// Deserialize.
  factory MessagesGetEmojiKeywordsLanguages.deserialize(Uint8List buffer) {
    // final langCodes = _readVector<string>(buffer);
    // final result = MessagesGetEmojiKeywordsLanguages(langCodes: langCodes,);

    // return result;

    throw Exception();
  }

  /// Lang Codes.
  final List<String> langCodes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4e9963b2);
    buffer.writeVectorString(langCodes);
  }
}

/// Messages Get Emoji U R L.
///
/// ID: `d5b10c26`.
class MessagesGetEmojiURL extends TlMethod<EmojiURLBase> {
  /// Messages Get Emoji U R L constructor.
  const MessagesGetEmojiURL({
    required this.langCode,
  }) : super._();

  /// Deserialize.
  factory MessagesGetEmojiURL.deserialize(Uint8List buffer) {
    // final langCode = _readstring(buffer);
    // final result = MessagesGetEmojiURL(langCode: langCode,);

    // return result;

    throw Exception();
  }

  /// Lang Code.
  final String langCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd5b10c26);
    buffer.writeString(langCode);
  }
}

/// Folders Edit Peer Folders.
///
/// ID: `6847d0ab`.
class FoldersEditPeerFolders extends TlMethod<UpdatesBase> {
  /// Folders Edit Peer Folders constructor.
  const FoldersEditPeerFolders({
    required this.folderPeers,
  }) : super._();

  /// Deserialize.
  factory FoldersEditPeerFolders.deserialize(Uint8List buffer) {
    // final folderPeers = _readVector<InputFolderPeer>(buffer);
    // final result = FoldersEditPeerFolders(folderPeers: folderPeers,);

    // return result;

    throw Exception();
  }

  /// Folder Peers.
  final List<InputFolderPeerBase> folderPeers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6847d0ab);
    buffer.writeVectorObject(folderPeers);
  }
}

/// Messages Get Search Counters.
///
/// ID: `1bbcf300`.
class MessagesGetSearchCounters
    extends TlMethod<List<MessagesSearchCounterBase>> {
  /// Messages Get Search Counters constructor.
  const MessagesGetSearchCounters({
    required this.peer,
    this.savedPeerId,
    this.topMsgId,
    required this.filters,
  }) : super._();

  /// Deserialize.
  factory MessagesGetSearchCounters.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final savedPeerId = _readflags.2?InputPeer(buffer);
    // final topMsgId = _readflags.0?int(buffer);
    // final filters = _readVector<MessagesFilter>(buffer);
    // final result = MessagesGetSearchCounters(flags: flags, peer: peer, savedPeerId: savedPeerId, topMsgId: topMsgId, filters: filters,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: savedPeerId != null,
      b00: topMsgId != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Saved Peer Id.
  final InputPeerBase? savedPeerId;

  /// Top Msg Id.
  final int? topMsgId;

  /// Filters.
  final List<MessagesFilterBase> filters;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1bbcf300);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localSavedPeerIdCopy = savedPeerId;
    if (localSavedPeerIdCopy != null) {
      buffer.writeObject(localSavedPeerIdCopy);
    }
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
    buffer.writeVectorObject(filters);
  }
}

/// Channels Get Groups For Discussion.
///
/// ID: `f5dad378`.
class ChannelsGetGroupsForDiscussion extends TlMethod<MessagesChatsBase> {
  /// Channels Get Groups For Discussion constructor.
  const ChannelsGetGroupsForDiscussion() : super._();

  /// Deserialize.
  factory ChannelsGetGroupsForDiscussion.deserialize(Uint8List buffer) {
    // final result = ChannelsGetGroupsForDiscussion();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf5dad378);
  }
}

/// Channels Set Discussion Group.
///
/// ID: `40582bb2`.
class ChannelsSetDiscussionGroup extends TlMethod<bool> {
  /// Channels Set Discussion Group constructor.
  const ChannelsSetDiscussionGroup({
    required this.broadcast,
    required this.group,
  }) : super._();

  /// Deserialize.
  factory ChannelsSetDiscussionGroup.deserialize(Uint8List buffer) {
    // final broadcast = _readInputChannel(buffer);
    // final group = _readInputChannel(buffer);
    // final result = ChannelsSetDiscussionGroup(broadcast: broadcast, group: group,);

    // return result;

    throw Exception();
  }

  /// Broadcast.
  final InputChannelBase broadcast;

  /// Group.
  final InputChannelBase group;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x40582bb2);
    buffer.writeObject(broadcast);
    buffer.writeObject(group);
  }
}

/// Messages Request Url Auth.
///
/// ID: `198fb446`.
class MessagesRequestUrlAuth extends TlMethod<UrlAuthResultBase> {
  /// Messages Request Url Auth constructor.
  const MessagesRequestUrlAuth({
    this.peer,
    this.msgId,
    this.buttonId,
    this.url,
  }) : super._();

  /// Deserialize.
  factory MessagesRequestUrlAuth.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readflags.1?InputPeer(buffer);
    // final msgId = _readflags.1?int(buffer);
    // final buttonId = _readflags.1?int(buffer);
    // final url = _readflags.2?string(buffer);
    // final result = MessagesRequestUrlAuth(flags: flags, peer: peer, msgId: msgId, buttonId: buttonId, url: url,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: peer != null || msgId != null || buttonId != null,
      b02: url != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase? peer;

  /// Msg Id.
  final int? msgId;

  /// Button Id.
  final int? buttonId;

  /// Url.
  final String? url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x198fb446);
    buffer.writeInt(flags);
    final localPeerCopy = peer;
    if (localPeerCopy != null) {
      buffer.writeObject(localPeerCopy);
    }
    final localMsgIdCopy = msgId;
    if (localMsgIdCopy != null) {
      buffer.writeInt(localMsgIdCopy);
    }
    final localButtonIdCopy = buttonId;
    if (localButtonIdCopy != null) {
      buffer.writeInt(localButtonIdCopy);
    }
    final localUrlCopy = url;
    if (localUrlCopy != null) {
      buffer.writeString(localUrlCopy);
    }
  }
}

/// Messages Accept Url Auth.
///
/// ID: `b12c7125`.
class MessagesAcceptUrlAuth extends TlMethod<UrlAuthResultBase> {
  /// Messages Accept Url Auth constructor.
  const MessagesAcceptUrlAuth({
    required this.writeAllowed,
    this.peer,
    this.msgId,
    this.buttonId,
    this.url,
  }) : super._();

  /// Deserialize.
  factory MessagesAcceptUrlAuth.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final writeAllowed = _readflags.0?true(buffer);
    // final peer = _readflags.1?InputPeer(buffer);
    // final msgId = _readflags.1?int(buffer);
    // final buttonId = _readflags.1?int(buffer);
    // final url = _readflags.2?string(buffer);
    // final result = MessagesAcceptUrlAuth(flags: flags, writeAllowed: writeAllowed, peer: peer, msgId: msgId, buttonId: buttonId, url: url,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: writeAllowed,
      b01: peer != null || msgId != null || buttonId != null,
      b02: url != null,
    );

    return v;
  }

  /// write_allowed: bit 0 of flags.0?true
  final bool writeAllowed;

  /// Peer.
  final InputPeerBase? peer;

  /// Msg Id.
  final int? msgId;

  /// Button Id.
  final int? buttonId;

  /// Url.
  final String? url;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb12c7125);
    buffer.writeInt(flags);
    final localPeerCopy = peer;
    if (localPeerCopy != null) {
      buffer.writeObject(localPeerCopy);
    }
    final localMsgIdCopy = msgId;
    if (localMsgIdCopy != null) {
      buffer.writeInt(localMsgIdCopy);
    }
    final localButtonIdCopy = buttonId;
    if (localButtonIdCopy != null) {
      buffer.writeInt(localButtonIdCopy);
    }
    final localUrlCopy = url;
    if (localUrlCopy != null) {
      buffer.writeString(localUrlCopy);
    }
  }
}

/// Messages Hide Peer Settings Bar.
///
/// ID: `4facb138`.
class MessagesHidePeerSettingsBar extends TlMethod<bool> {
  /// Messages Hide Peer Settings Bar constructor.
  const MessagesHidePeerSettingsBar({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory MessagesHidePeerSettingsBar.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = MessagesHidePeerSettingsBar(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4facb138);
    buffer.writeObject(peer);
  }
}

/// Contacts Add Contact.
///
/// ID: `e8f463d0`.
class ContactsAddContact extends TlMethod<UpdatesBase> {
  /// Contacts Add Contact constructor.
  const ContactsAddContact({
    required this.addPhonePrivacyException,
    required this.id,
    required this.firstName,
    required this.lastName,
    required this.phone,
  }) : super._();

  /// Deserialize.
  factory ContactsAddContact.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final addPhonePrivacyException = _readflags.0?true(buffer);
    // final id = _readInputUser(buffer);
    // final firstName = _readstring(buffer);
    // final lastName = _readstring(buffer);
    // final phone = _readstring(buffer);
    // final result = ContactsAddContact(flags: flags, addPhonePrivacyException: addPhonePrivacyException, id: id, firstName: firstName, lastName: lastName, phone: phone,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: addPhonePrivacyException,
    );

    return v;
  }

  /// add_phone_privacy_exception: bit 0 of flags.0?true
  final bool addPhonePrivacyException;

  /// Id.
  final InputUserBase id;

  /// First Name.
  final String firstName;

  /// Last Name.
  final String lastName;

  /// Phone.
  final String phone;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe8f463d0);
    buffer.writeInt(flags);
    buffer.writeObject(id);
    buffer.writeString(firstName);
    buffer.writeString(lastName);
    buffer.writeString(phone);
  }
}

/// Contacts Accept Contact.
///
/// ID: `f831a20f`.
class ContactsAcceptContact extends TlMethod<UpdatesBase> {
  /// Contacts Accept Contact constructor.
  const ContactsAcceptContact({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory ContactsAcceptContact.deserialize(Uint8List buffer) {
    // final id = _readInputUser(buffer);
    // final result = ContactsAcceptContact(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final InputUserBase id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf831a20f);
    buffer.writeObject(id);
  }
}

/// Channels Edit Creator.
///
/// ID: `8f38cd1f`.
class ChannelsEditCreator extends TlMethod<UpdatesBase> {
  /// Channels Edit Creator constructor.
  const ChannelsEditCreator({
    required this.channel,
    required this.userId,
    required this.password,
  }) : super._();

  /// Deserialize.
  factory ChannelsEditCreator.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final userId = _readInputUser(buffer);
    // final password = _readInputCheckPasswordSRP(buffer);
    // final result = ChannelsEditCreator(channel: channel, userId: userId, password: password,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// User Id.
  final InputUserBase userId;

  /// Password.
  final InputCheckPasswordSRPBase password;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8f38cd1f);
    buffer.writeObject(channel);
    buffer.writeObject(userId);
    buffer.writeObject(password);
  }
}

/// Contacts Get Located.
///
/// ID: `d348bc44`.
class ContactsGetLocated extends TlMethod<UpdatesBase> {
  /// Contacts Get Located constructor.
  const ContactsGetLocated({
    required this.background,
    required this.geoPoint,
    this.selfExpires,
  }) : super._();

  /// Deserialize.
  factory ContactsGetLocated.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final background = _readflags.1?true(buffer);
    // final geoPoint = _readInputGeoPoint(buffer);
    // final selfExpires = _readflags.0?int(buffer);
    // final result = ContactsGetLocated(flags: flags, background: background, geoPoint: geoPoint, selfExpires: selfExpires,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: background,
      b00: selfExpires != null,
    );

    return v;
  }

  /// background: bit 1 of flags.1?true
  final bool background;

  /// Geo Point.
  final InputGeoPointBase geoPoint;

  /// Self Expires.
  final int? selfExpires;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd348bc44);
    buffer.writeInt(flags);
    buffer.writeObject(geoPoint);
    final localSelfExpiresCopy = selfExpires;
    if (localSelfExpiresCopy != null) {
      buffer.writeInt(localSelfExpiresCopy);
    }
  }
}

/// Channels Edit Location.
///
/// ID: `58e63f6d`.
class ChannelsEditLocation extends TlMethod<bool> {
  /// Channels Edit Location constructor.
  const ChannelsEditLocation({
    required this.channel,
    required this.geoPoint,
    required this.address,
  }) : super._();

  /// Deserialize.
  factory ChannelsEditLocation.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final geoPoint = _readInputGeoPoint(buffer);
    // final address = _readstring(buffer);
    // final result = ChannelsEditLocation(channel: channel, geoPoint: geoPoint, address: address,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Geo Point.
  final InputGeoPointBase geoPoint;

  /// Address.
  final String address;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x58e63f6d);
    buffer.writeObject(channel);
    buffer.writeObject(geoPoint);
    buffer.writeString(address);
  }
}

/// Channels Toggle Slow Mode.
///
/// ID: `edd49ef0`.
class ChannelsToggleSlowMode extends TlMethod<UpdatesBase> {
  /// Channels Toggle Slow Mode constructor.
  const ChannelsToggleSlowMode({
    required this.channel,
    required this.seconds,
  }) : super._();

  /// Deserialize.
  factory ChannelsToggleSlowMode.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final seconds = _readint(buffer);
    // final result = ChannelsToggleSlowMode(channel: channel, seconds: seconds,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Seconds.
  final int seconds;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xedd49ef0);
    buffer.writeObject(channel);
    buffer.writeInt(seconds);
  }
}

/// Messages Get Scheduled History.
///
/// ID: `f516760b`.
class MessagesGetScheduledHistory extends TlMethod<MessagesMessagesBase> {
  /// Messages Get Scheduled History constructor.
  const MessagesGetScheduledHistory({
    required this.peer,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetScheduledHistory.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesGetScheduledHistory(peer: peer, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf516760b);
    buffer.writeObject(peer);
    buffer.writeLong(hash);
  }
}

/// Messages Get Scheduled Messages.
///
/// ID: `bdbb0464`.
class MessagesGetScheduledMessages extends TlMethod<MessagesMessagesBase> {
  /// Messages Get Scheduled Messages constructor.
  const MessagesGetScheduledMessages({
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesGetScheduledMessages.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final result = MessagesGetScheduledMessages(peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbdbb0464);
    buffer.writeObject(peer);
    buffer.writeVectorInt(id);
  }
}

/// Messages Send Scheduled Messages.
///
/// ID: `bd38850a`.
class MessagesSendScheduledMessages extends TlMethod<UpdatesBase> {
  /// Messages Send Scheduled Messages constructor.
  const MessagesSendScheduledMessages({
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesSendScheduledMessages.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final result = MessagesSendScheduledMessages(peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbd38850a);
    buffer.writeObject(peer);
    buffer.writeVectorInt(id);
  }
}

/// Messages Delete Scheduled Messages.
///
/// ID: `59ae2b16`.
class MessagesDeleteScheduledMessages extends TlMethod<UpdatesBase> {
  /// Messages Delete Scheduled Messages constructor.
  const MessagesDeleteScheduledMessages({
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesDeleteScheduledMessages.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final result = MessagesDeleteScheduledMessages(peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x59ae2b16);
    buffer.writeObject(peer);
    buffer.writeVectorInt(id);
  }
}

/// Account Upload Theme.
///
/// ID: `1c3db333`.
class AccountUploadTheme extends TlMethod<DocumentBase> {
  /// Account Upload Theme constructor.
  const AccountUploadTheme({
    required this.file,
    this.thumb,
    required this.fileName,
    required this.mimeType,
  }) : super._();

  /// Deserialize.
  factory AccountUploadTheme.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final file = _readInputFile(buffer);
    // final thumb = _readflags.0?InputFile(buffer);
    // final fileName = _readstring(buffer);
    // final mimeType = _readstring(buffer);
    // final result = AccountUploadTheme(flags: flags, file: file, thumb: thumb, fileName: fileName, mimeType: mimeType,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: thumb != null,
    );

    return v;
  }

  /// File.
  final InputFileBase file;

  /// Thumb.
  final InputFileBase? thumb;

  /// File Name.
  final String fileName;

  /// Mime Type.
  final String mimeType;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1c3db333);
    buffer.writeInt(flags);
    buffer.writeObject(file);
    final localThumbCopy = thumb;
    if (localThumbCopy != null) {
      buffer.writeObject(localThumbCopy);
    }
    buffer.writeString(fileName);
    buffer.writeString(mimeType);
  }
}

/// Account Create Theme.
///
/// ID: `652e4400`.
class AccountCreateTheme extends TlMethod<ThemeBase> {
  /// Account Create Theme constructor.
  const AccountCreateTheme({
    required this.slug,
    required this.title,
    this.document,
    this.settings,
  }) : super._();

  /// Deserialize.
  factory AccountCreateTheme.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final slug = _readstring(buffer);
    // final title = _readstring(buffer);
    // final document = _readflags.2?InputDocument(buffer);
    // final settings = _readflags.3?Vector<InputThemeSettings>(buffer);
    // final result = AccountCreateTheme(flags: flags, slug: slug, title: title, document: document, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: document != null,
      b03: settings != null,
    );

    return v;
  }

  /// Slug.
  final String slug;

  /// Title.
  final String title;

  /// Document.
  final InputDocumentBase? document;

  /// Settings.
  final List<InputThemeSettingsBase>? settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x652e4400);
    buffer.writeInt(flags);
    buffer.writeString(slug);
    buffer.writeString(title);
    final localDocumentCopy = document;
    if (localDocumentCopy != null) {
      buffer.writeObject(localDocumentCopy);
    }
    final localSettingsCopy = settings;
    if (localSettingsCopy != null) {
      buffer.writeVectorObject(localSettingsCopy);
    }
  }
}

/// Account Update Theme.
///
/// ID: `2bf40ccc`.
class AccountUpdateTheme extends TlMethod<ThemeBase> {
  /// Account Update Theme constructor.
  const AccountUpdateTheme({
    required this.format,
    required this.theme,
    this.slug,
    this.title,
    this.document,
    this.settings,
  }) : super._();

  /// Deserialize.
  factory AccountUpdateTheme.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final format = _readstring(buffer);
    // final theme = _readInputTheme(buffer);
    // final slug = _readflags.0?string(buffer);
    // final title = _readflags.1?string(buffer);
    // final document = _readflags.2?InputDocument(buffer);
    // final settings = _readflags.3?Vector<InputThemeSettings>(buffer);
    // final result = AccountUpdateTheme(flags: flags, format: format, theme: theme, slug: slug, title: title, document: document, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: slug != null,
      b01: title != null,
      b02: document != null,
      b03: settings != null,
    );

    return v;
  }

  /// Format.
  final String format;

  /// Theme.
  final InputThemeBase theme;

  /// Slug.
  final String? slug;

  /// Title.
  final String? title;

  /// Document.
  final InputDocumentBase? document;

  /// Settings.
  final List<InputThemeSettingsBase>? settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2bf40ccc);
    buffer.writeInt(flags);
    buffer.writeString(format);
    buffer.writeObject(theme);
    final localSlugCopy = slug;
    if (localSlugCopy != null) {
      buffer.writeString(localSlugCopy);
    }
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localDocumentCopy = document;
    if (localDocumentCopy != null) {
      buffer.writeObject(localDocumentCopy);
    }
    final localSettingsCopy = settings;
    if (localSettingsCopy != null) {
      buffer.writeVectorObject(localSettingsCopy);
    }
  }
}

/// Account Save Theme.
///
/// ID: `f257106c`.
class AccountSaveTheme extends TlMethod<bool> {
  /// Account Save Theme constructor.
  const AccountSaveTheme({
    required this.theme,
    required this.unsave,
  }) : super._();

  /// Deserialize.
  factory AccountSaveTheme.deserialize(Uint8List buffer) {
    // final theme = _readInputTheme(buffer);
    // final unsave = _readBool(buffer);
    // final result = AccountSaveTheme(theme: theme, unsave: unsave,);

    // return result;

    throw Exception();
  }

  /// Theme.
  final InputThemeBase theme;

  /// Unsave.
  final bool unsave;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf257106c);
    buffer.writeObject(theme);
    buffer.writeBool(unsave);
  }
}

/// Account Install Theme.
///
/// ID: `c727bb3b`.
class AccountInstallTheme extends TlMethod<bool> {
  /// Account Install Theme constructor.
  const AccountInstallTheme({
    required this.dark,
    this.theme,
    this.format,
    this.baseTheme,
  }) : super._();

  /// Deserialize.
  factory AccountInstallTheme.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final dark = _readflags.0?true(buffer);
    // final theme = _readflags.1?InputTheme(buffer);
    // final format = _readflags.2?string(buffer);
    // final baseTheme = _readflags.3?BaseTheme(buffer);
    // final result = AccountInstallTheme(flags: flags, dark: dark, theme: theme, format: format, baseTheme: baseTheme,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: dark,
      b01: theme != null,
      b02: format != null,
      b03: baseTheme != null,
    );

    return v;
  }

  /// dark: bit 0 of flags.0?true
  final bool dark;

  /// Theme.
  final InputThemeBase? theme;

  /// Format.
  final String? format;

  /// Base Theme.
  final BaseThemeBase? baseTheme;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc727bb3b);
    buffer.writeInt(flags);
    final localThemeCopy = theme;
    if (localThemeCopy != null) {
      buffer.writeObject(localThemeCopy);
    }
    final localFormatCopy = format;
    if (localFormatCopy != null) {
      buffer.writeString(localFormatCopy);
    }
    final localBaseThemeCopy = baseTheme;
    if (localBaseThemeCopy != null) {
      buffer.writeObject(localBaseThemeCopy);
    }
  }
}

/// Account Get Theme.
///
/// ID: `3a5869ec`.
class AccountGetTheme extends TlMethod<ThemeBase> {
  /// Account Get Theme constructor.
  const AccountGetTheme({
    required this.format,
    required this.theme,
  }) : super._();

  /// Deserialize.
  factory AccountGetTheme.deserialize(Uint8List buffer) {
    // final format = _readstring(buffer);
    // final theme = _readInputTheme(buffer);
    // final result = AccountGetTheme(format: format, theme: theme,);

    // return result;

    throw Exception();
  }

  /// Format.
  final String format;

  /// Theme.
  final InputThemeBase theme;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3a5869ec);
    buffer.writeString(format);
    buffer.writeObject(theme);
  }
}

/// Account Get Themes.
///
/// ID: `7206e458`.
class AccountGetThemes extends TlMethod<AccountThemesBase> {
  /// Account Get Themes constructor.
  const AccountGetThemes({
    required this.format,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory AccountGetThemes.deserialize(Uint8List buffer) {
    // final format = _readstring(buffer);
    // final hash = _readlong(buffer);
    // final result = AccountGetThemes(format: format, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Format.
  final String format;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7206e458);
    buffer.writeString(format);
    buffer.writeLong(hash);
  }
}

/// Auth Export Login Token.
///
/// ID: `b7e085fe`.
class AuthExportLoginToken extends TlMethod<AuthLoginTokenBase> {
  /// Auth Export Login Token constructor.
  const AuthExportLoginToken({
    required this.apiId,
    required this.apiHash,
    required this.exceptIds,
  }) : super._();

  /// Deserialize.
  factory AuthExportLoginToken.deserialize(Uint8List buffer) {
    // final apiId = _readint(buffer);
    // final apiHash = _readstring(buffer);
    // final exceptIds = _readVector<long>(buffer);
    // final result = AuthExportLoginToken(apiId: apiId, apiHash: apiHash, exceptIds: exceptIds,);

    // return result;

    throw Exception();
  }

  /// Api Id.
  final int apiId;

  /// Api Hash.
  final String apiHash;

  /// Except Ids.
  final List<int> exceptIds;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb7e085fe);
    buffer.writeInt(apiId);
    buffer.writeString(apiHash);
    buffer.writeVectorLong(exceptIds);
  }
}

/// Auth Import Login Token.
///
/// ID: `95ac5ce4`.
class AuthImportLoginToken extends TlMethod<AuthLoginTokenBase> {
  /// Auth Import Login Token constructor.
  const AuthImportLoginToken({
    required this.token,
  }) : super._();

  /// Deserialize.
  factory AuthImportLoginToken.deserialize(Uint8List buffer) {
    // final token = _readbytes(buffer);
    // final result = AuthImportLoginToken(token: token,);

    // return result;

    throw Exception();
  }

  /// Token.
  final Uint8List token;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x95ac5ce4);
    buffer.writeBytes(token);
  }
}

/// Auth Accept Login Token.
///
/// ID: `e894ad4d`.
class AuthAcceptLoginToken extends TlMethod<AuthorizationBase> {
  /// Auth Accept Login Token constructor.
  const AuthAcceptLoginToken({
    required this.token,
  }) : super._();

  /// Deserialize.
  factory AuthAcceptLoginToken.deserialize(Uint8List buffer) {
    // final token = _readbytes(buffer);
    // final result = AuthAcceptLoginToken(token: token,);

    // return result;

    throw Exception();
  }

  /// Token.
  final Uint8List token;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe894ad4d);
    buffer.writeBytes(token);
  }
}

/// Account Set Content Settings.
///
/// ID: `b574b16b`.
class AccountSetContentSettings extends TlMethod<bool> {
  /// Account Set Content Settings constructor.
  const AccountSetContentSettings({
    required this.sensitiveEnabled,
  }) : super._();

  /// Deserialize.
  factory AccountSetContentSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final sensitiveEnabled = _readflags.0?true(buffer);
    // final result = AccountSetContentSettings(flags: flags, sensitiveEnabled: sensitiveEnabled,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: sensitiveEnabled,
    );

    return v;
  }

  /// sensitive_enabled: bit 0 of flags.0?true
  final bool sensitiveEnabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb574b16b);
    buffer.writeInt(flags);
  }
}

/// Account Get Content Settings.
///
/// ID: `8b9b4dae`.
class AccountGetContentSettings extends TlMethod<AccountContentSettingsBase> {
  /// Account Get Content Settings constructor.
  const AccountGetContentSettings() : super._();

  /// Deserialize.
  factory AccountGetContentSettings.deserialize(Uint8List buffer) {
    // final result = AccountGetContentSettings();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8b9b4dae);
  }
}

/// Channels Get Inactive Channels.
///
/// ID: `11e831ee`.
class ChannelsGetInactiveChannels extends TlMethod<MessagesInactiveChatsBase> {
  /// Channels Get Inactive Channels constructor.
  const ChannelsGetInactiveChannels() : super._();

  /// Deserialize.
  factory ChannelsGetInactiveChannels.deserialize(Uint8List buffer) {
    // final result = ChannelsGetInactiveChannels();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x11e831ee);
  }
}

/// Account Get Multi Wall Papers.
///
/// ID: `65ad71dc`.
class AccountGetMultiWallPapers extends TlMethod<List<WallPaperBase>> {
  /// Account Get Multi Wall Papers constructor.
  const AccountGetMultiWallPapers({
    required this.wallpapers,
  }) : super._();

  /// Deserialize.
  factory AccountGetMultiWallPapers.deserialize(Uint8List buffer) {
    // final wallpapers = _readVector<InputWallPaper>(buffer);
    // final result = AccountGetMultiWallPapers(wallpapers: wallpapers,);

    // return result;

    throw Exception();
  }

  /// Wallpapers.
  final List<InputWallPaperBase> wallpapers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x65ad71dc);
    buffer.writeVectorObject(wallpapers);
  }
}

/// Messages Get Poll Votes.
///
/// ID: `b86e380e`.
class MessagesGetPollVotes extends TlMethod<MessagesVotesListBase> {
  /// Messages Get Poll Votes constructor.
  const MessagesGetPollVotes({
    required this.peer,
    required this.id,
    this.option,
    this.offset,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory MessagesGetPollVotes.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final option = _readflags.0?bytes(buffer);
    // final offset = _readflags.1?string(buffer);
    // final limit = _readint(buffer);
    // final result = MessagesGetPollVotes(flags: flags, peer: peer, id: id, option: option, offset: offset, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: option != null,
      b01: offset != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// Option.
  final Uint8List? option;

  /// Offset.
  final String? offset;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb86e380e);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(id);
    final localOptionCopy = option;
    if (localOptionCopy != null) {
      buffer.writeBytes(localOptionCopy);
    }
    final localOffsetCopy = offset;
    if (localOffsetCopy != null) {
      buffer.writeString(localOffsetCopy);
    }
    buffer.writeInt(limit);
  }
}

/// Messages Toggle Sticker Sets.
///
/// ID: `b5052fea`.
class MessagesToggleStickerSets extends TlMethod<bool> {
  /// Messages Toggle Sticker Sets constructor.
  const MessagesToggleStickerSets({
    required this.uninstall,
    required this.archive,
    required this.unarchive,
    required this.stickersets,
  }) : super._();

  /// Deserialize.
  factory MessagesToggleStickerSets.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final uninstall = _readflags.0?true(buffer);
    // final archive = _readflags.1?true(buffer);
    // final unarchive = _readflags.2?true(buffer);
    // final stickersets = _readVector<InputStickerSet>(buffer);
    // final result = MessagesToggleStickerSets(flags: flags, uninstall: uninstall, archive: archive, unarchive: unarchive, stickersets: stickersets,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: uninstall,
      b01: archive,
      b02: unarchive,
    );

    return v;
  }

  /// uninstall: bit 0 of flags.0?true
  final bool uninstall;

  /// archive: bit 1 of flags.1?true
  final bool archive;

  /// unarchive: bit 2 of flags.2?true
  final bool unarchive;

  /// Stickersets.
  final List<InputStickerSetBase> stickersets;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb5052fea);
    buffer.writeInt(flags);
    buffer.writeVectorObject(stickersets);
  }
}

/// Payments Get Bank Card Data.
///
/// ID: `2e79d779`.
class PaymentsGetBankCardData extends TlMethod<PaymentsBankCardDataBase> {
  /// Payments Get Bank Card Data constructor.
  const PaymentsGetBankCardData({
    required this.number,
  }) : super._();

  /// Deserialize.
  factory PaymentsGetBankCardData.deserialize(Uint8List buffer) {
    // final number = _readstring(buffer);
    // final result = PaymentsGetBankCardData(number: number,);

    // return result;

    throw Exception();
  }

  /// Number.
  final String number;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2e79d779);
    buffer.writeString(number);
  }
}

/// Messages Get Dialog Filters.
///
/// ID: `f19ed96d`.
class MessagesGetDialogFilters extends TlMethod<List<DialogFilterBase>> {
  /// Messages Get Dialog Filters constructor.
  const MessagesGetDialogFilters() : super._();

  /// Deserialize.
  factory MessagesGetDialogFilters.deserialize(Uint8List buffer) {
    // final result = MessagesGetDialogFilters();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf19ed96d);
  }
}

/// Messages Get Suggested Dialog Filters.
///
/// ID: `a29cd42c`.
class MessagesGetSuggestedDialogFilters
    extends TlMethod<List<DialogFilterSuggestedBase>> {
  /// Messages Get Suggested Dialog Filters constructor.
  const MessagesGetSuggestedDialogFilters() : super._();

  /// Deserialize.
  factory MessagesGetSuggestedDialogFilters.deserialize(Uint8List buffer) {
    // final result = MessagesGetSuggestedDialogFilters();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa29cd42c);
  }
}

/// Messages Update Dialog Filter.
///
/// ID: `1ad4a04a`.
class MessagesUpdateDialogFilter extends TlMethod<bool> {
  /// Messages Update Dialog Filter constructor.
  const MessagesUpdateDialogFilter({
    required this.id,
    this.filter,
  }) : super._();

  /// Deserialize.
  factory MessagesUpdateDialogFilter.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final id = _readint(buffer);
    // final filter = _readflags.0?DialogFilter(buffer);
    // final result = MessagesUpdateDialogFilter(flags: flags, id: id, filter: filter,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: filter != null,
    );

    return v;
  }

  /// Id.
  final int id;

  /// Filter.
  final DialogFilterBase? filter;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1ad4a04a);
    buffer.writeInt(flags);
    buffer.writeInt(id);
    final localFilterCopy = filter;
    if (localFilterCopy != null) {
      buffer.writeObject(localFilterCopy);
    }
  }
}

/// Messages Update Dialog Filters Order.
///
/// ID: `c563c1e4`.
class MessagesUpdateDialogFiltersOrder extends TlMethod<bool> {
  /// Messages Update Dialog Filters Order constructor.
  const MessagesUpdateDialogFiltersOrder({
    required this.order,
  }) : super._();

  /// Deserialize.
  factory MessagesUpdateDialogFiltersOrder.deserialize(Uint8List buffer) {
    // final order = _readVector<int>(buffer);
    // final result = MessagesUpdateDialogFiltersOrder(order: order,);

    // return result;

    throw Exception();
  }

  /// Order.
  final List<int> order;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc563c1e4);
    buffer.writeVectorInt(order);
  }
}

/// Stats Get Broadcast Stats.
///
/// ID: `ab42441a`.
class StatsGetBroadcastStats extends TlMethod<StatsBroadcastStatsBase> {
  /// Stats Get Broadcast Stats constructor.
  const StatsGetBroadcastStats({
    required this.dark,
    required this.channel,
  }) : super._();

  /// Deserialize.
  factory StatsGetBroadcastStats.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final dark = _readflags.0?true(buffer);
    // final channel = _readInputChannel(buffer);
    // final result = StatsGetBroadcastStats(flags: flags, dark: dark, channel: channel,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: dark,
    );

    return v;
  }

  /// dark: bit 0 of flags.0?true
  final bool dark;

  /// Channel.
  final InputChannelBase channel;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xab42441a);
    buffer.writeInt(flags);
    buffer.writeObject(channel);
  }
}

/// Stats Load Async Graph.
///
/// ID: `621d5fa0`.
class StatsLoadAsyncGraph extends TlMethod<StatsGraphBase> {
  /// Stats Load Async Graph constructor.
  const StatsLoadAsyncGraph({
    required this.token,
    this.x,
  }) : super._();

  /// Deserialize.
  factory StatsLoadAsyncGraph.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final token = _readstring(buffer);
    // final x = _readflags.0?long(buffer);
    // final result = StatsLoadAsyncGraph(flags: flags, token: token, x: x,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: x != null,
    );

    return v;
  }

  /// Token.
  final String token;

  /// X.
  final int? x;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x621d5fa0);
    buffer.writeInt(flags);
    buffer.writeString(token);
    final localXCopy = x;
    if (localXCopy != null) {
      buffer.writeLong(localXCopy);
    }
  }
}

/// Stickers Set Sticker Set Thumb.
///
/// ID: `a76a5392`.
class StickersSetStickerSetThumb extends TlMethod<MessagesStickerSetBase> {
  /// Stickers Set Sticker Set Thumb constructor.
  const StickersSetStickerSetThumb({
    required this.stickerset,
    this.thumb,
    this.thumbDocumentId,
  }) : super._();

  /// Deserialize.
  factory StickersSetStickerSetThumb.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final stickerset = _readInputStickerSet(buffer);
    // final thumb = _readflags.0?InputDocument(buffer);
    // final thumbDocumentId = _readflags.1?long(buffer);
    // final result = StickersSetStickerSetThumb(flags: flags, stickerset: stickerset, thumb: thumb, thumbDocumentId: thumbDocumentId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: thumb != null,
      b01: thumbDocumentId != null,
    );

    return v;
  }

  /// Stickerset.
  final InputStickerSetBase stickerset;

  /// Thumb.
  final InputDocumentBase? thumb;

  /// Thumb Document Id.
  final int? thumbDocumentId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa76a5392);
    buffer.writeInt(flags);
    buffer.writeObject(stickerset);
    final localThumbCopy = thumb;
    if (localThumbCopy != null) {
      buffer.writeObject(localThumbCopy);
    }
    final localThumbDocumentIdCopy = thumbDocumentId;
    if (localThumbDocumentIdCopy != null) {
      buffer.writeLong(localThumbDocumentIdCopy);
    }
  }
}

/// Bots Set Bot Commands.
///
/// ID: `0517165a`.
class BotsSetBotCommands extends TlMethod<bool> {
  /// Bots Set Bot Commands constructor.
  const BotsSetBotCommands({
    required this.scope,
    required this.langCode,
    required this.commands,
  }) : super._();

  /// Deserialize.
  factory BotsSetBotCommands.deserialize(Uint8List buffer) {
    // final scope = _readBotCommandScope(buffer);
    // final langCode = _readstring(buffer);
    // final commands = _readVector<BotCommand>(buffer);
    // final result = BotsSetBotCommands(scope: scope, langCode: langCode, commands: commands,);

    // return result;

    throw Exception();
  }

  /// Scope.
  final BotCommandScopeBase scope;

  /// Lang Code.
  final String langCode;

  /// Commands.
  final List<BotCommandBase> commands;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0517165a);
    buffer.writeObject(scope);
    buffer.writeString(langCode);
    buffer.writeVectorObject(commands);
  }
}

/// Messages Get Old Featured Stickers.
///
/// ID: `7ed094a1`.
class MessagesGetOldFeaturedStickers
    extends TlMethod<MessagesFeaturedStickersBase> {
  /// Messages Get Old Featured Stickers constructor.
  const MessagesGetOldFeaturedStickers({
    required this.offset,
    required this.limit,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetOldFeaturedStickers.deserialize(Uint8List buffer) {
    // final offset = _readint(buffer);
    // final limit = _readint(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesGetOldFeaturedStickers(offset: offset, limit: limit, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Offset.
  final int offset;

  /// Limit.
  final int limit;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7ed094a1);
    buffer.writeInt(offset);
    buffer.writeInt(limit);
    buffer.writeLong(hash);
  }
}

/// Help Get Promo Data.
///
/// ID: `c0977421`.
class HelpGetPromoData extends TlMethod<HelpPromoDataBase> {
  /// Help Get Promo Data constructor.
  const HelpGetPromoData() : super._();

  /// Deserialize.
  factory HelpGetPromoData.deserialize(Uint8List buffer) {
    // final result = HelpGetPromoData();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc0977421);
  }
}

/// Help Hide Promo Data.
///
/// ID: `1e251c95`.
class HelpHidePromoData extends TlMethod<bool> {
  /// Help Hide Promo Data constructor.
  const HelpHidePromoData({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory HelpHidePromoData.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = HelpHidePromoData(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1e251c95);
    buffer.writeObject(peer);
  }
}

/// Phone Send Signaling Data.
///
/// ID: `ff7a9383`.
class PhoneSendSignalingData extends TlMethod<bool> {
  /// Phone Send Signaling Data constructor.
  const PhoneSendSignalingData({
    required this.peer,
    required this.data,
  }) : super._();

  /// Deserialize.
  factory PhoneSendSignalingData.deserialize(Uint8List buffer) {
    // final peer = _readInputPhoneCall(buffer);
    // final data = _readbytes(buffer);
    // final result = PhoneSendSignalingData(peer: peer, data: data,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPhoneCallBase peer;

  /// Data.
  final Uint8List data;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xff7a9383);
    buffer.writeObject(peer);
    buffer.writeBytes(data);
  }
}

/// Stats Get Megagroup Stats.
///
/// ID: `dcdf8607`.
class StatsGetMegagroupStats extends TlMethod<StatsMegagroupStatsBase> {
  /// Stats Get Megagroup Stats constructor.
  const StatsGetMegagroupStats({
    required this.dark,
    required this.channel,
  }) : super._();

  /// Deserialize.
  factory StatsGetMegagroupStats.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final dark = _readflags.0?true(buffer);
    // final channel = _readInputChannel(buffer);
    // final result = StatsGetMegagroupStats(flags: flags, dark: dark, channel: channel,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: dark,
    );

    return v;
  }

  /// dark: bit 0 of flags.0?true
  final bool dark;

  /// Channel.
  final InputChannelBase channel;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdcdf8607);
    buffer.writeInt(flags);
    buffer.writeObject(channel);
  }
}

/// Account Get Global Privacy Settings.
///
/// ID: `eb2b4cf6`.
class AccountGetGlobalPrivacySettings
    extends TlMethod<GlobalPrivacySettingsBase> {
  /// Account Get Global Privacy Settings constructor.
  const AccountGetGlobalPrivacySettings() : super._();

  /// Deserialize.
  factory AccountGetGlobalPrivacySettings.deserialize(Uint8List buffer) {
    // final result = AccountGetGlobalPrivacySettings();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xeb2b4cf6);
  }
}

/// Account Set Global Privacy Settings.
///
/// ID: `1edaaac2`.
class AccountSetGlobalPrivacySettings
    extends TlMethod<GlobalPrivacySettingsBase> {
  /// Account Set Global Privacy Settings constructor.
  const AccountSetGlobalPrivacySettings({
    required this.settings,
  }) : super._();

  /// Deserialize.
  factory AccountSetGlobalPrivacySettings.deserialize(Uint8List buffer) {
    // final settings = _readGlobalPrivacySettings(buffer);
    // final result = AccountSetGlobalPrivacySettings(settings: settings,);

    // return result;

    throw Exception();
  }

  /// Settings.
  final GlobalPrivacySettingsBase settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1edaaac2);
    buffer.writeObject(settings);
  }
}

/// Help Dismiss Suggestion.
///
/// ID: `f50dbaa1`.
class HelpDismissSuggestion extends TlMethod<bool> {
  /// Help Dismiss Suggestion constructor.
  const HelpDismissSuggestion({
    required this.peer,
    required this.suggestion,
  }) : super._();

  /// Deserialize.
  factory HelpDismissSuggestion.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final suggestion = _readstring(buffer);
    // final result = HelpDismissSuggestion(peer: peer, suggestion: suggestion,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Suggestion.
  final String suggestion;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf50dbaa1);
    buffer.writeObject(peer);
    buffer.writeString(suggestion);
  }
}

/// Help Get Countries List.
///
/// ID: `735787a8`.
class HelpGetCountriesList extends TlMethod<HelpCountriesListBase> {
  /// Help Get Countries List constructor.
  const HelpGetCountriesList({
    required this.langCode,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory HelpGetCountriesList.deserialize(Uint8List buffer) {
    // final langCode = _readstring(buffer);
    // final hash = _readint(buffer);
    // final result = HelpGetCountriesList(langCode: langCode, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Lang Code.
  final String langCode;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x735787a8);
    buffer.writeString(langCode);
    buffer.writeInt(hash);
  }
}

/// Messages Get Replies.
///
/// ID: `22ddd30c`.
class MessagesGetReplies extends TlMethod<MessagesMessagesBase> {
  /// Messages Get Replies constructor.
  const MessagesGetReplies({
    required this.peer,
    required this.msgId,
    required this.offsetId,
    required this.offsetDate,
    required this.addOffset,
    required this.limit,
    required this.maxId,
    required this.minId,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetReplies.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final offsetId = _readint(buffer);
    // final offsetDate = _readint(buffer);
    // final addOffset = _readint(buffer);
    // final limit = _readint(buffer);
    // final maxId = _readint(buffer);
    // final minId = _readint(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesGetReplies(peer: peer, msgId: msgId, offsetId: offsetId, offsetDate: offsetDate, addOffset: addOffset, limit: limit, maxId: maxId, minId: minId, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Offset Id.
  final int offsetId;

  /// Offset Date.
  final DateTime offsetDate;

  /// Add Offset.
  final int addOffset;

  /// Limit.
  final int limit;

  /// Max Id.
  final int maxId;

  /// Min Id.
  final int minId;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x22ddd30c);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeInt(offsetId);
    buffer.writeDateTime(offsetDate);
    buffer.writeInt(addOffset);
    buffer.writeInt(limit);
    buffer.writeInt(maxId);
    buffer.writeInt(minId);
    buffer.writeLong(hash);
  }
}

/// Messages Get Discussion Message.
///
/// ID: `446972fd`.
class MessagesGetDiscussionMessage
    extends TlMethod<MessagesDiscussionMessageBase> {
  /// Messages Get Discussion Message constructor.
  const MessagesGetDiscussionMessage({
    required this.peer,
    required this.msgId,
  }) : super._();

  /// Deserialize.
  factory MessagesGetDiscussionMessage.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final result = MessagesGetDiscussionMessage(peer: peer, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x446972fd);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
  }
}

/// Messages Read Discussion.
///
/// ID: `f731a9f4`.
class MessagesReadDiscussion extends TlMethod<bool> {
  /// Messages Read Discussion constructor.
  const MessagesReadDiscussion({
    required this.peer,
    required this.msgId,
    required this.readMaxId,
  }) : super._();

  /// Deserialize.
  factory MessagesReadDiscussion.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final readMaxId = _readint(buffer);
    // final result = MessagesReadDiscussion(peer: peer, msgId: msgId, readMaxId: readMaxId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Read Max Id.
  final int readMaxId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf731a9f4);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeInt(readMaxId);
  }
}

/// Contacts Block From Replies.
///
/// ID: `29a8962c`.
class ContactsBlockFromReplies extends TlMethod<UpdatesBase> {
  /// Contacts Block From Replies constructor.
  const ContactsBlockFromReplies({
    required this.deleteMessage,
    required this.deleteHistory,
    required this.reportSpam,
    required this.msgId,
  }) : super._();

  /// Deserialize.
  factory ContactsBlockFromReplies.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final deleteMessage = _readflags.0?true(buffer);
    // final deleteHistory = _readflags.1?true(buffer);
    // final reportSpam = _readflags.2?true(buffer);
    // final msgId = _readint(buffer);
    // final result = ContactsBlockFromReplies(flags: flags, deleteMessage: deleteMessage, deleteHistory: deleteHistory, reportSpam: reportSpam, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: deleteMessage,
      b01: deleteHistory,
      b02: reportSpam,
    );

    return v;
  }

  /// delete_message: bit 0 of flags.0?true
  final bool deleteMessage;

  /// delete_history: bit 1 of flags.1?true
  final bool deleteHistory;

  /// report_spam: bit 2 of flags.2?true
  final bool reportSpam;

  /// Msg Id.
  final int msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x29a8962c);
    buffer.writeInt(flags);
    buffer.writeInt(msgId);
  }
}

/// Stats Get Message Public Forwards.
///
/// ID: `5f150144`.
class StatsGetMessagePublicForwards extends TlMethod<StatsPublicForwardsBase> {
  /// Stats Get Message Public Forwards constructor.
  const StatsGetMessagePublicForwards({
    required this.channel,
    required this.msgId,
    required this.offset,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory StatsGetMessagePublicForwards.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final msgId = _readint(buffer);
    // final offset = _readstring(buffer);
    // final limit = _readint(buffer);
    // final result = StatsGetMessagePublicForwards(channel: channel, msgId: msgId, offset: offset, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Msg Id.
  final int msgId;

  /// Offset.
  final String offset;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5f150144);
    buffer.writeObject(channel);
    buffer.writeInt(msgId);
    buffer.writeString(offset);
    buffer.writeInt(limit);
  }
}

/// Stats Get Message Stats.
///
/// ID: `b6e0a3f5`.
class StatsGetMessageStats extends TlMethod<StatsMessageStatsBase> {
  /// Stats Get Message Stats constructor.
  const StatsGetMessageStats({
    required this.dark,
    required this.channel,
    required this.msgId,
  }) : super._();

  /// Deserialize.
  factory StatsGetMessageStats.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final dark = _readflags.0?true(buffer);
    // final channel = _readInputChannel(buffer);
    // final msgId = _readint(buffer);
    // final result = StatsGetMessageStats(flags: flags, dark: dark, channel: channel, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: dark,
    );

    return v;
  }

  /// dark: bit 0 of flags.0?true
  final bool dark;

  /// Channel.
  final InputChannelBase channel;

  /// Msg Id.
  final int msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb6e0a3f5);
    buffer.writeInt(flags);
    buffer.writeObject(channel);
    buffer.writeInt(msgId);
  }
}

/// Messages Unpin All Messages.
///
/// ID: `ee22b9a8`.
class MessagesUnpinAllMessages extends TlMethod<MessagesAffectedHistoryBase> {
  /// Messages Unpin All Messages constructor.
  const MessagesUnpinAllMessages({
    required this.peer,
    this.topMsgId,
  }) : super._();

  /// Deserialize.
  factory MessagesUnpinAllMessages.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final topMsgId = _readflags.0?int(buffer);
    // final result = MessagesUnpinAllMessages(flags: flags, peer: peer, topMsgId: topMsgId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: topMsgId != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Top Msg Id.
  final int? topMsgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xee22b9a8);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
  }
}

/// Phone Create Group Call.
///
/// ID: `48cdc6d8`.
class PhoneCreateGroupCall extends TlMethod<UpdatesBase> {
  /// Phone Create Group Call constructor.
  const PhoneCreateGroupCall({
    required this.rtmpStream,
    required this.peer,
    required this.randomId,
    this.title,
    this.scheduleDate,
  }) : super._();

  /// Deserialize.
  factory PhoneCreateGroupCall.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final rtmpStream = _readflags.2?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final randomId = _readint(buffer);
    // final title = _readflags.0?string(buffer);
    // final scheduleDate = _readflags.1?int(buffer);
    // final result = PhoneCreateGroupCall(flags: flags, rtmpStream: rtmpStream, peer: peer, randomId: randomId, title: title, scheduleDate: scheduleDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: rtmpStream,
      b00: title != null,
      b01: scheduleDate != null,
    );

    return v;
  }

  /// rtmp_stream: bit 2 of flags.2?true
  final bool rtmpStream;

  /// Peer.
  final InputPeerBase peer;

  /// Random Id.
  final int randomId;

  /// Title.
  final String? title;

  /// Schedule Date.
  final DateTime? scheduleDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x48cdc6d8);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(randomId);
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localScheduleDateCopy = scheduleDate;
    if (localScheduleDateCopy != null) {
      buffer.writeDateTime(localScheduleDateCopy);
    }
  }
}

/// Phone Join Group Call.
///
/// ID: `b132ff7b`.
class PhoneJoinGroupCall extends TlMethod<UpdatesBase> {
  /// Phone Join Group Call constructor.
  const PhoneJoinGroupCall({
    required this.muted,
    required this.videoStopped,
    required this.call,
    required this.joinAs,
    this.inviteHash,
    required this.params,
  }) : super._();

  /// Deserialize.
  factory PhoneJoinGroupCall.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final muted = _readflags.0?true(buffer);
    // final videoStopped = _readflags.2?true(buffer);
    // final call = _readInputGroupCall(buffer);
    // final joinAs = _readInputPeer(buffer);
    // final inviteHash = _readflags.1?string(buffer);
    // final params = _readDataJSON(buffer);
    // final result = PhoneJoinGroupCall(flags: flags, muted: muted, videoStopped: videoStopped, call: call, joinAs: joinAs, inviteHash: inviteHash, params: params,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: muted,
      b02: videoStopped,
      b01: inviteHash != null,
    );

    return v;
  }

  /// muted: bit 0 of flags.0?true
  final bool muted;

  /// video_stopped: bit 2 of flags.2?true
  final bool videoStopped;

  /// Call.
  final InputGroupCallBase call;

  /// Join As.
  final InputPeerBase joinAs;

  /// Invite Hash.
  final String? inviteHash;

  /// Params.
  final DataJSONBase params;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb132ff7b);
    buffer.writeInt(flags);
    buffer.writeObject(call);
    buffer.writeObject(joinAs);
    final localInviteHashCopy = inviteHash;
    if (localInviteHashCopy != null) {
      buffer.writeString(localInviteHashCopy);
    }
    buffer.writeObject(params);
  }
}

/// Phone Leave Group Call.
///
/// ID: `500377f9`.
class PhoneLeaveGroupCall extends TlMethod<UpdatesBase> {
  /// Phone Leave Group Call constructor.
  const PhoneLeaveGroupCall({
    required this.call,
    required this.source,
  }) : super._();

  /// Deserialize.
  factory PhoneLeaveGroupCall.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final source = _readint(buffer);
    // final result = PhoneLeaveGroupCall(call: call, source: source,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Source.
  final int source;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x500377f9);
    buffer.writeObject(call);
    buffer.writeInt(source);
  }
}

/// Phone Invite To Group Call.
///
/// ID: `7b393160`.
class PhoneInviteToGroupCall extends TlMethod<UpdatesBase> {
  /// Phone Invite To Group Call constructor.
  const PhoneInviteToGroupCall({
    required this.call,
    required this.users,
  }) : super._();

  /// Deserialize.
  factory PhoneInviteToGroupCall.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final users = _readVector<InputUser>(buffer);
    // final result = PhoneInviteToGroupCall(call: call, users: users,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Users.
  final List<InputUserBase> users;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7b393160);
    buffer.writeObject(call);
    buffer.writeVectorObject(users);
  }
}

/// Phone Discard Group Call.
///
/// ID: `7a777135`.
class PhoneDiscardGroupCall extends TlMethod<UpdatesBase> {
  /// Phone Discard Group Call constructor.
  const PhoneDiscardGroupCall({
    required this.call,
  }) : super._();

  /// Deserialize.
  factory PhoneDiscardGroupCall.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final result = PhoneDiscardGroupCall(call: call,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7a777135);
    buffer.writeObject(call);
  }
}

/// Phone Toggle Group Call Settings.
///
/// ID: `74bbb43d`.
class PhoneToggleGroupCallSettings extends TlMethod<UpdatesBase> {
  /// Phone Toggle Group Call Settings constructor.
  const PhoneToggleGroupCallSettings({
    required this.resetInviteHash,
    required this.call,
    this.joinMuted,
  }) : super._();

  /// Deserialize.
  factory PhoneToggleGroupCallSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final resetInviteHash = _readflags.1?true(buffer);
    // final call = _readInputGroupCall(buffer);
    // final joinMuted = _readflags.0?Bool(buffer);
    // final result = PhoneToggleGroupCallSettings(flags: flags, resetInviteHash: resetInviteHash, call: call, joinMuted: joinMuted,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: resetInviteHash,
      b00: joinMuted != null,
    );

    return v;
  }

  /// reset_invite_hash: bit 1 of flags.1?true
  final bool resetInviteHash;

  /// Call.
  final InputGroupCallBase call;

  /// Join Muted.
  final bool? joinMuted;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x74bbb43d);
    buffer.writeInt(flags);
    buffer.writeObject(call);
    final localJoinMutedCopy = joinMuted;
    if (localJoinMutedCopy != null) {
      buffer.writeBool(localJoinMutedCopy);
    }
  }
}

/// Phone Get Group Call.
///
/// ID: `041845db`.
class PhoneGetGroupCall extends TlMethod<PhoneGroupCallBase> {
  /// Phone Get Group Call constructor.
  const PhoneGetGroupCall({
    required this.call,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory PhoneGetGroupCall.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final limit = _readint(buffer);
    // final result = PhoneGetGroupCall(call: call, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x041845db);
    buffer.writeObject(call);
    buffer.writeInt(limit);
  }
}

/// Phone Get Group Participants.
///
/// ID: `c558d8ab`.
class PhoneGetGroupParticipants extends TlMethod<PhoneGroupParticipantsBase> {
  /// Phone Get Group Participants constructor.
  const PhoneGetGroupParticipants({
    required this.call,
    required this.ids,
    required this.sources,
    required this.offset,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory PhoneGetGroupParticipants.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final ids = _readVector<InputPeer>(buffer);
    // final sources = _readVector<int>(buffer);
    // final offset = _readstring(buffer);
    // final limit = _readint(buffer);
    // final result = PhoneGetGroupParticipants(call: call, ids: ids, sources: sources, offset: offset, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Ids.
  final List<InputPeerBase> ids;

  /// Sources.
  final List<int> sources;

  /// Offset.
  final String offset;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc558d8ab);
    buffer.writeObject(call);
    buffer.writeVectorObject(ids);
    buffer.writeVectorInt(sources);
    buffer.writeString(offset);
    buffer.writeInt(limit);
  }
}

/// Phone Check Group Call.
///
/// ID: `b59cf977`.
class PhoneCheckGroupCall extends TlMethod<List<int>> {
  /// Phone Check Group Call constructor.
  const PhoneCheckGroupCall({
    required this.call,
    required this.sources,
  }) : super._();

  /// Deserialize.
  factory PhoneCheckGroupCall.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final sources = _readVector<int>(buffer);
    // final result = PhoneCheckGroupCall(call: call, sources: sources,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Sources.
  final List<int> sources;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb59cf977);
    buffer.writeObject(call);
    buffer.writeVectorInt(sources);
  }
}

/// Messages Delete Chat.
///
/// ID: `5bd0ee50`.
class MessagesDeleteChat extends TlMethod<bool> {
  /// Messages Delete Chat constructor.
  const MessagesDeleteChat({
    required this.chatId,
  }) : super._();

  /// Deserialize.
  factory MessagesDeleteChat.deserialize(Uint8List buffer) {
    // final chatId = _readlong(buffer);
    // final result = MessagesDeleteChat(chatId: chatId,);

    // return result;

    throw Exception();
  }

  /// Chat Id.
  final int chatId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5bd0ee50);
    buffer.writeLong(chatId);
  }
}

/// Messages Delete Phone Call History.
///
/// ID: `f9cbe409`.
class MessagesDeletePhoneCallHistory
    extends TlMethod<MessagesAffectedFoundMessagesBase> {
  /// Messages Delete Phone Call History constructor.
  const MessagesDeletePhoneCallHistory({
    required this.revoke,
  }) : super._();

  /// Deserialize.
  factory MessagesDeletePhoneCallHistory.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final revoke = _readflags.0?true(buffer);
    // final result = MessagesDeletePhoneCallHistory(flags: flags, revoke: revoke,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: revoke,
    );

    return v;
  }

  /// revoke: bit 0 of flags.0?true
  final bool revoke;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf9cbe409);
    buffer.writeInt(flags);
  }
}

/// Messages Check History Import.
///
/// ID: `43fe19f3`.
class MessagesCheckHistoryImport
    extends TlMethod<MessagesHistoryImportParsedBase> {
  /// Messages Check History Import constructor.
  const MessagesCheckHistoryImport({
    required this.importHead,
  }) : super._();

  /// Deserialize.
  factory MessagesCheckHistoryImport.deserialize(Uint8List buffer) {
    // final importHead = _readstring(buffer);
    // final result = MessagesCheckHistoryImport(importHead: importHead,);

    // return result;

    throw Exception();
  }

  /// Import Head.
  final String importHead;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x43fe19f3);
    buffer.writeString(importHead);
  }
}

/// Messages Init History Import.
///
/// ID: `34090c3b`.
class MessagesInitHistoryImport extends TlMethod<MessagesHistoryImportBase> {
  /// Messages Init History Import constructor.
  const MessagesInitHistoryImport({
    required this.peer,
    required this.file,
    required this.mediaCount,
  }) : super._();

  /// Deserialize.
  factory MessagesInitHistoryImport.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final file = _readInputFile(buffer);
    // final mediaCount = _readint(buffer);
    // final result = MessagesInitHistoryImport(peer: peer, file: file, mediaCount: mediaCount,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// File.
  final InputFileBase file;

  /// Media Count.
  final int mediaCount;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x34090c3b);
    buffer.writeObject(peer);
    buffer.writeObject(file);
    buffer.writeInt(mediaCount);
  }
}

/// Messages Upload Imported Media.
///
/// ID: `2a862092`.
class MessagesUploadImportedMedia extends TlMethod<MessageMediaBase> {
  /// Messages Upload Imported Media constructor.
  const MessagesUploadImportedMedia({
    required this.peer,
    required this.importId,
    required this.fileName,
    required this.media,
  }) : super._();

  /// Deserialize.
  factory MessagesUploadImportedMedia.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final importId = _readlong(buffer);
    // final fileName = _readstring(buffer);
    // final media = _readInputMedia(buffer);
    // final result = MessagesUploadImportedMedia(peer: peer, importId: importId, fileName: fileName, media: media,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Import Id.
  final int importId;

  /// File Name.
  final String fileName;

  /// Media.
  final InputMediaBase media;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2a862092);
    buffer.writeObject(peer);
    buffer.writeLong(importId);
    buffer.writeString(fileName);
    buffer.writeObject(media);
  }
}

/// Messages Start History Import.
///
/// ID: `b43df344`.
class MessagesStartHistoryImport extends TlMethod<bool> {
  /// Messages Start History Import constructor.
  const MessagesStartHistoryImport({
    required this.peer,
    required this.importId,
  }) : super._();

  /// Deserialize.
  factory MessagesStartHistoryImport.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final importId = _readlong(buffer);
    // final result = MessagesStartHistoryImport(peer: peer, importId: importId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Import Id.
  final int importId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb43df344);
    buffer.writeObject(peer);
    buffer.writeLong(importId);
  }
}

/// Messages Get Exported Chat Invites.
///
/// ID: `a2b5a3f6`.
class MessagesGetExportedChatInvites
    extends TlMethod<MessagesExportedChatInvitesBase> {
  /// Messages Get Exported Chat Invites constructor.
  const MessagesGetExportedChatInvites({
    required this.revoked,
    required this.peer,
    required this.adminId,
    this.offsetDate,
    this.offsetLink,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory MessagesGetExportedChatInvites.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final revoked = _readflags.3?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final adminId = _readInputUser(buffer);
    // final offsetDate = _readflags.2?int(buffer);
    // final offsetLink = _readflags.2?string(buffer);
    // final limit = _readint(buffer);
    // final result = MessagesGetExportedChatInvites(flags: flags, revoked: revoked, peer: peer, adminId: adminId, offsetDate: offsetDate, offsetLink: offsetLink, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: revoked,
      b02: offsetDate != null || offsetLink != null,
    );

    return v;
  }

  /// revoked: bit 3 of flags.3?true
  final bool revoked;

  /// Peer.
  final InputPeerBase peer;

  /// Admin Id.
  final InputUserBase adminId;

  /// Offset Date.
  final DateTime? offsetDate;

  /// Offset Link.
  final String? offsetLink;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa2b5a3f6);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeObject(adminId);
    final localOffsetDateCopy = offsetDate;
    if (localOffsetDateCopy != null) {
      buffer.writeDateTime(localOffsetDateCopy);
    }
    final localOffsetLinkCopy = offsetLink;
    if (localOffsetLinkCopy != null) {
      buffer.writeString(localOffsetLinkCopy);
    }
    buffer.writeInt(limit);
  }
}

/// Messages Get Exported Chat Invite.
///
/// ID: `73746f5c`.
class MessagesGetExportedChatInvite
    extends TlMethod<MessagesExportedChatInviteBase> {
  /// Messages Get Exported Chat Invite constructor.
  const MessagesGetExportedChatInvite({
    required this.peer,
    required this.link,
  }) : super._();

  /// Deserialize.
  factory MessagesGetExportedChatInvite.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final link = _readstring(buffer);
    // final result = MessagesGetExportedChatInvite(peer: peer, link: link,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Link.
  final String link;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x73746f5c);
    buffer.writeObject(peer);
    buffer.writeString(link);
  }
}

/// Messages Edit Exported Chat Invite.
///
/// ID: `bdca2f75`.
class MessagesEditExportedChatInvite
    extends TlMethod<MessagesExportedChatInviteBase> {
  /// Messages Edit Exported Chat Invite constructor.
  const MessagesEditExportedChatInvite({
    required this.revoked,
    required this.peer,
    required this.link,
    this.expireDate,
    this.usageLimit,
    this.requestNeeded,
    this.title,
  }) : super._();

  /// Deserialize.
  factory MessagesEditExportedChatInvite.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final revoked = _readflags.2?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final link = _readstring(buffer);
    // final expireDate = _readflags.0?int(buffer);
    // final usageLimit = _readflags.1?int(buffer);
    // final requestNeeded = _readflags.3?Bool(buffer);
    // final title = _readflags.4?string(buffer);
    // final result = MessagesEditExportedChatInvite(flags: flags, revoked: revoked, peer: peer, link: link, expireDate: expireDate, usageLimit: usageLimit, requestNeeded: requestNeeded, title: title,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: revoked,
      b00: expireDate != null,
      b01: usageLimit != null,
      b03: requestNeeded != null,
      b04: title != null,
    );

    return v;
  }

  /// revoked: bit 2 of flags.2?true
  final bool revoked;

  /// Peer.
  final InputPeerBase peer;

  /// Link.
  final String link;

  /// Expire Date.
  final DateTime? expireDate;

  /// Usage Limit.
  final int? usageLimit;

  /// Request Needed.
  final bool? requestNeeded;

  /// Title.
  final String? title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbdca2f75);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeString(link);
    final localExpireDateCopy = expireDate;
    if (localExpireDateCopy != null) {
      buffer.writeDateTime(localExpireDateCopy);
    }
    final localUsageLimitCopy = usageLimit;
    if (localUsageLimitCopy != null) {
      buffer.writeInt(localUsageLimitCopy);
    }
    final localRequestNeededCopy = requestNeeded;
    if (localRequestNeededCopy != null) {
      buffer.writeBool(localRequestNeededCopy);
    }
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
  }
}

/// Messages Delete Revoked Exported Chat Invites.
///
/// ID: `56987bd5`.
class MessagesDeleteRevokedExportedChatInvites extends TlMethod<bool> {
  /// Messages Delete Revoked Exported Chat Invites constructor.
  const MessagesDeleteRevokedExportedChatInvites({
    required this.peer,
    required this.adminId,
  }) : super._();

  /// Deserialize.
  factory MessagesDeleteRevokedExportedChatInvites.deserialize(
      Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final adminId = _readInputUser(buffer);
    // final result = MessagesDeleteRevokedExportedChatInvites(peer: peer, adminId: adminId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Admin Id.
  final InputUserBase adminId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x56987bd5);
    buffer.writeObject(peer);
    buffer.writeObject(adminId);
  }
}

/// Messages Delete Exported Chat Invite.
///
/// ID: `d464a42b`.
class MessagesDeleteExportedChatInvite extends TlMethod<bool> {
  /// Messages Delete Exported Chat Invite constructor.
  const MessagesDeleteExportedChatInvite({
    required this.peer,
    required this.link,
  }) : super._();

  /// Deserialize.
  factory MessagesDeleteExportedChatInvite.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final link = _readstring(buffer);
    // final result = MessagesDeleteExportedChatInvite(peer: peer, link: link,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Link.
  final String link;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd464a42b);
    buffer.writeObject(peer);
    buffer.writeString(link);
  }
}

/// Messages Get Admins With Invites.
///
/// ID: `3920e6ef`.
class MessagesGetAdminsWithInvites
    extends TlMethod<MessagesChatAdminsWithInvitesBase> {
  /// Messages Get Admins With Invites constructor.
  const MessagesGetAdminsWithInvites({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory MessagesGetAdminsWithInvites.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = MessagesGetAdminsWithInvites(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3920e6ef);
    buffer.writeObject(peer);
  }
}

/// Messages Get Chat Invite Importers.
///
/// ID: `df04dd4e`.
class MessagesGetChatInviteImporters
    extends TlMethod<MessagesChatInviteImportersBase> {
  /// Messages Get Chat Invite Importers constructor.
  const MessagesGetChatInviteImporters({
    required this.requested,
    required this.peer,
    this.link,
    this.q,
    required this.offsetDate,
    required this.offsetUser,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory MessagesGetChatInviteImporters.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final requested = _readflags.0?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final link = _readflags.1?string(buffer);
    // final q = _readflags.2?string(buffer);
    // final offsetDate = _readint(buffer);
    // final offsetUser = _readInputUser(buffer);
    // final limit = _readint(buffer);
    // final result = MessagesGetChatInviteImporters(flags: flags, requested: requested, peer: peer, link: link, q: q, offsetDate: offsetDate, offsetUser: offsetUser, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: requested,
      b01: link != null,
      b02: q != null,
    );

    return v;
  }

  /// requested: bit 0 of flags.0?true
  final bool requested;

  /// Peer.
  final InputPeerBase peer;

  /// Link.
  final String? link;

  /// Q.
  final String? q;

  /// Offset Date.
  final DateTime offsetDate;

  /// Offset User.
  final InputUserBase offsetUser;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdf04dd4e);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localLinkCopy = link;
    if (localLinkCopy != null) {
      buffer.writeString(localLinkCopy);
    }
    final localQCopy = q;
    if (localQCopy != null) {
      buffer.writeString(localQCopy);
    }
    buffer.writeDateTime(offsetDate);
    buffer.writeObject(offsetUser);
    buffer.writeInt(limit);
  }
}

/// Messages Set History T T L.
///
/// ID: `b80e5fe4`.
class MessagesSetHistoryTTL extends TlMethod<UpdatesBase> {
  /// Messages Set History T T L constructor.
  const MessagesSetHistoryTTL({
    required this.peer,
    required this.period,
  }) : super._();

  /// Deserialize.
  factory MessagesSetHistoryTTL.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final period = _readint(buffer);
    // final result = MessagesSetHistoryTTL(peer: peer, period: period,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Period.
  final int period;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb80e5fe4);
    buffer.writeObject(peer);
    buffer.writeInt(period);
  }
}

/// Account Report Profile Photo.
///
/// ID: `fa8cc6f5`.
class AccountReportProfilePhoto extends TlMethod<bool> {
  /// Account Report Profile Photo constructor.
  const AccountReportProfilePhoto({
    required this.peer,
    required this.photoId,
    required this.reason,
    required this.message,
  }) : super._();

  /// Deserialize.
  factory AccountReportProfilePhoto.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final photoId = _readInputPhoto(buffer);
    // final reason = _readReportReason(buffer);
    // final message = _readstring(buffer);
    // final result = AccountReportProfilePhoto(peer: peer, photoId: photoId, reason: reason, message: message,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Photo Id.
  final InputPhotoBase photoId;

  /// Reason.
  final ReportReasonBase reason;

  /// Message.
  final String message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfa8cc6f5);
    buffer.writeObject(peer);
    buffer.writeObject(photoId);
    buffer.writeObject(reason);
    buffer.writeString(message);
  }
}

/// Channels Convert To Gigagroup.
///
/// ID: `0b290c69`.
class ChannelsConvertToGigagroup extends TlMethod<UpdatesBase> {
  /// Channels Convert To Gigagroup constructor.
  const ChannelsConvertToGigagroup({
    required this.channel,
  }) : super._();

  /// Deserialize.
  factory ChannelsConvertToGigagroup.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final result = ChannelsConvertToGigagroup(channel: channel,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0b290c69);
    buffer.writeObject(channel);
  }
}

/// Messages Check History Import Peer.
///
/// ID: `5dc60f03`.
class MessagesCheckHistoryImportPeer
    extends TlMethod<MessagesCheckedHistoryImportPeerBase> {
  /// Messages Check History Import Peer constructor.
  const MessagesCheckHistoryImportPeer({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory MessagesCheckHistoryImportPeer.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = MessagesCheckHistoryImportPeer(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5dc60f03);
    buffer.writeObject(peer);
  }
}

/// Phone Toggle Group Call Record.
///
/// ID: `f128c708`.
class PhoneToggleGroupCallRecord extends TlMethod<UpdatesBase> {
  /// Phone Toggle Group Call Record constructor.
  const PhoneToggleGroupCallRecord({
    required this.start,
    required this.video,
    required this.call,
    this.title,
    this.videoPortrait,
  }) : super._();

  /// Deserialize.
  factory PhoneToggleGroupCallRecord.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final start = _readflags.0?true(buffer);
    // final video = _readflags.2?true(buffer);
    // final call = _readInputGroupCall(buffer);
    // final title = _readflags.1?string(buffer);
    // final videoPortrait = _readflags.2?Bool(buffer);
    // final result = PhoneToggleGroupCallRecord(flags: flags, start: start, video: video, call: call, title: title, videoPortrait: videoPortrait,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: start,
      b02: video || videoPortrait != null,
      b01: title != null,
    );

    return v;
  }

  /// start: bit 0 of flags.0?true
  final bool start;

  /// video: bit 2 of flags.2?true
  final bool video;

  /// Call.
  final InputGroupCallBase call;

  /// Title.
  final String? title;

  /// Video Portrait.
  final bool? videoPortrait;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf128c708);
    buffer.writeInt(flags);
    buffer.writeObject(call);
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localVideoPortraitCopy = videoPortrait;
    if (localVideoPortraitCopy != null) {
      buffer.writeBool(localVideoPortraitCopy);
    }
  }
}

/// Phone Edit Group Call Participant.
///
/// ID: `a5273abf`.
class PhoneEditGroupCallParticipant extends TlMethod<UpdatesBase> {
  /// Phone Edit Group Call Participant constructor.
  const PhoneEditGroupCallParticipant({
    required this.call,
    required this.participant,
    this.muted,
    this.volume,
    this.raiseHand,
    this.videoStopped,
    this.videoPaused,
    this.presentationPaused,
  }) : super._();

  /// Deserialize.
  factory PhoneEditGroupCallParticipant.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final call = _readInputGroupCall(buffer);
    // final participant = _readInputPeer(buffer);
    // final muted = _readflags.0?Bool(buffer);
    // final volume = _readflags.1?int(buffer);
    // final raiseHand = _readflags.2?Bool(buffer);
    // final videoStopped = _readflags.3?Bool(buffer);
    // final videoPaused = _readflags.4?Bool(buffer);
    // final presentationPaused = _readflags.5?Bool(buffer);
    // final result = PhoneEditGroupCallParticipant(flags: flags, call: call, participant: participant, muted: muted, volume: volume, raiseHand: raiseHand, videoStopped: videoStopped, videoPaused: videoPaused, presentationPaused: presentationPaused,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: muted != null,
      b01: volume != null,
      b02: raiseHand != null,
      b03: videoStopped != null,
      b04: videoPaused != null,
      b05: presentationPaused != null,
    );

    return v;
  }

  /// Call.
  final InputGroupCallBase call;

  /// Participant.
  final InputPeerBase participant;

  /// Muted.
  final bool? muted;

  /// Volume.
  final int? volume;

  /// Raise Hand.
  final bool? raiseHand;

  /// Video Stopped.
  final bool? videoStopped;

  /// Video Paused.
  final bool? videoPaused;

  /// Presentation Paused.
  final bool? presentationPaused;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa5273abf);
    buffer.writeInt(flags);
    buffer.writeObject(call);
    buffer.writeObject(participant);
    final localMutedCopy = muted;
    if (localMutedCopy != null) {
      buffer.writeBool(localMutedCopy);
    }
    final localVolumeCopy = volume;
    if (localVolumeCopy != null) {
      buffer.writeInt(localVolumeCopy);
    }
    final localRaiseHandCopy = raiseHand;
    if (localRaiseHandCopy != null) {
      buffer.writeBool(localRaiseHandCopy);
    }
    final localVideoStoppedCopy = videoStopped;
    if (localVideoStoppedCopy != null) {
      buffer.writeBool(localVideoStoppedCopy);
    }
    final localVideoPausedCopy = videoPaused;
    if (localVideoPausedCopy != null) {
      buffer.writeBool(localVideoPausedCopy);
    }
    final localPresentationPausedCopy = presentationPaused;
    if (localPresentationPausedCopy != null) {
      buffer.writeBool(localPresentationPausedCopy);
    }
  }
}

/// Phone Edit Group Call Title.
///
/// ID: `1ca6ac0a`.
class PhoneEditGroupCallTitle extends TlMethod<UpdatesBase> {
  /// Phone Edit Group Call Title constructor.
  const PhoneEditGroupCallTitle({
    required this.call,
    required this.title,
  }) : super._();

  /// Deserialize.
  factory PhoneEditGroupCallTitle.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final title = _readstring(buffer);
    // final result = PhoneEditGroupCallTitle(call: call, title: title,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Title.
  final String title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1ca6ac0a);
    buffer.writeObject(call);
    buffer.writeString(title);
  }
}

/// Phone Get Group Call Join As.
///
/// ID: `ef7c213a`.
class PhoneGetGroupCallJoinAs extends TlMethod<PhoneJoinAsPeersBase> {
  /// Phone Get Group Call Join As constructor.
  const PhoneGetGroupCallJoinAs({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory PhoneGetGroupCallJoinAs.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = PhoneGetGroupCallJoinAs(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xef7c213a);
    buffer.writeObject(peer);
  }
}

/// Phone Export Group Call Invite.
///
/// ID: `e6aa647f`.
class PhoneExportGroupCallInvite
    extends TlMethod<PhoneExportedGroupCallInviteBase> {
  /// Phone Export Group Call Invite constructor.
  const PhoneExportGroupCallInvite({
    required this.canSelfUnmute,
    required this.call,
  }) : super._();

  /// Deserialize.
  factory PhoneExportGroupCallInvite.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final canSelfUnmute = _readflags.0?true(buffer);
    // final call = _readInputGroupCall(buffer);
    // final result = PhoneExportGroupCallInvite(flags: flags, canSelfUnmute: canSelfUnmute, call: call,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: canSelfUnmute,
    );

    return v;
  }

  /// can_self_unmute: bit 0 of flags.0?true
  final bool canSelfUnmute;

  /// Call.
  final InputGroupCallBase call;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe6aa647f);
    buffer.writeInt(flags);
    buffer.writeObject(call);
  }
}

/// Phone Toggle Group Call Start Subscription.
///
/// ID: `219c34e6`.
class PhoneToggleGroupCallStartSubscription extends TlMethod<UpdatesBase> {
  /// Phone Toggle Group Call Start Subscription constructor.
  const PhoneToggleGroupCallStartSubscription({
    required this.call,
    required this.subscribed,
  }) : super._();

  /// Deserialize.
  factory PhoneToggleGroupCallStartSubscription.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final subscribed = _readBool(buffer);
    // final result = PhoneToggleGroupCallStartSubscription(call: call, subscribed: subscribed,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Subscribed.
  final bool subscribed;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x219c34e6);
    buffer.writeObject(call);
    buffer.writeBool(subscribed);
  }
}

/// Phone Start Scheduled Group Call.
///
/// ID: `5680e342`.
class PhoneStartScheduledGroupCall extends TlMethod<UpdatesBase> {
  /// Phone Start Scheduled Group Call constructor.
  const PhoneStartScheduledGroupCall({
    required this.call,
  }) : super._();

  /// Deserialize.
  factory PhoneStartScheduledGroupCall.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final result = PhoneStartScheduledGroupCall(call: call,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5680e342);
    buffer.writeObject(call);
  }
}

/// Phone Save Default Group Call Join As.
///
/// ID: `575e1f8c`.
class PhoneSaveDefaultGroupCallJoinAs extends TlMethod<bool> {
  /// Phone Save Default Group Call Join As constructor.
  const PhoneSaveDefaultGroupCallJoinAs({
    required this.peer,
    required this.joinAs,
  }) : super._();

  /// Deserialize.
  factory PhoneSaveDefaultGroupCallJoinAs.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final joinAs = _readInputPeer(buffer);
    // final result = PhoneSaveDefaultGroupCallJoinAs(peer: peer, joinAs: joinAs,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Join As.
  final InputPeerBase joinAs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x575e1f8c);
    buffer.writeObject(peer);
    buffer.writeObject(joinAs);
  }
}

/// Phone Join Group Call Presentation.
///
/// ID: `cbea6bc4`.
class PhoneJoinGroupCallPresentation extends TlMethod<UpdatesBase> {
  /// Phone Join Group Call Presentation constructor.
  const PhoneJoinGroupCallPresentation({
    required this.call,
    required this.params,
  }) : super._();

  /// Deserialize.
  factory PhoneJoinGroupCallPresentation.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final params = _readDataJSON(buffer);
    // final result = PhoneJoinGroupCallPresentation(call: call, params: params,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Params.
  final DataJSONBase params;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xcbea6bc4);
    buffer.writeObject(call);
    buffer.writeObject(params);
  }
}

/// Phone Leave Group Call Presentation.
///
/// ID: `1c50d144`.
class PhoneLeaveGroupCallPresentation extends TlMethod<UpdatesBase> {
  /// Phone Leave Group Call Presentation constructor.
  const PhoneLeaveGroupCallPresentation({
    required this.call,
  }) : super._();

  /// Deserialize.
  factory PhoneLeaveGroupCallPresentation.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final result = PhoneLeaveGroupCallPresentation(call: call,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1c50d144);
    buffer.writeObject(call);
  }
}

/// Stickers Check Short Name.
///
/// ID: `284b3639`.
class StickersCheckShortName extends TlMethod<bool> {
  /// Stickers Check Short Name constructor.
  const StickersCheckShortName({
    required this.shortName,
  }) : super._();

  /// Deserialize.
  factory StickersCheckShortName.deserialize(Uint8List buffer) {
    // final shortName = _readstring(buffer);
    // final result = StickersCheckShortName(shortName: shortName,);

    // return result;

    throw Exception();
  }

  /// Short Name.
  final String shortName;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x284b3639);
    buffer.writeString(shortName);
  }
}

/// Stickers Suggest Short Name.
///
/// ID: `4dafc503`.
class StickersSuggestShortName
    extends TlMethod<StickersSuggestedShortNameBase> {
  /// Stickers Suggest Short Name constructor.
  const StickersSuggestShortName({
    required this.title,
  }) : super._();

  /// Deserialize.
  factory StickersSuggestShortName.deserialize(Uint8List buffer) {
    // final title = _readstring(buffer);
    // final result = StickersSuggestShortName(title: title,);

    // return result;

    throw Exception();
  }

  /// Title.
  final String title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4dafc503);
    buffer.writeString(title);
  }
}

/// Bots Reset Bot Commands.
///
/// ID: `3d8de0f9`.
class BotsResetBotCommands extends TlMethod<bool> {
  /// Bots Reset Bot Commands constructor.
  const BotsResetBotCommands({
    required this.scope,
    required this.langCode,
  }) : super._();

  /// Deserialize.
  factory BotsResetBotCommands.deserialize(Uint8List buffer) {
    // final scope = _readBotCommandScope(buffer);
    // final langCode = _readstring(buffer);
    // final result = BotsResetBotCommands(scope: scope, langCode: langCode,);

    // return result;

    throw Exception();
  }

  /// Scope.
  final BotCommandScopeBase scope;

  /// Lang Code.
  final String langCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3d8de0f9);
    buffer.writeObject(scope);
    buffer.writeString(langCode);
  }
}

/// Bots Get Bot Commands.
///
/// ID: `e34c0dd6`.
class BotsGetBotCommands extends TlMethod<List<BotCommandBase>> {
  /// Bots Get Bot Commands constructor.
  const BotsGetBotCommands({
    required this.scope,
    required this.langCode,
  }) : super._();

  /// Deserialize.
  factory BotsGetBotCommands.deserialize(Uint8List buffer) {
    // final scope = _readBotCommandScope(buffer);
    // final langCode = _readstring(buffer);
    // final result = BotsGetBotCommands(scope: scope, langCode: langCode,);

    // return result;

    throw Exception();
  }

  /// Scope.
  final BotCommandScopeBase scope;

  /// Lang Code.
  final String langCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe34c0dd6);
    buffer.writeObject(scope);
    buffer.writeString(langCode);
  }
}

/// Account Reset Password.
///
/// ID: `9308ce1b`.
class AccountResetPassword extends TlMethod<AccountResetPasswordResultBase> {
  /// Account Reset Password constructor.
  const AccountResetPassword() : super._();

  /// Deserialize.
  factory AccountResetPassword.deserialize(Uint8List buffer) {
    // final result = AccountResetPassword();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9308ce1b);
  }
}

/// Account Decline Password Reset.
///
/// ID: `4c9409f6`.
class AccountDeclinePasswordReset extends TlMethod<bool> {
  /// Account Decline Password Reset constructor.
  const AccountDeclinePasswordReset() : super._();

  /// Deserialize.
  factory AccountDeclinePasswordReset.deserialize(Uint8List buffer) {
    // final result = AccountDeclinePasswordReset();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4c9409f6);
  }
}

/// Auth Check Recovery Password.
///
/// ID: `0d36bf79`.
class AuthCheckRecoveryPassword extends TlMethod<bool> {
  /// Auth Check Recovery Password constructor.
  const AuthCheckRecoveryPassword({
    required this.code,
  }) : super._();

  /// Deserialize.
  factory AuthCheckRecoveryPassword.deserialize(Uint8List buffer) {
    // final code = _readstring(buffer);
    // final result = AuthCheckRecoveryPassword(code: code,);

    // return result;

    throw Exception();
  }

  /// Code.
  final String code;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0d36bf79);
    buffer.writeString(code);
  }
}

/// Account Get Chat Themes.
///
/// ID: `d638de89`.
class AccountGetChatThemes extends TlMethod<AccountThemesBase> {
  /// Account Get Chat Themes constructor.
  const AccountGetChatThemes({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory AccountGetChatThemes.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = AccountGetChatThemes(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd638de89);
    buffer.writeLong(hash);
  }
}

/// Messages Set Chat Theme.
///
/// ID: `e63be13f`.
class MessagesSetChatTheme extends TlMethod<UpdatesBase> {
  /// Messages Set Chat Theme constructor.
  const MessagesSetChatTheme({
    required this.peer,
    required this.emoticon,
  }) : super._();

  /// Deserialize.
  factory MessagesSetChatTheme.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final emoticon = _readstring(buffer);
    // final result = MessagesSetChatTheme(peer: peer, emoticon: emoticon,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Emoticon.
  final String emoticon;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe63be13f);
    buffer.writeObject(peer);
    buffer.writeString(emoticon);
  }
}

/// Channels View Sponsored Message.
///
/// ID: `beaedb94`.
class ChannelsViewSponsoredMessage extends TlMethod<bool> {
  /// Channels View Sponsored Message constructor.
  const ChannelsViewSponsoredMessage({
    required this.channel,
    required this.randomId,
  }) : super._();

  /// Deserialize.
  factory ChannelsViewSponsoredMessage.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final randomId = _readbytes(buffer);
    // final result = ChannelsViewSponsoredMessage(channel: channel, randomId: randomId,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Random Id.
  final Uint8List randomId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbeaedb94);
    buffer.writeObject(channel);
    buffer.writeBytes(randomId);
  }
}

/// Channels Get Sponsored Messages.
///
/// ID: `ec210fbf`.
class ChannelsGetSponsoredMessages
    extends TlMethod<MessagesSponsoredMessagesBase> {
  /// Channels Get Sponsored Messages constructor.
  const ChannelsGetSponsoredMessages({
    required this.channel,
  }) : super._();

  /// Deserialize.
  factory ChannelsGetSponsoredMessages.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final result = ChannelsGetSponsoredMessages(channel: channel,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xec210fbf);
    buffer.writeObject(channel);
  }
}

/// Messages Get Message Read Participants.
///
/// ID: `31c1c44f`.
class MessagesGetMessageReadParticipants
    extends TlMethod<List<ReadParticipantDateBase>> {
  /// Messages Get Message Read Participants constructor.
  const MessagesGetMessageReadParticipants({
    required this.peer,
    required this.msgId,
  }) : super._();

  /// Deserialize.
  factory MessagesGetMessageReadParticipants.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final result = MessagesGetMessageReadParticipants(peer: peer, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x31c1c44f);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
  }
}

/// Messages Get Search Results Calendar.
///
/// ID: `6aa3f6bd`.
class MessagesGetSearchResultsCalendar
    extends TlMethod<MessagesSearchResultsCalendarBase> {
  /// Messages Get Search Results Calendar constructor.
  const MessagesGetSearchResultsCalendar({
    required this.peer,
    this.savedPeerId,
    required this.filter,
    required this.offsetId,
    required this.offsetDate,
  }) : super._();

  /// Deserialize.
  factory MessagesGetSearchResultsCalendar.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final savedPeerId = _readflags.2?InputPeer(buffer);
    // final filter = _readMessagesFilter(buffer);
    // final offsetId = _readint(buffer);
    // final offsetDate = _readint(buffer);
    // final result = MessagesGetSearchResultsCalendar(flags: flags, peer: peer, savedPeerId: savedPeerId, filter: filter, offsetId: offsetId, offsetDate: offsetDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: savedPeerId != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Saved Peer Id.
  final InputPeerBase? savedPeerId;

  /// Filter.
  final MessagesFilterBase filter;

  /// Offset Id.
  final int offsetId;

  /// Offset Date.
  final DateTime offsetDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6aa3f6bd);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localSavedPeerIdCopy = savedPeerId;
    if (localSavedPeerIdCopy != null) {
      buffer.writeObject(localSavedPeerIdCopy);
    }
    buffer.writeObject(filter);
    buffer.writeInt(offsetId);
    buffer.writeDateTime(offsetDate);
  }
}

/// Messages Get Search Results Positions.
///
/// ID: `9c7f2f10`.
class MessagesGetSearchResultsPositions
    extends TlMethod<MessagesSearchResultsPositionsBase> {
  /// Messages Get Search Results Positions constructor.
  const MessagesGetSearchResultsPositions({
    required this.peer,
    this.savedPeerId,
    required this.filter,
    required this.offsetId,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory MessagesGetSearchResultsPositions.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final savedPeerId = _readflags.2?InputPeer(buffer);
    // final filter = _readMessagesFilter(buffer);
    // final offsetId = _readint(buffer);
    // final limit = _readint(buffer);
    // final result = MessagesGetSearchResultsPositions(flags: flags, peer: peer, savedPeerId: savedPeerId, filter: filter, offsetId: offsetId, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: savedPeerId != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Saved Peer Id.
  final InputPeerBase? savedPeerId;

  /// Filter.
  final MessagesFilterBase filter;

  /// Offset Id.
  final int offsetId;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9c7f2f10);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localSavedPeerIdCopy = savedPeerId;
    if (localSavedPeerIdCopy != null) {
      buffer.writeObject(localSavedPeerIdCopy);
    }
    buffer.writeObject(filter);
    buffer.writeInt(offsetId);
    buffer.writeInt(limit);
  }
}

/// Messages Hide Chat Join Request.
///
/// ID: `7fe7e815`.
class MessagesHideChatJoinRequest extends TlMethod<UpdatesBase> {
  /// Messages Hide Chat Join Request constructor.
  const MessagesHideChatJoinRequest({
    required this.approved,
    required this.peer,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory MessagesHideChatJoinRequest.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final approved = _readflags.0?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final userId = _readInputUser(buffer);
    // final result = MessagesHideChatJoinRequest(flags: flags, approved: approved, peer: peer, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: approved,
    );

    return v;
  }

  /// approved: bit 0 of flags.0?true
  final bool approved;

  /// Peer.
  final InputPeerBase peer;

  /// User Id.
  final InputUserBase userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7fe7e815);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeObject(userId);
  }
}

/// Messages Hide All Chat Join Requests.
///
/// ID: `e085f4ea`.
class MessagesHideAllChatJoinRequests extends TlMethod<UpdatesBase> {
  /// Messages Hide All Chat Join Requests constructor.
  const MessagesHideAllChatJoinRequests({
    required this.approved,
    required this.peer,
    this.link,
  }) : super._();

  /// Deserialize.
  factory MessagesHideAllChatJoinRequests.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final approved = _readflags.0?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final link = _readflags.1?string(buffer);
    // final result = MessagesHideAllChatJoinRequests(flags: flags, approved: approved, peer: peer, link: link,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: approved,
      b01: link != null,
    );

    return v;
  }

  /// approved: bit 0 of flags.0?true
  final bool approved;

  /// Peer.
  final InputPeerBase peer;

  /// Link.
  final String? link;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe085f4ea);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localLinkCopy = link;
    if (localLinkCopy != null) {
      buffer.writeString(localLinkCopy);
    }
  }
}

/// Messages Toggle No Forwards.
///
/// ID: `b11eafa2`.
class MessagesToggleNoForwards extends TlMethod<UpdatesBase> {
  /// Messages Toggle No Forwards constructor.
  const MessagesToggleNoForwards({
    required this.peer,
    required this.enabled,
  }) : super._();

  /// Deserialize.
  factory MessagesToggleNoForwards.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final enabled = _readBool(buffer);
    // final result = MessagesToggleNoForwards(peer: peer, enabled: enabled,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Enabled.
  final bool enabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb11eafa2);
    buffer.writeObject(peer);
    buffer.writeBool(enabled);
  }
}

/// Messages Save Default Send As.
///
/// ID: `ccfddf96`.
class MessagesSaveDefaultSendAs extends TlMethod<bool> {
  /// Messages Save Default Send As constructor.
  const MessagesSaveDefaultSendAs({
    required this.peer,
    required this.sendAs,
  }) : super._();

  /// Deserialize.
  factory MessagesSaveDefaultSendAs.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final sendAs = _readInputPeer(buffer);
    // final result = MessagesSaveDefaultSendAs(peer: peer, sendAs: sendAs,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Send As.
  final InputPeerBase sendAs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xccfddf96);
    buffer.writeObject(peer);
    buffer.writeObject(sendAs);
  }
}

/// Channels Get Send As.
///
/// ID: `0dc770ee`.
class ChannelsGetSendAs extends TlMethod<ChannelsSendAsPeersBase> {
  /// Channels Get Send As constructor.
  const ChannelsGetSendAs({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory ChannelsGetSendAs.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = ChannelsGetSendAs(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0dc770ee);
    buffer.writeObject(peer);
  }
}

/// Account Set Authorization T T L.
///
/// ID: `bf899aa0`.
class AccountSetAuthorizationTTL extends TlMethod<bool> {
  /// Account Set Authorization T T L constructor.
  const AccountSetAuthorizationTTL({
    required this.authorizationTtlDays,
  }) : super._();

  /// Deserialize.
  factory AccountSetAuthorizationTTL.deserialize(Uint8List buffer) {
    // final authorizationTtlDays = _readint(buffer);
    // final result = AccountSetAuthorizationTTL(authorizationTtlDays: authorizationTtlDays,);

    // return result;

    throw Exception();
  }

  /// Authorization Ttl Days.
  final int authorizationTtlDays;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbf899aa0);
    buffer.writeInt(authorizationTtlDays);
  }
}

/// Account Change Authorization Settings.
///
/// ID: `40f48462`.
class AccountChangeAuthorizationSettings extends TlMethod<bool> {
  /// Account Change Authorization Settings constructor.
  const AccountChangeAuthorizationSettings({
    required this.confirmed,
    required this.hash,
    this.encryptedRequestsDisabled,
    this.callRequestsDisabled,
  }) : super._();

  /// Deserialize.
  factory AccountChangeAuthorizationSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final confirmed = _readflags.3?true(buffer);
    // final hash = _readlong(buffer);
    // final encryptedRequestsDisabled = _readflags.0?Bool(buffer);
    // final callRequestsDisabled = _readflags.1?Bool(buffer);
    // final result = AccountChangeAuthorizationSettings(flags: flags, confirmed: confirmed, hash: hash, encryptedRequestsDisabled: encryptedRequestsDisabled, callRequestsDisabled: callRequestsDisabled,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: confirmed,
      b00: encryptedRequestsDisabled != null,
      b01: callRequestsDisabled != null,
    );

    return v;
  }

  /// confirmed: bit 3 of flags.3?true
  final bool confirmed;

  /// Hash.
  final int hash;

  /// Encrypted Requests Disabled.
  final bool? encryptedRequestsDisabled;

  /// Call Requests Disabled.
  final bool? callRequestsDisabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x40f48462);
    buffer.writeInt(flags);
    buffer.writeLong(hash);
    final localEncryptedRequestsDisabledCopy = encryptedRequestsDisabled;
    if (localEncryptedRequestsDisabledCopy != null) {
      buffer.writeBool(localEncryptedRequestsDisabledCopy);
    }
    final localCallRequestsDisabledCopy = callRequestsDisabled;
    if (localCallRequestsDisabledCopy != null) {
      buffer.writeBool(localCallRequestsDisabledCopy);
    }
  }
}

/// Channels Delete Participant History.
///
/// ID: `367544db`.
class ChannelsDeleteParticipantHistory
    extends TlMethod<MessagesAffectedHistoryBase> {
  /// Channels Delete Participant History constructor.
  const ChannelsDeleteParticipantHistory({
    required this.channel,
    required this.participant,
  }) : super._();

  /// Deserialize.
  factory ChannelsDeleteParticipantHistory.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final participant = _readInputPeer(buffer);
    // final result = ChannelsDeleteParticipantHistory(channel: channel, participant: participant,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Participant.
  final InputPeerBase participant;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x367544db);
    buffer.writeObject(channel);
    buffer.writeObject(participant);
  }
}

/// Messages Send Reaction.
///
/// ID: `d30d78d4`.
class MessagesSendReaction extends TlMethod<UpdatesBase> {
  /// Messages Send Reaction constructor.
  const MessagesSendReaction({
    required this.big,
    required this.addToRecent,
    required this.peer,
    required this.msgId,
    this.reaction,
  }) : super._();

  /// Deserialize.
  factory MessagesSendReaction.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final big = _readflags.1?true(buffer);
    // final addToRecent = _readflags.2?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final reaction = _readflags.0?Vector<Reaction>(buffer);
    // final result = MessagesSendReaction(flags: flags, big: big, addToRecent: addToRecent, peer: peer, msgId: msgId, reaction: reaction,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: big,
      b02: addToRecent,
      b00: reaction != null,
    );

    return v;
  }

  /// big: bit 1 of flags.1?true
  final bool big;

  /// add_to_recent: bit 2 of flags.2?true
  final bool addToRecent;

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Reaction.
  final List<ReactionBase>? reaction;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd30d78d4);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    final localReactionCopy = reaction;
    if (localReactionCopy != null) {
      buffer.writeVectorObject(localReactionCopy);
    }
  }
}

/// Messages Get Messages Reactions.
///
/// ID: `8bba90e6`.
class MessagesGetMessagesReactions extends TlMethod<UpdatesBase> {
  /// Messages Get Messages Reactions constructor.
  const MessagesGetMessagesReactions({
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesGetMessagesReactions.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final result = MessagesGetMessagesReactions(peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8bba90e6);
    buffer.writeObject(peer);
    buffer.writeVectorInt(id);
  }
}

/// Messages Get Message Reactions List.
///
/// ID: `461b3f48`.
class MessagesGetMessageReactionsList
    extends TlMethod<MessagesMessageReactionsListBase> {
  /// Messages Get Message Reactions List constructor.
  const MessagesGetMessageReactionsList({
    required this.peer,
    required this.id,
    this.reaction,
    this.offset,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory MessagesGetMessageReactionsList.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final reaction = _readflags.0?Reaction(buffer);
    // final offset = _readflags.1?string(buffer);
    // final limit = _readint(buffer);
    // final result = MessagesGetMessageReactionsList(flags: flags, peer: peer, id: id, reaction: reaction, offset: offset, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: reaction != null,
      b01: offset != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// Reaction.
  final ReactionBase? reaction;

  /// Offset.
  final String? offset;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x461b3f48);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(id);
    final localReactionCopy = reaction;
    if (localReactionCopy != null) {
      buffer.writeObject(localReactionCopy);
    }
    final localOffsetCopy = offset;
    if (localOffsetCopy != null) {
      buffer.writeString(localOffsetCopy);
    }
    buffer.writeInt(limit);
  }
}

/// Messages Set Chat Available Reactions.
///
/// ID: `feb16771`.
class MessagesSetChatAvailableReactions extends TlMethod<UpdatesBase> {
  /// Messages Set Chat Available Reactions constructor.
  const MessagesSetChatAvailableReactions({
    required this.peer,
    required this.availableReactions,
  }) : super._();

  /// Deserialize.
  factory MessagesSetChatAvailableReactions.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final availableReactions = _readChatReactions(buffer);
    // final result = MessagesSetChatAvailableReactions(peer: peer, availableReactions: availableReactions,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Available Reactions.
  final ChatReactionsBase availableReactions;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfeb16771);
    buffer.writeObject(peer);
    buffer.writeObject(availableReactions);
  }
}

/// Messages Get Available Reactions.
///
/// ID: `18dea0ac`.
class MessagesGetAvailableReactions
    extends TlMethod<MessagesAvailableReactionsBase> {
  /// Messages Get Available Reactions constructor.
  const MessagesGetAvailableReactions({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetAvailableReactions.deserialize(Uint8List buffer) {
    // final hash = _readint(buffer);
    // final result = MessagesGetAvailableReactions(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x18dea0ac);
    buffer.writeInt(hash);
  }
}

/// Messages Set Default Reaction.
///
/// ID: `4f47a016`.
class MessagesSetDefaultReaction extends TlMethod<bool> {
  /// Messages Set Default Reaction constructor.
  const MessagesSetDefaultReaction({
    required this.reaction,
  }) : super._();

  /// Deserialize.
  factory MessagesSetDefaultReaction.deserialize(Uint8List buffer) {
    // final reaction = _readReaction(buffer);
    // final result = MessagesSetDefaultReaction(reaction: reaction,);

    // return result;

    throw Exception();
  }

  /// Reaction.
  final ReactionBase reaction;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4f47a016);
    buffer.writeObject(reaction);
  }
}

/// Messages Translate Text.
///
/// ID: `63183030`.
class MessagesTranslateText extends TlMethod<MessagesTranslatedTextBase> {
  /// Messages Translate Text constructor.
  const MessagesTranslateText({
    this.peer,
    this.id,
    this.text,
    required this.toLang,
  }) : super._();

  /// Deserialize.
  factory MessagesTranslateText.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readflags.0?InputPeer(buffer);
    // final id = _readflags.0?Vector<int>(buffer);
    // final text = _readflags.1?Vector<TextWithEntities>(buffer);
    // final toLang = _readstring(buffer);
    // final result = MessagesTranslateText(flags: flags, peer: peer, id: id, text: text, toLang: toLang,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: peer != null || id != null,
      b01: text != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase? peer;

  /// Id.
  final List<int>? id;

  /// Text.
  final List<TextWithEntitiesBase>? text;

  /// To Lang.
  final String toLang;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x63183030);
    buffer.writeInt(flags);
    final localPeerCopy = peer;
    if (localPeerCopy != null) {
      buffer.writeObject(localPeerCopy);
    }
    final localIdCopy = id;
    if (localIdCopy != null) {
      buffer.writeVectorInt(localIdCopy);
    }
    final localTextCopy = text;
    if (localTextCopy != null) {
      buffer.writeVectorObject(localTextCopy);
    }
    buffer.writeString(toLang);
  }
}

/// Messages Get Unread Reactions.
///
/// ID: `3223495b`.
class MessagesGetUnreadReactions extends TlMethod<MessagesMessagesBase> {
  /// Messages Get Unread Reactions constructor.
  const MessagesGetUnreadReactions({
    required this.peer,
    this.topMsgId,
    required this.offsetId,
    required this.addOffset,
    required this.limit,
    required this.maxId,
    required this.minId,
  }) : super._();

  /// Deserialize.
  factory MessagesGetUnreadReactions.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final topMsgId = _readflags.0?int(buffer);
    // final offsetId = _readint(buffer);
    // final addOffset = _readint(buffer);
    // final limit = _readint(buffer);
    // final maxId = _readint(buffer);
    // final minId = _readint(buffer);
    // final result = MessagesGetUnreadReactions(flags: flags, peer: peer, topMsgId: topMsgId, offsetId: offsetId, addOffset: addOffset, limit: limit, maxId: maxId, minId: minId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: topMsgId != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Top Msg Id.
  final int? topMsgId;

  /// Offset Id.
  final int offsetId;

  /// Add Offset.
  final int addOffset;

  /// Limit.
  final int limit;

  /// Max Id.
  final int maxId;

  /// Min Id.
  final int minId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3223495b);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
    buffer.writeInt(offsetId);
    buffer.writeInt(addOffset);
    buffer.writeInt(limit);
    buffer.writeInt(maxId);
    buffer.writeInt(minId);
  }
}

/// Messages Read Reactions.
///
/// ID: `54aa7f8e`.
class MessagesReadReactions extends TlMethod<MessagesAffectedHistoryBase> {
  /// Messages Read Reactions constructor.
  const MessagesReadReactions({
    required this.peer,
    this.topMsgId,
  }) : super._();

  /// Deserialize.
  factory MessagesReadReactions.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final topMsgId = _readflags.0?int(buffer);
    // final result = MessagesReadReactions(flags: flags, peer: peer, topMsgId: topMsgId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: topMsgId != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Top Msg Id.
  final int? topMsgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x54aa7f8e);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localTopMsgIdCopy = topMsgId;
    if (localTopMsgIdCopy != null) {
      buffer.writeInt(localTopMsgIdCopy);
    }
  }
}

/// Contacts Resolve Phone.
///
/// ID: `8af94344`.
class ContactsResolvePhone extends TlMethod<ContactsResolvedPeerBase> {
  /// Contacts Resolve Phone constructor.
  const ContactsResolvePhone({
    required this.phone,
  }) : super._();

  /// Deserialize.
  factory ContactsResolvePhone.deserialize(Uint8List buffer) {
    // final phone = _readstring(buffer);
    // final result = ContactsResolvePhone(phone: phone,);

    // return result;

    throw Exception();
  }

  /// Phone.
  final String phone;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8af94344);
    buffer.writeString(phone);
  }
}

/// Phone Get Group Call Stream Channels.
///
/// ID: `1ab21940`.
class PhoneGetGroupCallStreamChannels
    extends TlMethod<PhoneGroupCallStreamChannelsBase> {
  /// Phone Get Group Call Stream Channels constructor.
  const PhoneGetGroupCallStreamChannels({
    required this.call,
  }) : super._();

  /// Deserialize.
  factory PhoneGetGroupCallStreamChannels.deserialize(Uint8List buffer) {
    // final call = _readInputGroupCall(buffer);
    // final result = PhoneGetGroupCallStreamChannels(call: call,);

    // return result;

    throw Exception();
  }

  /// Call.
  final InputGroupCallBase call;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1ab21940);
    buffer.writeObject(call);
  }
}

/// Phone Get Group Call Stream Rtmp Url.
///
/// ID: `deb3abbf`.
class PhoneGetGroupCallStreamRtmpUrl
    extends TlMethod<PhoneGroupCallStreamRtmpUrlBase> {
  /// Phone Get Group Call Stream Rtmp Url constructor.
  const PhoneGetGroupCallStreamRtmpUrl({
    required this.peer,
    required this.revoke,
  }) : super._();

  /// Deserialize.
  factory PhoneGetGroupCallStreamRtmpUrl.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final revoke = _readBool(buffer);
    // final result = PhoneGetGroupCallStreamRtmpUrl(peer: peer, revoke: revoke,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Revoke.
  final bool revoke;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdeb3abbf);
    buffer.writeObject(peer);
    buffer.writeBool(revoke);
  }
}

/// Messages Search Sent Media.
///
/// ID: `107e31a0`.
class MessagesSearchSentMedia extends TlMethod<MessagesMessagesBase> {
  /// Messages Search Sent Media constructor.
  const MessagesSearchSentMedia({
    required this.q,
    required this.filter,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory MessagesSearchSentMedia.deserialize(Uint8List buffer) {
    // final q = _readstring(buffer);
    // final filter = _readMessagesFilter(buffer);
    // final limit = _readint(buffer);
    // final result = MessagesSearchSentMedia(q: q, filter: filter, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Q.
  final String q;

  /// Filter.
  final MessagesFilterBase filter;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x107e31a0);
    buffer.writeString(q);
    buffer.writeObject(filter);
    buffer.writeInt(limit);
  }
}

/// Messages Get Attach Menu Bots.
///
/// ID: `16fcc2cb`.
class MessagesGetAttachMenuBots extends TlMethod<AttachMenuBotsBase> {
  /// Messages Get Attach Menu Bots constructor.
  const MessagesGetAttachMenuBots({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetAttachMenuBots.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = MessagesGetAttachMenuBots(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x16fcc2cb);
    buffer.writeLong(hash);
  }
}

/// Messages Get Attach Menu Bot.
///
/// ID: `77216192`.
class MessagesGetAttachMenuBot extends TlMethod<AttachMenuBotsBotBase> {
  /// Messages Get Attach Menu Bot constructor.
  const MessagesGetAttachMenuBot({
    required this.bot,
  }) : super._();

  /// Deserialize.
  factory MessagesGetAttachMenuBot.deserialize(Uint8List buffer) {
    // final bot = _readInputUser(buffer);
    // final result = MessagesGetAttachMenuBot(bot: bot,);

    // return result;

    throw Exception();
  }

  /// Bot.
  final InputUserBase bot;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x77216192);
    buffer.writeObject(bot);
  }
}

/// Messages Toggle Bot In Attach Menu.
///
/// ID: `69f59d69`.
class MessagesToggleBotInAttachMenu extends TlMethod<bool> {
  /// Messages Toggle Bot In Attach Menu constructor.
  const MessagesToggleBotInAttachMenu({
    required this.writeAllowed,
    required this.bot,
    required this.enabled,
  }) : super._();

  /// Deserialize.
  factory MessagesToggleBotInAttachMenu.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final writeAllowed = _readflags.0?true(buffer);
    // final bot = _readInputUser(buffer);
    // final enabled = _readBool(buffer);
    // final result = MessagesToggleBotInAttachMenu(flags: flags, writeAllowed: writeAllowed, bot: bot, enabled: enabled,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: writeAllowed,
    );

    return v;
  }

  /// write_allowed: bit 0 of flags.0?true
  final bool writeAllowed;

  /// Bot.
  final InputUserBase bot;

  /// Enabled.
  final bool enabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x69f59d69);
    buffer.writeInt(flags);
    buffer.writeObject(bot);
    buffer.writeBool(enabled);
  }
}

/// Messages Request Web View.
///
/// ID: `269dc2c1`.
class MessagesRequestWebView extends TlMethod<WebViewResultBase> {
  /// Messages Request Web View constructor.
  const MessagesRequestWebView({
    required this.fromBotMenu,
    required this.silent,
    required this.peer,
    required this.bot,
    this.url,
    this.startParam,
    this.themeParams,
    required this.platform,
    this.replyTo,
    this.sendAs,
  }) : super._();

  /// Deserialize.
  factory MessagesRequestWebView.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final fromBotMenu = _readflags.4?true(buffer);
    // final silent = _readflags.5?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final bot = _readInputUser(buffer);
    // final url = _readflags.1?string(buffer);
    // final startParam = _readflags.3?string(buffer);
    // final themeParams = _readflags.2?DataJSON(buffer);
    // final platform = _readstring(buffer);
    // final replyTo = _readflags.0?InputReplyTo(buffer);
    // final sendAs = _readflags.13?InputPeer(buffer);
    // final result = MessagesRequestWebView(flags: flags, fromBotMenu: fromBotMenu, silent: silent, peer: peer, bot: bot, url: url, startParam: startParam, themeParams: themeParams, platform: platform, replyTo: replyTo, sendAs: sendAs,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b04: fromBotMenu,
      b05: silent,
      b01: url != null,
      b03: startParam != null,
      b02: themeParams != null,
      b00: replyTo != null,
      b13: sendAs != null,
    );

    return v;
  }

  /// from_bot_menu: bit 4 of flags.4?true
  final bool fromBotMenu;

  /// silent: bit 5 of flags.5?true
  final bool silent;

  /// Peer.
  final InputPeerBase peer;

  /// Bot.
  final InputUserBase bot;

  /// Url.
  final String? url;

  /// Start Param.
  final String? startParam;

  /// Theme Params.
  final DataJSONBase? themeParams;

  /// Platform.
  final String platform;

  /// Reply To.
  final InputReplyToBase? replyTo;

  /// Send As.
  final InputPeerBase? sendAs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x269dc2c1);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeObject(bot);
    final localUrlCopy = url;
    if (localUrlCopy != null) {
      buffer.writeString(localUrlCopy);
    }
    final localStartParamCopy = startParam;
    if (localStartParamCopy != null) {
      buffer.writeString(localStartParamCopy);
    }
    final localThemeParamsCopy = themeParams;
    if (localThemeParamsCopy != null) {
      buffer.writeObject(localThemeParamsCopy);
    }
    buffer.writeString(platform);
    final localReplyToCopy = replyTo;
    if (localReplyToCopy != null) {
      buffer.writeObject(localReplyToCopy);
    }
    final localSendAsCopy = sendAs;
    if (localSendAsCopy != null) {
      buffer.writeObject(localSendAsCopy);
    }
  }
}

/// Messages Prolong Web View.
///
/// ID: `b0d81a83`.
class MessagesProlongWebView extends TlMethod<bool> {
  /// Messages Prolong Web View constructor.
  const MessagesProlongWebView({
    required this.silent,
    required this.peer,
    required this.bot,
    required this.queryId,
    this.replyTo,
    this.sendAs,
  }) : super._();

  /// Deserialize.
  factory MessagesProlongWebView.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final silent = _readflags.5?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final bot = _readInputUser(buffer);
    // final queryId = _readlong(buffer);
    // final replyTo = _readflags.0?InputReplyTo(buffer);
    // final sendAs = _readflags.13?InputPeer(buffer);
    // final result = MessagesProlongWebView(flags: flags, silent: silent, peer: peer, bot: bot, queryId: queryId, replyTo: replyTo, sendAs: sendAs,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b05: silent,
      b00: replyTo != null,
      b13: sendAs != null,
    );

    return v;
  }

  /// silent: bit 5 of flags.5?true
  final bool silent;

  /// Peer.
  final InputPeerBase peer;

  /// Bot.
  final InputUserBase bot;

  /// Query Id.
  final int queryId;

  /// Reply To.
  final InputReplyToBase? replyTo;

  /// Send As.
  final InputPeerBase? sendAs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb0d81a83);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeObject(bot);
    buffer.writeLong(queryId);
    final localReplyToCopy = replyTo;
    if (localReplyToCopy != null) {
      buffer.writeObject(localReplyToCopy);
    }
    final localSendAsCopy = sendAs;
    if (localSendAsCopy != null) {
      buffer.writeObject(localSendAsCopy);
    }
  }
}

/// Messages Request Simple Web View.
///
/// ID: `1a46500a`.
class MessagesRequestSimpleWebView extends TlMethod<SimpleWebViewResultBase> {
  /// Messages Request Simple Web View constructor.
  const MessagesRequestSimpleWebView({
    required this.fromSwitchWebview,
    required this.fromSideMenu,
    required this.bot,
    this.url,
    this.startParam,
    this.themeParams,
    required this.platform,
  }) : super._();

  /// Deserialize.
  factory MessagesRequestSimpleWebView.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final fromSwitchWebview = _readflags.1?true(buffer);
    // final fromSideMenu = _readflags.2?true(buffer);
    // final bot = _readInputUser(buffer);
    // final url = _readflags.3?string(buffer);
    // final startParam = _readflags.4?string(buffer);
    // final themeParams = _readflags.0?DataJSON(buffer);
    // final platform = _readstring(buffer);
    // final result = MessagesRequestSimpleWebView(flags: flags, fromSwitchWebview: fromSwitchWebview, fromSideMenu: fromSideMenu, bot: bot, url: url, startParam: startParam, themeParams: themeParams, platform: platform,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: fromSwitchWebview,
      b02: fromSideMenu,
      b03: url != null,
      b04: startParam != null,
      b00: themeParams != null,
    );

    return v;
  }

  /// from_switch_webview: bit 1 of flags.1?true
  final bool fromSwitchWebview;

  /// from_side_menu: bit 2 of flags.2?true
  final bool fromSideMenu;

  /// Bot.
  final InputUserBase bot;

  /// Url.
  final String? url;

  /// Start Param.
  final String? startParam;

  /// Theme Params.
  final DataJSONBase? themeParams;

  /// Platform.
  final String platform;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1a46500a);
    buffer.writeInt(flags);
    buffer.writeObject(bot);
    final localUrlCopy = url;
    if (localUrlCopy != null) {
      buffer.writeString(localUrlCopy);
    }
    final localStartParamCopy = startParam;
    if (localStartParamCopy != null) {
      buffer.writeString(localStartParamCopy);
    }
    final localThemeParamsCopy = themeParams;
    if (localThemeParamsCopy != null) {
      buffer.writeObject(localThemeParamsCopy);
    }
    buffer.writeString(platform);
  }
}

/// Messages Send Web View Result Message.
///
/// ID: `0a4314f5`.
class MessagesSendWebViewResultMessage
    extends TlMethod<WebViewMessageSentBase> {
  /// Messages Send Web View Result Message constructor.
  const MessagesSendWebViewResultMessage({
    required this.botQueryId,
    required this.result,
  }) : super._();

  /// Deserialize.
  factory MessagesSendWebViewResultMessage.deserialize(Uint8List buffer) {
    // final botQueryId = _readstring(buffer);
    // final result = _readInputBotInlineResult(buffer);
    // final result = MessagesSendWebViewResultMessage(botQueryId: botQueryId, result: result,);

    // return result;

    throw Exception();
  }

  /// Bot Query Id.
  final String botQueryId;

  /// Result.
  final InputBotInlineResultBase result;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0a4314f5);
    buffer.writeString(botQueryId);
    buffer.writeObject(result);
  }
}

/// Messages Send Web View Data.
///
/// ID: `dc0242c8`.
class MessagesSendWebViewData extends TlMethod<UpdatesBase> {
  /// Messages Send Web View Data constructor.
  const MessagesSendWebViewData({
    required this.bot,
    required this.randomId,
    required this.buttonText,
    required this.data,
  }) : super._();

  /// Deserialize.
  factory MessagesSendWebViewData.deserialize(Uint8List buffer) {
    // final bot = _readInputUser(buffer);
    // final randomId = _readlong(buffer);
    // final buttonText = _readstring(buffer);
    // final data = _readstring(buffer);
    // final result = MessagesSendWebViewData(bot: bot, randomId: randomId, buttonText: buttonText, data: data,);

    // return result;

    throw Exception();
  }

  /// Bot.
  final InputUserBase bot;

  /// Random Id.
  final int randomId;

  /// Button Text.
  final String buttonText;

  /// Data.
  final String data;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdc0242c8);
    buffer.writeObject(bot);
    buffer.writeLong(randomId);
    buffer.writeString(buttonText);
    buffer.writeString(data);
  }
}

/// Bots Set Bot Menu Button.
///
/// ID: `4504d54f`.
class BotsSetBotMenuButton extends TlMethod<bool> {
  /// Bots Set Bot Menu Button constructor.
  const BotsSetBotMenuButton({
    required this.userId,
    required this.button,
  }) : super._();

  /// Deserialize.
  factory BotsSetBotMenuButton.deserialize(Uint8List buffer) {
    // final userId = _readInputUser(buffer);
    // final button = _readBotMenuButton(buffer);
    // final result = BotsSetBotMenuButton(userId: userId, button: button,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final InputUserBase userId;

  /// Button.
  final BotMenuButtonBase button;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4504d54f);
    buffer.writeObject(userId);
    buffer.writeObject(button);
  }
}

/// Bots Get Bot Menu Button.
///
/// ID: `9c60eb28`.
class BotsGetBotMenuButton extends TlMethod<BotMenuButtonBase> {
  /// Bots Get Bot Menu Button constructor.
  const BotsGetBotMenuButton({
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory BotsGetBotMenuButton.deserialize(Uint8List buffer) {
    // final userId = _readInputUser(buffer);
    // final result = BotsGetBotMenuButton(userId: userId,);

    // return result;

    throw Exception();
  }

  /// User Id.
  final InputUserBase userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9c60eb28);
    buffer.writeObject(userId);
  }
}

/// Account Get Saved Ringtones.
///
/// ID: `e1902288`.
class AccountGetSavedRingtones extends TlMethod<AccountSavedRingtonesBase> {
  /// Account Get Saved Ringtones constructor.
  const AccountGetSavedRingtones({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory AccountGetSavedRingtones.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = AccountGetSavedRingtones(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe1902288);
    buffer.writeLong(hash);
  }
}

/// Account Save Ringtone.
///
/// ID: `3dea5b03`.
class AccountSaveRingtone extends TlMethod<AccountSavedRingtoneBase> {
  /// Account Save Ringtone constructor.
  const AccountSaveRingtone({
    required this.id,
    required this.unsave,
  }) : super._();

  /// Deserialize.
  factory AccountSaveRingtone.deserialize(Uint8List buffer) {
    // final id = _readInputDocument(buffer);
    // final unsave = _readBool(buffer);
    // final result = AccountSaveRingtone(id: id, unsave: unsave,);

    // return result;

    throw Exception();
  }

  /// Id.
  final InputDocumentBase id;

  /// Unsave.
  final bool unsave;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3dea5b03);
    buffer.writeObject(id);
    buffer.writeBool(unsave);
  }
}

/// Account Upload Ringtone.
///
/// ID: `831a83a2`.
class AccountUploadRingtone extends TlMethod<DocumentBase> {
  /// Account Upload Ringtone constructor.
  const AccountUploadRingtone({
    required this.file,
    required this.fileName,
    required this.mimeType,
  }) : super._();

  /// Deserialize.
  factory AccountUploadRingtone.deserialize(Uint8List buffer) {
    // final file = _readInputFile(buffer);
    // final fileName = _readstring(buffer);
    // final mimeType = _readstring(buffer);
    // final result = AccountUploadRingtone(file: file, fileName: fileName, mimeType: mimeType,);

    // return result;

    throw Exception();
  }

  /// File.
  final InputFileBase file;

  /// File Name.
  final String fileName;

  /// Mime Type.
  final String mimeType;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x831a83a2);
    buffer.writeObject(file);
    buffer.writeString(fileName);
    buffer.writeString(mimeType);
  }
}

/// Bots Set Bot Broadcast Default Admin Rights.
///
/// ID: `788464e1`.
class BotsSetBotBroadcastDefaultAdminRights extends TlMethod<bool> {
  /// Bots Set Bot Broadcast Default Admin Rights constructor.
  const BotsSetBotBroadcastDefaultAdminRights({
    required this.adminRights,
  }) : super._();

  /// Deserialize.
  factory BotsSetBotBroadcastDefaultAdminRights.deserialize(Uint8List buffer) {
    // final adminRights = _readChatAdminRights(buffer);
    // final result = BotsSetBotBroadcastDefaultAdminRights(adminRights: adminRights,);

    // return result;

    throw Exception();
  }

  /// Admin Rights.
  final ChatAdminRightsBase adminRights;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x788464e1);
    buffer.writeObject(adminRights);
  }
}

/// Bots Set Bot Group Default Admin Rights.
///
/// ID: `925ec9ea`.
class BotsSetBotGroupDefaultAdminRights extends TlMethod<bool> {
  /// Bots Set Bot Group Default Admin Rights constructor.
  const BotsSetBotGroupDefaultAdminRights({
    required this.adminRights,
  }) : super._();

  /// Deserialize.
  factory BotsSetBotGroupDefaultAdminRights.deserialize(Uint8List buffer) {
    // final adminRights = _readChatAdminRights(buffer);
    // final result = BotsSetBotGroupDefaultAdminRights(adminRights: adminRights,);

    // return result;

    throw Exception();
  }

  /// Admin Rights.
  final ChatAdminRightsBase adminRights;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x925ec9ea);
    buffer.writeObject(adminRights);
  }
}

/// Phone Save Call Log.
///
/// ID: `41248786`.
class PhoneSaveCallLog extends TlMethod<bool> {
  /// Phone Save Call Log constructor.
  const PhoneSaveCallLog({
    required this.peer,
    required this.file,
  }) : super._();

  /// Deserialize.
  factory PhoneSaveCallLog.deserialize(Uint8List buffer) {
    // final peer = _readInputPhoneCall(buffer);
    // final file = _readInputFile(buffer);
    // final result = PhoneSaveCallLog(peer: peer, file: file,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPhoneCallBase peer;

  /// File.
  final InputFileBase file;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x41248786);
    buffer.writeObject(peer);
    buffer.writeObject(file);
  }
}

/// Channels Toggle Join To Send.
///
/// ID: `e4cb9580`.
class ChannelsToggleJoinToSend extends TlMethod<UpdatesBase> {
  /// Channels Toggle Join To Send constructor.
  const ChannelsToggleJoinToSend({
    required this.channel,
    required this.enabled,
  }) : super._();

  /// Deserialize.
  factory ChannelsToggleJoinToSend.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final enabled = _readBool(buffer);
    // final result = ChannelsToggleJoinToSend(channel: channel, enabled: enabled,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Enabled.
  final bool enabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe4cb9580);
    buffer.writeObject(channel);
    buffer.writeBool(enabled);
  }
}

/// Channels Toggle Join Request.
///
/// ID: `4c2985b6`.
class ChannelsToggleJoinRequest extends TlMethod<UpdatesBase> {
  /// Channels Toggle Join Request constructor.
  const ChannelsToggleJoinRequest({
    required this.channel,
    required this.enabled,
  }) : super._();

  /// Deserialize.
  factory ChannelsToggleJoinRequest.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final enabled = _readBool(buffer);
    // final result = ChannelsToggleJoinRequest(channel: channel, enabled: enabled,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Enabled.
  final bool enabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x4c2985b6);
    buffer.writeObject(channel);
    buffer.writeBool(enabled);
  }
}

/// Payments Export Invoice.
///
/// ID: `0f91b065`.
class PaymentsExportInvoice extends TlMethod<PaymentsExportedInvoiceBase> {
  /// Payments Export Invoice constructor.
  const PaymentsExportInvoice({
    required this.invoiceMedia,
  }) : super._();

  /// Deserialize.
  factory PaymentsExportInvoice.deserialize(Uint8List buffer) {
    // final invoiceMedia = _readInputMedia(buffer);
    // final result = PaymentsExportInvoice(invoiceMedia: invoiceMedia,);

    // return result;

    throw Exception();
  }

  /// Invoice Media.
  final InputMediaBase invoiceMedia;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0f91b065);
    buffer.writeObject(invoiceMedia);
  }
}

/// Messages Transcribe Audio.
///
/// ID: `269e9a49`.
class MessagesTranscribeAudio extends TlMethod<MessagesTranscribedAudioBase> {
  /// Messages Transcribe Audio constructor.
  const MessagesTranscribeAudio({
    required this.peer,
    required this.msgId,
  }) : super._();

  /// Deserialize.
  factory MessagesTranscribeAudio.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final result = MessagesTranscribeAudio(peer: peer, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x269e9a49);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
  }
}

/// Messages Rate Transcribed Audio.
///
/// ID: `7f1d072f`.
class MessagesRateTranscribedAudio extends TlMethod<bool> {
  /// Messages Rate Transcribed Audio constructor.
  const MessagesRateTranscribedAudio({
    required this.peer,
    required this.msgId,
    required this.transcriptionId,
    required this.good,
  }) : super._();

  /// Deserialize.
  factory MessagesRateTranscribedAudio.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final transcriptionId = _readlong(buffer);
    // final good = _readBool(buffer);
    // final result = MessagesRateTranscribedAudio(peer: peer, msgId: msgId, transcriptionId: transcriptionId, good: good,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Transcription Id.
  final int transcriptionId;

  /// Good.
  final bool good;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7f1d072f);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeLong(transcriptionId);
    buffer.writeBool(good);
  }
}

/// Payments Assign App Store Transaction.
///
/// ID: `80ed747d`.
class PaymentsAssignAppStoreTransaction extends TlMethod<UpdatesBase> {
  /// Payments Assign App Store Transaction constructor.
  const PaymentsAssignAppStoreTransaction({
    required this.receipt,
    required this.purpose,
  }) : super._();

  /// Deserialize.
  factory PaymentsAssignAppStoreTransaction.deserialize(Uint8List buffer) {
    // final receipt = _readbytes(buffer);
    // final purpose = _readInputStorePaymentPurpose(buffer);
    // final result = PaymentsAssignAppStoreTransaction(receipt: receipt, purpose: purpose,);

    // return result;

    throw Exception();
  }

  /// Receipt.
  final Uint8List receipt;

  /// Purpose.
  final InputStorePaymentPurposeBase purpose;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x80ed747d);
    buffer.writeBytes(receipt);
    buffer.writeObject(purpose);
  }
}

/// Payments Assign Play Market Transaction.
///
/// ID: `dffd50d3`.
class PaymentsAssignPlayMarketTransaction extends TlMethod<UpdatesBase> {
  /// Payments Assign Play Market Transaction constructor.
  const PaymentsAssignPlayMarketTransaction({
    required this.receipt,
    required this.purpose,
  }) : super._();

  /// Deserialize.
  factory PaymentsAssignPlayMarketTransaction.deserialize(Uint8List buffer) {
    // final receipt = _readDataJSON(buffer);
    // final purpose = _readInputStorePaymentPurpose(buffer);
    // final result = PaymentsAssignPlayMarketTransaction(receipt: receipt, purpose: purpose,);

    // return result;

    throw Exception();
  }

  /// Receipt.
  final DataJSONBase receipt;

  /// Purpose.
  final InputStorePaymentPurposeBase purpose;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdffd50d3);
    buffer.writeObject(receipt);
    buffer.writeObject(purpose);
  }
}

/// Payments Can Purchase Premium.
///
/// ID: `9fc19eb6`.
class PaymentsCanPurchasePremium extends TlMethod<bool> {
  /// Payments Can Purchase Premium constructor.
  const PaymentsCanPurchasePremium({
    required this.purpose,
  }) : super._();

  /// Deserialize.
  factory PaymentsCanPurchasePremium.deserialize(Uint8List buffer) {
    // final purpose = _readInputStorePaymentPurpose(buffer);
    // final result = PaymentsCanPurchasePremium(purpose: purpose,);

    // return result;

    throw Exception();
  }

  /// Purpose.
  final InputStorePaymentPurposeBase purpose;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9fc19eb6);
    buffer.writeObject(purpose);
  }
}

/// Help Get Premium Promo.
///
/// ID: `b81b93d4`.
class HelpGetPremiumPromo extends TlMethod<HelpPremiumPromoBase> {
  /// Help Get Premium Promo constructor.
  const HelpGetPremiumPromo() : super._();

  /// Deserialize.
  factory HelpGetPremiumPromo.deserialize(Uint8List buffer) {
    // final result = HelpGetPremiumPromo();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb81b93d4);
  }
}

/// Messages Get Custom Emoji Documents.
///
/// ID: `d9ab0f54`.
class MessagesGetCustomEmojiDocuments extends TlMethod<List<DocumentBase>> {
  /// Messages Get Custom Emoji Documents constructor.
  const MessagesGetCustomEmojiDocuments({
    required this.documentId,
  }) : super._();

  /// Deserialize.
  factory MessagesGetCustomEmojiDocuments.deserialize(Uint8List buffer) {
    // final documentId = _readVector<long>(buffer);
    // final result = MessagesGetCustomEmojiDocuments(documentId: documentId,);

    // return result;

    throw Exception();
  }

  /// Document Id.
  final List<int> documentId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd9ab0f54);
    buffer.writeVectorLong(documentId);
  }
}

/// Messages Get Emoji Stickers.
///
/// ID: `fbfca18f`.
class MessagesGetEmojiStickers extends TlMethod<MessagesAllStickersBase> {
  /// Messages Get Emoji Stickers constructor.
  const MessagesGetEmojiStickers({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetEmojiStickers.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = MessagesGetEmojiStickers(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfbfca18f);
    buffer.writeLong(hash);
  }
}

/// Messages Get Featured Emoji Stickers.
///
/// ID: `0ecf6736`.
class MessagesGetFeaturedEmojiStickers
    extends TlMethod<MessagesFeaturedStickersBase> {
  /// Messages Get Featured Emoji Stickers constructor.
  const MessagesGetFeaturedEmojiStickers({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetFeaturedEmojiStickers.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = MessagesGetFeaturedEmojiStickers(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0ecf6736);
    buffer.writeLong(hash);
  }
}

/// Account Update Emoji Status.
///
/// ID: `fbd3de6b`.
class AccountUpdateEmojiStatus extends TlMethod<bool> {
  /// Account Update Emoji Status constructor.
  const AccountUpdateEmojiStatus({
    required this.emojiStatus,
  }) : super._();

  /// Deserialize.
  factory AccountUpdateEmojiStatus.deserialize(Uint8List buffer) {
    // final emojiStatus = _readEmojiStatus(buffer);
    // final result = AccountUpdateEmojiStatus(emojiStatus: emojiStatus,);

    // return result;

    throw Exception();
  }

  /// Emoji Status.
  final EmojiStatusBase emojiStatus;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfbd3de6b);
    buffer.writeObject(emojiStatus);
  }
}

/// Account Get Default Emoji Statuses.
///
/// ID: `d6753386`.
class AccountGetDefaultEmojiStatuses
    extends TlMethod<AccountEmojiStatusesBase> {
  /// Account Get Default Emoji Statuses constructor.
  const AccountGetDefaultEmojiStatuses({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory AccountGetDefaultEmojiStatuses.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = AccountGetDefaultEmojiStatuses(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd6753386);
    buffer.writeLong(hash);
  }
}

/// Account Get Recent Emoji Statuses.
///
/// ID: `0f578105`.
class AccountGetRecentEmojiStatuses extends TlMethod<AccountEmojiStatusesBase> {
  /// Account Get Recent Emoji Statuses constructor.
  const AccountGetRecentEmojiStatuses({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory AccountGetRecentEmojiStatuses.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = AccountGetRecentEmojiStatuses(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0f578105);
    buffer.writeLong(hash);
  }
}

/// Account Clear Recent Emoji Statuses.
///
/// ID: `18201aae`.
class AccountClearRecentEmojiStatuses extends TlMethod<bool> {
  /// Account Clear Recent Emoji Statuses constructor.
  const AccountClearRecentEmojiStatuses() : super._();

  /// Deserialize.
  factory AccountClearRecentEmojiStatuses.deserialize(Uint8List buffer) {
    // final result = AccountClearRecentEmojiStatuses();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x18201aae);
  }
}

/// Messages Report Reaction.
///
/// ID: `3f64c076`.
class MessagesReportReaction extends TlMethod<bool> {
  /// Messages Report Reaction constructor.
  const MessagesReportReaction({
    required this.peer,
    required this.id,
    required this.reactionPeer,
  }) : super._();

  /// Deserialize.
  factory MessagesReportReaction.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final reactionPeer = _readInputPeer(buffer);
    // final result = MessagesReportReaction(peer: peer, id: id, reactionPeer: reactionPeer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// Reaction Peer.
  final InputPeerBase reactionPeer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3f64c076);
    buffer.writeObject(peer);
    buffer.writeInt(id);
    buffer.writeObject(reactionPeer);
  }
}

/// Messages Get Top Reactions.
///
/// ID: `bb8125ba`.
class MessagesGetTopReactions extends TlMethod<MessagesReactionsBase> {
  /// Messages Get Top Reactions constructor.
  const MessagesGetTopReactions({
    required this.limit,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetTopReactions.deserialize(Uint8List buffer) {
    // final limit = _readint(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesGetTopReactions(limit: limit, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Limit.
  final int limit;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbb8125ba);
    buffer.writeInt(limit);
    buffer.writeLong(hash);
  }
}

/// Messages Get Recent Reactions.
///
/// ID: `39461db2`.
class MessagesGetRecentReactions extends TlMethod<MessagesReactionsBase> {
  /// Messages Get Recent Reactions constructor.
  const MessagesGetRecentReactions({
    required this.limit,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetRecentReactions.deserialize(Uint8List buffer) {
    // final limit = _readint(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesGetRecentReactions(limit: limit, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Limit.
  final int limit;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x39461db2);
    buffer.writeInt(limit);
    buffer.writeLong(hash);
  }
}

/// Messages Clear Recent Reactions.
///
/// ID: `9dfeefb4`.
class MessagesClearRecentReactions extends TlMethod<bool> {
  /// Messages Clear Recent Reactions constructor.
  const MessagesClearRecentReactions() : super._();

  /// Deserialize.
  factory MessagesClearRecentReactions.deserialize(Uint8List buffer) {
    // final result = MessagesClearRecentReactions();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9dfeefb4);
  }
}

/// Messages Get Extended Media.
///
/// ID: `84f80814`.
class MessagesGetExtendedMedia extends TlMethod<UpdatesBase> {
  /// Messages Get Extended Media constructor.
  const MessagesGetExtendedMedia({
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesGetExtendedMedia.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final result = MessagesGetExtendedMedia(peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x84f80814);
    buffer.writeObject(peer);
    buffer.writeVectorInt(id);
  }
}

/// Auth Import Web Token Authorization.
///
/// ID: `2db873a9`.
class AuthImportWebTokenAuthorization extends TlMethod<AuthAuthorizationBase> {
  /// Auth Import Web Token Authorization constructor.
  const AuthImportWebTokenAuthorization({
    required this.apiId,
    required this.apiHash,
    required this.webAuthToken,
  }) : super._();

  /// Deserialize.
  factory AuthImportWebTokenAuthorization.deserialize(Uint8List buffer) {
    // final apiId = _readint(buffer);
    // final apiHash = _readstring(buffer);
    // final webAuthToken = _readstring(buffer);
    // final result = AuthImportWebTokenAuthorization(apiId: apiId, apiHash: apiHash, webAuthToken: webAuthToken,);

    // return result;

    throw Exception();
  }

  /// Api Id.
  final int apiId;

  /// Api Hash.
  final String apiHash;

  /// Web Auth Token.
  final String webAuthToken;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2db873a9);
    buffer.writeInt(apiId);
    buffer.writeString(apiHash);
    buffer.writeString(webAuthToken);
  }
}

/// Account Reorder Usernames.
///
/// ID: `ef500eab`.
class AccountReorderUsernames extends TlMethod<bool> {
  /// Account Reorder Usernames constructor.
  const AccountReorderUsernames({
    required this.order,
  }) : super._();

  /// Deserialize.
  factory AccountReorderUsernames.deserialize(Uint8List buffer) {
    // final order = _readVector<string>(buffer);
    // final result = AccountReorderUsernames(order: order,);

    // return result;

    throw Exception();
  }

  /// Order.
  final List<String> order;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xef500eab);
    buffer.writeVectorString(order);
  }
}

/// Account Toggle Username.
///
/// ID: `58d6b376`.
class AccountToggleUsername extends TlMethod<bool> {
  /// Account Toggle Username constructor.
  const AccountToggleUsername({
    required this.username,
    required this.active,
  }) : super._();

  /// Deserialize.
  factory AccountToggleUsername.deserialize(Uint8List buffer) {
    // final username = _readstring(buffer);
    // final active = _readBool(buffer);
    // final result = AccountToggleUsername(username: username, active: active,);

    // return result;

    throw Exception();
  }

  /// Username.
  final String username;

  /// Active.
  final bool active;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x58d6b376);
    buffer.writeString(username);
    buffer.writeBool(active);
  }
}

/// Channels Reorder Usernames.
///
/// ID: `b45ced1d`.
class ChannelsReorderUsernames extends TlMethod<bool> {
  /// Channels Reorder Usernames constructor.
  const ChannelsReorderUsernames({
    required this.channel,
    required this.order,
  }) : super._();

  /// Deserialize.
  factory ChannelsReorderUsernames.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final order = _readVector<string>(buffer);
    // final result = ChannelsReorderUsernames(channel: channel, order: order,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Order.
  final List<String> order;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb45ced1d);
    buffer.writeObject(channel);
    buffer.writeVectorString(order);
  }
}

/// Channels Toggle Username.
///
/// ID: `50f24105`.
class ChannelsToggleUsername extends TlMethod<bool> {
  /// Channels Toggle Username constructor.
  const ChannelsToggleUsername({
    required this.channel,
    required this.username,
    required this.active,
  }) : super._();

  /// Deserialize.
  factory ChannelsToggleUsername.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final username = _readstring(buffer);
    // final active = _readBool(buffer);
    // final result = ChannelsToggleUsername(channel: channel, username: username, active: active,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Username.
  final String username;

  /// Active.
  final bool active;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x50f24105);
    buffer.writeObject(channel);
    buffer.writeString(username);
    buffer.writeBool(active);
  }
}

/// Channels Deactivate All Usernames.
///
/// ID: `0a245dd3`.
class ChannelsDeactivateAllUsernames extends TlMethod<bool> {
  /// Channels Deactivate All Usernames constructor.
  const ChannelsDeactivateAllUsernames({
    required this.channel,
  }) : super._();

  /// Deserialize.
  factory ChannelsDeactivateAllUsernames.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final result = ChannelsDeactivateAllUsernames(channel: channel,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0a245dd3);
    buffer.writeObject(channel);
  }
}

/// Channels Toggle Forum.
///
/// ID: `a4298b29`.
class ChannelsToggleForum extends TlMethod<UpdatesBase> {
  /// Channels Toggle Forum constructor.
  const ChannelsToggleForum({
    required this.channel,
    required this.enabled,
  }) : super._();

  /// Deserialize.
  factory ChannelsToggleForum.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final enabled = _readBool(buffer);
    // final result = ChannelsToggleForum(channel: channel, enabled: enabled,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Enabled.
  final bool enabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa4298b29);
    buffer.writeObject(channel);
    buffer.writeBool(enabled);
  }
}

/// Channels Create Forum Topic.
///
/// ID: `f40c0224`.
class ChannelsCreateForumTopic extends TlMethod<UpdatesBase> {
  /// Channels Create Forum Topic constructor.
  const ChannelsCreateForumTopic({
    required this.channel,
    required this.title,
    this.iconColor,
    this.iconEmojiId,
    required this.randomId,
    this.sendAs,
  }) : super._();

  /// Deserialize.
  factory ChannelsCreateForumTopic.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final channel = _readInputChannel(buffer);
    // final title = _readstring(buffer);
    // final iconColor = _readflags.0?int(buffer);
    // final iconEmojiId = _readflags.3?long(buffer);
    // final randomId = _readlong(buffer);
    // final sendAs = _readflags.2?InputPeer(buffer);
    // final result = ChannelsCreateForumTopic(flags: flags, channel: channel, title: title, iconColor: iconColor, iconEmojiId: iconEmojiId, randomId: randomId, sendAs: sendAs,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: iconColor != null,
      b03: iconEmojiId != null,
      b02: sendAs != null,
    );

    return v;
  }

  /// Channel.
  final InputChannelBase channel;

  /// Title.
  final String title;

  /// Icon Color.
  final int? iconColor;

  /// Icon Emoji Id.
  final int? iconEmojiId;

  /// Random Id.
  final int randomId;

  /// Send As.
  final InputPeerBase? sendAs;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf40c0224);
    buffer.writeInt(flags);
    buffer.writeObject(channel);
    buffer.writeString(title);
    final localIconColorCopy = iconColor;
    if (localIconColorCopy != null) {
      buffer.writeInt(localIconColorCopy);
    }
    final localIconEmojiIdCopy = iconEmojiId;
    if (localIconEmojiIdCopy != null) {
      buffer.writeLong(localIconEmojiIdCopy);
    }
    buffer.writeLong(randomId);
    final localSendAsCopy = sendAs;
    if (localSendAsCopy != null) {
      buffer.writeObject(localSendAsCopy);
    }
  }
}

/// Channels Get Forum Topics.
///
/// ID: `0de560d1`.
class ChannelsGetForumTopics extends TlMethod<MessagesForumTopicsBase> {
  /// Channels Get Forum Topics constructor.
  const ChannelsGetForumTopics({
    required this.channel,
    this.q,
    required this.offsetDate,
    required this.offsetId,
    required this.offsetTopic,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory ChannelsGetForumTopics.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final channel = _readInputChannel(buffer);
    // final q = _readflags.0?string(buffer);
    // final offsetDate = _readint(buffer);
    // final offsetId = _readint(buffer);
    // final offsetTopic = _readint(buffer);
    // final limit = _readint(buffer);
    // final result = ChannelsGetForumTopics(flags: flags, channel: channel, q: q, offsetDate: offsetDate, offsetId: offsetId, offsetTopic: offsetTopic, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: q != null,
    );

    return v;
  }

  /// Channel.
  final InputChannelBase channel;

  /// Q.
  final String? q;

  /// Offset Date.
  final DateTime offsetDate;

  /// Offset Id.
  final int offsetId;

  /// Offset Topic.
  final int offsetTopic;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0de560d1);
    buffer.writeInt(flags);
    buffer.writeObject(channel);
    final localQCopy = q;
    if (localQCopy != null) {
      buffer.writeString(localQCopy);
    }
    buffer.writeDateTime(offsetDate);
    buffer.writeInt(offsetId);
    buffer.writeInt(offsetTopic);
    buffer.writeInt(limit);
  }
}

/// Channels Get Forum Topics By I D.
///
/// ID: `b0831eb9`.
class ChannelsGetForumTopicsByID extends TlMethod<MessagesForumTopicsBase> {
  /// Channels Get Forum Topics By I D constructor.
  const ChannelsGetForumTopicsByID({
    required this.channel,
    required this.topics,
  }) : super._();

  /// Deserialize.
  factory ChannelsGetForumTopicsByID.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final topics = _readVector<int>(buffer);
    // final result = ChannelsGetForumTopicsByID(channel: channel, topics: topics,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Topics.
  final List<int> topics;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb0831eb9);
    buffer.writeObject(channel);
    buffer.writeVectorInt(topics);
  }
}

/// Channels Edit Forum Topic.
///
/// ID: `f4dfa185`.
class ChannelsEditForumTopic extends TlMethod<UpdatesBase> {
  /// Channels Edit Forum Topic constructor.
  const ChannelsEditForumTopic({
    required this.channel,
    required this.topicId,
    this.title,
    this.iconEmojiId,
    this.closed,
    this.hidden,
  }) : super._();

  /// Deserialize.
  factory ChannelsEditForumTopic.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final channel = _readInputChannel(buffer);
    // final topicId = _readint(buffer);
    // final title = _readflags.0?string(buffer);
    // final iconEmojiId = _readflags.1?long(buffer);
    // final closed = _readflags.2?Bool(buffer);
    // final hidden = _readflags.3?Bool(buffer);
    // final result = ChannelsEditForumTopic(flags: flags, channel: channel, topicId: topicId, title: title, iconEmojiId: iconEmojiId, closed: closed, hidden: hidden,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: title != null,
      b01: iconEmojiId != null,
      b02: closed != null,
      b03: hidden != null,
    );

    return v;
  }

  /// Channel.
  final InputChannelBase channel;

  /// Topic Id.
  final int topicId;

  /// Title.
  final String? title;

  /// Icon Emoji Id.
  final int? iconEmojiId;

  /// Closed.
  final bool? closed;

  /// Hidden.
  final bool? hidden;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf4dfa185);
    buffer.writeInt(flags);
    buffer.writeObject(channel);
    buffer.writeInt(topicId);
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localIconEmojiIdCopy = iconEmojiId;
    if (localIconEmojiIdCopy != null) {
      buffer.writeLong(localIconEmojiIdCopy);
    }
    final localClosedCopy = closed;
    if (localClosedCopy != null) {
      buffer.writeBool(localClosedCopy);
    }
    final localHiddenCopy = hidden;
    if (localHiddenCopy != null) {
      buffer.writeBool(localHiddenCopy);
    }
  }
}

/// Channels Update Pinned Forum Topic.
///
/// ID: `6c2d9026`.
class ChannelsUpdatePinnedForumTopic extends TlMethod<UpdatesBase> {
  /// Channels Update Pinned Forum Topic constructor.
  const ChannelsUpdatePinnedForumTopic({
    required this.channel,
    required this.topicId,
    required this.pinned,
  }) : super._();

  /// Deserialize.
  factory ChannelsUpdatePinnedForumTopic.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final topicId = _readint(buffer);
    // final pinned = _readBool(buffer);
    // final result = ChannelsUpdatePinnedForumTopic(channel: channel, topicId: topicId, pinned: pinned,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Topic Id.
  final int topicId;

  /// Pinned.
  final bool pinned;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6c2d9026);
    buffer.writeObject(channel);
    buffer.writeInt(topicId);
    buffer.writeBool(pinned);
  }
}

/// Channels Delete Topic History.
///
/// ID: `34435f2d`.
class ChannelsDeleteTopicHistory extends TlMethod<MessagesAffectedHistoryBase> {
  /// Channels Delete Topic History constructor.
  const ChannelsDeleteTopicHistory({
    required this.channel,
    required this.topMsgId,
  }) : super._();

  /// Deserialize.
  factory ChannelsDeleteTopicHistory.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final topMsgId = _readint(buffer);
    // final result = ChannelsDeleteTopicHistory(channel: channel, topMsgId: topMsgId,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Top Msg Id.
  final int topMsgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x34435f2d);
    buffer.writeObject(channel);
    buffer.writeInt(topMsgId);
  }
}

/// Channels Reorder Pinned Forum Topics.
///
/// ID: `2950a18f`.
class ChannelsReorderPinnedForumTopics extends TlMethod<UpdatesBase> {
  /// Channels Reorder Pinned Forum Topics constructor.
  const ChannelsReorderPinnedForumTopics({
    required this.force,
    required this.channel,
    required this.order,
  }) : super._();

  /// Deserialize.
  factory ChannelsReorderPinnedForumTopics.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final force = _readflags.0?true(buffer);
    // final channel = _readInputChannel(buffer);
    // final order = _readVector<int>(buffer);
    // final result = ChannelsReorderPinnedForumTopics(flags: flags, force: force, channel: channel, order: order,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: force,
    );

    return v;
  }

  /// force: bit 0 of flags.0?true
  final bool force;

  /// Channel.
  final InputChannelBase channel;

  /// Order.
  final List<int> order;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2950a18f);
    buffer.writeInt(flags);
    buffer.writeObject(channel);
    buffer.writeVectorInt(order);
  }
}

/// Channels Toggle Anti Spam.
///
/// ID: `68f3e4eb`.
class ChannelsToggleAntiSpam extends TlMethod<UpdatesBase> {
  /// Channels Toggle Anti Spam constructor.
  const ChannelsToggleAntiSpam({
    required this.channel,
    required this.enabled,
  }) : super._();

  /// Deserialize.
  factory ChannelsToggleAntiSpam.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final enabled = _readBool(buffer);
    // final result = ChannelsToggleAntiSpam(channel: channel, enabled: enabled,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Enabled.
  final bool enabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x68f3e4eb);
    buffer.writeObject(channel);
    buffer.writeBool(enabled);
  }
}

/// Channels Report Anti Spam False Positive.
///
/// ID: `a850a693`.
class ChannelsReportAntiSpamFalsePositive extends TlMethod<bool> {
  /// Channels Report Anti Spam False Positive constructor.
  const ChannelsReportAntiSpamFalsePositive({
    required this.channel,
    required this.msgId,
  }) : super._();

  /// Deserialize.
  factory ChannelsReportAntiSpamFalsePositive.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final msgId = _readint(buffer);
    // final result = ChannelsReportAntiSpamFalsePositive(channel: channel, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Msg Id.
  final int msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa850a693);
    buffer.writeObject(channel);
    buffer.writeInt(msgId);
  }
}

/// Messages Set Default History T T L.
///
/// ID: `9eb51445`.
class MessagesSetDefaultHistoryTTL extends TlMethod<bool> {
  /// Messages Set Default History T T L constructor.
  const MessagesSetDefaultHistoryTTL({
    required this.period,
  }) : super._();

  /// Deserialize.
  factory MessagesSetDefaultHistoryTTL.deserialize(Uint8List buffer) {
    // final period = _readint(buffer);
    // final result = MessagesSetDefaultHistoryTTL(period: period,);

    // return result;

    throw Exception();
  }

  /// Period.
  final int period;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9eb51445);
    buffer.writeInt(period);
  }
}

/// Messages Get Default History T T L.
///
/// ID: `658b7188`.
class MessagesGetDefaultHistoryTTL extends TlMethod<DefaultHistoryTTLBase> {
  /// Messages Get Default History T T L constructor.
  const MessagesGetDefaultHistoryTTL() : super._();

  /// Deserialize.
  factory MessagesGetDefaultHistoryTTL.deserialize(Uint8List buffer) {
    // final result = MessagesGetDefaultHistoryTTL();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x658b7188);
  }
}

/// Contacts Export Contact Token.
///
/// ID: `f8654027`.
class ContactsExportContactToken extends TlMethod<ExportedContactTokenBase> {
  /// Contacts Export Contact Token constructor.
  const ContactsExportContactToken() : super._();

  /// Deserialize.
  factory ContactsExportContactToken.deserialize(Uint8List buffer) {
    // final result = ContactsExportContactToken();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf8654027);
  }
}

/// Contacts Import Contact Token.
///
/// ID: `13005788`.
class ContactsImportContactToken extends TlMethod<UserBase> {
  /// Contacts Import Contact Token constructor.
  const ContactsImportContactToken({
    required this.token,
  }) : super._();

  /// Deserialize.
  factory ContactsImportContactToken.deserialize(Uint8List buffer) {
    // final token = _readstring(buffer);
    // final result = ContactsImportContactToken(token: token,);

    // return result;

    throw Exception();
  }

  /// Token.
  final String token;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x13005788);
    buffer.writeString(token);
  }
}

/// Photos Upload Contact Profile Photo.
///
/// ID: `e14c4a71`.
class PhotosUploadContactProfilePhoto extends TlMethod<PhotosPhotoBase> {
  /// Photos Upload Contact Profile Photo constructor.
  const PhotosUploadContactProfilePhoto({
    required this.suggest,
    required this.save,
    required this.userId,
    this.file,
    this.video,
    this.videoStartTs,
    this.videoEmojiMarkup,
  }) : super._();

  /// Deserialize.
  factory PhotosUploadContactProfilePhoto.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final suggest = _readflags.3?true(buffer);
    // final save = _readflags.4?true(buffer);
    // final userId = _readInputUser(buffer);
    // final file = _readflags.0?InputFile(buffer);
    // final video = _readflags.1?InputFile(buffer);
    // final videoStartTs = _readflags.2?double(buffer);
    // final videoEmojiMarkup = _readflags.5?VideoSize(buffer);
    // final result = PhotosUploadContactProfilePhoto(flags: flags, suggest: suggest, save: save, userId: userId, file: file, video: video, videoStartTs: videoStartTs, videoEmojiMarkup: videoEmojiMarkup,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: suggest,
      b04: save,
      b00: file != null,
      b01: video != null,
      b02: videoStartTs != null,
      b05: videoEmojiMarkup != null,
    );

    return v;
  }

  /// suggest: bit 3 of flags.3?true
  final bool suggest;

  /// save: bit 4 of flags.4?true
  final bool save;

  /// User Id.
  final InputUserBase userId;

  /// File.
  final InputFileBase? file;

  /// Video.
  final InputFileBase? video;

  /// Video Start Ts.
  final double? videoStartTs;

  /// Video Emoji Markup.
  final VideoSizeBase? videoEmojiMarkup;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe14c4a71);
    buffer.writeInt(flags);
    buffer.writeObject(userId);
    final localFileCopy = file;
    if (localFileCopy != null) {
      buffer.writeObject(localFileCopy);
    }
    final localVideoCopy = video;
    if (localVideoCopy != null) {
      buffer.writeObject(localVideoCopy);
    }
    final localVideoStartTsCopy = videoStartTs;
    if (localVideoStartTsCopy != null) {
      buffer.writeDouble(localVideoStartTsCopy);
    }
    final localVideoEmojiMarkupCopy = videoEmojiMarkup;
    if (localVideoEmojiMarkupCopy != null) {
      buffer.writeObject(localVideoEmojiMarkupCopy);
    }
  }
}

/// Channels Toggle Participants Hidden.
///
/// ID: `6a6e7854`.
class ChannelsToggleParticipantsHidden extends TlMethod<UpdatesBase> {
  /// Channels Toggle Participants Hidden constructor.
  const ChannelsToggleParticipantsHidden({
    required this.channel,
    required this.enabled,
  }) : super._();

  /// Deserialize.
  factory ChannelsToggleParticipantsHidden.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final enabled = _readBool(buffer);
    // final result = ChannelsToggleParticipantsHidden(channel: channel, enabled: enabled,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Enabled.
  final bool enabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6a6e7854);
    buffer.writeObject(channel);
    buffer.writeBool(enabled);
  }
}

/// Messages Send Bot Requested Peer.
///
/// ID: `91b2d060`.
class MessagesSendBotRequestedPeer extends TlMethod<UpdatesBase> {
  /// Messages Send Bot Requested Peer constructor.
  const MessagesSendBotRequestedPeer({
    required this.peer,
    required this.msgId,
    required this.buttonId,
    required this.requestedPeers,
  }) : super._();

  /// Deserialize.
  factory MessagesSendBotRequestedPeer.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final buttonId = _readint(buffer);
    // final requestedPeers = _readVector<InputPeer>(buffer);
    // final result = MessagesSendBotRequestedPeer(peer: peer, msgId: msgId, buttonId: buttonId, requestedPeers: requestedPeers,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Button Id.
  final int buttonId;

  /// Requested Peers.
  final List<InputPeerBase> requestedPeers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x91b2d060);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
    buffer.writeInt(buttonId);
    buffer.writeVectorObject(requestedPeers);
  }
}

/// Account Get Default Profile Photo Emojis.
///
/// ID: `e2750328`.
class AccountGetDefaultProfilePhotoEmojis extends TlMethod<EmojiListBase> {
  /// Account Get Default Profile Photo Emojis constructor.
  const AccountGetDefaultProfilePhotoEmojis({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory AccountGetDefaultProfilePhotoEmojis.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = AccountGetDefaultProfilePhotoEmojis(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe2750328);
    buffer.writeLong(hash);
  }
}

/// Account Get Default Group Photo Emojis.
///
/// ID: `915860ae`.
class AccountGetDefaultGroupPhotoEmojis extends TlMethod<EmojiListBase> {
  /// Account Get Default Group Photo Emojis constructor.
  const AccountGetDefaultGroupPhotoEmojis({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory AccountGetDefaultGroupPhotoEmojis.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = AccountGetDefaultGroupPhotoEmojis(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x915860ae);
    buffer.writeLong(hash);
  }
}

/// Auth Request Firebase Sms.
///
/// ID: `89464b50`.
class AuthRequestFirebaseSms extends TlMethod<bool> {
  /// Auth Request Firebase Sms constructor.
  const AuthRequestFirebaseSms({
    required this.phoneNumber,
    required this.phoneCodeHash,
    this.safetyNetToken,
    this.iosPushSecret,
  }) : super._();

  /// Deserialize.
  factory AuthRequestFirebaseSms.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final phoneNumber = _readstring(buffer);
    // final phoneCodeHash = _readstring(buffer);
    // final safetyNetToken = _readflags.0?string(buffer);
    // final iosPushSecret = _readflags.1?string(buffer);
    // final result = AuthRequestFirebaseSms(flags: flags, phoneNumber: phoneNumber, phoneCodeHash: phoneCodeHash, safetyNetToken: safetyNetToken, iosPushSecret: iosPushSecret,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: safetyNetToken != null,
      b01: iosPushSecret != null,
    );

    return v;
  }

  /// Phone Number.
  final String phoneNumber;

  /// Phone Code Hash.
  final String phoneCodeHash;

  /// Safety Net Token.
  final String? safetyNetToken;

  /// Ios Push Secret.
  final String? iosPushSecret;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x89464b50);
    buffer.writeInt(flags);
    buffer.writeString(phoneNumber);
    buffer.writeString(phoneCodeHash);
    final localSafetyNetTokenCopy = safetyNetToken;
    if (localSafetyNetTokenCopy != null) {
      buffer.writeString(localSafetyNetTokenCopy);
    }
    final localIosPushSecretCopy = iosPushSecret;
    if (localIosPushSecretCopy != null) {
      buffer.writeString(localIosPushSecretCopy);
    }
  }
}

/// Messages Get Emoji Groups.
///
/// ID: `7488ce5b`.
class MessagesGetEmojiGroups extends TlMethod<MessagesEmojiGroupsBase> {
  /// Messages Get Emoji Groups constructor.
  const MessagesGetEmojiGroups({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetEmojiGroups.deserialize(Uint8List buffer) {
    // final hash = _readint(buffer);
    // final result = MessagesGetEmojiGroups(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7488ce5b);
    buffer.writeInt(hash);
  }
}

/// Messages Get Emoji Status Groups.
///
/// ID: `2ecd56cd`.
class MessagesGetEmojiStatusGroups extends TlMethod<MessagesEmojiGroupsBase> {
  /// Messages Get Emoji Status Groups constructor.
  const MessagesGetEmojiStatusGroups({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetEmojiStatusGroups.deserialize(Uint8List buffer) {
    // final hash = _readint(buffer);
    // final result = MessagesGetEmojiStatusGroups(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2ecd56cd);
    buffer.writeInt(hash);
  }
}

/// Messages Get Emoji Profile Photo Groups.
///
/// ID: `21a548f3`.
class MessagesGetEmojiProfilePhotoGroups
    extends TlMethod<MessagesEmojiGroupsBase> {
  /// Messages Get Emoji Profile Photo Groups constructor.
  const MessagesGetEmojiProfilePhotoGroups({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetEmojiProfilePhotoGroups.deserialize(Uint8List buffer) {
    // final hash = _readint(buffer);
    // final result = MessagesGetEmojiProfilePhotoGroups(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x21a548f3);
    buffer.writeInt(hash);
  }
}

/// Messages Search Custom Emoji.
///
/// ID: `2c11c0d7`.
class MessagesSearchCustomEmoji extends TlMethod<EmojiListBase> {
  /// Messages Search Custom Emoji constructor.
  const MessagesSearchCustomEmoji({
    required this.emoticon,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesSearchCustomEmoji.deserialize(Uint8List buffer) {
    // final emoticon = _readstring(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesSearchCustomEmoji(emoticon: emoticon, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Emoticon.
  final String emoticon;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2c11c0d7);
    buffer.writeString(emoticon);
    buffer.writeLong(hash);
  }
}

/// Messages Toggle Peer Translations.
///
/// ID: `e47cb579`.
class MessagesTogglePeerTranslations extends TlMethod<bool> {
  /// Messages Toggle Peer Translations constructor.
  const MessagesTogglePeerTranslations({
    required this.disabled,
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory MessagesTogglePeerTranslations.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final disabled = _readflags.0?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final result = MessagesTogglePeerTranslations(flags: flags, disabled: disabled, peer: peer,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: disabled,
    );

    return v;
  }

  /// disabled: bit 0 of flags.0?true
  final bool disabled;

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe47cb579);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
  }
}

/// Account Get Auto Save Settings.
///
/// ID: `adcbbcda`.
class AccountGetAutoSaveSettings extends TlMethod<AccountAutoSaveSettingsBase> {
  /// Account Get Auto Save Settings constructor.
  const AccountGetAutoSaveSettings() : super._();

  /// Deserialize.
  factory AccountGetAutoSaveSettings.deserialize(Uint8List buffer) {
    // final result = AccountGetAutoSaveSettings();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xadcbbcda);
  }
}

/// Account Save Auto Save Settings.
///
/// ID: `d69b8361`.
class AccountSaveAutoSaveSettings extends TlMethod<bool> {
  /// Account Save Auto Save Settings constructor.
  const AccountSaveAutoSaveSettings({
    required this.users,
    required this.chats,
    required this.broadcasts,
    this.peer,
    required this.settings,
  }) : super._();

  /// Deserialize.
  factory AccountSaveAutoSaveSettings.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final users = _readflags.0?true(buffer);
    // final chats = _readflags.1?true(buffer);
    // final broadcasts = _readflags.2?true(buffer);
    // final peer = _readflags.3?InputPeer(buffer);
    // final settings = _readAutoSaveSettings(buffer);
    // final result = AccountSaveAutoSaveSettings(flags: flags, users: users, chats: chats, broadcasts: broadcasts, peer: peer, settings: settings,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: users,
      b01: chats,
      b02: broadcasts,
      b03: peer != null,
    );

    return v;
  }

  /// users: bit 0 of flags.0?true
  final bool users;

  /// chats: bit 1 of flags.1?true
  final bool chats;

  /// broadcasts: bit 2 of flags.2?true
  final bool broadcasts;

  /// Peer.
  final InputPeerBase? peer;

  /// Settings.
  final AutoSaveSettingsBase settings;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd69b8361);
    buffer.writeInt(flags);
    final localPeerCopy = peer;
    if (localPeerCopy != null) {
      buffer.writeObject(localPeerCopy);
    }
    buffer.writeObject(settings);
  }
}

/// Account Delete Auto Save Exceptions.
///
/// ID: `53bc0020`.
class AccountDeleteAutoSaveExceptions extends TlMethod<bool> {
  /// Account Delete Auto Save Exceptions constructor.
  const AccountDeleteAutoSaveExceptions() : super._();

  /// Deserialize.
  factory AccountDeleteAutoSaveExceptions.deserialize(Uint8List buffer) {
    // final result = AccountDeleteAutoSaveExceptions();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x53bc0020);
  }
}

/// Stickers Change Sticker.
///
/// ID: `f5537ebc`.
class StickersChangeSticker extends TlMethod<MessagesStickerSetBase> {
  /// Stickers Change Sticker constructor.
  const StickersChangeSticker({
    required this.sticker,
    this.emoji,
    this.maskCoords,
    this.keywords,
  }) : super._();

  /// Deserialize.
  factory StickersChangeSticker.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final sticker = _readInputDocument(buffer);
    // final emoji = _readflags.0?string(buffer);
    // final maskCoords = _readflags.1?MaskCoords(buffer);
    // final keywords = _readflags.2?string(buffer);
    // final result = StickersChangeSticker(flags: flags, sticker: sticker, emoji: emoji, maskCoords: maskCoords, keywords: keywords,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: emoji != null,
      b01: maskCoords != null,
      b02: keywords != null,
    );

    return v;
  }

  /// Sticker.
  final InputDocumentBase sticker;

  /// Emoji.
  final String? emoji;

  /// Mask Coords.
  final MaskCoordsBase? maskCoords;

  /// Keywords.
  final String? keywords;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf5537ebc);
    buffer.writeInt(flags);
    buffer.writeObject(sticker);
    final localEmojiCopy = emoji;
    if (localEmojiCopy != null) {
      buffer.writeString(localEmojiCopy);
    }
    final localMaskCoordsCopy = maskCoords;
    if (localMaskCoordsCopy != null) {
      buffer.writeObject(localMaskCoordsCopy);
    }
    final localKeywordsCopy = keywords;
    if (localKeywordsCopy != null) {
      buffer.writeString(localKeywordsCopy);
    }
  }
}

/// Stickers Rename Sticker Set.
///
/// ID: `124b1c00`.
class StickersRenameStickerSet extends TlMethod<MessagesStickerSetBase> {
  /// Stickers Rename Sticker Set constructor.
  const StickersRenameStickerSet({
    required this.stickerset,
    required this.title,
  }) : super._();

  /// Deserialize.
  factory StickersRenameStickerSet.deserialize(Uint8List buffer) {
    // final stickerset = _readInputStickerSet(buffer);
    // final title = _readstring(buffer);
    // final result = StickersRenameStickerSet(stickerset: stickerset, title: title,);

    // return result;

    throw Exception();
  }

  /// Stickerset.
  final InputStickerSetBase stickerset;

  /// Title.
  final String title;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x124b1c00);
    buffer.writeObject(stickerset);
    buffer.writeString(title);
  }
}

/// Stickers Delete Sticker Set.
///
/// ID: `87704394`.
class StickersDeleteStickerSet extends TlMethod<bool> {
  /// Stickers Delete Sticker Set constructor.
  const StickersDeleteStickerSet({
    required this.stickerset,
  }) : super._();

  /// Deserialize.
  factory StickersDeleteStickerSet.deserialize(Uint8List buffer) {
    // final stickerset = _readInputStickerSet(buffer);
    // final result = StickersDeleteStickerSet(stickerset: stickerset,);

    // return result;

    throw Exception();
  }

  /// Stickerset.
  final InputStickerSetBase stickerset;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x87704394);
    buffer.writeObject(stickerset);
  }
}

/// Messages Get Bot App.
///
/// ID: `34fdc5c3`.
class MessagesGetBotApp extends TlMethod<MessagesBotAppBase> {
  /// Messages Get Bot App constructor.
  const MessagesGetBotApp({
    required this.app,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetBotApp.deserialize(Uint8List buffer) {
    // final app = _readInputBotApp(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesGetBotApp(app: app, hash: hash,);

    // return result;

    throw Exception();
  }

  /// App.
  final InputBotAppBase app;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x34fdc5c3);
    buffer.writeObject(app);
    buffer.writeLong(hash);
  }
}

/// Messages Request App Web View.
///
/// ID: `8c5a3b3c`.
class MessagesRequestAppWebView extends TlMethod<AppWebViewResultBase> {
  /// Messages Request App Web View constructor.
  const MessagesRequestAppWebView({
    required this.writeAllowed,
    required this.peer,
    required this.app,
    this.startParam,
    this.themeParams,
    required this.platform,
  }) : super._();

  /// Deserialize.
  factory MessagesRequestAppWebView.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final writeAllowed = _readflags.0?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final app = _readInputBotApp(buffer);
    // final startParam = _readflags.1?string(buffer);
    // final themeParams = _readflags.2?DataJSON(buffer);
    // final platform = _readstring(buffer);
    // final result = MessagesRequestAppWebView(flags: flags, writeAllowed: writeAllowed, peer: peer, app: app, startParam: startParam, themeParams: themeParams, platform: platform,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: writeAllowed,
      b01: startParam != null,
      b02: themeParams != null,
    );

    return v;
  }

  /// write_allowed: bit 0 of flags.0?true
  final bool writeAllowed;

  /// Peer.
  final InputPeerBase peer;

  /// App.
  final InputBotAppBase app;

  /// Start Param.
  final String? startParam;

  /// Theme Params.
  final DataJSONBase? themeParams;

  /// Platform.
  final String platform;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8c5a3b3c);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeObject(app);
    final localStartParamCopy = startParam;
    if (localStartParamCopy != null) {
      buffer.writeString(localStartParamCopy);
    }
    final localThemeParamsCopy = themeParams;
    if (localThemeParamsCopy != null) {
      buffer.writeObject(localThemeParamsCopy);
    }
    buffer.writeString(platform);
  }
}

/// Bots Set Bot Info.
///
/// ID: `10cf3123`.
class BotsSetBotInfo extends TlMethod<bool> {
  /// Bots Set Bot Info constructor.
  const BotsSetBotInfo({
    this.bot,
    required this.langCode,
    this.name,
    this.about,
    this.description,
  }) : super._();

  /// Deserialize.
  factory BotsSetBotInfo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final bot = _readflags.2?InputUser(buffer);
    // final langCode = _readstring(buffer);
    // final name = _readflags.3?string(buffer);
    // final about = _readflags.0?string(buffer);
    // final description = _readflags.1?string(buffer);
    // final result = BotsSetBotInfo(flags: flags, bot: bot, langCode: langCode, name: name, about: about, description: description,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: bot != null,
      b03: name != null,
      b00: about != null,
      b01: description != null,
    );

    return v;
  }

  /// Bot.
  final InputUserBase? bot;

  /// Lang Code.
  final String langCode;

  /// Name.
  final String? name;

  /// About.
  final String? about;

  /// Description.
  final String? description;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x10cf3123);
    buffer.writeInt(flags);
    final localBotCopy = bot;
    if (localBotCopy != null) {
      buffer.writeObject(localBotCopy);
    }
    buffer.writeString(langCode);
    final localNameCopy = name;
    if (localNameCopy != null) {
      buffer.writeString(localNameCopy);
    }
    final localAboutCopy = about;
    if (localAboutCopy != null) {
      buffer.writeString(localAboutCopy);
    }
    final localDescriptionCopy = description;
    if (localDescriptionCopy != null) {
      buffer.writeString(localDescriptionCopy);
    }
  }
}

/// Bots Get Bot Info.
///
/// ID: `dcd914fd`.
class BotsGetBotInfo extends TlMethod<BotsBotInfoBase> {
  /// Bots Get Bot Info constructor.
  const BotsGetBotInfo({
    this.bot,
    required this.langCode,
  }) : super._();

  /// Deserialize.
  factory BotsGetBotInfo.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final bot = _readflags.0?InputUser(buffer);
    // final langCode = _readstring(buffer);
    // final result = BotsGetBotInfo(flags: flags, bot: bot, langCode: langCode,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: bot != null,
    );

    return v;
  }

  /// Bot.
  final InputUserBase? bot;

  /// Lang Code.
  final String langCode;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xdcd914fd);
    buffer.writeInt(flags);
    final localBotCopy = bot;
    if (localBotCopy != null) {
      buffer.writeObject(localBotCopy);
    }
    buffer.writeString(langCode);
  }
}

/// Auth Reset Login Email.
///
/// ID: `7e960193`.
class AuthResetLoginEmail extends TlMethod<AuthSentCodeBase> {
  /// Auth Reset Login Email constructor.
  const AuthResetLoginEmail({
    required this.phoneNumber,
    required this.phoneCodeHash,
  }) : super._();

  /// Deserialize.
  factory AuthResetLoginEmail.deserialize(Uint8List buffer) {
    // final phoneNumber = _readstring(buffer);
    // final phoneCodeHash = _readstring(buffer);
    // final result = AuthResetLoginEmail(phoneNumber: phoneNumber, phoneCodeHash: phoneCodeHash,);

    // return result;

    throw Exception();
  }

  /// Phone Number.
  final String phoneNumber;

  /// Phone Code Hash.
  final String phoneCodeHash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7e960193);
    buffer.writeString(phoneNumber);
    buffer.writeString(phoneCodeHash);
  }
}

/// Chatlists Export Chatlist Invite.
///
/// ID: `8472478e`.
class ChatlistsExportChatlistInvite
    extends TlMethod<ChatlistsExportedChatlistInviteBase> {
  /// Chatlists Export Chatlist Invite constructor.
  const ChatlistsExportChatlistInvite({
    required this.chatlist,
    required this.title,
    required this.peers,
  }) : super._();

  /// Deserialize.
  factory ChatlistsExportChatlistInvite.deserialize(Uint8List buffer) {
    // final chatlist = _readInputChatlist(buffer);
    // final title = _readstring(buffer);
    // final peers = _readVector<InputPeer>(buffer);
    // final result = ChatlistsExportChatlistInvite(chatlist: chatlist, title: title, peers: peers,);

    // return result;

    throw Exception();
  }

  /// Chatlist.
  final InputChatlistBase chatlist;

  /// Title.
  final String title;

  /// Peers.
  final List<InputPeerBase> peers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8472478e);
    buffer.writeObject(chatlist);
    buffer.writeString(title);
    buffer.writeVectorObject(peers);
  }
}

/// Chatlists Delete Exported Invite.
///
/// ID: `719c5c5e`.
class ChatlistsDeleteExportedInvite extends TlMethod<bool> {
  /// Chatlists Delete Exported Invite constructor.
  const ChatlistsDeleteExportedInvite({
    required this.chatlist,
    required this.slug,
  }) : super._();

  /// Deserialize.
  factory ChatlistsDeleteExportedInvite.deserialize(Uint8List buffer) {
    // final chatlist = _readInputChatlist(buffer);
    // final slug = _readstring(buffer);
    // final result = ChatlistsDeleteExportedInvite(chatlist: chatlist, slug: slug,);

    // return result;

    throw Exception();
  }

  /// Chatlist.
  final InputChatlistBase chatlist;

  /// Slug.
  final String slug;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x719c5c5e);
    buffer.writeObject(chatlist);
    buffer.writeString(slug);
  }
}

/// Chatlists Edit Exported Invite.
///
/// ID: `653db63d`.
class ChatlistsEditExportedInvite extends TlMethod<ExportedChatlistInviteBase> {
  /// Chatlists Edit Exported Invite constructor.
  const ChatlistsEditExportedInvite({
    required this.chatlist,
    required this.slug,
    this.title,
    this.peers,
  }) : super._();

  /// Deserialize.
  factory ChatlistsEditExportedInvite.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final chatlist = _readInputChatlist(buffer);
    // final slug = _readstring(buffer);
    // final title = _readflags.1?string(buffer);
    // final peers = _readflags.2?Vector<InputPeer>(buffer);
    // final result = ChatlistsEditExportedInvite(flags: flags, chatlist: chatlist, slug: slug, title: title, peers: peers,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: title != null,
      b02: peers != null,
    );

    return v;
  }

  /// Chatlist.
  final InputChatlistBase chatlist;

  /// Slug.
  final String slug;

  /// Title.
  final String? title;

  /// Peers.
  final List<InputPeerBase>? peers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x653db63d);
    buffer.writeInt(flags);
    buffer.writeObject(chatlist);
    buffer.writeString(slug);
    final localTitleCopy = title;
    if (localTitleCopy != null) {
      buffer.writeString(localTitleCopy);
    }
    final localPeersCopy = peers;
    if (localPeersCopy != null) {
      buffer.writeVectorObject(localPeersCopy);
    }
  }
}

/// Chatlists Get Exported Invites.
///
/// ID: `ce03da83`.
class ChatlistsGetExportedInvites
    extends TlMethod<ChatlistsExportedInvitesBase> {
  /// Chatlists Get Exported Invites constructor.
  const ChatlistsGetExportedInvites({
    required this.chatlist,
  }) : super._();

  /// Deserialize.
  factory ChatlistsGetExportedInvites.deserialize(Uint8List buffer) {
    // final chatlist = _readInputChatlist(buffer);
    // final result = ChatlistsGetExportedInvites(chatlist: chatlist,);

    // return result;

    throw Exception();
  }

  /// Chatlist.
  final InputChatlistBase chatlist;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xce03da83);
    buffer.writeObject(chatlist);
  }
}

/// Chatlists Check Chatlist Invite.
///
/// ID: `41c10fff`.
class ChatlistsCheckChatlistInvite
    extends TlMethod<ChatlistsChatlistInviteBase> {
  /// Chatlists Check Chatlist Invite constructor.
  const ChatlistsCheckChatlistInvite({
    required this.slug,
  }) : super._();

  /// Deserialize.
  factory ChatlistsCheckChatlistInvite.deserialize(Uint8List buffer) {
    // final slug = _readstring(buffer);
    // final result = ChatlistsCheckChatlistInvite(slug: slug,);

    // return result;

    throw Exception();
  }

  /// Slug.
  final String slug;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x41c10fff);
    buffer.writeString(slug);
  }
}

/// Chatlists Join Chatlist Invite.
///
/// ID: `a6b1e39a`.
class ChatlistsJoinChatlistInvite extends TlMethod<UpdatesBase> {
  /// Chatlists Join Chatlist Invite constructor.
  const ChatlistsJoinChatlistInvite({
    required this.slug,
    required this.peers,
  }) : super._();

  /// Deserialize.
  factory ChatlistsJoinChatlistInvite.deserialize(Uint8List buffer) {
    // final slug = _readstring(buffer);
    // final peers = _readVector<InputPeer>(buffer);
    // final result = ChatlistsJoinChatlistInvite(slug: slug, peers: peers,);

    // return result;

    throw Exception();
  }

  /// Slug.
  final String slug;

  /// Peers.
  final List<InputPeerBase> peers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa6b1e39a);
    buffer.writeString(slug);
    buffer.writeVectorObject(peers);
  }
}

/// Chatlists Get Chatlist Updates.
///
/// ID: `89419521`.
class ChatlistsGetChatlistUpdates
    extends TlMethod<ChatlistsChatlistUpdatesBase> {
  /// Chatlists Get Chatlist Updates constructor.
  const ChatlistsGetChatlistUpdates({
    required this.chatlist,
  }) : super._();

  /// Deserialize.
  factory ChatlistsGetChatlistUpdates.deserialize(Uint8List buffer) {
    // final chatlist = _readInputChatlist(buffer);
    // final result = ChatlistsGetChatlistUpdates(chatlist: chatlist,);

    // return result;

    throw Exception();
  }

  /// Chatlist.
  final InputChatlistBase chatlist;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x89419521);
    buffer.writeObject(chatlist);
  }
}

/// Chatlists Join Chatlist Updates.
///
/// ID: `e089f8f5`.
class ChatlistsJoinChatlistUpdates extends TlMethod<UpdatesBase> {
  /// Chatlists Join Chatlist Updates constructor.
  const ChatlistsJoinChatlistUpdates({
    required this.chatlist,
    required this.peers,
  }) : super._();

  /// Deserialize.
  factory ChatlistsJoinChatlistUpdates.deserialize(Uint8List buffer) {
    // final chatlist = _readInputChatlist(buffer);
    // final peers = _readVector<InputPeer>(buffer);
    // final result = ChatlistsJoinChatlistUpdates(chatlist: chatlist, peers: peers,);

    // return result;

    throw Exception();
  }

  /// Chatlist.
  final InputChatlistBase chatlist;

  /// Peers.
  final List<InputPeerBase> peers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe089f8f5);
    buffer.writeObject(chatlist);
    buffer.writeVectorObject(peers);
  }
}

/// Chatlists Hide Chatlist Updates.
///
/// ID: `66e486fb`.
class ChatlistsHideChatlistUpdates extends TlMethod<bool> {
  /// Chatlists Hide Chatlist Updates constructor.
  const ChatlistsHideChatlistUpdates({
    required this.chatlist,
  }) : super._();

  /// Deserialize.
  factory ChatlistsHideChatlistUpdates.deserialize(Uint8List buffer) {
    // final chatlist = _readInputChatlist(buffer);
    // final result = ChatlistsHideChatlistUpdates(chatlist: chatlist,);

    // return result;

    throw Exception();
  }

  /// Chatlist.
  final InputChatlistBase chatlist;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x66e486fb);
    buffer.writeObject(chatlist);
  }
}

/// Chatlists Get Leave Chatlist Suggestions.
///
/// ID: `fdbcd714`.
class ChatlistsGetLeaveChatlistSuggestions extends TlMethod<List<PeerBase>> {
  /// Chatlists Get Leave Chatlist Suggestions constructor.
  const ChatlistsGetLeaveChatlistSuggestions({
    required this.chatlist,
  }) : super._();

  /// Deserialize.
  factory ChatlistsGetLeaveChatlistSuggestions.deserialize(Uint8List buffer) {
    // final chatlist = _readInputChatlist(buffer);
    // final result = ChatlistsGetLeaveChatlistSuggestions(chatlist: chatlist,);

    // return result;

    throw Exception();
  }

  /// Chatlist.
  final InputChatlistBase chatlist;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xfdbcd714);
    buffer.writeObject(chatlist);
  }
}

/// Chatlists Leave Chatlist.
///
/// ID: `74fae13a`.
class ChatlistsLeaveChatlist extends TlMethod<UpdatesBase> {
  /// Chatlists Leave Chatlist constructor.
  const ChatlistsLeaveChatlist({
    required this.chatlist,
    required this.peers,
  }) : super._();

  /// Deserialize.
  factory ChatlistsLeaveChatlist.deserialize(Uint8List buffer) {
    // final chatlist = _readInputChatlist(buffer);
    // final peers = _readVector<InputPeer>(buffer);
    // final result = ChatlistsLeaveChatlist(chatlist: chatlist, peers: peers,);

    // return result;

    throw Exception();
  }

  /// Chatlist.
  final InputChatlistBase chatlist;

  /// Peers.
  final List<InputPeerBase> peers;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x74fae13a);
    buffer.writeObject(chatlist);
    buffer.writeVectorObject(peers);
  }
}

/// Bots Reorder Usernames.
///
/// ID: `9709b1c2`.
class BotsReorderUsernames extends TlMethod<bool> {
  /// Bots Reorder Usernames constructor.
  const BotsReorderUsernames({
    required this.bot,
    required this.order,
  }) : super._();

  /// Deserialize.
  factory BotsReorderUsernames.deserialize(Uint8List buffer) {
    // final bot = _readInputUser(buffer);
    // final order = _readVector<string>(buffer);
    // final result = BotsReorderUsernames(bot: bot, order: order,);

    // return result;

    throw Exception();
  }

  /// Bot.
  final InputUserBase bot;

  /// Order.
  final List<String> order;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9709b1c2);
    buffer.writeObject(bot);
    buffer.writeVectorString(order);
  }
}

/// Bots Toggle Username.
///
/// ID: `053ca973`.
class BotsToggleUsername extends TlMethod<bool> {
  /// Bots Toggle Username constructor.
  const BotsToggleUsername({
    required this.bot,
    required this.username,
    required this.active,
  }) : super._();

  /// Deserialize.
  factory BotsToggleUsername.deserialize(Uint8List buffer) {
    // final bot = _readInputUser(buffer);
    // final username = _readstring(buffer);
    // final active = _readBool(buffer);
    // final result = BotsToggleUsername(bot: bot, username: username, active: active,);

    // return result;

    throw Exception();
  }

  /// Bot.
  final InputUserBase bot;

  /// Username.
  final String username;

  /// Active.
  final bool active;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x053ca973);
    buffer.writeObject(bot);
    buffer.writeString(username);
    buffer.writeBool(active);
  }
}

/// Messages Set Chat Wall Paper.
///
/// ID: `8ffacae1`.
class MessagesSetChatWallPaper extends TlMethod<UpdatesBase> {
  /// Messages Set Chat Wall Paper constructor.
  const MessagesSetChatWallPaper({
    required this.forBoth,
    required this.revert,
    required this.peer,
    this.wallpaper,
    this.settings,
    this.id,
  }) : super._();

  /// Deserialize.
  factory MessagesSetChatWallPaper.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final forBoth = _readflags.3?true(buffer);
    // final revert = _readflags.4?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final wallpaper = _readflags.0?InputWallPaper(buffer);
    // final settings = _readflags.2?WallPaperSettings(buffer);
    // final id = _readflags.1?int(buffer);
    // final result = MessagesSetChatWallPaper(flags: flags, forBoth: forBoth, revert: revert, peer: peer, wallpaper: wallpaper, settings: settings, id: id,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b03: forBoth,
      b04: revert,
      b00: wallpaper != null,
      b02: settings != null,
      b01: id != null,
    );

    return v;
  }

  /// for_both: bit 3 of flags.3?true
  final bool forBoth;

  /// revert: bit 4 of flags.4?true
  final bool revert;

  /// Peer.
  final InputPeerBase peer;

  /// Wallpaper.
  final InputWallPaperBase? wallpaper;

  /// Settings.
  final WallPaperSettingsBase? settings;

  /// Id.
  final int? id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8ffacae1);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localWallpaperCopy = wallpaper;
    if (localWallpaperCopy != null) {
      buffer.writeObject(localWallpaperCopy);
    }
    final localSettingsCopy = settings;
    if (localSettingsCopy != null) {
      buffer.writeObject(localSettingsCopy);
    }
    final localIdCopy = id;
    if (localIdCopy != null) {
      buffer.writeInt(localIdCopy);
    }
  }
}

/// Account Invalidate Sign In Codes.
///
/// ID: `ca8ae8ba`.
class AccountInvalidateSignInCodes extends TlMethod<bool> {
  /// Account Invalidate Sign In Codes constructor.
  const AccountInvalidateSignInCodes({
    required this.codes,
  }) : super._();

  /// Deserialize.
  factory AccountInvalidateSignInCodes.deserialize(Uint8List buffer) {
    // final codes = _readVector<string>(buffer);
    // final result = AccountInvalidateSignInCodes(codes: codes,);

    // return result;

    throw Exception();
  }

  /// Codes.
  final List<String> codes;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xca8ae8ba);
    buffer.writeVectorString(codes);
  }
}

/// Channels Click Sponsored Message.
///
/// ID: `18afbc93`.
class ChannelsClickSponsoredMessage extends TlMethod<bool> {
  /// Channels Click Sponsored Message constructor.
  const ChannelsClickSponsoredMessage({
    required this.channel,
    required this.randomId,
  }) : super._();

  /// Deserialize.
  factory ChannelsClickSponsoredMessage.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final randomId = _readbytes(buffer);
    // final result = ChannelsClickSponsoredMessage(channel: channel, randomId: randomId,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Random Id.
  final Uint8List randomId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x18afbc93);
    buffer.writeObject(channel);
    buffer.writeBytes(randomId);
  }
}

/// Contacts Edit Close Friends.
///
/// ID: `ba6705f0`.
class ContactsEditCloseFriends extends TlMethod<bool> {
  /// Contacts Edit Close Friends constructor.
  const ContactsEditCloseFriends({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory ContactsEditCloseFriends.deserialize(Uint8List buffer) {
    // final id = _readVector<long>(buffer);
    // final result = ContactsEditCloseFriends(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xba6705f0);
    buffer.writeVectorLong(id);
  }
}

/// Stories Can Send Story.
///
/// ID: `c7dfdfdd`.
class StoriesCanSendStory extends TlMethod<bool> {
  /// Stories Can Send Story constructor.
  const StoriesCanSendStory({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory StoriesCanSendStory.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = StoriesCanSendStory(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xc7dfdfdd);
    buffer.writeObject(peer);
  }
}

/// Stories Send Story.
///
/// ID: `e4e6694b`.
class StoriesSendStory extends TlMethod<UpdatesBase> {
  /// Stories Send Story constructor.
  const StoriesSendStory({
    required this.pinned,
    required this.noforwards,
    required this.fwdModified,
    required this.peer,
    required this.media,
    this.mediaAreas,
    this.caption,
    this.entities,
    required this.privacyRules,
    required this.randomId,
    this.period,
    this.fwdFromId,
    this.fwdFromStory,
  }) : super._();

  /// Deserialize.
  factory StoriesSendStory.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pinned = _readflags.2?true(buffer);
    // final noforwards = _readflags.4?true(buffer);
    // final fwdModified = _readflags.7?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final media = _readInputMedia(buffer);
    // final mediaAreas = _readflags.5?Vector<MediaArea>(buffer);
    // final caption = _readflags.0?string(buffer);
    // final entities = _readflags.1?Vector<MessageEntity>(buffer);
    // final privacyRules = _readVector<InputPrivacyRule>(buffer);
    // final randomId = _readlong(buffer);
    // final period = _readflags.3?int(buffer);
    // final fwdFromId = _readflags.6?InputPeer(buffer);
    // final fwdFromStory = _readflags.6?int(buffer);
    // final result = StoriesSendStory(flags: flags, pinned: pinned, noforwards: noforwards, fwdModified: fwdModified, peer: peer, media: media, mediaAreas: mediaAreas, caption: caption, entities: entities, privacyRules: privacyRules, randomId: randomId, period: period, fwdFromId: fwdFromId, fwdFromStory: fwdFromStory,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: pinned,
      b04: noforwards,
      b07: fwdModified,
      b05: mediaAreas != null,
      b00: caption != null,
      b01: entities != null,
      b03: period != null,
      b06: fwdFromId != null || fwdFromStory != null,
    );

    return v;
  }

  /// pinned: bit 2 of flags.2?true
  final bool pinned;

  /// noforwards: bit 4 of flags.4?true
  final bool noforwards;

  /// fwd_modified: bit 7 of flags.7?true
  final bool fwdModified;

  /// Peer.
  final InputPeerBase peer;

  /// Media.
  final InputMediaBase media;

  /// Media Areas.
  final List<MediaAreaBase>? mediaAreas;

  /// Caption.
  final String? caption;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Privacy Rules.
  final List<InputPrivacyRuleBase> privacyRules;

  /// Random Id.
  final int randomId;

  /// Period.
  final int? period;

  /// Fwd From Id.
  final InputPeerBase? fwdFromId;

  /// Fwd From Story.
  final int? fwdFromStory;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xe4e6694b);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeObject(media);
    final localMediaAreasCopy = mediaAreas;
    if (localMediaAreasCopy != null) {
      buffer.writeVectorObject(localMediaAreasCopy);
    }
    final localCaptionCopy = caption;
    if (localCaptionCopy != null) {
      buffer.writeString(localCaptionCopy);
    }
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    buffer.writeVectorObject(privacyRules);
    buffer.writeLong(randomId);
    final localPeriodCopy = period;
    if (localPeriodCopy != null) {
      buffer.writeInt(localPeriodCopy);
    }
    final localFwdFromIdCopy = fwdFromId;
    if (localFwdFromIdCopy != null) {
      buffer.writeObject(localFwdFromIdCopy);
    }
    final localFwdFromStoryCopy = fwdFromStory;
    if (localFwdFromStoryCopy != null) {
      buffer.writeInt(localFwdFromStoryCopy);
    }
  }
}

/// Stories Edit Story.
///
/// ID: `b583ba46`.
class StoriesEditStory extends TlMethod<UpdatesBase> {
  /// Stories Edit Story constructor.
  const StoriesEditStory({
    required this.peer,
    required this.id,
    this.media,
    this.mediaAreas,
    this.caption,
    this.entities,
    this.privacyRules,
  }) : super._();

  /// Deserialize.
  factory StoriesEditStory.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final media = _readflags.0?InputMedia(buffer);
    // final mediaAreas = _readflags.3?Vector<MediaArea>(buffer);
    // final caption = _readflags.1?string(buffer);
    // final entities = _readflags.1?Vector<MessageEntity>(buffer);
    // final privacyRules = _readflags.2?Vector<InputPrivacyRule>(buffer);
    // final result = StoriesEditStory(flags: flags, peer: peer, id: id, media: media, mediaAreas: mediaAreas, caption: caption, entities: entities, privacyRules: privacyRules,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: media != null,
      b03: mediaAreas != null,
      b01: caption != null || entities != null,
      b02: privacyRules != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// Media.
  final InputMediaBase? media;

  /// Media Areas.
  final List<MediaAreaBase>? mediaAreas;

  /// Caption.
  final String? caption;

  /// Entities.
  final List<MessageEntityBase>? entities;

  /// Privacy Rules.
  final List<InputPrivacyRuleBase>? privacyRules;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb583ba46);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(id);
    final localMediaCopy = media;
    if (localMediaCopy != null) {
      buffer.writeObject(localMediaCopy);
    }
    final localMediaAreasCopy = mediaAreas;
    if (localMediaAreasCopy != null) {
      buffer.writeVectorObject(localMediaAreasCopy);
    }
    final localCaptionCopy = caption;
    if (localCaptionCopy != null) {
      buffer.writeString(localCaptionCopy);
    }
    final localEntitiesCopy = entities;
    if (localEntitiesCopy != null) {
      buffer.writeVectorObject(localEntitiesCopy);
    }
    final localPrivacyRulesCopy = privacyRules;
    if (localPrivacyRulesCopy != null) {
      buffer.writeVectorObject(localPrivacyRulesCopy);
    }
  }
}

/// Stories Delete Stories.
///
/// ID: `ae59db5f`.
class StoriesDeleteStories extends TlMethod<List<int>> {
  /// Stories Delete Stories constructor.
  const StoriesDeleteStories({
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory StoriesDeleteStories.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final result = StoriesDeleteStories(peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xae59db5f);
    buffer.writeObject(peer);
    buffer.writeVectorInt(id);
  }
}

/// Stories Toggle Pinned.
///
/// ID: `9a75a1ef`.
class StoriesTogglePinned extends TlMethod<List<int>> {
  /// Stories Toggle Pinned constructor.
  const StoriesTogglePinned({
    required this.peer,
    required this.id,
    required this.pinned,
  }) : super._();

  /// Deserialize.
  factory StoriesTogglePinned.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final pinned = _readBool(buffer);
    // final result = StoriesTogglePinned(peer: peer, id: id, pinned: pinned,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final List<int> id;

  /// Pinned.
  final bool pinned;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9a75a1ef);
    buffer.writeObject(peer);
    buffer.writeVectorInt(id);
    buffer.writeBool(pinned);
  }
}

/// Stories Get All Stories.
///
/// ID: `eeb0d625`.
class StoriesGetAllStories extends TlMethod<StoriesAllStoriesBase> {
  /// Stories Get All Stories constructor.
  const StoriesGetAllStories({
    required this.next,
    required this.hidden,
    this.state,
  }) : super._();

  /// Deserialize.
  factory StoriesGetAllStories.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final next = _readflags.1?true(buffer);
    // final hidden = _readflags.2?true(buffer);
    // final state = _readflags.0?string(buffer);
    // final result = StoriesGetAllStories(flags: flags, next: next, hidden: hidden, state: state,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: next,
      b02: hidden,
      b00: state != null,
    );

    return v;
  }

  /// next: bit 1 of flags.1?true
  final bool next;

  /// hidden: bit 2 of flags.2?true
  final bool hidden;

  /// State.
  final String? state;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xeeb0d625);
    buffer.writeInt(flags);
    final localStateCopy = state;
    if (localStateCopy != null) {
      buffer.writeString(localStateCopy);
    }
  }
}

/// Stories Get Pinned Stories.
///
/// ID: `5821a5dc`.
class StoriesGetPinnedStories extends TlMethod<StoriesStoriesBase> {
  /// Stories Get Pinned Stories constructor.
  const StoriesGetPinnedStories({
    required this.peer,
    required this.offsetId,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory StoriesGetPinnedStories.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final offsetId = _readint(buffer);
    // final limit = _readint(buffer);
    // final result = StoriesGetPinnedStories(peer: peer, offsetId: offsetId, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Offset Id.
  final int offsetId;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5821a5dc);
    buffer.writeObject(peer);
    buffer.writeInt(offsetId);
    buffer.writeInt(limit);
  }
}

/// Stories Get Stories Archive.
///
/// ID: `b4352016`.
class StoriesGetStoriesArchive extends TlMethod<StoriesStoriesBase> {
  /// Stories Get Stories Archive constructor.
  const StoriesGetStoriesArchive({
    required this.peer,
    required this.offsetId,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory StoriesGetStoriesArchive.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final offsetId = _readint(buffer);
    // final limit = _readint(buffer);
    // final result = StoriesGetStoriesArchive(peer: peer, offsetId: offsetId, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Offset Id.
  final int offsetId;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb4352016);
    buffer.writeObject(peer);
    buffer.writeInt(offsetId);
    buffer.writeInt(limit);
  }
}

/// Stories Get Stories By I D.
///
/// ID: `5774ca74`.
class StoriesGetStoriesByID extends TlMethod<StoriesStoriesBase> {
  /// Stories Get Stories By I D constructor.
  const StoriesGetStoriesByID({
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory StoriesGetStoriesByID.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final result = StoriesGetStoriesByID(peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5774ca74);
    buffer.writeObject(peer);
    buffer.writeVectorInt(id);
  }
}

/// Stories Toggle All Stories Hidden.
///
/// ID: `7c2557c4`.
class StoriesToggleAllStoriesHidden extends TlMethod<bool> {
  /// Stories Toggle All Stories Hidden constructor.
  const StoriesToggleAllStoriesHidden({
    required this.hidden,
  }) : super._();

  /// Deserialize.
  factory StoriesToggleAllStoriesHidden.deserialize(Uint8List buffer) {
    // final hidden = _readBool(buffer);
    // final result = StoriesToggleAllStoriesHidden(hidden: hidden,);

    // return result;

    throw Exception();
  }

  /// Hidden.
  final bool hidden;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7c2557c4);
    buffer.writeBool(hidden);
  }
}

/// Stories Read Stories.
///
/// ID: `a556dac8`.
class StoriesReadStories extends TlMethod<List<int>> {
  /// Stories Read Stories constructor.
  const StoriesReadStories({
    required this.peer,
    required this.maxId,
  }) : super._();

  /// Deserialize.
  factory StoriesReadStories.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final maxId = _readint(buffer);
    // final result = StoriesReadStories(peer: peer, maxId: maxId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Max Id.
  final int maxId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa556dac8);
    buffer.writeObject(peer);
    buffer.writeInt(maxId);
  }
}

/// Stories Increment Story Views.
///
/// ID: `b2028afb`.
class StoriesIncrementStoryViews extends TlMethod<bool> {
  /// Stories Increment Story Views constructor.
  const StoriesIncrementStoryViews({
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory StoriesIncrementStoryViews.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final result = StoriesIncrementStoryViews(peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb2028afb);
    buffer.writeObject(peer);
    buffer.writeVectorInt(id);
  }
}

/// Stories Get Story Views List.
///
/// ID: `7ed23c57`.
class StoriesGetStoryViewsList extends TlMethod<StoriesStoryViewsListBase> {
  /// Stories Get Story Views List constructor.
  const StoriesGetStoryViewsList({
    required this.justContacts,
    required this.reactionsFirst,
    required this.forwardsFirst,
    required this.peer,
    this.q,
    required this.id,
    required this.offset,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory StoriesGetStoryViewsList.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final justContacts = _readflags.0?true(buffer);
    // final reactionsFirst = _readflags.2?true(buffer);
    // final forwardsFirst = _readflags.3?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final q = _readflags.1?string(buffer);
    // final id = _readint(buffer);
    // final offset = _readstring(buffer);
    // final limit = _readint(buffer);
    // final result = StoriesGetStoryViewsList(flags: flags, justContacts: justContacts, reactionsFirst: reactionsFirst, forwardsFirst: forwardsFirst, peer: peer, q: q, id: id, offset: offset, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: justContacts,
      b02: reactionsFirst,
      b03: forwardsFirst,
      b01: q != null,
    );

    return v;
  }

  /// just_contacts: bit 0 of flags.0?true
  final bool justContacts;

  /// reactions_first: bit 2 of flags.2?true
  final bool reactionsFirst;

  /// forwards_first: bit 3 of flags.3?true
  final bool forwardsFirst;

  /// Peer.
  final InputPeerBase peer;

  /// Q.
  final String? q;

  /// Id.
  final int id;

  /// Offset.
  final String offset;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7ed23c57);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    final localQCopy = q;
    if (localQCopy != null) {
      buffer.writeString(localQCopy);
    }
    buffer.writeInt(id);
    buffer.writeString(offset);
    buffer.writeInt(limit);
  }
}

/// Stories Get Stories Views.
///
/// ID: `28e16cc8`.
class StoriesGetStoriesViews extends TlMethod<StoriesStoryViewsBase> {
  /// Stories Get Stories Views constructor.
  const StoriesGetStoriesViews({
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory StoriesGetStoriesViews.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final result = StoriesGetStoriesViews(peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final List<int> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x28e16cc8);
    buffer.writeObject(peer);
    buffer.writeVectorInt(id);
  }
}

/// Stories Export Story Link.
///
/// ID: `7b8def20`.
class StoriesExportStoryLink extends TlMethod<ExportedStoryLinkBase> {
  /// Stories Export Story Link constructor.
  const StoriesExportStoryLink({
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory StoriesExportStoryLink.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final result = StoriesExportStoryLink(peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7b8def20);
    buffer.writeObject(peer);
    buffer.writeInt(id);
  }
}

/// Stories Report.
///
/// ID: `1923fa8c`.
class StoriesReport extends TlMethod<bool> {
  /// Stories Report constructor.
  const StoriesReport({
    required this.peer,
    required this.id,
    required this.reason,
    required this.message,
  }) : super._();

  /// Deserialize.
  factory StoriesReport.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readVector<int>(buffer);
    // final reason = _readReportReason(buffer);
    // final message = _readstring(buffer);
    // final result = StoriesReport(peer: peer, id: id, reason: reason, message: message,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final List<int> id;

  /// Reason.
  final ReportReasonBase reason;

  /// Message.
  final String message;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1923fa8c);
    buffer.writeObject(peer);
    buffer.writeVectorInt(id);
    buffer.writeObject(reason);
    buffer.writeString(message);
  }
}

/// Stories Activate Stealth Mode.
///
/// ID: `57bbd166`.
class StoriesActivateStealthMode extends TlMethod<UpdatesBase> {
  /// Stories Activate Stealth Mode constructor.
  const StoriesActivateStealthMode({
    required this.past,
    required this.future,
  }) : super._();

  /// Deserialize.
  factory StoriesActivateStealthMode.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final past = _readflags.0?true(buffer);
    // final future = _readflags.1?true(buffer);
    // final result = StoriesActivateStealthMode(flags: flags, past: past, future: future,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: past,
      b01: future,
    );

    return v;
  }

  /// past: bit 0 of flags.0?true
  final bool past;

  /// future: bit 1 of flags.1?true
  final bool future;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x57bbd166);
    buffer.writeInt(flags);
  }
}

/// Contacts Set Blocked.
///
/// ID: `94c65c76`.
class ContactsSetBlocked extends TlMethod<bool> {
  /// Contacts Set Blocked constructor.
  const ContactsSetBlocked({
    required this.myStoriesFrom,
    required this.id,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory ContactsSetBlocked.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final myStoriesFrom = _readflags.0?true(buffer);
    // final id = _readVector<InputPeer>(buffer);
    // final limit = _readint(buffer);
    // final result = ContactsSetBlocked(flags: flags, myStoriesFrom: myStoriesFrom, id: id, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: myStoriesFrom,
    );

    return v;
  }

  /// my_stories_from: bit 0 of flags.0?true
  final bool myStoriesFrom;

  /// Id.
  final List<InputPeerBase> id;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x94c65c76);
    buffer.writeInt(flags);
    buffer.writeVectorObject(id);
    buffer.writeInt(limit);
  }
}

/// Stories Send Reaction.
///
/// ID: `7fd736b2`.
class StoriesSendReaction extends TlMethod<UpdatesBase> {
  /// Stories Send Reaction constructor.
  const StoriesSendReaction({
    required this.addToRecent,
    required this.peer,
    required this.storyId,
    required this.reaction,
  }) : super._();

  /// Deserialize.
  factory StoriesSendReaction.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final addToRecent = _readflags.0?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final storyId = _readint(buffer);
    // final reaction = _readReaction(buffer);
    // final result = StoriesSendReaction(flags: flags, addToRecent: addToRecent, peer: peer, storyId: storyId, reaction: reaction,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: addToRecent,
    );

    return v;
  }

  /// add_to_recent: bit 0 of flags.0?true
  final bool addToRecent;

  /// Peer.
  final InputPeerBase peer;

  /// Story Id.
  final int storyId;

  /// Reaction.
  final ReactionBase reaction;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7fd736b2);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(storyId);
    buffer.writeObject(reaction);
  }
}

/// Bots Can Send Message.
///
/// ID: `1359f4e6`.
class BotsCanSendMessage extends TlMethod<bool> {
  /// Bots Can Send Message constructor.
  const BotsCanSendMessage({
    required this.bot,
  }) : super._();

  /// Deserialize.
  factory BotsCanSendMessage.deserialize(Uint8List buffer) {
    // final bot = _readInputUser(buffer);
    // final result = BotsCanSendMessage(bot: bot,);

    // return result;

    throw Exception();
  }

  /// Bot.
  final InputUserBase bot;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x1359f4e6);
    buffer.writeObject(bot);
  }
}

/// Bots Allow Send Message.
///
/// ID: `f132e3ef`.
class BotsAllowSendMessage extends TlMethod<UpdatesBase> {
  /// Bots Allow Send Message constructor.
  const BotsAllowSendMessage({
    required this.bot,
  }) : super._();

  /// Deserialize.
  factory BotsAllowSendMessage.deserialize(Uint8List buffer) {
    // final bot = _readInputUser(buffer);
    // final result = BotsAllowSendMessage(bot: bot,);

    // return result;

    throw Exception();
  }

  /// Bot.
  final InputUserBase bot;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf132e3ef);
    buffer.writeObject(bot);
  }
}

/// Bots Invoke Web View Custom Method.
///
/// ID: `087fc5e7`.
class BotsInvokeWebViewCustomMethod extends TlMethod<DataJSONBase> {
  /// Bots Invoke Web View Custom Method constructor.
  const BotsInvokeWebViewCustomMethod({
    required this.bot,
    required this.customMethod,
    required this.params,
  }) : super._();

  /// Deserialize.
  factory BotsInvokeWebViewCustomMethod.deserialize(Uint8List buffer) {
    // final bot = _readInputUser(buffer);
    // final customMethod = _readstring(buffer);
    // final params = _readDataJSON(buffer);
    // final result = BotsInvokeWebViewCustomMethod(bot: bot, customMethod: customMethod, params: params,);

    // return result;

    throw Exception();
  }

  /// Bot.
  final InputUserBase bot;

  /// Custom Method.
  final String customMethod;

  /// Params.
  final DataJSONBase params;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x087fc5e7);
    buffer.writeObject(bot);
    buffer.writeString(customMethod);
    buffer.writeObject(params);
  }
}

/// Stories Get Peer Stories.
///
/// ID: `2c4ada50`.
class StoriesGetPeerStories extends TlMethod<StoriesPeerStoriesBase> {
  /// Stories Get Peer Stories constructor.
  const StoriesGetPeerStories({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory StoriesGetPeerStories.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = StoriesGetPeerStories(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2c4ada50);
    buffer.writeObject(peer);
  }
}

/// Stories Get All Read Peer Stories.
///
/// ID: `9b5ae7f9`.
class StoriesGetAllReadPeerStories extends TlMethod<UpdatesBase> {
  /// Stories Get All Read Peer Stories constructor.
  const StoriesGetAllReadPeerStories() : super._();

  /// Deserialize.
  factory StoriesGetAllReadPeerStories.deserialize(Uint8List buffer) {
    // final result = StoriesGetAllReadPeerStories();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9b5ae7f9);
  }
}

/// Stories Get Peer Max I Ds.
///
/// ID: `535983c3`.
class StoriesGetPeerMaxIDs extends TlMethod<List<int>> {
  /// Stories Get Peer Max I Ds constructor.
  const StoriesGetPeerMaxIDs({
    required this.id,
  }) : super._();

  /// Deserialize.
  factory StoriesGetPeerMaxIDs.deserialize(Uint8List buffer) {
    // final id = _readVector<InputPeer>(buffer);
    // final result = StoriesGetPeerMaxIDs(id: id,);

    // return result;

    throw Exception();
  }

  /// Id.
  final List<InputPeerBase> id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x535983c3);
    buffer.writeVectorObject(id);
  }
}

/// Stories Get Chats To Send.
///
/// ID: `a56a8b60`.
class StoriesGetChatsToSend extends TlMethod<MessagesChatsBase> {
  /// Stories Get Chats To Send constructor.
  const StoriesGetChatsToSend() : super._();

  /// Deserialize.
  factory StoriesGetChatsToSend.deserialize(Uint8List buffer) {
    // final result = StoriesGetChatsToSend();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa56a8b60);
  }
}

/// Stories Toggle Peer Stories Hidden.
///
/// ID: `bd0415c4`.
class StoriesTogglePeerStoriesHidden extends TlMethod<bool> {
  /// Stories Toggle Peer Stories Hidden constructor.
  const StoriesTogglePeerStoriesHidden({
    required this.peer,
    required this.hidden,
  }) : super._();

  /// Deserialize.
  factory StoriesTogglePeerStoriesHidden.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final hidden = _readBool(buffer);
    // final result = StoriesTogglePeerStoriesHidden(peer: peer, hidden: hidden,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Hidden.
  final bool hidden;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xbd0415c4);
    buffer.writeObject(peer);
    buffer.writeBool(hidden);
  }
}

/// Payments Get Premium Gift Code Options.
///
/// ID: `2757ba54`.
class PaymentsGetPremiumGiftCodeOptions
    extends TlMethod<List<PremiumGiftCodeOptionBase>> {
  /// Payments Get Premium Gift Code Options constructor.
  const PaymentsGetPremiumGiftCodeOptions({
    this.boostPeer,
  }) : super._();

  /// Deserialize.
  factory PaymentsGetPremiumGiftCodeOptions.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final boostPeer = _readflags.0?InputPeer(buffer);
    // final result = PaymentsGetPremiumGiftCodeOptions(flags: flags, boostPeer: boostPeer,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: boostPeer != null,
    );

    return v;
  }

  /// Boost Peer.
  final InputPeerBase? boostPeer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x2757ba54);
    buffer.writeInt(flags);
    final localBoostPeerCopy = boostPeer;
    if (localBoostPeerCopy != null) {
      buffer.writeObject(localBoostPeerCopy);
    }
  }
}

/// Payments Check Gift Code.
///
/// ID: `8e51b4c1`.
class PaymentsCheckGiftCode extends TlMethod<PaymentsCheckedGiftCodeBase> {
  /// Payments Check Gift Code constructor.
  const PaymentsCheckGiftCode({
    required this.slug,
  }) : super._();

  /// Deserialize.
  factory PaymentsCheckGiftCode.deserialize(Uint8List buffer) {
    // final slug = _readstring(buffer);
    // final result = PaymentsCheckGiftCode(slug: slug,);

    // return result;

    throw Exception();
  }

  /// Slug.
  final String slug;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8e51b4c1);
    buffer.writeString(slug);
  }
}

/// Payments Apply Gift Code.
///
/// ID: `f6e26854`.
class PaymentsApplyGiftCode extends TlMethod<UpdatesBase> {
  /// Payments Apply Gift Code constructor.
  const PaymentsApplyGiftCode({
    required this.slug,
  }) : super._();

  /// Deserialize.
  factory PaymentsApplyGiftCode.deserialize(Uint8List buffer) {
    // final slug = _readstring(buffer);
    // final result = PaymentsApplyGiftCode(slug: slug,);

    // return result;

    throw Exception();
  }

  /// Slug.
  final String slug;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf6e26854);
    buffer.writeString(slug);
  }
}

/// Payments Get Giveaway Info.
///
/// ID: `f4239425`.
class PaymentsGetGiveawayInfo extends TlMethod<PaymentsGiveawayInfoBase> {
  /// Payments Get Giveaway Info constructor.
  const PaymentsGetGiveawayInfo({
    required this.peer,
    required this.msgId,
  }) : super._();

  /// Deserialize.
  factory PaymentsGetGiveawayInfo.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final msgId = _readint(buffer);
    // final result = PaymentsGetGiveawayInfo(peer: peer, msgId: msgId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Msg Id.
  final int msgId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf4239425);
    buffer.writeObject(peer);
    buffer.writeInt(msgId);
  }
}

/// Payments Launch Prepaid Giveaway.
///
/// ID: `5ff58f20`.
class PaymentsLaunchPrepaidGiveaway extends TlMethod<UpdatesBase> {
  /// Payments Launch Prepaid Giveaway constructor.
  const PaymentsLaunchPrepaidGiveaway({
    required this.peer,
    required this.giveawayId,
    required this.purpose,
  }) : super._();

  /// Deserialize.
  factory PaymentsLaunchPrepaidGiveaway.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final giveawayId = _readlong(buffer);
    // final purpose = _readInputStorePaymentPurpose(buffer);
    // final result = PaymentsLaunchPrepaidGiveaway(peer: peer, giveawayId: giveawayId, purpose: purpose,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Giveaway Id.
  final int giveawayId;

  /// Purpose.
  final InputStorePaymentPurposeBase purpose;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5ff58f20);
    buffer.writeObject(peer);
    buffer.writeLong(giveawayId);
    buffer.writeObject(purpose);
  }
}

/// Account Update Color.
///
/// ID: `7cefa15d`.
class AccountUpdateColor extends TlMethod<bool> {
  /// Account Update Color constructor.
  const AccountUpdateColor({
    required this.forProfile,
    this.color,
    this.backgroundEmojiId,
  }) : super._();

  /// Deserialize.
  factory AccountUpdateColor.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final forProfile = _readflags.1?true(buffer);
    // final color = _readflags.2?int(buffer);
    // final backgroundEmojiId = _readflags.0?long(buffer);
    // final result = AccountUpdateColor(flags: flags, forProfile: forProfile, color: color, backgroundEmojiId: backgroundEmojiId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: forProfile,
      b02: color != null,
      b00: backgroundEmojiId != null,
    );

    return v;
  }

  /// for_profile: bit 1 of flags.1?true
  final bool forProfile;

  /// Color.
  final int? color;

  /// Background Emoji Id.
  final int? backgroundEmojiId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7cefa15d);
    buffer.writeInt(flags);
    final localColorCopy = color;
    if (localColorCopy != null) {
      buffer.writeInt(localColorCopy);
    }
    final localBackgroundEmojiIdCopy = backgroundEmojiId;
    if (localBackgroundEmojiIdCopy != null) {
      buffer.writeLong(localBackgroundEmojiIdCopy);
    }
  }
}

/// Channels Update Color.
///
/// ID: `d8aa3671`.
class ChannelsUpdateColor extends TlMethod<UpdatesBase> {
  /// Channels Update Color constructor.
  const ChannelsUpdateColor({
    required this.forProfile,
    required this.channel,
    this.color,
    this.backgroundEmojiId,
  }) : super._();

  /// Deserialize.
  factory ChannelsUpdateColor.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final forProfile = _readflags.1?true(buffer);
    // final channel = _readInputChannel(buffer);
    // final color = _readflags.2?int(buffer);
    // final backgroundEmojiId = _readflags.0?long(buffer);
    // final result = ChannelsUpdateColor(flags: flags, forProfile: forProfile, channel: channel, color: color, backgroundEmojiId: backgroundEmojiId,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b01: forProfile,
      b02: color != null,
      b00: backgroundEmojiId != null,
    );

    return v;
  }

  /// for_profile: bit 1 of flags.1?true
  final bool forProfile;

  /// Channel.
  final InputChannelBase channel;

  /// Color.
  final int? color;

  /// Background Emoji Id.
  final int? backgroundEmojiId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd8aa3671);
    buffer.writeInt(flags);
    buffer.writeObject(channel);
    final localColorCopy = color;
    if (localColorCopy != null) {
      buffer.writeInt(localColorCopy);
    }
    final localBackgroundEmojiIdCopy = backgroundEmojiId;
    if (localBackgroundEmojiIdCopy != null) {
      buffer.writeLong(localBackgroundEmojiIdCopy);
    }
  }
}

/// Account Get Default Background Emojis.
///
/// ID: `a60ab9ce`.
class AccountGetDefaultBackgroundEmojis extends TlMethod<EmojiListBase> {
  /// Account Get Default Background Emojis constructor.
  const AccountGetDefaultBackgroundEmojis({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory AccountGetDefaultBackgroundEmojis.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = AccountGetDefaultBackgroundEmojis(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa60ab9ce);
    buffer.writeLong(hash);
  }
}

/// Premium Get Boosts List.
///
/// ID: `60f67660`.
class PremiumGetBoostsList extends TlMethod<PremiumBoostsListBase> {
  /// Premium Get Boosts List constructor.
  const PremiumGetBoostsList({
    required this.gifts,
    required this.peer,
    required this.offset,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory PremiumGetBoostsList.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final gifts = _readflags.0?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final offset = _readstring(buffer);
    // final limit = _readint(buffer);
    // final result = PremiumGetBoostsList(flags: flags, gifts: gifts, peer: peer, offset: offset, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: gifts,
    );

    return v;
  }

  /// gifts: bit 0 of flags.0?true
  final bool gifts;

  /// Peer.
  final InputPeerBase peer;

  /// Offset.
  final String offset;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x60f67660);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeString(offset);
    buffer.writeInt(limit);
  }
}

/// Premium Get My Boosts.
///
/// ID: `0be77b4a`.
class PremiumGetMyBoosts extends TlMethod<PremiumMyBoostsBase> {
  /// Premium Get My Boosts constructor.
  const PremiumGetMyBoosts() : super._();

  /// Deserialize.
  factory PremiumGetMyBoosts.deserialize(Uint8List buffer) {
    // final result = PremiumGetMyBoosts();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x0be77b4a);
  }
}

/// Premium Apply Boost.
///
/// ID: `6b7da746`.
class PremiumApplyBoost extends TlMethod<PremiumMyBoostsBase> {
  /// Premium Apply Boost constructor.
  const PremiumApplyBoost({
    this.slots,
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory PremiumApplyBoost.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final slots = _readflags.0?Vector<int>(buffer);
    // final peer = _readInputPeer(buffer);
    // final result = PremiumApplyBoost(flags: flags, slots: slots, peer: peer,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: slots != null,
    );

    return v;
  }

  /// Slots.
  final List<int>? slots;

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6b7da746);
    buffer.writeInt(flags);
    final localSlotsCopy = slots;
    if (localSlotsCopy != null) {
      buffer.writeVectorInt(localSlotsCopy);
    }
    buffer.writeObject(peer);
  }
}

/// Premium Get Boosts Status.
///
/// ID: `042f1f61`.
class PremiumGetBoostsStatus extends TlMethod<PremiumBoostsStatusBase> {
  /// Premium Get Boosts Status constructor.
  const PremiumGetBoostsStatus({
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory PremiumGetBoostsStatus.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final result = PremiumGetBoostsStatus(peer: peer,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x042f1f61);
    buffer.writeObject(peer);
  }
}

/// Premium Get User Boosts.
///
/// ID: `39854d1f`.
class PremiumGetUserBoosts extends TlMethod<PremiumBoostsListBase> {
  /// Premium Get User Boosts constructor.
  const PremiumGetUserBoosts({
    required this.peer,
    required this.userId,
  }) : super._();

  /// Deserialize.
  factory PremiumGetUserBoosts.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final userId = _readInputUser(buffer);
    // final result = PremiumGetUserBoosts(peer: peer, userId: userId,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// User Id.
  final InputUserBase userId;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x39854d1f);
    buffer.writeObject(peer);
    buffer.writeObject(userId);
  }
}

/// Channels Toggle View Forum As Messages.
///
/// ID: `9738bb15`.
class ChannelsToggleViewForumAsMessages extends TlMethod<UpdatesBase> {
  /// Channels Toggle View Forum As Messages constructor.
  const ChannelsToggleViewForumAsMessages({
    required this.channel,
    required this.enabled,
  }) : super._();

  /// Deserialize.
  factory ChannelsToggleViewForumAsMessages.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final enabled = _readBool(buffer);
    // final result = ChannelsToggleViewForumAsMessages(channel: channel, enabled: enabled,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Enabled.
  final bool enabled;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x9738bb15);
    buffer.writeObject(channel);
    buffer.writeBool(enabled);
  }
}

/// Messages Search Emoji Sticker Sets.
///
/// ID: `92b4494c`.
class MessagesSearchEmojiStickerSets
    extends TlMethod<MessagesFoundStickerSetsBase> {
  /// Messages Search Emoji Sticker Sets constructor.
  const MessagesSearchEmojiStickerSets({
    required this.excludeFeatured,
    required this.q,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesSearchEmojiStickerSets.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final excludeFeatured = _readflags.0?true(buffer);
    // final q = _readstring(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesSearchEmojiStickerSets(flags: flags, excludeFeatured: excludeFeatured, q: q, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: excludeFeatured,
    );

    return v;
  }

  /// exclude_featured: bit 0 of flags.0?true
  final bool excludeFeatured;

  /// Q.
  final String q;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x92b4494c);
    buffer.writeInt(flags);
    buffer.writeString(q);
    buffer.writeLong(hash);
  }
}

/// Channels Get Channel Recommendations.
///
/// ID: `83b70d97`.
class ChannelsGetChannelRecommendations extends TlMethod<MessagesChatsBase> {
  /// Channels Get Channel Recommendations constructor.
  const ChannelsGetChannelRecommendations({
    required this.channel,
  }) : super._();

  /// Deserialize.
  factory ChannelsGetChannelRecommendations.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final result = ChannelsGetChannelRecommendations(channel: channel,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x83b70d97);
    buffer.writeObject(channel);
  }
}

/// Stats Get Story Stats.
///
/// ID: `374fef40`.
class StatsGetStoryStats extends TlMethod<StatsStoryStatsBase> {
  /// Stats Get Story Stats constructor.
  const StatsGetStoryStats({
    required this.dark,
    required this.peer,
    required this.id,
  }) : super._();

  /// Deserialize.
  factory StatsGetStoryStats.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final dark = _readflags.0?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final result = StatsGetStoryStats(flags: flags, dark: dark, peer: peer, id: id,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: dark,
    );

    return v;
  }

  /// dark: bit 0 of flags.0?true
  final bool dark;

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x374fef40);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(id);
  }
}

/// Stats Get Story Public Forwards.
///
/// ID: `a6437ef6`.
class StatsGetStoryPublicForwards extends TlMethod<StatsPublicForwardsBase> {
  /// Stats Get Story Public Forwards constructor.
  const StatsGetStoryPublicForwards({
    required this.peer,
    required this.id,
    required this.offset,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory StatsGetStoryPublicForwards.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final offset = _readstring(buffer);
    // final limit = _readint(buffer);
    // final result = StatsGetStoryPublicForwards(peer: peer, id: id, offset: offset, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// Offset.
  final String offset;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xa6437ef6);
    buffer.writeObject(peer);
    buffer.writeInt(id);
    buffer.writeString(offset);
    buffer.writeInt(limit);
  }
}

/// Help Get Peer Colors.
///
/// ID: `da80f42f`.
class HelpGetPeerColors extends TlMethod<HelpPeerColorsBase> {
  /// Help Get Peer Colors constructor.
  const HelpGetPeerColors({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory HelpGetPeerColors.deserialize(Uint8List buffer) {
    // final hash = _readint(buffer);
    // final result = HelpGetPeerColors(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xda80f42f);
    buffer.writeInt(hash);
  }
}

/// Help Get Peer Profile Colors.
///
/// ID: `abcfa9fd`.
class HelpGetPeerProfileColors extends TlMethod<HelpPeerColorsBase> {
  /// Help Get Peer Profile Colors constructor.
  const HelpGetPeerProfileColors({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory HelpGetPeerProfileColors.deserialize(Uint8List buffer) {
    // final hash = _readint(buffer);
    // final result = HelpGetPeerProfileColors(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xabcfa9fd);
    buffer.writeInt(hash);
  }
}

/// Stories Get Story Reactions List.
///
/// ID: `b9b2881f`.
class StoriesGetStoryReactionsList
    extends TlMethod<StoriesStoryReactionsListBase> {
  /// Stories Get Story Reactions List constructor.
  const StoriesGetStoryReactionsList({
    required this.forwardsFirst,
    required this.peer,
    required this.id,
    this.reaction,
    this.offset,
    required this.limit,
  }) : super._();

  /// Deserialize.
  factory StoriesGetStoryReactionsList.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final forwardsFirst = _readflags.2?true(buffer);
    // final peer = _readInputPeer(buffer);
    // final id = _readint(buffer);
    // final reaction = _readflags.0?Reaction(buffer);
    // final offset = _readflags.1?string(buffer);
    // final limit = _readint(buffer);
    // final result = StoriesGetStoryReactionsList(flags: flags, forwardsFirst: forwardsFirst, peer: peer, id: id, reaction: reaction, offset: offset, limit: limit,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: forwardsFirst,
      b00: reaction != null,
      b01: offset != null,
    );

    return v;
  }

  /// forwards_first: bit 2 of flags.2?true
  final bool forwardsFirst;

  /// Peer.
  final InputPeerBase peer;

  /// Id.
  final int id;

  /// Reaction.
  final ReactionBase? reaction;

  /// Offset.
  final String? offset;

  /// Limit.
  final int limit;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xb9b2881f);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(id);
    final localReactionCopy = reaction;
    if (localReactionCopy != null) {
      buffer.writeObject(localReactionCopy);
    }
    final localOffsetCopy = offset;
    if (localOffsetCopy != null) {
      buffer.writeString(localOffsetCopy);
    }
    buffer.writeInt(limit);
  }
}

/// Channels Update Emoji Status.
///
/// ID: `f0d3e6a8`.
class ChannelsUpdateEmojiStatus extends TlMethod<UpdatesBase> {
  /// Channels Update Emoji Status constructor.
  const ChannelsUpdateEmojiStatus({
    required this.channel,
    required this.emojiStatus,
  }) : super._();

  /// Deserialize.
  factory ChannelsUpdateEmojiStatus.deserialize(Uint8List buffer) {
    // final channel = _readInputChannel(buffer);
    // final emojiStatus = _readEmojiStatus(buffer);
    // final result = ChannelsUpdateEmojiStatus(channel: channel, emojiStatus: emojiStatus,);

    // return result;

    throw Exception();
  }

  /// Channel.
  final InputChannelBase channel;

  /// Emoji Status.
  final EmojiStatusBase emojiStatus;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xf0d3e6a8);
    buffer.writeObject(channel);
    buffer.writeObject(emojiStatus);
  }
}

/// Account Get Channel Default Emoji Statuses.
///
/// ID: `7727a7d5`.
class AccountGetChannelDefaultEmojiStatuses
    extends TlMethod<AccountEmojiStatusesBase> {
  /// Account Get Channel Default Emoji Statuses constructor.
  const AccountGetChannelDefaultEmojiStatuses({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory AccountGetChannelDefaultEmojiStatuses.deserialize(Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = AccountGetChannelDefaultEmojiStatuses(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x7727a7d5);
    buffer.writeLong(hash);
  }
}

/// Account Get Channel Restricted Status Emojis.
///
/// ID: `35a9e0d5`.
class AccountGetChannelRestrictedStatusEmojis extends TlMethod<EmojiListBase> {
  /// Account Get Channel Restricted Status Emojis constructor.
  const AccountGetChannelRestrictedStatusEmojis({
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory AccountGetChannelRestrictedStatusEmojis.deserialize(
      Uint8List buffer) {
    // final hash = _readlong(buffer);
    // final result = AccountGetChannelRestrictedStatusEmojis(hash: hash,);

    // return result;

    throw Exception();
  }

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x35a9e0d5);
    buffer.writeLong(hash);
  }
}

/// Messages Get Saved Dialogs.
///
/// ID: `5381d21a`.
class MessagesGetSavedDialogs extends TlMethod<MessagesSavedDialogsBase> {
  /// Messages Get Saved Dialogs constructor.
  const MessagesGetSavedDialogs({
    required this.excludePinned,
    required this.offsetDate,
    required this.offsetId,
    required this.offsetPeer,
    required this.limit,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetSavedDialogs.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final excludePinned = _readflags.0?true(buffer);
    // final offsetDate = _readint(buffer);
    // final offsetId = _readint(buffer);
    // final offsetPeer = _readInputPeer(buffer);
    // final limit = _readint(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesGetSavedDialogs(flags: flags, excludePinned: excludePinned, offsetDate: offsetDate, offsetId: offsetId, offsetPeer: offsetPeer, limit: limit, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: excludePinned,
    );

    return v;
  }

  /// exclude_pinned: bit 0 of flags.0?true
  final bool excludePinned;

  /// Offset Date.
  final DateTime offsetDate;

  /// Offset Id.
  final int offsetId;

  /// Offset Peer.
  final InputPeerBase offsetPeer;

  /// Limit.
  final int limit;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x5381d21a);
    buffer.writeInt(flags);
    buffer.writeDateTime(offsetDate);
    buffer.writeInt(offsetId);
    buffer.writeObject(offsetPeer);
    buffer.writeInt(limit);
    buffer.writeLong(hash);
  }
}

/// Messages Get Saved History.
///
/// ID: `3d9a414d`.
class MessagesGetSavedHistory extends TlMethod<MessagesMessagesBase> {
  /// Messages Get Saved History constructor.
  const MessagesGetSavedHistory({
    required this.peer,
    required this.offsetId,
    required this.offsetDate,
    required this.addOffset,
    required this.limit,
    required this.maxId,
    required this.minId,
    required this.hash,
  }) : super._();

  /// Deserialize.
  factory MessagesGetSavedHistory.deserialize(Uint8List buffer) {
    // final peer = _readInputPeer(buffer);
    // final offsetId = _readint(buffer);
    // final offsetDate = _readint(buffer);
    // final addOffset = _readint(buffer);
    // final limit = _readint(buffer);
    // final maxId = _readint(buffer);
    // final minId = _readint(buffer);
    // final hash = _readlong(buffer);
    // final result = MessagesGetSavedHistory(peer: peer, offsetId: offsetId, offsetDate: offsetDate, addOffset: addOffset, limit: limit, maxId: maxId, minId: minId, hash: hash,);

    // return result;

    throw Exception();
  }

  /// Peer.
  final InputPeerBase peer;

  /// Offset Id.
  final int offsetId;

  /// Offset Date.
  final DateTime offsetDate;

  /// Add Offset.
  final int addOffset;

  /// Limit.
  final int limit;

  /// Max Id.
  final int maxId;

  /// Min Id.
  final int minId;

  /// Hash.
  final int hash;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x3d9a414d);
    buffer.writeObject(peer);
    buffer.writeInt(offsetId);
    buffer.writeDateTime(offsetDate);
    buffer.writeInt(addOffset);
    buffer.writeInt(limit);
    buffer.writeInt(maxId);
    buffer.writeInt(minId);
    buffer.writeLong(hash);
  }
}

/// Messages Delete Saved History.
///
/// ID: `6e98102b`.
class MessagesDeleteSavedHistory extends TlMethod<MessagesAffectedHistoryBase> {
  /// Messages Delete Saved History constructor.
  const MessagesDeleteSavedHistory({
    required this.peer,
    required this.maxId,
    this.minDate,
    this.maxDate,
  }) : super._();

  /// Deserialize.
  factory MessagesDeleteSavedHistory.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final peer = _readInputPeer(buffer);
    // final maxId = _readint(buffer);
    // final minDate = _readflags.2?int(buffer);
    // final maxDate = _readflags.3?int(buffer);
    // final result = MessagesDeleteSavedHistory(flags: flags, peer: peer, maxId: maxId, minDate: minDate, maxDate: maxDate,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b02: minDate != null,
      b03: maxDate != null,
    );

    return v;
  }

  /// Peer.
  final InputPeerBase peer;

  /// Max Id.
  final int maxId;

  /// Min Date.
  final DateTime? minDate;

  /// Max Date.
  final DateTime? maxDate;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x6e98102b);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
    buffer.writeInt(maxId);
    final localMinDateCopy = minDate;
    if (localMinDateCopy != null) {
      buffer.writeDateTime(localMinDateCopy);
    }
    final localMaxDateCopy = maxDate;
    if (localMaxDateCopy != null) {
      buffer.writeDateTime(localMaxDateCopy);
    }
  }
}

/// Messages Get Pinned Saved Dialogs.
///
/// ID: `d63d94e0`.
class MessagesGetPinnedSavedDialogs extends TlMethod<MessagesSavedDialogsBase> {
  /// Messages Get Pinned Saved Dialogs constructor.
  const MessagesGetPinnedSavedDialogs() : super._();

  /// Deserialize.
  factory MessagesGetPinnedSavedDialogs.deserialize(Uint8List buffer) {
    // final result = MessagesGetPinnedSavedDialogs();

    // return result;

    throw Exception();
  }

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xd63d94e0);
  }
}

/// Messages Toggle Saved Dialog Pin.
///
/// ID: `ac81bbde`.
class MessagesToggleSavedDialogPin extends TlMethod<bool> {
  /// Messages Toggle Saved Dialog Pin constructor.
  const MessagesToggleSavedDialogPin({
    required this.pinned,
    required this.peer,
  }) : super._();

  /// Deserialize.
  factory MessagesToggleSavedDialogPin.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final pinned = _readflags.0?true(buffer);
    // final peer = _readInputDialogPeer(buffer);
    // final result = MessagesToggleSavedDialogPin(flags: flags, pinned: pinned, peer: peer,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: pinned,
    );

    return v;
  }

  /// pinned: bit 0 of flags.0?true
  final bool pinned;

  /// Peer.
  final InputDialogPeerBase peer;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0xac81bbde);
    buffer.writeInt(flags);
    buffer.writeObject(peer);
  }
}

/// Messages Reorder Pinned Saved Dialogs.
///
/// ID: `8b716587`.
class MessagesReorderPinnedSavedDialogs extends TlMethod<bool> {
  /// Messages Reorder Pinned Saved Dialogs constructor.
  const MessagesReorderPinnedSavedDialogs({
    required this.force,
    required this.order,
  }) : super._();

  /// Deserialize.
  factory MessagesReorderPinnedSavedDialogs.deserialize(Uint8List buffer) {
    // final flags = _read#(buffer);
    // final force = _readflags.0?true(buffer);
    // final order = _readVector<InputDialogPeer>(buffer);
    // final result = MessagesReorderPinnedSavedDialogs(flags: flags, force: force, order: order,);

    // return result;

    throw Exception();
  }

  /// Flags.
  int get flags {
    final v = _flag(
      b00: force,
    );

    return v;
  }

  /// force: bit 0 of flags.0?true
  final bool force;

  /// Order.
  final List<InputDialogPeerBase> order;

  /// Serialize.
  @override
  void serialize(List<int> buffer) {
    buffer.writeInt(0x8b716587);
    buffer.writeInt(flags);
    buffer.writeVectorObject(order);
  }
}
